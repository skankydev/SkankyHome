// @__NO_SIDE_EFFECTS__
function makeMap(str) {
  const map = /* @__PURE__ */ Object.create(null);
  for (const key of str.split(",")) map[key] = 1;
  return (val) => val in map;
}
const EMPTY_OBJ = {};
const EMPTY_ARR = [];
const NOOP = () => {
};
const NO = () => false;
const isOn = (key) => key.charCodeAt(0) === 111 && key.charCodeAt(1) === 110 && // uppercase letter
(key.charCodeAt(2) > 122 || key.charCodeAt(2) < 97);
const isModelListener = (key) => key.startsWith("onUpdate:");
const extend = Object.assign;
const remove = (arr, el2) => {
  const i = arr.indexOf(el2);
  if (i > -1) {
    arr.splice(i, 1);
  }
};
const hasOwnProperty$1 = Object.prototype.hasOwnProperty;
const hasOwn = (val, key) => hasOwnProperty$1.call(val, key);
const isArray = Array.isArray;
const isMap = (val) => toTypeString(val) === "[object Map]";
const isSet = (val) => toTypeString(val) === "[object Set]";
const isDate = (val) => toTypeString(val) === "[object Date]";
const isRegExp = (val) => toTypeString(val) === "[object RegExp]";
const isFunction = (val) => typeof val === "function";
const isString = (val) => typeof val === "string";
const isSymbol = (val) => typeof val === "symbol";
const isObject = (val) => val !== null && typeof val === "object";
const isPromise = (val) => {
  return (isObject(val) || isFunction(val)) && isFunction(val.then) && isFunction(val.catch);
};
const objectToString = Object.prototype.toString;
const toTypeString = (value) => objectToString.call(value);
const toRawType = (value) => {
  return toTypeString(value).slice(8, -1);
};
const isPlainObject = (val) => toTypeString(val) === "[object Object]";
const isIntegerKey = (key) => isString(key) && key !== "NaN" && key[0] !== "-" && "" + parseInt(key, 10) === key;
const isReservedProp = /* @__PURE__ */ makeMap(
  // the leading comma is intentional so empty string "" is also included
  ",key,ref,ref_for,ref_key,onVnodeBeforeMount,onVnodeMounted,onVnodeBeforeUpdate,onVnodeUpdated,onVnodeBeforeUnmount,onVnodeUnmounted"
);
const isBuiltInDirective = /* @__PURE__ */ makeMap(
  "bind,cloak,else-if,else,for,html,if,model,on,once,pre,show,slot,text,memo"
);
const cacheStringFunction = (fn) => {
  const cache = /* @__PURE__ */ Object.create(null);
  return ((str) => {
    const hit = cache[str];
    return hit || (cache[str] = fn(str));
  });
};
const camelizeRE = /-\w/g;
const camelize = cacheStringFunction(
  (str) => {
    return str.replace(camelizeRE, (c) => c.slice(1).toUpperCase());
  }
);
const hyphenateRE = /\B([A-Z])/g;
const hyphenate = cacheStringFunction(
  (str) => str.replace(hyphenateRE, "-$1").toLowerCase()
);
const capitalize = cacheStringFunction((str) => {
  return str.charAt(0).toUpperCase() + str.slice(1);
});
const toHandlerKey = cacheStringFunction(
  (str) => {
    const s = str ? `on${capitalize(str)}` : ``;
    return s;
  }
);
const hasChanged = (value, oldValue) => !Object.is(value, oldValue);
const invokeArrayFns = (fns, ...arg) => {
  for (let i = 0; i < fns.length; i++) {
    fns[i](...arg);
  }
};
const def = (obj, key, value, writable = false) => {
  Object.defineProperty(obj, key, {
    configurable: true,
    enumerable: false,
    writable,
    value
  });
};
const looseToNumber = (val) => {
  const n = parseFloat(val);
  return isNaN(n) ? val : n;
};
const toNumber = (val) => {
  const n = isString(val) ? Number(val) : NaN;
  return isNaN(n) ? val : n;
};
let _globalThis;
const getGlobalThis = () => {
  return _globalThis || (_globalThis = typeof globalThis !== "undefined" ? globalThis : typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : typeof global !== "undefined" ? global : {});
};
function genCacheKey(source, options) {
  return source + JSON.stringify(
    options,
    (_, val) => typeof val === "function" ? val.toString() : val
  );
}
const GLOBALS_ALLOWED = "Infinity,undefined,NaN,isFinite,isNaN,parseFloat,parseInt,decodeURI,decodeURIComponent,encodeURI,encodeURIComponent,Math,Number,Date,Array,Object,Boolean,String,RegExp,Map,Set,JSON,Intl,BigInt,console,Error,Symbol";
const isGloballyAllowed = /* @__PURE__ */ makeMap(GLOBALS_ALLOWED);
function normalizeStyle(value) {
  if (isArray(value)) {
    const res = {};
    for (let i = 0; i < value.length; i++) {
      const item = value[i];
      const normalized = isString(item) ? parseStringStyle(item) : normalizeStyle(item);
      if (normalized) {
        for (const key in normalized) {
          res[key] = normalized[key];
        }
      }
    }
    return res;
  } else if (isString(value) || isObject(value)) {
    return value;
  }
}
const listDelimiterRE = /;(?![^(]*\))/g;
const propertyDelimiterRE = /:([^]+)/;
const styleCommentRE = /\/\*[^]*?\*\//g;
function parseStringStyle(cssText) {
  const ret = {};
  cssText.replace(styleCommentRE, "").split(listDelimiterRE).forEach((item) => {
    if (item) {
      const tmp = item.split(propertyDelimiterRE);
      tmp.length > 1 && (ret[tmp[0].trim()] = tmp[1].trim());
    }
  });
  return ret;
}
function stringifyStyle(styles) {
  if (!styles) return "";
  if (isString(styles)) return styles;
  let ret = "";
  for (const key in styles) {
    const value = styles[key];
    if (isString(value) || typeof value === "number") {
      const normalizedKey = key.startsWith(`--`) ? key : hyphenate(key);
      ret += `${normalizedKey}:${value};`;
    }
  }
  return ret;
}
function normalizeClass(value) {
  let res = "";
  if (isString(value)) {
    res = value;
  } else if (isArray(value)) {
    for (let i = 0; i < value.length; i++) {
      const normalized = normalizeClass(value[i]);
      if (normalized) {
        res += normalized + " ";
      }
    }
  } else if (isObject(value)) {
    for (const name2 in value) {
      if (value[name2]) {
        res += name2 + " ";
      }
    }
  }
  return res.trim();
}
function normalizeProps(props) {
  if (!props) return null;
  let { class: klass, style } = props;
  if (klass && !isString(klass)) {
    props.class = normalizeClass(klass);
  }
  if (style) {
    props.style = normalizeStyle(style);
  }
  return props;
}
const HTML_TAGS = "html,body,base,head,link,meta,style,title,address,article,aside,footer,header,hgroup,h1,h2,h3,h4,h5,h6,nav,section,div,dd,dl,dt,figcaption,figure,picture,hr,img,li,main,ol,p,pre,ul,a,b,abbr,bdi,bdo,br,cite,code,data,dfn,em,i,kbd,mark,q,rp,rt,ruby,s,samp,small,span,strong,sub,sup,time,u,var,wbr,area,audio,map,track,video,embed,object,param,source,canvas,script,noscript,del,ins,caption,col,colgroup,table,thead,tbody,td,th,tr,button,datalist,fieldset,form,input,label,legend,meter,optgroup,option,output,progress,select,textarea,details,dialog,menu,summary,template,blockquote,iframe,tfoot";
const SVG_TAGS = "svg,animate,animateMotion,animateTransform,circle,clipPath,color-profile,defs,desc,discard,ellipse,feBlend,feColorMatrix,feComponentTransfer,feComposite,feConvolveMatrix,feDiffuseLighting,feDisplacementMap,feDistantLight,feDropShadow,feFlood,feFuncA,feFuncB,feFuncG,feFuncR,feGaussianBlur,feImage,feMerge,feMergeNode,feMorphology,feOffset,fePointLight,feSpecularLighting,feSpotLight,feTile,feTurbulence,filter,foreignObject,g,hatch,hatchpath,image,line,linearGradient,marker,mask,mesh,meshgradient,meshpatch,meshrow,metadata,mpath,path,pattern,polygon,polyline,radialGradient,rect,set,solidcolor,stop,switch,symbol,text,textPath,title,tspan,unknown,use,view";
const MATH_TAGS = "annotation,annotation-xml,maction,maligngroup,malignmark,math,menclose,merror,mfenced,mfrac,mfraction,mglyph,mi,mlabeledtr,mlongdiv,mmultiscripts,mn,mo,mover,mpadded,mphantom,mprescripts,mroot,mrow,ms,mscarries,mscarry,msgroup,msline,mspace,msqrt,msrow,mstack,mstyle,msub,msubsup,msup,mtable,mtd,mtext,mtr,munder,munderover,none,semantics";
const VOID_TAGS = "area,base,br,col,embed,hr,img,input,link,meta,param,source,track,wbr";
const isHTMLTag = /* @__PURE__ */ makeMap(HTML_TAGS);
const isSVGTag = /* @__PURE__ */ makeMap(SVG_TAGS);
const isMathMLTag = /* @__PURE__ */ makeMap(MATH_TAGS);
const isVoidTag = /* @__PURE__ */ makeMap(VOID_TAGS);
const specialBooleanAttrs = `itemscope,allowfullscreen,formnovalidate,ismap,nomodule,novalidate,readonly`;
const isSpecialBooleanAttr = /* @__PURE__ */ makeMap(specialBooleanAttrs);
const isBooleanAttr = /* @__PURE__ */ makeMap(
  specialBooleanAttrs + `,async,autofocus,autoplay,controls,default,defer,disabled,hidden,inert,loop,open,required,reversed,scoped,seamless,checked,muted,multiple,selected`
);
function includeBooleanAttr(value) {
  return !!value || value === "";
}
const isKnownHtmlAttr = /* @__PURE__ */ makeMap(
  `accept,accept-charset,accesskey,action,align,allow,alt,async,autocapitalize,autocomplete,autofocus,autoplay,background,bgcolor,border,buffered,capture,challenge,charset,checked,cite,class,code,codebase,color,cols,colspan,content,contenteditable,contextmenu,controls,coords,crossorigin,csp,data,datetime,decoding,default,defer,dir,dirname,disabled,download,draggable,dropzone,enctype,enterkeyhint,for,form,formaction,formenctype,formmethod,formnovalidate,formtarget,headers,height,hidden,high,href,hreflang,http-equiv,icon,id,importance,inert,integrity,ismap,itemprop,keytype,kind,label,lang,language,loading,list,loop,low,manifest,max,maxlength,minlength,media,min,multiple,muted,name,novalidate,open,optimum,pattern,ping,placeholder,poster,preload,radiogroup,readonly,referrerpolicy,rel,required,reversed,rows,rowspan,sandbox,scope,scoped,selected,shape,size,sizes,slot,span,spellcheck,src,srcdoc,srclang,srcset,start,step,style,summary,tabindex,target,title,translate,type,usemap,value,width,wrap`
);
const isKnownSvgAttr = /* @__PURE__ */ makeMap(
  `xmlns,accent-height,accumulate,additive,alignment-baseline,alphabetic,amplitude,arabic-form,ascent,attributeName,attributeType,azimuth,baseFrequency,baseline-shift,baseProfile,bbox,begin,bias,by,calcMode,cap-height,class,clip,clipPathUnits,clip-path,clip-rule,color,color-interpolation,color-interpolation-filters,color-profile,color-rendering,contentScriptType,contentStyleType,crossorigin,cursor,cx,cy,d,decelerate,descent,diffuseConstant,direction,display,divisor,dominant-baseline,dur,dx,dy,edgeMode,elevation,enable-background,end,exponent,fill,fill-opacity,fill-rule,filter,filterRes,filterUnits,flood-color,flood-opacity,font-family,font-size,font-size-adjust,font-stretch,font-style,font-variant,font-weight,format,from,fr,fx,fy,g1,g2,glyph-name,glyph-orientation-horizontal,glyph-orientation-vertical,glyphRef,gradientTransform,gradientUnits,hanging,height,href,hreflang,horiz-adv-x,horiz-origin-x,id,ideographic,image-rendering,in,in2,intercept,k,k1,k2,k3,k4,kernelMatrix,kernelUnitLength,kerning,keyPoints,keySplines,keyTimes,lang,lengthAdjust,letter-spacing,lighting-color,limitingConeAngle,local,marker-end,marker-mid,marker-start,markerHeight,markerUnits,markerWidth,mask,maskContentUnits,maskUnits,mathematical,max,media,method,min,mode,name,numOctaves,offset,opacity,operator,order,orient,orientation,origin,overflow,overline-position,overline-thickness,panose-1,paint-order,path,pathLength,patternContentUnits,patternTransform,patternUnits,ping,pointer-events,points,pointsAtX,pointsAtY,pointsAtZ,preserveAlpha,preserveAspectRatio,primitiveUnits,r,radius,referrerPolicy,refX,refY,rel,rendering-intent,repeatCount,repeatDur,requiredExtensions,requiredFeatures,restart,result,rotate,rx,ry,scale,seed,shape-rendering,slope,spacing,specularConstant,specularExponent,speed,spreadMethod,startOffset,stdDeviation,stemh,stemv,stitchTiles,stop-color,stop-opacity,strikethrough-position,strikethrough-thickness,string,stroke,stroke-dasharray,stroke-dashoffset,stroke-linecap,stroke-linejoin,stroke-miterlimit,stroke-opacity,stroke-width,style,surfaceScale,systemLanguage,tabindex,tableValues,target,targetX,targetY,text-anchor,text-decoration,text-rendering,textLength,to,transform,transform-origin,type,u1,u2,underline-position,underline-thickness,unicode,unicode-bidi,unicode-range,units-per-em,v-alphabetic,v-hanging,v-ideographic,v-mathematical,values,vector-effect,version,vert-adv-y,vert-origin-x,vert-origin-y,viewBox,viewTarget,visibility,width,widths,word-spacing,writing-mode,x,x-height,x1,x2,xChannelSelector,xlink:actuate,xlink:arcrole,xlink:href,xlink:role,xlink:show,xlink:title,xlink:type,xmlns:xlink,xml:base,xml:lang,xml:space,y,y1,y2,yChannelSelector,z,zoomAndPan`
);
function isRenderableAttrValue(value) {
  if (value == null) {
    return false;
  }
  const type = typeof value;
  return type === "string" || type === "number" || type === "boolean";
}
const cssVarNameEscapeSymbolsRE = /[ !"#$%&'()*+,./:;<=>?@[\\\]^`{|}~]/g;
function getEscapedCssVarName(key, doubleEscape) {
  return key.replace(
    cssVarNameEscapeSymbolsRE,
    (s) => `\\${s}`
  );
}
function looseCompareArrays(a, b) {
  if (a.length !== b.length) return false;
  let equal = true;
  for (let i = 0; equal && i < a.length; i++) {
    equal = looseEqual(a[i], b[i]);
  }
  return equal;
}
function looseEqual(a, b) {
  if (a === b) return true;
  let aValidType = isDate(a);
  let bValidType = isDate(b);
  if (aValidType || bValidType) {
    return aValidType && bValidType ? a.getTime() === b.getTime() : false;
  }
  aValidType = isSymbol(a);
  bValidType = isSymbol(b);
  if (aValidType || bValidType) {
    return a === b;
  }
  aValidType = isArray(a);
  bValidType = isArray(b);
  if (aValidType || bValidType) {
    return aValidType && bValidType ? looseCompareArrays(a, b) : false;
  }
  aValidType = isObject(a);
  bValidType = isObject(b);
  if (aValidType || bValidType) {
    if (!aValidType || !bValidType) {
      return false;
    }
    const aKeysCount = Object.keys(a).length;
    const bKeysCount = Object.keys(b).length;
    if (aKeysCount !== bKeysCount) {
      return false;
    }
    for (const key in a) {
      const aHasKey = a.hasOwnProperty(key);
      const bHasKey = b.hasOwnProperty(key);
      if (aHasKey && !bHasKey || !aHasKey && bHasKey || !looseEqual(a[key], b[key])) {
        return false;
      }
    }
  }
  return String(a) === String(b);
}
function looseIndexOf(arr, val) {
  return arr.findIndex((item) => looseEqual(item, val));
}
const isRef$1 = (val) => {
  return !!(val && val["__v_isRef"] === true);
};
const toDisplayString = (val) => {
  return isString(val) ? val : val == null ? "" : isArray(val) || isObject(val) && (val.toString === objectToString || !isFunction(val.toString)) ? isRef$1(val) ? toDisplayString(val.value) : JSON.stringify(val, replacer, 2) : String(val);
};
const replacer = (_key, val) => {
  if (isRef$1(val)) {
    return replacer(_key, val.value);
  } else if (isMap(val)) {
    return {
      [`Map(${val.size})`]: [...val.entries()].reduce(
        (entries, [key, val2], i) => {
          entries[stringifySymbol(key, i) + " =>"] = val2;
          return entries;
        },
        {}
      )
    };
  } else if (isSet(val)) {
    return {
      [`Set(${val.size})`]: [...val.values()].map((v) => stringifySymbol(v))
    };
  } else if (isSymbol(val)) {
    return stringifySymbol(val);
  } else if (isObject(val) && !isArray(val) && !isPlainObject(val)) {
    return String(val);
  }
  return val;
};
const stringifySymbol = (v, i = "") => {
  var _a;
  return (
    // Symbol.description in es2019+ so we need to cast here to pass
    // the lib: es2016 check
    isSymbol(v) ? `Symbol(${(_a = v.description) != null ? _a : i})` : v
  );
};
function normalizeCssVarValue(value) {
  if (value == null) {
    return "initial";
  }
  if (typeof value === "string") {
    return value === "" ? " " : value;
  }
  return String(value);
}
let activeEffectScope;
class EffectScope {
  constructor(detached = false) {
    this.detached = detached;
    this._active = true;
    this._on = 0;
    this.effects = [];
    this.cleanups = [];
    this._isPaused = false;
    this.parent = activeEffectScope;
    if (!detached && activeEffectScope) {
      this.index = (activeEffectScope.scopes || (activeEffectScope.scopes = [])).push(
        this
      ) - 1;
    }
  }
  get active() {
    return this._active;
  }
  pause() {
    if (this._active) {
      this._isPaused = true;
      let i, l;
      if (this.scopes) {
        for (i = 0, l = this.scopes.length; i < l; i++) {
          this.scopes[i].pause();
        }
      }
      for (i = 0, l = this.effects.length; i < l; i++) {
        this.effects[i].pause();
      }
    }
  }
  /**
   * Resumes the effect scope, including all child scopes and effects.
   */
  resume() {
    if (this._active) {
      if (this._isPaused) {
        this._isPaused = false;
        let i, l;
        if (this.scopes) {
          for (i = 0, l = this.scopes.length; i < l; i++) {
            this.scopes[i].resume();
          }
        }
        for (i = 0, l = this.effects.length; i < l; i++) {
          this.effects[i].resume();
        }
      }
    }
  }
  run(fn) {
    if (this._active) {
      const currentEffectScope = activeEffectScope;
      try {
        activeEffectScope = this;
        return fn();
      } finally {
        activeEffectScope = currentEffectScope;
      }
    }
  }
  /**
   * This should only be called on non-detached scopes
   * @internal
   */
  on() {
    if (++this._on === 1) {
      this.prevScope = activeEffectScope;
      activeEffectScope = this;
    }
  }
  /**
   * This should only be called on non-detached scopes
   * @internal
   */
  off() {
    if (this._on > 0 && --this._on === 0) {
      activeEffectScope = this.prevScope;
      this.prevScope = void 0;
    }
  }
  stop(fromParent) {
    if (this._active) {
      this._active = false;
      let i, l;
      for (i = 0, l = this.effects.length; i < l; i++) {
        this.effects[i].stop();
      }
      this.effects.length = 0;
      for (i = 0, l = this.cleanups.length; i < l; i++) {
        this.cleanups[i]();
      }
      this.cleanups.length = 0;
      if (this.scopes) {
        for (i = 0, l = this.scopes.length; i < l; i++) {
          this.scopes[i].stop(true);
        }
        this.scopes.length = 0;
      }
      if (!this.detached && this.parent && !fromParent) {
        const last = this.parent.scopes.pop();
        if (last && last !== this) {
          this.parent.scopes[this.index] = last;
          last.index = this.index;
        }
      }
      this.parent = void 0;
    }
  }
}
function effectScope(detached) {
  return new EffectScope(detached);
}
function getCurrentScope() {
  return activeEffectScope;
}
function onScopeDispose(fn, failSilently = false) {
  if (activeEffectScope) {
    activeEffectScope.cleanups.push(fn);
  }
}
let activeSub;
const pausedQueueEffects = /* @__PURE__ */ new WeakSet();
class ReactiveEffect {
  constructor(fn) {
    this.fn = fn;
    this.deps = void 0;
    this.depsTail = void 0;
    this.flags = 1 | 4;
    this.next = void 0;
    this.cleanup = void 0;
    this.scheduler = void 0;
    if (activeEffectScope && activeEffectScope.active) {
      activeEffectScope.effects.push(this);
    }
  }
  pause() {
    this.flags |= 64;
  }
  resume() {
    if (this.flags & 64) {
      this.flags &= -65;
      if (pausedQueueEffects.has(this)) {
        pausedQueueEffects.delete(this);
        this.trigger();
      }
    }
  }
  /**
   * @internal
   */
  notify() {
    if (this.flags & 2 && !(this.flags & 32)) {
      return;
    }
    if (!(this.flags & 8)) {
      batch(this);
    }
  }
  run() {
    if (!(this.flags & 1)) {
      return this.fn();
    }
    this.flags |= 2;
    cleanupEffect(this);
    prepareDeps(this);
    const prevEffect = activeSub;
    const prevShouldTrack = shouldTrack;
    activeSub = this;
    shouldTrack = true;
    try {
      return this.fn();
    } finally {
      cleanupDeps(this);
      activeSub = prevEffect;
      shouldTrack = prevShouldTrack;
      this.flags &= -3;
    }
  }
  stop() {
    if (this.flags & 1) {
      for (let link = this.deps; link; link = link.nextDep) {
        removeSub(link);
      }
      this.deps = this.depsTail = void 0;
      cleanupEffect(this);
      this.onStop && this.onStop();
      this.flags &= -2;
    }
  }
  trigger() {
    if (this.flags & 64) {
      pausedQueueEffects.add(this);
    } else if (this.scheduler) {
      this.scheduler();
    } else {
      this.runIfDirty();
    }
  }
  /**
   * @internal
   */
  runIfDirty() {
    if (isDirty(this)) {
      this.run();
    }
  }
  get dirty() {
    return isDirty(this);
  }
}
let batchDepth = 0;
let batchedSub;
let batchedComputed;
function batch(sub, isComputed = false) {
  sub.flags |= 8;
  if (isComputed) {
    sub.next = batchedComputed;
    batchedComputed = sub;
    return;
  }
  sub.next = batchedSub;
  batchedSub = sub;
}
function startBatch() {
  batchDepth++;
}
function endBatch() {
  if (--batchDepth > 0) {
    return;
  }
  if (batchedComputed) {
    let e = batchedComputed;
    batchedComputed = void 0;
    while (e) {
      const next = e.next;
      e.next = void 0;
      e.flags &= -9;
      e = next;
    }
  }
  let error;
  while (batchedSub) {
    let e = batchedSub;
    batchedSub = void 0;
    while (e) {
      const next = e.next;
      e.next = void 0;
      e.flags &= -9;
      if (e.flags & 1) {
        try {
          ;
          e.trigger();
        } catch (err) {
          if (!error) error = err;
        }
      }
      e = next;
    }
  }
  if (error) throw error;
}
function prepareDeps(sub) {
  for (let link = sub.deps; link; link = link.nextDep) {
    link.version = -1;
    link.prevActiveLink = link.dep.activeLink;
    link.dep.activeLink = link;
  }
}
function cleanupDeps(sub) {
  let head;
  let tail = sub.depsTail;
  let link = tail;
  while (link) {
    const prev = link.prevDep;
    if (link.version === -1) {
      if (link === tail) tail = prev;
      removeSub(link);
      removeDep(link);
    } else {
      head = link;
    }
    link.dep.activeLink = link.prevActiveLink;
    link.prevActiveLink = void 0;
    link = prev;
  }
  sub.deps = head;
  sub.depsTail = tail;
}
function isDirty(sub) {
  for (let link = sub.deps; link; link = link.nextDep) {
    if (link.dep.version !== link.version || link.dep.computed && (refreshComputed(link.dep.computed) || link.dep.version !== link.version)) {
      return true;
    }
  }
  if (sub._dirty) {
    return true;
  }
  return false;
}
function refreshComputed(computed2) {
  if (computed2.flags & 4 && !(computed2.flags & 16)) {
    return;
  }
  computed2.flags &= -17;
  if (computed2.globalVersion === globalVersion) {
    return;
  }
  computed2.globalVersion = globalVersion;
  if (!computed2.isSSR && computed2.flags & 128 && (!computed2.deps && !computed2._dirty || !isDirty(computed2))) {
    return;
  }
  computed2.flags |= 2;
  const dep = computed2.dep;
  const prevSub = activeSub;
  const prevShouldTrack = shouldTrack;
  activeSub = computed2;
  shouldTrack = true;
  try {
    prepareDeps(computed2);
    const value = computed2.fn(computed2._value);
    if (dep.version === 0 || hasChanged(value, computed2._value)) {
      computed2.flags |= 128;
      computed2._value = value;
      dep.version++;
    }
  } catch (err) {
    dep.version++;
    throw err;
  } finally {
    activeSub = prevSub;
    shouldTrack = prevShouldTrack;
    cleanupDeps(computed2);
    computed2.flags &= -3;
  }
}
function removeSub(link, soft = false) {
  const { dep, prevSub, nextSub } = link;
  if (prevSub) {
    prevSub.nextSub = nextSub;
    link.prevSub = void 0;
  }
  if (nextSub) {
    nextSub.prevSub = prevSub;
    link.nextSub = void 0;
  }
  if (dep.subs === link) {
    dep.subs = prevSub;
    if (!prevSub && dep.computed) {
      dep.computed.flags &= -5;
      for (let l = dep.computed.deps; l; l = l.nextDep) {
        removeSub(l, true);
      }
    }
  }
  if (!soft && !--dep.sc && dep.map) {
    dep.map.delete(dep.key);
  }
}
function removeDep(link) {
  const { prevDep, nextDep } = link;
  if (prevDep) {
    prevDep.nextDep = nextDep;
    link.prevDep = void 0;
  }
  if (nextDep) {
    nextDep.prevDep = prevDep;
    link.nextDep = void 0;
  }
}
function effect(fn, options) {
  if (fn.effect instanceof ReactiveEffect) {
    fn = fn.effect.fn;
  }
  const e = new ReactiveEffect(fn);
  if (options) {
    extend(e, options);
  }
  try {
    e.run();
  } catch (err) {
    e.stop();
    throw err;
  }
  const runner = e.run.bind(e);
  runner.effect = e;
  return runner;
}
function stop(runner) {
  runner.effect.stop();
}
let shouldTrack = true;
const trackStack = [];
function pauseTracking() {
  trackStack.push(shouldTrack);
  shouldTrack = false;
}
function resetTracking() {
  const last = trackStack.pop();
  shouldTrack = last === void 0 ? true : last;
}
function cleanupEffect(e) {
  const { cleanup } = e;
  e.cleanup = void 0;
  if (cleanup) {
    const prevSub = activeSub;
    activeSub = void 0;
    try {
      cleanup();
    } finally {
      activeSub = prevSub;
    }
  }
}
let globalVersion = 0;
class Link {
  constructor(sub, dep) {
    this.sub = sub;
    this.dep = dep;
    this.version = dep.version;
    this.nextDep = this.prevDep = this.nextSub = this.prevSub = this.prevActiveLink = void 0;
  }
}
class Dep {
  // TODO isolatedDeclarations "__v_skip"
  constructor(computed2) {
    this.computed = computed2;
    this.version = 0;
    this.activeLink = void 0;
    this.subs = void 0;
    this.map = void 0;
    this.key = void 0;
    this.sc = 0;
    this.__v_skip = true;
  }
  track(debugInfo) {
    if (!activeSub || !shouldTrack || activeSub === this.computed) {
      return;
    }
    let link = this.activeLink;
    if (link === void 0 || link.sub !== activeSub) {
      link = this.activeLink = new Link(activeSub, this);
      if (!activeSub.deps) {
        activeSub.deps = activeSub.depsTail = link;
      } else {
        link.prevDep = activeSub.depsTail;
        activeSub.depsTail.nextDep = link;
        activeSub.depsTail = link;
      }
      addSub(link);
    } else if (link.version === -1) {
      link.version = this.version;
      if (link.nextDep) {
        const next = link.nextDep;
        next.prevDep = link.prevDep;
        if (link.prevDep) {
          link.prevDep.nextDep = next;
        }
        link.prevDep = activeSub.depsTail;
        link.nextDep = void 0;
        activeSub.depsTail.nextDep = link;
        activeSub.depsTail = link;
        if (activeSub.deps === link) {
          activeSub.deps = next;
        }
      }
    }
    return link;
  }
  trigger(debugInfo) {
    this.version++;
    globalVersion++;
    this.notify(debugInfo);
  }
  notify(debugInfo) {
    startBatch();
    try {
      if (false) ;
      for (let link = this.subs; link; link = link.prevSub) {
        if (link.sub.notify()) {
          ;
          link.sub.dep.notify();
        }
      }
    } finally {
      endBatch();
    }
  }
}
function addSub(link) {
  link.dep.sc++;
  if (link.sub.flags & 4) {
    const computed2 = link.dep.computed;
    if (computed2 && !link.dep.subs) {
      computed2.flags |= 4 | 16;
      for (let l = computed2.deps; l; l = l.nextDep) {
        addSub(l);
      }
    }
    const currentTail = link.dep.subs;
    if (currentTail !== link) {
      link.prevSub = currentTail;
      if (currentTail) currentTail.nextSub = link;
    }
    link.dep.subs = link;
  }
}
const targetMap = /* @__PURE__ */ new WeakMap();
const ITERATE_KEY = Symbol(
  ""
);
const MAP_KEY_ITERATE_KEY = Symbol(
  ""
);
const ARRAY_ITERATE_KEY = Symbol(
  ""
);
function track(target, type, key) {
  if (shouldTrack && activeSub) {
    let depsMap = targetMap.get(target);
    if (!depsMap) {
      targetMap.set(target, depsMap = /* @__PURE__ */ new Map());
    }
    let dep = depsMap.get(key);
    if (!dep) {
      depsMap.set(key, dep = new Dep());
      dep.map = depsMap;
      dep.key = key;
    }
    {
      dep.track();
    }
  }
}
function trigger(target, type, key, newValue, oldValue, oldTarget) {
  const depsMap = targetMap.get(target);
  if (!depsMap) {
    globalVersion++;
    return;
  }
  const run = (dep) => {
    if (dep) {
      {
        dep.trigger();
      }
    }
  };
  startBatch();
  if (type === "clear") {
    depsMap.forEach(run);
  } else {
    const targetIsArray = isArray(target);
    const isArrayIndex = targetIsArray && isIntegerKey(key);
    if (targetIsArray && key === "length") {
      const newLength = Number(newValue);
      depsMap.forEach((dep, key2) => {
        if (key2 === "length" || key2 === ARRAY_ITERATE_KEY || !isSymbol(key2) && key2 >= newLength) {
          run(dep);
        }
      });
    } else {
      if (key !== void 0 || depsMap.has(void 0)) {
        run(depsMap.get(key));
      }
      if (isArrayIndex) {
        run(depsMap.get(ARRAY_ITERATE_KEY));
      }
      switch (type) {
        case "add":
          if (!targetIsArray) {
            run(depsMap.get(ITERATE_KEY));
            if (isMap(target)) {
              run(depsMap.get(MAP_KEY_ITERATE_KEY));
            }
          } else if (isArrayIndex) {
            run(depsMap.get("length"));
          }
          break;
        case "delete":
          if (!targetIsArray) {
            run(depsMap.get(ITERATE_KEY));
            if (isMap(target)) {
              run(depsMap.get(MAP_KEY_ITERATE_KEY));
            }
          }
          break;
        case "set":
          if (isMap(target)) {
            run(depsMap.get(ITERATE_KEY));
          }
          break;
      }
    }
  }
  endBatch();
}
function getDepFromReactive(object, key) {
  const depMap = targetMap.get(object);
  return depMap && depMap.get(key);
}
function reactiveReadArray(array) {
  const raw = toRaw(array);
  if (raw === array) return raw;
  track(raw, "iterate", ARRAY_ITERATE_KEY);
  return isShallow(array) ? raw : raw.map(toReactive);
}
function shallowReadArray(arr) {
  track(arr = toRaw(arr), "iterate", ARRAY_ITERATE_KEY);
  return arr;
}
function toWrapped(target, item) {
  if (isReadonly(target)) {
    return isReactive(target) ? toReadonly(toReactive(item)) : toReadonly(item);
  }
  return toReactive(item);
}
const arrayInstrumentations = {
  __proto__: null,
  [Symbol.iterator]() {
    return iterator(this, Symbol.iterator, (item) => toWrapped(this, item));
  },
  concat(...args) {
    return reactiveReadArray(this).concat(
      ...args.map((x2) => isArray(x2) ? reactiveReadArray(x2) : x2)
    );
  },
  entries() {
    return iterator(this, "entries", (value) => {
      value[1] = toWrapped(this, value[1]);
      return value;
    });
  },
  every(fn, thisArg) {
    return apply(this, "every", fn, thisArg, void 0, arguments);
  },
  filter(fn, thisArg) {
    return apply(
      this,
      "filter",
      fn,
      thisArg,
      (v) => v.map((item) => toWrapped(this, item)),
      arguments
    );
  },
  find(fn, thisArg) {
    return apply(
      this,
      "find",
      fn,
      thisArg,
      (item) => toWrapped(this, item),
      arguments
    );
  },
  findIndex(fn, thisArg) {
    return apply(this, "findIndex", fn, thisArg, void 0, arguments);
  },
  findLast(fn, thisArg) {
    return apply(
      this,
      "findLast",
      fn,
      thisArg,
      (item) => toWrapped(this, item),
      arguments
    );
  },
  findLastIndex(fn, thisArg) {
    return apply(this, "findLastIndex", fn, thisArg, void 0, arguments);
  },
  // flat, flatMap could benefit from ARRAY_ITERATE but are not straight-forward to implement
  forEach(fn, thisArg) {
    return apply(this, "forEach", fn, thisArg, void 0, arguments);
  },
  includes(...args) {
    return searchProxy(this, "includes", args);
  },
  indexOf(...args) {
    return searchProxy(this, "indexOf", args);
  },
  join(separator) {
    return reactiveReadArray(this).join(separator);
  },
  // keys() iterator only reads `length`, no optimization required
  lastIndexOf(...args) {
    return searchProxy(this, "lastIndexOf", args);
  },
  map(fn, thisArg) {
    return apply(this, "map", fn, thisArg, void 0, arguments);
  },
  pop() {
    return noTracking(this, "pop");
  },
  push(...args) {
    return noTracking(this, "push", args);
  },
  reduce(fn, ...args) {
    return reduce(this, "reduce", fn, args);
  },
  reduceRight(fn, ...args) {
    return reduce(this, "reduceRight", fn, args);
  },
  shift() {
    return noTracking(this, "shift");
  },
  // slice could use ARRAY_ITERATE but also seems to beg for range tracking
  some(fn, thisArg) {
    return apply(this, "some", fn, thisArg, void 0, arguments);
  },
  splice(...args) {
    return noTracking(this, "splice", args);
  },
  toReversed() {
    return reactiveReadArray(this).toReversed();
  },
  toSorted(comparer) {
    return reactiveReadArray(this).toSorted(comparer);
  },
  toSpliced(...args) {
    return reactiveReadArray(this).toSpliced(...args);
  },
  unshift(...args) {
    return noTracking(this, "unshift", args);
  },
  values() {
    return iterator(this, "values", (item) => toWrapped(this, item));
  }
};
function iterator(self2, method, wrapValue) {
  const arr = shallowReadArray(self2);
  const iter = arr[method]();
  if (arr !== self2 && !isShallow(self2)) {
    iter._next = iter.next;
    iter.next = () => {
      const result = iter._next();
      if (!result.done) {
        result.value = wrapValue(result.value);
      }
      return result;
    };
  }
  return iter;
}
const arrayProto = Array.prototype;
function apply(self2, method, fn, thisArg, wrappedRetFn, args) {
  const arr = shallowReadArray(self2);
  const needsWrap = arr !== self2 && !isShallow(self2);
  const methodFn = arr[method];
  if (methodFn !== arrayProto[method]) {
    const result2 = methodFn.apply(self2, args);
    return needsWrap ? toReactive(result2) : result2;
  }
  let wrappedFn = fn;
  if (arr !== self2) {
    if (needsWrap) {
      wrappedFn = function(item, index) {
        return fn.call(this, toWrapped(self2, item), index, self2);
      };
    } else if (fn.length > 2) {
      wrappedFn = function(item, index) {
        return fn.call(this, item, index, self2);
      };
    }
  }
  const result = methodFn.call(arr, wrappedFn, thisArg);
  return needsWrap && wrappedRetFn ? wrappedRetFn(result) : result;
}
function reduce(self2, method, fn, args) {
  const arr = shallowReadArray(self2);
  let wrappedFn = fn;
  if (arr !== self2) {
    if (!isShallow(self2)) {
      wrappedFn = function(acc, item, index) {
        return fn.call(this, acc, toWrapped(self2, item), index, self2);
      };
    } else if (fn.length > 3) {
      wrappedFn = function(acc, item, index) {
        return fn.call(this, acc, item, index, self2);
      };
    }
  }
  return arr[method](wrappedFn, ...args);
}
function searchProxy(self2, method, args) {
  const arr = toRaw(self2);
  track(arr, "iterate", ARRAY_ITERATE_KEY);
  const res = arr[method](...args);
  if ((res === -1 || res === false) && isProxy(args[0])) {
    args[0] = toRaw(args[0]);
    return arr[method](...args);
  }
  return res;
}
function noTracking(self2, method, args = []) {
  pauseTracking();
  startBatch();
  const res = toRaw(self2)[method].apply(self2, args);
  endBatch();
  resetTracking();
  return res;
}
const isNonTrackableKeys = /* @__PURE__ */ makeMap(`__proto__,__v_isRef,__isVue`);
const builtInSymbols = new Set(
  /* @__PURE__ */ Object.getOwnPropertyNames(Symbol).filter((key) => key !== "arguments" && key !== "caller").map((key) => Symbol[key]).filter(isSymbol)
);
function hasOwnProperty(key) {
  if (!isSymbol(key)) key = String(key);
  const obj = toRaw(this);
  track(obj, "has", key);
  return obj.hasOwnProperty(key);
}
class BaseReactiveHandler {
  constructor(_isReadonly = false, _isShallow = false) {
    this._isReadonly = _isReadonly;
    this._isShallow = _isShallow;
  }
  get(target, key, receiver) {
    if (key === "__v_skip") return target["__v_skip"];
    const isReadonly2 = this._isReadonly, isShallow2 = this._isShallow;
    if (key === "__v_isReactive") {
      return !isReadonly2;
    } else if (key === "__v_isReadonly") {
      return isReadonly2;
    } else if (key === "__v_isShallow") {
      return isShallow2;
    } else if (key === "__v_raw") {
      if (receiver === (isReadonly2 ? isShallow2 ? shallowReadonlyMap : readonlyMap : isShallow2 ? shallowReactiveMap : reactiveMap).get(target) || // receiver is not the reactive proxy, but has the same prototype
      // this means the receiver is a user proxy of the reactive proxy
      Object.getPrototypeOf(target) === Object.getPrototypeOf(receiver)) {
        return target;
      }
      return;
    }
    const targetIsArray = isArray(target);
    if (!isReadonly2) {
      let fn;
      if (targetIsArray && (fn = arrayInstrumentations[key])) {
        return fn;
      }
      if (key === "hasOwnProperty") {
        return hasOwnProperty;
      }
    }
    const res = Reflect.get(
      target,
      key,
      // if this is a proxy wrapping a ref, return methods using the raw ref
      // as receiver so that we don't have to call `toRaw` on the ref in all
      // its class methods
      isRef(target) ? target : receiver
    );
    if (isSymbol(key) ? builtInSymbols.has(key) : isNonTrackableKeys(key)) {
      return res;
    }
    if (!isReadonly2) {
      track(target, "get", key);
    }
    if (isShallow2) {
      return res;
    }
    if (isRef(res)) {
      const value = targetIsArray && isIntegerKey(key) ? res : res.value;
      return isReadonly2 && isObject(value) ? readonly(value) : value;
    }
    if (isObject(res)) {
      return isReadonly2 ? readonly(res) : reactive(res);
    }
    return res;
  }
}
class MutableReactiveHandler extends BaseReactiveHandler {
  constructor(isShallow2 = false) {
    super(false, isShallow2);
  }
  set(target, key, value, receiver) {
    let oldValue = target[key];
    const isArrayWithIntegerKey = isArray(target) && isIntegerKey(key);
    if (!this._isShallow) {
      const isOldValueReadonly = isReadonly(oldValue);
      if (!isShallow(value) && !isReadonly(value)) {
        oldValue = toRaw(oldValue);
        value = toRaw(value);
      }
      if (!isArrayWithIntegerKey && isRef(oldValue) && !isRef(value)) {
        if (isOldValueReadonly) {
          return true;
        } else {
          oldValue.value = value;
          return true;
        }
      }
    }
    const hadKey = isArrayWithIntegerKey ? Number(key) < target.length : hasOwn(target, key);
    const result = Reflect.set(
      target,
      key,
      value,
      isRef(target) ? target : receiver
    );
    if (target === toRaw(receiver)) {
      if (!hadKey) {
        trigger(target, "add", key, value);
      } else if (hasChanged(value, oldValue)) {
        trigger(target, "set", key, value);
      }
    }
    return result;
  }
  deleteProperty(target, key) {
    const hadKey = hasOwn(target, key);
    target[key];
    const result = Reflect.deleteProperty(target, key);
    if (result && hadKey) {
      trigger(target, "delete", key, void 0);
    }
    return result;
  }
  has(target, key) {
    const result = Reflect.has(target, key);
    if (!isSymbol(key) || !builtInSymbols.has(key)) {
      track(target, "has", key);
    }
    return result;
  }
  ownKeys(target) {
    track(
      target,
      "iterate",
      isArray(target) ? "length" : ITERATE_KEY
    );
    return Reflect.ownKeys(target);
  }
}
class ReadonlyReactiveHandler extends BaseReactiveHandler {
  constructor(isShallow2 = false) {
    super(true, isShallow2);
  }
  set(target, key) {
    return true;
  }
  deleteProperty(target, key) {
    return true;
  }
}
const mutableHandlers = /* @__PURE__ */ new MutableReactiveHandler();
const readonlyHandlers = /* @__PURE__ */ new ReadonlyReactiveHandler();
const shallowReactiveHandlers = /* @__PURE__ */ new MutableReactiveHandler(true);
const shallowReadonlyHandlers = /* @__PURE__ */ new ReadonlyReactiveHandler(true);
const toShallow = (value) => value;
const getProto = (v) => Reflect.getPrototypeOf(v);
function createIterableMethod(method, isReadonly2, isShallow2) {
  return function(...args) {
    const target = this["__v_raw"];
    const rawTarget = toRaw(target);
    const targetIsMap = isMap(rawTarget);
    const isPair = method === "entries" || method === Symbol.iterator && targetIsMap;
    const isKeyOnly = method === "keys" && targetIsMap;
    const innerIterator = target[method](...args);
    const wrap = isShallow2 ? toShallow : isReadonly2 ? toReadonly : toReactive;
    !isReadonly2 && track(
      rawTarget,
      "iterate",
      isKeyOnly ? MAP_KEY_ITERATE_KEY : ITERATE_KEY
    );
    return {
      // iterator protocol
      next() {
        const { value, done } = innerIterator.next();
        return done ? { value, done } : {
          value: isPair ? [wrap(value[0]), wrap(value[1])] : wrap(value),
          done
        };
      },
      // iterable protocol
      [Symbol.iterator]() {
        return this;
      }
    };
  };
}
function createReadonlyMethod(type) {
  return function(...args) {
    return type === "delete" ? false : type === "clear" ? void 0 : this;
  };
}
function createInstrumentations(readonly2, shallow) {
  const instrumentations = {
    get(key) {
      const target = this["__v_raw"];
      const rawTarget = toRaw(target);
      const rawKey = toRaw(key);
      if (!readonly2) {
        if (hasChanged(key, rawKey)) {
          track(rawTarget, "get", key);
        }
        track(rawTarget, "get", rawKey);
      }
      const { has } = getProto(rawTarget);
      const wrap = shallow ? toShallow : readonly2 ? toReadonly : toReactive;
      if (has.call(rawTarget, key)) {
        return wrap(target.get(key));
      } else if (has.call(rawTarget, rawKey)) {
        return wrap(target.get(rawKey));
      } else if (target !== rawTarget) {
        target.get(key);
      }
    },
    get size() {
      const target = this["__v_raw"];
      !readonly2 && track(toRaw(target), "iterate", ITERATE_KEY);
      return target.size;
    },
    has(key) {
      const target = this["__v_raw"];
      const rawTarget = toRaw(target);
      const rawKey = toRaw(key);
      if (!readonly2) {
        if (hasChanged(key, rawKey)) {
          track(rawTarget, "has", key);
        }
        track(rawTarget, "has", rawKey);
      }
      return key === rawKey ? target.has(key) : target.has(key) || target.has(rawKey);
    },
    forEach(callback, thisArg) {
      const observed = this;
      const target = observed["__v_raw"];
      const rawTarget = toRaw(target);
      const wrap = shallow ? toShallow : readonly2 ? toReadonly : toReactive;
      !readonly2 && track(rawTarget, "iterate", ITERATE_KEY);
      return target.forEach((value, key) => {
        return callback.call(thisArg, wrap(value), wrap(key), observed);
      });
    }
  };
  extend(
    instrumentations,
    readonly2 ? {
      add: createReadonlyMethod("add"),
      set: createReadonlyMethod("set"),
      delete: createReadonlyMethod("delete"),
      clear: createReadonlyMethod("clear")
    } : {
      add(value) {
        if (!shallow && !isShallow(value) && !isReadonly(value)) {
          value = toRaw(value);
        }
        const target = toRaw(this);
        const proto = getProto(target);
        const hadKey = proto.has.call(target, value);
        if (!hadKey) {
          target.add(value);
          trigger(target, "add", value, value);
        }
        return this;
      },
      set(key, value) {
        if (!shallow && !isShallow(value) && !isReadonly(value)) {
          value = toRaw(value);
        }
        const target = toRaw(this);
        const { has, get } = getProto(target);
        let hadKey = has.call(target, key);
        if (!hadKey) {
          key = toRaw(key);
          hadKey = has.call(target, key);
        }
        const oldValue = get.call(target, key);
        target.set(key, value);
        if (!hadKey) {
          trigger(target, "add", key, value);
        } else if (hasChanged(value, oldValue)) {
          trigger(target, "set", key, value);
        }
        return this;
      },
      delete(key) {
        const target = toRaw(this);
        const { has, get } = getProto(target);
        let hadKey = has.call(target, key);
        if (!hadKey) {
          key = toRaw(key);
          hadKey = has.call(target, key);
        }
        get ? get.call(target, key) : void 0;
        const result = target.delete(key);
        if (hadKey) {
          trigger(target, "delete", key, void 0);
        }
        return result;
      },
      clear() {
        const target = toRaw(this);
        const hadItems = target.size !== 0;
        const result = target.clear();
        if (hadItems) {
          trigger(
            target,
            "clear",
            void 0,
            void 0
          );
        }
        return result;
      }
    }
  );
  const iteratorMethods = [
    "keys",
    "values",
    "entries",
    Symbol.iterator
  ];
  iteratorMethods.forEach((method) => {
    instrumentations[method] = createIterableMethod(method, readonly2, shallow);
  });
  return instrumentations;
}
function createInstrumentationGetter(isReadonly2, shallow) {
  const instrumentations = createInstrumentations(isReadonly2, shallow);
  return (target, key, receiver) => {
    if (key === "__v_isReactive") {
      return !isReadonly2;
    } else if (key === "__v_isReadonly") {
      return isReadonly2;
    } else if (key === "__v_raw") {
      return target;
    }
    return Reflect.get(
      hasOwn(instrumentations, key) && key in target ? instrumentations : target,
      key,
      receiver
    );
  };
}
const mutableCollectionHandlers = {
  get: /* @__PURE__ */ createInstrumentationGetter(false, false)
};
const shallowCollectionHandlers = {
  get: /* @__PURE__ */ createInstrumentationGetter(false, true)
};
const readonlyCollectionHandlers = {
  get: /* @__PURE__ */ createInstrumentationGetter(true, false)
};
const shallowReadonlyCollectionHandlers = {
  get: /* @__PURE__ */ createInstrumentationGetter(true, true)
};
const reactiveMap = /* @__PURE__ */ new WeakMap();
const shallowReactiveMap = /* @__PURE__ */ new WeakMap();
const readonlyMap = /* @__PURE__ */ new WeakMap();
const shallowReadonlyMap = /* @__PURE__ */ new WeakMap();
function targetTypeMap(rawType) {
  switch (rawType) {
    case "Object":
    case "Array":
      return 1;
    case "Map":
    case "Set":
    case "WeakMap":
    case "WeakSet":
      return 2;
    default:
      return 0;
  }
}
function getTargetType(value) {
  return value["__v_skip"] || !Object.isExtensible(value) ? 0 : targetTypeMap(toRawType(value));
}
function reactive(target) {
  if (isReadonly(target)) {
    return target;
  }
  return createReactiveObject(
    target,
    false,
    mutableHandlers,
    mutableCollectionHandlers,
    reactiveMap
  );
}
function shallowReactive(target) {
  return createReactiveObject(
    target,
    false,
    shallowReactiveHandlers,
    shallowCollectionHandlers,
    shallowReactiveMap
  );
}
function readonly(target) {
  return createReactiveObject(
    target,
    true,
    readonlyHandlers,
    readonlyCollectionHandlers,
    readonlyMap
  );
}
function shallowReadonly(target) {
  return createReactiveObject(
    target,
    true,
    shallowReadonlyHandlers,
    shallowReadonlyCollectionHandlers,
    shallowReadonlyMap
  );
}
function createReactiveObject(target, isReadonly2, baseHandlers, collectionHandlers, proxyMap) {
  if (!isObject(target)) {
    return target;
  }
  if (target["__v_raw"] && !(isReadonly2 && target["__v_isReactive"])) {
    return target;
  }
  const targetType = getTargetType(target);
  if (targetType === 0) {
    return target;
  }
  const existingProxy = proxyMap.get(target);
  if (existingProxy) {
    return existingProxy;
  }
  const proxy = new Proxy(
    target,
    targetType === 2 ? collectionHandlers : baseHandlers
  );
  proxyMap.set(target, proxy);
  return proxy;
}
function isReactive(value) {
  if (isReadonly(value)) {
    return isReactive(value["__v_raw"]);
  }
  return !!(value && value["__v_isReactive"]);
}
function isReadonly(value) {
  return !!(value && value["__v_isReadonly"]);
}
function isShallow(value) {
  return !!(value && value["__v_isShallow"]);
}
function isProxy(value) {
  return value ? !!value["__v_raw"] : false;
}
function toRaw(observed) {
  const raw = observed && observed["__v_raw"];
  return raw ? toRaw(raw) : observed;
}
function markRaw(value) {
  if (!hasOwn(value, "__v_skip") && Object.isExtensible(value)) {
    def(value, "__v_skip", true);
  }
  return value;
}
const toReactive = (value) => isObject(value) ? reactive(value) : value;
const toReadonly = (value) => isObject(value) ? readonly(value) : value;
function isRef(r) {
  return r ? r["__v_isRef"] === true : false;
}
function ref(value) {
  return createRef(value, false);
}
function shallowRef(value) {
  return createRef(value, true);
}
function createRef(rawValue, shallow) {
  if (isRef(rawValue)) {
    return rawValue;
  }
  return new RefImpl(rawValue, shallow);
}
class RefImpl {
  constructor(value, isShallow2) {
    this.dep = new Dep();
    this["__v_isRef"] = true;
    this["__v_isShallow"] = false;
    this._rawValue = isShallow2 ? value : toRaw(value);
    this._value = isShallow2 ? value : toReactive(value);
    this["__v_isShallow"] = isShallow2;
  }
  get value() {
    {
      this.dep.track();
    }
    return this._value;
  }
  set value(newValue) {
    const oldValue = this._rawValue;
    const useDirectValue = this["__v_isShallow"] || isShallow(newValue) || isReadonly(newValue);
    newValue = useDirectValue ? newValue : toRaw(newValue);
    if (hasChanged(newValue, oldValue)) {
      this._rawValue = newValue;
      this._value = useDirectValue ? newValue : toReactive(newValue);
      {
        this.dep.trigger();
      }
    }
  }
}
function triggerRef(ref2) {
  if (ref2.dep) {
    {
      ref2.dep.trigger();
    }
  }
}
function unref(ref2) {
  return isRef(ref2) ? ref2.value : ref2;
}
function toValue(source) {
  return isFunction(source) ? source() : unref(source);
}
const shallowUnwrapHandlers = {
  get: (target, key, receiver) => key === "__v_raw" ? target : unref(Reflect.get(target, key, receiver)),
  set: (target, key, value, receiver) => {
    const oldValue = target[key];
    if (isRef(oldValue) && !isRef(value)) {
      oldValue.value = value;
      return true;
    } else {
      return Reflect.set(target, key, value, receiver);
    }
  }
};
function proxyRefs(objectWithRefs) {
  return isReactive(objectWithRefs) ? objectWithRefs : new Proxy(objectWithRefs, shallowUnwrapHandlers);
}
class CustomRefImpl {
  constructor(factory) {
    this["__v_isRef"] = true;
    this._value = void 0;
    const dep = this.dep = new Dep();
    const { get, set } = factory(dep.track.bind(dep), dep.trigger.bind(dep));
    this._get = get;
    this._set = set;
  }
  get value() {
    return this._value = this._get();
  }
  set value(newVal) {
    this._set(newVal);
  }
}
function customRef(factory) {
  return new CustomRefImpl(factory);
}
function toRefs(object) {
  const ret = isArray(object) ? new Array(object.length) : {};
  for (const key in object) {
    ret[key] = propertyToRef(object, key);
  }
  return ret;
}
class ObjectRefImpl {
  constructor(_object, _key, _defaultValue) {
    this._object = _object;
    this._key = _key;
    this._defaultValue = _defaultValue;
    this["__v_isRef"] = true;
    this._value = void 0;
    this._raw = toRaw(_object);
    let shallow = true;
    let obj = _object;
    if (!isArray(_object) || !isIntegerKey(String(_key))) {
      do {
        shallow = !isProxy(obj) || isShallow(obj);
      } while (shallow && (obj = obj["__v_raw"]));
    }
    this._shallow = shallow;
  }
  get value() {
    let val = this._object[this._key];
    if (this._shallow) {
      val = unref(val);
    }
    return this._value = val === void 0 ? this._defaultValue : val;
  }
  set value(newVal) {
    if (this._shallow && isRef(this._raw[this._key])) {
      const nestedRef = this._object[this._key];
      if (isRef(nestedRef)) {
        nestedRef.value = newVal;
        return;
      }
    }
    this._object[this._key] = newVal;
  }
  get dep() {
    return getDepFromReactive(this._raw, this._key);
  }
}
class GetterRefImpl {
  constructor(_getter) {
    this._getter = _getter;
    this["__v_isRef"] = true;
    this["__v_isReadonly"] = true;
    this._value = void 0;
  }
  get value() {
    return this._value = this._getter();
  }
}
function toRef(source, key, defaultValue) {
  if (isRef(source)) {
    return source;
  } else if (isFunction(source)) {
    return new GetterRefImpl(source);
  } else if (isObject(source) && arguments.length > 1) {
    return propertyToRef(source, key, defaultValue);
  } else {
    return ref(source);
  }
}
function propertyToRef(source, key, defaultValue) {
  return new ObjectRefImpl(source, key, defaultValue);
}
class ComputedRefImpl {
  constructor(fn, setter, isSSR) {
    this.fn = fn;
    this.setter = setter;
    this._value = void 0;
    this.dep = new Dep(this);
    this.__v_isRef = true;
    this.deps = void 0;
    this.depsTail = void 0;
    this.flags = 16;
    this.globalVersion = globalVersion - 1;
    this.next = void 0;
    this.effect = this;
    this["__v_isReadonly"] = !setter;
    this.isSSR = isSSR;
  }
  /**
   * @internal
   */
  notify() {
    this.flags |= 16;
    if (!(this.flags & 8) && // avoid infinite self recursion
    activeSub !== this) {
      batch(this, true);
      return true;
    }
  }
  get value() {
    const link = this.dep.track();
    refreshComputed(this);
    if (link) {
      link.version = this.dep.version;
    }
    return this._value;
  }
  set value(newValue) {
    if (this.setter) {
      this.setter(newValue);
    }
  }
}
function computed$1(getterOrOptions, debugOptions, isSSR = false) {
  let getter;
  let setter;
  if (isFunction(getterOrOptions)) {
    getter = getterOrOptions;
  } else {
    getter = getterOrOptions.get;
    setter = getterOrOptions.set;
  }
  const cRef = new ComputedRefImpl(getter, setter, isSSR);
  return cRef;
}
const TrackOpTypes = {
  "GET": "get",
  "HAS": "has",
  "ITERATE": "iterate"
};
const TriggerOpTypes = {
  "SET": "set",
  "ADD": "add",
  "DELETE": "delete",
  "CLEAR": "clear"
};
const INITIAL_WATCHER_VALUE = {};
const cleanupMap = /* @__PURE__ */ new WeakMap();
let activeWatcher = void 0;
function getCurrentWatcher() {
  return activeWatcher;
}
function onWatcherCleanup(cleanupFn, failSilently = false, owner = activeWatcher) {
  if (owner) {
    let cleanups = cleanupMap.get(owner);
    if (!cleanups) cleanupMap.set(owner, cleanups = []);
    cleanups.push(cleanupFn);
  }
}
function watch$1(source, cb2, options = EMPTY_OBJ) {
  const { immediate, deep, once, scheduler, augmentJob, call } = options;
  const reactiveGetter = (source2) => {
    if (deep) return source2;
    if (isShallow(source2) || deep === false || deep === 0)
      return traverse(source2, 1);
    return traverse(source2);
  };
  let effect2;
  let getter;
  let cleanup;
  let boundCleanup;
  let forceTrigger = false;
  let isMultiSource = false;
  if (isRef(source)) {
    getter = () => source.value;
    forceTrigger = isShallow(source);
  } else if (isReactive(source)) {
    getter = () => reactiveGetter(source);
    forceTrigger = true;
  } else if (isArray(source)) {
    isMultiSource = true;
    forceTrigger = source.some((s) => isReactive(s) || isShallow(s));
    getter = () => source.map((s) => {
      if (isRef(s)) {
        return s.value;
      } else if (isReactive(s)) {
        return reactiveGetter(s);
      } else if (isFunction(s)) {
        return call ? call(s, 2) : s();
      } else ;
    });
  } else if (isFunction(source)) {
    if (cb2) {
      getter = call ? () => call(source, 2) : source;
    } else {
      getter = () => {
        if (cleanup) {
          pauseTracking();
          try {
            cleanup();
          } finally {
            resetTracking();
          }
        }
        const currentEffect = activeWatcher;
        activeWatcher = effect2;
        try {
          return call ? call(source, 3, [boundCleanup]) : source(boundCleanup);
        } finally {
          activeWatcher = currentEffect;
        }
      };
    }
  } else {
    getter = NOOP;
  }
  if (cb2 && deep) {
    const baseGetter = getter;
    const depth = deep === true ? Infinity : deep;
    getter = () => traverse(baseGetter(), depth);
  }
  const scope = getCurrentScope();
  const watchHandle = () => {
    effect2.stop();
    if (scope && scope.active) {
      remove(scope.effects, effect2);
    }
  };
  if (once && cb2) {
    const _cb = cb2;
    cb2 = (...args) => {
      _cb(...args);
      watchHandle();
    };
  }
  let oldValue = isMultiSource ? new Array(source.length).fill(INITIAL_WATCHER_VALUE) : INITIAL_WATCHER_VALUE;
  const job = (immediateFirstRun) => {
    if (!(effect2.flags & 1) || !effect2.dirty && !immediateFirstRun) {
      return;
    }
    if (cb2) {
      const newValue = effect2.run();
      if (deep || forceTrigger || (isMultiSource ? newValue.some((v, i) => hasChanged(v, oldValue[i])) : hasChanged(newValue, oldValue))) {
        if (cleanup) {
          cleanup();
        }
        const currentWatcher = activeWatcher;
        activeWatcher = effect2;
        try {
          const args = [
            newValue,
            // pass undefined as the old value when it's changed for the first time
            oldValue === INITIAL_WATCHER_VALUE ? void 0 : isMultiSource && oldValue[0] === INITIAL_WATCHER_VALUE ? [] : oldValue,
            boundCleanup
          ];
          oldValue = newValue;
          call ? call(cb2, 3, args) : (
            // @ts-expect-error
            cb2(...args)
          );
        } finally {
          activeWatcher = currentWatcher;
        }
      }
    } else {
      effect2.run();
    }
  };
  if (augmentJob) {
    augmentJob(job);
  }
  effect2 = new ReactiveEffect(getter);
  effect2.scheduler = scheduler ? () => scheduler(job, false) : job;
  boundCleanup = (fn) => onWatcherCleanup(fn, false, effect2);
  cleanup = effect2.onStop = () => {
    const cleanups = cleanupMap.get(effect2);
    if (cleanups) {
      if (call) {
        call(cleanups, 4);
      } else {
        for (const cleanup2 of cleanups) cleanup2();
      }
      cleanupMap.delete(effect2);
    }
  };
  if (cb2) {
    if (immediate) {
      job(true);
    } else {
      oldValue = effect2.run();
    }
  } else if (scheduler) {
    scheduler(job.bind(null, true), true);
  } else {
    effect2.run();
  }
  watchHandle.pause = effect2.pause.bind(effect2);
  watchHandle.resume = effect2.resume.bind(effect2);
  watchHandle.stop = watchHandle;
  return watchHandle;
}
function traverse(value, depth = Infinity, seen2) {
  if (depth <= 0 || !isObject(value) || value["__v_skip"]) {
    return value;
  }
  seen2 = seen2 || /* @__PURE__ */ new Map();
  if ((seen2.get(value) || 0) >= depth) {
    return value;
  }
  seen2.set(value, depth);
  depth--;
  if (isRef(value)) {
    traverse(value.value, depth, seen2);
  } else if (isArray(value)) {
    for (let i = 0; i < value.length; i++) {
      traverse(value[i], depth, seen2);
    }
  } else if (isSet(value) || isMap(value)) {
    value.forEach((v) => {
      traverse(v, depth, seen2);
    });
  } else if (isPlainObject(value)) {
    for (const key in value) {
      traverse(value[key], depth, seen2);
    }
    for (const key of Object.getOwnPropertySymbols(value)) {
      if (Object.prototype.propertyIsEnumerable.call(value, key)) {
        traverse(value[key], depth, seen2);
      }
    }
  }
  return value;
}
const stack$1 = [];
function pushWarningContext(vnode) {
  stack$1.push(vnode);
}
function popWarningContext() {
  stack$1.pop();
}
let isWarning = false;
function warn$1(msg, ...args) {
  if (isWarning) return;
  isWarning = true;
  pauseTracking();
  const instance = stack$1.length ? stack$1[stack$1.length - 1].component : null;
  const appWarnHandler = instance && instance.appContext.config.warnHandler;
  const trace = getComponentTrace();
  if (appWarnHandler) {
    callWithErrorHandling(
      appWarnHandler,
      instance,
      11,
      [
        // eslint-disable-next-line no-restricted-syntax
        msg + args.map((a) => {
          var _a, _b2;
          return (_b2 = (_a = a.toString) == null ? void 0 : _a.call(a)) != null ? _b2 : JSON.stringify(a);
        }).join(""),
        instance && instance.proxy,
        trace.map(
          ({ vnode }) => `at <${formatComponentName(instance, vnode.type)}>`
        ).join("\n"),
        trace
      ]
    );
  } else {
    const warnArgs = [`[Vue warn]: ${msg}`, ...args];
    if (trace.length && // avoid spamming console during tests
    true) {
      warnArgs.push(`
`, ...formatTrace(trace));
    }
    console.warn(...warnArgs);
  }
  resetTracking();
  isWarning = false;
}
function getComponentTrace() {
  let currentVNode = stack$1[stack$1.length - 1];
  if (!currentVNode) {
    return [];
  }
  const normalizedStack = [];
  while (currentVNode) {
    const last = normalizedStack[0];
    if (last && last.vnode === currentVNode) {
      last.recurseCount++;
    } else {
      normalizedStack.push({
        vnode: currentVNode,
        recurseCount: 0
      });
    }
    const parentInstance = currentVNode.component && currentVNode.component.parent;
    currentVNode = parentInstance && parentInstance.vnode;
  }
  return normalizedStack;
}
function formatTrace(trace) {
  const logs = [];
  trace.forEach((entry, i) => {
    logs.push(...i === 0 ? [] : [`
`], ...formatTraceEntry(entry));
  });
  return logs;
}
function formatTraceEntry({ vnode, recurseCount }) {
  const postfix = recurseCount > 0 ? `... (${recurseCount} recursive calls)` : ``;
  const isRoot = vnode.component ? vnode.component.parent == null : false;
  const open = ` at <${formatComponentName(
    vnode.component,
    vnode.type,
    isRoot
  )}`;
  const close = `>` + postfix;
  return vnode.props ? [open, ...formatProps(vnode.props), close] : [open + close];
}
function formatProps(props) {
  const res = [];
  const keys = Object.keys(props);
  keys.slice(0, 3).forEach((key) => {
    res.push(...formatProp(key, props[key]));
  });
  if (keys.length > 3) {
    res.push(` ...`);
  }
  return res;
}
function formatProp(key, value, raw) {
  if (isString(value)) {
    value = JSON.stringify(value);
    return raw ? value : [`${key}=${value}`];
  } else if (typeof value === "number" || typeof value === "boolean" || value == null) {
    return raw ? value : [`${key}=${value}`];
  } else if (isRef(value)) {
    value = formatProp(key, toRaw(value.value), true);
    return raw ? value : [`${key}=Ref<`, value, `>`];
  } else if (isFunction(value)) {
    return [`${key}=fn${value.name ? `<${value.name}>` : ``}`];
  } else {
    value = toRaw(value);
    return raw ? value : [`${key}=`, value];
  }
}
function assertNumber(val, type) {
  return;
}
const ErrorCodes = {
  "SETUP_FUNCTION": 0,
  "0": "SETUP_FUNCTION",
  "RENDER_FUNCTION": 1,
  "1": "RENDER_FUNCTION",
  "NATIVE_EVENT_HANDLER": 5,
  "5": "NATIVE_EVENT_HANDLER",
  "COMPONENT_EVENT_HANDLER": 6,
  "6": "COMPONENT_EVENT_HANDLER",
  "VNODE_HOOK": 7,
  "7": "VNODE_HOOK",
  "DIRECTIVE_HOOK": 8,
  "8": "DIRECTIVE_HOOK",
  "TRANSITION_HOOK": 9,
  "9": "TRANSITION_HOOK",
  "APP_ERROR_HANDLER": 10,
  "10": "APP_ERROR_HANDLER",
  "APP_WARN_HANDLER": 11,
  "11": "APP_WARN_HANDLER",
  "FUNCTION_REF": 12,
  "12": "FUNCTION_REF",
  "ASYNC_COMPONENT_LOADER": 13,
  "13": "ASYNC_COMPONENT_LOADER",
  "SCHEDULER": 14,
  "14": "SCHEDULER",
  "COMPONENT_UPDATE": 15,
  "15": "COMPONENT_UPDATE",
  "APP_UNMOUNT_CLEANUP": 16,
  "16": "APP_UNMOUNT_CLEANUP"
};
const ErrorTypeStrings$1 = {
  ["sp"]: "serverPrefetch hook",
  ["bc"]: "beforeCreate hook",
  ["c"]: "created hook",
  ["bm"]: "beforeMount hook",
  ["m"]: "mounted hook",
  ["bu"]: "beforeUpdate hook",
  ["u"]: "updated",
  ["bum"]: "beforeUnmount hook",
  ["um"]: "unmounted hook",
  ["a"]: "activated hook",
  ["da"]: "deactivated hook",
  ["ec"]: "errorCaptured hook",
  ["rtc"]: "renderTracked hook",
  ["rtg"]: "renderTriggered hook",
  [0]: "setup function",
  [1]: "render function",
  [2]: "watcher getter",
  [3]: "watcher callback",
  [4]: "watcher cleanup function",
  [5]: "native event handler",
  [6]: "component event handler",
  [7]: "vnode hook",
  [8]: "directive hook",
  [9]: "transition hook",
  [10]: "app errorHandler",
  [11]: "app warnHandler",
  [12]: "ref function",
  [13]: "async component loader",
  [14]: "scheduler flush",
  [15]: "component update",
  [16]: "app unmount cleanup function"
};
function callWithErrorHandling(fn, instance, type, args) {
  try {
    return args ? fn(...args) : fn();
  } catch (err) {
    handleError(err, instance, type);
  }
}
function callWithAsyncErrorHandling(fn, instance, type, args) {
  if (isFunction(fn)) {
    const res = callWithErrorHandling(fn, instance, type, args);
    if (res && isPromise(res)) {
      res.catch((err) => {
        handleError(err, instance, type);
      });
    }
    return res;
  }
  if (isArray(fn)) {
    const values = [];
    for (let i = 0; i < fn.length; i++) {
      values.push(callWithAsyncErrorHandling(fn[i], instance, type, args));
    }
    return values;
  }
}
function handleError(err, instance, type, throwInDev = true) {
  const contextVNode = instance ? instance.vnode : null;
  const { errorHandler, throwUnhandledErrorInProduction } = instance && instance.appContext.config || EMPTY_OBJ;
  if (instance) {
    let cur = instance.parent;
    const exposedInstance = instance.proxy;
    const errorInfo = `https://vuejs.org/error-reference/#runtime-${type}`;
    while (cur) {
      const errorCapturedHooks = cur.ec;
      if (errorCapturedHooks) {
        for (let i = 0; i < errorCapturedHooks.length; i++) {
          if (errorCapturedHooks[i](err, exposedInstance, errorInfo) === false) {
            return;
          }
        }
      }
      cur = cur.parent;
    }
    if (errorHandler) {
      pauseTracking();
      callWithErrorHandling(errorHandler, null, 10, [
        err,
        exposedInstance,
        errorInfo
      ]);
      resetTracking();
      return;
    }
  }
  logError(err, type, contextVNode, throwInDev, throwUnhandledErrorInProduction);
}
function logError(err, type, contextVNode, throwInDev = true, throwInProd = false) {
  if (throwInProd) {
    throw err;
  } else {
    console.error(err);
  }
}
const queue = [];
let flushIndex = -1;
const pendingPostFlushCbs = [];
let activePostFlushCbs = null;
let postFlushIndex = 0;
const resolvedPromise = /* @__PURE__ */ Promise.resolve();
let currentFlushPromise = null;
function nextTick(fn) {
  const p2 = currentFlushPromise || resolvedPromise;
  return fn ? p2.then(this ? fn.bind(this) : fn) : p2;
}
function findInsertionIndex(id) {
  let start = flushIndex + 1;
  let end = queue.length;
  while (start < end) {
    const middle = start + end >>> 1;
    const middleJob = queue[middle];
    const middleJobId = getId(middleJob);
    if (middleJobId < id || middleJobId === id && middleJob.flags & 2) {
      start = middle + 1;
    } else {
      end = middle;
    }
  }
  return start;
}
function queueJob(job) {
  if (!(job.flags & 1)) {
    const jobId = getId(job);
    const lastJob = queue[queue.length - 1];
    if (!lastJob || // fast path when the job id is larger than the tail
    !(job.flags & 2) && jobId >= getId(lastJob)) {
      queue.push(job);
    } else {
      queue.splice(findInsertionIndex(jobId), 0, job);
    }
    job.flags |= 1;
    queueFlush();
  }
}
function queueFlush() {
  if (!currentFlushPromise) {
    currentFlushPromise = resolvedPromise.then(flushJobs);
  }
}
function queuePostFlushCb(cb2) {
  if (!isArray(cb2)) {
    if (activePostFlushCbs && cb2.id === -1) {
      activePostFlushCbs.splice(postFlushIndex + 1, 0, cb2);
    } else if (!(cb2.flags & 1)) {
      pendingPostFlushCbs.push(cb2);
      cb2.flags |= 1;
    }
  } else {
    pendingPostFlushCbs.push(...cb2);
  }
  queueFlush();
}
function flushPreFlushCbs(instance, seen2, i = flushIndex + 1) {
  for (; i < queue.length; i++) {
    const cb2 = queue[i];
    if (cb2 && cb2.flags & 2) {
      if (instance && cb2.id !== instance.uid) {
        continue;
      }
      queue.splice(i, 1);
      i--;
      if (cb2.flags & 4) {
        cb2.flags &= -2;
      }
      cb2();
      if (!(cb2.flags & 4)) {
        cb2.flags &= -2;
      }
    }
  }
}
function flushPostFlushCbs(seen2) {
  if (pendingPostFlushCbs.length) {
    const deduped = [...new Set(pendingPostFlushCbs)].sort(
      (a, b) => getId(a) - getId(b)
    );
    pendingPostFlushCbs.length = 0;
    if (activePostFlushCbs) {
      activePostFlushCbs.push(...deduped);
      return;
    }
    activePostFlushCbs = deduped;
    for (postFlushIndex = 0; postFlushIndex < activePostFlushCbs.length; postFlushIndex++) {
      const cb2 = activePostFlushCbs[postFlushIndex];
      if (cb2.flags & 4) {
        cb2.flags &= -2;
      }
      if (!(cb2.flags & 8)) cb2();
      cb2.flags &= -2;
    }
    activePostFlushCbs = null;
    postFlushIndex = 0;
  }
}
const getId = (job) => job.id == null ? job.flags & 2 ? -1 : Infinity : job.id;
function flushJobs(seen2) {
  try {
    for (flushIndex = 0; flushIndex < queue.length; flushIndex++) {
      const job = queue[flushIndex];
      if (job && !(job.flags & 8)) {
        if (false) ;
        if (job.flags & 4) {
          job.flags &= ~1;
        }
        callWithErrorHandling(
          job,
          job.i,
          job.i ? 15 : 14
        );
        if (!(job.flags & 4)) {
          job.flags &= ~1;
        }
      }
    }
  } finally {
    for (; flushIndex < queue.length; flushIndex++) {
      const job = queue[flushIndex];
      if (job) {
        job.flags &= -2;
      }
    }
    flushIndex = -1;
    queue.length = 0;
    flushPostFlushCbs();
    currentFlushPromise = null;
    if (queue.length || pendingPostFlushCbs.length) {
      flushJobs();
    }
  }
}
let devtools$1;
let buffer = [];
let devtoolsNotInstalled = false;
function emit$1(event, ...args) {
  if (devtools$1) {
    devtools$1.emit(event, ...args);
  } else if (!devtoolsNotInstalled) {
    buffer.push({ event, args });
  }
}
function setDevtoolsHook$1(hook, target) {
  var _a, _b2;
  devtools$1 = hook;
  if (devtools$1) {
    devtools$1.enabled = true;
    buffer.forEach(({ event, args }) => devtools$1.emit(event, ...args));
    buffer = [];
  } else if (
    // handle late devtools injection - only do this if we are in an actual
    // browser environment to avoid the timer handle stalling test runner exit
    // (#4815)
    typeof window !== "undefined" && // some envs mock window but not fully
    window.HTMLElement && // also exclude jsdom
    // eslint-disable-next-line no-restricted-syntax
    !((_b2 = (_a = window.navigator) == null ? void 0 : _a.userAgent) == null ? void 0 : _b2.includes("jsdom"))
  ) {
    const replay = target.__VUE_DEVTOOLS_HOOK_REPLAY__ = target.__VUE_DEVTOOLS_HOOK_REPLAY__ || [];
    replay.push((newHook) => {
      setDevtoolsHook$1(newHook, target);
    });
    setTimeout(() => {
      if (!devtools$1) {
        target.__VUE_DEVTOOLS_HOOK_REPLAY__ = null;
        devtoolsNotInstalled = true;
        buffer = [];
      }
    }, 3e3);
  } else {
    devtoolsNotInstalled = true;
    buffer = [];
  }
}
function devtoolsInitApp(app, version2) {
  emit$1("app:init", app, version2, {
    Fragment,
    Text,
    Comment,
    Static
  });
}
function devtoolsUnmountApp(app) {
  emit$1("app:unmount", app);
}
const devtoolsComponentAdded = /* @__PURE__ */ createDevtoolsComponentHook(
  "component:added"
  /* COMPONENT_ADDED */
);
const devtoolsComponentUpdated = /* @__PURE__ */ createDevtoolsComponentHook(
  "component:updated"
  /* COMPONENT_UPDATED */
);
const _devtoolsComponentRemoved = /* @__PURE__ */ createDevtoolsComponentHook(
  "component:removed"
  /* COMPONENT_REMOVED */
);
const devtoolsComponentRemoved = (component) => {
  if (devtools$1 && typeof devtools$1.cleanupBuffer === "function" && // remove the component if it wasn't buffered
  !devtools$1.cleanupBuffer(component)) {
    _devtoolsComponentRemoved(component);
  }
};
// @__NO_SIDE_EFFECTS__
function createDevtoolsComponentHook(hook) {
  return (component) => {
    emit$1(
      hook,
      component.appContext.app,
      component.uid,
      component.parent ? component.parent.uid : void 0,
      component
    );
  };
}
function devtoolsComponentEmit(component, event, params) {
  emit$1(
    "component:emit",
    component.appContext.app,
    component,
    event,
    params
  );
}
let currentRenderingInstance = null;
let currentScopeId = null;
function setCurrentRenderingInstance(instance) {
  const prev = currentRenderingInstance;
  currentRenderingInstance = instance;
  currentScopeId = instance && instance.type.__scopeId || null;
  return prev;
}
function pushScopeId(id) {
  currentScopeId = id;
}
function popScopeId() {
  currentScopeId = null;
}
const withScopeId = (_id) => withCtx;
function withCtx(fn, ctx = currentRenderingInstance, isNonScopedSlot) {
  if (!ctx) return fn;
  if (fn._n) {
    return fn;
  }
  const renderFnWithContext = (...args) => {
    if (renderFnWithContext._d) {
      setBlockTracking(-1);
    }
    const prevInstance = setCurrentRenderingInstance(ctx);
    let res;
    try {
      res = fn(...args);
    } finally {
      setCurrentRenderingInstance(prevInstance);
      if (renderFnWithContext._d) {
        setBlockTracking(1);
      }
    }
    {
      devtoolsComponentUpdated(ctx);
    }
    return res;
  };
  renderFnWithContext._n = true;
  renderFnWithContext._c = true;
  renderFnWithContext._d = true;
  return renderFnWithContext;
}
function withDirectives(vnode, directives) {
  if (currentRenderingInstance === null) {
    return vnode;
  }
  const instance = getComponentPublicInstance(currentRenderingInstance);
  const bindings = vnode.dirs || (vnode.dirs = []);
  for (let i = 0; i < directives.length; i++) {
    let [dir, value, arg, modifiers = EMPTY_OBJ] = directives[i];
    if (dir) {
      if (isFunction(dir)) {
        dir = {
          mounted: dir,
          updated: dir
        };
      }
      if (dir.deep) {
        traverse(value);
      }
      bindings.push({
        dir,
        instance,
        value,
        oldValue: void 0,
        arg,
        modifiers
      });
    }
  }
  return vnode;
}
function invokeDirectiveHook(vnode, prevVNode, instance, name2) {
  const bindings = vnode.dirs;
  const oldBindings = prevVNode && prevVNode.dirs;
  for (let i = 0; i < bindings.length; i++) {
    const binding = bindings[i];
    if (oldBindings) {
      binding.oldValue = oldBindings[i].value;
    }
    let hook = binding.dir[name2];
    if (hook) {
      pauseTracking();
      callWithAsyncErrorHandling(hook, instance, 8, [
        vnode.el,
        binding,
        vnode,
        prevVNode
      ]);
      resetTracking();
    }
  }
}
const TeleportEndKey = Symbol("_vte");
const isTeleport = (type) => type.__isTeleport;
const isTeleportDisabled = (props) => props && (props.disabled || props.disabled === "");
const isTeleportDeferred = (props) => props && (props.defer || props.defer === "");
const isTargetSVG = (target) => typeof SVGElement !== "undefined" && target instanceof SVGElement;
const isTargetMathML = (target) => typeof MathMLElement === "function" && target instanceof MathMLElement;
const resolveTarget = (props, select) => {
  const targetSelector = props && props.to;
  if (isString(targetSelector)) {
    if (!select) {
      return null;
    } else {
      const target = select(targetSelector);
      return target;
    }
  } else {
    return targetSelector;
  }
};
const TeleportImpl = {
  name: "Teleport",
  __isTeleport: true,
  process(n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized, internals) {
    const {
      mc: mountChildren,
      pc: patchChildren,
      pbc: patchBlockChildren,
      o: { insert, querySelector, createText, createComment }
    } = internals;
    const disabled = isTeleportDisabled(n2.props);
    let { shapeFlag, children, dynamicChildren } = n2;
    if (n1 == null) {
      const placeholder = n2.el = createText("");
      const mainAnchor = n2.anchor = createText("");
      insert(placeholder, container, anchor);
      insert(mainAnchor, container, anchor);
      const mount = (container2, anchor2) => {
        if (shapeFlag & 16) {
          mountChildren(
            children,
            container2,
            anchor2,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        }
      };
      const mountToTarget = () => {
        const target = n2.target = resolveTarget(n2.props, querySelector);
        const targetAnchor = prepareAnchor(target, n2, createText, insert);
        if (target) {
          if (namespace !== "svg" && isTargetSVG(target)) {
            namespace = "svg";
          } else if (namespace !== "mathml" && isTargetMathML(target)) {
            namespace = "mathml";
          }
          if (parentComponent && parentComponent.isCE) {
            (parentComponent.ce._teleportTargets || (parentComponent.ce._teleportTargets = /* @__PURE__ */ new Set())).add(target);
          }
          if (!disabled) {
            mount(target, targetAnchor);
            updateCssVars(n2, false);
          }
        }
      };
      if (disabled) {
        mount(container, mainAnchor);
        updateCssVars(n2, true);
      }
      if (isTeleportDeferred(n2.props)) {
        n2.el.__isMounted = false;
        queuePostRenderEffect(() => {
          mountToTarget();
          delete n2.el.__isMounted;
        }, parentSuspense);
      } else {
        mountToTarget();
      }
    } else {
      if (isTeleportDeferred(n2.props) && n1.el.__isMounted === false) {
        queuePostRenderEffect(() => {
          TeleportImpl.process(
            n1,
            n2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized,
            internals
          );
        }, parentSuspense);
        return;
      }
      n2.el = n1.el;
      n2.targetStart = n1.targetStart;
      const mainAnchor = n2.anchor = n1.anchor;
      const target = n2.target = n1.target;
      const targetAnchor = n2.targetAnchor = n1.targetAnchor;
      const wasDisabled = isTeleportDisabled(n1.props);
      const currentContainer = wasDisabled ? container : target;
      const currentAnchor = wasDisabled ? mainAnchor : targetAnchor;
      if (namespace === "svg" || isTargetSVG(target)) {
        namespace = "svg";
      } else if (namespace === "mathml" || isTargetMathML(target)) {
        namespace = "mathml";
      }
      if (dynamicChildren) {
        patchBlockChildren(
          n1.dynamicChildren,
          dynamicChildren,
          currentContainer,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds
        );
        traverseStaticChildren(n1, n2, true);
      } else if (!optimized) {
        patchChildren(
          n1,
          n2,
          currentContainer,
          currentAnchor,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          false
        );
      }
      if (disabled) {
        if (!wasDisabled) {
          moveTeleport(
            n2,
            container,
            mainAnchor,
            internals,
            1
          );
        } else {
          if (n2.props && n1.props && n2.props.to !== n1.props.to) {
            n2.props.to = n1.props.to;
          }
        }
      } else {
        if ((n2.props && n2.props.to) !== (n1.props && n1.props.to)) {
          const nextTarget = n2.target = resolveTarget(
            n2.props,
            querySelector
          );
          if (nextTarget) {
            moveTeleport(
              n2,
              nextTarget,
              null,
              internals,
              0
            );
          }
        } else if (wasDisabled) {
          moveTeleport(
            n2,
            target,
            targetAnchor,
            internals,
            1
          );
        }
      }
      updateCssVars(n2, disabled);
    }
  },
  remove(vnode, parentComponent, parentSuspense, { um: unmount, o: { remove: hostRemove } }, doRemove) {
    const {
      shapeFlag,
      children,
      anchor,
      targetStart,
      targetAnchor,
      target,
      props
    } = vnode;
    if (target) {
      hostRemove(targetStart);
      hostRemove(targetAnchor);
    }
    doRemove && hostRemove(anchor);
    if (shapeFlag & 16) {
      const shouldRemove = doRemove || !isTeleportDisabled(props);
      for (let i = 0; i < children.length; i++) {
        const child = children[i];
        unmount(
          child,
          parentComponent,
          parentSuspense,
          shouldRemove,
          !!child.dynamicChildren
        );
      }
    }
  },
  move: moveTeleport,
  hydrate: hydrateTeleport
};
function moveTeleport(vnode, container, parentAnchor, { o: { insert }, m: move }, moveType = 2) {
  if (moveType === 0) {
    insert(vnode.targetAnchor, container, parentAnchor);
  }
  const { el: el2, anchor, shapeFlag, children, props } = vnode;
  const isReorder = moveType === 2;
  if (isReorder) {
    insert(el2, container, parentAnchor);
  }
  if (!isReorder || isTeleportDisabled(props)) {
    if (shapeFlag & 16) {
      for (let i = 0; i < children.length; i++) {
        move(
          children[i],
          container,
          parentAnchor,
          2
        );
      }
    }
  }
  if (isReorder) {
    insert(anchor, container, parentAnchor);
  }
}
function hydrateTeleport(node, vnode, parentComponent, parentSuspense, slotScopeIds, optimized, {
  o: { nextSibling, parentNode, querySelector, insert, createText }
}, hydrateChildren) {
  function hydrateDisabledTeleport(node2, vnode2, targetStart, targetAnchor) {
    vnode2.anchor = hydrateChildren(
      nextSibling(node2),
      vnode2,
      parentNode(node2),
      parentComponent,
      parentSuspense,
      slotScopeIds,
      optimized
    );
    vnode2.targetStart = targetStart;
    vnode2.targetAnchor = targetAnchor;
  }
  const target = vnode.target = resolveTarget(
    vnode.props,
    querySelector
  );
  const disabled = isTeleportDisabled(vnode.props);
  if (target) {
    const targetNode = target._lpa || target.firstChild;
    if (vnode.shapeFlag & 16) {
      if (disabled) {
        hydrateDisabledTeleport(
          node,
          vnode,
          targetNode,
          targetNode && nextSibling(targetNode)
        );
      } else {
        vnode.anchor = nextSibling(node);
        let targetAnchor = targetNode;
        while (targetAnchor) {
          if (targetAnchor && targetAnchor.nodeType === 8) {
            if (targetAnchor.data === "teleport start anchor") {
              vnode.targetStart = targetAnchor;
            } else if (targetAnchor.data === "teleport anchor") {
              vnode.targetAnchor = targetAnchor;
              target._lpa = vnode.targetAnchor && nextSibling(vnode.targetAnchor);
              break;
            }
          }
          targetAnchor = nextSibling(targetAnchor);
        }
        if (!vnode.targetAnchor) {
          prepareAnchor(target, vnode, createText, insert);
        }
        hydrateChildren(
          targetNode && nextSibling(targetNode),
          vnode,
          target,
          parentComponent,
          parentSuspense,
          slotScopeIds,
          optimized
        );
      }
    }
    updateCssVars(vnode, disabled);
  } else if (disabled) {
    if (vnode.shapeFlag & 16) {
      hydrateDisabledTeleport(node, vnode, node, nextSibling(node));
    }
  }
  return vnode.anchor && nextSibling(vnode.anchor);
}
const Teleport = TeleportImpl;
function updateCssVars(vnode, isDisabled) {
  const ctx = vnode.ctx;
  if (ctx && ctx.ut) {
    let node, anchor;
    if (isDisabled) {
      node = vnode.el;
      anchor = vnode.anchor;
    } else {
      node = vnode.targetStart;
      anchor = vnode.targetAnchor;
    }
    while (node && node !== anchor) {
      if (node.nodeType === 1) node.setAttribute("data-v-owner", ctx.uid);
      node = node.nextSibling;
    }
    ctx.ut();
  }
}
function prepareAnchor(target, vnode, createText, insert) {
  const targetStart = vnode.targetStart = createText("");
  const targetAnchor = vnode.targetAnchor = createText("");
  targetStart[TeleportEndKey] = targetAnchor;
  if (target) {
    insert(targetStart, target);
    insert(targetAnchor, target);
  }
  return targetAnchor;
}
const leaveCbKey = Symbol("_leaveCb");
const enterCbKey$1 = Symbol("_enterCb");
function useTransitionState() {
  const state = {
    isMounted: false,
    isLeaving: false,
    isUnmounting: false,
    leavingVNodes: /* @__PURE__ */ new Map()
  };
  onMounted(() => {
    state.isMounted = true;
  });
  onBeforeUnmount(() => {
    state.isUnmounting = true;
  });
  return state;
}
const TransitionHookValidator = [Function, Array];
const BaseTransitionPropsValidators = {
  mode: String,
  appear: Boolean,
  persisted: Boolean,
  // enter
  onBeforeEnter: TransitionHookValidator,
  onEnter: TransitionHookValidator,
  onAfterEnter: TransitionHookValidator,
  onEnterCancelled: TransitionHookValidator,
  // leave
  onBeforeLeave: TransitionHookValidator,
  onLeave: TransitionHookValidator,
  onAfterLeave: TransitionHookValidator,
  onLeaveCancelled: TransitionHookValidator,
  // appear
  onBeforeAppear: TransitionHookValidator,
  onAppear: TransitionHookValidator,
  onAfterAppear: TransitionHookValidator,
  onAppearCancelled: TransitionHookValidator
};
const recursiveGetSubtree = (instance) => {
  const subTree = instance.subTree;
  return subTree.component ? recursiveGetSubtree(subTree.component) : subTree;
};
const BaseTransitionImpl = {
  name: `BaseTransition`,
  props: BaseTransitionPropsValidators,
  setup(props, { slots }) {
    const instance = getCurrentInstance();
    const state = useTransitionState();
    return () => {
      const children = slots.default && getTransitionRawChildren(slots.default(), true);
      if (!children || !children.length) {
        return;
      }
      const child = findNonCommentChild(children);
      const rawProps = toRaw(props);
      const { mode } = rawProps;
      if (state.isLeaving) {
        return emptyPlaceholder(child);
      }
      const innerChild = getInnerChild$1(child);
      if (!innerChild) {
        return emptyPlaceholder(child);
      }
      let enterHooks = resolveTransitionHooks(
        innerChild,
        rawProps,
        state,
        instance,
        // #11061, ensure enterHooks is fresh after clone
        (hooks) => enterHooks = hooks
      );
      if (innerChild.type !== Comment) {
        setTransitionHooks(innerChild, enterHooks);
      }
      let oldInnerChild = instance.subTree && getInnerChild$1(instance.subTree);
      if (oldInnerChild && oldInnerChild.type !== Comment && !isSameVNodeType(oldInnerChild, innerChild) && recursiveGetSubtree(instance).type !== Comment) {
        let leavingHooks = resolveTransitionHooks(
          oldInnerChild,
          rawProps,
          state,
          instance
        );
        setTransitionHooks(oldInnerChild, leavingHooks);
        if (mode === "out-in" && innerChild.type !== Comment) {
          state.isLeaving = true;
          leavingHooks.afterLeave = () => {
            state.isLeaving = false;
            if (!(instance.job.flags & 8)) {
              instance.update();
            }
            delete leavingHooks.afterLeave;
            oldInnerChild = void 0;
          };
          return emptyPlaceholder(child);
        } else if (mode === "in-out" && innerChild.type !== Comment) {
          leavingHooks.delayLeave = (el2, earlyRemove, delayedLeave) => {
            const leavingVNodesCache = getLeavingNodesForType(
              state,
              oldInnerChild
            );
            leavingVNodesCache[String(oldInnerChild.key)] = oldInnerChild;
            el2[leaveCbKey] = () => {
              earlyRemove();
              el2[leaveCbKey] = void 0;
              delete enterHooks.delayedLeave;
              oldInnerChild = void 0;
            };
            enterHooks.delayedLeave = () => {
              delayedLeave();
              delete enterHooks.delayedLeave;
              oldInnerChild = void 0;
            };
          };
        } else {
          oldInnerChild = void 0;
        }
      } else if (oldInnerChild) {
        oldInnerChild = void 0;
      }
      return child;
    };
  }
};
function findNonCommentChild(children) {
  let child = children[0];
  if (children.length > 1) {
    for (const c of children) {
      if (c.type !== Comment) {
        child = c;
        break;
      }
    }
  }
  return child;
}
const BaseTransition = BaseTransitionImpl;
function getLeavingNodesForType(state, vnode) {
  const { leavingVNodes } = state;
  let leavingVNodesCache = leavingVNodes.get(vnode.type);
  if (!leavingVNodesCache) {
    leavingVNodesCache = /* @__PURE__ */ Object.create(null);
    leavingVNodes.set(vnode.type, leavingVNodesCache);
  }
  return leavingVNodesCache;
}
function resolveTransitionHooks(vnode, props, state, instance, postClone) {
  const {
    appear,
    mode,
    persisted = false,
    onBeforeEnter,
    onEnter,
    onAfterEnter,
    onEnterCancelled,
    onBeforeLeave,
    onLeave,
    onAfterLeave,
    onLeaveCancelled,
    onBeforeAppear,
    onAppear,
    onAfterAppear,
    onAppearCancelled
  } = props;
  const key = String(vnode.key);
  const leavingVNodesCache = getLeavingNodesForType(state, vnode);
  const callHook2 = (hook, args) => {
    hook && callWithAsyncErrorHandling(
      hook,
      instance,
      9,
      args
    );
  };
  const callAsyncHook = (hook, args) => {
    const done = args[1];
    callHook2(hook, args);
    if (isArray(hook)) {
      if (hook.every((hook2) => hook2.length <= 1)) done();
    } else if (hook.length <= 1) {
      done();
    }
  };
  const hooks = {
    mode,
    persisted,
    beforeEnter(el2) {
      let hook = onBeforeEnter;
      if (!state.isMounted) {
        if (appear) {
          hook = onBeforeAppear || onBeforeEnter;
        } else {
          return;
        }
      }
      if (el2[leaveCbKey]) {
        el2[leaveCbKey](
          true
          /* cancelled */
        );
      }
      const leavingVNode = leavingVNodesCache[key];
      if (leavingVNode && isSameVNodeType(vnode, leavingVNode) && leavingVNode.el[leaveCbKey]) {
        leavingVNode.el[leaveCbKey]();
      }
      callHook2(hook, [el2]);
    },
    enter(el2) {
      let hook = onEnter;
      let afterHook = onAfterEnter;
      let cancelHook = onEnterCancelled;
      if (!state.isMounted) {
        if (appear) {
          hook = onAppear || onEnter;
          afterHook = onAfterAppear || onAfterEnter;
          cancelHook = onAppearCancelled || onEnterCancelled;
        } else {
          return;
        }
      }
      let called = false;
      const done = el2[enterCbKey$1] = (cancelled) => {
        if (called) return;
        called = true;
        if (cancelled) {
          callHook2(cancelHook, [el2]);
        } else {
          callHook2(afterHook, [el2]);
        }
        if (hooks.delayedLeave) {
          hooks.delayedLeave();
        }
        el2[enterCbKey$1] = void 0;
      };
      if (hook) {
        callAsyncHook(hook, [el2, done]);
      } else {
        done();
      }
    },
    leave(el2, remove2) {
      const key2 = String(vnode.key);
      if (el2[enterCbKey$1]) {
        el2[enterCbKey$1](
          true
          /* cancelled */
        );
      }
      if (state.isUnmounting) {
        return remove2();
      }
      callHook2(onBeforeLeave, [el2]);
      let called = false;
      const done = el2[leaveCbKey] = (cancelled) => {
        if (called) return;
        called = true;
        remove2();
        if (cancelled) {
          callHook2(onLeaveCancelled, [el2]);
        } else {
          callHook2(onAfterLeave, [el2]);
        }
        el2[leaveCbKey] = void 0;
        if (leavingVNodesCache[key2] === vnode) {
          delete leavingVNodesCache[key2];
        }
      };
      leavingVNodesCache[key2] = vnode;
      if (onLeave) {
        callAsyncHook(onLeave, [el2, done]);
      } else {
        done();
      }
    },
    clone(vnode2) {
      const hooks2 = resolveTransitionHooks(
        vnode2,
        props,
        state,
        instance,
        postClone
      );
      if (postClone) postClone(hooks2);
      return hooks2;
    }
  };
  return hooks;
}
function emptyPlaceholder(vnode) {
  if (isKeepAlive(vnode)) {
    vnode = cloneVNode(vnode);
    vnode.children = null;
    return vnode;
  }
}
function getInnerChild$1(vnode) {
  if (!isKeepAlive(vnode)) {
    if (isTeleport(vnode.type) && vnode.children) {
      return findNonCommentChild(vnode.children);
    }
    return vnode;
  }
  if (vnode.component) {
    return vnode.component.subTree;
  }
  const { shapeFlag, children } = vnode;
  if (children) {
    if (shapeFlag & 16) {
      return children[0];
    }
    if (shapeFlag & 32 && isFunction(children.default)) {
      return children.default();
    }
  }
}
function setTransitionHooks(vnode, hooks) {
  if (vnode.shapeFlag & 6 && vnode.component) {
    vnode.transition = hooks;
    setTransitionHooks(vnode.component.subTree, hooks);
  } else if (vnode.shapeFlag & 128) {
    vnode.ssContent.transition = hooks.clone(vnode.ssContent);
    vnode.ssFallback.transition = hooks.clone(vnode.ssFallback);
  } else {
    vnode.transition = hooks;
  }
}
function getTransitionRawChildren(children, keepComment = false, parentKey) {
  let ret = [];
  let keyedFragmentCount = 0;
  for (let i = 0; i < children.length; i++) {
    let child = children[i];
    const key = parentKey == null ? child.key : String(parentKey) + String(child.key != null ? child.key : i);
    if (child.type === Fragment) {
      if (child.patchFlag & 128) keyedFragmentCount++;
      ret = ret.concat(
        getTransitionRawChildren(child.children, keepComment, key)
      );
    } else if (keepComment || child.type !== Comment) {
      ret.push(key != null ? cloneVNode(child, { key }) : child);
    }
  }
  if (keyedFragmentCount > 1) {
    for (let i = 0; i < ret.length; i++) {
      ret[i].patchFlag = -2;
    }
  }
  return ret;
}
// @__NO_SIDE_EFFECTS__
function defineComponent(options, extraOptions) {
  return isFunction(options) ? (
    // #8236: extend call and options.name access are considered side-effects
    // by Rollup, so we have to wrap it in a pure-annotated IIFE.
    /* @__PURE__ */ (() => extend({ name: options.name }, extraOptions, { setup: options }))()
  ) : options;
}
function useId() {
  const i = getCurrentInstance();
  if (i) {
    return (i.appContext.config.idPrefix || "v") + "-" + i.ids[0] + i.ids[1]++;
  }
  return "";
}
function markAsyncBoundary(instance) {
  instance.ids = [instance.ids[0] + instance.ids[2]++ + "-", 0, 0];
}
function useTemplateRef(key) {
  const i = getCurrentInstance();
  const r = shallowRef(null);
  if (i) {
    const refs = i.refs === EMPTY_OBJ ? i.refs = {} : i.refs;
    {
      Object.defineProperty(refs, key, {
        enumerable: true,
        get: () => r.value,
        set: (val) => r.value = val
      });
    }
  }
  const ret = r;
  return ret;
}
const pendingSetRefMap = /* @__PURE__ */ new WeakMap();
function setRef(rawRef, oldRawRef, parentSuspense, vnode, isUnmount = false) {
  if (isArray(rawRef)) {
    rawRef.forEach(
      (r, i) => setRef(
        r,
        oldRawRef && (isArray(oldRawRef) ? oldRawRef[i] : oldRawRef),
        parentSuspense,
        vnode,
        isUnmount
      )
    );
    return;
  }
  if (isAsyncWrapper(vnode) && !isUnmount) {
    if (vnode.shapeFlag & 512 && vnode.type.__asyncResolved && vnode.component.subTree.component) {
      setRef(rawRef, oldRawRef, parentSuspense, vnode.component.subTree);
    }
    return;
  }
  const refValue = vnode.shapeFlag & 4 ? getComponentPublicInstance(vnode.component) : vnode.el;
  const value = isUnmount ? null : refValue;
  const { i: owner, r: ref3 } = rawRef;
  const oldRef = oldRawRef && oldRawRef.r;
  const refs = owner.refs === EMPTY_OBJ ? owner.refs = {} : owner.refs;
  const setupState = owner.setupState;
  const rawSetupState = toRaw(setupState);
  const canSetSetupRef = setupState === EMPTY_OBJ ? NO : (key) => {
    return hasOwn(rawSetupState, key);
  };
  if (oldRef != null && oldRef !== ref3) {
    invalidatePendingSetRef(oldRawRef);
    if (isString(oldRef)) {
      refs[oldRef] = null;
      if (canSetSetupRef(oldRef)) {
        setupState[oldRef] = null;
      }
    } else if (isRef(oldRef)) {
      {
        oldRef.value = null;
      }
      const oldRawRefAtom = oldRawRef;
      if (oldRawRefAtom.k) refs[oldRawRefAtom.k] = null;
    }
  }
  if (isFunction(ref3)) {
    callWithErrorHandling(ref3, owner, 12, [value, refs]);
  } else {
    const _isString = isString(ref3);
    const _isRef = isRef(ref3);
    if (_isString || _isRef) {
      const doSet = () => {
        if (rawRef.f) {
          const existing = _isString ? canSetSetupRef(ref3) ? setupState[ref3] : refs[ref3] : ref3.value;
          if (isUnmount) {
            isArray(existing) && remove(existing, refValue);
          } else {
            if (!isArray(existing)) {
              if (_isString) {
                refs[ref3] = [refValue];
                if (canSetSetupRef(ref3)) {
                  setupState[ref3] = refs[ref3];
                }
              } else {
                const newVal = [refValue];
                {
                  ref3.value = newVal;
                }
                if (rawRef.k) refs[rawRef.k] = newVal;
              }
            } else if (!existing.includes(refValue)) {
              existing.push(refValue);
            }
          }
        } else if (_isString) {
          refs[ref3] = value;
          if (canSetSetupRef(ref3)) {
            setupState[ref3] = value;
          }
        } else if (_isRef) {
          {
            ref3.value = value;
          }
          if (rawRef.k) refs[rawRef.k] = value;
        } else ;
      };
      if (value) {
        const job = () => {
          doSet();
          pendingSetRefMap.delete(rawRef);
        };
        job.id = -1;
        pendingSetRefMap.set(rawRef, job);
        queuePostRenderEffect(job, parentSuspense);
      } else {
        invalidatePendingSetRef(rawRef);
        doSet();
      }
    }
  }
}
function invalidatePendingSetRef(rawRef) {
  const pendingSetRef = pendingSetRefMap.get(rawRef);
  if (pendingSetRef) {
    pendingSetRef.flags |= 8;
    pendingSetRefMap.delete(rawRef);
  }
}
let hasLoggedMismatchError = false;
const logMismatchError = () => {
  if (hasLoggedMismatchError) {
    return;
  }
  console.error("Hydration completed but contains mismatches.");
  hasLoggedMismatchError = true;
};
const isSVGContainer = (container) => container.namespaceURI.includes("svg") && container.tagName !== "foreignObject";
const isMathMLContainer = (container) => container.namespaceURI.includes("MathML");
const getContainerType = (container) => {
  if (container.nodeType !== 1) return void 0;
  if (isSVGContainer(container)) return "svg";
  if (isMathMLContainer(container)) return "mathml";
  return void 0;
};
const isComment = (node) => node.nodeType === 8;
function createHydrationFunctions(rendererInternals) {
  const {
    mt: mountComponent,
    p: patch,
    o: {
      patchProp: patchProp2,
      createText,
      nextSibling,
      parentNode,
      remove: remove2,
      insert,
      createComment
    }
  } = rendererInternals;
  const hydrate2 = (vnode, container) => {
    if (!container.hasChildNodes()) {
      warn$1(
        `Attempting to hydrate existing markup but container is empty. Performing full mount instead.`
      );
      patch(null, vnode, container);
      flushPostFlushCbs();
      container._vnode = vnode;
      return;
    }
    hydrateNode(container.firstChild, vnode, null, null, null);
    flushPostFlushCbs();
    container._vnode = vnode;
  };
  const hydrateNode = (node, vnode, parentComponent, parentSuspense, slotScopeIds, optimized = false) => {
    optimized = optimized || !!vnode.dynamicChildren;
    const isFragmentStart = isComment(node) && node.data === "[";
    const onMismatch = () => handleMismatch(
      node,
      vnode,
      parentComponent,
      parentSuspense,
      slotScopeIds,
      isFragmentStart
    );
    const { type, ref: ref3, shapeFlag, patchFlag } = vnode;
    let domType = node.nodeType;
    vnode.el = node;
    {
      def(node, "__vnode", vnode, true);
      def(node, "__vueParentComponent", parentComponent, true);
    }
    if (patchFlag === -2) {
      optimized = false;
      vnode.dynamicChildren = null;
    }
    let nextNode = null;
    switch (type) {
      case Text:
        if (domType !== 3) {
          if (vnode.children === "") {
            insert(vnode.el = createText(""), parentNode(node), node);
            nextNode = node;
          } else {
            nextNode = onMismatch();
          }
        } else {
          if (node.data !== vnode.children) {
            warn$1(
              `Hydration text mismatch in`,
              node.parentNode,
              `
  - rendered on server: ${JSON.stringify(
                node.data
              )}
  - expected on client: ${JSON.stringify(vnode.children)}`
            );
            logMismatchError();
            node.data = vnode.children;
          }
          nextNode = nextSibling(node);
        }
        break;
      case Comment:
        if (isTemplateNode2(node)) {
          nextNode = nextSibling(node);
          replaceNode(
            vnode.el = node.content.firstChild,
            node,
            parentComponent
          );
        } else if (domType !== 8 || isFragmentStart) {
          nextNode = onMismatch();
        } else {
          nextNode = nextSibling(node);
        }
        break;
      case Static:
        if (isFragmentStart) {
          node = nextSibling(node);
          domType = node.nodeType;
        }
        if (domType === 1 || domType === 3) {
          nextNode = node;
          const needToAdoptContent = !vnode.children.length;
          for (let i = 0; i < vnode.staticCount; i++) {
            if (needToAdoptContent)
              vnode.children += nextNode.nodeType === 1 ? nextNode.outerHTML : nextNode.data;
            if (i === vnode.staticCount - 1) {
              vnode.anchor = nextNode;
            }
            nextNode = nextSibling(nextNode);
          }
          return isFragmentStart ? nextSibling(nextNode) : nextNode;
        } else {
          onMismatch();
        }
        break;
      case Fragment:
        if (!isFragmentStart) {
          nextNode = onMismatch();
        } else {
          nextNode = hydrateFragment(
            node,
            vnode,
            parentComponent,
            parentSuspense,
            slotScopeIds,
            optimized
          );
        }
        break;
      default:
        if (shapeFlag & 1) {
          if ((domType !== 1 || vnode.type.toLowerCase() !== node.tagName.toLowerCase()) && !isTemplateNode2(node)) {
            nextNode = onMismatch();
          } else {
            nextNode = hydrateElement(
              node,
              vnode,
              parentComponent,
              parentSuspense,
              slotScopeIds,
              optimized
            );
          }
        } else if (shapeFlag & 6) {
          vnode.slotScopeIds = slotScopeIds;
          const container = parentNode(node);
          if (isFragmentStart) {
            nextNode = locateClosingAnchor(node);
          } else if (isComment(node) && node.data === "teleport start") {
            nextNode = locateClosingAnchor(node, node.data, "teleport end");
          } else {
            nextNode = nextSibling(node);
          }
          mountComponent(
            vnode,
            container,
            null,
            parentComponent,
            parentSuspense,
            getContainerType(container),
            optimized
          );
          if (isAsyncWrapper(vnode) && !vnode.type.__asyncResolved) {
            let subTree;
            if (isFragmentStart) {
              subTree = createVNode(Fragment);
              subTree.anchor = nextNode ? nextNode.previousSibling : container.lastChild;
            } else {
              subTree = node.nodeType === 3 ? createTextVNode("") : createVNode("div");
            }
            subTree.el = node;
            vnode.component.subTree = subTree;
          }
        } else if (shapeFlag & 64) {
          if (domType !== 8) {
            nextNode = onMismatch();
          } else {
            nextNode = vnode.type.hydrate(
              node,
              vnode,
              parentComponent,
              parentSuspense,
              slotScopeIds,
              optimized,
              rendererInternals,
              hydrateChildren
            );
          }
        } else if (shapeFlag & 128) {
          nextNode = vnode.type.hydrate(
            node,
            vnode,
            parentComponent,
            parentSuspense,
            getContainerType(parentNode(node)),
            slotScopeIds,
            optimized,
            rendererInternals,
            hydrateNode
          );
        } else {
          warn$1("Invalid HostVNode type:", type, `(${typeof type})`);
        }
    }
    if (ref3 != null) {
      setRef(ref3, null, parentSuspense, vnode);
    }
    return nextNode;
  };
  const hydrateElement = (el2, vnode, parentComponent, parentSuspense, slotScopeIds, optimized) => {
    optimized = optimized || !!vnode.dynamicChildren;
    const { type, props, patchFlag, shapeFlag, dirs, transition } = vnode;
    const forcePatch = type === "input" || type === "option";
    if (forcePatch || patchFlag !== -1) {
      if (dirs) {
        invokeDirectiveHook(vnode, null, parentComponent, "created");
      }
      let needCallTransitionHooks = false;
      if (isTemplateNode2(el2)) {
        needCallTransitionHooks = needTransition(
          null,
          // no need check parentSuspense in hydration
          transition
        ) && parentComponent && parentComponent.vnode.props && parentComponent.vnode.props.appear;
        const content = el2.content.firstChild;
        if (needCallTransitionHooks) {
          const cls = content.getAttribute("class");
          if (cls) content.$cls = cls;
          transition.beforeEnter(content);
        }
        replaceNode(content, el2, parentComponent);
        vnode.el = el2 = content;
      }
      if (shapeFlag & 16 && // skip if element has innerHTML / textContent
      !(props && (props.innerHTML || props.textContent))) {
        let next = hydrateChildren(
          el2.firstChild,
          vnode,
          el2,
          parentComponent,
          parentSuspense,
          slotScopeIds,
          optimized
        );
        let hasWarned2 = false;
        while (next) {
          if (!isMismatchAllowed(
            el2,
            1
            /* CHILDREN */
          )) {
            if (!hasWarned2) {
              warn$1(
                `Hydration children mismatch on`,
                el2,
                `
Server rendered element contains more child nodes than client vdom.`
              );
              hasWarned2 = true;
            }
            logMismatchError();
          }
          const cur = next;
          next = next.nextSibling;
          remove2(cur);
        }
      } else if (shapeFlag & 8) {
        let clientText = vnode.children;
        if (clientText[0] === "\n" && (el2.tagName === "PRE" || el2.tagName === "TEXTAREA")) {
          clientText = clientText.slice(1);
        }
        const { textContent } = el2;
        if (textContent !== clientText && // innerHTML normalize \r\n or \r into a single \n in the DOM
        textContent !== clientText.replace(/\r\n|\r/g, "\n")) {
          if (!isMismatchAllowed(
            el2,
            0
            /* TEXT */
          )) {
            warn$1(
              `Hydration text content mismatch on`,
              el2,
              `
  - rendered on server: ${textContent}
  - expected on client: ${clientText}`
            );
            logMismatchError();
          }
          el2.textContent = vnode.children;
        }
      }
      if (props) {
        {
          const isCustomElement = el2.tagName.includes("-");
          for (const key in props) {
            if (
              // #11189 skip if this node has directives that have created hooks
              // as it could have mutated the DOM in any possible way
              !(dirs && dirs.some((d) => d.dir.created)) && propHasMismatch(el2, key, props[key], vnode, parentComponent)
            ) {
              logMismatchError();
            }
            if (forcePatch && (key.endsWith("value") || key === "indeterminate") || isOn(key) && !isReservedProp(key) || // force hydrate v-bind with .prop modifiers
            key[0] === "." || isCustomElement) {
              patchProp2(el2, key, null, props[key], void 0, parentComponent);
            }
          }
        }
      }
      let vnodeHooks;
      if (vnodeHooks = props && props.onVnodeBeforeMount) {
        invokeVNodeHook(vnodeHooks, parentComponent, vnode);
      }
      if (dirs) {
        invokeDirectiveHook(vnode, null, parentComponent, "beforeMount");
      }
      if ((vnodeHooks = props && props.onVnodeMounted) || dirs || needCallTransitionHooks) {
        queueEffectWithSuspense(() => {
          vnodeHooks && invokeVNodeHook(vnodeHooks, parentComponent, vnode);
          needCallTransitionHooks && transition.enter(el2);
          dirs && invokeDirectiveHook(vnode, null, parentComponent, "mounted");
        }, parentSuspense);
      }
    }
    return el2.nextSibling;
  };
  const hydrateChildren = (node, parentVNode, container, parentComponent, parentSuspense, slotScopeIds, optimized) => {
    optimized = optimized || !!parentVNode.dynamicChildren;
    const children = parentVNode.children;
    const l = children.length;
    let hasWarned2 = false;
    for (let i = 0; i < l; i++) {
      const vnode = optimized ? children[i] : children[i] = normalizeVNode(children[i]);
      const isText = vnode.type === Text;
      if (node) {
        if (isText && !optimized) {
          if (i + 1 < l && normalizeVNode(children[i + 1]).type === Text) {
            insert(
              createText(
                node.data.slice(vnode.children.length)
              ),
              container,
              nextSibling(node)
            );
            node.data = vnode.children;
          }
        }
        node = hydrateNode(
          node,
          vnode,
          parentComponent,
          parentSuspense,
          slotScopeIds,
          optimized
        );
      } else if (isText && !vnode.children) {
        insert(vnode.el = createText(""), container);
      } else {
        if (!isMismatchAllowed(
          container,
          1
          /* CHILDREN */
        )) {
          if (!hasWarned2) {
            warn$1(
              `Hydration children mismatch on`,
              container,
              `
Server rendered element contains fewer child nodes than client vdom.`
            );
            hasWarned2 = true;
          }
          logMismatchError();
        }
        patch(
          null,
          vnode,
          container,
          null,
          parentComponent,
          parentSuspense,
          getContainerType(container),
          slotScopeIds
        );
      }
    }
    return node;
  };
  const hydrateFragment = (node, vnode, parentComponent, parentSuspense, slotScopeIds, optimized) => {
    const { slotScopeIds: fragmentSlotScopeIds } = vnode;
    if (fragmentSlotScopeIds) {
      slotScopeIds = slotScopeIds ? slotScopeIds.concat(fragmentSlotScopeIds) : fragmentSlotScopeIds;
    }
    const container = parentNode(node);
    const next = hydrateChildren(
      nextSibling(node),
      vnode,
      container,
      parentComponent,
      parentSuspense,
      slotScopeIds,
      optimized
    );
    if (next && isComment(next) && next.data === "]") {
      return nextSibling(vnode.anchor = next);
    } else {
      logMismatchError();
      insert(vnode.anchor = createComment(`]`), container, next);
      return next;
    }
  };
  const handleMismatch = (node, vnode, parentComponent, parentSuspense, slotScopeIds, isFragment) => {
    if (!isMismatchAllowed(
      node.parentElement,
      1
      /* CHILDREN */
    )) {
      warn$1(
        `Hydration node mismatch:
- rendered on server:`,
        node,
        node.nodeType === 3 ? `(text)` : isComment(node) && node.data === "[" ? `(start of fragment)` : ``,
        `
- expected on client:`,
        vnode.type
      );
      logMismatchError();
    }
    vnode.el = null;
    if (isFragment) {
      const end = locateClosingAnchor(node);
      while (true) {
        const next2 = nextSibling(node);
        if (next2 && next2 !== end) {
          remove2(next2);
        } else {
          break;
        }
      }
    }
    const next = nextSibling(node);
    const container = parentNode(node);
    remove2(node);
    patch(
      null,
      vnode,
      container,
      next,
      parentComponent,
      parentSuspense,
      getContainerType(container),
      slotScopeIds
    );
    if (parentComponent) {
      parentComponent.vnode.el = vnode.el;
      updateHOCHostEl(parentComponent, vnode.el);
    }
    return next;
  };
  const locateClosingAnchor = (node, open = "[", close = "]") => {
    let match = 0;
    while (node) {
      node = nextSibling(node);
      if (node && isComment(node)) {
        if (node.data === open) match++;
        if (node.data === close) {
          if (match === 0) {
            return nextSibling(node);
          } else {
            match--;
          }
        }
      }
    }
    return node;
  };
  const replaceNode = (newNode, oldNode, parentComponent) => {
    const parentNode2 = oldNode.parentNode;
    if (parentNode2) {
      parentNode2.replaceChild(newNode, oldNode);
    }
    let parent = parentComponent;
    while (parent) {
      if (parent.vnode.el === oldNode) {
        parent.vnode.el = parent.subTree.el = newNode;
      }
      parent = parent.parent;
    }
  };
  const isTemplateNode2 = (node) => {
    return node.nodeType === 1 && node.tagName === "TEMPLATE";
  };
  return [hydrate2, hydrateNode];
}
function propHasMismatch(el2, key, clientValue, vnode, instance) {
  let mismatchType;
  let mismatchKey;
  let actual;
  let expected;
  if (key === "class") {
    if (el2.$cls) {
      actual = el2.$cls;
      delete el2.$cls;
    } else {
      actual = el2.getAttribute("class");
    }
    expected = normalizeClass(clientValue);
    if (!isSetEqual(toClassSet(actual || ""), toClassSet(expected))) {
      mismatchType = 2;
      mismatchKey = `class`;
    }
  } else if (key === "style") {
    actual = el2.getAttribute("style") || "";
    expected = isString(clientValue) ? clientValue : stringifyStyle(normalizeStyle(clientValue));
    const actualMap = toStyleMap(actual);
    const expectedMap = toStyleMap(expected);
    if (vnode.dirs) {
      for (const { dir, value } of vnode.dirs) {
        if (dir.name === "show" && !value) {
          expectedMap.set("display", "none");
        }
      }
    }
    if (instance) {
      resolveCssVars(instance, vnode, expectedMap);
    }
    if (!isMapEqual(actualMap, expectedMap)) {
      mismatchType = 3;
      mismatchKey = "style";
    }
  } else if (el2 instanceof SVGElement && isKnownSvgAttr(key) || el2 instanceof HTMLElement && (isBooleanAttr(key) || isKnownHtmlAttr(key))) {
    if (isBooleanAttr(key)) {
      actual = el2.hasAttribute(key);
      expected = includeBooleanAttr(clientValue);
    } else if (clientValue == null) {
      actual = el2.hasAttribute(key);
      expected = false;
    } else {
      if (el2.hasAttribute(key)) {
        actual = el2.getAttribute(key);
      } else if (key === "value" && el2.tagName === "TEXTAREA") {
        actual = el2.value;
      } else {
        actual = false;
      }
      expected = isRenderableAttrValue(clientValue) ? String(clientValue) : false;
    }
    if (actual !== expected) {
      mismatchType = 4;
      mismatchKey = key;
    }
  }
  if (mismatchType != null && !isMismatchAllowed(el2, mismatchType)) {
    const format = (v) => v === false ? `(not rendered)` : `${mismatchKey}="${v}"`;
    const preSegment = `Hydration ${MismatchTypeString[mismatchType]} mismatch on`;
    const postSegment = `
  - rendered on server: ${format(actual)}
  - expected on client: ${format(expected)}
  Note: this mismatch is check-only. The DOM will not be rectified in production due to performance overhead.
  You should fix the source of the mismatch.`;
    {
      warn$1(preSegment, el2, postSegment);
    }
    return true;
  }
  return false;
}
function toClassSet(str) {
  return new Set(str.trim().split(/\s+/));
}
function isSetEqual(a, b) {
  if (a.size !== b.size) {
    return false;
  }
  for (const s of a) {
    if (!b.has(s)) {
      return false;
    }
  }
  return true;
}
function toStyleMap(str) {
  const styleMap = /* @__PURE__ */ new Map();
  for (const item of str.split(";")) {
    let [key, value] = item.split(":");
    key = key.trim();
    value = value && value.trim();
    if (key && value) {
      styleMap.set(key, value);
    }
  }
  return styleMap;
}
function isMapEqual(a, b) {
  if (a.size !== b.size) {
    return false;
  }
  for (const [key, value] of a) {
    if (value !== b.get(key)) {
      return false;
    }
  }
  return true;
}
function resolveCssVars(instance, vnode, expectedMap) {
  const root = instance.subTree;
  if (instance.getCssVars && (vnode === root || root && root.type === Fragment && root.children.includes(vnode))) {
    const cssVars = instance.getCssVars();
    for (const key in cssVars) {
      const value = normalizeCssVarValue(cssVars[key]);
      expectedMap.set(`--${getEscapedCssVarName(key)}`, value);
    }
  }
  if (vnode === root && instance.parent) {
    resolveCssVars(instance.parent, instance.vnode, expectedMap);
  }
}
const allowMismatchAttr = "data-allow-mismatch";
const MismatchTypeString = {
  [
    0
    /* TEXT */
  ]: "text",
  [
    1
    /* CHILDREN */
  ]: "children",
  [
    2
    /* CLASS */
  ]: "class",
  [
    3
    /* STYLE */
  ]: "style",
  [
    4
    /* ATTRIBUTE */
  ]: "attribute"
};
function isMismatchAllowed(el2, allowedType) {
  if (allowedType === 0 || allowedType === 1) {
    while (el2 && !el2.hasAttribute(allowMismatchAttr)) {
      el2 = el2.parentElement;
    }
  }
  const allowedAttr = el2 && el2.getAttribute(allowMismatchAttr);
  if (allowedAttr == null) {
    return false;
  } else if (allowedAttr === "") {
    return true;
  } else {
    const list = allowedAttr.split(",");
    if (allowedType === 0 && list.includes("children")) {
      return true;
    }
    return list.includes(MismatchTypeString[allowedType]);
  }
}
const requestIdleCallback = getGlobalThis().requestIdleCallback || ((cb2) => setTimeout(cb2, 1));
const cancelIdleCallback = getGlobalThis().cancelIdleCallback || ((id) => clearTimeout(id));
const hydrateOnIdle = (timeout = 1e4) => (hydrate2) => {
  const id = requestIdleCallback(hydrate2, { timeout });
  return () => cancelIdleCallback(id);
};
function elementIsVisibleInViewport(el2) {
  const { top, left, bottom, right } = el2.getBoundingClientRect();
  const { innerHeight, innerWidth } = window;
  return (top > 0 && top < innerHeight || bottom > 0 && bottom < innerHeight) && (left > 0 && left < innerWidth || right > 0 && right < innerWidth);
}
const hydrateOnVisible = (opts) => (hydrate2, forEach) => {
  const ob2 = new IntersectionObserver((entries) => {
    for (const e of entries) {
      if (!e.isIntersecting) continue;
      ob2.disconnect();
      hydrate2();
      break;
    }
  }, opts);
  forEach((el2) => {
    if (!(el2 instanceof Element)) return;
    if (elementIsVisibleInViewport(el2)) {
      hydrate2();
      ob2.disconnect();
      return false;
    }
    ob2.observe(el2);
  });
  return () => ob2.disconnect();
};
const hydrateOnMediaQuery = (query) => (hydrate2) => {
  if (query) {
    const mql = matchMedia(query);
    if (mql.matches) {
      hydrate2();
    } else {
      mql.addEventListener("change", hydrate2, { once: true });
      return () => mql.removeEventListener("change", hydrate2);
    }
  }
};
const hydrateOnInteraction = (interactions = []) => (hydrate2, forEach) => {
  if (isString(interactions)) interactions = [interactions];
  let hasHydrated = false;
  const doHydrate = (e) => {
    if (!hasHydrated) {
      hasHydrated = true;
      teardown();
      hydrate2();
      e.target.dispatchEvent(new e.constructor(e.type, e));
    }
  };
  const teardown = () => {
    forEach((el2) => {
      for (const i of interactions) {
        el2.removeEventListener(i, doHydrate);
      }
    });
  };
  forEach((el2) => {
    for (const i of interactions) {
      el2.addEventListener(i, doHydrate, { once: true });
    }
  });
  return teardown;
};
function forEachElement(node, cb2) {
  if (isComment(node) && node.data === "[") {
    let depth = 1;
    let next = node.nextSibling;
    while (next) {
      if (next.nodeType === 1) {
        const result = cb2(next);
        if (result === false) {
          break;
        }
      } else if (isComment(next)) {
        if (next.data === "]") {
          if (--depth === 0) break;
        } else if (next.data === "[") {
          depth++;
        }
      }
      next = next.nextSibling;
    }
  } else {
    cb2(node);
  }
}
const isAsyncWrapper = (i) => !!i.type.__asyncLoader;
// @__NO_SIDE_EFFECTS__
function defineAsyncComponent(source) {
  if (isFunction(source)) {
    source = { loader: source };
  }
  const {
    loader,
    loadingComponent,
    errorComponent,
    delay = 200,
    hydrate: hydrateStrategy,
    timeout,
    // undefined = never times out
    suspensible = true,
    onError: userOnError
  } = source;
  let pendingRequest = null;
  let resolvedComp;
  let retries = 0;
  const retry = () => {
    retries++;
    pendingRequest = null;
    return load();
  };
  const load = () => {
    let thisRequest;
    return pendingRequest || (thisRequest = pendingRequest = loader().catch((err) => {
      err = err instanceof Error ? err : new Error(String(err));
      if (userOnError) {
        return new Promise((resolve2, reject) => {
          const userRetry = () => resolve2(retry());
          const userFail = () => reject(err);
          userOnError(err, userRetry, userFail, retries + 1);
        });
      } else {
        throw err;
      }
    }).then((comp) => {
      if (thisRequest !== pendingRequest && pendingRequest) {
        return pendingRequest;
      }
      if (comp && (comp.__esModule || comp[Symbol.toStringTag] === "Module")) {
        comp = comp.default;
      }
      resolvedComp = comp;
      return comp;
    }));
  };
  return /* @__PURE__ */ defineComponent({
    name: "AsyncComponentWrapper",
    __asyncLoader: load,
    __asyncHydrate(el2, instance, hydrate2) {
      let patched = false;
      (instance.bu || (instance.bu = [])).push(() => patched = true);
      const performHydrate = () => {
        if (patched) {
          return;
        }
        hydrate2();
      };
      const doHydrate = hydrateStrategy ? () => {
        const teardown = hydrateStrategy(
          performHydrate,
          (cb2) => forEachElement(el2, cb2)
        );
        if (teardown) {
          (instance.bum || (instance.bum = [])).push(teardown);
        }
      } : performHydrate;
      if (resolvedComp) {
        doHydrate();
      } else {
        load().then(() => !instance.isUnmounted && doHydrate());
      }
    },
    get __asyncResolved() {
      return resolvedComp;
    },
    setup() {
      const instance = currentInstance;
      markAsyncBoundary(instance);
      if (resolvedComp) {
        return () => createInnerComp(resolvedComp, instance);
      }
      const onError = (err) => {
        pendingRequest = null;
        handleError(
          err,
          instance,
          13,
          !errorComponent
        );
      };
      if (suspensible && instance.suspense || isInSSRComponentSetup) {
        return load().then((comp) => {
          return () => createInnerComp(comp, instance);
        }).catch((err) => {
          onError(err);
          return () => errorComponent ? createVNode(errorComponent, {
            error: err
          }) : null;
        });
      }
      const loaded = ref(false);
      const error = ref();
      const delayed = ref(!!delay);
      if (delay) {
        setTimeout(() => {
          delayed.value = false;
        }, delay);
      }
      if (timeout != null) {
        setTimeout(() => {
          if (!loaded.value && !error.value) {
            const err = new Error(
              `Async component timed out after ${timeout}ms.`
            );
            onError(err);
            error.value = err;
          }
        }, timeout);
      }
      load().then(() => {
        loaded.value = true;
        if (instance.parent && isKeepAlive(instance.parent.vnode)) {
          instance.parent.update();
        }
      }).catch((err) => {
        onError(err);
        error.value = err;
      });
      return () => {
        if (loaded.value && resolvedComp) {
          return createInnerComp(resolvedComp, instance);
        } else if (error.value && errorComponent) {
          return createVNode(errorComponent, {
            error: error.value
          });
        } else if (loadingComponent && !delayed.value) {
          return createInnerComp(
            loadingComponent,
            instance
          );
        }
      };
    }
  });
}
function createInnerComp(comp, parent) {
  const { ref: ref22, props, children, ce } = parent.vnode;
  const vnode = createVNode(comp, props, children);
  vnode.ref = ref22;
  vnode.ce = ce;
  delete parent.vnode.ce;
  return vnode;
}
const isKeepAlive = (vnode) => vnode.type.__isKeepAlive;
const KeepAliveImpl = {
  name: `KeepAlive`,
  // Marker for special handling inside the renderer. We are not using a ===
  // check directly on KeepAlive in the renderer, because importing it directly
  // would prevent it from being tree-shaken.
  __isKeepAlive: true,
  props: {
    include: [String, RegExp, Array],
    exclude: [String, RegExp, Array],
    max: [String, Number]
  },
  setup(props, { slots }) {
    const instance = getCurrentInstance();
    const sharedContext = instance.ctx;
    if (!sharedContext.renderer) {
      return () => {
        const children = slots.default && slots.default();
        return children && children.length === 1 ? children[0] : children;
      };
    }
    const cache = /* @__PURE__ */ new Map();
    const keys = /* @__PURE__ */ new Set();
    let current = null;
    {
      instance.__v_cache = cache;
    }
    const parentSuspense = instance.suspense;
    const {
      renderer: {
        p: patch,
        m: move,
        um: _unmount,
        o: { createElement }
      }
    } = sharedContext;
    const storageContainer = createElement("div");
    sharedContext.activate = (vnode, container, anchor, namespace, optimized) => {
      const instance2 = vnode.component;
      move(vnode, container, anchor, 0, parentSuspense);
      patch(
        instance2.vnode,
        vnode,
        container,
        anchor,
        instance2,
        parentSuspense,
        namespace,
        vnode.slotScopeIds,
        optimized
      );
      queuePostRenderEffect(() => {
        instance2.isDeactivated = false;
        if (instance2.a) {
          invokeArrayFns(instance2.a);
        }
        const vnodeHook = vnode.props && vnode.props.onVnodeMounted;
        if (vnodeHook) {
          invokeVNodeHook(vnodeHook, instance2.parent, vnode);
        }
      }, parentSuspense);
      {
        devtoolsComponentAdded(instance2);
      }
    };
    sharedContext.deactivate = (vnode) => {
      const instance2 = vnode.component;
      invalidateMount(instance2.m);
      invalidateMount(instance2.a);
      move(vnode, storageContainer, null, 1, parentSuspense);
      queuePostRenderEffect(() => {
        if (instance2.da) {
          invokeArrayFns(instance2.da);
        }
        const vnodeHook = vnode.props && vnode.props.onVnodeUnmounted;
        if (vnodeHook) {
          invokeVNodeHook(vnodeHook, instance2.parent, vnode);
        }
        instance2.isDeactivated = true;
      }, parentSuspense);
      {
        devtoolsComponentAdded(instance2);
      }
    };
    function unmount(vnode) {
      resetShapeFlag(vnode);
      _unmount(vnode, instance, parentSuspense, true);
    }
    function pruneCache(filter) {
      cache.forEach((vnode, key) => {
        const name2 = getComponentName(vnode.type);
        if (name2 && !filter(name2)) {
          pruneCacheEntry(key);
        }
      });
    }
    function pruneCacheEntry(key) {
      const cached = cache.get(key);
      if (cached && (!current || !isSameVNodeType(cached, current))) {
        unmount(cached);
      } else if (current) {
        resetShapeFlag(current);
      }
      cache.delete(key);
      keys.delete(key);
    }
    watch(
      () => [props.include, props.exclude],
      ([include, exclude]) => {
        include && pruneCache((name2) => matches(include, name2));
        exclude && pruneCache((name2) => !matches(exclude, name2));
      },
      // prune post-render after `current` has been updated
      { flush: "post", deep: true }
    );
    let pendingCacheKey = null;
    const cacheSubtree = () => {
      if (pendingCacheKey != null) {
        if (isSuspense(instance.subTree.type)) {
          queuePostRenderEffect(() => {
            cache.set(pendingCacheKey, getInnerChild(instance.subTree));
          }, instance.subTree.suspense);
        } else {
          cache.set(pendingCacheKey, getInnerChild(instance.subTree));
        }
      }
    };
    onMounted(cacheSubtree);
    onUpdated(cacheSubtree);
    onBeforeUnmount(() => {
      cache.forEach((cached) => {
        const { subTree, suspense } = instance;
        const vnode = getInnerChild(subTree);
        if (cached.type === vnode.type && cached.key === vnode.key) {
          resetShapeFlag(vnode);
          const da = vnode.component.da;
          da && queuePostRenderEffect(da, suspense);
          return;
        }
        unmount(cached);
      });
    });
    return () => {
      pendingCacheKey = null;
      if (!slots.default) {
        return current = null;
      }
      const children = slots.default();
      const rawVNode = children[0];
      if (children.length > 1) {
        current = null;
        return children;
      } else if (!isVNode(rawVNode) || !(rawVNode.shapeFlag & 4) && !(rawVNode.shapeFlag & 128)) {
        current = null;
        return rawVNode;
      }
      let vnode = getInnerChild(rawVNode);
      if (vnode.type === Comment) {
        current = null;
        return vnode;
      }
      const comp = vnode.type;
      const name2 = getComponentName(
        isAsyncWrapper(vnode) ? vnode.type.__asyncResolved || {} : comp
      );
      const { include, exclude, max } = props;
      if (include && (!name2 || !matches(include, name2)) || exclude && name2 && matches(exclude, name2)) {
        vnode.shapeFlag &= -257;
        current = vnode;
        return rawVNode;
      }
      const key = vnode.key == null ? comp : vnode.key;
      const cachedVNode = cache.get(key);
      if (vnode.el) {
        vnode = cloneVNode(vnode);
        if (rawVNode.shapeFlag & 128) {
          rawVNode.ssContent = vnode;
        }
      }
      pendingCacheKey = key;
      if (cachedVNode) {
        vnode.el = cachedVNode.el;
        vnode.component = cachedVNode.component;
        if (vnode.transition) {
          setTransitionHooks(vnode, vnode.transition);
        }
        vnode.shapeFlag |= 512;
        keys.delete(key);
        keys.add(key);
      } else {
        keys.add(key);
        if (max && keys.size > parseInt(max, 10)) {
          pruneCacheEntry(keys.values().next().value);
        }
      }
      vnode.shapeFlag |= 256;
      current = vnode;
      return isSuspense(rawVNode.type) ? rawVNode : vnode;
    };
  }
};
const KeepAlive = KeepAliveImpl;
function matches(pattern, name2) {
  if (isArray(pattern)) {
    return pattern.some((p2) => matches(p2, name2));
  } else if (isString(pattern)) {
    return pattern.split(",").includes(name2);
  } else if (isRegExp(pattern)) {
    pattern.lastIndex = 0;
    return pattern.test(name2);
  }
  return false;
}
function onActivated(hook, target) {
  registerKeepAliveHook(hook, "a", target);
}
function onDeactivated(hook, target) {
  registerKeepAliveHook(hook, "da", target);
}
function registerKeepAliveHook(hook, type, target = currentInstance) {
  const wrappedHook = hook.__wdc || (hook.__wdc = () => {
    let current = target;
    while (current) {
      if (current.isDeactivated) {
        return;
      }
      current = current.parent;
    }
    return hook();
  });
  injectHook(type, wrappedHook, target);
  if (target) {
    let current = target.parent;
    while (current && current.parent) {
      if (isKeepAlive(current.parent.vnode)) {
        injectToKeepAliveRoot(wrappedHook, type, target, current);
      }
      current = current.parent;
    }
  }
}
function injectToKeepAliveRoot(hook, type, target, keepAliveRoot) {
  const injected = injectHook(
    type,
    hook,
    keepAliveRoot,
    true
    /* prepend */
  );
  onUnmounted(() => {
    remove(keepAliveRoot[type], injected);
  }, target);
}
function resetShapeFlag(vnode) {
  vnode.shapeFlag &= -257;
  vnode.shapeFlag &= -513;
}
function getInnerChild(vnode) {
  return vnode.shapeFlag & 128 ? vnode.ssContent : vnode;
}
function injectHook(type, hook, target = currentInstance, prepend = false) {
  if (target) {
    const hooks = target[type] || (target[type] = []);
    const wrappedHook = hook.__weh || (hook.__weh = (...args) => {
      pauseTracking();
      const reset2 = setCurrentInstance(target);
      const res = callWithAsyncErrorHandling(hook, target, type, args);
      reset2();
      resetTracking();
      return res;
    });
    if (prepend) {
      hooks.unshift(wrappedHook);
    } else {
      hooks.push(wrappedHook);
    }
    return wrappedHook;
  }
}
const createHook = (lifecycle) => (hook, target = currentInstance) => {
  if (!isInSSRComponentSetup || lifecycle === "sp") {
    injectHook(lifecycle, (...args) => hook(...args), target);
  }
};
const onBeforeMount = createHook("bm");
const onMounted = createHook("m");
const onBeforeUpdate = createHook(
  "bu"
);
const onUpdated = createHook("u");
const onBeforeUnmount = createHook(
  "bum"
);
const onUnmounted = createHook("um");
const onServerPrefetch = createHook(
  "sp"
);
const onRenderTriggered = createHook("rtg");
const onRenderTracked = createHook("rtc");
function onErrorCaptured(hook, target = currentInstance) {
  injectHook("ec", hook, target);
}
const COMPONENTS = "components";
const DIRECTIVES = "directives";
function resolveComponent(name2, maybeSelfReference) {
  return resolveAsset(COMPONENTS, name2, true, maybeSelfReference) || name2;
}
const NULL_DYNAMIC_COMPONENT = Symbol.for("v-ndc");
function resolveDynamicComponent(component) {
  if (isString(component)) {
    return resolveAsset(COMPONENTS, component, false) || component;
  } else {
    return component || NULL_DYNAMIC_COMPONENT;
  }
}
function resolveDirective(name2) {
  return resolveAsset(DIRECTIVES, name2);
}
function resolveAsset(type, name2, warnMissing = true, maybeSelfReference = false) {
  const instance = currentRenderingInstance || currentInstance;
  if (instance) {
    const Component = instance.type;
    if (type === COMPONENTS) {
      const selfName = getComponentName(
        Component,
        false
      );
      if (selfName && (selfName === name2 || selfName === camelize(name2) || selfName === capitalize(camelize(name2)))) {
        return Component;
      }
    }
    const res = (
      // local registration
      // check instance[type] first which is resolved for options API
      resolve(instance[type] || Component[type], name2) || // global registration
      resolve(instance.appContext[type], name2)
    );
    if (!res && maybeSelfReference) {
      return Component;
    }
    return res;
  }
}
function resolve(registry, name2) {
  return registry && (registry[name2] || registry[camelize(name2)] || registry[capitalize(camelize(name2))]);
}
function renderList(source, renderItem, cache, index) {
  let ret;
  const cached = cache && cache[index];
  const sourceIsArray = isArray(source);
  if (sourceIsArray || isString(source)) {
    const sourceIsReactiveArray = sourceIsArray && isReactive(source);
    let needsWrap = false;
    let isReadonlySource = false;
    if (sourceIsReactiveArray) {
      needsWrap = !isShallow(source);
      isReadonlySource = isReadonly(source);
      source = shallowReadArray(source);
    }
    ret = new Array(source.length);
    for (let i = 0, l = source.length; i < l; i++) {
      ret[i] = renderItem(
        needsWrap ? isReadonlySource ? toReadonly(toReactive(source[i])) : toReactive(source[i]) : source[i],
        i,
        void 0,
        cached && cached[i]
      );
    }
  } else if (typeof source === "number") {
    ret = new Array(source);
    for (let i = 0; i < source; i++) {
      ret[i] = renderItem(i + 1, i, void 0, cached && cached[i]);
    }
  } else if (isObject(source)) {
    if (source[Symbol.iterator]) {
      ret = Array.from(
        source,
        (item, i) => renderItem(item, i, void 0, cached && cached[i])
      );
    } else {
      const keys = Object.keys(source);
      ret = new Array(keys.length);
      for (let i = 0, l = keys.length; i < l; i++) {
        const key = keys[i];
        ret[i] = renderItem(source[key], key, i, cached && cached[i]);
      }
    }
  } else {
    ret = [];
  }
  if (cache) {
    cache[index] = ret;
  }
  return ret;
}
function createSlots(slots, dynamicSlots) {
  for (let i = 0; i < dynamicSlots.length; i++) {
    const slot = dynamicSlots[i];
    if (isArray(slot)) {
      for (let j = 0; j < slot.length; j++) {
        slots[slot[j].name] = slot[j].fn;
      }
    } else if (slot) {
      slots[slot.name] = slot.key ? (...args) => {
        const res = slot.fn(...args);
        if (res) res.key = slot.key;
        return res;
      } : slot.fn;
    }
  }
  return slots;
}
function renderSlot(slots, name2, props = {}, fallback, noSlotted) {
  if (currentRenderingInstance.ce || currentRenderingInstance.parent && isAsyncWrapper(currentRenderingInstance.parent) && currentRenderingInstance.parent.ce) {
    const hasProps = Object.keys(props).length > 0;
    if (name2 !== "default") props.name = name2;
    return openBlock(), createBlock(
      Fragment,
      null,
      [createVNode("slot", props, fallback && fallback())],
      hasProps ? -2 : 64
    );
  }
  let slot = slots[name2];
  if (slot && slot._c) {
    slot._d = false;
  }
  openBlock();
  const validSlotContent = slot && ensureValidVNode(slot(props));
  const slotKey = props.key || // slot content array of a dynamic conditional slot may have a branch
  // key attached in the `createSlots` helper, respect that
  validSlotContent && validSlotContent.key;
  const rendered = createBlock(
    Fragment,
    {
      key: (slotKey && !isSymbol(slotKey) ? slotKey : `_${name2}`) + // #7256 force differentiate fallback content from actual content
      (!validSlotContent && fallback ? "_fb" : "")
    },
    validSlotContent || (fallback ? fallback() : []),
    validSlotContent && slots._ === 1 ? 64 : -2
  );
  if (!noSlotted && rendered.scopeId) {
    rendered.slotScopeIds = [rendered.scopeId + "-s"];
  }
  if (slot && slot._c) {
    slot._d = true;
  }
  return rendered;
}
function ensureValidVNode(vnodes) {
  return vnodes.some((child) => {
    if (!isVNode(child)) return true;
    if (child.type === Comment) return false;
    if (child.type === Fragment && !ensureValidVNode(child.children))
      return false;
    return true;
  }) ? vnodes : null;
}
function toHandlers(obj, preserveCaseIfNecessary) {
  const ret = {};
  for (const key in obj) {
    ret[preserveCaseIfNecessary && /[A-Z]/.test(key) ? `on:${key}` : toHandlerKey(key)] = obj[key];
  }
  return ret;
}
const getPublicInstance = (i) => {
  if (!i) return null;
  if (isStatefulComponent(i)) return getComponentPublicInstance(i);
  return getPublicInstance(i.parent);
};
const publicPropertiesMap = (
  // Move PURE marker to new line to workaround compiler discarding it
  // due to type annotation
  /* @__PURE__ */ extend(/* @__PURE__ */ Object.create(null), {
    $: (i) => i,
    $el: (i) => i.vnode.el,
    $data: (i) => i.data,
    $props: (i) => i.props,
    $attrs: (i) => i.attrs,
    $slots: (i) => i.slots,
    $refs: (i) => i.refs,
    $parent: (i) => getPublicInstance(i.parent),
    $root: (i) => getPublicInstance(i.root),
    $host: (i) => i.ce,
    $emit: (i) => i.emit,
    $options: (i) => resolveMergedOptions(i),
    $forceUpdate: (i) => i.f || (i.f = () => {
      queueJob(i.update);
    }),
    $nextTick: (i) => i.n || (i.n = nextTick.bind(i.proxy)),
    $watch: (i) => instanceWatch.bind(i)
  })
);
const hasSetupBinding = (state, key) => state !== EMPTY_OBJ && !state.__isScriptSetup && hasOwn(state, key);
const PublicInstanceProxyHandlers = {
  get({ _: instance }, key) {
    if (key === "__v_skip") {
      return true;
    }
    const { ctx, setupState, data, props, accessCache, type, appContext } = instance;
    if (key[0] !== "$") {
      const n = accessCache[key];
      if (n !== void 0) {
        switch (n) {
          case 1:
            return setupState[key];
          case 2:
            return data[key];
          case 4:
            return ctx[key];
          case 3:
            return props[key];
        }
      } else if (hasSetupBinding(setupState, key)) {
        accessCache[key] = 1;
        return setupState[key];
      } else if (data !== EMPTY_OBJ && hasOwn(data, key)) {
        accessCache[key] = 2;
        return data[key];
      } else if (hasOwn(props, key)) {
        accessCache[key] = 3;
        return props[key];
      } else if (ctx !== EMPTY_OBJ && hasOwn(ctx, key)) {
        accessCache[key] = 4;
        return ctx[key];
      } else if (shouldCacheAccess) {
        accessCache[key] = 0;
      }
    }
    const publicGetter = publicPropertiesMap[key];
    let cssModule, globalProperties;
    if (publicGetter) {
      if (key === "$attrs") {
        track(instance.attrs, "get", "");
      }
      return publicGetter(instance);
    } else if (
      // css module (injected by vue-loader)
      (cssModule = type.__cssModules) && (cssModule = cssModule[key])
    ) {
      return cssModule;
    } else if (ctx !== EMPTY_OBJ && hasOwn(ctx, key)) {
      accessCache[key] = 4;
      return ctx[key];
    } else if (
      // global properties
      globalProperties = appContext.config.globalProperties, hasOwn(globalProperties, key)
    ) {
      {
        return globalProperties[key];
      }
    } else ;
  },
  set({ _: instance }, key, value) {
    const { data, setupState, ctx } = instance;
    if (hasSetupBinding(setupState, key)) {
      setupState[key] = value;
      return true;
    } else if (data !== EMPTY_OBJ && hasOwn(data, key)) {
      data[key] = value;
      return true;
    } else if (hasOwn(instance.props, key)) {
      return false;
    }
    if (key[0] === "$" && key.slice(1) in instance) {
      return false;
    } else {
      {
        ctx[key] = value;
      }
    }
    return true;
  },
  has({
    _: { data, setupState, accessCache, ctx, appContext, props, type }
  }, key) {
    let cssModules;
    return !!(accessCache[key] || data !== EMPTY_OBJ && key[0] !== "$" && hasOwn(data, key) || hasSetupBinding(setupState, key) || hasOwn(props, key) || hasOwn(ctx, key) || hasOwn(publicPropertiesMap, key) || hasOwn(appContext.config.globalProperties, key) || (cssModules = type.__cssModules) && cssModules[key]);
  },
  defineProperty(target, key, descriptor) {
    if (descriptor.get != null) {
      target._.accessCache[key] = 0;
    } else if (hasOwn(descriptor, "value")) {
      this.set(target, key, descriptor.value, null);
    }
    return Reflect.defineProperty(target, key, descriptor);
  }
};
const RuntimeCompiledPublicInstanceProxyHandlers = /* @__PURE__ */ extend({}, PublicInstanceProxyHandlers, {
  get(target, key) {
    if (key === Symbol.unscopables) {
      return;
    }
    return PublicInstanceProxyHandlers.get(target, key, target);
  },
  has(_, key) {
    const has = key[0] !== "_" && !isGloballyAllowed(key);
    return has;
  }
});
function defineProps() {
  return null;
}
function defineEmits() {
  return null;
}
function defineExpose(exposed) {
}
function defineOptions(options) {
}
function defineSlots() {
  return null;
}
function defineModel() {
}
function withDefaults(props, defaults) {
  return null;
}
function useSlots() {
  return getContext().slots;
}
function useAttrs() {
  return getContext().attrs;
}
function getContext(calledFunctionName) {
  const i = getCurrentInstance();
  return i.setupContext || (i.setupContext = createSetupContext(i));
}
function normalizePropsOrEmits(props) {
  return isArray(props) ? props.reduce(
    (normalized, p2) => (normalized[p2] = null, normalized),
    {}
  ) : props;
}
function mergeDefaults(raw, defaults) {
  const props = normalizePropsOrEmits(raw);
  for (const key in defaults) {
    if (key.startsWith("__skip")) continue;
    let opt = props[key];
    if (opt) {
      if (isArray(opt) || isFunction(opt)) {
        opt = props[key] = { type: opt, default: defaults[key] };
      } else {
        opt.default = defaults[key];
      }
    } else if (opt === null) {
      opt = props[key] = { default: defaults[key] };
    } else ;
    if (opt && defaults[`__skip_${key}`]) {
      opt.skipFactory = true;
    }
  }
  return props;
}
function mergeModels(a, b) {
  if (!a || !b) return a || b;
  if (isArray(a) && isArray(b)) return a.concat(b);
  return extend({}, normalizePropsOrEmits(a), normalizePropsOrEmits(b));
}
function createPropsRestProxy(props, excludedKeys) {
  const ret = {};
  for (const key in props) {
    if (!excludedKeys.includes(key)) {
      Object.defineProperty(ret, key, {
        enumerable: true,
        get: () => props[key]
      });
    }
  }
  return ret;
}
function withAsyncContext(getAwaitable) {
  const ctx = getCurrentInstance();
  let awaitable = getAwaitable();
  unsetCurrentInstance();
  if (isPromise(awaitable)) {
    awaitable = awaitable.catch((e) => {
      setCurrentInstance(ctx);
      throw e;
    });
  }
  return [awaitable, () => setCurrentInstance(ctx)];
}
let shouldCacheAccess = true;
function applyOptions(instance) {
  const options = resolveMergedOptions(instance);
  const publicThis = instance.proxy;
  const ctx = instance.ctx;
  shouldCacheAccess = false;
  if (options.beforeCreate) {
    callHook$1(options.beforeCreate, instance, "bc");
  }
  const {
    // state
    data: dataOptions,
    computed: computedOptions,
    methods,
    watch: watchOptions,
    provide: provideOptions,
    inject: injectOptions,
    // lifecycle
    created,
    beforeMount,
    mounted,
    beforeUpdate,
    updated,
    activated,
    deactivated,
    beforeDestroy,
    beforeUnmount,
    destroyed,
    unmounted,
    render: render2,
    renderTracked,
    renderTriggered,
    errorCaptured,
    serverPrefetch,
    // public API
    expose,
    inheritAttrs,
    // assets
    components,
    directives,
    filters
  } = options;
  const checkDuplicateProperties = null;
  if (injectOptions) {
    resolveInjections(injectOptions, ctx, checkDuplicateProperties);
  }
  if (methods) {
    for (const key in methods) {
      const methodHandler = methods[key];
      if (isFunction(methodHandler)) {
        {
          ctx[key] = methodHandler.bind(publicThis);
        }
      }
    }
  }
  if (dataOptions) {
    const data = dataOptions.call(publicThis, publicThis);
    if (!isObject(data)) ;
    else {
      instance.data = reactive(data);
    }
  }
  shouldCacheAccess = true;
  if (computedOptions) {
    for (const key in computedOptions) {
      const opt = computedOptions[key];
      const get = isFunction(opt) ? opt.bind(publicThis, publicThis) : isFunction(opt.get) ? opt.get.bind(publicThis, publicThis) : NOOP;
      const set = !isFunction(opt) && isFunction(opt.set) ? opt.set.bind(publicThis) : NOOP;
      const c = computed({
        get,
        set
      });
      Object.defineProperty(ctx, key, {
        enumerable: true,
        configurable: true,
        get: () => c.value,
        set: (v) => c.value = v
      });
    }
  }
  if (watchOptions) {
    for (const key in watchOptions) {
      createWatcher(watchOptions[key], ctx, publicThis, key);
    }
  }
  if (provideOptions) {
    const provides = isFunction(provideOptions) ? provideOptions.call(publicThis) : provideOptions;
    Reflect.ownKeys(provides).forEach((key) => {
      provide(key, provides[key]);
    });
  }
  if (created) {
    callHook$1(created, instance, "c");
  }
  function registerLifecycleHook(register, hook) {
    if (isArray(hook)) {
      hook.forEach((_hook) => register(_hook.bind(publicThis)));
    } else if (hook) {
      register(hook.bind(publicThis));
    }
  }
  registerLifecycleHook(onBeforeMount, beforeMount);
  registerLifecycleHook(onMounted, mounted);
  registerLifecycleHook(onBeforeUpdate, beforeUpdate);
  registerLifecycleHook(onUpdated, updated);
  registerLifecycleHook(onActivated, activated);
  registerLifecycleHook(onDeactivated, deactivated);
  registerLifecycleHook(onErrorCaptured, errorCaptured);
  registerLifecycleHook(onRenderTracked, renderTracked);
  registerLifecycleHook(onRenderTriggered, renderTriggered);
  registerLifecycleHook(onBeforeUnmount, beforeUnmount);
  registerLifecycleHook(onUnmounted, unmounted);
  registerLifecycleHook(onServerPrefetch, serverPrefetch);
  if (isArray(expose)) {
    if (expose.length) {
      const exposed = instance.exposed || (instance.exposed = {});
      expose.forEach((key) => {
        Object.defineProperty(exposed, key, {
          get: () => publicThis[key],
          set: (val) => publicThis[key] = val,
          enumerable: true
        });
      });
    } else if (!instance.exposed) {
      instance.exposed = {};
    }
  }
  if (render2 && instance.render === NOOP) {
    instance.render = render2;
  }
  if (inheritAttrs != null) {
    instance.inheritAttrs = inheritAttrs;
  }
  if (components) instance.components = components;
  if (directives) instance.directives = directives;
  if (serverPrefetch) {
    markAsyncBoundary(instance);
  }
}
function resolveInjections(injectOptions, ctx, checkDuplicateProperties = NOOP) {
  if (isArray(injectOptions)) {
    injectOptions = normalizeInject(injectOptions);
  }
  for (const key in injectOptions) {
    const opt = injectOptions[key];
    let injected;
    if (isObject(opt)) {
      if ("default" in opt) {
        injected = inject(
          opt.from || key,
          opt.default,
          true
        );
      } else {
        injected = inject(opt.from || key);
      }
    } else {
      injected = inject(opt);
    }
    if (isRef(injected)) {
      Object.defineProperty(ctx, key, {
        enumerable: true,
        configurable: true,
        get: () => injected.value,
        set: (v) => injected.value = v
      });
    } else {
      ctx[key] = injected;
    }
  }
}
function callHook$1(hook, instance, type) {
  callWithAsyncErrorHandling(
    isArray(hook) ? hook.map((h2) => h2.bind(instance.proxy)) : hook.bind(instance.proxy),
    instance,
    type
  );
}
function createWatcher(raw, ctx, publicThis, key) {
  let getter = key.includes(".") ? createPathGetter(publicThis, key) : () => publicThis[key];
  if (isString(raw)) {
    const handler = ctx[raw];
    if (isFunction(handler)) {
      {
        watch(getter, handler);
      }
    }
  } else if (isFunction(raw)) {
    {
      watch(getter, raw.bind(publicThis));
    }
  } else if (isObject(raw)) {
    if (isArray(raw)) {
      raw.forEach((r) => createWatcher(r, ctx, publicThis, key));
    } else {
      const handler = isFunction(raw.handler) ? raw.handler.bind(publicThis) : ctx[raw.handler];
      if (isFunction(handler)) {
        watch(getter, handler, raw);
      }
    }
  } else ;
}
function resolveMergedOptions(instance) {
  const base = instance.type;
  const { mixins, extends: extendsOptions } = base;
  const {
    mixins: globalMixins,
    optionsCache: cache,
    config: { optionMergeStrategies }
  } = instance.appContext;
  const cached = cache.get(base);
  let resolved;
  if (cached) {
    resolved = cached;
  } else if (!globalMixins.length && !mixins && !extendsOptions) {
    {
      resolved = base;
    }
  } else {
    resolved = {};
    if (globalMixins.length) {
      globalMixins.forEach(
        (m) => mergeOptions(resolved, m, optionMergeStrategies, true)
      );
    }
    mergeOptions(resolved, base, optionMergeStrategies);
  }
  if (isObject(base)) {
    cache.set(base, resolved);
  }
  return resolved;
}
function mergeOptions(to, from, strats, asMixin = false) {
  const { mixins, extends: extendsOptions } = from;
  if (extendsOptions) {
    mergeOptions(to, extendsOptions, strats, true);
  }
  if (mixins) {
    mixins.forEach(
      (m) => mergeOptions(to, m, strats, true)
    );
  }
  for (const key in from) {
    if (asMixin && key === "expose") ;
    else {
      const strat = internalOptionMergeStrats[key] || strats && strats[key];
      to[key] = strat ? strat(to[key], from[key]) : from[key];
    }
  }
  return to;
}
const internalOptionMergeStrats = {
  data: mergeDataFn,
  props: mergeEmitsOrPropsOptions,
  emits: mergeEmitsOrPropsOptions,
  // objects
  methods: mergeObjectOptions,
  computed: mergeObjectOptions,
  // lifecycle
  beforeCreate: mergeAsArray$1,
  created: mergeAsArray$1,
  beforeMount: mergeAsArray$1,
  mounted: mergeAsArray$1,
  beforeUpdate: mergeAsArray$1,
  updated: mergeAsArray$1,
  beforeDestroy: mergeAsArray$1,
  beforeUnmount: mergeAsArray$1,
  destroyed: mergeAsArray$1,
  unmounted: mergeAsArray$1,
  activated: mergeAsArray$1,
  deactivated: mergeAsArray$1,
  errorCaptured: mergeAsArray$1,
  serverPrefetch: mergeAsArray$1,
  // assets
  components: mergeObjectOptions,
  directives: mergeObjectOptions,
  // watch
  watch: mergeWatchOptions,
  // provide / inject
  provide: mergeDataFn,
  inject: mergeInject
};
function mergeDataFn(to, from) {
  if (!from) {
    return to;
  }
  if (!to) {
    return from;
  }
  return function mergedDataFn() {
    return extend(
      isFunction(to) ? to.call(this, this) : to,
      isFunction(from) ? from.call(this, this) : from
    );
  };
}
function mergeInject(to, from) {
  return mergeObjectOptions(normalizeInject(to), normalizeInject(from));
}
function normalizeInject(raw) {
  if (isArray(raw)) {
    const res = {};
    for (let i = 0; i < raw.length; i++) {
      res[raw[i]] = raw[i];
    }
    return res;
  }
  return raw;
}
function mergeAsArray$1(to, from) {
  return to ? [...new Set([].concat(to, from))] : from;
}
function mergeObjectOptions(to, from) {
  return to ? extend(/* @__PURE__ */ Object.create(null), to, from) : from;
}
function mergeEmitsOrPropsOptions(to, from) {
  if (to) {
    if (isArray(to) && isArray(from)) {
      return [.../* @__PURE__ */ new Set([...to, ...from])];
    }
    return extend(
      /* @__PURE__ */ Object.create(null),
      normalizePropsOrEmits(to),
      normalizePropsOrEmits(from != null ? from : {})
    );
  } else {
    return from;
  }
}
function mergeWatchOptions(to, from) {
  if (!to) return from;
  if (!from) return to;
  const merged = extend(/* @__PURE__ */ Object.create(null), to);
  for (const key in from) {
    merged[key] = mergeAsArray$1(to[key], from[key]);
  }
  return merged;
}
function createAppContext() {
  return {
    app: null,
    config: {
      isNativeTag: NO,
      performance: false,
      globalProperties: {},
      optionMergeStrategies: {},
      errorHandler: void 0,
      warnHandler: void 0,
      compilerOptions: {}
    },
    mixins: [],
    components: {},
    directives: {},
    provides: /* @__PURE__ */ Object.create(null),
    optionsCache: /* @__PURE__ */ new WeakMap(),
    propsCache: /* @__PURE__ */ new WeakMap(),
    emitsCache: /* @__PURE__ */ new WeakMap()
  };
}
let uid$1 = 0;
function createAppAPI(render2, hydrate2) {
  return function createApp2(rootComponent, rootProps = null) {
    if (!isFunction(rootComponent)) {
      rootComponent = extend({}, rootComponent);
    }
    if (rootProps != null && !isObject(rootProps)) {
      rootProps = null;
    }
    const context = createAppContext();
    const installedPlugins = /* @__PURE__ */ new WeakSet();
    const pluginCleanupFns = [];
    let isMounted = false;
    const app = context.app = {
      _uid: uid$1++,
      _component: rootComponent,
      _props: rootProps,
      _container: null,
      _context: context,
      _instance: null,
      version,
      get config() {
        return context.config;
      },
      set config(v) {
      },
      use(plugin, ...options) {
        if (installedPlugins.has(plugin)) ;
        else if (plugin && isFunction(plugin.install)) {
          installedPlugins.add(plugin);
          plugin.install(app, ...options);
        } else if (isFunction(plugin)) {
          installedPlugins.add(plugin);
          plugin(app, ...options);
        } else ;
        return app;
      },
      mixin(mixin) {
        {
          if (!context.mixins.includes(mixin)) {
            context.mixins.push(mixin);
          }
        }
        return app;
      },
      component(name2, component) {
        if (!component) {
          return context.components[name2];
        }
        context.components[name2] = component;
        return app;
      },
      directive(name2, directive) {
        if (!directive) {
          return context.directives[name2];
        }
        context.directives[name2] = directive;
        return app;
      },
      mount(rootContainer, isHydrate, namespace) {
        if (!isMounted) {
          const vnode = app._ceVNode || createVNode(rootComponent, rootProps);
          vnode.appContext = context;
          if (namespace === true) {
            namespace = "svg";
          } else if (namespace === false) {
            namespace = void 0;
          }
          if (isHydrate && hydrate2) {
            hydrate2(vnode, rootContainer);
          } else {
            render2(vnode, rootContainer, namespace);
          }
          isMounted = true;
          app._container = rootContainer;
          rootContainer.__vue_app__ = app;
          {
            app._instance = vnode.component;
            devtoolsInitApp(app, version);
          }
          return getComponentPublicInstance(vnode.component);
        }
      },
      onUnmount(cleanupFn) {
        pluginCleanupFns.push(cleanupFn);
      },
      unmount() {
        if (isMounted) {
          callWithAsyncErrorHandling(
            pluginCleanupFns,
            app._instance,
            16
          );
          render2(null, app._container);
          {
            app._instance = null;
            devtoolsUnmountApp(app);
          }
          delete app._container.__vue_app__;
        }
      },
      provide(key, value) {
        context.provides[key] = value;
        return app;
      },
      runWithContext(fn) {
        const lastApp = currentApp;
        currentApp = app;
        try {
          return fn();
        } finally {
          currentApp = lastApp;
        }
      }
    };
    return app;
  };
}
let currentApp = null;
function provide(key, value) {
  if (currentInstance) {
    let provides = currentInstance.provides;
    const parentProvides = currentInstance.parent && currentInstance.parent.provides;
    if (parentProvides === provides) {
      provides = currentInstance.provides = Object.create(parentProvides);
    }
    provides[key] = value;
  }
}
function inject(key, defaultValue, treatDefaultAsFactory = false) {
  const instance = getCurrentInstance();
  if (instance || currentApp) {
    let provides = currentApp ? currentApp._context.provides : instance ? instance.parent == null || instance.ce ? instance.vnode.appContext && instance.vnode.appContext.provides : instance.parent.provides : void 0;
    if (provides && key in provides) {
      return provides[key];
    } else if (arguments.length > 1) {
      return treatDefaultAsFactory && isFunction(defaultValue) ? defaultValue.call(instance && instance.proxy) : defaultValue;
    } else ;
  }
}
function hasInjectionContext() {
  return !!(getCurrentInstance() || currentApp);
}
const ssrContextKey = Symbol.for("v-scx");
const useSSRContext = () => {
  {
    const ctx = inject(ssrContextKey);
    return ctx;
  }
};
function watchEffect(effect2, options) {
  return doWatch(effect2, null, options);
}
function watchPostEffect(effect2, options) {
  return doWatch(
    effect2,
    null,
    { flush: "post" }
  );
}
function watchSyncEffect(effect2, options) {
  return doWatch(
    effect2,
    null,
    { flush: "sync" }
  );
}
function watch(source, cb2, options) {
  return doWatch(source, cb2, options);
}
function doWatch(source, cb2, options = EMPTY_OBJ) {
  const { immediate, deep, flush, once } = options;
  const baseWatchOptions = extend({}, options);
  const runsImmediately = cb2 && immediate || !cb2 && flush !== "post";
  let ssrCleanup;
  if (isInSSRComponentSetup) {
    if (flush === "sync") {
      const ctx = useSSRContext();
      ssrCleanup = ctx.__watcherHandles || (ctx.__watcherHandles = []);
    } else if (!runsImmediately) {
      const watchStopHandle = () => {
      };
      watchStopHandle.stop = NOOP;
      watchStopHandle.resume = NOOP;
      watchStopHandle.pause = NOOP;
      return watchStopHandle;
    }
  }
  const instance = currentInstance;
  baseWatchOptions.call = (fn, type, args) => callWithAsyncErrorHandling(fn, instance, type, args);
  let isPre = false;
  if (flush === "post") {
    baseWatchOptions.scheduler = (job) => {
      queuePostRenderEffect(job, instance && instance.suspense);
    };
  } else if (flush !== "sync") {
    isPre = true;
    baseWatchOptions.scheduler = (job, isFirstRun) => {
      if (isFirstRun) {
        job();
      } else {
        queueJob(job);
      }
    };
  }
  baseWatchOptions.augmentJob = (job) => {
    if (cb2) {
      job.flags |= 4;
    }
    if (isPre) {
      job.flags |= 2;
      if (instance) {
        job.id = instance.uid;
        job.i = instance;
      }
    }
  };
  const watchHandle = watch$1(source, cb2, baseWatchOptions);
  if (isInSSRComponentSetup) {
    if (ssrCleanup) {
      ssrCleanup.push(watchHandle);
    } else if (runsImmediately) {
      watchHandle();
    }
  }
  return watchHandle;
}
function instanceWatch(source, value, options) {
  const publicThis = this.proxy;
  const getter = isString(source) ? source.includes(".") ? createPathGetter(publicThis, source) : () => publicThis[source] : source.bind(publicThis, publicThis);
  let cb2;
  if (isFunction(value)) {
    cb2 = value;
  } else {
    cb2 = value.handler;
    options = value;
  }
  const reset2 = setCurrentInstance(this);
  const res = doWatch(getter, cb2.bind(publicThis), options);
  reset2();
  return res;
}
function createPathGetter(ctx, path) {
  const segments = path.split(".");
  return () => {
    let cur = ctx;
    for (let i = 0; i < segments.length && cur; i++) {
      cur = cur[segments[i]];
    }
    return cur;
  };
}
function useModel(props, name2, options = EMPTY_OBJ) {
  const i = getCurrentInstance();
  const camelizedName = camelize(name2);
  const hyphenatedName = hyphenate(name2);
  const modifiers = getModelModifiers(props, camelizedName);
  const res = customRef((track2, trigger2) => {
    let localValue;
    let prevSetValue = EMPTY_OBJ;
    let prevEmittedValue;
    watchSyncEffect(() => {
      const propValue = props[camelizedName];
      if (hasChanged(localValue, propValue)) {
        localValue = propValue;
        trigger2();
      }
    });
    return {
      get() {
        track2();
        return options.get ? options.get(localValue) : localValue;
      },
      set(value) {
        const emittedValue = options.set ? options.set(value) : value;
        if (!hasChanged(emittedValue, localValue) && !(prevSetValue !== EMPTY_OBJ && hasChanged(value, prevSetValue))) {
          return;
        }
        const rawProps = i.vnode.props;
        if (!(rawProps && // check if parent has passed v-model
        (name2 in rawProps || camelizedName in rawProps || hyphenatedName in rawProps) && (`onUpdate:${name2}` in rawProps || `onUpdate:${camelizedName}` in rawProps || `onUpdate:${hyphenatedName}` in rawProps))) {
          localValue = value;
          trigger2();
        }
        i.emit(`update:${name2}`, emittedValue);
        if (hasChanged(value, emittedValue) && hasChanged(value, prevSetValue) && !hasChanged(emittedValue, prevEmittedValue)) {
          trigger2();
        }
        prevSetValue = value;
        prevEmittedValue = emittedValue;
      }
    };
  });
  res[Symbol.iterator] = () => {
    let i2 = 0;
    return {
      next() {
        if (i2 < 2) {
          return { value: i2++ ? modifiers || EMPTY_OBJ : res, done: false };
        } else {
          return { done: true };
        }
      }
    };
  };
  return res;
}
const getModelModifiers = (props, modelName) => {
  return modelName === "modelValue" || modelName === "model-value" ? props.modelModifiers : props[`${modelName}Modifiers`] || props[`${camelize(modelName)}Modifiers`] || props[`${hyphenate(modelName)}Modifiers`];
};
function emit(instance, event, ...rawArgs) {
  if (instance.isUnmounted) return;
  const props = instance.vnode.props || EMPTY_OBJ;
  let args = rawArgs;
  const isModelListener2 = event.startsWith("update:");
  const modifiers = isModelListener2 && getModelModifiers(props, event.slice(7));
  if (modifiers) {
    if (modifiers.trim) {
      args = rawArgs.map((a) => isString(a) ? a.trim() : a);
    }
    if (modifiers.number) {
      args = rawArgs.map(looseToNumber);
    }
  }
  {
    devtoolsComponentEmit(instance, event, args);
  }
  let handlerName;
  let handler = props[handlerName = toHandlerKey(event)] || // also try camelCase event handler (#2249)
  props[handlerName = toHandlerKey(camelize(event))];
  if (!handler && isModelListener2) {
    handler = props[handlerName = toHandlerKey(hyphenate(event))];
  }
  if (handler) {
    callWithAsyncErrorHandling(
      handler,
      instance,
      6,
      args
    );
  }
  const onceHandler = props[handlerName + `Once`];
  if (onceHandler) {
    if (!instance.emitted) {
      instance.emitted = {};
    } else if (instance.emitted[handlerName]) {
      return;
    }
    instance.emitted[handlerName] = true;
    callWithAsyncErrorHandling(
      onceHandler,
      instance,
      6,
      args
    );
  }
}
const mixinEmitsCache = /* @__PURE__ */ new WeakMap();
function normalizeEmitsOptions(comp, appContext, asMixin = false) {
  const cache = asMixin ? mixinEmitsCache : appContext.emitsCache;
  const cached = cache.get(comp);
  if (cached !== void 0) {
    return cached;
  }
  const raw = comp.emits;
  let normalized = {};
  let hasExtends = false;
  if (!isFunction(comp)) {
    const extendEmits = (raw2) => {
      const normalizedFromExtend = normalizeEmitsOptions(raw2, appContext, true);
      if (normalizedFromExtend) {
        hasExtends = true;
        extend(normalized, normalizedFromExtend);
      }
    };
    if (!asMixin && appContext.mixins.length) {
      appContext.mixins.forEach(extendEmits);
    }
    if (comp.extends) {
      extendEmits(comp.extends);
    }
    if (comp.mixins) {
      comp.mixins.forEach(extendEmits);
    }
  }
  if (!raw && !hasExtends) {
    if (isObject(comp)) {
      cache.set(comp, null);
    }
    return null;
  }
  if (isArray(raw)) {
    raw.forEach((key) => normalized[key] = null);
  } else {
    extend(normalized, raw);
  }
  if (isObject(comp)) {
    cache.set(comp, normalized);
  }
  return normalized;
}
function isEmitListener(options, key) {
  if (!options || !isOn(key)) {
    return false;
  }
  key = key.slice(2).replace(/Once$/, "");
  return hasOwn(options, key[0].toLowerCase() + key.slice(1)) || hasOwn(options, hyphenate(key)) || hasOwn(options, key);
}
function markAttrsAccessed() {
}
function renderComponentRoot(instance) {
  const {
    type: Component,
    vnode,
    proxy,
    withProxy,
    propsOptions: [propsOptions],
    slots,
    attrs,
    emit: emit2,
    render: render2,
    renderCache,
    props,
    data,
    setupState,
    ctx,
    inheritAttrs
  } = instance;
  const prev = setCurrentRenderingInstance(instance);
  let result;
  let fallthroughAttrs;
  try {
    if (vnode.shapeFlag & 4) {
      const proxyToUse = withProxy || proxy;
      const thisProxy = false ? new Proxy(proxyToUse, {
        get(target, key, receiver) {
          warn$1(
            `Property '${String(
              key
            )}' was accessed via 'this'. Avoid using 'this' in templates.`
          );
          return Reflect.get(target, key, receiver);
        }
      }) : proxyToUse;
      result = normalizeVNode(
        render2.call(
          thisProxy,
          proxyToUse,
          renderCache,
          false ? shallowReadonly(props) : props,
          setupState,
          data,
          ctx
        )
      );
      fallthroughAttrs = attrs;
    } else {
      const render22 = Component;
      if (false) ;
      result = normalizeVNode(
        render22.length > 1 ? render22(
          false ? shallowReadonly(props) : props,
          false ? {
            get attrs() {
              markAttrsAccessed();
              return shallowReadonly(attrs);
            },
            slots,
            emit: emit2
          } : { attrs, slots, emit: emit2 }
        ) : render22(
          false ? shallowReadonly(props) : props,
          null
        )
      );
      fallthroughAttrs = Component.props ? attrs : getFunctionalFallthrough(attrs);
    }
  } catch (err) {
    blockStack.length = 0;
    handleError(err, instance, 1);
    result = createVNode(Comment);
  }
  let root = result;
  if (fallthroughAttrs && inheritAttrs !== false) {
    const keys = Object.keys(fallthroughAttrs);
    const { shapeFlag } = root;
    if (keys.length) {
      if (shapeFlag & (1 | 6)) {
        if (propsOptions && keys.some(isModelListener)) {
          fallthroughAttrs = filterModelListeners(
            fallthroughAttrs,
            propsOptions
          );
        }
        root = cloneVNode(root, fallthroughAttrs, false, true);
      }
    }
  }
  if (vnode.dirs) {
    root = cloneVNode(root, null, false, true);
    root.dirs = root.dirs ? root.dirs.concat(vnode.dirs) : vnode.dirs;
  }
  if (vnode.transition) {
    setTransitionHooks(root, vnode.transition);
  }
  {
    result = root;
  }
  setCurrentRenderingInstance(prev);
  return result;
}
function filterSingleRoot(children, recurse = true) {
  let singleRoot;
  for (let i = 0; i < children.length; i++) {
    const child = children[i];
    if (isVNode(child)) {
      if (child.type !== Comment || child.children === "v-if") {
        if (singleRoot) {
          return;
        } else {
          singleRoot = child;
        }
      }
    } else {
      return;
    }
  }
  return singleRoot;
}
const getFunctionalFallthrough = (attrs) => {
  let res;
  for (const key in attrs) {
    if (key === "class" || key === "style" || isOn(key)) {
      (res || (res = {}))[key] = attrs[key];
    }
  }
  return res;
};
const filterModelListeners = (attrs, props) => {
  const res = {};
  for (const key in attrs) {
    if (!isModelListener(key) || !(key.slice(9) in props)) {
      res[key] = attrs[key];
    }
  }
  return res;
};
function shouldUpdateComponent(prevVNode, nextVNode, optimized) {
  const { props: prevProps, children: prevChildren, component } = prevVNode;
  const { props: nextProps, children: nextChildren, patchFlag } = nextVNode;
  const emits = component.emitsOptions;
  if (nextVNode.dirs || nextVNode.transition) {
    return true;
  }
  if (optimized && patchFlag >= 0) {
    if (patchFlag & 1024) {
      return true;
    }
    if (patchFlag & 16) {
      if (!prevProps) {
        return !!nextProps;
      }
      return hasPropsChanged(prevProps, nextProps, emits);
    } else if (patchFlag & 8) {
      const dynamicProps = nextVNode.dynamicProps;
      for (let i = 0; i < dynamicProps.length; i++) {
        const key = dynamicProps[i];
        if (nextProps[key] !== prevProps[key] && !isEmitListener(emits, key)) {
          return true;
        }
      }
    }
  } else {
    if (prevChildren || nextChildren) {
      if (!nextChildren || !nextChildren.$stable) {
        return true;
      }
    }
    if (prevProps === nextProps) {
      return false;
    }
    if (!prevProps) {
      return !!nextProps;
    }
    if (!nextProps) {
      return true;
    }
    return hasPropsChanged(prevProps, nextProps, emits);
  }
  return false;
}
function hasPropsChanged(prevProps, nextProps, emitsOptions) {
  const nextKeys = Object.keys(nextProps);
  if (nextKeys.length !== Object.keys(prevProps).length) {
    return true;
  }
  for (let i = 0; i < nextKeys.length; i++) {
    const key = nextKeys[i];
    if (nextProps[key] !== prevProps[key] && !isEmitListener(emitsOptions, key)) {
      return true;
    }
  }
  return false;
}
function updateHOCHostEl({ vnode, parent }, el2) {
  while (parent) {
    const root = parent.subTree;
    if (root.suspense && root.suspense.activeBranch === vnode) {
      root.el = vnode.el;
    }
    if (root === vnode) {
      (vnode = parent.vnode).el = el2;
      parent = parent.parent;
    } else {
      break;
    }
  }
}
const internalObjectProto = {};
const createInternalObject = () => Object.create(internalObjectProto);
const isInternalObject = (obj) => Object.getPrototypeOf(obj) === internalObjectProto;
function initProps(instance, rawProps, isStateful, isSSR = false) {
  const props = {};
  const attrs = createInternalObject();
  instance.propsDefaults = /* @__PURE__ */ Object.create(null);
  setFullProps(instance, rawProps, props, attrs);
  for (const key in instance.propsOptions[0]) {
    if (!(key in props)) {
      props[key] = void 0;
    }
  }
  if (isStateful) {
    instance.props = isSSR ? props : shallowReactive(props);
  } else {
    if (!instance.type.props) {
      instance.props = attrs;
    } else {
      instance.props = props;
    }
  }
  instance.attrs = attrs;
}
function updateProps(instance, rawProps, rawPrevProps, optimized) {
  const {
    props,
    attrs,
    vnode: { patchFlag }
  } = instance;
  const rawCurrentProps = toRaw(props);
  const [options] = instance.propsOptions;
  let hasAttrsChanged = false;
  if (
    // always force full diff in dev
    // - #1942 if hmr is enabled with sfc component
    // - vite#872 non-sfc component used by sfc component
    (optimized || patchFlag > 0) && !(patchFlag & 16)
  ) {
    if (patchFlag & 8) {
      const propsToUpdate = instance.vnode.dynamicProps;
      for (let i = 0; i < propsToUpdate.length; i++) {
        let key = propsToUpdate[i];
        if (isEmitListener(instance.emitsOptions, key)) {
          continue;
        }
        const value = rawProps[key];
        if (options) {
          if (hasOwn(attrs, key)) {
            if (value !== attrs[key]) {
              attrs[key] = value;
              hasAttrsChanged = true;
            }
          } else {
            const camelizedKey = camelize(key);
            props[camelizedKey] = resolvePropValue(
              options,
              rawCurrentProps,
              camelizedKey,
              value,
              instance,
              false
            );
          }
        } else {
          if (value !== attrs[key]) {
            attrs[key] = value;
            hasAttrsChanged = true;
          }
        }
      }
    }
  } else {
    if (setFullProps(instance, rawProps, props, attrs)) {
      hasAttrsChanged = true;
    }
    let kebabKey;
    for (const key in rawCurrentProps) {
      if (!rawProps || // for camelCase
      !hasOwn(rawProps, key) && // it's possible the original props was passed in as kebab-case
      // and converted to camelCase (#955)
      ((kebabKey = hyphenate(key)) === key || !hasOwn(rawProps, kebabKey))) {
        if (options) {
          if (rawPrevProps && // for camelCase
          (rawPrevProps[key] !== void 0 || // for kebab-case
          rawPrevProps[kebabKey] !== void 0)) {
            props[key] = resolvePropValue(
              options,
              rawCurrentProps,
              key,
              void 0,
              instance,
              true
            );
          }
        } else {
          delete props[key];
        }
      }
    }
    if (attrs !== rawCurrentProps) {
      for (const key in attrs) {
        if (!rawProps || !hasOwn(rawProps, key) && true) {
          delete attrs[key];
          hasAttrsChanged = true;
        }
      }
    }
  }
  if (hasAttrsChanged) {
    trigger(instance.attrs, "set", "");
  }
}
function setFullProps(instance, rawProps, props, attrs) {
  const [options, needCastKeys] = instance.propsOptions;
  let hasAttrsChanged = false;
  let rawCastValues;
  if (rawProps) {
    for (let key in rawProps) {
      if (isReservedProp(key)) {
        continue;
      }
      const value = rawProps[key];
      let camelKey;
      if (options && hasOwn(options, camelKey = camelize(key))) {
        if (!needCastKeys || !needCastKeys.includes(camelKey)) {
          props[camelKey] = value;
        } else {
          (rawCastValues || (rawCastValues = {}))[camelKey] = value;
        }
      } else if (!isEmitListener(instance.emitsOptions, key)) {
        if (!(key in attrs) || value !== attrs[key]) {
          attrs[key] = value;
          hasAttrsChanged = true;
        }
      }
    }
  }
  if (needCastKeys) {
    const rawCurrentProps = toRaw(props);
    const castValues = rawCastValues || EMPTY_OBJ;
    for (let i = 0; i < needCastKeys.length; i++) {
      const key = needCastKeys[i];
      props[key] = resolvePropValue(
        options,
        rawCurrentProps,
        key,
        castValues[key],
        instance,
        !hasOwn(castValues, key)
      );
    }
  }
  return hasAttrsChanged;
}
function resolvePropValue(options, props, key, value, instance, isAbsent) {
  const opt = options[key];
  if (opt != null) {
    const hasDefault = hasOwn(opt, "default");
    if (hasDefault && value === void 0) {
      const defaultValue = opt.default;
      if (opt.type !== Function && !opt.skipFactory && isFunction(defaultValue)) {
        const { propsDefaults } = instance;
        if (key in propsDefaults) {
          value = propsDefaults[key];
        } else {
          const reset2 = setCurrentInstance(instance);
          value = propsDefaults[key] = defaultValue.call(
            null,
            props
          );
          reset2();
        }
      } else {
        value = defaultValue;
      }
      if (instance.ce) {
        instance.ce._setProp(key, value);
      }
    }
    if (opt[
      0
      /* shouldCast */
    ]) {
      if (isAbsent && !hasDefault) {
        value = false;
      } else if (opt[
        1
        /* shouldCastTrue */
      ] && (value === "" || value === hyphenate(key))) {
        value = true;
      }
    }
  }
  return value;
}
const mixinPropsCache = /* @__PURE__ */ new WeakMap();
function normalizePropsOptions(comp, appContext, asMixin = false) {
  const cache = asMixin ? mixinPropsCache : appContext.propsCache;
  const cached = cache.get(comp);
  if (cached) {
    return cached;
  }
  const raw = comp.props;
  const normalized = {};
  const needCastKeys = [];
  let hasExtends = false;
  if (!isFunction(comp)) {
    const extendProps = (raw2) => {
      hasExtends = true;
      const [props, keys] = normalizePropsOptions(raw2, appContext, true);
      extend(normalized, props);
      if (keys) needCastKeys.push(...keys);
    };
    if (!asMixin && appContext.mixins.length) {
      appContext.mixins.forEach(extendProps);
    }
    if (comp.extends) {
      extendProps(comp.extends);
    }
    if (comp.mixins) {
      comp.mixins.forEach(extendProps);
    }
  }
  if (!raw && !hasExtends) {
    if (isObject(comp)) {
      cache.set(comp, EMPTY_ARR);
    }
    return EMPTY_ARR;
  }
  if (isArray(raw)) {
    for (let i = 0; i < raw.length; i++) {
      const normalizedKey = camelize(raw[i]);
      if (validatePropName(normalizedKey)) {
        normalized[normalizedKey] = EMPTY_OBJ;
      }
    }
  } else if (raw) {
    for (const key in raw) {
      const normalizedKey = camelize(key);
      if (validatePropName(normalizedKey)) {
        const opt = raw[key];
        const prop = normalized[normalizedKey] = isArray(opt) || isFunction(opt) ? { type: opt } : extend({}, opt);
        const propType = prop.type;
        let shouldCast = false;
        let shouldCastTrue = true;
        if (isArray(propType)) {
          for (let index = 0; index < propType.length; ++index) {
            const type = propType[index];
            const typeName = isFunction(type) && type.name;
            if (typeName === "Boolean") {
              shouldCast = true;
              break;
            } else if (typeName === "String") {
              shouldCastTrue = false;
            }
          }
        } else {
          shouldCast = isFunction(propType) && propType.name === "Boolean";
        }
        prop[
          0
          /* shouldCast */
        ] = shouldCast;
        prop[
          1
          /* shouldCastTrue */
        ] = shouldCastTrue;
        if (shouldCast || hasOwn(prop, "default")) {
          needCastKeys.push(normalizedKey);
        }
      }
    }
  }
  const res = [normalized, needCastKeys];
  if (isObject(comp)) {
    cache.set(comp, res);
  }
  return res;
}
function validatePropName(key) {
  if (key[0] !== "$" && !isReservedProp(key)) {
    return true;
  }
  return false;
}
const isInternalKey = (key) => key === "_" || key === "_ctx" || key === "$stable";
const normalizeSlotValue = (value) => isArray(value) ? value.map(normalizeVNode) : [normalizeVNode(value)];
const normalizeSlot = (key, rawSlot, ctx) => {
  if (rawSlot._n) {
    return rawSlot;
  }
  const normalized = withCtx((...args) => {
    if (false) ;
    return normalizeSlotValue(rawSlot(...args));
  }, ctx);
  normalized._c = false;
  return normalized;
};
const normalizeObjectSlots = (rawSlots, slots, instance) => {
  const ctx = rawSlots._ctx;
  for (const key in rawSlots) {
    if (isInternalKey(key)) continue;
    const value = rawSlots[key];
    if (isFunction(value)) {
      slots[key] = normalizeSlot(key, value, ctx);
    } else if (value != null) {
      const normalized = normalizeSlotValue(value);
      slots[key] = () => normalized;
    }
  }
};
const normalizeVNodeSlots = (instance, children) => {
  const normalized = normalizeSlotValue(children);
  instance.slots.default = () => normalized;
};
const assignSlots = (slots, children, optimized) => {
  for (const key in children) {
    if (optimized || !isInternalKey(key)) {
      slots[key] = children[key];
    }
  }
};
const initSlots = (instance, children, optimized) => {
  const slots = instance.slots = createInternalObject();
  if (instance.vnode.shapeFlag & 32) {
    const type = children._;
    if (type) {
      assignSlots(slots, children, optimized);
      if (optimized) {
        def(slots, "_", type, true);
      }
    } else {
      normalizeObjectSlots(children, slots);
    }
  } else if (children) {
    normalizeVNodeSlots(instance, children);
  }
};
const updateSlots = (instance, children, optimized) => {
  const { vnode, slots } = instance;
  let needDeletionCheck = true;
  let deletionComparisonTarget = EMPTY_OBJ;
  if (vnode.shapeFlag & 32) {
    const type = children._;
    if (type) {
      if (optimized && type === 1) {
        needDeletionCheck = false;
      } else {
        assignSlots(slots, children, optimized);
      }
    } else {
      needDeletionCheck = !children.$stable;
      normalizeObjectSlots(children, slots);
    }
    deletionComparisonTarget = children;
  } else if (children) {
    normalizeVNodeSlots(instance, children);
    deletionComparisonTarget = { default: 1 };
  }
  if (needDeletionCheck) {
    for (const key in slots) {
      if (!isInternalKey(key) && deletionComparisonTarget[key] == null) {
        delete slots[key];
      }
    }
  }
};
const queuePostRenderEffect = queueEffectWithSuspense;
function createRenderer(options) {
  return baseCreateRenderer(options);
}
function createHydrationRenderer(options) {
  return baseCreateRenderer(options, createHydrationFunctions);
}
function baseCreateRenderer(options, createHydrationFns) {
  const target = getGlobalThis();
  target.__VUE__ = true;
  {
    setDevtoolsHook$1(target.__VUE_DEVTOOLS_GLOBAL_HOOK__, target);
  }
  const {
    insert: hostInsert,
    remove: hostRemove,
    patchProp: hostPatchProp,
    createElement: hostCreateElement,
    createText: hostCreateText,
    createComment: hostCreateComment,
    setText: hostSetText,
    setElementText: hostSetElementText,
    parentNode: hostParentNode,
    nextSibling: hostNextSibling,
    setScopeId: hostSetScopeId = NOOP,
    insertStaticContent: hostInsertStaticContent
  } = options;
  const patch = (n1, n2, container, anchor = null, parentComponent = null, parentSuspense = null, namespace = void 0, slotScopeIds = null, optimized = !!n2.dynamicChildren) => {
    if (n1 === n2) {
      return;
    }
    if (n1 && !isSameVNodeType(n1, n2)) {
      anchor = getNextHostNode(n1);
      unmount(n1, parentComponent, parentSuspense, true);
      n1 = null;
    }
    if (n2.patchFlag === -2) {
      optimized = false;
      n2.dynamicChildren = null;
    }
    const { type, ref: ref3, shapeFlag } = n2;
    switch (type) {
      case Text:
        processText(n1, n2, container, anchor);
        break;
      case Comment:
        processCommentNode(n1, n2, container, anchor);
        break;
      case Static:
        if (n1 == null) {
          mountStaticNode(n2, container, anchor, namespace);
        }
        break;
      case Fragment:
        processFragment(
          n1,
          n2,
          container,
          anchor,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
        break;
      default:
        if (shapeFlag & 1) {
          processElement(
            n1,
            n2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        } else if (shapeFlag & 6) {
          processComponent(
            n1,
            n2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        } else if (shapeFlag & 64) {
          type.process(
            n1,
            n2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized,
            internals
          );
        } else if (shapeFlag & 128) {
          type.process(
            n1,
            n2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized,
            internals
          );
        } else ;
    }
    if (ref3 != null && parentComponent) {
      setRef(ref3, n1 && n1.ref, parentSuspense, n2 || n1, !n2);
    } else if (ref3 == null && n1 && n1.ref != null) {
      setRef(n1.ref, null, parentSuspense, n1, true);
    }
  };
  const processText = (n1, n2, container, anchor) => {
    if (n1 == null) {
      hostInsert(
        n2.el = hostCreateText(n2.children),
        container,
        anchor
      );
    } else {
      const el2 = n2.el = n1.el;
      if (n2.children !== n1.children) {
        hostSetText(el2, n2.children);
      }
    }
  };
  const processCommentNode = (n1, n2, container, anchor) => {
    if (n1 == null) {
      hostInsert(
        n2.el = hostCreateComment(n2.children || ""),
        container,
        anchor
      );
    } else {
      n2.el = n1.el;
    }
  };
  const mountStaticNode = (n2, container, anchor, namespace) => {
    [n2.el, n2.anchor] = hostInsertStaticContent(
      n2.children,
      container,
      anchor,
      namespace,
      n2.el,
      n2.anchor
    );
  };
  const moveStaticNode = ({ el: el2, anchor }, container, nextSibling) => {
    let next;
    while (el2 && el2 !== anchor) {
      next = hostNextSibling(el2);
      hostInsert(el2, container, nextSibling);
      el2 = next;
    }
    hostInsert(anchor, container, nextSibling);
  };
  const removeStaticNode = ({ el: el2, anchor }) => {
    let next;
    while (el2 && el2 !== anchor) {
      next = hostNextSibling(el2);
      hostRemove(el2);
      el2 = next;
    }
    hostRemove(anchor);
  };
  const processElement = (n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    if (n2.type === "svg") {
      namespace = "svg";
    } else if (n2.type === "math") {
      namespace = "mathml";
    }
    if (n1 == null) {
      mountElement(
        n2,
        container,
        anchor,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized
      );
    } else {
      const customElement = !!(n1.el && n1.el._isVueCE) ? n1.el : null;
      try {
        if (customElement) {
          customElement._beginPatch();
        }
        patchElement(
          n1,
          n2,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
      } finally {
        if (customElement) {
          customElement._endPatch();
        }
      }
    }
  };
  const mountElement = (vnode, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    let el2;
    let vnodeHook;
    const { props, shapeFlag, transition, dirs } = vnode;
    el2 = vnode.el = hostCreateElement(
      vnode.type,
      namespace,
      props && props.is,
      props
    );
    if (shapeFlag & 8) {
      hostSetElementText(el2, vnode.children);
    } else if (shapeFlag & 16) {
      mountChildren(
        vnode.children,
        el2,
        null,
        parentComponent,
        parentSuspense,
        resolveChildrenNamespace(vnode, namespace),
        slotScopeIds,
        optimized
      );
    }
    if (dirs) {
      invokeDirectiveHook(vnode, null, parentComponent, "created");
    }
    setScopeId(el2, vnode, vnode.scopeId, slotScopeIds, parentComponent);
    if (props) {
      for (const key in props) {
        if (key !== "value" && !isReservedProp(key)) {
          hostPatchProp(el2, key, null, props[key], namespace, parentComponent);
        }
      }
      if ("value" in props) {
        hostPatchProp(el2, "value", null, props.value, namespace);
      }
      if (vnodeHook = props.onVnodeBeforeMount) {
        invokeVNodeHook(vnodeHook, parentComponent, vnode);
      }
    }
    {
      def(el2, "__vnode", vnode, true);
      def(el2, "__vueParentComponent", parentComponent, true);
    }
    if (dirs) {
      invokeDirectiveHook(vnode, null, parentComponent, "beforeMount");
    }
    const needCallTransitionHooks = needTransition(parentSuspense, transition);
    if (needCallTransitionHooks) {
      transition.beforeEnter(el2);
    }
    hostInsert(el2, container, anchor);
    if ((vnodeHook = props && props.onVnodeMounted) || needCallTransitionHooks || dirs) {
      queuePostRenderEffect(() => {
        vnodeHook && invokeVNodeHook(vnodeHook, parentComponent, vnode);
        needCallTransitionHooks && transition.enter(el2);
        dirs && invokeDirectiveHook(vnode, null, parentComponent, "mounted");
      }, parentSuspense);
    }
  };
  const setScopeId = (el2, vnode, scopeId, slotScopeIds, parentComponent) => {
    if (scopeId) {
      hostSetScopeId(el2, scopeId);
    }
    if (slotScopeIds) {
      for (let i = 0; i < slotScopeIds.length; i++) {
        hostSetScopeId(el2, slotScopeIds[i]);
      }
    }
    if (parentComponent) {
      let subTree = parentComponent.subTree;
      if (vnode === subTree || isSuspense(subTree.type) && (subTree.ssContent === vnode || subTree.ssFallback === vnode)) {
        const parentVNode = parentComponent.vnode;
        setScopeId(
          el2,
          parentVNode,
          parentVNode.scopeId,
          parentVNode.slotScopeIds,
          parentComponent.parent
        );
      }
    }
  };
  const mountChildren = (children, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized, start = 0) => {
    for (let i = start; i < children.length; i++) {
      const child = children[i] = optimized ? cloneIfMounted(children[i]) : normalizeVNode(children[i]);
      patch(
        null,
        child,
        container,
        anchor,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized
      );
    }
  };
  const patchElement = (n1, n2, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    const el2 = n2.el = n1.el;
    {
      el2.__vnode = n2;
    }
    let { patchFlag, dynamicChildren, dirs } = n2;
    patchFlag |= n1.patchFlag & 16;
    const oldProps = n1.props || EMPTY_OBJ;
    const newProps = n2.props || EMPTY_OBJ;
    let vnodeHook;
    parentComponent && toggleRecurse(parentComponent, false);
    if (vnodeHook = newProps.onVnodeBeforeUpdate) {
      invokeVNodeHook(vnodeHook, parentComponent, n2, n1);
    }
    if (dirs) {
      invokeDirectiveHook(n2, n1, parentComponent, "beforeUpdate");
    }
    parentComponent && toggleRecurse(parentComponent, true);
    if (oldProps.innerHTML && newProps.innerHTML == null || oldProps.textContent && newProps.textContent == null) {
      hostSetElementText(el2, "");
    }
    if (dynamicChildren) {
      patchBlockChildren(
        n1.dynamicChildren,
        dynamicChildren,
        el2,
        parentComponent,
        parentSuspense,
        resolveChildrenNamespace(n2, namespace),
        slotScopeIds
      );
    } else if (!optimized) {
      patchChildren(
        n1,
        n2,
        el2,
        null,
        parentComponent,
        parentSuspense,
        resolveChildrenNamespace(n2, namespace),
        slotScopeIds,
        false
      );
    }
    if (patchFlag > 0) {
      if (patchFlag & 16) {
        patchProps(el2, oldProps, newProps, parentComponent, namespace);
      } else {
        if (patchFlag & 2) {
          if (oldProps.class !== newProps.class) {
            hostPatchProp(el2, "class", null, newProps.class, namespace);
          }
        }
        if (patchFlag & 4) {
          hostPatchProp(el2, "style", oldProps.style, newProps.style, namespace);
        }
        if (patchFlag & 8) {
          const propsToUpdate = n2.dynamicProps;
          for (let i = 0; i < propsToUpdate.length; i++) {
            const key = propsToUpdate[i];
            const prev = oldProps[key];
            const next = newProps[key];
            if (next !== prev || key === "value") {
              hostPatchProp(el2, key, prev, next, namespace, parentComponent);
            }
          }
        }
      }
      if (patchFlag & 1) {
        if (n1.children !== n2.children) {
          hostSetElementText(el2, n2.children);
        }
      }
    } else if (!optimized && dynamicChildren == null) {
      patchProps(el2, oldProps, newProps, parentComponent, namespace);
    }
    if ((vnodeHook = newProps.onVnodeUpdated) || dirs) {
      queuePostRenderEffect(() => {
        vnodeHook && invokeVNodeHook(vnodeHook, parentComponent, n2, n1);
        dirs && invokeDirectiveHook(n2, n1, parentComponent, "updated");
      }, parentSuspense);
    }
  };
  const patchBlockChildren = (oldChildren, newChildren, fallbackContainer, parentComponent, parentSuspense, namespace, slotScopeIds) => {
    for (let i = 0; i < newChildren.length; i++) {
      const oldVNode = oldChildren[i];
      const newVNode = newChildren[i];
      const container = (
        // oldVNode may be an errored async setup() component inside Suspense
        // which will not have a mounted element
        oldVNode.el && // - In the case of a Fragment, we need to provide the actual parent
        // of the Fragment itself so it can move its children.
        (oldVNode.type === Fragment || // - In the case of different nodes, there is going to be a replacement
        // which also requires the correct parent container
        !isSameVNodeType(oldVNode, newVNode) || // - In the case of a component, it could contain anything.
        oldVNode.shapeFlag & (6 | 64 | 128)) ? hostParentNode(oldVNode.el) : (
          // In other cases, the parent container is not actually used so we
          // just pass the block element here to avoid a DOM parentNode call.
          fallbackContainer
        )
      );
      patch(
        oldVNode,
        newVNode,
        container,
        null,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        true
      );
    }
  };
  const patchProps = (el2, oldProps, newProps, parentComponent, namespace) => {
    if (oldProps !== newProps) {
      if (oldProps !== EMPTY_OBJ) {
        for (const key in oldProps) {
          if (!isReservedProp(key) && !(key in newProps)) {
            hostPatchProp(
              el2,
              key,
              oldProps[key],
              null,
              namespace,
              parentComponent
            );
          }
        }
      }
      for (const key in newProps) {
        if (isReservedProp(key)) continue;
        const next = newProps[key];
        const prev = oldProps[key];
        if (next !== prev && key !== "value") {
          hostPatchProp(el2, key, prev, next, namespace, parentComponent);
        }
      }
      if ("value" in newProps) {
        hostPatchProp(el2, "value", oldProps.value, newProps.value, namespace);
      }
    }
  };
  const processFragment = (n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    const fragmentStartAnchor = n2.el = n1 ? n1.el : hostCreateText("");
    const fragmentEndAnchor = n2.anchor = n1 ? n1.anchor : hostCreateText("");
    let { patchFlag, dynamicChildren, slotScopeIds: fragmentSlotScopeIds } = n2;
    if (fragmentSlotScopeIds) {
      slotScopeIds = slotScopeIds ? slotScopeIds.concat(fragmentSlotScopeIds) : fragmentSlotScopeIds;
    }
    if (n1 == null) {
      hostInsert(fragmentStartAnchor, container, anchor);
      hostInsert(fragmentEndAnchor, container, anchor);
      mountChildren(
        // #10007
        // such fragment like `<></>` will be compiled into
        // a fragment which doesn't have a children.
        // In this case fallback to an empty array
        n2.children || [],
        container,
        fragmentEndAnchor,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized
      );
    } else {
      if (patchFlag > 0 && patchFlag & 64 && dynamicChildren && // #2715 the previous fragment could've been a BAILed one as a result
      // of renderSlot() with no valid children
      n1.dynamicChildren) {
        patchBlockChildren(
          n1.dynamicChildren,
          dynamicChildren,
          container,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds
        );
        if (
          // #2080 if the stable fragment has a key, it's a <template v-for> that may
          //  get moved around. Make sure all root level vnodes inherit el.
          // #2134 or if it's a component root, it may also get moved around
          // as the component is being moved.
          n2.key != null || parentComponent && n2 === parentComponent.subTree
        ) {
          traverseStaticChildren(
            n1,
            n2,
            true
            /* shallow */
          );
        }
      } else {
        patchChildren(
          n1,
          n2,
          container,
          fragmentEndAnchor,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
      }
    }
  };
  const processComponent = (n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    n2.slotScopeIds = slotScopeIds;
    if (n1 == null) {
      if (n2.shapeFlag & 512) {
        parentComponent.ctx.activate(
          n2,
          container,
          anchor,
          namespace,
          optimized
        );
      } else {
        mountComponent(
          n2,
          container,
          anchor,
          parentComponent,
          parentSuspense,
          namespace,
          optimized
        );
      }
    } else {
      updateComponent(n1, n2, optimized);
    }
  };
  const mountComponent = (initialVNode, container, anchor, parentComponent, parentSuspense, namespace, optimized) => {
    const instance = initialVNode.component = createComponentInstance(
      initialVNode,
      parentComponent,
      parentSuspense
    );
    if (isKeepAlive(initialVNode)) {
      instance.ctx.renderer = internals;
    }
    {
      setupComponent(instance, false, optimized);
    }
    if (instance.asyncDep) {
      parentSuspense && parentSuspense.registerDep(instance, setupRenderEffect, optimized);
      if (!initialVNode.el) {
        const placeholder = instance.subTree = createVNode(Comment);
        processCommentNode(null, placeholder, container, anchor);
        initialVNode.placeholder = placeholder.el;
      }
    } else {
      setupRenderEffect(
        instance,
        initialVNode,
        container,
        anchor,
        parentSuspense,
        namespace,
        optimized
      );
    }
  };
  const updateComponent = (n1, n2, optimized) => {
    const instance = n2.component = n1.component;
    if (shouldUpdateComponent(n1, n2, optimized)) {
      if (instance.asyncDep && !instance.asyncResolved) {
        updateComponentPreRender(instance, n2, optimized);
        return;
      } else {
        instance.next = n2;
        instance.update();
      }
    } else {
      n2.el = n1.el;
      instance.vnode = n2;
    }
  };
  const setupRenderEffect = (instance, initialVNode, container, anchor, parentSuspense, namespace, optimized) => {
    const componentUpdateFn = () => {
      if (!instance.isMounted) {
        let vnodeHook;
        const { el: el2, props } = initialVNode;
        const { bm, m, parent, root, type } = instance;
        const isAsyncWrapperVNode = isAsyncWrapper(initialVNode);
        toggleRecurse(instance, false);
        if (bm) {
          invokeArrayFns(bm);
        }
        if (!isAsyncWrapperVNode && (vnodeHook = props && props.onVnodeBeforeMount)) {
          invokeVNodeHook(vnodeHook, parent, initialVNode);
        }
        toggleRecurse(instance, true);
        if (el2 && hydrateNode) {
          const hydrateSubTree = () => {
            instance.subTree = renderComponentRoot(instance);
            hydrateNode(
              el2,
              instance.subTree,
              instance,
              parentSuspense,
              null
            );
          };
          if (isAsyncWrapperVNode && type.__asyncHydrate) {
            type.__asyncHydrate(
              el2,
              instance,
              hydrateSubTree
            );
          } else {
            hydrateSubTree();
          }
        } else {
          if (root.ce && // @ts-expect-error _def is private
          root.ce._def.shadowRoot !== false) {
            root.ce._injectChildStyle(type);
          }
          const subTree = instance.subTree = renderComponentRoot(instance);
          patch(
            null,
            subTree,
            container,
            anchor,
            instance,
            parentSuspense,
            namespace
          );
          initialVNode.el = subTree.el;
        }
        if (m) {
          queuePostRenderEffect(m, parentSuspense);
        }
        if (!isAsyncWrapperVNode && (vnodeHook = props && props.onVnodeMounted)) {
          const scopedInitialVNode = initialVNode;
          queuePostRenderEffect(
            () => invokeVNodeHook(vnodeHook, parent, scopedInitialVNode),
            parentSuspense
          );
        }
        if (initialVNode.shapeFlag & 256 || parent && isAsyncWrapper(parent.vnode) && parent.vnode.shapeFlag & 256) {
          instance.a && queuePostRenderEffect(instance.a, parentSuspense);
        }
        instance.isMounted = true;
        {
          devtoolsComponentAdded(instance);
        }
        initialVNode = container = anchor = null;
      } else {
        let { next, bu, u, parent, vnode } = instance;
        {
          const nonHydratedAsyncRoot = locateNonHydratedAsyncRoot(instance);
          if (nonHydratedAsyncRoot) {
            if (next) {
              next.el = vnode.el;
              updateComponentPreRender(instance, next, optimized);
            }
            nonHydratedAsyncRoot.asyncDep.then(() => {
              if (!instance.isUnmounted) {
                componentUpdateFn();
              }
            });
            return;
          }
        }
        let originNext = next;
        let vnodeHook;
        toggleRecurse(instance, false);
        if (next) {
          next.el = vnode.el;
          updateComponentPreRender(instance, next, optimized);
        } else {
          next = vnode;
        }
        if (bu) {
          invokeArrayFns(bu);
        }
        if (vnodeHook = next.props && next.props.onVnodeBeforeUpdate) {
          invokeVNodeHook(vnodeHook, parent, next, vnode);
        }
        toggleRecurse(instance, true);
        const nextTree = renderComponentRoot(instance);
        const prevTree = instance.subTree;
        instance.subTree = nextTree;
        patch(
          prevTree,
          nextTree,
          // parent may have changed if it's in a teleport
          hostParentNode(prevTree.el),
          // anchor may have changed if it's in a fragment
          getNextHostNode(prevTree),
          instance,
          parentSuspense,
          namespace
        );
        next.el = nextTree.el;
        if (originNext === null) {
          updateHOCHostEl(instance, nextTree.el);
        }
        if (u) {
          queuePostRenderEffect(u, parentSuspense);
        }
        if (vnodeHook = next.props && next.props.onVnodeUpdated) {
          queuePostRenderEffect(
            () => invokeVNodeHook(vnodeHook, parent, next, vnode),
            parentSuspense
          );
        }
        {
          devtoolsComponentUpdated(instance);
        }
      }
    };
    instance.scope.on();
    const effect2 = instance.effect = new ReactiveEffect(componentUpdateFn);
    instance.scope.off();
    const update = instance.update = effect2.run.bind(effect2);
    const job = instance.job = effect2.runIfDirty.bind(effect2);
    job.i = instance;
    job.id = instance.uid;
    effect2.scheduler = () => queueJob(job);
    toggleRecurse(instance, true);
    update();
  };
  const updateComponentPreRender = (instance, nextVNode, optimized) => {
    nextVNode.component = instance;
    const prevProps = instance.vnode.props;
    instance.vnode = nextVNode;
    instance.next = null;
    updateProps(instance, nextVNode.props, prevProps, optimized);
    updateSlots(instance, nextVNode.children, optimized);
    pauseTracking();
    flushPreFlushCbs(instance);
    resetTracking();
  };
  const patchChildren = (n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized = false) => {
    const c1 = n1 && n1.children;
    const prevShapeFlag = n1 ? n1.shapeFlag : 0;
    const c2 = n2.children;
    const { patchFlag, shapeFlag } = n2;
    if (patchFlag > 0) {
      if (patchFlag & 128) {
        patchKeyedChildren(
          c1,
          c2,
          container,
          anchor,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
        return;
      } else if (patchFlag & 256) {
        patchUnkeyedChildren(
          c1,
          c2,
          container,
          anchor,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
        return;
      }
    }
    if (shapeFlag & 8) {
      if (prevShapeFlag & 16) {
        unmountChildren(c1, parentComponent, parentSuspense);
      }
      if (c2 !== c1) {
        hostSetElementText(container, c2);
      }
    } else {
      if (prevShapeFlag & 16) {
        if (shapeFlag & 16) {
          patchKeyedChildren(
            c1,
            c2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        } else {
          unmountChildren(c1, parentComponent, parentSuspense, true);
        }
      } else {
        if (prevShapeFlag & 8) {
          hostSetElementText(container, "");
        }
        if (shapeFlag & 16) {
          mountChildren(
            c2,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        }
      }
    }
  };
  const patchUnkeyedChildren = (c1, c2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    c1 = c1 || EMPTY_ARR;
    c2 = c2 || EMPTY_ARR;
    const oldLength = c1.length;
    const newLength = c2.length;
    const commonLength = Math.min(oldLength, newLength);
    let i;
    for (i = 0; i < commonLength; i++) {
      const nextChild = c2[i] = optimized ? cloneIfMounted(c2[i]) : normalizeVNode(c2[i]);
      patch(
        c1[i],
        nextChild,
        container,
        null,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized
      );
    }
    if (oldLength > newLength) {
      unmountChildren(
        c1,
        parentComponent,
        parentSuspense,
        true,
        false,
        commonLength
      );
    } else {
      mountChildren(
        c2,
        container,
        anchor,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized,
        commonLength
      );
    }
  };
  const patchKeyedChildren = (c1, c2, container, parentAnchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized) => {
    let i = 0;
    const l2 = c2.length;
    let e1 = c1.length - 1;
    let e2 = l2 - 1;
    while (i <= e1 && i <= e2) {
      const n1 = c1[i];
      const n2 = c2[i] = optimized ? cloneIfMounted(c2[i]) : normalizeVNode(c2[i]);
      if (isSameVNodeType(n1, n2)) {
        patch(
          n1,
          n2,
          container,
          null,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
      } else {
        break;
      }
      i++;
    }
    while (i <= e1 && i <= e2) {
      const n1 = c1[e1];
      const n2 = c2[e2] = optimized ? cloneIfMounted(c2[e2]) : normalizeVNode(c2[e2]);
      if (isSameVNodeType(n1, n2)) {
        patch(
          n1,
          n2,
          container,
          null,
          parentComponent,
          parentSuspense,
          namespace,
          slotScopeIds,
          optimized
        );
      } else {
        break;
      }
      e1--;
      e2--;
    }
    if (i > e1) {
      if (i <= e2) {
        const nextPos = e2 + 1;
        const anchor = nextPos < l2 ? c2[nextPos].el : parentAnchor;
        while (i <= e2) {
          patch(
            null,
            c2[i] = optimized ? cloneIfMounted(c2[i]) : normalizeVNode(c2[i]),
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
          i++;
        }
      }
    } else if (i > e2) {
      while (i <= e1) {
        unmount(c1[i], parentComponent, parentSuspense, true);
        i++;
      }
    } else {
      const s1 = i;
      const s2 = i;
      const keyToNewIndexMap = /* @__PURE__ */ new Map();
      for (i = s2; i <= e2; i++) {
        const nextChild = c2[i] = optimized ? cloneIfMounted(c2[i]) : normalizeVNode(c2[i]);
        if (nextChild.key != null) {
          keyToNewIndexMap.set(nextChild.key, i);
        }
      }
      let j;
      let patched = 0;
      const toBePatched = e2 - s2 + 1;
      let moved = false;
      let maxNewIndexSoFar = 0;
      const newIndexToOldIndexMap = new Array(toBePatched);
      for (i = 0; i < toBePatched; i++) newIndexToOldIndexMap[i] = 0;
      for (i = s1; i <= e1; i++) {
        const prevChild = c1[i];
        if (patched >= toBePatched) {
          unmount(prevChild, parentComponent, parentSuspense, true);
          continue;
        }
        let newIndex;
        if (prevChild.key != null) {
          newIndex = keyToNewIndexMap.get(prevChild.key);
        } else {
          for (j = s2; j <= e2; j++) {
            if (newIndexToOldIndexMap[j - s2] === 0 && isSameVNodeType(prevChild, c2[j])) {
              newIndex = j;
              break;
            }
          }
        }
        if (newIndex === void 0) {
          unmount(prevChild, parentComponent, parentSuspense, true);
        } else {
          newIndexToOldIndexMap[newIndex - s2] = i + 1;
          if (newIndex >= maxNewIndexSoFar) {
            maxNewIndexSoFar = newIndex;
          } else {
            moved = true;
          }
          patch(
            prevChild,
            c2[newIndex],
            container,
            null,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
          patched++;
        }
      }
      const increasingNewIndexSequence = moved ? getSequence(newIndexToOldIndexMap) : EMPTY_ARR;
      j = increasingNewIndexSequence.length - 1;
      for (i = toBePatched - 1; i >= 0; i--) {
        const nextIndex = s2 + i;
        const nextChild = c2[nextIndex];
        const anchorVNode = c2[nextIndex + 1];
        const anchor = nextIndex + 1 < l2 ? (
          // #13559, fallback to el placeholder for unresolved async component
          anchorVNode.el || anchorVNode.placeholder
        ) : parentAnchor;
        if (newIndexToOldIndexMap[i] === 0) {
          patch(
            null,
            nextChild,
            container,
            anchor,
            parentComponent,
            parentSuspense,
            namespace,
            slotScopeIds,
            optimized
          );
        } else if (moved) {
          if (j < 0 || i !== increasingNewIndexSequence[j]) {
            move(nextChild, container, anchor, 2);
          } else {
            j--;
          }
        }
      }
    }
  };
  const move = (vnode, container, anchor, moveType, parentSuspense = null) => {
    const { el: el2, type, transition, children, shapeFlag } = vnode;
    if (shapeFlag & 6) {
      move(vnode.component.subTree, container, anchor, moveType);
      return;
    }
    if (shapeFlag & 128) {
      vnode.suspense.move(container, anchor, moveType);
      return;
    }
    if (shapeFlag & 64) {
      type.move(vnode, container, anchor, internals);
      return;
    }
    if (type === Fragment) {
      hostInsert(el2, container, anchor);
      for (let i = 0; i < children.length; i++) {
        move(children[i], container, anchor, moveType);
      }
      hostInsert(vnode.anchor, container, anchor);
      return;
    }
    if (type === Static) {
      moveStaticNode(vnode, container, anchor);
      return;
    }
    const needTransition2 = moveType !== 2 && shapeFlag & 1 && transition;
    if (needTransition2) {
      if (moveType === 0) {
        transition.beforeEnter(el2);
        hostInsert(el2, container, anchor);
        queuePostRenderEffect(() => transition.enter(el2), parentSuspense);
      } else {
        const { leave, delayLeave, afterLeave } = transition;
        const remove22 = () => {
          if (vnode.ctx.isUnmounted) {
            hostRemove(el2);
          } else {
            hostInsert(el2, container, anchor);
          }
        };
        const performLeave = () => {
          if (el2._isLeaving) {
            el2[leaveCbKey](
              true
              /* cancelled */
            );
          }
          leave(el2, () => {
            remove22();
            afterLeave && afterLeave();
          });
        };
        if (delayLeave) {
          delayLeave(el2, remove22, performLeave);
        } else {
          performLeave();
        }
      }
    } else {
      hostInsert(el2, container, anchor);
    }
  };
  const unmount = (vnode, parentComponent, parentSuspense, doRemove = false, optimized = false) => {
    const {
      type,
      props,
      ref: ref3,
      children,
      dynamicChildren,
      shapeFlag,
      patchFlag,
      dirs,
      cacheIndex
    } = vnode;
    if (patchFlag === -2) {
      optimized = false;
    }
    if (ref3 != null) {
      pauseTracking();
      setRef(ref3, null, parentSuspense, vnode, true);
      resetTracking();
    }
    if (cacheIndex != null) {
      parentComponent.renderCache[cacheIndex] = void 0;
    }
    if (shapeFlag & 256) {
      parentComponent.ctx.deactivate(vnode);
      return;
    }
    const shouldInvokeDirs = shapeFlag & 1 && dirs;
    const shouldInvokeVnodeHook = !isAsyncWrapper(vnode);
    let vnodeHook;
    if (shouldInvokeVnodeHook && (vnodeHook = props && props.onVnodeBeforeUnmount)) {
      invokeVNodeHook(vnodeHook, parentComponent, vnode);
    }
    if (shapeFlag & 6) {
      unmountComponent(vnode.component, parentSuspense, doRemove);
    } else {
      if (shapeFlag & 128) {
        vnode.suspense.unmount(parentSuspense, doRemove);
        return;
      }
      if (shouldInvokeDirs) {
        invokeDirectiveHook(vnode, null, parentComponent, "beforeUnmount");
      }
      if (shapeFlag & 64) {
        vnode.type.remove(
          vnode,
          parentComponent,
          parentSuspense,
          internals,
          doRemove
        );
      } else if (dynamicChildren && // #5154
      // when v-once is used inside a block, setBlockTracking(-1) marks the
      // parent block with hasOnce: true
      // so that it doesn't take the fast path during unmount - otherwise
      // components nested in v-once are never unmounted.
      !dynamicChildren.hasOnce && // #1153: fast path should not be taken for non-stable (v-for) fragments
      (type !== Fragment || patchFlag > 0 && patchFlag & 64)) {
        unmountChildren(
          dynamicChildren,
          parentComponent,
          parentSuspense,
          false,
          true
        );
      } else if (type === Fragment && patchFlag & (128 | 256) || !optimized && shapeFlag & 16) {
        unmountChildren(children, parentComponent, parentSuspense);
      }
      if (doRemove) {
        remove2(vnode);
      }
    }
    if (shouldInvokeVnodeHook && (vnodeHook = props && props.onVnodeUnmounted) || shouldInvokeDirs) {
      queuePostRenderEffect(() => {
        vnodeHook && invokeVNodeHook(vnodeHook, parentComponent, vnode);
        shouldInvokeDirs && invokeDirectiveHook(vnode, null, parentComponent, "unmounted");
      }, parentSuspense);
    }
  };
  const remove2 = (vnode) => {
    const { type, el: el2, anchor, transition } = vnode;
    if (type === Fragment) {
      {
        removeFragment(el2, anchor);
      }
      return;
    }
    if (type === Static) {
      removeStaticNode(vnode);
      return;
    }
    const performRemove = () => {
      hostRemove(el2);
      if (transition && !transition.persisted && transition.afterLeave) {
        transition.afterLeave();
      }
    };
    if (vnode.shapeFlag & 1 && transition && !transition.persisted) {
      const { leave, delayLeave } = transition;
      const performLeave = () => leave(el2, performRemove);
      if (delayLeave) {
        delayLeave(vnode.el, performRemove, performLeave);
      } else {
        performLeave();
      }
    } else {
      performRemove();
    }
  };
  const removeFragment = (cur, end) => {
    let next;
    while (cur !== end) {
      next = hostNextSibling(cur);
      hostRemove(cur);
      cur = next;
    }
    hostRemove(end);
  };
  const unmountComponent = (instance, parentSuspense, doRemove) => {
    const { bum, scope, job, subTree, um, m, a } = instance;
    invalidateMount(m);
    invalidateMount(a);
    if (bum) {
      invokeArrayFns(bum);
    }
    scope.stop();
    if (job) {
      job.flags |= 8;
      unmount(subTree, instance, parentSuspense, doRemove);
    }
    if (um) {
      queuePostRenderEffect(um, parentSuspense);
    }
    queuePostRenderEffect(() => {
      instance.isUnmounted = true;
    }, parentSuspense);
    {
      devtoolsComponentRemoved(instance);
    }
  };
  const unmountChildren = (children, parentComponent, parentSuspense, doRemove = false, optimized = false, start = 0) => {
    for (let i = start; i < children.length; i++) {
      unmount(children[i], parentComponent, parentSuspense, doRemove, optimized);
    }
  };
  const getNextHostNode = (vnode) => {
    if (vnode.shapeFlag & 6) {
      return getNextHostNode(vnode.component.subTree);
    }
    if (vnode.shapeFlag & 128) {
      return vnode.suspense.next();
    }
    const el2 = hostNextSibling(vnode.anchor || vnode.el);
    const teleportEnd = el2 && el2[TeleportEndKey];
    return teleportEnd ? hostNextSibling(teleportEnd) : el2;
  };
  let isFlushing = false;
  const render2 = (vnode, container, namespace) => {
    if (vnode == null) {
      if (container._vnode) {
        unmount(container._vnode, null, null, true);
      }
    } else {
      patch(
        container._vnode || null,
        vnode,
        container,
        null,
        null,
        null,
        namespace
      );
    }
    container._vnode = vnode;
    if (!isFlushing) {
      isFlushing = true;
      flushPreFlushCbs();
      flushPostFlushCbs();
      isFlushing = false;
    }
  };
  const internals = {
    p: patch,
    um: unmount,
    m: move,
    r: remove2,
    mt: mountComponent,
    mc: mountChildren,
    pc: patchChildren,
    pbc: patchBlockChildren,
    n: getNextHostNode,
    o: options
  };
  let hydrate2;
  let hydrateNode;
  if (createHydrationFns) {
    [hydrate2, hydrateNode] = createHydrationFns(
      internals
    );
  }
  return {
    render: render2,
    hydrate: hydrate2,
    createApp: createAppAPI(render2, hydrate2)
  };
}
function resolveChildrenNamespace({ type, props }, currentNamespace) {
  return currentNamespace === "svg" && type === "foreignObject" || currentNamespace === "mathml" && type === "annotation-xml" && props && props.encoding && props.encoding.includes("html") ? void 0 : currentNamespace;
}
function toggleRecurse({ effect: effect2, job }, allowed) {
  if (allowed) {
    effect2.flags |= 32;
    job.flags |= 4;
  } else {
    effect2.flags &= -33;
    job.flags &= -5;
  }
}
function needTransition(parentSuspense, transition) {
  return (!parentSuspense || parentSuspense && !parentSuspense.pendingBranch) && transition && !transition.persisted;
}
function traverseStaticChildren(n1, n2, shallow = false) {
  const ch1 = n1.children;
  const ch2 = n2.children;
  if (isArray(ch1) && isArray(ch2)) {
    for (let i = 0; i < ch1.length; i++) {
      const c1 = ch1[i];
      let c2 = ch2[i];
      if (c2.shapeFlag & 1 && !c2.dynamicChildren) {
        if (c2.patchFlag <= 0 || c2.patchFlag === 32) {
          c2 = ch2[i] = cloneIfMounted(ch2[i]);
          c2.el = c1.el;
        }
        if (!shallow && c2.patchFlag !== -2)
          traverseStaticChildren(c1, c2);
      }
      if (c2.type === Text && // avoid cached text nodes retaining detached dom nodes
      c2.patchFlag !== -1) {
        c2.el = c1.el;
      }
      if (c2.type === Comment && !c2.el) {
        c2.el = c1.el;
      }
    }
  }
}
function getSequence(arr) {
  const p2 = arr.slice();
  const result = [0];
  let i, j, u, v, c;
  const len = arr.length;
  for (i = 0; i < len; i++) {
    const arrI = arr[i];
    if (arrI !== 0) {
      j = result[result.length - 1];
      if (arr[j] < arrI) {
        p2[i] = j;
        result.push(i);
        continue;
      }
      u = 0;
      v = result.length - 1;
      while (u < v) {
        c = u + v >> 1;
        if (arr[result[c]] < arrI) {
          u = c + 1;
        } else {
          v = c;
        }
      }
      if (arrI < arr[result[u]]) {
        if (u > 0) {
          p2[i] = result[u - 1];
        }
        result[u] = i;
      }
    }
  }
  u = result.length;
  v = result[u - 1];
  while (u-- > 0) {
    result[u] = v;
    v = p2[v];
  }
  return result;
}
function locateNonHydratedAsyncRoot(instance) {
  const subComponent = instance.subTree.component;
  if (subComponent) {
    if (subComponent.asyncDep && !subComponent.asyncResolved) {
      return subComponent;
    } else {
      return locateNonHydratedAsyncRoot(subComponent);
    }
  }
}
function invalidateMount(hooks) {
  if (hooks) {
    for (let i = 0; i < hooks.length; i++)
      hooks[i].flags |= 8;
  }
}
const isSuspense = (type) => type.__isSuspense;
let suspenseId = 0;
const SuspenseImpl = {
  name: "Suspense",
  // In order to make Suspense tree-shakable, we need to avoid importing it
  // directly in the renderer. The renderer checks for the __isSuspense flag
  // on a vnode's type and calls the `process` method, passing in renderer
  // internals.
  __isSuspense: true,
  process(n1, n2, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized, rendererInternals) {
    if (n1 == null) {
      mountSuspense(
        n2,
        container,
        anchor,
        parentComponent,
        parentSuspense,
        namespace,
        slotScopeIds,
        optimized,
        rendererInternals
      );
    } else {
      if (parentSuspense && parentSuspense.deps > 0 && !n1.suspense.isInFallback) {
        n2.suspense = n1.suspense;
        n2.suspense.vnode = n2;
        n2.el = n1.el;
        return;
      }
      patchSuspense(
        n1,
        n2,
        container,
        anchor,
        parentComponent,
        namespace,
        slotScopeIds,
        optimized,
        rendererInternals
      );
    }
  },
  hydrate: hydrateSuspense,
  normalize: normalizeSuspenseChildren
};
const Suspense = SuspenseImpl;
function triggerEvent(vnode, name2) {
  const eventListener = vnode.props && vnode.props[name2];
  if (isFunction(eventListener)) {
    eventListener();
  }
}
function mountSuspense(vnode, container, anchor, parentComponent, parentSuspense, namespace, slotScopeIds, optimized, rendererInternals) {
  const {
    p: patch,
    o: { createElement }
  } = rendererInternals;
  const hiddenContainer = createElement("div");
  const suspense = vnode.suspense = createSuspenseBoundary(
    vnode,
    parentSuspense,
    parentComponent,
    container,
    hiddenContainer,
    anchor,
    namespace,
    slotScopeIds,
    optimized,
    rendererInternals
  );
  patch(
    null,
    suspense.pendingBranch = vnode.ssContent,
    hiddenContainer,
    null,
    parentComponent,
    suspense,
    namespace,
    slotScopeIds
  );
  if (suspense.deps > 0) {
    triggerEvent(vnode, "onPending");
    triggerEvent(vnode, "onFallback");
    patch(
      null,
      vnode.ssFallback,
      container,
      anchor,
      parentComponent,
      null,
      // fallback tree will not have suspense context
      namespace,
      slotScopeIds
    );
    setActiveBranch(suspense, vnode.ssFallback);
  } else {
    suspense.resolve(false, true);
  }
}
function patchSuspense(n1, n2, container, anchor, parentComponent, namespace, slotScopeIds, optimized, { p: patch, um: unmount, o: { createElement } }) {
  const suspense = n2.suspense = n1.suspense;
  suspense.vnode = n2;
  n2.el = n1.el;
  const newBranch = n2.ssContent;
  const newFallback = n2.ssFallback;
  const { activeBranch, pendingBranch, isInFallback, isHydrating } = suspense;
  if (pendingBranch) {
    suspense.pendingBranch = newBranch;
    if (isSameVNodeType(pendingBranch, newBranch)) {
      patch(
        pendingBranch,
        newBranch,
        suspense.hiddenContainer,
        null,
        parentComponent,
        suspense,
        namespace,
        slotScopeIds,
        optimized
      );
      if (suspense.deps <= 0) {
        suspense.resolve();
      } else if (isInFallback) {
        if (!isHydrating) {
          patch(
            activeBranch,
            newFallback,
            container,
            anchor,
            parentComponent,
            null,
            // fallback tree will not have suspense context
            namespace,
            slotScopeIds,
            optimized
          );
          setActiveBranch(suspense, newFallback);
        }
      }
    } else {
      suspense.pendingId = suspenseId++;
      if (isHydrating) {
        suspense.isHydrating = false;
        suspense.activeBranch = pendingBranch;
      } else {
        unmount(pendingBranch, parentComponent, suspense);
      }
      suspense.deps = 0;
      suspense.effects.length = 0;
      suspense.hiddenContainer = createElement("div");
      if (isInFallback) {
        patch(
          null,
          newBranch,
          suspense.hiddenContainer,
          null,
          parentComponent,
          suspense,
          namespace,
          slotScopeIds,
          optimized
        );
        if (suspense.deps <= 0) {
          suspense.resolve();
        } else {
          patch(
            activeBranch,
            newFallback,
            container,
            anchor,
            parentComponent,
            null,
            // fallback tree will not have suspense context
            namespace,
            slotScopeIds,
            optimized
          );
          setActiveBranch(suspense, newFallback);
        }
      } else if (activeBranch && isSameVNodeType(activeBranch, newBranch)) {
        patch(
          activeBranch,
          newBranch,
          container,
          anchor,
          parentComponent,
          suspense,
          namespace,
          slotScopeIds,
          optimized
        );
        suspense.resolve(true);
      } else {
        patch(
          null,
          newBranch,
          suspense.hiddenContainer,
          null,
          parentComponent,
          suspense,
          namespace,
          slotScopeIds,
          optimized
        );
        if (suspense.deps <= 0) {
          suspense.resolve();
        }
      }
    }
  } else {
    if (activeBranch && isSameVNodeType(activeBranch, newBranch)) {
      patch(
        activeBranch,
        newBranch,
        container,
        anchor,
        parentComponent,
        suspense,
        namespace,
        slotScopeIds,
        optimized
      );
      setActiveBranch(suspense, newBranch);
    } else {
      triggerEvent(n2, "onPending");
      suspense.pendingBranch = newBranch;
      if (newBranch.shapeFlag & 512) {
        suspense.pendingId = newBranch.component.suspenseId;
      } else {
        suspense.pendingId = suspenseId++;
      }
      patch(
        null,
        newBranch,
        suspense.hiddenContainer,
        null,
        parentComponent,
        suspense,
        namespace,
        slotScopeIds,
        optimized
      );
      if (suspense.deps <= 0) {
        suspense.resolve();
      } else {
        const { timeout, pendingId } = suspense;
        if (timeout > 0) {
          setTimeout(() => {
            if (suspense.pendingId === pendingId) {
              suspense.fallback(newFallback);
            }
          }, timeout);
        } else if (timeout === 0) {
          suspense.fallback(newFallback);
        }
      }
    }
  }
}
function createSuspenseBoundary(vnode, parentSuspense, parentComponent, container, hiddenContainer, anchor, namespace, slotScopeIds, optimized, rendererInternals, isHydrating = false) {
  const {
    p: patch,
    m: move,
    um: unmount,
    n: next,
    o: { parentNode, remove: remove2 }
  } = rendererInternals;
  let parentSuspenseId;
  const isSuspensible = isVNodeSuspensible(vnode);
  if (isSuspensible) {
    if (parentSuspense && parentSuspense.pendingBranch) {
      parentSuspenseId = parentSuspense.pendingId;
      parentSuspense.deps++;
    }
  }
  const timeout = vnode.props ? toNumber(vnode.props.timeout) : void 0;
  const initialAnchor = anchor;
  const suspense = {
    vnode,
    parent: parentSuspense,
    parentComponent,
    namespace,
    container,
    hiddenContainer,
    deps: 0,
    pendingId: suspenseId++,
    timeout: typeof timeout === "number" ? timeout : -1,
    activeBranch: null,
    pendingBranch: null,
    isInFallback: !isHydrating,
    isHydrating,
    isUnmounted: false,
    effects: [],
    resolve(resume = false, sync = false) {
      const {
        vnode: vnode2,
        activeBranch,
        pendingBranch,
        pendingId,
        effects,
        parentComponent: parentComponent2,
        container: container2,
        isInFallback
      } = suspense;
      let delayEnter = false;
      if (suspense.isHydrating) {
        suspense.isHydrating = false;
      } else if (!resume) {
        delayEnter = activeBranch && pendingBranch.transition && pendingBranch.transition.mode === "out-in";
        if (delayEnter) {
          activeBranch.transition.afterLeave = () => {
            if (pendingId === suspense.pendingId) {
              move(
                pendingBranch,
                container2,
                anchor === initialAnchor ? next(activeBranch) : anchor,
                0
              );
              queuePostFlushCb(effects);
              if (isInFallback && vnode2.ssFallback) {
                vnode2.ssFallback.el = null;
              }
            }
          };
        }
        if (activeBranch) {
          if (parentNode(activeBranch.el) === container2) {
            anchor = next(activeBranch);
          }
          unmount(activeBranch, parentComponent2, suspense, true);
          if (!delayEnter && isInFallback && vnode2.ssFallback) {
            queuePostRenderEffect(() => vnode2.ssFallback.el = null, suspense);
          }
        }
        if (!delayEnter) {
          move(pendingBranch, container2, anchor, 0);
        }
      }
      setActiveBranch(suspense, pendingBranch);
      suspense.pendingBranch = null;
      suspense.isInFallback = false;
      let parent = suspense.parent;
      let hasUnresolvedAncestor = false;
      while (parent) {
        if (parent.pendingBranch) {
          parent.effects.push(...effects);
          hasUnresolvedAncestor = true;
          break;
        }
        parent = parent.parent;
      }
      if (!hasUnresolvedAncestor && !delayEnter) {
        queuePostFlushCb(effects);
      }
      suspense.effects = [];
      if (isSuspensible) {
        if (parentSuspense && parentSuspense.pendingBranch && parentSuspenseId === parentSuspense.pendingId) {
          parentSuspense.deps--;
          if (parentSuspense.deps === 0 && !sync) {
            parentSuspense.resolve();
          }
        }
      }
      triggerEvent(vnode2, "onResolve");
    },
    fallback(fallbackVNode) {
      if (!suspense.pendingBranch) {
        return;
      }
      const { vnode: vnode2, activeBranch, parentComponent: parentComponent2, container: container2, namespace: namespace2 } = suspense;
      triggerEvent(vnode2, "onFallback");
      const anchor2 = next(activeBranch);
      const mountFallback = () => {
        if (!suspense.isInFallback) {
          return;
        }
        patch(
          null,
          fallbackVNode,
          container2,
          anchor2,
          parentComponent2,
          null,
          // fallback tree will not have suspense context
          namespace2,
          slotScopeIds,
          optimized
        );
        setActiveBranch(suspense, fallbackVNode);
      };
      const delayEnter = fallbackVNode.transition && fallbackVNode.transition.mode === "out-in";
      if (delayEnter) {
        activeBranch.transition.afterLeave = mountFallback;
      }
      suspense.isInFallback = true;
      unmount(
        activeBranch,
        parentComponent2,
        null,
        // no suspense so unmount hooks fire now
        true
        // shouldRemove
      );
      if (!delayEnter) {
        mountFallback();
      }
    },
    move(container2, anchor2, type) {
      suspense.activeBranch && move(suspense.activeBranch, container2, anchor2, type);
      suspense.container = container2;
    },
    next() {
      return suspense.activeBranch && next(suspense.activeBranch);
    },
    registerDep(instance, setupRenderEffect, optimized2) {
      const isInPendingSuspense = !!suspense.pendingBranch;
      if (isInPendingSuspense) {
        suspense.deps++;
      }
      const hydratedEl = instance.vnode.el;
      instance.asyncDep.catch((err) => {
        handleError(err, instance, 0);
      }).then((asyncSetupResult) => {
        if (instance.isUnmounted || suspense.isUnmounted || suspense.pendingId !== instance.suspenseId) {
          return;
        }
        instance.asyncResolved = true;
        const { vnode: vnode2 } = instance;
        handleSetupResult(instance, asyncSetupResult, false);
        if (hydratedEl) {
          vnode2.el = hydratedEl;
        }
        const placeholder = !hydratedEl && instance.subTree.el;
        setupRenderEffect(
          instance,
          vnode2,
          // component may have been moved before resolve.
          // if this is not a hydration, instance.subTree will be the comment
          // placeholder.
          parentNode(hydratedEl || instance.subTree.el),
          // anchor will not be used if this is hydration, so only need to
          // consider the comment placeholder case.
          hydratedEl ? null : next(instance.subTree),
          suspense,
          namespace,
          optimized2
        );
        if (placeholder) {
          vnode2.placeholder = null;
          remove2(placeholder);
        }
        updateHOCHostEl(instance, vnode2.el);
        if (isInPendingSuspense && --suspense.deps === 0) {
          suspense.resolve();
        }
      });
    },
    unmount(parentSuspense2, doRemove) {
      suspense.isUnmounted = true;
      if (suspense.activeBranch) {
        unmount(
          suspense.activeBranch,
          parentComponent,
          parentSuspense2,
          doRemove
        );
      }
      if (suspense.pendingBranch) {
        unmount(
          suspense.pendingBranch,
          parentComponent,
          parentSuspense2,
          doRemove
        );
      }
    }
  };
  return suspense;
}
function hydrateSuspense(node, vnode, parentComponent, parentSuspense, namespace, slotScopeIds, optimized, rendererInternals, hydrateNode) {
  const suspense = vnode.suspense = createSuspenseBoundary(
    vnode,
    parentSuspense,
    parentComponent,
    node.parentNode,
    // eslint-disable-next-line no-restricted-globals
    document.createElement("div"),
    null,
    namespace,
    slotScopeIds,
    optimized,
    rendererInternals,
    true
  );
  const result = hydrateNode(
    node,
    suspense.pendingBranch = vnode.ssContent,
    parentComponent,
    suspense,
    slotScopeIds,
    optimized
  );
  if (suspense.deps === 0) {
    suspense.resolve(false, true);
  }
  return result;
}
function normalizeSuspenseChildren(vnode) {
  const { shapeFlag, children } = vnode;
  const isSlotChildren = shapeFlag & 32;
  vnode.ssContent = normalizeSuspenseSlot(
    isSlotChildren ? children.default : children
  );
  vnode.ssFallback = isSlotChildren ? normalizeSuspenseSlot(children.fallback) : createVNode(Comment);
}
function normalizeSuspenseSlot(s) {
  let block;
  if (isFunction(s)) {
    const trackBlock = isBlockTreeEnabled && s._c;
    if (trackBlock) {
      s._d = false;
      openBlock();
    }
    s = s();
    if (trackBlock) {
      s._d = true;
      block = currentBlock;
      closeBlock();
    }
  }
  if (isArray(s)) {
    const singleChild = filterSingleRoot(s);
    s = singleChild;
  }
  s = normalizeVNode(s);
  if (block && !s.dynamicChildren) {
    s.dynamicChildren = block.filter((c) => c !== s);
  }
  return s;
}
function queueEffectWithSuspense(fn, suspense) {
  if (suspense && suspense.pendingBranch) {
    if (isArray(fn)) {
      suspense.effects.push(...fn);
    } else {
      suspense.effects.push(fn);
    }
  } else {
    queuePostFlushCb(fn);
  }
}
function setActiveBranch(suspense, branch) {
  suspense.activeBranch = branch;
  const { vnode, parentComponent } = suspense;
  let el2 = branch.el;
  while (!el2 && branch.component) {
    branch = branch.component.subTree;
    el2 = branch.el;
  }
  vnode.el = el2;
  if (parentComponent && parentComponent.subTree === vnode) {
    parentComponent.vnode.el = el2;
    updateHOCHostEl(parentComponent, el2);
  }
}
function isVNodeSuspensible(vnode) {
  const suspensible = vnode.props && vnode.props.suspensible;
  return suspensible != null && suspensible !== false;
}
const Fragment = Symbol.for("v-fgt");
const Text = Symbol.for("v-txt");
const Comment = Symbol.for("v-cmt");
const Static = Symbol.for("v-stc");
const blockStack = [];
let currentBlock = null;
function openBlock(disableTracking = false) {
  blockStack.push(currentBlock = disableTracking ? null : []);
}
function closeBlock() {
  blockStack.pop();
  currentBlock = blockStack[blockStack.length - 1] || null;
}
let isBlockTreeEnabled = 1;
function setBlockTracking(value, inVOnce = false) {
  isBlockTreeEnabled += value;
  if (value < 0 && currentBlock && inVOnce) {
    currentBlock.hasOnce = true;
  }
}
function setupBlock(vnode) {
  vnode.dynamicChildren = isBlockTreeEnabled > 0 ? currentBlock || EMPTY_ARR : null;
  closeBlock();
  if (isBlockTreeEnabled > 0 && currentBlock) {
    currentBlock.push(vnode);
  }
  return vnode;
}
function createElementBlock(type, props, children, patchFlag, dynamicProps, shapeFlag) {
  return setupBlock(
    createBaseVNode(
      type,
      props,
      children,
      patchFlag,
      dynamicProps,
      shapeFlag,
      true
    )
  );
}
function createBlock(type, props, children, patchFlag, dynamicProps) {
  return setupBlock(
    createVNode(
      type,
      props,
      children,
      patchFlag,
      dynamicProps,
      true
    )
  );
}
function isVNode(value) {
  return value ? value.__v_isVNode === true : false;
}
function isSameVNodeType(n1, n2) {
  return n1.type === n2.type && n1.key === n2.key;
}
function transformVNodeArgs(transformer) {
}
const normalizeKey = ({ key }) => key != null ? key : null;
const normalizeRef = ({
  ref: ref3,
  ref_key,
  ref_for
}) => {
  if (typeof ref3 === "number") {
    ref3 = "" + ref3;
  }
  return ref3 != null ? isString(ref3) || isRef(ref3) || isFunction(ref3) ? { i: currentRenderingInstance, r: ref3, k: ref_key, f: !!ref_for } : ref3 : null;
};
function createBaseVNode(type, props = null, children = null, patchFlag = 0, dynamicProps = null, shapeFlag = type === Fragment ? 0 : 1, isBlockNode = false, needFullChildrenNormalization = false) {
  const vnode = {
    __v_isVNode: true,
    __v_skip: true,
    type,
    props,
    key: props && normalizeKey(props),
    ref: props && normalizeRef(props),
    scopeId: currentScopeId,
    slotScopeIds: null,
    children,
    component: null,
    suspense: null,
    ssContent: null,
    ssFallback: null,
    dirs: null,
    transition: null,
    el: null,
    anchor: null,
    target: null,
    targetStart: null,
    targetAnchor: null,
    staticCount: 0,
    shapeFlag,
    patchFlag,
    dynamicProps,
    dynamicChildren: null,
    appContext: null,
    ctx: currentRenderingInstance
  };
  if (needFullChildrenNormalization) {
    normalizeChildren(vnode, children);
    if (shapeFlag & 128) {
      type.normalize(vnode);
    }
  } else if (children) {
    vnode.shapeFlag |= isString(children) ? 8 : 16;
  }
  if (isBlockTreeEnabled > 0 && // avoid a block node from tracking itself
  !isBlockNode && // has current parent block
  currentBlock && // presence of a patch flag indicates this node needs patching on updates.
  // component nodes also should always be patched, because even if the
  // component doesn't need to update, it needs to persist the instance on to
  // the next vnode so that it can be properly unmounted later.
  (vnode.patchFlag > 0 || shapeFlag & 6) && // the EVENTS flag is only for hydration and if it is the only flag, the
  // vnode should not be considered dynamic due to handler caching.
  vnode.patchFlag !== 32) {
    currentBlock.push(vnode);
  }
  return vnode;
}
const createVNode = _createVNode;
function _createVNode(type, props = null, children = null, patchFlag = 0, dynamicProps = null, isBlockNode = false) {
  if (!type || type === NULL_DYNAMIC_COMPONENT) {
    type = Comment;
  }
  if (isVNode(type)) {
    const cloned = cloneVNode(
      type,
      props,
      true
      /* mergeRef: true */
    );
    if (children) {
      normalizeChildren(cloned, children);
    }
    if (isBlockTreeEnabled > 0 && !isBlockNode && currentBlock) {
      if (cloned.shapeFlag & 6) {
        currentBlock[currentBlock.indexOf(type)] = cloned;
      } else {
        currentBlock.push(cloned);
      }
    }
    cloned.patchFlag = -2;
    return cloned;
  }
  if (isClassComponent(type)) {
    type = type.__vccOpts;
  }
  if (props) {
    props = guardReactiveProps(props);
    let { class: klass, style } = props;
    if (klass && !isString(klass)) {
      props.class = normalizeClass(klass);
    }
    if (isObject(style)) {
      if (isProxy(style) && !isArray(style)) {
        style = extend({}, style);
      }
      props.style = normalizeStyle(style);
    }
  }
  const shapeFlag = isString(type) ? 1 : isSuspense(type) ? 128 : isTeleport(type) ? 64 : isObject(type) ? 4 : isFunction(type) ? 2 : 0;
  return createBaseVNode(
    type,
    props,
    children,
    patchFlag,
    dynamicProps,
    shapeFlag,
    isBlockNode,
    true
  );
}
function guardReactiveProps(props) {
  if (!props) return null;
  return isProxy(props) || isInternalObject(props) ? extend({}, props) : props;
}
function cloneVNode(vnode, extraProps, mergeRef = false, cloneTransition = false) {
  const { props, ref: ref3, patchFlag, children, transition } = vnode;
  const mergedProps = extraProps ? mergeProps(props || {}, extraProps) : props;
  const cloned = {
    __v_isVNode: true,
    __v_skip: true,
    type: vnode.type,
    props: mergedProps,
    key: mergedProps && normalizeKey(mergedProps),
    ref: extraProps && extraProps.ref ? (
      // #2078 in the case of <component :is="vnode" ref="extra"/>
      // if the vnode itself already has a ref, cloneVNode will need to merge
      // the refs so the single vnode can be set on multiple refs
      mergeRef && ref3 ? isArray(ref3) ? ref3.concat(normalizeRef(extraProps)) : [ref3, normalizeRef(extraProps)] : normalizeRef(extraProps)
    ) : ref3,
    scopeId: vnode.scopeId,
    slotScopeIds: vnode.slotScopeIds,
    children,
    target: vnode.target,
    targetStart: vnode.targetStart,
    targetAnchor: vnode.targetAnchor,
    staticCount: vnode.staticCount,
    shapeFlag: vnode.shapeFlag,
    // if the vnode is cloned with extra props, we can no longer assume its
    // existing patch flag to be reliable and need to add the FULL_PROPS flag.
    // note: preserve flag for fragments since they use the flag for children
    // fast paths only.
    patchFlag: extraProps && vnode.type !== Fragment ? patchFlag === -1 ? 16 : patchFlag | 16 : patchFlag,
    dynamicProps: vnode.dynamicProps,
    dynamicChildren: vnode.dynamicChildren,
    appContext: vnode.appContext,
    dirs: vnode.dirs,
    transition,
    // These should technically only be non-null on mounted VNodes. However,
    // they *should* be copied for kept-alive vnodes. So we just always copy
    // them since them being non-null during a mount doesn't affect the logic as
    // they will simply be overwritten.
    component: vnode.component,
    suspense: vnode.suspense,
    ssContent: vnode.ssContent && cloneVNode(vnode.ssContent),
    ssFallback: vnode.ssFallback && cloneVNode(vnode.ssFallback),
    placeholder: vnode.placeholder,
    el: vnode.el,
    anchor: vnode.anchor,
    ctx: vnode.ctx,
    ce: vnode.ce
  };
  if (transition && cloneTransition) {
    setTransitionHooks(
      cloned,
      transition.clone(cloned)
    );
  }
  return cloned;
}
function createTextVNode(text = " ", flag = 0) {
  return createVNode(Text, null, text, flag);
}
function createStaticVNode(content, numberOfNodes) {
  const vnode = createVNode(Static, null, content);
  vnode.staticCount = numberOfNodes;
  return vnode;
}
function createCommentVNode(text = "", asBlock = false) {
  return asBlock ? (openBlock(), createBlock(Comment, null, text)) : createVNode(Comment, null, text);
}
function normalizeVNode(child) {
  if (child == null || typeof child === "boolean") {
    return createVNode(Comment);
  } else if (isArray(child)) {
    return createVNode(
      Fragment,
      null,
      // #3666, avoid reference pollution when reusing vnode
      child.slice()
    );
  } else if (isVNode(child)) {
    return cloneIfMounted(child);
  } else {
    return createVNode(Text, null, String(child));
  }
}
function cloneIfMounted(child) {
  return child.el === null && child.patchFlag !== -1 || child.memo ? child : cloneVNode(child);
}
function normalizeChildren(vnode, children) {
  let type = 0;
  const { shapeFlag } = vnode;
  if (children == null) {
    children = null;
  } else if (isArray(children)) {
    type = 16;
  } else if (typeof children === "object") {
    if (shapeFlag & (1 | 64)) {
      const slot = children.default;
      if (slot) {
        slot._c && (slot._d = false);
        normalizeChildren(vnode, slot());
        slot._c && (slot._d = true);
      }
      return;
    } else {
      type = 32;
      const slotFlag = children._;
      if (!slotFlag && !isInternalObject(children)) {
        children._ctx = currentRenderingInstance;
      } else if (slotFlag === 3 && currentRenderingInstance) {
        if (currentRenderingInstance.slots._ === 1) {
          children._ = 1;
        } else {
          children._ = 2;
          vnode.patchFlag |= 1024;
        }
      }
    }
  } else if (isFunction(children)) {
    children = { default: children, _ctx: currentRenderingInstance };
    type = 32;
  } else {
    children = String(children);
    if (shapeFlag & 64) {
      type = 16;
      children = [createTextVNode(children)];
    } else {
      type = 8;
    }
  }
  vnode.children = children;
  vnode.shapeFlag |= type;
}
function mergeProps(...args) {
  const ret = {};
  for (let i = 0; i < args.length; i++) {
    const toMerge = args[i];
    for (const key in toMerge) {
      if (key === "class") {
        if (ret.class !== toMerge.class) {
          ret.class = normalizeClass([ret.class, toMerge.class]);
        }
      } else if (key === "style") {
        ret.style = normalizeStyle([ret.style, toMerge.style]);
      } else if (isOn(key)) {
        const existing = ret[key];
        const incoming = toMerge[key];
        if (incoming && existing !== incoming && !(isArray(existing) && existing.includes(incoming))) {
          ret[key] = existing ? [].concat(existing, incoming) : incoming;
        }
      } else if (key !== "") {
        ret[key] = toMerge[key];
      }
    }
  }
  return ret;
}
function invokeVNodeHook(hook, instance, vnode, prevVNode = null) {
  callWithAsyncErrorHandling(hook, instance, 7, [
    vnode,
    prevVNode
  ]);
}
const emptyAppContext = createAppContext();
let uid = 0;
function createComponentInstance(vnode, parent, suspense) {
  const type = vnode.type;
  const appContext = (parent ? parent.appContext : vnode.appContext) || emptyAppContext;
  const instance = {
    uid: uid++,
    vnode,
    type,
    parent,
    appContext,
    root: null,
    // to be immediately set
    next: null,
    subTree: null,
    // will be set synchronously right after creation
    effect: null,
    update: null,
    // will be set synchronously right after creation
    job: null,
    scope: new EffectScope(
      true
      /* detached */
    ),
    render: null,
    proxy: null,
    exposed: null,
    exposeProxy: null,
    withProxy: null,
    provides: parent ? parent.provides : Object.create(appContext.provides),
    ids: parent ? parent.ids : ["", 0, 0],
    accessCache: null,
    renderCache: [],
    // local resolved assets
    components: null,
    directives: null,
    // resolved props and emits options
    propsOptions: normalizePropsOptions(type, appContext),
    emitsOptions: normalizeEmitsOptions(type, appContext),
    // emit
    emit: null,
    // to be set immediately
    emitted: null,
    // props default value
    propsDefaults: EMPTY_OBJ,
    // inheritAttrs
    inheritAttrs: type.inheritAttrs,
    // state
    ctx: EMPTY_OBJ,
    data: EMPTY_OBJ,
    props: EMPTY_OBJ,
    attrs: EMPTY_OBJ,
    slots: EMPTY_OBJ,
    refs: EMPTY_OBJ,
    setupState: EMPTY_OBJ,
    setupContext: null,
    // suspense related
    suspense,
    suspenseId: suspense ? suspense.pendingId : 0,
    asyncDep: null,
    asyncResolved: false,
    // lifecycle hooks
    // not using enums here because it results in computed properties
    isMounted: false,
    isUnmounted: false,
    isDeactivated: false,
    bc: null,
    c: null,
    bm: null,
    m: null,
    bu: null,
    u: null,
    um: null,
    bum: null,
    da: null,
    a: null,
    rtg: null,
    rtc: null,
    ec: null,
    sp: null
  };
  {
    instance.ctx = { _: instance };
  }
  instance.root = parent ? parent.root : instance;
  instance.emit = emit.bind(null, instance);
  if (vnode.ce) {
    vnode.ce(instance);
  }
  return instance;
}
let currentInstance = null;
const getCurrentInstance = () => currentInstance || currentRenderingInstance;
let internalSetCurrentInstance;
let setInSSRSetupState;
{
  const g = getGlobalThis();
  const registerGlobalSetter = (key, setter) => {
    let setters;
    if (!(setters = g[key])) setters = g[key] = [];
    setters.push(setter);
    return (v) => {
      if (setters.length > 1) setters.forEach((set) => set(v));
      else setters[0](v);
    };
  };
  internalSetCurrentInstance = registerGlobalSetter(
    `__VUE_INSTANCE_SETTERS__`,
    (v) => currentInstance = v
  );
  setInSSRSetupState = registerGlobalSetter(
    `__VUE_SSR_SETTERS__`,
    (v) => isInSSRComponentSetup = v
  );
}
const setCurrentInstance = (instance) => {
  const prev = currentInstance;
  internalSetCurrentInstance(instance);
  instance.scope.on();
  return () => {
    instance.scope.off();
    internalSetCurrentInstance(prev);
  };
};
const unsetCurrentInstance = () => {
  currentInstance && currentInstance.scope.off();
  internalSetCurrentInstance(null);
};
function isStatefulComponent(instance) {
  return instance.vnode.shapeFlag & 4;
}
let isInSSRComponentSetup = false;
function setupComponent(instance, isSSR = false, optimized = false) {
  isSSR && setInSSRSetupState(isSSR);
  const { props, children } = instance.vnode;
  const isStateful = isStatefulComponent(instance);
  initProps(instance, props, isStateful, isSSR);
  initSlots(instance, children, optimized || isSSR);
  const setupResult = isStateful ? setupStatefulComponent(instance, isSSR) : void 0;
  isSSR && setInSSRSetupState(false);
  return setupResult;
}
function setupStatefulComponent(instance, isSSR) {
  const Component = instance.type;
  instance.accessCache = /* @__PURE__ */ Object.create(null);
  instance.proxy = new Proxy(instance.ctx, PublicInstanceProxyHandlers);
  const { setup } = Component;
  if (setup) {
    pauseTracking();
    const setupContext = instance.setupContext = setup.length > 1 ? createSetupContext(instance) : null;
    const reset2 = setCurrentInstance(instance);
    const setupResult = callWithErrorHandling(
      setup,
      instance,
      0,
      [
        instance.props,
        setupContext
      ]
    );
    const isAsyncSetup = isPromise(setupResult);
    resetTracking();
    reset2();
    if ((isAsyncSetup || instance.sp) && !isAsyncWrapper(instance)) {
      markAsyncBoundary(instance);
    }
    if (isAsyncSetup) {
      setupResult.then(unsetCurrentInstance, unsetCurrentInstance);
      if (isSSR) {
        return setupResult.then((resolvedResult) => {
          handleSetupResult(instance, resolvedResult, isSSR);
        }).catch((e) => {
          handleError(e, instance, 0);
        });
      } else {
        instance.asyncDep = setupResult;
      }
    } else {
      handleSetupResult(instance, setupResult, isSSR);
    }
  } else {
    finishComponentSetup(instance, isSSR);
  }
}
function handleSetupResult(instance, setupResult, isSSR) {
  if (isFunction(setupResult)) {
    if (instance.type.__ssrInlineRender) {
      instance.ssrRender = setupResult;
    } else {
      instance.render = setupResult;
    }
  } else if (isObject(setupResult)) {
    {
      instance.devtoolsRawSetupState = setupResult;
    }
    instance.setupState = proxyRefs(setupResult);
  } else ;
  finishComponentSetup(instance, isSSR);
}
let compile$1;
let installWithProxy;
function registerRuntimeCompiler(_compile) {
  compile$1 = _compile;
  installWithProxy = (i) => {
    if (i.render._rc) {
      i.withProxy = new Proxy(i.ctx, RuntimeCompiledPublicInstanceProxyHandlers);
    }
  };
}
const isRuntimeOnly = () => !compile$1;
function finishComponentSetup(instance, isSSR, skipOptions) {
  const Component = instance.type;
  if (!instance.render) {
    if (!isSSR && compile$1 && !Component.render) {
      const template = Component.template || resolveMergedOptions(instance).template;
      if (template) {
        const { isCustomElement, compilerOptions } = instance.appContext.config;
        const { delimiters, compilerOptions: componentCompilerOptions } = Component;
        const finalCompilerOptions = extend(
          extend(
            {
              isCustomElement,
              delimiters
            },
            compilerOptions
          ),
          componentCompilerOptions
        );
        Component.render = compile$1(template, finalCompilerOptions);
      }
    }
    instance.render = Component.render || NOOP;
    if (installWithProxy) {
      installWithProxy(instance);
    }
  }
  {
    const reset2 = setCurrentInstance(instance);
    pauseTracking();
    try {
      applyOptions(instance);
    } finally {
      resetTracking();
      reset2();
    }
  }
}
const attrsProxyHandlers = {
  get(target, key) {
    track(target, "get", "");
    return target[key];
  }
};
function createSetupContext(instance) {
  const expose = (exposed) => {
    instance.exposed = exposed || {};
  };
  {
    return {
      attrs: new Proxy(instance.attrs, attrsProxyHandlers),
      slots: instance.slots,
      emit: instance.emit,
      expose
    };
  }
}
function getComponentPublicInstance(instance) {
  if (instance.exposed) {
    return instance.exposeProxy || (instance.exposeProxy = new Proxy(proxyRefs(markRaw(instance.exposed)), {
      get(target, key) {
        if (key in target) {
          return target[key];
        } else if (key in publicPropertiesMap) {
          return publicPropertiesMap[key](instance);
        }
      },
      has(target, key) {
        return key in target || key in publicPropertiesMap;
      }
    }));
  } else {
    return instance.proxy;
  }
}
const classifyRE = /(?:^|[-_])\w/g;
const classify = (str) => str.replace(classifyRE, (c) => c.toUpperCase()).replace(/[-_]/g, "");
function getComponentName(Component, includeInferred = true) {
  return isFunction(Component) ? Component.displayName || Component.name : Component.name || includeInferred && Component.__name;
}
function formatComponentName(instance, Component, isRoot = false) {
  let name2 = getComponentName(Component);
  if (!name2 && Component.__file) {
    const match = Component.__file.match(/([^/\\]+)\.\w+$/);
    if (match) {
      name2 = match[1];
    }
  }
  if (!name2 && instance) {
    const inferFromRegistry = (registry) => {
      for (const key in registry) {
        if (registry[key] === Component) {
          return key;
        }
      }
    };
    name2 = inferFromRegistry(instance.components) || instance.parent && inferFromRegistry(
      instance.parent.type.components
    ) || inferFromRegistry(instance.appContext.components);
  }
  return name2 ? classify(name2) : isRoot ? `App` : `Anonymous`;
}
function isClassComponent(value) {
  return isFunction(value) && "__vccOpts" in value;
}
const computed = (getterOrOptions, debugOptions) => {
  const c = computed$1(getterOrOptions, debugOptions, isInSSRComponentSetup);
  return c;
};
function h(type, propsOrChildren, children) {
  try {
    setBlockTracking(-1);
    const l = arguments.length;
    if (l === 2) {
      if (isObject(propsOrChildren) && !isArray(propsOrChildren)) {
        if (isVNode(propsOrChildren)) {
          return createVNode(type, null, [propsOrChildren]);
        }
        return createVNode(type, propsOrChildren);
      } else {
        return createVNode(type, null, propsOrChildren);
      }
    } else {
      if (l > 3) {
        children = Array.prototype.slice.call(arguments, 2);
      } else if (l === 3 && isVNode(children)) {
        children = [children];
      }
      return createVNode(type, propsOrChildren, children);
    }
  } finally {
    setBlockTracking(1);
  }
}
function initCustomFormatter() {
  {
    return;
  }
}
function withMemo(memo, render2, cache, index) {
  const cached = cache[index];
  if (cached && isMemoSame(cached, memo)) {
    return cached;
  }
  const ret = render2();
  ret.memo = memo.slice();
  ret.cacheIndex = index;
  return cache[index] = ret;
}
function isMemoSame(cached, memo) {
  const prev = cached.memo;
  if (prev.length != memo.length) {
    return false;
  }
  for (let i = 0; i < prev.length; i++) {
    if (hasChanged(prev[i], memo[i])) {
      return false;
    }
  }
  if (isBlockTreeEnabled > 0 && currentBlock) {
    currentBlock.push(cached);
  }
  return true;
}
const version = "3.5.25";
const warn = NOOP;
const ErrorTypeStrings = ErrorTypeStrings$1;
const devtools = devtools$1;
const setDevtoolsHook = setDevtoolsHook$1;
const _ssrUtils = {
  createComponentInstance,
  setupComponent,
  renderComponentRoot,
  setCurrentRenderingInstance,
  isVNode,
  normalizeVNode,
  getComponentPublicInstance,
  ensureValidVNode,
  pushWarningContext,
  popWarningContext
};
const ssrUtils = _ssrUtils;
const resolveFilter = null;
const compatUtils = null;
const DeprecationTypes = null;
let policy = void 0;
const tt = typeof window !== "undefined" && window.trustedTypes;
if (tt) {
  try {
    policy = /* @__PURE__ */ tt.createPolicy("vue", {
      createHTML: (val) => val
    });
  } catch (e) {
  }
}
const unsafeToTrustedHTML = policy ? (val) => policy.createHTML(val) : (val) => val;
const svgNS = "http://www.w3.org/2000/svg";
const mathmlNS = "http://www.w3.org/1998/Math/MathML";
const doc = typeof document !== "undefined" ? document : null;
const templateContainer = doc && /* @__PURE__ */ doc.createElement("template");
const nodeOps = {
  insert: (child, parent, anchor) => {
    parent.insertBefore(child, anchor || null);
  },
  remove: (child) => {
    const parent = child.parentNode;
    if (parent) {
      parent.removeChild(child);
    }
  },
  createElement: (tag, namespace, is, props) => {
    const el2 = namespace === "svg" ? doc.createElementNS(svgNS, tag) : namespace === "mathml" ? doc.createElementNS(mathmlNS, tag) : is ? doc.createElement(tag, { is }) : doc.createElement(tag);
    if (tag === "select" && props && props.multiple != null) {
      el2.setAttribute("multiple", props.multiple);
    }
    return el2;
  },
  createText: (text) => doc.createTextNode(text),
  createComment: (text) => doc.createComment(text),
  setText: (node, text) => {
    node.nodeValue = text;
  },
  setElementText: (el2, text) => {
    el2.textContent = text;
  },
  parentNode: (node) => node.parentNode,
  nextSibling: (node) => node.nextSibling,
  querySelector: (selector) => doc.querySelector(selector),
  setScopeId(el2, id) {
    el2.setAttribute(id, "");
  },
  // __UNSAFE__
  // Reason: innerHTML.
  // Static content here can only come from compiled templates.
  // As long as the user only uses trusted templates, this is safe.
  insertStaticContent(content, parent, anchor, namespace, start, end) {
    const before = anchor ? anchor.previousSibling : parent.lastChild;
    if (start && (start === end || start.nextSibling)) {
      while (true) {
        parent.insertBefore(start.cloneNode(true), anchor);
        if (start === end || !(start = start.nextSibling)) break;
      }
    } else {
      templateContainer.innerHTML = unsafeToTrustedHTML(
        namespace === "svg" ? `<svg>${content}</svg>` : namespace === "mathml" ? `<math>${content}</math>` : content
      );
      const template = templateContainer.content;
      if (namespace === "svg" || namespace === "mathml") {
        const wrapper = template.firstChild;
        while (wrapper.firstChild) {
          template.appendChild(wrapper.firstChild);
        }
        template.removeChild(wrapper);
      }
      parent.insertBefore(template, anchor);
    }
    return [
      // first
      before ? before.nextSibling : parent.firstChild,
      // last
      anchor ? anchor.previousSibling : parent.lastChild
    ];
  }
};
const TRANSITION$1 = "transition";
const ANIMATION = "animation";
const vtcKey = Symbol("_vtc");
const DOMTransitionPropsValidators = {
  name: String,
  type: String,
  css: {
    type: Boolean,
    default: true
  },
  duration: [String, Number, Object],
  enterFromClass: String,
  enterActiveClass: String,
  enterToClass: String,
  appearFromClass: String,
  appearActiveClass: String,
  appearToClass: String,
  leaveFromClass: String,
  leaveActiveClass: String,
  leaveToClass: String
};
const TransitionPropsValidators = /* @__PURE__ */ extend(
  {},
  BaseTransitionPropsValidators,
  DOMTransitionPropsValidators
);
const decorate$1 = (t) => {
  t.displayName = "Transition";
  t.props = TransitionPropsValidators;
  return t;
};
const Transition = /* @__PURE__ */ decorate$1(
  (props, { slots }) => h(BaseTransition, resolveTransitionProps(props), slots)
);
const callHook = (hook, args = []) => {
  if (isArray(hook)) {
    hook.forEach((h2) => h2(...args));
  } else if (hook) {
    hook(...args);
  }
};
const hasExplicitCallback = (hook) => {
  return hook ? isArray(hook) ? hook.some((h2) => h2.length > 1) : hook.length > 1 : false;
};
function resolveTransitionProps(rawProps) {
  const baseProps = {};
  for (const key in rawProps) {
    if (!(key in DOMTransitionPropsValidators)) {
      baseProps[key] = rawProps[key];
    }
  }
  if (rawProps.css === false) {
    return baseProps;
  }
  const {
    name: name2 = "v",
    type,
    duration,
    enterFromClass = `${name2}-enter-from`,
    enterActiveClass = `${name2}-enter-active`,
    enterToClass = `${name2}-enter-to`,
    appearFromClass = enterFromClass,
    appearActiveClass = enterActiveClass,
    appearToClass = enterToClass,
    leaveFromClass = `${name2}-leave-from`,
    leaveActiveClass = `${name2}-leave-active`,
    leaveToClass = `${name2}-leave-to`
  } = rawProps;
  const durations = normalizeDuration(duration);
  const enterDuration = durations && durations[0];
  const leaveDuration = durations && durations[1];
  const {
    onBeforeEnter,
    onEnter,
    onEnterCancelled,
    onLeave,
    onLeaveCancelled,
    onBeforeAppear = onBeforeEnter,
    onAppear = onEnter,
    onAppearCancelled = onEnterCancelled
  } = baseProps;
  const finishEnter = (el2, isAppear, done, isCancelled) => {
    el2._enterCancelled = isCancelled;
    removeTransitionClass(el2, isAppear ? appearToClass : enterToClass);
    removeTransitionClass(el2, isAppear ? appearActiveClass : enterActiveClass);
    done && done();
  };
  const finishLeave = (el2, done) => {
    el2._isLeaving = false;
    removeTransitionClass(el2, leaveFromClass);
    removeTransitionClass(el2, leaveToClass);
    removeTransitionClass(el2, leaveActiveClass);
    done && done();
  };
  const makeEnterHook = (isAppear) => {
    return (el2, done) => {
      const hook = isAppear ? onAppear : onEnter;
      const resolve2 = () => finishEnter(el2, isAppear, done);
      callHook(hook, [el2, resolve2]);
      nextFrame(() => {
        removeTransitionClass(el2, isAppear ? appearFromClass : enterFromClass);
        addTransitionClass(el2, isAppear ? appearToClass : enterToClass);
        if (!hasExplicitCallback(hook)) {
          whenTransitionEnds(el2, type, enterDuration, resolve2);
        }
      });
    };
  };
  return extend(baseProps, {
    onBeforeEnter(el2) {
      callHook(onBeforeEnter, [el2]);
      addTransitionClass(el2, enterFromClass);
      addTransitionClass(el2, enterActiveClass);
    },
    onBeforeAppear(el2) {
      callHook(onBeforeAppear, [el2]);
      addTransitionClass(el2, appearFromClass);
      addTransitionClass(el2, appearActiveClass);
    },
    onEnter: makeEnterHook(false),
    onAppear: makeEnterHook(true),
    onLeave(el2, done) {
      el2._isLeaving = true;
      const resolve2 = () => finishLeave(el2, done);
      addTransitionClass(el2, leaveFromClass);
      if (!el2._enterCancelled) {
        forceReflow(el2);
        addTransitionClass(el2, leaveActiveClass);
      } else {
        addTransitionClass(el2, leaveActiveClass);
        forceReflow(el2);
      }
      nextFrame(() => {
        if (!el2._isLeaving) {
          return;
        }
        removeTransitionClass(el2, leaveFromClass);
        addTransitionClass(el2, leaveToClass);
        if (!hasExplicitCallback(onLeave)) {
          whenTransitionEnds(el2, type, leaveDuration, resolve2);
        }
      });
      callHook(onLeave, [el2, resolve2]);
    },
    onEnterCancelled(el2) {
      finishEnter(el2, false, void 0, true);
      callHook(onEnterCancelled, [el2]);
    },
    onAppearCancelled(el2) {
      finishEnter(el2, true, void 0, true);
      callHook(onAppearCancelled, [el2]);
    },
    onLeaveCancelled(el2) {
      finishLeave(el2);
      callHook(onLeaveCancelled, [el2]);
    }
  });
}
function normalizeDuration(duration) {
  if (duration == null) {
    return null;
  } else if (isObject(duration)) {
    return [NumberOf(duration.enter), NumberOf(duration.leave)];
  } else {
    const n = NumberOf(duration);
    return [n, n];
  }
}
function NumberOf(val) {
  const res = toNumber(val);
  return res;
}
function addTransitionClass(el2, cls) {
  cls.split(/\s+/).forEach((c) => c && el2.classList.add(c));
  (el2[vtcKey] || (el2[vtcKey] = /* @__PURE__ */ new Set())).add(cls);
}
function removeTransitionClass(el2, cls) {
  cls.split(/\s+/).forEach((c) => c && el2.classList.remove(c));
  const _vtc = el2[vtcKey];
  if (_vtc) {
    _vtc.delete(cls);
    if (!_vtc.size) {
      el2[vtcKey] = void 0;
    }
  }
}
function nextFrame(cb2) {
  requestAnimationFrame(() => {
    requestAnimationFrame(cb2);
  });
}
let endId = 0;
function whenTransitionEnds(el2, expectedType, explicitTimeout, resolve2) {
  const id = el2._endId = ++endId;
  const resolveIfNotStale = () => {
    if (id === el2._endId) {
      resolve2();
    }
  };
  if (explicitTimeout != null) {
    return setTimeout(resolveIfNotStale, explicitTimeout);
  }
  const { type, timeout, propCount } = getTransitionInfo(el2, expectedType);
  if (!type) {
    return resolve2();
  }
  const endEvent = type + "end";
  let ended = 0;
  const end = () => {
    el2.removeEventListener(endEvent, onEnd);
    resolveIfNotStale();
  };
  const onEnd = (e) => {
    if (e.target === el2 && ++ended >= propCount) {
      end();
    }
  };
  setTimeout(() => {
    if (ended < propCount) {
      end();
    }
  }, timeout + 1);
  el2.addEventListener(endEvent, onEnd);
}
function getTransitionInfo(el2, expectedType) {
  const styles = window.getComputedStyle(el2);
  const getStyleProperties = (key) => (styles[key] || "").split(", ");
  const transitionDelays = getStyleProperties(`${TRANSITION$1}Delay`);
  const transitionDurations = getStyleProperties(`${TRANSITION$1}Duration`);
  const transitionTimeout = getTimeout(transitionDelays, transitionDurations);
  const animationDelays = getStyleProperties(`${ANIMATION}Delay`);
  const animationDurations = getStyleProperties(`${ANIMATION}Duration`);
  const animationTimeout = getTimeout(animationDelays, animationDurations);
  let type = null;
  let timeout = 0;
  let propCount = 0;
  if (expectedType === TRANSITION$1) {
    if (transitionTimeout > 0) {
      type = TRANSITION$1;
      timeout = transitionTimeout;
      propCount = transitionDurations.length;
    }
  } else if (expectedType === ANIMATION) {
    if (animationTimeout > 0) {
      type = ANIMATION;
      timeout = animationTimeout;
      propCount = animationDurations.length;
    }
  } else {
    timeout = Math.max(transitionTimeout, animationTimeout);
    type = timeout > 0 ? transitionTimeout > animationTimeout ? TRANSITION$1 : ANIMATION : null;
    propCount = type ? type === TRANSITION$1 ? transitionDurations.length : animationDurations.length : 0;
  }
  const hasTransform = type === TRANSITION$1 && /\b(?:transform|all)(?:,|$)/.test(
    getStyleProperties(`${TRANSITION$1}Property`).toString()
  );
  return {
    type,
    timeout,
    propCount,
    hasTransform
  };
}
function getTimeout(delays, durations) {
  while (delays.length < durations.length) {
    delays = delays.concat(delays);
  }
  return Math.max(...durations.map((d, i) => toMs(d) + toMs(delays[i])));
}
function toMs(s) {
  if (s === "auto") return 0;
  return Number(s.slice(0, -1).replace(",", ".")) * 1e3;
}
function forceReflow(el2) {
  const targetDocument = el2 ? el2.ownerDocument : document;
  return targetDocument.body.offsetHeight;
}
function patchClass(el2, value, isSVG) {
  const transitionClasses = el2[vtcKey];
  if (transitionClasses) {
    value = (value ? [value, ...transitionClasses] : [...transitionClasses]).join(" ");
  }
  if (value == null) {
    el2.removeAttribute("class");
  } else if (isSVG) {
    el2.setAttribute("class", value);
  } else {
    el2.className = value;
  }
}
const vShowOriginalDisplay = Symbol("_vod");
const vShowHidden = Symbol("_vsh");
const vShow = {
  // used for prop mismatch check during hydration
  name: "show",
  beforeMount(el2, { value }, { transition }) {
    el2[vShowOriginalDisplay] = el2.style.display === "none" ? "" : el2.style.display;
    if (transition && value) {
      transition.beforeEnter(el2);
    } else {
      setDisplay(el2, value);
    }
  },
  mounted(el2, { value }, { transition }) {
    if (transition && value) {
      transition.enter(el2);
    }
  },
  updated(el2, { value, oldValue }, { transition }) {
    if (!value === !oldValue) return;
    if (transition) {
      if (value) {
        transition.beforeEnter(el2);
        setDisplay(el2, true);
        transition.enter(el2);
      } else {
        transition.leave(el2, () => {
          setDisplay(el2, false);
        });
      }
    } else {
      setDisplay(el2, value);
    }
  },
  beforeUnmount(el2, { value }) {
    setDisplay(el2, value);
  }
};
function setDisplay(el2, value) {
  el2.style.display = value ? el2[vShowOriginalDisplay] : "none";
  el2[vShowHidden] = !value;
}
function initVShowForSSR() {
  vShow.getSSRProps = ({ value }) => {
    if (!value) {
      return { style: { display: "none" } };
    }
  };
}
const CSS_VAR_TEXT = Symbol("");
function useCssVars(getter) {
  const instance = getCurrentInstance();
  if (!instance) {
    return;
  }
  const updateTeleports = instance.ut = (vars = getter(instance.proxy)) => {
    Array.from(
      document.querySelectorAll(`[data-v-owner="${instance.uid}"]`)
    ).forEach((node) => setVarsOnNode(node, vars));
  };
  const setVars = () => {
    const vars = getter(instance.proxy);
    if (instance.ce) {
      setVarsOnNode(instance.ce, vars);
    } else {
      setVarsOnVNode(instance.subTree, vars);
    }
    updateTeleports(vars);
  };
  onBeforeUpdate(() => {
    queuePostFlushCb(setVars);
  });
  onMounted(() => {
    watch(setVars, NOOP, { flush: "post" });
    const ob2 = new MutationObserver(setVars);
    ob2.observe(instance.subTree.el.parentNode, { childList: true });
    onUnmounted(() => ob2.disconnect());
  });
}
function setVarsOnVNode(vnode, vars) {
  if (vnode.shapeFlag & 128) {
    const suspense = vnode.suspense;
    vnode = suspense.activeBranch;
    if (suspense.pendingBranch && !suspense.isHydrating) {
      suspense.effects.push(() => {
        setVarsOnVNode(suspense.activeBranch, vars);
      });
    }
  }
  while (vnode.component) {
    vnode = vnode.component.subTree;
  }
  if (vnode.shapeFlag & 1 && vnode.el) {
    setVarsOnNode(vnode.el, vars);
  } else if (vnode.type === Fragment) {
    vnode.children.forEach((c) => setVarsOnVNode(c, vars));
  } else if (vnode.type === Static) {
    let { el: el2, anchor } = vnode;
    while (el2) {
      setVarsOnNode(el2, vars);
      if (el2 === anchor) break;
      el2 = el2.nextSibling;
    }
  }
}
function setVarsOnNode(el2, vars) {
  if (el2.nodeType === 1) {
    const style = el2.style;
    let cssText = "";
    for (const key in vars) {
      const value = normalizeCssVarValue(vars[key]);
      style.setProperty(`--${key}`, value);
      cssText += `--${key}: ${value};`;
    }
    style[CSS_VAR_TEXT] = cssText;
  }
}
const displayRE = /(?:^|;)\s*display\s*:/;
function patchStyle(el2, prev, next) {
  const style = el2.style;
  const isCssString = isString(next);
  let hasControlledDisplay = false;
  if (next && !isCssString) {
    if (prev) {
      if (!isString(prev)) {
        for (const key in prev) {
          if (next[key] == null) {
            setStyle(style, key, "");
          }
        }
      } else {
        for (const prevStyle of prev.split(";")) {
          const key = prevStyle.slice(0, prevStyle.indexOf(":")).trim();
          if (next[key] == null) {
            setStyle(style, key, "");
          }
        }
      }
    }
    for (const key in next) {
      if (key === "display") {
        hasControlledDisplay = true;
      }
      setStyle(style, key, next[key]);
    }
  } else {
    if (isCssString) {
      if (prev !== next) {
        const cssVarText = style[CSS_VAR_TEXT];
        if (cssVarText) {
          next += ";" + cssVarText;
        }
        style.cssText = next;
        hasControlledDisplay = displayRE.test(next);
      }
    } else if (prev) {
      el2.removeAttribute("style");
    }
  }
  if (vShowOriginalDisplay in el2) {
    el2[vShowOriginalDisplay] = hasControlledDisplay ? style.display : "";
    if (el2[vShowHidden]) {
      style.display = "none";
    }
  }
}
const importantRE = /\s*!important$/;
function setStyle(style, name2, val) {
  if (isArray(val)) {
    val.forEach((v) => setStyle(style, name2, v));
  } else {
    if (val == null) val = "";
    if (name2.startsWith("--")) {
      style.setProperty(name2, val);
    } else {
      const prefixed = autoPrefix(style, name2);
      if (importantRE.test(val)) {
        style.setProperty(
          hyphenate(prefixed),
          val.replace(importantRE, ""),
          "important"
        );
      } else {
        style[prefixed] = val;
      }
    }
  }
}
const prefixes = ["Webkit", "Moz", "ms"];
const prefixCache = {};
function autoPrefix(style, rawName) {
  const cached = prefixCache[rawName];
  if (cached) {
    return cached;
  }
  let name2 = camelize(rawName);
  if (name2 !== "filter" && name2 in style) {
    return prefixCache[rawName] = name2;
  }
  name2 = capitalize(name2);
  for (let i = 0; i < prefixes.length; i++) {
    const prefixed = prefixes[i] + name2;
    if (prefixed in style) {
      return prefixCache[rawName] = prefixed;
    }
  }
  return rawName;
}
const xlinkNS = "http://www.w3.org/1999/xlink";
function patchAttr(el2, key, value, isSVG, instance, isBoolean = isSpecialBooleanAttr(key)) {
  if (isSVG && key.startsWith("xlink:")) {
    if (value == null) {
      el2.removeAttributeNS(xlinkNS, key.slice(6, key.length));
    } else {
      el2.setAttributeNS(xlinkNS, key, value);
    }
  } else {
    if (value == null || isBoolean && !includeBooleanAttr(value)) {
      el2.removeAttribute(key);
    } else {
      el2.setAttribute(
        key,
        isBoolean ? "" : isSymbol(value) ? String(value) : value
      );
    }
  }
}
function patchDOMProp(el2, key, value, parentComponent, attrName) {
  if (key === "innerHTML" || key === "textContent") {
    if (value != null) {
      el2[key] = key === "innerHTML" ? unsafeToTrustedHTML(value) : value;
    }
    return;
  }
  const tag = el2.tagName;
  if (key === "value" && tag !== "PROGRESS" && // custom elements may use _value internally
  !tag.includes("-")) {
    const oldValue = tag === "OPTION" ? el2.getAttribute("value") || "" : el2.value;
    const newValue = value == null ? (
      // #11647: value should be set as empty string for null and undefined,
      // but <input type="checkbox"> should be set as 'on'.
      el2.type === "checkbox" ? "on" : ""
    ) : String(value);
    if (oldValue !== newValue || !("_value" in el2)) {
      el2.value = newValue;
    }
    if (value == null) {
      el2.removeAttribute(key);
    }
    el2._value = value;
    return;
  }
  let needRemove = false;
  if (value === "" || value == null) {
    const type = typeof el2[key];
    if (type === "boolean") {
      value = includeBooleanAttr(value);
    } else if (value == null && type === "string") {
      value = "";
      needRemove = true;
    } else if (type === "number") {
      value = 0;
      needRemove = true;
    }
  }
  try {
    el2[key] = value;
  } catch (e) {
  }
  needRemove && el2.removeAttribute(attrName || key);
}
function addEventListener(el2, event, handler, options) {
  el2.addEventListener(event, handler, options);
}
function removeEventListener(el2, event, handler, options) {
  el2.removeEventListener(event, handler, options);
}
const veiKey = Symbol("_vei");
function patchEvent(el2, rawName, prevValue, nextValue, instance = null) {
  const invokers = el2[veiKey] || (el2[veiKey] = {});
  const existingInvoker = invokers[rawName];
  if (nextValue && existingInvoker) {
    existingInvoker.value = nextValue;
  } else {
    const [name2, options] = parseName(rawName);
    if (nextValue) {
      const invoker = invokers[rawName] = createInvoker(
        nextValue,
        instance
      );
      addEventListener(el2, name2, invoker, options);
    } else if (existingInvoker) {
      removeEventListener(el2, name2, existingInvoker, options);
      invokers[rawName] = void 0;
    }
  }
}
const optionsModifierRE = /(?:Once|Passive|Capture)$/;
function parseName(name2) {
  let options;
  if (optionsModifierRE.test(name2)) {
    options = {};
    let m;
    while (m = name2.match(optionsModifierRE)) {
      name2 = name2.slice(0, name2.length - m[0].length);
      options[m[0].toLowerCase()] = true;
    }
  }
  const event = name2[2] === ":" ? name2.slice(3) : hyphenate(name2.slice(2));
  return [event, options];
}
let cachedNow = 0;
const p = /* @__PURE__ */ Promise.resolve();
const getNow = () => cachedNow || (p.then(() => cachedNow = 0), cachedNow = Date.now());
function createInvoker(initialValue, instance) {
  const invoker = (e) => {
    if (!e._vts) {
      e._vts = Date.now();
    } else if (e._vts <= invoker.attached) {
      return;
    }
    callWithAsyncErrorHandling(
      patchStopImmediatePropagation(e, invoker.value),
      instance,
      5,
      [e]
    );
  };
  invoker.value = initialValue;
  invoker.attached = getNow();
  return invoker;
}
function patchStopImmediatePropagation(e, value) {
  if (isArray(value)) {
    const originalStop = e.stopImmediatePropagation;
    e.stopImmediatePropagation = () => {
      originalStop.call(e);
      e._stopped = true;
    };
    return value.map(
      (fn) => (e2) => !e2._stopped && fn && fn(e2)
    );
  } else {
    return value;
  }
}
const isNativeOn = (key) => key.charCodeAt(0) === 111 && key.charCodeAt(1) === 110 && // lowercase letter
key.charCodeAt(2) > 96 && key.charCodeAt(2) < 123;
const patchProp = (el2, key, prevValue, nextValue, namespace, parentComponent) => {
  const isSVG = namespace === "svg";
  if (key === "class") {
    patchClass(el2, nextValue, isSVG);
  } else if (key === "style") {
    patchStyle(el2, prevValue, nextValue);
  } else if (isOn(key)) {
    if (!isModelListener(key)) {
      patchEvent(el2, key, prevValue, nextValue, parentComponent);
    }
  } else if (key[0] === "." ? (key = key.slice(1), true) : key[0] === "^" ? (key = key.slice(1), false) : shouldSetAsProp(el2, key, nextValue, isSVG)) {
    patchDOMProp(el2, key, nextValue);
    if (!el2.tagName.includes("-") && (key === "value" || key === "checked" || key === "selected")) {
      patchAttr(el2, key, nextValue, isSVG, parentComponent, key !== "value");
    }
  } else if (
    // #11081 force set props for possible async custom element
    el2._isVueCE && (/[A-Z]/.test(key) || !isString(nextValue))
  ) {
    patchDOMProp(el2, camelize(key), nextValue, parentComponent, key);
  } else {
    if (key === "true-value") {
      el2._trueValue = nextValue;
    } else if (key === "false-value") {
      el2._falseValue = nextValue;
    }
    patchAttr(el2, key, nextValue, isSVG);
  }
};
function shouldSetAsProp(el2, key, value, isSVG) {
  if (isSVG) {
    if (key === "innerHTML" || key === "textContent") {
      return true;
    }
    if (key in el2 && isNativeOn(key) && isFunction(value)) {
      return true;
    }
    return false;
  }
  if (key === "spellcheck" || key === "draggable" || key === "translate" || key === "autocorrect") {
    return false;
  }
  if (key === "sandbox" && el2.tagName === "IFRAME") {
    return false;
  }
  if (key === "form") {
    return false;
  }
  if (key === "list" && el2.tagName === "INPUT") {
    return false;
  }
  if (key === "type" && el2.tagName === "TEXTAREA") {
    return false;
  }
  if (key === "width" || key === "height") {
    const tag = el2.tagName;
    if (tag === "IMG" || tag === "VIDEO" || tag === "CANVAS" || tag === "SOURCE") {
      return false;
    }
  }
  if (isNativeOn(key) && isString(value)) {
    return false;
  }
  return key in el2;
}
const REMOVAL = {};
// @__NO_SIDE_EFFECTS__
function defineCustomElement(options, extraOptions, _createApp) {
  let Comp = /* @__PURE__ */ defineComponent(options, extraOptions);
  if (isPlainObject(Comp)) Comp = extend({}, Comp, extraOptions);
  class VueCustomElement extends VueElement {
    constructor(initialProps) {
      super(Comp, initialProps, _createApp);
    }
  }
  VueCustomElement.def = Comp;
  return VueCustomElement;
}
const defineSSRCustomElement = (/* @__NO_SIDE_EFFECTS__ */ (options, extraOptions) => {
  return /* @__PURE__ */ defineCustomElement(options, extraOptions, createSSRApp);
});
const BaseClass = typeof HTMLElement !== "undefined" ? HTMLElement : class {
};
class VueElement extends BaseClass {
  constructor(_def, _props = {}, _createApp = createApp) {
    super();
    this._def = _def;
    this._props = _props;
    this._createApp = _createApp;
    this._isVueCE = true;
    this._instance = null;
    this._app = null;
    this._nonce = this._def.nonce;
    this._connected = false;
    this._resolved = false;
    this._patching = false;
    this._dirty = false;
    this._numberProps = null;
    this._styleChildren = /* @__PURE__ */ new WeakSet();
    this._ob = null;
    if (this.shadowRoot && _createApp !== createApp) {
      this._root = this.shadowRoot;
    } else {
      if (_def.shadowRoot !== false) {
        this.attachShadow(
          extend({}, _def.shadowRootOptions, {
            mode: "open"
          })
        );
        this._root = this.shadowRoot;
      } else {
        this._root = this;
      }
    }
  }
  connectedCallback() {
    if (!this.isConnected) return;
    if (!this.shadowRoot && !this._resolved) {
      this._parseSlots();
    }
    this._connected = true;
    let parent = this;
    while (parent = parent && (parent.parentNode || parent.host)) {
      if (parent instanceof VueElement) {
        this._parent = parent;
        break;
      }
    }
    if (!this._instance) {
      if (this._resolved) {
        this._mount(this._def);
      } else {
        if (parent && parent._pendingResolve) {
          this._pendingResolve = parent._pendingResolve.then(() => {
            this._pendingResolve = void 0;
            this._resolveDef();
          });
        } else {
          this._resolveDef();
        }
      }
    }
  }
  _setParent(parent = this._parent) {
    if (parent) {
      this._instance.parent = parent._instance;
      this._inheritParentContext(parent);
    }
  }
  _inheritParentContext(parent = this._parent) {
    if (parent && this._app) {
      Object.setPrototypeOf(
        this._app._context.provides,
        parent._instance.provides
      );
    }
  }
  disconnectedCallback() {
    this._connected = false;
    nextTick(() => {
      if (!this._connected) {
        if (this._ob) {
          this._ob.disconnect();
          this._ob = null;
        }
        this._app && this._app.unmount();
        if (this._instance) this._instance.ce = void 0;
        this._app = this._instance = null;
        if (this._teleportTargets) {
          this._teleportTargets.clear();
          this._teleportTargets = void 0;
        }
      }
    });
  }
  _processMutations(mutations) {
    for (const m of mutations) {
      this._setAttr(m.attributeName);
    }
  }
  /**
   * resolve inner component definition (handle possible async component)
   */
  _resolveDef() {
    if (this._pendingResolve) {
      return;
    }
    for (let i = 0; i < this.attributes.length; i++) {
      this._setAttr(this.attributes[i].name);
    }
    this._ob = new MutationObserver(this._processMutations.bind(this));
    this._ob.observe(this, { attributes: true });
    const resolve2 = (def2, isAsync = false) => {
      this._resolved = true;
      this._pendingResolve = void 0;
      const { props, styles } = def2;
      let numberProps;
      if (props && !isArray(props)) {
        for (const key in props) {
          const opt = props[key];
          if (opt === Number || opt && opt.type === Number) {
            if (key in this._props) {
              this._props[key] = toNumber(this._props[key]);
            }
            (numberProps || (numberProps = /* @__PURE__ */ Object.create(null)))[camelize(key)] = true;
          }
        }
      }
      this._numberProps = numberProps;
      this._resolveProps(def2);
      if (this.shadowRoot) {
        this._applyStyles(styles);
      }
      this._mount(def2);
    };
    const asyncDef = this._def.__asyncLoader;
    if (asyncDef) {
      this._pendingResolve = asyncDef().then((def2) => {
        def2.configureApp = this._def.configureApp;
        resolve2(this._def = def2, true);
      });
    } else {
      resolve2(this._def);
    }
  }
  _mount(def2) {
    if (!def2.name) {
      def2.name = "VueElement";
    }
    this._app = this._createApp(def2);
    this._inheritParentContext();
    if (def2.configureApp) {
      def2.configureApp(this._app);
    }
    this._app._ceVNode = this._createVNode();
    this._app.mount(this._root);
    const exposed = this._instance && this._instance.exposed;
    if (!exposed) return;
    for (const key in exposed) {
      if (!hasOwn(this, key)) {
        Object.defineProperty(this, key, {
          // unwrap ref to be consistent with public instance behavior
          get: () => unref(exposed[key])
        });
      }
    }
  }
  _resolveProps(def2) {
    const { props } = def2;
    const declaredPropKeys = isArray(props) ? props : Object.keys(props || {});
    for (const key of Object.keys(this)) {
      if (key[0] !== "_" && declaredPropKeys.includes(key)) {
        this._setProp(key, this[key]);
      }
    }
    for (const key of declaredPropKeys.map(camelize)) {
      Object.defineProperty(this, key, {
        get() {
          return this._getProp(key);
        },
        set(val) {
          this._setProp(key, val, true, !this._patching);
        }
      });
    }
  }
  _setAttr(key) {
    if (key.startsWith("data-v-")) return;
    const has = this.hasAttribute(key);
    let value = has ? this.getAttribute(key) : REMOVAL;
    const camelKey = camelize(key);
    if (has && this._numberProps && this._numberProps[camelKey]) {
      value = toNumber(value);
    }
    this._setProp(camelKey, value, false, true);
  }
  /**
   * @internal
   */
  _getProp(key) {
    return this._props[key];
  }
  /**
   * @internal
   */
  _setProp(key, val, shouldReflect = true, shouldUpdate = false) {
    if (val !== this._props[key]) {
      this._dirty = true;
      if (val === REMOVAL) {
        delete this._props[key];
      } else {
        this._props[key] = val;
        if (key === "key" && this._app) {
          this._app._ceVNode.key = val;
        }
      }
      if (shouldUpdate && this._instance) {
        this._update();
      }
      if (shouldReflect) {
        const ob2 = this._ob;
        if (ob2) {
          this._processMutations(ob2.takeRecords());
          ob2.disconnect();
        }
        if (val === true) {
          this.setAttribute(hyphenate(key), "");
        } else if (typeof val === "string" || typeof val === "number") {
          this.setAttribute(hyphenate(key), val + "");
        } else if (!val) {
          this.removeAttribute(hyphenate(key));
        }
        ob2 && ob2.observe(this, { attributes: true });
      }
    }
  }
  _update() {
    const vnode = this._createVNode();
    if (this._app) vnode.appContext = this._app._context;
    render(vnode, this._root);
  }
  _createVNode() {
    const baseProps = {};
    if (!this.shadowRoot) {
      baseProps.onVnodeMounted = baseProps.onVnodeUpdated = this._renderSlots.bind(this);
    }
    const vnode = createVNode(this._def, extend(baseProps, this._props));
    if (!this._instance) {
      vnode.ce = (instance) => {
        this._instance = instance;
        instance.ce = this;
        instance.isCE = true;
        const dispatch = (event, args) => {
          this.dispatchEvent(
            new CustomEvent(
              event,
              isPlainObject(args[0]) ? extend({ detail: args }, args[0]) : { detail: args }
            )
          );
        };
        instance.emit = (event, ...args) => {
          dispatch(event, args);
          if (hyphenate(event) !== event) {
            dispatch(hyphenate(event), args);
          }
        };
        this._setParent();
      };
    }
    return vnode;
  }
  _applyStyles(styles, owner) {
    if (!styles) return;
    if (owner) {
      if (owner === this._def || this._styleChildren.has(owner)) {
        return;
      }
      this._styleChildren.add(owner);
    }
    const nonce = this._nonce;
    for (let i = styles.length - 1; i >= 0; i--) {
      const s = document.createElement("style");
      if (nonce) s.setAttribute("nonce", nonce);
      s.textContent = styles[i];
      this.shadowRoot.prepend(s);
    }
  }
  /**
   * Only called when shadowRoot is false
   */
  _parseSlots() {
    const slots = this._slots = {};
    let n;
    while (n = this.firstChild) {
      const slotName = n.nodeType === 1 && n.getAttribute("slot") || "default";
      (slots[slotName] || (slots[slotName] = [])).push(n);
      this.removeChild(n);
    }
  }
  /**
   * Only called when shadowRoot is false
   */
  _renderSlots() {
    const outlets = this._getSlots();
    const scopeId = this._instance.type.__scopeId;
    for (let i = 0; i < outlets.length; i++) {
      const o = outlets[i];
      const slotName = o.getAttribute("name") || "default";
      const content = this._slots[slotName];
      const parent = o.parentNode;
      if (content) {
        for (const n of content) {
          if (scopeId && n.nodeType === 1) {
            const id = scopeId + "-s";
            const walker = document.createTreeWalker(n, 1);
            n.setAttribute(id, "");
            let child;
            while (child = walker.nextNode()) {
              child.setAttribute(id, "");
            }
          }
          parent.insertBefore(n, o);
        }
      } else {
        while (o.firstChild) parent.insertBefore(o.firstChild, o);
      }
      parent.removeChild(o);
    }
  }
  /**
   * @internal
   */
  _getSlots() {
    const roots = [this];
    if (this._teleportTargets) {
      roots.push(...this._teleportTargets);
    }
    const slots = /* @__PURE__ */ new Set();
    for (const root of roots) {
      const found = root.querySelectorAll("slot");
      for (let i = 0; i < found.length; i++) {
        slots.add(found[i]);
      }
    }
    return Array.from(slots);
  }
  /**
   * @internal
   */
  _injectChildStyle(comp) {
    this._applyStyles(comp.styles, comp);
  }
  /**
   * @internal
   */
  _beginPatch() {
    this._patching = true;
    this._dirty = false;
  }
  /**
   * @internal
   */
  _endPatch() {
    this._patching = false;
    if (this._dirty && this._instance) {
      this._update();
    }
  }
  /**
   * @internal
   */
  _removeChildStyle(comp) {
  }
}
function useHost(caller) {
  const instance = getCurrentInstance();
  const el2 = instance && instance.ce;
  if (el2) {
    return el2;
  }
  return null;
}
function useShadowRoot() {
  const el2 = useHost();
  return el2 && el2.shadowRoot;
}
function useCssModule(name2 = "$style") {
  {
    const instance = getCurrentInstance();
    if (!instance) {
      return EMPTY_OBJ;
    }
    const modules = instance.type.__cssModules;
    if (!modules) {
      return EMPTY_OBJ;
    }
    const mod = modules[name2];
    if (!mod) {
      return EMPTY_OBJ;
    }
    return mod;
  }
}
const positionMap = /* @__PURE__ */ new WeakMap();
const newPositionMap = /* @__PURE__ */ new WeakMap();
const moveCbKey = Symbol("_moveCb");
const enterCbKey = Symbol("_enterCb");
const decorate = (t) => {
  delete t.props.mode;
  return t;
};
const TransitionGroupImpl = /* @__PURE__ */ decorate({
  name: "TransitionGroup",
  props: /* @__PURE__ */ extend({}, TransitionPropsValidators, {
    tag: String,
    moveClass: String
  }),
  setup(props, { slots }) {
    const instance = getCurrentInstance();
    const state = useTransitionState();
    let prevChildren;
    let children;
    onUpdated(() => {
      if (!prevChildren.length) {
        return;
      }
      const moveClass = props.moveClass || `${props.name || "v"}-move`;
      if (!hasCSSTransform(
        prevChildren[0].el,
        instance.vnode.el,
        moveClass
      )) {
        prevChildren = [];
        return;
      }
      prevChildren.forEach(callPendingCbs);
      prevChildren.forEach(recordPosition);
      const movedChildren = prevChildren.filter(applyTranslation);
      forceReflow(instance.vnode.el);
      movedChildren.forEach((c) => {
        const el2 = c.el;
        const style = el2.style;
        addTransitionClass(el2, moveClass);
        style.transform = style.webkitTransform = style.transitionDuration = "";
        const cb2 = el2[moveCbKey] = (e) => {
          if (e && e.target !== el2) {
            return;
          }
          if (!e || e.propertyName.endsWith("transform")) {
            el2.removeEventListener("transitionend", cb2);
            el2[moveCbKey] = null;
            removeTransitionClass(el2, moveClass);
          }
        };
        el2.addEventListener("transitionend", cb2);
      });
      prevChildren = [];
    });
    return () => {
      const rawProps = toRaw(props);
      const cssTransitionProps = resolveTransitionProps(rawProps);
      let tag = rawProps.tag || Fragment;
      prevChildren = [];
      if (children) {
        for (let i = 0; i < children.length; i++) {
          const child = children[i];
          if (child.el && child.el instanceof Element) {
            prevChildren.push(child);
            setTransitionHooks(
              child,
              resolveTransitionHooks(
                child,
                cssTransitionProps,
                state,
                instance
              )
            );
            positionMap.set(child, {
              left: child.el.offsetLeft,
              top: child.el.offsetTop
            });
          }
        }
      }
      children = slots.default ? getTransitionRawChildren(slots.default()) : [];
      for (let i = 0; i < children.length; i++) {
        const child = children[i];
        if (child.key != null) {
          setTransitionHooks(
            child,
            resolveTransitionHooks(child, cssTransitionProps, state, instance)
          );
        }
      }
      return createVNode(tag, null, children);
    };
  }
});
const TransitionGroup = TransitionGroupImpl;
function callPendingCbs(c) {
  const el2 = c.el;
  if (el2[moveCbKey]) {
    el2[moveCbKey]();
  }
  if (el2[enterCbKey]) {
    el2[enterCbKey]();
  }
}
function recordPosition(c) {
  newPositionMap.set(c, {
    left: c.el.offsetLeft,
    top: c.el.offsetTop
  });
}
function applyTranslation(c) {
  const oldPos = positionMap.get(c);
  const newPos = newPositionMap.get(c);
  const dx2 = oldPos.left - newPos.left;
  const dy2 = oldPos.top - newPos.top;
  if (dx2 || dy2) {
    const s = c.el.style;
    s.transform = s.webkitTransform = `translate(${dx2}px,${dy2}px)`;
    s.transitionDuration = "0s";
    return c;
  }
}
function hasCSSTransform(el2, root, moveClass) {
  const clone = el2.cloneNode();
  const _vtc = el2[vtcKey];
  if (_vtc) {
    _vtc.forEach((cls) => {
      cls.split(/\s+/).forEach((c) => c && clone.classList.remove(c));
    });
  }
  moveClass.split(/\s+/).forEach((c) => c && clone.classList.add(c));
  clone.style.display = "none";
  const container = root.nodeType === 1 ? root : root.parentNode;
  container.appendChild(clone);
  const { hasTransform } = getTransitionInfo(clone);
  container.removeChild(clone);
  return hasTransform;
}
const getModelAssigner = (vnode) => {
  const fn = vnode.props["onUpdate:modelValue"] || false;
  return isArray(fn) ? (value) => invokeArrayFns(fn, value) : fn;
};
function onCompositionStart(e) {
  e.target.composing = true;
}
function onCompositionEnd(e) {
  const target = e.target;
  if (target.composing) {
    target.composing = false;
    target.dispatchEvent(new Event("input"));
  }
}
const assignKey = Symbol("_assign");
function castValue(value, trim, number) {
  if (trim) value = value.trim();
  if (number) value = looseToNumber(value);
  return value;
}
const vModelText = {
  created(el2, { modifiers: { lazy, trim, number } }, vnode) {
    el2[assignKey] = getModelAssigner(vnode);
    const castToNumber = number || vnode.props && vnode.props.type === "number";
    addEventListener(el2, lazy ? "change" : "input", (e) => {
      if (e.target.composing) return;
      el2[assignKey](castValue(el2.value, trim, castToNumber));
    });
    if (trim || castToNumber) {
      addEventListener(el2, "change", () => {
        el2.value = castValue(el2.value, trim, castToNumber);
      });
    }
    if (!lazy) {
      addEventListener(el2, "compositionstart", onCompositionStart);
      addEventListener(el2, "compositionend", onCompositionEnd);
      addEventListener(el2, "change", onCompositionEnd);
    }
  },
  // set value on mounted so it's after min/max for type="range"
  mounted(el2, { value }) {
    el2.value = value == null ? "" : value;
  },
  beforeUpdate(el2, { value, oldValue, modifiers: { lazy, trim, number } }, vnode) {
    el2[assignKey] = getModelAssigner(vnode);
    if (el2.composing) return;
    const elValue = (number || el2.type === "number") && !/^0\d/.test(el2.value) ? looseToNumber(el2.value) : el2.value;
    const newValue = value == null ? "" : value;
    if (elValue === newValue) {
      return;
    }
    if (document.activeElement === el2 && el2.type !== "range") {
      if (lazy && value === oldValue) {
        return;
      }
      if (trim && el2.value.trim() === newValue) {
        return;
      }
    }
    el2.value = newValue;
  }
};
const vModelCheckbox = {
  // #4096 array checkboxes need to be deep traversed
  deep: true,
  created(el2, _, vnode) {
    el2[assignKey] = getModelAssigner(vnode);
    addEventListener(el2, "change", () => {
      const modelValue = el2._modelValue;
      const elementValue = getValue(el2);
      const checked = el2.checked;
      const assign = el2[assignKey];
      if (isArray(modelValue)) {
        const index = looseIndexOf(modelValue, elementValue);
        const found = index !== -1;
        if (checked && !found) {
          assign(modelValue.concat(elementValue));
        } else if (!checked && found) {
          const filtered = [...modelValue];
          filtered.splice(index, 1);
          assign(filtered);
        }
      } else if (isSet(modelValue)) {
        const cloned = new Set(modelValue);
        if (checked) {
          cloned.add(elementValue);
        } else {
          cloned.delete(elementValue);
        }
        assign(cloned);
      } else {
        assign(getCheckboxValue(el2, checked));
      }
    });
  },
  // set initial checked on mount to wait for true-value/false-value
  mounted: setChecked,
  beforeUpdate(el2, binding, vnode) {
    el2[assignKey] = getModelAssigner(vnode);
    setChecked(el2, binding, vnode);
  }
};
function setChecked(el2, { value, oldValue }, vnode) {
  el2._modelValue = value;
  let checked;
  if (isArray(value)) {
    checked = looseIndexOf(value, vnode.props.value) > -1;
  } else if (isSet(value)) {
    checked = value.has(vnode.props.value);
  } else {
    if (value === oldValue) return;
    checked = looseEqual(value, getCheckboxValue(el2, true));
  }
  if (el2.checked !== checked) {
    el2.checked = checked;
  }
}
const vModelRadio = {
  created(el2, { value }, vnode) {
    el2.checked = looseEqual(value, vnode.props.value);
    el2[assignKey] = getModelAssigner(vnode);
    addEventListener(el2, "change", () => {
      el2[assignKey](getValue(el2));
    });
  },
  beforeUpdate(el2, { value, oldValue }, vnode) {
    el2[assignKey] = getModelAssigner(vnode);
    if (value !== oldValue) {
      el2.checked = looseEqual(value, vnode.props.value);
    }
  }
};
const vModelSelect = {
  // <select multiple> value need to be deep traversed
  deep: true,
  created(el2, { value, modifiers: { number } }, vnode) {
    const isSetModel = isSet(value);
    addEventListener(el2, "change", () => {
      const selectedVal = Array.prototype.filter.call(el2.options, (o) => o.selected).map(
        (o) => number ? looseToNumber(getValue(o)) : getValue(o)
      );
      el2[assignKey](
        el2.multiple ? isSetModel ? new Set(selectedVal) : selectedVal : selectedVal[0]
      );
      el2._assigning = true;
      nextTick(() => {
        el2._assigning = false;
      });
    });
    el2[assignKey] = getModelAssigner(vnode);
  },
  // set value in mounted & updated because <select> relies on its children
  // <option>s.
  mounted(el2, { value }) {
    setSelected(el2, value);
  },
  beforeUpdate(el2, _binding, vnode) {
    el2[assignKey] = getModelAssigner(vnode);
  },
  updated(el2, { value }) {
    if (!el2._assigning) {
      setSelected(el2, value);
    }
  }
};
function setSelected(el2, value) {
  const isMultiple = el2.multiple;
  const isArrayValue = isArray(value);
  if (isMultiple && !isArrayValue && !isSet(value)) {
    return;
  }
  for (let i = 0, l = el2.options.length; i < l; i++) {
    const option = el2.options[i];
    const optionValue = getValue(option);
    if (isMultiple) {
      if (isArrayValue) {
        const optionType = typeof optionValue;
        if (optionType === "string" || optionType === "number") {
          option.selected = value.some((v) => String(v) === String(optionValue));
        } else {
          option.selected = looseIndexOf(value, optionValue) > -1;
        }
      } else {
        option.selected = value.has(optionValue);
      }
    } else if (looseEqual(getValue(option), value)) {
      if (el2.selectedIndex !== i) el2.selectedIndex = i;
      return;
    }
  }
  if (!isMultiple && el2.selectedIndex !== -1) {
    el2.selectedIndex = -1;
  }
}
function getValue(el2) {
  return "_value" in el2 ? el2._value : el2.value;
}
function getCheckboxValue(el2, checked) {
  const key = checked ? "_trueValue" : "_falseValue";
  return key in el2 ? el2[key] : checked;
}
const vModelDynamic = {
  created(el2, binding, vnode) {
    callModelHook(el2, binding, vnode, null, "created");
  },
  mounted(el2, binding, vnode) {
    callModelHook(el2, binding, vnode, null, "mounted");
  },
  beforeUpdate(el2, binding, vnode, prevVNode) {
    callModelHook(el2, binding, vnode, prevVNode, "beforeUpdate");
  },
  updated(el2, binding, vnode, prevVNode) {
    callModelHook(el2, binding, vnode, prevVNode, "updated");
  }
};
function resolveDynamicModel(tagName, type) {
  switch (tagName) {
    case "SELECT":
      return vModelSelect;
    case "TEXTAREA":
      return vModelText;
    default:
      switch (type) {
        case "checkbox":
          return vModelCheckbox;
        case "radio":
          return vModelRadio;
        default:
          return vModelText;
      }
  }
}
function callModelHook(el2, binding, vnode, prevVNode, hook) {
  const modelToUse = resolveDynamicModel(
    el2.tagName,
    vnode.props && vnode.props.type
  );
  const fn = modelToUse[hook];
  fn && fn(el2, binding, vnode, prevVNode);
}
function initVModelForSSR() {
  vModelText.getSSRProps = ({ value }) => ({ value });
  vModelRadio.getSSRProps = ({ value }, vnode) => {
    if (vnode.props && looseEqual(vnode.props.value, value)) {
      return { checked: true };
    }
  };
  vModelCheckbox.getSSRProps = ({ value }, vnode) => {
    if (isArray(value)) {
      if (vnode.props && looseIndexOf(value, vnode.props.value) > -1) {
        return { checked: true };
      }
    } else if (isSet(value)) {
      if (vnode.props && value.has(vnode.props.value)) {
        return { checked: true };
      }
    } else if (value) {
      return { checked: true };
    }
  };
  vModelDynamic.getSSRProps = (binding, vnode) => {
    if (typeof vnode.type !== "string") {
      return;
    }
    const modelToUse = resolveDynamicModel(
      // resolveDynamicModel expects an uppercase tag name, but vnode.type is lowercase
      vnode.type.toUpperCase(),
      vnode.props && vnode.props.type
    );
    if (modelToUse.getSSRProps) {
      return modelToUse.getSSRProps(binding, vnode);
    }
  };
}
const systemModifiers = ["ctrl", "shift", "alt", "meta"];
const modifierGuards = {
  stop: (e) => e.stopPropagation(),
  prevent: (e) => e.preventDefault(),
  self: (e) => e.target !== e.currentTarget,
  ctrl: (e) => !e.ctrlKey,
  shift: (e) => !e.shiftKey,
  alt: (e) => !e.altKey,
  meta: (e) => !e.metaKey,
  left: (e) => "button" in e && e.button !== 0,
  middle: (e) => "button" in e && e.button !== 1,
  right: (e) => "button" in e && e.button !== 2,
  exact: (e, modifiers) => systemModifiers.some((m) => e[`${m}Key`] && !modifiers.includes(m))
};
const withModifiers = (fn, modifiers) => {
  const cache = fn._withMods || (fn._withMods = {});
  const cacheKey = modifiers.join(".");
  return cache[cacheKey] || (cache[cacheKey] = ((event, ...args) => {
    for (let i = 0; i < modifiers.length; i++) {
      const guard = modifierGuards[modifiers[i]];
      if (guard && guard(event, modifiers)) return;
    }
    return fn(event, ...args);
  }));
};
const keyNames = {
  esc: "escape",
  space: " ",
  up: "arrow-up",
  left: "arrow-left",
  right: "arrow-right",
  down: "arrow-down",
  delete: "backspace"
};
const withKeys = (fn, modifiers) => {
  const cache = fn._withKeys || (fn._withKeys = {});
  const cacheKey = modifiers.join(".");
  return cache[cacheKey] || (cache[cacheKey] = ((event) => {
    if (!("key" in event)) {
      return;
    }
    const eventKey = hyphenate(event.key);
    if (modifiers.some(
      (k) => k === eventKey || keyNames[k] === eventKey
    )) {
      return fn(event);
    }
  }));
};
const rendererOptions = /* @__PURE__ */ extend({ patchProp }, nodeOps);
let renderer;
let enabledHydration = false;
function ensureRenderer() {
  return renderer || (renderer = createRenderer(rendererOptions));
}
function ensureHydrationRenderer() {
  renderer = enabledHydration ? renderer : createHydrationRenderer(rendererOptions);
  enabledHydration = true;
  return renderer;
}
const render = ((...args) => {
  ensureRenderer().render(...args);
});
const hydrate = ((...args) => {
  ensureHydrationRenderer().hydrate(...args);
});
const createApp = ((...args) => {
  const app = ensureRenderer().createApp(...args);
  const { mount } = app;
  app.mount = (containerOrSelector) => {
    const container = normalizeContainer(containerOrSelector);
    if (!container) return;
    const component = app._component;
    if (!isFunction(component) && !component.render && !component.template) {
      component.template = container.innerHTML;
    }
    if (container.nodeType === 1) {
      container.textContent = "";
    }
    const proxy = mount(container, false, resolveRootNamespace(container));
    if (container instanceof Element) {
      container.removeAttribute("v-cloak");
      container.setAttribute("data-v-app", "");
    }
    return proxy;
  };
  return app;
});
const createSSRApp = ((...args) => {
  const app = ensureHydrationRenderer().createApp(...args);
  const { mount } = app;
  app.mount = (containerOrSelector) => {
    const container = normalizeContainer(containerOrSelector);
    if (container) {
      return mount(container, true, resolveRootNamespace(container));
    }
  };
  return app;
});
function resolveRootNamespace(container) {
  if (container instanceof SVGElement) {
    return "svg";
  }
  if (typeof MathMLElement === "function" && container instanceof MathMLElement) {
    return "mathml";
  }
}
function normalizeContainer(container) {
  if (isString(container)) {
    const res = document.querySelector(container);
    return res;
  }
  return container;
}
let ssrDirectiveInitialized = false;
const initDirectivesForSSR = () => {
  if (!ssrDirectiveInitialized) {
    ssrDirectiveInitialized = true;
    initVModelForSSR();
    initVShowForSSR();
  }
};
const runtimeDom = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  BaseTransition,
  BaseTransitionPropsValidators,
  Comment,
  DeprecationTypes,
  EffectScope,
  ErrorCodes,
  ErrorTypeStrings,
  Fragment,
  KeepAlive,
  ReactiveEffect,
  Static,
  Suspense,
  Teleport,
  Text,
  TrackOpTypes,
  Transition,
  TransitionGroup,
  TriggerOpTypes,
  VueElement,
  assertNumber,
  callWithAsyncErrorHandling,
  callWithErrorHandling,
  camelize,
  capitalize,
  cloneVNode,
  compatUtils,
  computed,
  createApp,
  createBlock,
  createCommentVNode,
  createElementBlock,
  createElementVNode: createBaseVNode,
  createHydrationRenderer,
  createPropsRestProxy,
  createRenderer,
  createSSRApp,
  createSlots,
  createStaticVNode,
  createTextVNode,
  createVNode,
  customRef,
  defineAsyncComponent,
  defineComponent,
  defineCustomElement,
  defineEmits,
  defineExpose,
  defineModel,
  defineOptions,
  defineProps,
  defineSSRCustomElement,
  defineSlots,
  devtools,
  effect,
  effectScope,
  getCurrentInstance,
  getCurrentScope,
  getCurrentWatcher,
  getTransitionRawChildren,
  guardReactiveProps,
  h,
  handleError,
  hasInjectionContext,
  hydrate,
  hydrateOnIdle,
  hydrateOnInteraction,
  hydrateOnMediaQuery,
  hydrateOnVisible,
  initCustomFormatter,
  initDirectivesForSSR,
  inject,
  isMemoSame,
  isProxy,
  isReactive,
  isReadonly,
  isRef,
  isRuntimeOnly,
  isShallow,
  isVNode,
  markRaw,
  mergeDefaults,
  mergeModels,
  mergeProps,
  nextTick,
  nodeOps,
  normalizeClass,
  normalizeProps,
  normalizeStyle,
  onActivated,
  onBeforeMount,
  onBeforeUnmount,
  onBeforeUpdate,
  onDeactivated,
  onErrorCaptured,
  onMounted,
  onRenderTracked,
  onRenderTriggered,
  onScopeDispose,
  onServerPrefetch,
  onUnmounted,
  onUpdated,
  onWatcherCleanup,
  openBlock,
  patchProp,
  popScopeId,
  provide,
  proxyRefs,
  pushScopeId,
  queuePostFlushCb,
  reactive,
  readonly,
  ref,
  registerRuntimeCompiler,
  render,
  renderList,
  renderSlot,
  resolveComponent,
  resolveDirective,
  resolveDynamicComponent,
  resolveFilter,
  resolveTransitionHooks,
  setBlockTracking,
  setDevtoolsHook,
  setTransitionHooks,
  shallowReactive,
  shallowReadonly,
  shallowRef,
  ssrContextKey,
  ssrUtils,
  stop,
  toDisplayString,
  toHandlerKey,
  toHandlers,
  toRaw,
  toRef,
  toRefs,
  toValue,
  transformVNodeArgs,
  triggerRef,
  unref,
  useAttrs,
  useCssModule,
  useCssVars,
  useHost,
  useId,
  useModel,
  useSSRContext,
  useShadowRoot,
  useSlots,
  useTemplateRef,
  useTransitionState,
  vModelCheckbox,
  vModelDynamic,
  vModelRadio,
  vModelSelect,
  vModelText,
  vShow,
  version,
  warn,
  watch,
  watchEffect,
  watchPostEffect,
  watchSyncEffect,
  withAsyncContext,
  withCtx,
  withDefaults,
  withDirectives,
  withKeys,
  withMemo,
  withModifiers,
  withScopeId
}, Symbol.toStringTag, { value: "Module" }));
const FRAGMENT = Symbol(``);
const TELEPORT = Symbol(``);
const SUSPENSE = Symbol(``);
const KEEP_ALIVE = Symbol(``);
const BASE_TRANSITION = Symbol(
  ``
);
const OPEN_BLOCK = Symbol(``);
const CREATE_BLOCK = Symbol(``);
const CREATE_ELEMENT_BLOCK = Symbol(
  ``
);
const CREATE_VNODE = Symbol(``);
const CREATE_ELEMENT_VNODE = Symbol(
  ``
);
const CREATE_COMMENT = Symbol(
  ``
);
const CREATE_TEXT = Symbol(
  ``
);
const CREATE_STATIC = Symbol(
  ``
);
const RESOLVE_COMPONENT = Symbol(
  ``
);
const RESOLVE_DYNAMIC_COMPONENT = Symbol(
  ``
);
const RESOLVE_DIRECTIVE = Symbol(
  ``
);
const RESOLVE_FILTER = Symbol(
  ``
);
const WITH_DIRECTIVES = Symbol(
  ``
);
const RENDER_LIST = Symbol(``);
const RENDER_SLOT = Symbol(``);
const CREATE_SLOTS = Symbol(``);
const TO_DISPLAY_STRING = Symbol(
  ``
);
const MERGE_PROPS = Symbol(``);
const NORMALIZE_CLASS = Symbol(
  ``
);
const NORMALIZE_STYLE = Symbol(
  ``
);
const NORMALIZE_PROPS = Symbol(
  ``
);
const GUARD_REACTIVE_PROPS = Symbol(
  ``
);
const TO_HANDLERS = Symbol(``);
const CAMELIZE = Symbol(``);
const CAPITALIZE = Symbol(``);
const TO_HANDLER_KEY = Symbol(
  ``
);
const SET_BLOCK_TRACKING = Symbol(
  ``
);
const PUSH_SCOPE_ID = Symbol(``);
const POP_SCOPE_ID = Symbol(``);
const WITH_CTX = Symbol(``);
const UNREF = Symbol(``);
const IS_REF = Symbol(``);
const WITH_MEMO = Symbol(``);
const IS_MEMO_SAME = Symbol(``);
const helperNameMap = {
  [FRAGMENT]: `Fragment`,
  [TELEPORT]: `Teleport`,
  [SUSPENSE]: `Suspense`,
  [KEEP_ALIVE]: `KeepAlive`,
  [BASE_TRANSITION]: `BaseTransition`,
  [OPEN_BLOCK]: `openBlock`,
  [CREATE_BLOCK]: `createBlock`,
  [CREATE_ELEMENT_BLOCK]: `createElementBlock`,
  [CREATE_VNODE]: `createVNode`,
  [CREATE_ELEMENT_VNODE]: `createElementVNode`,
  [CREATE_COMMENT]: `createCommentVNode`,
  [CREATE_TEXT]: `createTextVNode`,
  [CREATE_STATIC]: `createStaticVNode`,
  [RESOLVE_COMPONENT]: `resolveComponent`,
  [RESOLVE_DYNAMIC_COMPONENT]: `resolveDynamicComponent`,
  [RESOLVE_DIRECTIVE]: `resolveDirective`,
  [RESOLVE_FILTER]: `resolveFilter`,
  [WITH_DIRECTIVES]: `withDirectives`,
  [RENDER_LIST]: `renderList`,
  [RENDER_SLOT]: `renderSlot`,
  [CREATE_SLOTS]: `createSlots`,
  [TO_DISPLAY_STRING]: `toDisplayString`,
  [MERGE_PROPS]: `mergeProps`,
  [NORMALIZE_CLASS]: `normalizeClass`,
  [NORMALIZE_STYLE]: `normalizeStyle`,
  [NORMALIZE_PROPS]: `normalizeProps`,
  [GUARD_REACTIVE_PROPS]: `guardReactiveProps`,
  [TO_HANDLERS]: `toHandlers`,
  [CAMELIZE]: `camelize`,
  [CAPITALIZE]: `capitalize`,
  [TO_HANDLER_KEY]: `toHandlerKey`,
  [SET_BLOCK_TRACKING]: `setBlockTracking`,
  [PUSH_SCOPE_ID]: `pushScopeId`,
  [POP_SCOPE_ID]: `popScopeId`,
  [WITH_CTX]: `withCtx`,
  [UNREF]: `unref`,
  [IS_REF]: `isRef`,
  [WITH_MEMO]: `withMemo`,
  [IS_MEMO_SAME]: `isMemoSame`
};
function registerRuntimeHelpers(helpers) {
  Object.getOwnPropertySymbols(helpers).forEach((s) => {
    helperNameMap[s] = helpers[s];
  });
}
const locStub = {
  start: { line: 1, column: 1, offset: 0 },
  end: { line: 1, column: 1, offset: 0 },
  source: ""
};
function createRoot(children, source = "") {
  return {
    type: 0,
    source,
    children,
    helpers: /* @__PURE__ */ new Set(),
    components: [],
    directives: [],
    hoists: [],
    imports: [],
    cached: [],
    temps: 0,
    codegenNode: void 0,
    loc: locStub
  };
}
function createVNodeCall(context, tag, props, children, patchFlag, dynamicProps, directives, isBlock = false, disableTracking = false, isComponent2 = false, loc = locStub) {
  if (context) {
    if (isBlock) {
      context.helper(OPEN_BLOCK);
      context.helper(getVNodeBlockHelper(context.inSSR, isComponent2));
    } else {
      context.helper(getVNodeHelper(context.inSSR, isComponent2));
    }
    if (directives) {
      context.helper(WITH_DIRECTIVES);
    }
  }
  return {
    type: 13,
    tag,
    props,
    children,
    patchFlag,
    dynamicProps,
    directives,
    isBlock,
    disableTracking,
    isComponent: isComponent2,
    loc
  };
}
function createArrayExpression(elements, loc = locStub) {
  return {
    type: 17,
    loc,
    elements
  };
}
function createObjectExpression(properties, loc = locStub) {
  return {
    type: 15,
    loc,
    properties
  };
}
function createObjectProperty(key, value) {
  return {
    type: 16,
    loc: locStub,
    key: isString(key) ? createSimpleExpression(key, true) : key,
    value
  };
}
function createSimpleExpression(content, isStatic = false, loc = locStub, constType = 0) {
  return {
    type: 4,
    loc,
    content,
    isStatic,
    constType: isStatic ? 3 : constType
  };
}
function createCompoundExpression(children, loc = locStub) {
  return {
    type: 8,
    loc,
    children
  };
}
function createCallExpression(callee, args = [], loc = locStub) {
  return {
    type: 14,
    loc,
    callee,
    arguments: args
  };
}
function createFunctionExpression(params, returns = void 0, newline = false, isSlot = false, loc = locStub) {
  return {
    type: 18,
    params,
    returns,
    newline,
    isSlot,
    loc
  };
}
function createConditionalExpression(test, consequent, alternate, newline = true) {
  return {
    type: 19,
    test,
    consequent,
    alternate,
    newline,
    loc: locStub
  };
}
function createCacheExpression(index, value, needPauseTracking = false, inVOnce = false) {
  return {
    type: 20,
    index,
    value,
    needPauseTracking,
    inVOnce,
    needArraySpread: false,
    loc: locStub
  };
}
function createBlockStatement(body) {
  return {
    type: 21,
    body,
    loc: locStub
  };
}
function getVNodeHelper(ssr, isComponent2) {
  return ssr || isComponent2 ? CREATE_VNODE : CREATE_ELEMENT_VNODE;
}
function getVNodeBlockHelper(ssr, isComponent2) {
  return ssr || isComponent2 ? CREATE_BLOCK : CREATE_ELEMENT_BLOCK;
}
function convertToBlock(node, { helper, removeHelper, inSSR }) {
  if (!node.isBlock) {
    node.isBlock = true;
    removeHelper(getVNodeHelper(inSSR, node.isComponent));
    helper(OPEN_BLOCK);
    helper(getVNodeBlockHelper(inSSR, node.isComponent));
  }
}
const defaultDelimitersOpen = new Uint8Array([123, 123]);
const defaultDelimitersClose = new Uint8Array([125, 125]);
function isTagStartChar(c) {
  return c >= 97 && c <= 122 || c >= 65 && c <= 90;
}
function isWhitespace(c) {
  return c === 32 || c === 10 || c === 9 || c === 12 || c === 13;
}
function isEndOfTagSection(c) {
  return c === 47 || c === 62 || isWhitespace(c);
}
function toCharCodes(str) {
  const ret = new Uint8Array(str.length);
  for (let i = 0; i < str.length; i++) {
    ret[i] = str.charCodeAt(i);
  }
  return ret;
}
const Sequences = {
  Cdata: new Uint8Array([67, 68, 65, 84, 65, 91]),
  // CDATA[
  CdataEnd: new Uint8Array([93, 93, 62]),
  // ]]>
  CommentEnd: new Uint8Array([45, 45, 62]),
  // `-->`
  ScriptEnd: new Uint8Array([60, 47, 115, 99, 114, 105, 112, 116]),
  // `<\/script`
  StyleEnd: new Uint8Array([60, 47, 115, 116, 121, 108, 101]),
  // `</style`
  TitleEnd: new Uint8Array([60, 47, 116, 105, 116, 108, 101]),
  // `</title`
  TextareaEnd: new Uint8Array([
    60,
    47,
    116,
    101,
    120,
    116,
    97,
    114,
    101,
    97
  ])
  // `</textarea
};
class Tokenizer {
  constructor(stack2, cbs) {
    this.stack = stack2;
    this.cbs = cbs;
    this.state = 1;
    this.buffer = "";
    this.sectionStart = 0;
    this.index = 0;
    this.entityStart = 0;
    this.baseState = 1;
    this.inRCDATA = false;
    this.inXML = false;
    this.inVPre = false;
    this.newlines = [];
    this.mode = 0;
    this.delimiterOpen = defaultDelimitersOpen;
    this.delimiterClose = defaultDelimitersClose;
    this.delimiterIndex = -1;
    this.currentSequence = void 0;
    this.sequenceIndex = 0;
  }
  get inSFCRoot() {
    return this.mode === 2 && this.stack.length === 0;
  }
  reset() {
    this.state = 1;
    this.mode = 0;
    this.buffer = "";
    this.sectionStart = 0;
    this.index = 0;
    this.baseState = 1;
    this.inRCDATA = false;
    this.currentSequence = void 0;
    this.newlines.length = 0;
    this.delimiterOpen = defaultDelimitersOpen;
    this.delimiterClose = defaultDelimitersClose;
  }
  /**
   * Generate Position object with line / column information using recorded
   * newline positions. We know the index is always going to be an already
   * processed index, so all the newlines up to this index should have been
   * recorded.
   */
  getPos(index) {
    let line = 1;
    let column = index + 1;
    for (let i = this.newlines.length - 1; i >= 0; i--) {
      const newlineIndex = this.newlines[i];
      if (index > newlineIndex) {
        line = i + 2;
        column = index - newlineIndex;
        break;
      }
    }
    return {
      column,
      line,
      offset: index
    };
  }
  peek() {
    return this.buffer.charCodeAt(this.index + 1);
  }
  stateText(c) {
    if (c === 60) {
      if (this.index > this.sectionStart) {
        this.cbs.ontext(this.sectionStart, this.index);
      }
      this.state = 5;
      this.sectionStart = this.index;
    } else if (!this.inVPre && c === this.delimiterOpen[0]) {
      this.state = 2;
      this.delimiterIndex = 0;
      this.stateInterpolationOpen(c);
    }
  }
  stateInterpolationOpen(c) {
    if (c === this.delimiterOpen[this.delimiterIndex]) {
      if (this.delimiterIndex === this.delimiterOpen.length - 1) {
        const start = this.index + 1 - this.delimiterOpen.length;
        if (start > this.sectionStart) {
          this.cbs.ontext(this.sectionStart, start);
        }
        this.state = 3;
        this.sectionStart = start;
      } else {
        this.delimiterIndex++;
      }
    } else if (this.inRCDATA) {
      this.state = 32;
      this.stateInRCDATA(c);
    } else {
      this.state = 1;
      this.stateText(c);
    }
  }
  stateInterpolation(c) {
    if (c === this.delimiterClose[0]) {
      this.state = 4;
      this.delimiterIndex = 0;
      this.stateInterpolationClose(c);
    }
  }
  stateInterpolationClose(c) {
    if (c === this.delimiterClose[this.delimiterIndex]) {
      if (this.delimiterIndex === this.delimiterClose.length - 1) {
        this.cbs.oninterpolation(this.sectionStart, this.index + 1);
        if (this.inRCDATA) {
          this.state = 32;
        } else {
          this.state = 1;
        }
        this.sectionStart = this.index + 1;
      } else {
        this.delimiterIndex++;
      }
    } else {
      this.state = 3;
      this.stateInterpolation(c);
    }
  }
  stateSpecialStartSequence(c) {
    const isEnd = this.sequenceIndex === this.currentSequence.length;
    const isMatch = isEnd ? (
      // If we are at the end of the sequence, make sure the tag name has ended
      isEndOfTagSection(c)
    ) : (
      // Otherwise, do a case-insensitive comparison
      (c | 32) === this.currentSequence[this.sequenceIndex]
    );
    if (!isMatch) {
      this.inRCDATA = false;
    } else if (!isEnd) {
      this.sequenceIndex++;
      return;
    }
    this.sequenceIndex = 0;
    this.state = 6;
    this.stateInTagName(c);
  }
  /** Look for an end tag. For <title> and <textarea>, also decode entities. */
  stateInRCDATA(c) {
    if (this.sequenceIndex === this.currentSequence.length) {
      if (c === 62 || isWhitespace(c)) {
        const endOfText = this.index - this.currentSequence.length;
        if (this.sectionStart < endOfText) {
          const actualIndex = this.index;
          this.index = endOfText;
          this.cbs.ontext(this.sectionStart, endOfText);
          this.index = actualIndex;
        }
        this.sectionStart = endOfText + 2;
        this.stateInClosingTagName(c);
        this.inRCDATA = false;
        return;
      }
      this.sequenceIndex = 0;
    }
    if ((c | 32) === this.currentSequence[this.sequenceIndex]) {
      this.sequenceIndex += 1;
    } else if (this.sequenceIndex === 0) {
      if (this.currentSequence === Sequences.TitleEnd || this.currentSequence === Sequences.TextareaEnd && !this.inSFCRoot) {
        if (!this.inVPre && c === this.delimiterOpen[0]) {
          this.state = 2;
          this.delimiterIndex = 0;
          this.stateInterpolationOpen(c);
        }
      } else if (this.fastForwardTo(60)) {
        this.sequenceIndex = 1;
      }
    } else {
      this.sequenceIndex = Number(c === 60);
    }
  }
  stateCDATASequence(c) {
    if (c === Sequences.Cdata[this.sequenceIndex]) {
      if (++this.sequenceIndex === Sequences.Cdata.length) {
        this.state = 28;
        this.currentSequence = Sequences.CdataEnd;
        this.sequenceIndex = 0;
        this.sectionStart = this.index + 1;
      }
    } else {
      this.sequenceIndex = 0;
      this.state = 23;
      this.stateInDeclaration(c);
    }
  }
  /**
   * When we wait for one specific character, we can speed things up
   * by skipping through the buffer until we find it.
   *
   * @returns Whether the character was found.
   */
  fastForwardTo(c) {
    while (++this.index < this.buffer.length) {
      const cc2 = this.buffer.charCodeAt(this.index);
      if (cc2 === 10) {
        this.newlines.push(this.index);
      }
      if (cc2 === c) {
        return true;
      }
    }
    this.index = this.buffer.length - 1;
    return false;
  }
  /**
   * Comments and CDATA end with `-->` and `]]>`.
   *
   * Their common qualities are:
   * - Their end sequences have a distinct character they start with.
   * - That character is then repeated, so we have to check multiple repeats.
   * - All characters but the start character of the sequence can be skipped.
   */
  stateInCommentLike(c) {
    if (c === this.currentSequence[this.sequenceIndex]) {
      if (++this.sequenceIndex === this.currentSequence.length) {
        if (this.currentSequence === Sequences.CdataEnd) {
          this.cbs.oncdata(this.sectionStart, this.index - 2);
        } else {
          this.cbs.oncomment(this.sectionStart, this.index - 2);
        }
        this.sequenceIndex = 0;
        this.sectionStart = this.index + 1;
        this.state = 1;
      }
    } else if (this.sequenceIndex === 0) {
      if (this.fastForwardTo(this.currentSequence[0])) {
        this.sequenceIndex = 1;
      }
    } else if (c !== this.currentSequence[this.sequenceIndex - 1]) {
      this.sequenceIndex = 0;
    }
  }
  startSpecial(sequence, offset) {
    this.enterRCDATA(sequence, offset);
    this.state = 31;
  }
  enterRCDATA(sequence, offset) {
    this.inRCDATA = true;
    this.currentSequence = sequence;
    this.sequenceIndex = offset;
  }
  stateBeforeTagName(c) {
    if (c === 33) {
      this.state = 22;
      this.sectionStart = this.index + 1;
    } else if (c === 63) {
      this.state = 24;
      this.sectionStart = this.index + 1;
    } else if (isTagStartChar(c)) {
      this.sectionStart = this.index;
      if (this.mode === 0) {
        this.state = 6;
      } else if (this.inSFCRoot) {
        this.state = 34;
      } else if (!this.inXML) {
        if (c === 116) {
          this.state = 30;
        } else {
          this.state = c === 115 ? 29 : 6;
        }
      } else {
        this.state = 6;
      }
    } else if (c === 47) {
      this.state = 8;
    } else {
      this.state = 1;
      this.stateText(c);
    }
  }
  stateInTagName(c) {
    if (isEndOfTagSection(c)) {
      this.handleTagName(c);
    }
  }
  stateInSFCRootTagName(c) {
    if (isEndOfTagSection(c)) {
      const tag = this.buffer.slice(this.sectionStart, this.index);
      if (tag !== "template") {
        this.enterRCDATA(toCharCodes(`</` + tag), 0);
      }
      this.handleTagName(c);
    }
  }
  handleTagName(c) {
    this.cbs.onopentagname(this.sectionStart, this.index);
    this.sectionStart = -1;
    this.state = 11;
    this.stateBeforeAttrName(c);
  }
  stateBeforeClosingTagName(c) {
    if (isWhitespace(c)) ;
    else if (c === 62) {
      this.state = 1;
      this.sectionStart = this.index + 1;
    } else {
      this.state = isTagStartChar(c) ? 9 : 27;
      this.sectionStart = this.index;
    }
  }
  stateInClosingTagName(c) {
    if (c === 62 || isWhitespace(c)) {
      this.cbs.onclosetag(this.sectionStart, this.index);
      this.sectionStart = -1;
      this.state = 10;
      this.stateAfterClosingTagName(c);
    }
  }
  stateAfterClosingTagName(c) {
    if (c === 62) {
      this.state = 1;
      this.sectionStart = this.index + 1;
    }
  }
  stateBeforeAttrName(c) {
    if (c === 62) {
      this.cbs.onopentagend(this.index);
      if (this.inRCDATA) {
        this.state = 32;
      } else {
        this.state = 1;
      }
      this.sectionStart = this.index + 1;
    } else if (c === 47) {
      this.state = 7;
    } else if (c === 60 && this.peek() === 47) {
      this.cbs.onopentagend(this.index);
      this.state = 5;
      this.sectionStart = this.index;
    } else if (!isWhitespace(c)) {
      this.handleAttrStart(c);
    }
  }
  handleAttrStart(c) {
    if (c === 118 && this.peek() === 45) {
      this.state = 13;
      this.sectionStart = this.index;
    } else if (c === 46 || c === 58 || c === 64 || c === 35) {
      this.cbs.ondirname(this.index, this.index + 1);
      this.state = 14;
      this.sectionStart = this.index + 1;
    } else {
      this.state = 12;
      this.sectionStart = this.index;
    }
  }
  stateInSelfClosingTag(c) {
    if (c === 62) {
      this.cbs.onselfclosingtag(this.index);
      this.state = 1;
      this.sectionStart = this.index + 1;
      this.inRCDATA = false;
    } else if (!isWhitespace(c)) {
      this.state = 11;
      this.stateBeforeAttrName(c);
    }
  }
  stateInAttrName(c) {
    if (c === 61 || isEndOfTagSection(c)) {
      this.cbs.onattribname(this.sectionStart, this.index);
      this.handleAttrNameEnd(c);
    }
  }
  stateInDirName(c) {
    if (c === 61 || isEndOfTagSection(c)) {
      this.cbs.ondirname(this.sectionStart, this.index);
      this.handleAttrNameEnd(c);
    } else if (c === 58) {
      this.cbs.ondirname(this.sectionStart, this.index);
      this.state = 14;
      this.sectionStart = this.index + 1;
    } else if (c === 46) {
      this.cbs.ondirname(this.sectionStart, this.index);
      this.state = 16;
      this.sectionStart = this.index + 1;
    }
  }
  stateInDirArg(c) {
    if (c === 61 || isEndOfTagSection(c)) {
      this.cbs.ondirarg(this.sectionStart, this.index);
      this.handleAttrNameEnd(c);
    } else if (c === 91) {
      this.state = 15;
    } else if (c === 46) {
      this.cbs.ondirarg(this.sectionStart, this.index);
      this.state = 16;
      this.sectionStart = this.index + 1;
    }
  }
  stateInDynamicDirArg(c) {
    if (c === 93) {
      this.state = 14;
    } else if (c === 61 || isEndOfTagSection(c)) {
      this.cbs.ondirarg(this.sectionStart, this.index + 1);
      this.handleAttrNameEnd(c);
    }
  }
  stateInDirModifier(c) {
    if (c === 61 || isEndOfTagSection(c)) {
      this.cbs.ondirmodifier(this.sectionStart, this.index);
      this.handleAttrNameEnd(c);
    } else if (c === 46) {
      this.cbs.ondirmodifier(this.sectionStart, this.index);
      this.sectionStart = this.index + 1;
    }
  }
  handleAttrNameEnd(c) {
    this.sectionStart = this.index;
    this.state = 17;
    this.cbs.onattribnameend(this.index);
    this.stateAfterAttrName(c);
  }
  stateAfterAttrName(c) {
    if (c === 61) {
      this.state = 18;
    } else if (c === 47 || c === 62) {
      this.cbs.onattribend(0, this.sectionStart);
      this.sectionStart = -1;
      this.state = 11;
      this.stateBeforeAttrName(c);
    } else if (!isWhitespace(c)) {
      this.cbs.onattribend(0, this.sectionStart);
      this.handleAttrStart(c);
    }
  }
  stateBeforeAttrValue(c) {
    if (c === 34) {
      this.state = 19;
      this.sectionStart = this.index + 1;
    } else if (c === 39) {
      this.state = 20;
      this.sectionStart = this.index + 1;
    } else if (!isWhitespace(c)) {
      this.sectionStart = this.index;
      this.state = 21;
      this.stateInAttrValueNoQuotes(c);
    }
  }
  handleInAttrValue(c, quote) {
    if (c === quote || this.fastForwardTo(quote)) {
      this.cbs.onattribdata(this.sectionStart, this.index);
      this.sectionStart = -1;
      this.cbs.onattribend(
        quote === 34 ? 3 : 2,
        this.index + 1
      );
      this.state = 11;
    }
  }
  stateInAttrValueDoubleQuotes(c) {
    this.handleInAttrValue(c, 34);
  }
  stateInAttrValueSingleQuotes(c) {
    this.handleInAttrValue(c, 39);
  }
  stateInAttrValueNoQuotes(c) {
    if (isWhitespace(c) || c === 62) {
      this.cbs.onattribdata(this.sectionStart, this.index);
      this.sectionStart = -1;
      this.cbs.onattribend(1, this.index);
      this.state = 11;
      this.stateBeforeAttrName(c);
    } else if (c === 39 || c === 60 || c === 61 || c === 96) {
      this.cbs.onerr(
        18,
        this.index
      );
    } else ;
  }
  stateBeforeDeclaration(c) {
    if (c === 91) {
      this.state = 26;
      this.sequenceIndex = 0;
    } else {
      this.state = c === 45 ? 25 : 23;
    }
  }
  stateInDeclaration(c) {
    if (c === 62 || this.fastForwardTo(62)) {
      this.state = 1;
      this.sectionStart = this.index + 1;
    }
  }
  stateInProcessingInstruction(c) {
    if (c === 62 || this.fastForwardTo(62)) {
      this.cbs.onprocessinginstruction(this.sectionStart, this.index);
      this.state = 1;
      this.sectionStart = this.index + 1;
    }
  }
  stateBeforeComment(c) {
    if (c === 45) {
      this.state = 28;
      this.currentSequence = Sequences.CommentEnd;
      this.sequenceIndex = 2;
      this.sectionStart = this.index + 1;
    } else {
      this.state = 23;
    }
  }
  stateInSpecialComment(c) {
    if (c === 62 || this.fastForwardTo(62)) {
      this.cbs.oncomment(this.sectionStart, this.index);
      this.state = 1;
      this.sectionStart = this.index + 1;
    }
  }
  stateBeforeSpecialS(c) {
    if (c === Sequences.ScriptEnd[3]) {
      this.startSpecial(Sequences.ScriptEnd, 4);
    } else if (c === Sequences.StyleEnd[3]) {
      this.startSpecial(Sequences.StyleEnd, 4);
    } else {
      this.state = 6;
      this.stateInTagName(c);
    }
  }
  stateBeforeSpecialT(c) {
    if (c === Sequences.TitleEnd[3]) {
      this.startSpecial(Sequences.TitleEnd, 4);
    } else if (c === Sequences.TextareaEnd[3]) {
      this.startSpecial(Sequences.TextareaEnd, 4);
    } else {
      this.state = 6;
      this.stateInTagName(c);
    }
  }
  startEntity() {
  }
  stateInEntity() {
  }
  /**
   * Iterates through the buffer, calling the function corresponding to the current state.
   *
   * States that are more likely to be hit are higher up, as a performance improvement.
   */
  parse(input) {
    this.buffer = input;
    while (this.index < this.buffer.length) {
      const c = this.buffer.charCodeAt(this.index);
      if (c === 10 && this.state !== 33) {
        this.newlines.push(this.index);
      }
      switch (this.state) {
        case 1: {
          this.stateText(c);
          break;
        }
        case 2: {
          this.stateInterpolationOpen(c);
          break;
        }
        case 3: {
          this.stateInterpolation(c);
          break;
        }
        case 4: {
          this.stateInterpolationClose(c);
          break;
        }
        case 31: {
          this.stateSpecialStartSequence(c);
          break;
        }
        case 32: {
          this.stateInRCDATA(c);
          break;
        }
        case 26: {
          this.stateCDATASequence(c);
          break;
        }
        case 19: {
          this.stateInAttrValueDoubleQuotes(c);
          break;
        }
        case 12: {
          this.stateInAttrName(c);
          break;
        }
        case 13: {
          this.stateInDirName(c);
          break;
        }
        case 14: {
          this.stateInDirArg(c);
          break;
        }
        case 15: {
          this.stateInDynamicDirArg(c);
          break;
        }
        case 16: {
          this.stateInDirModifier(c);
          break;
        }
        case 28: {
          this.stateInCommentLike(c);
          break;
        }
        case 27: {
          this.stateInSpecialComment(c);
          break;
        }
        case 11: {
          this.stateBeforeAttrName(c);
          break;
        }
        case 6: {
          this.stateInTagName(c);
          break;
        }
        case 34: {
          this.stateInSFCRootTagName(c);
          break;
        }
        case 9: {
          this.stateInClosingTagName(c);
          break;
        }
        case 5: {
          this.stateBeforeTagName(c);
          break;
        }
        case 17: {
          this.stateAfterAttrName(c);
          break;
        }
        case 20: {
          this.stateInAttrValueSingleQuotes(c);
          break;
        }
        case 18: {
          this.stateBeforeAttrValue(c);
          break;
        }
        case 8: {
          this.stateBeforeClosingTagName(c);
          break;
        }
        case 10: {
          this.stateAfterClosingTagName(c);
          break;
        }
        case 29: {
          this.stateBeforeSpecialS(c);
          break;
        }
        case 30: {
          this.stateBeforeSpecialT(c);
          break;
        }
        case 21: {
          this.stateInAttrValueNoQuotes(c);
          break;
        }
        case 7: {
          this.stateInSelfClosingTag(c);
          break;
        }
        case 23: {
          this.stateInDeclaration(c);
          break;
        }
        case 22: {
          this.stateBeforeDeclaration(c);
          break;
        }
        case 25: {
          this.stateBeforeComment(c);
          break;
        }
        case 24: {
          this.stateInProcessingInstruction(c);
          break;
        }
        case 33: {
          this.stateInEntity();
          break;
        }
      }
      this.index++;
    }
    this.cleanup();
    this.finish();
  }
  /**
   * Remove data that has already been consumed from the buffer.
   */
  cleanup() {
    if (this.sectionStart !== this.index) {
      if (this.state === 1 || this.state === 32 && this.sequenceIndex === 0) {
        this.cbs.ontext(this.sectionStart, this.index);
        this.sectionStart = this.index;
      } else if (this.state === 19 || this.state === 20 || this.state === 21) {
        this.cbs.onattribdata(this.sectionStart, this.index);
        this.sectionStart = this.index;
      }
    }
  }
  finish() {
    this.handleTrailingData();
    this.cbs.onend();
  }
  /** Handle any trailing data. */
  handleTrailingData() {
    const endIndex = this.buffer.length;
    if (this.sectionStart >= endIndex) {
      return;
    }
    if (this.state === 28) {
      if (this.currentSequence === Sequences.CdataEnd) {
        this.cbs.oncdata(this.sectionStart, endIndex);
      } else {
        this.cbs.oncomment(this.sectionStart, endIndex);
      }
    } else if (this.state === 6 || this.state === 11 || this.state === 18 || this.state === 17 || this.state === 12 || this.state === 13 || this.state === 14 || this.state === 15 || this.state === 16 || this.state === 20 || this.state === 19 || this.state === 21 || this.state === 9) ;
    else {
      this.cbs.ontext(this.sectionStart, endIndex);
    }
  }
  emitCodePoint(cp, consumed) {
  }
}
function getCompatValue(key, { compatConfig }) {
  const value = compatConfig && compatConfig[key];
  if (key === "MODE") {
    return value || 3;
  } else {
    return value;
  }
}
function isCompatEnabled(key, context) {
  const mode = getCompatValue("MODE", context);
  const value = getCompatValue(key, context);
  return mode === 3 ? value === true : value !== false;
}
function checkCompatEnabled(key, context, loc, ...args) {
  const enabled = isCompatEnabled(key, context);
  return enabled;
}
function defaultOnError(error) {
  throw error;
}
function defaultOnWarn(msg) {
}
function createCompilerError(code, loc, messages, additionalMessage) {
  const msg = `https://vuejs.org/error-reference/#compiler-${code}`;
  const error = new SyntaxError(String(msg));
  error.code = code;
  error.loc = loc;
  return error;
}
const isStaticExp = (p2) => p2.type === 4 && p2.isStatic;
function isCoreComponent(tag) {
  switch (tag) {
    case "Teleport":
    case "teleport":
      return TELEPORT;
    case "Suspense":
    case "suspense":
      return SUSPENSE;
    case "KeepAlive":
    case "keep-alive":
      return KEEP_ALIVE;
    case "BaseTransition":
    case "base-transition":
      return BASE_TRANSITION;
  }
}
const nonIdentifierRE = /^$|^\d|[^\$\w\xA0-\uFFFF]/;
const isSimpleIdentifier = (name2) => !nonIdentifierRE.test(name2);
const validFirstIdentCharRE = /[A-Za-z_$\xA0-\uFFFF]/;
const validIdentCharRE = /[\.\?\w$\xA0-\uFFFF]/;
const whitespaceRE = /\s+[.[]\s*|\s*[.[]\s+/g;
const getExpSource = (exp) => exp.type === 4 ? exp.content : exp.loc.source;
const isMemberExpressionBrowser = (exp) => {
  const path = getExpSource(exp).trim().replace(whitespaceRE, (s) => s.trim());
  let state = 0;
  let stateStack = [];
  let currentOpenBracketCount = 0;
  let currentOpenParensCount = 0;
  let currentStringType = null;
  for (let i = 0; i < path.length; i++) {
    const char = path.charAt(i);
    switch (state) {
      case 0:
        if (char === "[") {
          stateStack.push(state);
          state = 1;
          currentOpenBracketCount++;
        } else if (char === "(") {
          stateStack.push(state);
          state = 2;
          currentOpenParensCount++;
        } else if (!(i === 0 ? validFirstIdentCharRE : validIdentCharRE).test(char)) {
          return false;
        }
        break;
      case 1:
        if (char === `'` || char === `"` || char === "`") {
          stateStack.push(state);
          state = 3;
          currentStringType = char;
        } else if (char === `[`) {
          currentOpenBracketCount++;
        } else if (char === `]`) {
          if (!--currentOpenBracketCount) {
            state = stateStack.pop();
          }
        }
        break;
      case 2:
        if (char === `'` || char === `"` || char === "`") {
          stateStack.push(state);
          state = 3;
          currentStringType = char;
        } else if (char === `(`) {
          currentOpenParensCount++;
        } else if (char === `)`) {
          if (i === path.length - 1) {
            return false;
          }
          if (!--currentOpenParensCount) {
            state = stateStack.pop();
          }
        }
        break;
      case 3:
        if (char === currentStringType) {
          state = stateStack.pop();
          currentStringType = null;
        }
        break;
    }
  }
  return !currentOpenBracketCount && !currentOpenParensCount;
};
const isMemberExpression = isMemberExpressionBrowser;
const fnExpRE = /^\s*(?:async\s*)?(?:\([^)]*?\)|[\w$_]+)\s*(?::[^=]+)?=>|^\s*(?:async\s+)?function(?:\s+[\w$]+)?\s*\(/;
const isFnExpressionBrowser = (exp) => fnExpRE.test(getExpSource(exp));
const isFnExpression = isFnExpressionBrowser;
function findDir(node, name2, allowEmpty = false) {
  for (let i = 0; i < node.props.length; i++) {
    const p2 = node.props[i];
    if (p2.type === 7 && (allowEmpty || p2.exp) && (isString(name2) ? p2.name === name2 : name2.test(p2.name))) {
      return p2;
    }
  }
}
function findProp(node, name2, dynamicOnly = false, allowEmpty = false) {
  for (let i = 0; i < node.props.length; i++) {
    const p2 = node.props[i];
    if (p2.type === 6) {
      if (dynamicOnly) continue;
      if (p2.name === name2 && (p2.value || allowEmpty)) {
        return p2;
      }
    } else if (p2.name === "bind" && (p2.exp || allowEmpty) && isStaticArgOf(p2.arg, name2)) {
      return p2;
    }
  }
}
function isStaticArgOf(arg, name2) {
  return !!(arg && isStaticExp(arg) && arg.content === name2);
}
function hasDynamicKeyVBind(node) {
  return node.props.some(
    (p2) => p2.type === 7 && p2.name === "bind" && (!p2.arg || // v-bind="obj"
    p2.arg.type !== 4 || // v-bind:[_ctx.foo]
    !p2.arg.isStatic)
    // v-bind:[foo]
  );
}
function isText$1(node) {
  return node.type === 5 || node.type === 2;
}
function isVPre(p2) {
  return p2.type === 7 && p2.name === "pre";
}
function isVSlot(p2) {
  return p2.type === 7 && p2.name === "slot";
}
function isTemplateNode(node) {
  return node.type === 1 && node.tagType === 3;
}
function isSlotOutlet(node) {
  return node.type === 1 && node.tagType === 2;
}
const propsHelperSet = /* @__PURE__ */ new Set([NORMALIZE_PROPS, GUARD_REACTIVE_PROPS]);
function getUnnormalizedProps(props, callPath = []) {
  if (props && !isString(props) && props.type === 14) {
    const callee = props.callee;
    if (!isString(callee) && propsHelperSet.has(callee)) {
      return getUnnormalizedProps(
        props.arguments[0],
        callPath.concat(props)
      );
    }
  }
  return [props, callPath];
}
function injectProp(node, prop, context) {
  let propsWithInjection;
  let props = node.type === 13 ? node.props : node.arguments[2];
  let callPath = [];
  let parentCall;
  if (props && !isString(props) && props.type === 14) {
    const ret = getUnnormalizedProps(props);
    props = ret[0];
    callPath = ret[1];
    parentCall = callPath[callPath.length - 1];
  }
  if (props == null || isString(props)) {
    propsWithInjection = createObjectExpression([prop]);
  } else if (props.type === 14) {
    const first = props.arguments[0];
    if (!isString(first) && first.type === 15) {
      if (!hasProp(prop, first)) {
        first.properties.unshift(prop);
      }
    } else {
      if (props.callee === TO_HANDLERS) {
        propsWithInjection = createCallExpression(context.helper(MERGE_PROPS), [
          createObjectExpression([prop]),
          props
        ]);
      } else {
        props.arguments.unshift(createObjectExpression([prop]));
      }
    }
    !propsWithInjection && (propsWithInjection = props);
  } else if (props.type === 15) {
    if (!hasProp(prop, props)) {
      props.properties.unshift(prop);
    }
    propsWithInjection = props;
  } else {
    propsWithInjection = createCallExpression(context.helper(MERGE_PROPS), [
      createObjectExpression([prop]),
      props
    ]);
    if (parentCall && parentCall.callee === GUARD_REACTIVE_PROPS) {
      parentCall = callPath[callPath.length - 2];
    }
  }
  if (node.type === 13) {
    if (parentCall) {
      parentCall.arguments[0] = propsWithInjection;
    } else {
      node.props = propsWithInjection;
    }
  } else {
    if (parentCall) {
      parentCall.arguments[0] = propsWithInjection;
    } else {
      node.arguments[2] = propsWithInjection;
    }
  }
}
function hasProp(prop, props) {
  let result = false;
  if (prop.key.type === 4) {
    const propKeyName = prop.key.content;
    result = props.properties.some(
      (p2) => p2.key.type === 4 && p2.key.content === propKeyName
    );
  }
  return result;
}
function toValidAssetId(name2, type) {
  return `_${type}_${name2.replace(/[^\w]/g, (searchValue, replaceValue) => {
    return searchValue === "-" ? "_" : name2.charCodeAt(replaceValue).toString();
  })}`;
}
function getMemoedVNodeCall(node) {
  if (node.type === 14 && node.callee === WITH_MEMO) {
    return node.arguments[1].returns;
  } else {
    return node;
  }
}
const forAliasRE = /([\s\S]*?)\s+(?:in|of)\s+(\S[\s\S]*)/;
function isAllWhitespace(str) {
  for (let i = 0; i < str.length; i++) {
    if (!isWhitespace(str.charCodeAt(i))) {
      return false;
    }
  }
  return true;
}
function isWhitespaceText(node) {
  return node.type === 2 && isAllWhitespace(node.content) || node.type === 12 && isWhitespaceText(node.content);
}
function isCommentOrWhitespace(node) {
  return node.type === 3 || isWhitespaceText(node);
}
const defaultParserOptions = {
  parseMode: "base",
  ns: 0,
  delimiters: [`{{`, `}}`],
  getNamespace: () => 0,
  isVoidTag: NO,
  isPreTag: NO,
  isIgnoreNewlineTag: NO,
  isCustomElement: NO,
  onError: defaultOnError,
  onWarn: defaultOnWarn,
  comments: false,
  prefixIdentifiers: false
};
let currentOptions = defaultParserOptions;
let currentRoot = null;
let currentInput = "";
let currentOpenTag = null;
let currentProp = null;
let currentAttrValue = "";
let currentAttrStartIndex = -1;
let currentAttrEndIndex = -1;
let inPre = 0;
let inVPre = false;
let currentVPreBoundary = null;
const stack = [];
const tokenizer = new Tokenizer(stack, {
  onerr: emitError,
  ontext(start, end) {
    onText(getSlice(start, end), start, end);
  },
  ontextentity(char, start, end) {
    onText(char, start, end);
  },
  oninterpolation(start, end) {
    if (inVPre) {
      return onText(getSlice(start, end), start, end);
    }
    let innerStart = start + tokenizer.delimiterOpen.length;
    let innerEnd = end - tokenizer.delimiterClose.length;
    while (isWhitespace(currentInput.charCodeAt(innerStart))) {
      innerStart++;
    }
    while (isWhitespace(currentInput.charCodeAt(innerEnd - 1))) {
      innerEnd--;
    }
    let exp = getSlice(innerStart, innerEnd);
    if (exp.includes("&")) {
      {
        exp = currentOptions.decodeEntities(exp, false);
      }
    }
    addNode({
      type: 5,
      content: createExp(exp, false, getLoc(innerStart, innerEnd)),
      loc: getLoc(start, end)
    });
  },
  onopentagname(start, end) {
    const name2 = getSlice(start, end);
    currentOpenTag = {
      type: 1,
      tag: name2,
      ns: currentOptions.getNamespace(name2, stack[0], currentOptions.ns),
      tagType: 0,
      // will be refined on tag close
      props: [],
      children: [],
      loc: getLoc(start - 1, end),
      codegenNode: void 0
    };
  },
  onopentagend(end) {
    endOpenTag(end);
  },
  onclosetag(start, end) {
    const name2 = getSlice(start, end);
    if (!currentOptions.isVoidTag(name2)) {
      let found = false;
      for (let i = 0; i < stack.length; i++) {
        const e = stack[i];
        if (e.tag.toLowerCase() === name2.toLowerCase()) {
          found = true;
          if (i > 0) {
            emitError(24, stack[0].loc.start.offset);
          }
          for (let j = 0; j <= i; j++) {
            const el2 = stack.shift();
            onCloseTag(el2, end, j < i);
          }
          break;
        }
      }
      if (!found) {
        emitError(23, backTrack(start, 60));
      }
    }
  },
  onselfclosingtag(end) {
    const name2 = currentOpenTag.tag;
    currentOpenTag.isSelfClosing = true;
    endOpenTag(end);
    if (stack[0] && stack[0].tag === name2) {
      onCloseTag(stack.shift(), end);
    }
  },
  onattribname(start, end) {
    currentProp = {
      type: 6,
      name: getSlice(start, end),
      nameLoc: getLoc(start, end),
      value: void 0,
      loc: getLoc(start)
    };
  },
  ondirname(start, end) {
    const raw = getSlice(start, end);
    const name2 = raw === "." || raw === ":" ? "bind" : raw === "@" ? "on" : raw === "#" ? "slot" : raw.slice(2);
    if (!inVPre && name2 === "") {
      emitError(26, start);
    }
    if (inVPre || name2 === "") {
      currentProp = {
        type: 6,
        name: raw,
        nameLoc: getLoc(start, end),
        value: void 0,
        loc: getLoc(start)
      };
    } else {
      currentProp = {
        type: 7,
        name: name2,
        rawName: raw,
        exp: void 0,
        arg: void 0,
        modifiers: raw === "." ? [createSimpleExpression("prop")] : [],
        loc: getLoc(start)
      };
      if (name2 === "pre") {
        inVPre = tokenizer.inVPre = true;
        currentVPreBoundary = currentOpenTag;
        const props = currentOpenTag.props;
        for (let i = 0; i < props.length; i++) {
          if (props[i].type === 7) {
            props[i] = dirToAttr(props[i]);
          }
        }
      }
    }
  },
  ondirarg(start, end) {
    if (start === end) return;
    const arg = getSlice(start, end);
    if (inVPre && !isVPre(currentProp)) {
      currentProp.name += arg;
      setLocEnd(currentProp.nameLoc, end);
    } else {
      const isStatic = arg[0] !== `[`;
      currentProp.arg = createExp(
        isStatic ? arg : arg.slice(1, -1),
        isStatic,
        getLoc(start, end),
        isStatic ? 3 : 0
      );
    }
  },
  ondirmodifier(start, end) {
    const mod = getSlice(start, end);
    if (inVPre && !isVPre(currentProp)) {
      currentProp.name += "." + mod;
      setLocEnd(currentProp.nameLoc, end);
    } else if (currentProp.name === "slot") {
      const arg = currentProp.arg;
      if (arg) {
        arg.content += "." + mod;
        setLocEnd(arg.loc, end);
      }
    } else {
      const exp = createSimpleExpression(mod, true, getLoc(start, end));
      currentProp.modifiers.push(exp);
    }
  },
  onattribdata(start, end) {
    currentAttrValue += getSlice(start, end);
    if (currentAttrStartIndex < 0) currentAttrStartIndex = start;
    currentAttrEndIndex = end;
  },
  onattribentity(char, start, end) {
    currentAttrValue += char;
    if (currentAttrStartIndex < 0) currentAttrStartIndex = start;
    currentAttrEndIndex = end;
  },
  onattribnameend(end) {
    const start = currentProp.loc.start.offset;
    const name2 = getSlice(start, end);
    if (currentProp.type === 7) {
      currentProp.rawName = name2;
    }
    if (currentOpenTag.props.some(
      (p2) => (p2.type === 7 ? p2.rawName : p2.name) === name2
    )) {
      emitError(2, start);
    }
  },
  onattribend(quote, end) {
    if (currentOpenTag && currentProp) {
      setLocEnd(currentProp.loc, end);
      if (quote !== 0) {
        if (currentAttrValue.includes("&")) {
          currentAttrValue = currentOptions.decodeEntities(
            currentAttrValue,
            true
          );
        }
        if (currentProp.type === 6) {
          if (currentProp.name === "class") {
            currentAttrValue = condense(currentAttrValue).trim();
          }
          if (quote === 1 && !currentAttrValue) {
            emitError(13, end);
          }
          currentProp.value = {
            type: 2,
            content: currentAttrValue,
            loc: quote === 1 ? getLoc(currentAttrStartIndex, currentAttrEndIndex) : getLoc(currentAttrStartIndex - 1, currentAttrEndIndex + 1)
          };
          if (tokenizer.inSFCRoot && currentOpenTag.tag === "template" && currentProp.name === "lang" && currentAttrValue && currentAttrValue !== "html") {
            tokenizer.enterRCDATA(toCharCodes(`</template`), 0);
          }
        } else {
          let expParseMode = 0;
          currentProp.exp = createExp(
            currentAttrValue,
            false,
            getLoc(currentAttrStartIndex, currentAttrEndIndex),
            0,
            expParseMode
          );
          if (currentProp.name === "for") {
            currentProp.forParseResult = parseForExpression(currentProp.exp);
          }
          let syncIndex = -1;
          if (currentProp.name === "bind" && (syncIndex = currentProp.modifiers.findIndex(
            (mod) => mod.content === "sync"
          )) > -1 && checkCompatEnabled(
            "COMPILER_V_BIND_SYNC",
            currentOptions,
            currentProp.loc,
            currentProp.arg.loc.source
          )) {
            currentProp.name = "model";
            currentProp.modifiers.splice(syncIndex, 1);
          }
        }
      }
      if (currentProp.type !== 7 || currentProp.name !== "pre") {
        currentOpenTag.props.push(currentProp);
      }
    }
    currentAttrValue = "";
    currentAttrStartIndex = currentAttrEndIndex = -1;
  },
  oncomment(start, end) {
    if (currentOptions.comments) {
      addNode({
        type: 3,
        content: getSlice(start, end),
        loc: getLoc(start - 4, end + 3)
      });
    }
  },
  onend() {
    const end = currentInput.length;
    for (let index = 0; index < stack.length; index++) {
      onCloseTag(stack[index], end - 1);
      emitError(24, stack[index].loc.start.offset);
    }
  },
  oncdata(start, end) {
    if (stack[0].ns !== 0) {
      onText(getSlice(start, end), start, end);
    } else {
      emitError(1, start - 9);
    }
  },
  onprocessinginstruction(start) {
    if ((stack[0] ? stack[0].ns : currentOptions.ns) === 0) {
      emitError(
        21,
        start - 1
      );
    }
  }
});
const forIteratorRE = /,([^,\}\]]*)(?:,([^,\}\]]*))?$/;
const stripParensRE = /^\(|\)$/g;
function parseForExpression(input) {
  const loc = input.loc;
  const exp = input.content;
  const inMatch = exp.match(forAliasRE);
  if (!inMatch) return;
  const [, LHS, RHS] = inMatch;
  const createAliasExpression = (content, offset, asParam = false) => {
    const start = loc.start.offset + offset;
    const end = start + content.length;
    return createExp(
      content,
      false,
      getLoc(start, end),
      0,
      asParam ? 1 : 0
      /* Normal */
    );
  };
  const result = {
    source: createAliasExpression(RHS.trim(), exp.indexOf(RHS, LHS.length)),
    value: void 0,
    key: void 0,
    index: void 0,
    finalized: false
  };
  let valueContent = LHS.trim().replace(stripParensRE, "").trim();
  const trimmedOffset = LHS.indexOf(valueContent);
  const iteratorMatch = valueContent.match(forIteratorRE);
  if (iteratorMatch) {
    valueContent = valueContent.replace(forIteratorRE, "").trim();
    const keyContent = iteratorMatch[1].trim();
    let keyOffset;
    if (keyContent) {
      keyOffset = exp.indexOf(keyContent, trimmedOffset + valueContent.length);
      result.key = createAliasExpression(keyContent, keyOffset, true);
    }
    if (iteratorMatch[2]) {
      const indexContent = iteratorMatch[2].trim();
      if (indexContent) {
        result.index = createAliasExpression(
          indexContent,
          exp.indexOf(
            indexContent,
            result.key ? keyOffset + keyContent.length : trimmedOffset + valueContent.length
          ),
          true
        );
      }
    }
  }
  if (valueContent) {
    result.value = createAliasExpression(valueContent, trimmedOffset, true);
  }
  return result;
}
function getSlice(start, end) {
  return currentInput.slice(start, end);
}
function endOpenTag(end) {
  if (tokenizer.inSFCRoot) {
    currentOpenTag.innerLoc = getLoc(end + 1, end + 1);
  }
  addNode(currentOpenTag);
  const { tag, ns } = currentOpenTag;
  if (ns === 0 && currentOptions.isPreTag(tag)) {
    inPre++;
  }
  if (currentOptions.isVoidTag(tag)) {
    onCloseTag(currentOpenTag, end);
  } else {
    stack.unshift(currentOpenTag);
    if (ns === 1 || ns === 2) {
      tokenizer.inXML = true;
    }
  }
  currentOpenTag = null;
}
function onText(content, start, end) {
  {
    const tag = stack[0] && stack[0].tag;
    if (tag !== "script" && tag !== "style" && content.includes("&")) {
      content = currentOptions.decodeEntities(content, false);
    }
  }
  const parent = stack[0] || currentRoot;
  const lastNode = parent.children[parent.children.length - 1];
  if (lastNode && lastNode.type === 2) {
    lastNode.content += content;
    setLocEnd(lastNode.loc, end);
  } else {
    parent.children.push({
      type: 2,
      content,
      loc: getLoc(start, end)
    });
  }
}
function onCloseTag(el2, end, isImplied = false) {
  if (isImplied) {
    setLocEnd(el2.loc, backTrack(end, 60));
  } else {
    setLocEnd(el2.loc, lookAhead(end, 62) + 1);
  }
  if (tokenizer.inSFCRoot) {
    if (el2.children.length) {
      el2.innerLoc.end = extend({}, el2.children[el2.children.length - 1].loc.end);
    } else {
      el2.innerLoc.end = extend({}, el2.innerLoc.start);
    }
    el2.innerLoc.source = getSlice(
      el2.innerLoc.start.offset,
      el2.innerLoc.end.offset
    );
  }
  const { tag, ns, children } = el2;
  if (!inVPre) {
    if (tag === "slot") {
      el2.tagType = 2;
    } else if (isFragmentTemplate(el2)) {
      el2.tagType = 3;
    } else if (isComponent(el2)) {
      el2.tagType = 1;
    }
  }
  if (!tokenizer.inRCDATA) {
    el2.children = condenseWhitespace(children);
  }
  if (ns === 0 && currentOptions.isIgnoreNewlineTag(tag)) {
    const first = children[0];
    if (first && first.type === 2) {
      first.content = first.content.replace(/^\r?\n/, "");
    }
  }
  if (ns === 0 && currentOptions.isPreTag(tag)) {
    inPre--;
  }
  if (currentVPreBoundary === el2) {
    inVPre = tokenizer.inVPre = false;
    currentVPreBoundary = null;
  }
  if (tokenizer.inXML && (stack[0] ? stack[0].ns : currentOptions.ns) === 0) {
    tokenizer.inXML = false;
  }
  {
    const props = el2.props;
    if (!tokenizer.inSFCRoot && isCompatEnabled(
      "COMPILER_NATIVE_TEMPLATE",
      currentOptions
    ) && el2.tag === "template" && !isFragmentTemplate(el2)) {
      const parent = stack[0] || currentRoot;
      const index = parent.children.indexOf(el2);
      parent.children.splice(index, 1, ...el2.children);
    }
    const inlineTemplateProp = props.find(
      (p2) => p2.type === 6 && p2.name === "inline-template"
    );
    if (inlineTemplateProp && checkCompatEnabled(
      "COMPILER_INLINE_TEMPLATE",
      currentOptions,
      inlineTemplateProp.loc
    ) && el2.children.length) {
      inlineTemplateProp.value = {
        type: 2,
        content: getSlice(
          el2.children[0].loc.start.offset,
          el2.children[el2.children.length - 1].loc.end.offset
        ),
        loc: inlineTemplateProp.loc
      };
    }
  }
}
function lookAhead(index, c) {
  let i = index;
  while (currentInput.charCodeAt(i) !== c && i < currentInput.length - 1) i++;
  return i;
}
function backTrack(index, c) {
  let i = index;
  while (currentInput.charCodeAt(i) !== c && i >= 0) i--;
  return i;
}
const specialTemplateDir = /* @__PURE__ */ new Set(["if", "else", "else-if", "for", "slot"]);
function isFragmentTemplate({ tag, props }) {
  if (tag === "template") {
    for (let i = 0; i < props.length; i++) {
      if (props[i].type === 7 && specialTemplateDir.has(props[i].name)) {
        return true;
      }
    }
  }
  return false;
}
function isComponent({ tag, props }) {
  if (currentOptions.isCustomElement(tag)) {
    return false;
  }
  if (tag === "component" || isUpperCase(tag.charCodeAt(0)) || isCoreComponent(tag) || currentOptions.isBuiltInComponent && currentOptions.isBuiltInComponent(tag) || currentOptions.isNativeTag && !currentOptions.isNativeTag(tag)) {
    return true;
  }
  for (let i = 0; i < props.length; i++) {
    const p2 = props[i];
    if (p2.type === 6) {
      if (p2.name === "is" && p2.value) {
        if (p2.value.content.startsWith("vue:")) {
          return true;
        } else if (checkCompatEnabled(
          "COMPILER_IS_ON_ELEMENT",
          currentOptions,
          p2.loc
        )) {
          return true;
        }
      }
    } else if (
      // :is on plain element - only treat as component in compat mode
      p2.name === "bind" && isStaticArgOf(p2.arg, "is") && checkCompatEnabled(
        "COMPILER_IS_ON_ELEMENT",
        currentOptions,
        p2.loc
      )
    ) {
      return true;
    }
  }
  return false;
}
function isUpperCase(c) {
  return c > 64 && c < 91;
}
const windowsNewlineRE = /\r\n/g;
function condenseWhitespace(nodes) {
  const shouldCondense = currentOptions.whitespace !== "preserve";
  let removedWhitespace = false;
  for (let i = 0; i < nodes.length; i++) {
    const node = nodes[i];
    if (node.type === 2) {
      if (!inPre) {
        if (isAllWhitespace(node.content)) {
          const prev = nodes[i - 1] && nodes[i - 1].type;
          const next = nodes[i + 1] && nodes[i + 1].type;
          if (!prev || !next || shouldCondense && (prev === 3 && (next === 3 || next === 1) || prev === 1 && (next === 3 || next === 1 && hasNewlineChar(node.content)))) {
            removedWhitespace = true;
            nodes[i] = null;
          } else {
            node.content = " ";
          }
        } else if (shouldCondense) {
          node.content = condense(node.content);
        }
      } else {
        node.content = node.content.replace(windowsNewlineRE, "\n");
      }
    }
  }
  return removedWhitespace ? nodes.filter(Boolean) : nodes;
}
function hasNewlineChar(str) {
  for (let i = 0; i < str.length; i++) {
    const c = str.charCodeAt(i);
    if (c === 10 || c === 13) {
      return true;
    }
  }
  return false;
}
function condense(str) {
  let ret = "";
  let prevCharIsWhitespace = false;
  for (let i = 0; i < str.length; i++) {
    if (isWhitespace(str.charCodeAt(i))) {
      if (!prevCharIsWhitespace) {
        ret += " ";
        prevCharIsWhitespace = true;
      }
    } else {
      ret += str[i];
      prevCharIsWhitespace = false;
    }
  }
  return ret;
}
function addNode(node) {
  (stack[0] || currentRoot).children.push(node);
}
function getLoc(start, end) {
  return {
    start: tokenizer.getPos(start),
    // @ts-expect-error allow late attachment
    end: end == null ? end : tokenizer.getPos(end),
    // @ts-expect-error allow late attachment
    source: end == null ? end : getSlice(start, end)
  };
}
function cloneLoc(loc) {
  return getLoc(loc.start.offset, loc.end.offset);
}
function setLocEnd(loc, end) {
  loc.end = tokenizer.getPos(end);
  loc.source = getSlice(loc.start.offset, end);
}
function dirToAttr(dir) {
  const attr = {
    type: 6,
    name: dir.rawName,
    nameLoc: getLoc(
      dir.loc.start.offset,
      dir.loc.start.offset + dir.rawName.length
    ),
    value: void 0,
    loc: dir.loc
  };
  if (dir.exp) {
    const loc = dir.exp.loc;
    if (loc.end.offset < dir.loc.end.offset) {
      loc.start.offset--;
      loc.start.column--;
      loc.end.offset++;
      loc.end.column++;
    }
    attr.value = {
      type: 2,
      content: dir.exp.content,
      loc
    };
  }
  return attr;
}
function createExp(content, isStatic = false, loc, constType = 0, parseMode = 0) {
  const exp = createSimpleExpression(content, isStatic, loc, constType);
  return exp;
}
function emitError(code, index, message) {
  currentOptions.onError(
    createCompilerError(code, getLoc(index, index))
  );
}
function reset() {
  tokenizer.reset();
  currentOpenTag = null;
  currentProp = null;
  currentAttrValue = "";
  currentAttrStartIndex = -1;
  currentAttrEndIndex = -1;
  stack.length = 0;
}
function baseParse(input, options) {
  reset();
  currentInput = input;
  currentOptions = extend({}, defaultParserOptions);
  if (options) {
    let key;
    for (key in options) {
      if (options[key] != null) {
        currentOptions[key] = options[key];
      }
    }
  }
  tokenizer.mode = currentOptions.parseMode === "html" ? 1 : currentOptions.parseMode === "sfc" ? 2 : 0;
  tokenizer.inXML = currentOptions.ns === 1 || currentOptions.ns === 2;
  const delimiters = options && options.delimiters;
  if (delimiters) {
    tokenizer.delimiterOpen = toCharCodes(delimiters[0]);
    tokenizer.delimiterClose = toCharCodes(delimiters[1]);
  }
  const root = currentRoot = createRoot([], input);
  tokenizer.parse(currentInput);
  root.loc = getLoc(0, input.length);
  root.children = condenseWhitespace(root.children);
  currentRoot = null;
  return root;
}
function cacheStatic(root, context) {
  walk(
    root,
    void 0,
    context,
    // Root node is unfortunately non-hoistable due to potential parent
    // fallthrough attributes.
    !!getSingleElementRoot(root)
  );
}
function getSingleElementRoot(root) {
  const children = root.children.filter((x2) => x2.type !== 3);
  return children.length === 1 && children[0].type === 1 && !isSlotOutlet(children[0]) ? children[0] : null;
}
function walk(node, parent, context, doNotHoistNode = false, inFor = false) {
  const { children } = node;
  const toCache = [];
  for (let i = 0; i < children.length; i++) {
    const child = children[i];
    if (child.type === 1 && child.tagType === 0) {
      const constantType = doNotHoistNode ? 0 : getConstantType(child, context);
      if (constantType > 0) {
        if (constantType >= 2) {
          child.codegenNode.patchFlag = -1;
          toCache.push(child);
          continue;
        }
      } else {
        const codegenNode = child.codegenNode;
        if (codegenNode.type === 13) {
          const flag = codegenNode.patchFlag;
          if ((flag === void 0 || flag === 512 || flag === 1) && getGeneratedPropsConstantType(child, context) >= 2) {
            const props = getNodeProps(child);
            if (props) {
              codegenNode.props = context.hoist(props);
            }
          }
          if (codegenNode.dynamicProps) {
            codegenNode.dynamicProps = context.hoist(codegenNode.dynamicProps);
          }
        }
      }
    } else if (child.type === 12) {
      const constantType = doNotHoistNode ? 0 : getConstantType(child, context);
      if (constantType >= 2) {
        if (child.codegenNode.type === 14 && child.codegenNode.arguments.length > 0) {
          child.codegenNode.arguments.push(
            `-1`
          );
        }
        toCache.push(child);
        continue;
      }
    }
    if (child.type === 1) {
      const isComponent2 = child.tagType === 1;
      if (isComponent2) {
        context.scopes.vSlot++;
      }
      walk(child, node, context, false, inFor);
      if (isComponent2) {
        context.scopes.vSlot--;
      }
    } else if (child.type === 11) {
      walk(child, node, context, child.children.length === 1, true);
    } else if (child.type === 9) {
      for (let i2 = 0; i2 < child.branches.length; i2++) {
        walk(
          child.branches[i2],
          node,
          context,
          child.branches[i2].children.length === 1,
          inFor
        );
      }
    }
  }
  let cachedAsArray = false;
  if (toCache.length === children.length && node.type === 1) {
    if (node.tagType === 0 && node.codegenNode && node.codegenNode.type === 13 && isArray(node.codegenNode.children)) {
      node.codegenNode.children = getCacheExpression(
        createArrayExpression(node.codegenNode.children)
      );
      cachedAsArray = true;
    } else if (node.tagType === 1 && node.codegenNode && node.codegenNode.type === 13 && node.codegenNode.children && !isArray(node.codegenNode.children) && node.codegenNode.children.type === 15) {
      const slot = getSlotNode(node.codegenNode, "default");
      if (slot) {
        slot.returns = getCacheExpression(
          createArrayExpression(slot.returns)
        );
        cachedAsArray = true;
      }
    } else if (node.tagType === 3 && parent && parent.type === 1 && parent.tagType === 1 && parent.codegenNode && parent.codegenNode.type === 13 && parent.codegenNode.children && !isArray(parent.codegenNode.children) && parent.codegenNode.children.type === 15) {
      const slotName = findDir(node, "slot", true);
      const slot = slotName && slotName.arg && getSlotNode(parent.codegenNode, slotName.arg);
      if (slot) {
        slot.returns = getCacheExpression(
          createArrayExpression(slot.returns)
        );
        cachedAsArray = true;
      }
    }
  }
  if (!cachedAsArray) {
    for (const child of toCache) {
      child.codegenNode = context.cache(child.codegenNode);
    }
  }
  function getCacheExpression(value) {
    const exp = context.cache(value);
    exp.needArraySpread = true;
    return exp;
  }
  function getSlotNode(node2, name2) {
    if (node2.children && !isArray(node2.children) && node2.children.type === 15) {
      const slot = node2.children.properties.find(
        (p2) => p2.key === name2 || p2.key.content === name2
      );
      return slot && slot.value;
    }
  }
  if (toCache.length && context.transformHoist) {
    context.transformHoist(children, context, node);
  }
}
function getConstantType(node, context) {
  const { constantCache } = context;
  switch (node.type) {
    case 1:
      if (node.tagType !== 0) {
        return 0;
      }
      const cached = constantCache.get(node);
      if (cached !== void 0) {
        return cached;
      }
      const codegenNode = node.codegenNode;
      if (codegenNode.type !== 13) {
        return 0;
      }
      if (codegenNode.isBlock && node.tag !== "svg" && node.tag !== "foreignObject" && node.tag !== "math") {
        return 0;
      }
      if (codegenNode.patchFlag === void 0) {
        let returnType2 = 3;
        const generatedPropsType = getGeneratedPropsConstantType(node, context);
        if (generatedPropsType === 0) {
          constantCache.set(node, 0);
          return 0;
        }
        if (generatedPropsType < returnType2) {
          returnType2 = generatedPropsType;
        }
        for (let i = 0; i < node.children.length; i++) {
          const childType = getConstantType(node.children[i], context);
          if (childType === 0) {
            constantCache.set(node, 0);
            return 0;
          }
          if (childType < returnType2) {
            returnType2 = childType;
          }
        }
        if (returnType2 > 1) {
          for (let i = 0; i < node.props.length; i++) {
            const p2 = node.props[i];
            if (p2.type === 7 && p2.name === "bind" && p2.exp) {
              const expType = getConstantType(p2.exp, context);
              if (expType === 0) {
                constantCache.set(node, 0);
                return 0;
              }
              if (expType < returnType2) {
                returnType2 = expType;
              }
            }
          }
        }
        if (codegenNode.isBlock) {
          for (let i = 0; i < node.props.length; i++) {
            const p2 = node.props[i];
            if (p2.type === 7) {
              constantCache.set(node, 0);
              return 0;
            }
          }
          context.removeHelper(OPEN_BLOCK);
          context.removeHelper(
            getVNodeBlockHelper(context.inSSR, codegenNode.isComponent)
          );
          codegenNode.isBlock = false;
          context.helper(getVNodeHelper(context.inSSR, codegenNode.isComponent));
        }
        constantCache.set(node, returnType2);
        return returnType2;
      } else {
        constantCache.set(node, 0);
        return 0;
      }
    case 2:
    case 3:
      return 3;
    case 9:
    case 11:
    case 10:
      return 0;
    case 5:
    case 12:
      return getConstantType(node.content, context);
    case 4:
      return node.constType;
    case 8:
      let returnType = 3;
      for (let i = 0; i < node.children.length; i++) {
        const child = node.children[i];
        if (isString(child) || isSymbol(child)) {
          continue;
        }
        const childType = getConstantType(child, context);
        if (childType === 0) {
          return 0;
        } else if (childType < returnType) {
          returnType = childType;
        }
      }
      return returnType;
    case 20:
      return 2;
    default:
      return 0;
  }
}
const allowHoistedHelperSet = /* @__PURE__ */ new Set([
  NORMALIZE_CLASS,
  NORMALIZE_STYLE,
  NORMALIZE_PROPS,
  GUARD_REACTIVE_PROPS
]);
function getConstantTypeOfHelperCall(value, context) {
  if (value.type === 14 && !isString(value.callee) && allowHoistedHelperSet.has(value.callee)) {
    const arg = value.arguments[0];
    if (arg.type === 4) {
      return getConstantType(arg, context);
    } else if (arg.type === 14) {
      return getConstantTypeOfHelperCall(arg, context);
    }
  }
  return 0;
}
function getGeneratedPropsConstantType(node, context) {
  let returnType = 3;
  const props = getNodeProps(node);
  if (props && props.type === 15) {
    const { properties } = props;
    for (let i = 0; i < properties.length; i++) {
      const { key, value } = properties[i];
      const keyType = getConstantType(key, context);
      if (keyType === 0) {
        return keyType;
      }
      if (keyType < returnType) {
        returnType = keyType;
      }
      let valueType;
      if (value.type === 4) {
        valueType = getConstantType(value, context);
      } else if (value.type === 14) {
        valueType = getConstantTypeOfHelperCall(value, context);
      } else {
        valueType = 0;
      }
      if (valueType === 0) {
        return valueType;
      }
      if (valueType < returnType) {
        returnType = valueType;
      }
    }
  }
  return returnType;
}
function getNodeProps(node) {
  const codegenNode = node.codegenNode;
  if (codegenNode.type === 13) {
    return codegenNode.props;
  }
}
function createTransformContext(root, {
  filename = "",
  prefixIdentifiers = false,
  hoistStatic = false,
  hmr = false,
  cacheHandlers = false,
  nodeTransforms = [],
  directiveTransforms = {},
  transformHoist = null,
  isBuiltInComponent = NOOP,
  isCustomElement = NOOP,
  expressionPlugins = [],
  scopeId = null,
  slotted = true,
  ssr = false,
  inSSR = false,
  ssrCssVars = ``,
  bindingMetadata = EMPTY_OBJ,
  inline = false,
  isTS = false,
  onError = defaultOnError,
  onWarn = defaultOnWarn,
  compatConfig
}) {
  const nameMatch = filename.replace(/\?.*$/, "").match(/([^/\\]+)\.\w+$/);
  const context = {
    // options
    filename,
    selfName: nameMatch && capitalize(camelize(nameMatch[1])),
    prefixIdentifiers,
    hoistStatic,
    hmr,
    cacheHandlers,
    nodeTransforms,
    directiveTransforms,
    transformHoist,
    isBuiltInComponent,
    isCustomElement,
    expressionPlugins,
    scopeId,
    slotted,
    ssr,
    inSSR,
    ssrCssVars,
    bindingMetadata,
    inline,
    isTS,
    onError,
    onWarn,
    compatConfig,
    // state
    root,
    helpers: /* @__PURE__ */ new Map(),
    components: /* @__PURE__ */ new Set(),
    directives: /* @__PURE__ */ new Set(),
    hoists: [],
    imports: [],
    cached: [],
    constantCache: /* @__PURE__ */ new WeakMap(),
    temps: 0,
    identifiers: /* @__PURE__ */ Object.create(null),
    scopes: {
      vFor: 0,
      vSlot: 0,
      vPre: 0,
      vOnce: 0
    },
    parent: null,
    grandParent: null,
    currentNode: root,
    childIndex: 0,
    inVOnce: false,
    // methods
    helper(name2) {
      const count = context.helpers.get(name2) || 0;
      context.helpers.set(name2, count + 1);
      return name2;
    },
    removeHelper(name2) {
      const count = context.helpers.get(name2);
      if (count) {
        const currentCount = count - 1;
        if (!currentCount) {
          context.helpers.delete(name2);
        } else {
          context.helpers.set(name2, currentCount);
        }
      }
    },
    helperString(name2) {
      return `_${helperNameMap[context.helper(name2)]}`;
    },
    replaceNode(node) {
      context.parent.children[context.childIndex] = context.currentNode = node;
    },
    removeNode(node) {
      const list = context.parent.children;
      const removalIndex = node ? list.indexOf(node) : context.currentNode ? context.childIndex : -1;
      if (!node || node === context.currentNode) {
        context.currentNode = null;
        context.onNodeRemoved();
      } else {
        if (context.childIndex > removalIndex) {
          context.childIndex--;
          context.onNodeRemoved();
        }
      }
      context.parent.children.splice(removalIndex, 1);
    },
    onNodeRemoved: NOOP,
    addIdentifiers(exp) {
    },
    removeIdentifiers(exp) {
    },
    hoist(exp) {
      if (isString(exp)) exp = createSimpleExpression(exp);
      context.hoists.push(exp);
      const identifier = createSimpleExpression(
        `_hoisted_${context.hoists.length}`,
        false,
        exp.loc,
        2
      );
      identifier.hoisted = exp;
      return identifier;
    },
    cache(exp, isVNode2 = false, inVOnce = false) {
      const cacheExp = createCacheExpression(
        context.cached.length,
        exp,
        isVNode2,
        inVOnce
      );
      context.cached.push(cacheExp);
      return cacheExp;
    }
  };
  {
    context.filters = /* @__PURE__ */ new Set();
  }
  return context;
}
function transform(root, options) {
  const context = createTransformContext(root, options);
  traverseNode(root, context);
  if (options.hoistStatic) {
    cacheStatic(root, context);
  }
  if (!options.ssr) {
    createRootCodegen(root, context);
  }
  root.helpers = /* @__PURE__ */ new Set([...context.helpers.keys()]);
  root.components = [...context.components];
  root.directives = [...context.directives];
  root.imports = context.imports;
  root.hoists = context.hoists;
  root.temps = context.temps;
  root.cached = context.cached;
  root.transformed = true;
  {
    root.filters = [...context.filters];
  }
}
function createRootCodegen(root, context) {
  const { helper } = context;
  const { children } = root;
  if (children.length === 1) {
    const singleElementRootChild = getSingleElementRoot(root);
    if (singleElementRootChild && singleElementRootChild.codegenNode) {
      const codegenNode = singleElementRootChild.codegenNode;
      if (codegenNode.type === 13) {
        convertToBlock(codegenNode, context);
      }
      root.codegenNode = codegenNode;
    } else {
      root.codegenNode = children[0];
    }
  } else if (children.length > 1) {
    let patchFlag = 64;
    root.codegenNode = createVNodeCall(
      context,
      helper(FRAGMENT),
      void 0,
      root.children,
      patchFlag,
      void 0,
      void 0,
      true,
      void 0,
      false
    );
  } else ;
}
function traverseChildren(parent, context) {
  let i = 0;
  const nodeRemoved = () => {
    i--;
  };
  for (; i < parent.children.length; i++) {
    const child = parent.children[i];
    if (isString(child)) continue;
    context.grandParent = context.parent;
    context.parent = parent;
    context.childIndex = i;
    context.onNodeRemoved = nodeRemoved;
    traverseNode(child, context);
  }
}
function traverseNode(node, context) {
  context.currentNode = node;
  const { nodeTransforms } = context;
  const exitFns = [];
  for (let i2 = 0; i2 < nodeTransforms.length; i2++) {
    const onExit = nodeTransforms[i2](node, context);
    if (onExit) {
      if (isArray(onExit)) {
        exitFns.push(...onExit);
      } else {
        exitFns.push(onExit);
      }
    }
    if (!context.currentNode) {
      return;
    } else {
      node = context.currentNode;
    }
  }
  switch (node.type) {
    case 3:
      if (!context.ssr) {
        context.helper(CREATE_COMMENT);
      }
      break;
    case 5:
      if (!context.ssr) {
        context.helper(TO_DISPLAY_STRING);
      }
      break;
    // for container types, further traverse downwards
    case 9:
      for (let i2 = 0; i2 < node.branches.length; i2++) {
        traverseNode(node.branches[i2], context);
      }
      break;
    case 10:
    case 11:
    case 1:
    case 0:
      traverseChildren(node, context);
      break;
  }
  context.currentNode = node;
  let i = exitFns.length;
  while (i--) {
    exitFns[i]();
  }
}
function createStructuralDirectiveTransform(name2, fn) {
  const matches2 = isString(name2) ? (n) => n === name2 : (n) => name2.test(n);
  return (node, context) => {
    if (node.type === 1) {
      const { props } = node;
      if (node.tagType === 3 && props.some(isVSlot)) {
        return;
      }
      const exitFns = [];
      for (let i = 0; i < props.length; i++) {
        const prop = props[i];
        if (prop.type === 7 && matches2(prop.name)) {
          props.splice(i, 1);
          i--;
          const onExit = fn(node, prop, context);
          if (onExit) exitFns.push(onExit);
        }
      }
      return exitFns;
    }
  };
}
const PURE_ANNOTATION = `/*@__PURE__*/`;
const aliasHelper = (s) => `${helperNameMap[s]}: _${helperNameMap[s]}`;
function createCodegenContext(ast, {
  mode = "function",
  prefixIdentifiers = mode === "module",
  sourceMap = false,
  filename = `template.vue.html`,
  scopeId = null,
  optimizeImports = false,
  runtimeGlobalName = `Vue`,
  runtimeModuleName = `vue`,
  ssrRuntimeModuleName = "vue/server-renderer",
  ssr = false,
  isTS = false,
  inSSR = false
}) {
  const context = {
    mode,
    prefixIdentifiers,
    sourceMap,
    filename,
    scopeId,
    optimizeImports,
    runtimeGlobalName,
    runtimeModuleName,
    ssrRuntimeModuleName,
    ssr,
    isTS,
    inSSR,
    source: ast.source,
    code: ``,
    column: 1,
    line: 1,
    offset: 0,
    indentLevel: 0,
    pure: false,
    map: void 0,
    helper(key) {
      return `_${helperNameMap[key]}`;
    },
    push(code, newlineIndex = -2, node) {
      context.code += code;
    },
    indent() {
      newline(++context.indentLevel);
    },
    deindent(withoutNewLine = false) {
      if (withoutNewLine) {
        --context.indentLevel;
      } else {
        newline(--context.indentLevel);
      }
    },
    newline() {
      newline(context.indentLevel);
    }
  };
  function newline(n) {
    context.push(
      "\n" + `  `.repeat(n),
      0
      /* Start */
    );
  }
  return context;
}
function generate(ast, options = {}) {
  const context = createCodegenContext(ast, options);
  if (options.onContextCreated) options.onContextCreated(context);
  const {
    mode,
    push,
    prefixIdentifiers,
    indent,
    deindent,
    newline,
    scopeId,
    ssr
  } = context;
  const helpers = Array.from(ast.helpers);
  const hasHelpers = helpers.length > 0;
  const useWithBlock = !prefixIdentifiers && mode !== "module";
  const preambleContext = context;
  {
    genFunctionPreamble(ast, preambleContext);
  }
  const functionName = ssr ? `ssrRender` : `render`;
  const args = ssr ? ["_ctx", "_push", "_parent", "_attrs"] : ["_ctx", "_cache"];
  const signature = args.join(", ");
  {
    push(`function ${functionName}(${signature}) {`);
  }
  indent();
  if (useWithBlock) {
    push(`with (_ctx) {`);
    indent();
    if (hasHelpers) {
      push(
        `const { ${helpers.map(aliasHelper).join(", ")} } = _Vue
`,
        -1
        /* End */
      );
      newline();
    }
  }
  if (ast.components.length) {
    genAssets(ast.components, "component", context);
    if (ast.directives.length || ast.temps > 0) {
      newline();
    }
  }
  if (ast.directives.length) {
    genAssets(ast.directives, "directive", context);
    if (ast.temps > 0) {
      newline();
    }
  }
  if (ast.filters && ast.filters.length) {
    newline();
    genAssets(ast.filters, "filter", context);
    newline();
  }
  if (ast.temps > 0) {
    push(`let `);
    for (let i = 0; i < ast.temps; i++) {
      push(`${i > 0 ? `, ` : ``}_temp${i}`);
    }
  }
  if (ast.components.length || ast.directives.length || ast.temps) {
    push(
      `
`,
      0
      /* Start */
    );
    newline();
  }
  if (!ssr) {
    push(`return `);
  }
  if (ast.codegenNode) {
    genNode(ast.codegenNode, context);
  } else {
    push(`null`);
  }
  if (useWithBlock) {
    deindent();
    push(`}`);
  }
  deindent();
  push(`}`);
  return {
    ast,
    code: context.code,
    preamble: ``,
    map: context.map ? context.map.toJSON() : void 0
  };
}
function genFunctionPreamble(ast, context) {
  const {
    ssr,
    prefixIdentifiers,
    push,
    newline,
    runtimeModuleName,
    runtimeGlobalName,
    ssrRuntimeModuleName
  } = context;
  const VueBinding = runtimeGlobalName;
  const helpers = Array.from(ast.helpers);
  if (helpers.length > 0) {
    {
      push(
        `const _Vue = ${VueBinding}
`,
        -1
        /* End */
      );
      if (ast.hoists.length) {
        const staticHelpers = [
          CREATE_VNODE,
          CREATE_ELEMENT_VNODE,
          CREATE_COMMENT,
          CREATE_TEXT,
          CREATE_STATIC
        ].filter((helper) => helpers.includes(helper)).map(aliasHelper).join(", ");
        push(
          `const { ${staticHelpers} } = _Vue
`,
          -1
          /* End */
        );
      }
    }
  }
  genHoists(ast.hoists, context);
  newline();
  push(`return `);
}
function genAssets(assets, type, { helper, push, newline, isTS }) {
  const resolver = helper(
    type === "filter" ? RESOLVE_FILTER : type === "component" ? RESOLVE_COMPONENT : RESOLVE_DIRECTIVE
  );
  for (let i = 0; i < assets.length; i++) {
    let id = assets[i];
    const maybeSelfReference = id.endsWith("__self");
    if (maybeSelfReference) {
      id = id.slice(0, -6);
    }
    push(
      `const ${toValidAssetId(id, type)} = ${resolver}(${JSON.stringify(id)}${maybeSelfReference ? `, true` : ``})${isTS ? `!` : ``}`
    );
    if (i < assets.length - 1) {
      newline();
    }
  }
}
function genHoists(hoists, context) {
  if (!hoists.length) {
    return;
  }
  context.pure = true;
  const { push, newline } = context;
  newline();
  for (let i = 0; i < hoists.length; i++) {
    const exp = hoists[i];
    if (exp) {
      push(`const _hoisted_${i + 1} = `);
      genNode(exp, context);
      newline();
    }
  }
  context.pure = false;
}
function genNodeListAsArray(nodes, context) {
  const multilines = nodes.length > 3 || false;
  context.push(`[`);
  multilines && context.indent();
  genNodeList(nodes, context, multilines);
  multilines && context.deindent();
  context.push(`]`);
}
function genNodeList(nodes, context, multilines = false, comma = true) {
  const { push, newline } = context;
  for (let i = 0; i < nodes.length; i++) {
    const node = nodes[i];
    if (isString(node)) {
      push(
        node,
        -3
        /* Unknown */
      );
    } else if (isArray(node)) {
      genNodeListAsArray(node, context);
    } else {
      genNode(node, context);
    }
    if (i < nodes.length - 1) {
      if (multilines) {
        comma && push(",");
        newline();
      } else {
        comma && push(", ");
      }
    }
  }
}
function genNode(node, context) {
  if (isString(node)) {
    context.push(
      node,
      -3
      /* Unknown */
    );
    return;
  }
  if (isSymbol(node)) {
    context.push(context.helper(node));
    return;
  }
  switch (node.type) {
    case 1:
    case 9:
    case 11:
      genNode(node.codegenNode, context);
      break;
    case 2:
      genText(node, context);
      break;
    case 4:
      genExpression(node, context);
      break;
    case 5:
      genInterpolation(node, context);
      break;
    case 12:
      genNode(node.codegenNode, context);
      break;
    case 8:
      genCompoundExpression(node, context);
      break;
    case 3:
      genComment(node, context);
      break;
    case 13:
      genVNodeCall(node, context);
      break;
    case 14:
      genCallExpression(node, context);
      break;
    case 15:
      genObjectExpression(node, context);
      break;
    case 17:
      genArrayExpression(node, context);
      break;
    case 18:
      genFunctionExpression(node, context);
      break;
    case 19:
      genConditionalExpression(node, context);
      break;
    case 20:
      genCacheExpression(node, context);
      break;
    case 21:
      genNodeList(node.body, context, true, false);
      break;
  }
}
function genText(node, context) {
  context.push(JSON.stringify(node.content), -3, node);
}
function genExpression(node, context) {
  const { content, isStatic } = node;
  context.push(
    isStatic ? JSON.stringify(content) : content,
    -3,
    node
  );
}
function genInterpolation(node, context) {
  const { push, helper, pure } = context;
  if (pure) push(PURE_ANNOTATION);
  push(`${helper(TO_DISPLAY_STRING)}(`);
  genNode(node.content, context);
  push(`)`);
}
function genCompoundExpression(node, context) {
  for (let i = 0; i < node.children.length; i++) {
    const child = node.children[i];
    if (isString(child)) {
      context.push(
        child,
        -3
        /* Unknown */
      );
    } else {
      genNode(child, context);
    }
  }
}
function genExpressionAsPropertyKey(node, context) {
  const { push } = context;
  if (node.type === 8) {
    push(`[`);
    genCompoundExpression(node, context);
    push(`]`);
  } else if (node.isStatic) {
    const text = isSimpleIdentifier(node.content) ? node.content : JSON.stringify(node.content);
    push(text, -2, node);
  } else {
    push(`[${node.content}]`, -3, node);
  }
}
function genComment(node, context) {
  const { push, helper, pure } = context;
  if (pure) {
    push(PURE_ANNOTATION);
  }
  push(
    `${helper(CREATE_COMMENT)}(${JSON.stringify(node.content)})`,
    -3,
    node
  );
}
function genVNodeCall(node, context) {
  const { push, helper, pure } = context;
  const {
    tag,
    props,
    children,
    patchFlag,
    dynamicProps,
    directives,
    isBlock,
    disableTracking,
    isComponent: isComponent2
  } = node;
  let patchFlagString;
  if (patchFlag) {
    {
      patchFlagString = String(patchFlag);
    }
  }
  if (directives) {
    push(helper(WITH_DIRECTIVES) + `(`);
  }
  if (isBlock) {
    push(`(${helper(OPEN_BLOCK)}(${disableTracking ? `true` : ``}), `);
  }
  if (pure) {
    push(PURE_ANNOTATION);
  }
  const callHelper = isBlock ? getVNodeBlockHelper(context.inSSR, isComponent2) : getVNodeHelper(context.inSSR, isComponent2);
  push(helper(callHelper) + `(`, -2, node);
  genNodeList(
    genNullableArgs([tag, props, children, patchFlagString, dynamicProps]),
    context
  );
  push(`)`);
  if (isBlock) {
    push(`)`);
  }
  if (directives) {
    push(`, `);
    genNode(directives, context);
    push(`)`);
  }
}
function genNullableArgs(args) {
  let i = args.length;
  while (i--) {
    if (args[i] != null) break;
  }
  return args.slice(0, i + 1).map((arg) => arg || `null`);
}
function genCallExpression(node, context) {
  const { push, helper, pure } = context;
  const callee = isString(node.callee) ? node.callee : helper(node.callee);
  if (pure) {
    push(PURE_ANNOTATION);
  }
  push(callee + `(`, -2, node);
  genNodeList(node.arguments, context);
  push(`)`);
}
function genObjectExpression(node, context) {
  const { push, indent, deindent, newline } = context;
  const { properties } = node;
  if (!properties.length) {
    push(`{}`, -2, node);
    return;
  }
  const multilines = properties.length > 1 || false;
  push(multilines ? `{` : `{ `);
  multilines && indent();
  for (let i = 0; i < properties.length; i++) {
    const { key, value } = properties[i];
    genExpressionAsPropertyKey(key, context);
    push(`: `);
    genNode(value, context);
    if (i < properties.length - 1) {
      push(`,`);
      newline();
    }
  }
  multilines && deindent();
  push(multilines ? `}` : ` }`);
}
function genArrayExpression(node, context) {
  genNodeListAsArray(node.elements, context);
}
function genFunctionExpression(node, context) {
  const { push, indent, deindent } = context;
  const { params, returns, body, newline, isSlot } = node;
  if (isSlot) {
    push(`_${helperNameMap[WITH_CTX]}(`);
  }
  push(`(`, -2, node);
  if (isArray(params)) {
    genNodeList(params, context);
  } else if (params) {
    genNode(params, context);
  }
  push(`) => `);
  if (newline || body) {
    push(`{`);
    indent();
  }
  if (returns) {
    if (newline) {
      push(`return `);
    }
    if (isArray(returns)) {
      genNodeListAsArray(returns, context);
    } else {
      genNode(returns, context);
    }
  } else if (body) {
    genNode(body, context);
  }
  if (newline || body) {
    deindent();
    push(`}`);
  }
  if (isSlot) {
    if (node.isNonScopedSlot) {
      push(`, undefined, true`);
    }
    push(`)`);
  }
}
function genConditionalExpression(node, context) {
  const { test, consequent, alternate, newline: needNewline } = node;
  const { push, indent, deindent, newline } = context;
  if (test.type === 4) {
    const needsParens = !isSimpleIdentifier(test.content);
    needsParens && push(`(`);
    genExpression(test, context);
    needsParens && push(`)`);
  } else {
    push(`(`);
    genNode(test, context);
    push(`)`);
  }
  needNewline && indent();
  context.indentLevel++;
  needNewline || push(` `);
  push(`? `);
  genNode(consequent, context);
  context.indentLevel--;
  needNewline && newline();
  needNewline || push(` `);
  push(`: `);
  const isNested = alternate.type === 19;
  if (!isNested) {
    context.indentLevel++;
  }
  genNode(alternate, context);
  if (!isNested) {
    context.indentLevel--;
  }
  needNewline && deindent(
    true
    /* without newline */
  );
}
function genCacheExpression(node, context) {
  const { push, helper, indent, deindent, newline } = context;
  const { needPauseTracking, needArraySpread } = node;
  if (needArraySpread) {
    push(`[...(`);
  }
  push(`_cache[${node.index}] || (`);
  if (needPauseTracking) {
    indent();
    push(`${helper(SET_BLOCK_TRACKING)}(-1`);
    if (node.inVOnce) push(`, true`);
    push(`),`);
    newline();
    push(`(`);
  }
  push(`_cache[${node.index}] = `);
  genNode(node.value, context);
  if (needPauseTracking) {
    push(`).cacheIndex = ${node.index},`);
    newline();
    push(`${helper(SET_BLOCK_TRACKING)}(1),`);
    newline();
    push(`_cache[${node.index}]`);
    deindent();
  }
  push(`)`);
  if (needArraySpread) {
    push(`)]`);
  }
}
new RegExp(
  "\\b" + "arguments,await,break,case,catch,class,const,continue,debugger,default,delete,do,else,export,extends,finally,for,function,if,import,let,new,return,super,switch,throw,try,var,void,while,with,yield".split(",").join("\\b|\\b") + "\\b"
);
const transformIf = createStructuralDirectiveTransform(
  /^(?:if|else|else-if)$/,
  (node, dir, context) => {
    return processIf(node, dir, context, (ifNode, branch, isRoot) => {
      const siblings = context.parent.children;
      let i = siblings.indexOf(ifNode);
      let key = 0;
      while (i-- >= 0) {
        const sibling = siblings[i];
        if (sibling && sibling.type === 9) {
          key += sibling.branches.length;
        }
      }
      return () => {
        if (isRoot) {
          ifNode.codegenNode = createCodegenNodeForBranch(
            branch,
            key,
            context
          );
        } else {
          const parentCondition = getParentCondition(ifNode.codegenNode);
          parentCondition.alternate = createCodegenNodeForBranch(
            branch,
            key + ifNode.branches.length - 1,
            context
          );
        }
      };
    });
  }
);
function processIf(node, dir, context, processCodegen) {
  if (dir.name !== "else" && (!dir.exp || !dir.exp.content.trim())) {
    const loc = dir.exp ? dir.exp.loc : node.loc;
    context.onError(
      createCompilerError(28, dir.loc)
    );
    dir.exp = createSimpleExpression(`true`, false, loc);
  }
  if (dir.name === "if") {
    const branch = createIfBranch(node, dir);
    const ifNode = {
      type: 9,
      loc: cloneLoc(node.loc),
      branches: [branch]
    };
    context.replaceNode(ifNode);
    if (processCodegen) {
      return processCodegen(ifNode, branch, true);
    }
  } else {
    const siblings = context.parent.children;
    let i = siblings.indexOf(node);
    while (i-- >= -1) {
      const sibling = siblings[i];
      if (sibling && isCommentOrWhitespace(sibling)) {
        context.removeNode(sibling);
        continue;
      }
      if (sibling && sibling.type === 9) {
        if ((dir.name === "else-if" || dir.name === "else") && sibling.branches[sibling.branches.length - 1].condition === void 0) {
          context.onError(
            createCompilerError(30, node.loc)
          );
        }
        context.removeNode();
        const branch = createIfBranch(node, dir);
        sibling.branches.push(branch);
        const onExit = processCodegen && processCodegen(sibling, branch, false);
        traverseNode(branch, context);
        if (onExit) onExit();
        context.currentNode = null;
      } else {
        context.onError(
          createCompilerError(30, node.loc)
        );
      }
      break;
    }
  }
}
function createIfBranch(node, dir) {
  const isTemplateIf = node.tagType === 3;
  return {
    type: 10,
    loc: node.loc,
    condition: dir.name === "else" ? void 0 : dir.exp,
    children: isTemplateIf && !findDir(node, "for") ? node.children : [node],
    userKey: findProp(node, `key`),
    isTemplateIf
  };
}
function createCodegenNodeForBranch(branch, keyIndex, context) {
  if (branch.condition) {
    return createConditionalExpression(
      branch.condition,
      createChildrenCodegenNode(branch, keyIndex, context),
      // make sure to pass in asBlock: true so that the comment node call
      // closes the current block.
      createCallExpression(context.helper(CREATE_COMMENT), [
        '""',
        "true"
      ])
    );
  } else {
    return createChildrenCodegenNode(branch, keyIndex, context);
  }
}
function createChildrenCodegenNode(branch, keyIndex, context) {
  const { helper } = context;
  const keyProperty = createObjectProperty(
    `key`,
    createSimpleExpression(
      `${keyIndex}`,
      false,
      locStub,
      2
    )
  );
  const { children } = branch;
  const firstChild = children[0];
  const needFragmentWrapper = children.length !== 1 || firstChild.type !== 1;
  if (needFragmentWrapper) {
    if (children.length === 1 && firstChild.type === 11) {
      const vnodeCall = firstChild.codegenNode;
      injectProp(vnodeCall, keyProperty, context);
      return vnodeCall;
    } else {
      let patchFlag = 64;
      return createVNodeCall(
        context,
        helper(FRAGMENT),
        createObjectExpression([keyProperty]),
        children,
        patchFlag,
        void 0,
        void 0,
        true,
        false,
        false,
        branch.loc
      );
    }
  } else {
    const ret = firstChild.codegenNode;
    const vnodeCall = getMemoedVNodeCall(ret);
    if (vnodeCall.type === 13) {
      convertToBlock(vnodeCall, context);
    }
    injectProp(vnodeCall, keyProperty, context);
    return ret;
  }
}
function getParentCondition(node) {
  while (true) {
    if (node.type === 19) {
      if (node.alternate.type === 19) {
        node = node.alternate;
      } else {
        return node;
      }
    } else if (node.type === 20) {
      node = node.value;
    }
  }
}
const transformFor = createStructuralDirectiveTransform(
  "for",
  (node, dir, context) => {
    const { helper, removeHelper } = context;
    return processFor(node, dir, context, (forNode) => {
      const renderExp = createCallExpression(helper(RENDER_LIST), [
        forNode.source
      ]);
      const isTemplate = isTemplateNode(node);
      const memo = findDir(node, "memo");
      const keyProp = findProp(node, `key`, false, true);
      keyProp && keyProp.type === 7;
      let keyExp = keyProp && (keyProp.type === 6 ? keyProp.value ? createSimpleExpression(keyProp.value.content, true) : void 0 : keyProp.exp);
      const keyProperty = keyProp && keyExp ? createObjectProperty(`key`, keyExp) : null;
      const isStableFragment = forNode.source.type === 4 && forNode.source.constType > 0;
      const fragmentFlag = isStableFragment ? 64 : keyProp ? 128 : 256;
      forNode.codegenNode = createVNodeCall(
        context,
        helper(FRAGMENT),
        void 0,
        renderExp,
        fragmentFlag,
        void 0,
        void 0,
        true,
        !isStableFragment,
        false,
        node.loc
      );
      return () => {
        let childBlock;
        const { children } = forNode;
        const needFragmentWrapper = children.length !== 1 || children[0].type !== 1;
        const slotOutlet = isSlotOutlet(node) ? node : isTemplate && node.children.length === 1 && isSlotOutlet(node.children[0]) ? node.children[0] : null;
        if (slotOutlet) {
          childBlock = slotOutlet.codegenNode;
          if (isTemplate && keyProperty) {
            injectProp(childBlock, keyProperty, context);
          }
        } else if (needFragmentWrapper) {
          childBlock = createVNodeCall(
            context,
            helper(FRAGMENT),
            keyProperty ? createObjectExpression([keyProperty]) : void 0,
            node.children,
            64,
            void 0,
            void 0,
            true,
            void 0,
            false
          );
        } else {
          childBlock = children[0].codegenNode;
          if (isTemplate && keyProperty) {
            injectProp(childBlock, keyProperty, context);
          }
          if (childBlock.isBlock !== !isStableFragment) {
            if (childBlock.isBlock) {
              removeHelper(OPEN_BLOCK);
              removeHelper(
                getVNodeBlockHelper(context.inSSR, childBlock.isComponent)
              );
            } else {
              removeHelper(
                getVNodeHelper(context.inSSR, childBlock.isComponent)
              );
            }
          }
          childBlock.isBlock = !isStableFragment;
          if (childBlock.isBlock) {
            helper(OPEN_BLOCK);
            helper(getVNodeBlockHelper(context.inSSR, childBlock.isComponent));
          } else {
            helper(getVNodeHelper(context.inSSR, childBlock.isComponent));
          }
        }
        if (memo) {
          const loop = createFunctionExpression(
            createForLoopParams(forNode.parseResult, [
              createSimpleExpression(`_cached`)
            ])
          );
          loop.body = createBlockStatement([
            createCompoundExpression([`const _memo = (`, memo.exp, `)`]),
            createCompoundExpression([
              `if (_cached`,
              ...keyExp ? [` && _cached.key === `, keyExp] : [],
              ` && ${context.helperString(
                IS_MEMO_SAME
              )}(_cached, _memo)) return _cached`
            ]),
            createCompoundExpression([`const _item = `, childBlock]),
            createSimpleExpression(`_item.memo = _memo`),
            createSimpleExpression(`return _item`)
          ]);
          renderExp.arguments.push(
            loop,
            createSimpleExpression(`_cache`),
            createSimpleExpression(String(context.cached.length))
          );
          context.cached.push(null);
        } else {
          renderExp.arguments.push(
            createFunctionExpression(
              createForLoopParams(forNode.parseResult),
              childBlock,
              true
            )
          );
        }
      };
    });
  }
);
function processFor(node, dir, context, processCodegen) {
  if (!dir.exp) {
    context.onError(
      createCompilerError(31, dir.loc)
    );
    return;
  }
  const parseResult = dir.forParseResult;
  if (!parseResult) {
    context.onError(
      createCompilerError(32, dir.loc)
    );
    return;
  }
  finalizeForParseResult(parseResult);
  const { addIdentifiers, removeIdentifiers, scopes } = context;
  const { source, value, key, index } = parseResult;
  const forNode = {
    type: 11,
    loc: dir.loc,
    source,
    valueAlias: value,
    keyAlias: key,
    objectIndexAlias: index,
    parseResult,
    children: isTemplateNode(node) ? node.children : [node]
  };
  context.replaceNode(forNode);
  scopes.vFor++;
  const onExit = processCodegen && processCodegen(forNode);
  return () => {
    scopes.vFor--;
    if (onExit) onExit();
  };
}
function finalizeForParseResult(result, context) {
  if (result.finalized) return;
  result.finalized = true;
}
function createForLoopParams({ value, key, index }, memoArgs = []) {
  return createParamsList([value, key, index, ...memoArgs]);
}
function createParamsList(args) {
  let i = args.length;
  while (i--) {
    if (args[i]) break;
  }
  return args.slice(0, i + 1).map((arg, i2) => arg || createSimpleExpression(`_`.repeat(i2 + 1), false));
}
const defaultFallback = createSimpleExpression(`undefined`, false);
const trackSlotScopes = (node, context) => {
  if (node.type === 1 && (node.tagType === 1 || node.tagType === 3)) {
    const vSlot = findDir(node, "slot");
    if (vSlot) {
      vSlot.exp;
      context.scopes.vSlot++;
      return () => {
        context.scopes.vSlot--;
      };
    }
  }
};
const buildClientSlotFn = (props, _vForExp, children, loc) => createFunctionExpression(
  props,
  children,
  false,
  true,
  children.length ? children[0].loc : loc
);
function buildSlots(node, context, buildSlotFn = buildClientSlotFn) {
  context.helper(WITH_CTX);
  const { children, loc } = node;
  const slotsProperties = [];
  const dynamicSlots = [];
  let hasDynamicSlots = context.scopes.vSlot > 0 || context.scopes.vFor > 0;
  const onComponentSlot = findDir(node, "slot", true);
  if (onComponentSlot) {
    const { arg, exp } = onComponentSlot;
    if (arg && !isStaticExp(arg)) {
      hasDynamicSlots = true;
    }
    slotsProperties.push(
      createObjectProperty(
        arg || createSimpleExpression("default", true),
        buildSlotFn(exp, void 0, children, loc)
      )
    );
  }
  let hasTemplateSlots = false;
  let hasNamedDefaultSlot = false;
  const implicitDefaultChildren = [];
  const seenSlotNames = /* @__PURE__ */ new Set();
  let conditionalBranchIndex = 0;
  for (let i = 0; i < children.length; i++) {
    const slotElement = children[i];
    let slotDir;
    if (!isTemplateNode(slotElement) || !(slotDir = findDir(slotElement, "slot", true))) {
      if (slotElement.type !== 3) {
        implicitDefaultChildren.push(slotElement);
      }
      continue;
    }
    if (onComponentSlot) {
      context.onError(
        createCompilerError(37, slotDir.loc)
      );
      break;
    }
    hasTemplateSlots = true;
    const { children: slotChildren, loc: slotLoc } = slotElement;
    const {
      arg: slotName = createSimpleExpression(`default`, true),
      exp: slotProps,
      loc: dirLoc
    } = slotDir;
    let staticSlotName;
    if (isStaticExp(slotName)) {
      staticSlotName = slotName ? slotName.content : `default`;
    } else {
      hasDynamicSlots = true;
    }
    const vFor = findDir(slotElement, "for");
    const slotFunction = buildSlotFn(slotProps, vFor, slotChildren, slotLoc);
    let vIf;
    let vElse;
    if (vIf = findDir(slotElement, "if")) {
      hasDynamicSlots = true;
      dynamicSlots.push(
        createConditionalExpression(
          vIf.exp,
          buildDynamicSlot(slotName, slotFunction, conditionalBranchIndex++),
          defaultFallback
        )
      );
    } else if (vElse = findDir(
      slotElement,
      /^else(?:-if)?$/,
      true
      /* allowEmpty */
    )) {
      let j = i;
      let prev;
      while (j--) {
        prev = children[j];
        if (!isCommentOrWhitespace(prev)) {
          break;
        }
      }
      if (prev && isTemplateNode(prev) && findDir(prev, /^(?:else-)?if$/)) {
        let conditional = dynamicSlots[dynamicSlots.length - 1];
        while (conditional.alternate.type === 19) {
          conditional = conditional.alternate;
        }
        conditional.alternate = vElse.exp ? createConditionalExpression(
          vElse.exp,
          buildDynamicSlot(
            slotName,
            slotFunction,
            conditionalBranchIndex++
          ),
          defaultFallback
        ) : buildDynamicSlot(slotName, slotFunction, conditionalBranchIndex++);
      } else {
        context.onError(
          createCompilerError(30, vElse.loc)
        );
      }
    } else if (vFor) {
      hasDynamicSlots = true;
      const parseResult = vFor.forParseResult;
      if (parseResult) {
        finalizeForParseResult(parseResult);
        dynamicSlots.push(
          createCallExpression(context.helper(RENDER_LIST), [
            parseResult.source,
            createFunctionExpression(
              createForLoopParams(parseResult),
              buildDynamicSlot(slotName, slotFunction),
              true
            )
          ])
        );
      } else {
        context.onError(
          createCompilerError(
            32,
            vFor.loc
          )
        );
      }
    } else {
      if (staticSlotName) {
        if (seenSlotNames.has(staticSlotName)) {
          context.onError(
            createCompilerError(
              38,
              dirLoc
            )
          );
          continue;
        }
        seenSlotNames.add(staticSlotName);
        if (staticSlotName === "default") {
          hasNamedDefaultSlot = true;
        }
      }
      slotsProperties.push(createObjectProperty(slotName, slotFunction));
    }
  }
  if (!onComponentSlot) {
    const buildDefaultSlotProperty = (props, children2) => {
      const fn = buildSlotFn(props, void 0, children2, loc);
      if (context.compatConfig) {
        fn.isNonScopedSlot = true;
      }
      return createObjectProperty(`default`, fn);
    };
    if (!hasTemplateSlots) {
      slotsProperties.push(buildDefaultSlotProperty(void 0, children));
    } else if (implicitDefaultChildren.length && // #3766
    // with whitespace: 'preserve', whitespaces between slots will end up in
    // implicitDefaultChildren. Ignore if all implicit children are whitespaces.
    !implicitDefaultChildren.every(isWhitespaceText)) {
      if (hasNamedDefaultSlot) {
        context.onError(
          createCompilerError(
            39,
            implicitDefaultChildren[0].loc
          )
        );
      } else {
        slotsProperties.push(
          buildDefaultSlotProperty(void 0, implicitDefaultChildren)
        );
      }
    }
  }
  const slotFlag = hasDynamicSlots ? 2 : hasForwardedSlots(node.children) ? 3 : 1;
  let slots = createObjectExpression(
    slotsProperties.concat(
      createObjectProperty(
        `_`,
        // 2 = compiled but dynamic = can skip normalization, but must run diff
        // 1 = compiled and static = can skip normalization AND diff as optimized
        createSimpleExpression(
          slotFlag + ``,
          false
        )
      )
    ),
    loc
  );
  if (dynamicSlots.length) {
    slots = createCallExpression(context.helper(CREATE_SLOTS), [
      slots,
      createArrayExpression(dynamicSlots)
    ]);
  }
  return {
    slots,
    hasDynamicSlots
  };
}
function buildDynamicSlot(name2, fn, index) {
  const props = [
    createObjectProperty(`name`, name2),
    createObjectProperty(`fn`, fn)
  ];
  if (index != null) {
    props.push(
      createObjectProperty(`key`, createSimpleExpression(String(index), true))
    );
  }
  return createObjectExpression(props);
}
function hasForwardedSlots(children) {
  for (let i = 0; i < children.length; i++) {
    const child = children[i];
    switch (child.type) {
      case 1:
        if (child.tagType === 2 || hasForwardedSlots(child.children)) {
          return true;
        }
        break;
      case 9:
        if (hasForwardedSlots(child.branches)) return true;
        break;
      case 10:
      case 11:
        if (hasForwardedSlots(child.children)) return true;
        break;
    }
  }
  return false;
}
const directiveImportMap = /* @__PURE__ */ new WeakMap();
const transformElement = (node, context) => {
  return function postTransformElement() {
    node = context.currentNode;
    if (!(node.type === 1 && (node.tagType === 0 || node.tagType === 1))) {
      return;
    }
    const { tag, props } = node;
    const isComponent2 = node.tagType === 1;
    let vnodeTag = isComponent2 ? resolveComponentType(node, context) : `"${tag}"`;
    const isDynamicComponent = isObject(vnodeTag) && vnodeTag.callee === RESOLVE_DYNAMIC_COMPONENT;
    let vnodeProps;
    let vnodeChildren;
    let patchFlag = 0;
    let vnodeDynamicProps;
    let dynamicPropNames;
    let vnodeDirectives;
    let shouldUseBlock = (
      // dynamic component may resolve to plain elements
      isDynamicComponent || vnodeTag === TELEPORT || vnodeTag === SUSPENSE || !isComponent2 && // <svg> and <foreignObject> must be forced into blocks so that block
      // updates inside get proper isSVG flag at runtime. (#639, #643)
      // This is technically web-specific, but splitting the logic out of core
      // leads to too much unnecessary complexity.
      (tag === "svg" || tag === "foreignObject" || tag === "math")
    );
    if (props.length > 0) {
      const propsBuildResult = buildProps(
        node,
        context,
        void 0,
        isComponent2,
        isDynamicComponent
      );
      vnodeProps = propsBuildResult.props;
      patchFlag = propsBuildResult.patchFlag;
      dynamicPropNames = propsBuildResult.dynamicPropNames;
      const directives = propsBuildResult.directives;
      vnodeDirectives = directives && directives.length ? createArrayExpression(
        directives.map((dir) => buildDirectiveArgs(dir, context))
      ) : void 0;
      if (propsBuildResult.shouldUseBlock) {
        shouldUseBlock = true;
      }
    }
    if (node.children.length > 0) {
      if (vnodeTag === KEEP_ALIVE) {
        shouldUseBlock = true;
        patchFlag |= 1024;
      }
      const shouldBuildAsSlots = isComponent2 && // Teleport is not a real component and has dedicated runtime handling
      vnodeTag !== TELEPORT && // explained above.
      vnodeTag !== KEEP_ALIVE;
      if (shouldBuildAsSlots) {
        const { slots, hasDynamicSlots } = buildSlots(node, context);
        vnodeChildren = slots;
        if (hasDynamicSlots) {
          patchFlag |= 1024;
        }
      } else if (node.children.length === 1 && vnodeTag !== TELEPORT) {
        const child = node.children[0];
        const type = child.type;
        const hasDynamicTextChild = type === 5 || type === 8;
        if (hasDynamicTextChild && getConstantType(child, context) === 0) {
          patchFlag |= 1;
        }
        if (hasDynamicTextChild || type === 2) {
          vnodeChildren = child;
        } else {
          vnodeChildren = node.children;
        }
      } else {
        vnodeChildren = node.children;
      }
    }
    if (dynamicPropNames && dynamicPropNames.length) {
      vnodeDynamicProps = stringifyDynamicPropNames(dynamicPropNames);
    }
    node.codegenNode = createVNodeCall(
      context,
      vnodeTag,
      vnodeProps,
      vnodeChildren,
      patchFlag === 0 ? void 0 : patchFlag,
      vnodeDynamicProps,
      vnodeDirectives,
      !!shouldUseBlock,
      false,
      isComponent2,
      node.loc
    );
  };
};
function resolveComponentType(node, context, ssr = false) {
  let { tag } = node;
  const isExplicitDynamic = isComponentTag(tag);
  const isProp = findProp(
    node,
    "is",
    false,
    true
    /* allow empty */
  );
  if (isProp) {
    if (isExplicitDynamic || isCompatEnabled(
      "COMPILER_IS_ON_ELEMENT",
      context
    )) {
      let exp;
      if (isProp.type === 6) {
        exp = isProp.value && createSimpleExpression(isProp.value.content, true);
      } else {
        exp = isProp.exp;
        if (!exp) {
          exp = createSimpleExpression(`is`, false, isProp.arg.loc);
        }
      }
      if (exp) {
        return createCallExpression(context.helper(RESOLVE_DYNAMIC_COMPONENT), [
          exp
        ]);
      }
    } else if (isProp.type === 6 && isProp.value.content.startsWith("vue:")) {
      tag = isProp.value.content.slice(4);
    }
  }
  const builtIn = isCoreComponent(tag) || context.isBuiltInComponent(tag);
  if (builtIn) {
    if (!ssr) context.helper(builtIn);
    return builtIn;
  }
  context.helper(RESOLVE_COMPONENT);
  context.components.add(tag);
  return toValidAssetId(tag, `component`);
}
function buildProps(node, context, props = node.props, isComponent2, isDynamicComponent, ssr = false) {
  const { tag, loc: elementLoc, children } = node;
  let properties = [];
  const mergeArgs = [];
  const runtimeDirectives = [];
  const hasChildren = children.length > 0;
  let shouldUseBlock = false;
  let patchFlag = 0;
  let hasRef = false;
  let hasClassBinding = false;
  let hasStyleBinding = false;
  let hasHydrationEventBinding = false;
  let hasDynamicKeys = false;
  let hasVnodeHook = false;
  const dynamicPropNames = [];
  const pushMergeArg = (arg) => {
    if (properties.length) {
      mergeArgs.push(
        createObjectExpression(dedupeProperties(properties), elementLoc)
      );
      properties = [];
    }
    if (arg) mergeArgs.push(arg);
  };
  const pushRefVForMarker = () => {
    if (context.scopes.vFor > 0) {
      properties.push(
        createObjectProperty(
          createSimpleExpression("ref_for", true),
          createSimpleExpression("true")
        )
      );
    }
  };
  const analyzePatchFlag = ({ key, value }) => {
    if (isStaticExp(key)) {
      const name2 = key.content;
      const isEventHandler = isOn(name2);
      if (isEventHandler && (!isComponent2 || isDynamicComponent) && // omit the flag for click handlers because hydration gives click
      // dedicated fast path.
      name2.toLowerCase() !== "onclick" && // omit v-model handlers
      name2 !== "onUpdate:modelValue" && // omit onVnodeXXX hooks
      !isReservedProp(name2)) {
        hasHydrationEventBinding = true;
      }
      if (isEventHandler && isReservedProp(name2)) {
        hasVnodeHook = true;
      }
      if (isEventHandler && value.type === 14) {
        value = value.arguments[0];
      }
      if (value.type === 20 || (value.type === 4 || value.type === 8) && getConstantType(value, context) > 0) {
        return;
      }
      if (name2 === "ref") {
        hasRef = true;
      } else if (name2 === "class") {
        hasClassBinding = true;
      } else if (name2 === "style") {
        hasStyleBinding = true;
      } else if (name2 !== "key" && !dynamicPropNames.includes(name2)) {
        dynamicPropNames.push(name2);
      }
      if (isComponent2 && (name2 === "class" || name2 === "style") && !dynamicPropNames.includes(name2)) {
        dynamicPropNames.push(name2);
      }
    } else {
      hasDynamicKeys = true;
    }
  };
  for (let i = 0; i < props.length; i++) {
    const prop = props[i];
    if (prop.type === 6) {
      const { loc, name: name2, nameLoc, value } = prop;
      let isStatic = true;
      if (name2 === "ref") {
        hasRef = true;
        pushRefVForMarker();
      }
      if (name2 === "is" && (isComponentTag(tag) || value && value.content.startsWith("vue:") || isCompatEnabled(
        "COMPILER_IS_ON_ELEMENT",
        context
      ))) {
        continue;
      }
      properties.push(
        createObjectProperty(
          createSimpleExpression(name2, true, nameLoc),
          createSimpleExpression(
            value ? value.content : "",
            isStatic,
            value ? value.loc : loc
          )
        )
      );
    } else {
      const { name: name2, arg, exp, loc, modifiers } = prop;
      const isVBind = name2 === "bind";
      const isVOn = name2 === "on";
      if (name2 === "slot") {
        if (!isComponent2) {
          context.onError(
            createCompilerError(40, loc)
          );
        }
        continue;
      }
      if (name2 === "once" || name2 === "memo") {
        continue;
      }
      if (name2 === "is" || isVBind && isStaticArgOf(arg, "is") && (isComponentTag(tag) || isCompatEnabled(
        "COMPILER_IS_ON_ELEMENT",
        context
      ))) {
        continue;
      }
      if (isVOn && ssr) {
        continue;
      }
      if (
        // #938: elements with dynamic keys should be forced into blocks
        isVBind && isStaticArgOf(arg, "key") || // inline before-update hooks need to force block so that it is invoked
        // before children
        isVOn && hasChildren && isStaticArgOf(arg, "vue:before-update")
      ) {
        shouldUseBlock = true;
      }
      if (isVBind && isStaticArgOf(arg, "ref")) {
        pushRefVForMarker();
      }
      if (!arg && (isVBind || isVOn)) {
        hasDynamicKeys = true;
        if (exp) {
          if (isVBind) {
            {
              pushMergeArg();
              if (isCompatEnabled(
                "COMPILER_V_BIND_OBJECT_ORDER",
                context
              )) {
                mergeArgs.unshift(exp);
                continue;
              }
            }
            pushRefVForMarker();
            pushMergeArg();
            mergeArgs.push(exp);
          } else {
            pushMergeArg({
              type: 14,
              loc,
              callee: context.helper(TO_HANDLERS),
              arguments: isComponent2 ? [exp] : [exp, `true`]
            });
          }
        } else {
          context.onError(
            createCompilerError(
              isVBind ? 34 : 35,
              loc
            )
          );
        }
        continue;
      }
      if (isVBind && modifiers.some((mod) => mod.content === "prop")) {
        patchFlag |= 32;
      }
      const directiveTransform = context.directiveTransforms[name2];
      if (directiveTransform) {
        const { props: props2, needRuntime } = directiveTransform(prop, node, context);
        !ssr && props2.forEach(analyzePatchFlag);
        if (isVOn && arg && !isStaticExp(arg)) {
          pushMergeArg(createObjectExpression(props2, elementLoc));
        } else {
          properties.push(...props2);
        }
        if (needRuntime) {
          runtimeDirectives.push(prop);
          if (isSymbol(needRuntime)) {
            directiveImportMap.set(prop, needRuntime);
          }
        }
      } else if (!isBuiltInDirective(name2)) {
        runtimeDirectives.push(prop);
        if (hasChildren) {
          shouldUseBlock = true;
        }
      }
    }
  }
  let propsExpression = void 0;
  if (mergeArgs.length) {
    pushMergeArg();
    if (mergeArgs.length > 1) {
      propsExpression = createCallExpression(
        context.helper(MERGE_PROPS),
        mergeArgs,
        elementLoc
      );
    } else {
      propsExpression = mergeArgs[0];
    }
  } else if (properties.length) {
    propsExpression = createObjectExpression(
      dedupeProperties(properties),
      elementLoc
    );
  }
  if (hasDynamicKeys) {
    patchFlag |= 16;
  } else {
    if (hasClassBinding && !isComponent2) {
      patchFlag |= 2;
    }
    if (hasStyleBinding && !isComponent2) {
      patchFlag |= 4;
    }
    if (dynamicPropNames.length) {
      patchFlag |= 8;
    }
    if (hasHydrationEventBinding) {
      patchFlag |= 32;
    }
  }
  if (!shouldUseBlock && (patchFlag === 0 || patchFlag === 32) && (hasRef || hasVnodeHook || runtimeDirectives.length > 0)) {
    patchFlag |= 512;
  }
  if (!context.inSSR && propsExpression) {
    switch (propsExpression.type) {
      case 15:
        let classKeyIndex = -1;
        let styleKeyIndex = -1;
        let hasDynamicKey = false;
        for (let i = 0; i < propsExpression.properties.length; i++) {
          const key = propsExpression.properties[i].key;
          if (isStaticExp(key)) {
            if (key.content === "class") {
              classKeyIndex = i;
            } else if (key.content === "style") {
              styleKeyIndex = i;
            }
          } else if (!key.isHandlerKey) {
            hasDynamicKey = true;
          }
        }
        const classProp = propsExpression.properties[classKeyIndex];
        const styleProp = propsExpression.properties[styleKeyIndex];
        if (!hasDynamicKey) {
          if (classProp && !isStaticExp(classProp.value)) {
            classProp.value = createCallExpression(
              context.helper(NORMALIZE_CLASS),
              [classProp.value]
            );
          }
          if (styleProp && // the static style is compiled into an object,
          // so use `hasStyleBinding` to ensure that it is a dynamic style binding
          (hasStyleBinding || styleProp.value.type === 4 && styleProp.value.content.trim()[0] === `[` || // v-bind:style and style both exist,
          // v-bind:style with static literal object
          styleProp.value.type === 17)) {
            styleProp.value = createCallExpression(
              context.helper(NORMALIZE_STYLE),
              [styleProp.value]
            );
          }
        } else {
          propsExpression = createCallExpression(
            context.helper(NORMALIZE_PROPS),
            [propsExpression]
          );
        }
        break;
      case 14:
        break;
      default:
        propsExpression = createCallExpression(
          context.helper(NORMALIZE_PROPS),
          [
            createCallExpression(context.helper(GUARD_REACTIVE_PROPS), [
              propsExpression
            ])
          ]
        );
        break;
    }
  }
  return {
    props: propsExpression,
    directives: runtimeDirectives,
    patchFlag,
    dynamicPropNames,
    shouldUseBlock
  };
}
function dedupeProperties(properties) {
  const knownProps = /* @__PURE__ */ new Map();
  const deduped = [];
  for (let i = 0; i < properties.length; i++) {
    const prop = properties[i];
    if (prop.key.type === 8 || !prop.key.isStatic) {
      deduped.push(prop);
      continue;
    }
    const name2 = prop.key.content;
    const existing = knownProps.get(name2);
    if (existing) {
      if (name2 === "style" || name2 === "class" || isOn(name2)) {
        mergeAsArray(existing, prop);
      }
    } else {
      knownProps.set(name2, prop);
      deduped.push(prop);
    }
  }
  return deduped;
}
function mergeAsArray(existing, incoming) {
  if (existing.value.type === 17) {
    existing.value.elements.push(incoming.value);
  } else {
    existing.value = createArrayExpression(
      [existing.value, incoming.value],
      existing.loc
    );
  }
}
function buildDirectiveArgs(dir, context) {
  const dirArgs = [];
  const runtime = directiveImportMap.get(dir);
  if (runtime) {
    dirArgs.push(context.helperString(runtime));
  } else {
    {
      context.helper(RESOLVE_DIRECTIVE);
      context.directives.add(dir.name);
      dirArgs.push(toValidAssetId(dir.name, `directive`));
    }
  }
  const { loc } = dir;
  if (dir.exp) dirArgs.push(dir.exp);
  if (dir.arg) {
    if (!dir.exp) {
      dirArgs.push(`void 0`);
    }
    dirArgs.push(dir.arg);
  }
  if (Object.keys(dir.modifiers).length) {
    if (!dir.arg) {
      if (!dir.exp) {
        dirArgs.push(`void 0`);
      }
      dirArgs.push(`void 0`);
    }
    const trueExpression = createSimpleExpression(`true`, false, loc);
    dirArgs.push(
      createObjectExpression(
        dir.modifiers.map(
          (modifier) => createObjectProperty(modifier, trueExpression)
        ),
        loc
      )
    );
  }
  return createArrayExpression(dirArgs, dir.loc);
}
function stringifyDynamicPropNames(props) {
  let propsNamesString = `[`;
  for (let i = 0, l = props.length; i < l; i++) {
    propsNamesString += JSON.stringify(props[i]);
    if (i < l - 1) propsNamesString += ", ";
  }
  return propsNamesString + `]`;
}
function isComponentTag(tag) {
  return tag === "component" || tag === "Component";
}
const transformSlotOutlet = (node, context) => {
  if (isSlotOutlet(node)) {
    const { children, loc } = node;
    const { slotName, slotProps } = processSlotOutlet(node, context);
    const slotArgs = [
      context.prefixIdentifiers ? `_ctx.$slots` : `$slots`,
      slotName,
      "{}",
      "undefined",
      "true"
    ];
    let expectedLen = 2;
    if (slotProps) {
      slotArgs[2] = slotProps;
      expectedLen = 3;
    }
    if (children.length) {
      slotArgs[3] = createFunctionExpression([], children, false, false, loc);
      expectedLen = 4;
    }
    if (context.scopeId && !context.slotted) {
      expectedLen = 5;
    }
    slotArgs.splice(expectedLen);
    node.codegenNode = createCallExpression(
      context.helper(RENDER_SLOT),
      slotArgs,
      loc
    );
  }
};
function processSlotOutlet(node, context) {
  let slotName = `"default"`;
  let slotProps = void 0;
  const nonNameProps = [];
  for (let i = 0; i < node.props.length; i++) {
    const p2 = node.props[i];
    if (p2.type === 6) {
      if (p2.value) {
        if (p2.name === "name") {
          slotName = JSON.stringify(p2.value.content);
        } else {
          p2.name = camelize(p2.name);
          nonNameProps.push(p2);
        }
      }
    } else {
      if (p2.name === "bind" && isStaticArgOf(p2.arg, "name")) {
        if (p2.exp) {
          slotName = p2.exp;
        } else if (p2.arg && p2.arg.type === 4) {
          const name2 = camelize(p2.arg.content);
          slotName = p2.exp = createSimpleExpression(name2, false, p2.arg.loc);
        }
      } else {
        if (p2.name === "bind" && p2.arg && isStaticExp(p2.arg)) {
          p2.arg.content = camelize(p2.arg.content);
        }
        nonNameProps.push(p2);
      }
    }
  }
  if (nonNameProps.length > 0) {
    const { props, directives } = buildProps(
      node,
      context,
      nonNameProps,
      false,
      false
    );
    slotProps = props;
    if (directives.length) {
      context.onError(
        createCompilerError(
          36,
          directives[0].loc
        )
      );
    }
  }
  return {
    slotName,
    slotProps
  };
}
const transformOn$1 = (dir, node, context, augmentor) => {
  const { loc, modifiers, arg } = dir;
  if (!dir.exp && !modifiers.length) {
    context.onError(createCompilerError(35, loc));
  }
  let eventName;
  if (arg.type === 4) {
    if (arg.isStatic) {
      let rawName = arg.content;
      if (rawName.startsWith("vue:")) {
        rawName = `vnode-${rawName.slice(4)}`;
      }
      const eventString = node.tagType !== 0 || rawName.startsWith("vnode") || !/[A-Z]/.test(rawName) ? (
        // for non-element and vnode lifecycle event listeners, auto convert
        // it to camelCase. See issue #2249
        toHandlerKey(camelize(rawName))
      ) : (
        // preserve case for plain element listeners that have uppercase
        // letters, as these may be custom elements' custom events
        `on:${rawName}`
      );
      eventName = createSimpleExpression(eventString, true, arg.loc);
    } else {
      eventName = createCompoundExpression([
        `${context.helperString(TO_HANDLER_KEY)}(`,
        arg,
        `)`
      ]);
    }
  } else {
    eventName = arg;
    eventName.children.unshift(`${context.helperString(TO_HANDLER_KEY)}(`);
    eventName.children.push(`)`);
  }
  let exp = dir.exp;
  if (exp && !exp.content.trim()) {
    exp = void 0;
  }
  let shouldCache = context.cacheHandlers && !exp && !context.inVOnce;
  if (exp) {
    const isMemberExp = isMemberExpression(exp);
    const isInlineStatement = !(isMemberExp || isFnExpression(exp));
    const hasMultipleStatements = exp.content.includes(`;`);
    if (isInlineStatement || shouldCache && isMemberExp) {
      exp = createCompoundExpression([
        `${isInlineStatement ? `$event` : `${``}(...args)`} => ${hasMultipleStatements ? `{` : `(`}`,
        exp,
        hasMultipleStatements ? `}` : `)`
      ]);
    }
  }
  let ret = {
    props: [
      createObjectProperty(
        eventName,
        exp || createSimpleExpression(`() => {}`, false, loc)
      )
    ]
  };
  if (augmentor) {
    ret = augmentor(ret);
  }
  if (shouldCache) {
    ret.props[0].value = context.cache(ret.props[0].value);
  }
  ret.props.forEach((p2) => p2.key.isHandlerKey = true);
  return ret;
};
const transformBind = (dir, _node, context) => {
  const { modifiers, loc } = dir;
  const arg = dir.arg;
  let { exp } = dir;
  if (exp && exp.type === 4 && !exp.content.trim()) {
    {
      exp = void 0;
    }
  }
  if (arg.type !== 4) {
    arg.children.unshift(`(`);
    arg.children.push(`) || ""`);
  } else if (!arg.isStatic) {
    arg.content = arg.content ? `${arg.content} || ""` : `""`;
  }
  if (modifiers.some((mod) => mod.content === "camel")) {
    if (arg.type === 4) {
      if (arg.isStatic) {
        arg.content = camelize(arg.content);
      } else {
        arg.content = `${context.helperString(CAMELIZE)}(${arg.content})`;
      }
    } else {
      arg.children.unshift(`${context.helperString(CAMELIZE)}(`);
      arg.children.push(`)`);
    }
  }
  if (!context.inSSR) {
    if (modifiers.some((mod) => mod.content === "prop")) {
      injectPrefix(arg, ".");
    }
    if (modifiers.some((mod) => mod.content === "attr")) {
      injectPrefix(arg, "^");
    }
  }
  return {
    props: [createObjectProperty(arg, exp)]
  };
};
const injectPrefix = (arg, prefix) => {
  if (arg.type === 4) {
    if (arg.isStatic) {
      arg.content = prefix + arg.content;
    } else {
      arg.content = `\`${prefix}\${${arg.content}}\``;
    }
  } else {
    arg.children.unshift(`'${prefix}' + (`);
    arg.children.push(`)`);
  }
};
const transformText = (node, context) => {
  if (node.type === 0 || node.type === 1 || node.type === 11 || node.type === 10) {
    return () => {
      const children = node.children;
      let currentContainer = void 0;
      let hasText = false;
      for (let i = 0; i < children.length; i++) {
        const child = children[i];
        if (isText$1(child)) {
          hasText = true;
          for (let j = i + 1; j < children.length; j++) {
            const next = children[j];
            if (isText$1(next)) {
              if (!currentContainer) {
                currentContainer = children[i] = createCompoundExpression(
                  [child],
                  child.loc
                );
              }
              currentContainer.children.push(` + `, next);
              children.splice(j, 1);
              j--;
            } else {
              currentContainer = void 0;
              break;
            }
          }
        }
      }
      if (!hasText || // if this is a plain element with a single text child, leave it
      // as-is since the runtime has dedicated fast path for this by directly
      // setting textContent of the element.
      // for component root it's always normalized anyway.
      children.length === 1 && (node.type === 0 || node.type === 1 && node.tagType === 0 && // #3756
      // custom directives can potentially add DOM elements arbitrarily,
      // we need to avoid setting textContent of the element at runtime
      // to avoid accidentally overwriting the DOM elements added
      // by the user through custom directives.
      !node.props.find(
        (p2) => p2.type === 7 && !context.directiveTransforms[p2.name]
      ) && // in compat mode, <template> tags with no special directives
      // will be rendered as a fragment so its children must be
      // converted into vnodes.
      !(node.tag === "template"))) {
        return;
      }
      for (let i = 0; i < children.length; i++) {
        const child = children[i];
        if (isText$1(child) || child.type === 8) {
          const callArgs = [];
          if (child.type !== 2 || child.content !== " ") {
            callArgs.push(child);
          }
          if (!context.ssr && getConstantType(child, context) === 0) {
            callArgs.push(
              `1`
            );
          }
          children[i] = {
            type: 12,
            content: child,
            loc: child.loc,
            codegenNode: createCallExpression(
              context.helper(CREATE_TEXT),
              callArgs
            )
          };
        }
      }
    };
  }
};
const seen$1 = /* @__PURE__ */ new WeakSet();
const transformOnce = (node, context) => {
  if (node.type === 1 && findDir(node, "once", true)) {
    if (seen$1.has(node) || context.inVOnce || context.inSSR) {
      return;
    }
    seen$1.add(node);
    context.inVOnce = true;
    context.helper(SET_BLOCK_TRACKING);
    return () => {
      context.inVOnce = false;
      const cur = context.currentNode;
      if (cur.codegenNode) {
        cur.codegenNode = context.cache(
          cur.codegenNode,
          true,
          true
        );
      }
    };
  }
};
const transformModel$1 = (dir, node, context) => {
  const { exp, arg } = dir;
  if (!exp) {
    context.onError(
      createCompilerError(41, dir.loc)
    );
    return createTransformProps();
  }
  const rawExp = exp.loc.source.trim();
  const expString = exp.type === 4 ? exp.content : rawExp;
  const bindingType = context.bindingMetadata[rawExp];
  if (bindingType === "props" || bindingType === "props-aliased") {
    context.onError(createCompilerError(44, exp.loc));
    return createTransformProps();
  }
  if (!expString.trim() || !isMemberExpression(exp) && true) {
    context.onError(
      createCompilerError(42, exp.loc)
    );
    return createTransformProps();
  }
  const propName = arg ? arg : createSimpleExpression("modelValue", true);
  const eventName = arg ? isStaticExp(arg) ? `onUpdate:${camelize(arg.content)}` : createCompoundExpression(['"onUpdate:" + ', arg]) : `onUpdate:modelValue`;
  let assignmentExp;
  const eventArg = context.isTS ? `($event: any)` : `$event`;
  {
    assignmentExp = createCompoundExpression([
      `${eventArg} => ((`,
      exp,
      `) = $event)`
    ]);
  }
  const props = [
    // modelValue: foo
    createObjectProperty(propName, dir.exp),
    // "onUpdate:modelValue": $event => (foo = $event)
    createObjectProperty(eventName, assignmentExp)
  ];
  if (dir.modifiers.length && node.tagType === 1) {
    const modifiers = dir.modifiers.map((m) => m.content).map((m) => (isSimpleIdentifier(m) ? m : JSON.stringify(m)) + `: true`).join(`, `);
    const modifiersKey = arg ? isStaticExp(arg) ? `${arg.content}Modifiers` : createCompoundExpression([arg, ' + "Modifiers"']) : `modelModifiers`;
    props.push(
      createObjectProperty(
        modifiersKey,
        createSimpleExpression(
          `{ ${modifiers} }`,
          false,
          dir.loc,
          2
        )
      )
    );
  }
  return createTransformProps(props);
};
function createTransformProps(props = []) {
  return { props };
}
const validDivisionCharRE = /[\w).+\-_$\]]/;
const transformFilter = (node, context) => {
  if (!isCompatEnabled("COMPILER_FILTERS", context)) {
    return;
  }
  if (node.type === 5) {
    rewriteFilter(node.content, context);
  } else if (node.type === 1) {
    node.props.forEach((prop) => {
      if (prop.type === 7 && prop.name !== "for" && prop.exp) {
        rewriteFilter(prop.exp, context);
      }
    });
  }
};
function rewriteFilter(node, context) {
  if (node.type === 4) {
    parseFilter(node, context);
  } else {
    for (let i = 0; i < node.children.length; i++) {
      const child = node.children[i];
      if (typeof child !== "object") continue;
      if (child.type === 4) {
        parseFilter(child, context);
      } else if (child.type === 8) {
        rewriteFilter(node, context);
      } else if (child.type === 5) {
        rewriteFilter(child.content, context);
      }
    }
  }
}
function parseFilter(node, context) {
  const exp = node.content;
  let inSingle = false;
  let inDouble = false;
  let inTemplateString = false;
  let inRegex = false;
  let curly = 0;
  let square = 0;
  let paren = 0;
  let lastFilterIndex = 0;
  let c, prev, i, expression, filters = [];
  for (i = 0; i < exp.length; i++) {
    prev = c;
    c = exp.charCodeAt(i);
    if (inSingle) {
      if (c === 39 && prev !== 92) inSingle = false;
    } else if (inDouble) {
      if (c === 34 && prev !== 92) inDouble = false;
    } else if (inTemplateString) {
      if (c === 96 && prev !== 92) inTemplateString = false;
    } else if (inRegex) {
      if (c === 47 && prev !== 92) inRegex = false;
    } else if (c === 124 && // pipe
    exp.charCodeAt(i + 1) !== 124 && exp.charCodeAt(i - 1) !== 124 && !curly && !square && !paren) {
      if (expression === void 0) {
        lastFilterIndex = i + 1;
        expression = exp.slice(0, i).trim();
      } else {
        pushFilter();
      }
    } else {
      switch (c) {
        case 34:
          inDouble = true;
          break;
        // "
        case 39:
          inSingle = true;
          break;
        // '
        case 96:
          inTemplateString = true;
          break;
        // `
        case 40:
          paren++;
          break;
        // (
        case 41:
          paren--;
          break;
        // )
        case 91:
          square++;
          break;
        // [
        case 93:
          square--;
          break;
        // ]
        case 123:
          curly++;
          break;
        // {
        case 125:
          curly--;
          break;
      }
      if (c === 47) {
        let j = i - 1;
        let p2;
        for (; j >= 0; j--) {
          p2 = exp.charAt(j);
          if (p2 !== " ") break;
        }
        if (!p2 || !validDivisionCharRE.test(p2)) {
          inRegex = true;
        }
      }
    }
  }
  if (expression === void 0) {
    expression = exp.slice(0, i).trim();
  } else if (lastFilterIndex !== 0) {
    pushFilter();
  }
  function pushFilter() {
    filters.push(exp.slice(lastFilterIndex, i).trim());
    lastFilterIndex = i + 1;
  }
  if (filters.length) {
    for (i = 0; i < filters.length; i++) {
      expression = wrapFilter(expression, filters[i], context);
    }
    node.content = expression;
    node.ast = void 0;
  }
}
function wrapFilter(exp, filter, context) {
  context.helper(RESOLVE_FILTER);
  const i = filter.indexOf("(");
  if (i < 0) {
    context.filters.add(filter);
    return `${toValidAssetId(filter, "filter")}(${exp})`;
  } else {
    const name2 = filter.slice(0, i);
    const args = filter.slice(i + 1);
    context.filters.add(name2);
    return `${toValidAssetId(name2, "filter")}(${exp}${args !== ")" ? "," + args : args}`;
  }
}
const seen = /* @__PURE__ */ new WeakSet();
const transformMemo = (node, context) => {
  if (node.type === 1) {
    const dir = findDir(node, "memo");
    if (!dir || seen.has(node) || context.inSSR) {
      return;
    }
    seen.add(node);
    return () => {
      const codegenNode = node.codegenNode || context.currentNode.codegenNode;
      if (codegenNode && codegenNode.type === 13) {
        if (node.tagType !== 1) {
          convertToBlock(codegenNode, context);
        }
        node.codegenNode = createCallExpression(context.helper(WITH_MEMO), [
          dir.exp,
          createFunctionExpression(void 0, codegenNode),
          `_cache`,
          String(context.cached.length)
        ]);
        context.cached.push(null);
      }
    };
  }
};
const transformVBindShorthand = (node, context) => {
  if (node.type === 1) {
    for (const prop of node.props) {
      if (prop.type === 7 && prop.name === "bind" && (!prop.exp || // #13930 :foo in in-DOM templates will be parsed into :foo="" by browser
      prop.exp.type === 4 && !prop.exp.content.trim()) && prop.arg) {
        const arg = prop.arg;
        if (arg.type !== 4 || !arg.isStatic) {
          context.onError(
            createCompilerError(
              52,
              arg.loc
            )
          );
          prop.exp = createSimpleExpression("", true, arg.loc);
        } else {
          const propName = camelize(arg.content);
          if (validFirstIdentCharRE.test(propName[0]) || // allow hyphen first char for https://github.com/vuejs/language-tools/pull/3424
          propName[0] === "-") {
            prop.exp = createSimpleExpression(propName, false, arg.loc);
          }
        }
      }
    }
  }
};
function getBaseTransformPreset(prefixIdentifiers) {
  return [
    [
      transformVBindShorthand,
      transformOnce,
      transformIf,
      transformMemo,
      transformFor,
      ...[transformFilter],
      ...[],
      transformSlotOutlet,
      transformElement,
      trackSlotScopes,
      transformText
    ],
    {
      on: transformOn$1,
      bind: transformBind,
      model: transformModel$1
    }
  ];
}
function baseCompile(source, options = {}) {
  const onError = options.onError || defaultOnError;
  const isModuleMode = options.mode === "module";
  {
    if (options.prefixIdentifiers === true) {
      onError(createCompilerError(47));
    } else if (isModuleMode) {
      onError(createCompilerError(48));
    }
  }
  const prefixIdentifiers = false;
  if (options.cacheHandlers) {
    onError(createCompilerError(49));
  }
  if (options.scopeId && !isModuleMode) {
    onError(createCompilerError(50));
  }
  const resolvedOptions = extend({}, options, {
    prefixIdentifiers
  });
  const ast = isString(source) ? baseParse(source, resolvedOptions) : source;
  const [nodeTransforms, directiveTransforms] = getBaseTransformPreset();
  transform(
    ast,
    extend({}, resolvedOptions, {
      nodeTransforms: [
        ...nodeTransforms,
        ...options.nodeTransforms || []
        // user transforms
      ],
      directiveTransforms: extend(
        {},
        directiveTransforms,
        options.directiveTransforms || {}
        // user transforms
      )
    })
  );
  return generate(ast, resolvedOptions);
}
const noopDirectiveTransform = () => ({ props: [] });
const V_MODEL_RADIO = Symbol(``);
const V_MODEL_CHECKBOX = Symbol(
  ``
);
const V_MODEL_TEXT = Symbol(``);
const V_MODEL_SELECT = Symbol(
  ``
);
const V_MODEL_DYNAMIC = Symbol(
  ``
);
const V_ON_WITH_MODIFIERS = Symbol(
  ``
);
const V_ON_WITH_KEYS = Symbol(
  ``
);
const V_SHOW = Symbol(``);
const TRANSITION = Symbol(``);
const TRANSITION_GROUP = Symbol(
  ``
);
registerRuntimeHelpers({
  [V_MODEL_RADIO]: `vModelRadio`,
  [V_MODEL_CHECKBOX]: `vModelCheckbox`,
  [V_MODEL_TEXT]: `vModelText`,
  [V_MODEL_SELECT]: `vModelSelect`,
  [V_MODEL_DYNAMIC]: `vModelDynamic`,
  [V_ON_WITH_MODIFIERS]: `withModifiers`,
  [V_ON_WITH_KEYS]: `withKeys`,
  [V_SHOW]: `vShow`,
  [TRANSITION]: `Transition`,
  [TRANSITION_GROUP]: `TransitionGroup`
});
let decoder;
function decodeHtmlBrowser(raw, asAttr = false) {
  if (!decoder) {
    decoder = document.createElement("div");
  }
  if (asAttr) {
    decoder.innerHTML = `<div foo="${raw.replace(/"/g, "&quot;")}">`;
    return decoder.children[0].getAttribute("foo");
  } else {
    decoder.innerHTML = raw;
    return decoder.textContent;
  }
}
const parserOptions = {
  parseMode: "html",
  isVoidTag,
  isNativeTag: (tag) => isHTMLTag(tag) || isSVGTag(tag) || isMathMLTag(tag),
  isPreTag: (tag) => tag === "pre",
  isIgnoreNewlineTag: (tag) => tag === "pre" || tag === "textarea",
  decodeEntities: decodeHtmlBrowser,
  isBuiltInComponent: (tag) => {
    if (tag === "Transition" || tag === "transition") {
      return TRANSITION;
    } else if (tag === "TransitionGroup" || tag === "transition-group") {
      return TRANSITION_GROUP;
    }
  },
  // https://html.spec.whatwg.org/multipage/parsing.html#tree-construction-dispatcher
  getNamespace(tag, parent, rootNamespace) {
    let ns = parent ? parent.ns : rootNamespace;
    if (parent && ns === 2) {
      if (parent.tag === "annotation-xml") {
        if (tag === "svg") {
          return 1;
        }
        if (parent.props.some(
          (a) => a.type === 6 && a.name === "encoding" && a.value != null && (a.value.content === "text/html" || a.value.content === "application/xhtml+xml")
        )) {
          ns = 0;
        }
      } else if (/^m(?:[ions]|text)$/.test(parent.tag) && tag !== "mglyph" && tag !== "malignmark") {
        ns = 0;
      }
    } else if (parent && ns === 1) {
      if (parent.tag === "foreignObject" || parent.tag === "desc" || parent.tag === "title") {
        ns = 0;
      }
    }
    if (ns === 0) {
      if (tag === "svg") {
        return 1;
      }
      if (tag === "math") {
        return 2;
      }
    }
    return ns;
  }
};
const transformStyle = (node) => {
  if (node.type === 1) {
    node.props.forEach((p2, i) => {
      if (p2.type === 6 && p2.name === "style" && p2.value) {
        node.props[i] = {
          type: 7,
          name: `bind`,
          arg: createSimpleExpression(`style`, true, p2.loc),
          exp: parseInlineCSS(p2.value.content, p2.loc),
          modifiers: [],
          loc: p2.loc
        };
      }
    });
  }
};
const parseInlineCSS = (cssText, loc) => {
  const normalized = parseStringStyle(cssText);
  return createSimpleExpression(
    JSON.stringify(normalized),
    false,
    loc,
    3
  );
};
function createDOMCompilerError(code, loc) {
  return createCompilerError(
    code,
    loc
  );
}
const transformVHtml = (dir, node, context) => {
  const { exp, loc } = dir;
  if (!exp) {
    context.onError(
      createDOMCompilerError(53, loc)
    );
  }
  if (node.children.length) {
    context.onError(
      createDOMCompilerError(54, loc)
    );
    node.children.length = 0;
  }
  return {
    props: [
      createObjectProperty(
        createSimpleExpression(`innerHTML`, true, loc),
        exp || createSimpleExpression("", true)
      )
    ]
  };
};
const transformVText = (dir, node, context) => {
  const { exp, loc } = dir;
  if (!exp) {
    context.onError(
      createDOMCompilerError(55, loc)
    );
  }
  if (node.children.length) {
    context.onError(
      createDOMCompilerError(56, loc)
    );
    node.children.length = 0;
  }
  return {
    props: [
      createObjectProperty(
        createSimpleExpression(`textContent`, true),
        exp ? getConstantType(exp, context) > 0 ? exp : createCallExpression(
          context.helperString(TO_DISPLAY_STRING),
          [exp],
          loc
        ) : createSimpleExpression("", true)
      )
    ]
  };
};
const transformModel = (dir, node, context) => {
  const baseResult = transformModel$1(dir, node, context);
  if (!baseResult.props.length || node.tagType === 1) {
    return baseResult;
  }
  if (dir.arg) {
    context.onError(
      createDOMCompilerError(
        58,
        dir.arg.loc
      )
    );
  }
  const { tag } = node;
  const isCustomElement = context.isCustomElement(tag);
  if (tag === "input" || tag === "textarea" || tag === "select" || isCustomElement) {
    let directiveToUse = V_MODEL_TEXT;
    let isInvalidType = false;
    if (tag === "input" || isCustomElement) {
      const type = findProp(node, `type`);
      if (type) {
        if (type.type === 7) {
          directiveToUse = V_MODEL_DYNAMIC;
        } else if (type.value) {
          switch (type.value.content) {
            case "radio":
              directiveToUse = V_MODEL_RADIO;
              break;
            case "checkbox":
              directiveToUse = V_MODEL_CHECKBOX;
              break;
            case "file":
              isInvalidType = true;
              context.onError(
                createDOMCompilerError(
                  59,
                  dir.loc
                )
              );
              break;
          }
        }
      } else if (hasDynamicKeyVBind(node)) {
        directiveToUse = V_MODEL_DYNAMIC;
      } else ;
    } else if (tag === "select") {
      directiveToUse = V_MODEL_SELECT;
    } else ;
    if (!isInvalidType) {
      baseResult.needRuntime = context.helper(directiveToUse);
    }
  } else {
    context.onError(
      createDOMCompilerError(
        57,
        dir.loc
      )
    );
  }
  baseResult.props = baseResult.props.filter(
    (p2) => !(p2.key.type === 4 && p2.key.content === "modelValue")
  );
  return baseResult;
};
const isEventOptionModifier = /* @__PURE__ */ makeMap(`passive,once,capture`);
const isNonKeyModifier = /* @__PURE__ */ makeMap(
  // event propagation management
  `stop,prevent,self,ctrl,shift,alt,meta,exact,middle`
);
const maybeKeyModifier = /* @__PURE__ */ makeMap("left,right");
const isKeyboardEvent = /* @__PURE__ */ makeMap(`onkeyup,onkeydown,onkeypress`);
const resolveModifiers = (key, modifiers, context, loc) => {
  const keyModifiers = [];
  const nonKeyModifiers = [];
  const eventOptionModifiers = [];
  for (let i = 0; i < modifiers.length; i++) {
    const modifier = modifiers[i].content;
    if (modifier === "native" && checkCompatEnabled(
      "COMPILER_V_ON_NATIVE",
      context
    )) {
      eventOptionModifiers.push(modifier);
    } else if (isEventOptionModifier(modifier)) {
      eventOptionModifiers.push(modifier);
    } else {
      if (maybeKeyModifier(modifier)) {
        if (isStaticExp(key)) {
          if (isKeyboardEvent(key.content.toLowerCase())) {
            keyModifiers.push(modifier);
          } else {
            nonKeyModifiers.push(modifier);
          }
        } else {
          keyModifiers.push(modifier);
          nonKeyModifiers.push(modifier);
        }
      } else {
        if (isNonKeyModifier(modifier)) {
          nonKeyModifiers.push(modifier);
        } else {
          keyModifiers.push(modifier);
        }
      }
    }
  }
  return {
    keyModifiers,
    nonKeyModifiers,
    eventOptionModifiers
  };
};
const transformClick = (key, event) => {
  const isStaticClick = isStaticExp(key) && key.content.toLowerCase() === "onclick";
  return isStaticClick ? createSimpleExpression(event, true) : key.type !== 4 ? createCompoundExpression([
    `(`,
    key,
    `) === "onClick" ? "${event}" : (`,
    key,
    `)`
  ]) : key;
};
const transformOn = (dir, node, context) => {
  return transformOn$1(dir, node, context, (baseResult) => {
    const { modifiers } = dir;
    if (!modifiers.length) return baseResult;
    let { key, value: handlerExp } = baseResult.props[0];
    const { keyModifiers, nonKeyModifiers, eventOptionModifiers } = resolveModifiers(key, modifiers, context, dir.loc);
    if (nonKeyModifiers.includes("right")) {
      key = transformClick(key, `onContextmenu`);
    }
    if (nonKeyModifiers.includes("middle")) {
      key = transformClick(key, `onMouseup`);
    }
    if (nonKeyModifiers.length) {
      handlerExp = createCallExpression(context.helper(V_ON_WITH_MODIFIERS), [
        handlerExp,
        JSON.stringify(nonKeyModifiers)
      ]);
    }
    if (keyModifiers.length && // if event name is dynamic, always wrap with keys guard
    (!isStaticExp(key) || isKeyboardEvent(key.content.toLowerCase()))) {
      handlerExp = createCallExpression(context.helper(V_ON_WITH_KEYS), [
        handlerExp,
        JSON.stringify(keyModifiers)
      ]);
    }
    if (eventOptionModifiers.length) {
      const modifierPostfix = eventOptionModifiers.map(capitalize).join("");
      key = isStaticExp(key) ? createSimpleExpression(`${key.content}${modifierPostfix}`, true) : createCompoundExpression([`(`, key, `) + "${modifierPostfix}"`]);
    }
    return {
      props: [createObjectProperty(key, handlerExp)]
    };
  });
};
const transformShow = (dir, node, context) => {
  const { exp, loc } = dir;
  if (!exp) {
    context.onError(
      createDOMCompilerError(61, loc)
    );
  }
  return {
    props: [],
    needRuntime: context.helper(V_SHOW)
  };
};
const ignoreSideEffectTags = (node, context) => {
  if (node.type === 1 && node.tagType === 0 && (node.tag === "script" || node.tag === "style")) {
    context.removeNode();
  }
};
const DOMNodeTransforms = [
  transformStyle,
  ...[]
];
const DOMDirectiveTransforms = {
  cloak: noopDirectiveTransform,
  html: transformVHtml,
  text: transformVText,
  model: transformModel,
  // override compiler-core
  on: transformOn,
  // override compiler-core
  show: transformShow
};
function compile(src, options = {}) {
  return baseCompile(
    src,
    extend({}, parserOptions, options, {
      nodeTransforms: [
        // ignore <script> and <tag>
        // this is not put inside DOMNodeTransforms because that list is used
        // by compiler-ssr to generate vnode fallback branches
        ignoreSideEffectTags,
        ...DOMNodeTransforms,
        ...options.nodeTransforms || []
      ],
      directiveTransforms: extend(
        {},
        DOMDirectiveTransforms,
        options.directiveTransforms || {}
      ),
      transformHoist: null
    })
  );
}
const compileCache = /* @__PURE__ */ Object.create(null);
function compileToFunction(template, options) {
  if (!isString(template)) {
    if (template.nodeType) {
      template = template.innerHTML;
    } else {
      return NOOP;
    }
  }
  const key = genCacheKey(template, options);
  const cached = compileCache[key];
  if (cached) {
    return cached;
  }
  if (template[0] === "#") {
    const el2 = document.querySelector(template);
    template = el2 ? el2.innerHTML : ``;
  }
  const opts = extend(
    {
      hoistStatic: true,
      onError: void 0,
      onWarn: NOOP
    },
    options
  );
  if (!opts.isCustomElement && typeof customElements !== "undefined") {
    opts.isCustomElement = (tag) => !!customElements.get(tag);
  }
  const { code } = compile(template, opts);
  const render2 = new Function("Vue", code)(runtimeDom);
  render2._rc = true;
  return compileCache[key] = render2;
}
registerRuntimeCompiler(compileToFunction);
const _export_sfc = (sfc, props) => {
  const target = sfc.__vccOpts || sfc;
  for (const [key, val] of props) {
    target[key] = val;
  }
  return target;
};
const _sfc_main$5 = {
  __name: "LedEditor",
  props: {
    value: String
  },
  setup(__props, { expose: __expose }) {
    __expose();
    const props = __props;
    const count = ref(0);
    console.log("LedEditor");
    const __returned__ = { props, count, ref };
    Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
    return __returned__;
  }
};
const _hoisted_1$5 = { class: "led-editor" };
function _sfc_render$5(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", _hoisted_1$5, [
    createBaseVNode("h2", null, toDisplayString($props.value), 1),
    createBaseVNode("p", null, "Compteur: " + toDisplayString($setup.count), 1),
    createBaseVNode("button", {
      onClick: _cache[0] || (_cache[0] = ($event) => $setup.count++)
    }, "+1")
  ]);
}
const LedEditor = /* @__PURE__ */ _export_sfc(_sfc_main$5, [["render", _sfc_render$5], ["__file", "LedEditor.vue"]]);
const _sfc_main$4 = {
  __name: "ColorPicker",
  props: {
    modelValue: {
      type: String,
      default: "#000000"
    }
  },
  emits: ["update:modelValue"],
  setup(__props, { expose: __expose, emit: __emit }) {
    __expose();
    const props = __props;
    const emit2 = __emit;
    const color = computed({
      get: () => props.modelValue,
      set: (value) => emit2("update:modelValue", value)
    });
    const canvas = ref(null);
    const isDragging = ref(false);
    const pickerX = ref(0);
    const pickerY = ref(0);
    const red = ref(0);
    const green = ref(0);
    const blue = ref(0);
    const brightness = ref(100);
    const drawPicker = () => {
      const ctx = canvas.value.getContext("2d");
      const width = canvas.value.width;
      const height = canvas.value.height;
      1 - brightness.value / 100;
      if (brightness.value < 100) {
        ctx.fillStyle = "#fff";
        ctx.fillRect(0, 0, width, height);
      }
      const gradientH = ctx.createLinearGradient(0, 0, width, 0);
      gradientH.addColorStop(0, `rgba(255, 0, 0, ${brightness.value / 100})`);
      gradientH.addColorStop(0.2, `rgba(255, 255, 0, ${brightness.value / 100})`);
      gradientH.addColorStop(0.4, `rgba(0, 255, 0, ${brightness.value / 100})`);
      gradientH.addColorStop(0.6, `rgba(0, 255, 255, ${brightness.value / 100})`);
      gradientH.addColorStop(0.8, `rgba(0, 0, 255, ${brightness.value / 100})`);
      gradientH.addColorStop(1, `rgba(255, 0, 255, ${brightness.value / 100})`);
      ctx.fillStyle = gradientH;
      ctx.fillRect(0, 0, width, height);
      const gradientV = ctx.createLinearGradient(0, 0, 0, height);
      gradientV.addColorStop(0, "rgba(0, 0, 0, 0)");
      gradientV.addColorStop(1, "#000");
      ctx.fillStyle = gradientV;
      ctx.fillRect(0, 0, width, height);
    };
    const getColorAtPosition = (x2, y) => {
      const ctx = canvas.value.getContext("2d");
      const pixel = ctx.getImageData(x2, y, 1, 1).data;
      red.value = pixel[0];
      green.value = pixel[1];
      blue.value = pixel[2];
      let value = `#${((1 << 24) + (pixel[0] << 16) + (pixel[1] << 8) + pixel[2]).toString(16).slice(1)}`;
      return value;
    };
    const handleCanvasClick = (event) => {
      const rect = canvas.value.getBoundingClientRect();
      const x2 = event.clientX - rect.left;
      const y = event.clientY - rect.top;
      pickerX.value = Math.max(0, Math.min(x2, canvas.value.width));
      pickerY.value = Math.max(0, Math.min(y, canvas.value.height));
      const color2 = getColorAtPosition(pickerX.value, pickerY.value);
      emit2("update:modelValue", color2);
      drawCursor();
    };
    const handleMouseDown = (event) => {
      isDragging.value = true;
      handleCanvasClick(event);
    };
    const handleMouseMove = (event) => {
      if (isDragging.value) {
        handleCanvasClick(event);
      }
    };
    const handleMouseUp = () => {
      isDragging.value = false;
    };
    const changeColor = () => {
      const color2 = `#${((1 << 24) + (red.value << 16) + (green.value << 8) + blue.value).toString(16).slice(1)}`;
      emit2("update:modelValue", color2);
      const pos = findPositionForColor(color2);
      pickerX.value = pos.x;
      pickerY.value = pos.y;
      drawCursor();
    };
    const findPositionForColor = (hexColor) => {
      const ctx = canvas.value.getContext("2d");
      const width = canvas.value.width;
      const height = canvas.value.height;
      const r = parseInt(hexColor.slice(1, 3), 16);
      const g = parseInt(hexColor.slice(3, 5), 16);
      const b = parseInt(hexColor.slice(5, 7), 16);
      const imageData = ctx.getImageData(0, 0, width, height);
      let minDistance = Infinity;
      let bestX = 0;
      let bestY = 0;
      for (let y = 0; y < height; y += 5) {
        for (let x2 = 0; x2 < width; x2 += 5) {
          const i = (y * width + x2) * 4;
          const pixelR = imageData.data[i];
          const pixelG = imageData.data[i + 1];
          const pixelB = imageData.data[i + 2];
          const distance = Math.sqrt(
            Math.pow(r - pixelR, 2) + Math.pow(g - pixelG, 2) + Math.pow(b - pixelB, 2)
          );
          if (distance < minDistance) {
            minDistance = distance;
            bestX = x2;
            bestY = y;
          }
        }
      }
      return { x: bestX, y: bestY };
    };
    const drawCursor = () => {
      const ctx = canvas.value.getContext("2d");
      drawPicker();
      ctx.beginPath();
      ctx.arc(pickerX.value, pickerY.value, 3, 0, Math.PI / 2);
      ctx.strokeStyle = "#BEBEBE";
      ctx.lineWidth = 2;
      ctx.stroke();
      ctx.beginPath();
      ctx.arc(pickerX.value, pickerY.value, 3, Math.PI / 2, Math.PI);
      ctx.strokeStyle = "#0F0F0F";
      ctx.lineWidth = 2;
      ctx.stroke();
      ctx.beginPath();
      ctx.arc(pickerX.value, pickerY.value, 3, Math.PI, 3 * Math.PI / 2);
      ctx.strokeStyle = "#BEBEBE";
      ctx.lineWidth = 2;
      ctx.stroke();
      ctx.beginPath();
      ctx.arc(pickerX.value, pickerY.value, 3, 3 * Math.PI / 2, 2 * Math.PI);
      ctx.strokeStyle = "#0F0F0F";
      ctx.lineWidth = 2;
      ctx.stroke();
    };
    onMounted(() => {
      drawPicker();
      window.addEventListener("mouseup", handleMouseUp);
    });
    const __returned__ = { props, emit: emit2, color, canvas, isDragging, pickerX, pickerY, red, green, blue, brightness, drawPicker, getColorAtPosition, handleCanvasClick, handleMouseDown, handleMouseMove, handleMouseUp, changeColor, findPositionForColor, drawCursor, computed, ref, onMounted };
    Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
    return __returned__;
  }
};
const _hoisted_1$4 = { class: "color-picker" };
const _hoisted_2$4 = { class: "canva-wrapper" };
const _hoisted_3$4 = { class: "color-brightness-slider" };
const _hoisted_4$4 = { class: "color-value form-inline" };
const _hoisted_5$4 = { class: "form-group" };
const _hoisted_6$4 = { class: "form-group" };
const _hoisted_7$3 = { class: "form-group" };
function _sfc_render$4(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", _hoisted_1$4, [
    createBaseVNode("div", _hoisted_2$4, [
      createBaseVNode("canvas", {
        ref: "canvas",
        class: "color-picker-canva",
        width: "450",
        height: "280",
        onMousedown: $setup.handleMouseDown,
        onMousemove: $setup.handleMouseMove
      }, null, 544)
    ]),
    createBaseVNode("div", _hoisted_3$4, [
      _cache[6] || (_cache[6] = createBaseVNode("label", null, "Luminosité", -1)),
      withDirectives(createBaseVNode("input", {
        type: "range",
        min: "0",
        max: "100",
        "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.brightness = $event),
        onInput: _cache[1] || (_cache[1] = ($event) => $setup.drawPicker())
      }, null, 544), [
        [vModelText, $setup.brightness]
      ])
    ]),
    createBaseVNode("div", _hoisted_4$4, [
      createBaseVNode("div", _hoisted_5$4, [
        _cache[7] || (_cache[7] = createBaseVNode("label", {
          for: "",
          class: "form-label"
        }, "Red", -1)),
        withDirectives(createBaseVNode("input", {
          class: "form-input",
          type: "number",
          min: "0",
          max: "255",
          "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.red = $event),
          onChange: $setup.changeColor
        }, null, 544), [
          [vModelText, $setup.red]
        ])
      ]),
      createBaseVNode("div", _hoisted_6$4, [
        _cache[8] || (_cache[8] = createBaseVNode("label", {
          for: "",
          class: "form-label"
        }, "Green", -1)),
        withDirectives(createBaseVNode("input", {
          class: "form-input",
          type: "number",
          min: "0",
          max: "255",
          "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.green = $event),
          onChange: $setup.changeColor
        }, null, 544), [
          [vModelText, $setup.green]
        ])
      ]),
      createBaseVNode("div", _hoisted_7$3, [
        _cache[9] || (_cache[9] = createBaseVNode("label", {
          for: "",
          class: "form-label"
        }, "Bleu", -1)),
        withDirectives(createBaseVNode("input", {
          class: "form-input",
          type: "number",
          min: "0",
          max: "255",
          "onUpdate:modelValue": _cache[4] || (_cache[4] = ($event) => $setup.blue = $event),
          onChange: $setup.changeColor
        }, null, 544), [
          [vModelText, $setup.blue]
        ])
      ])
    ]),
    withDirectives(createBaseVNode("input", {
      type: "hidden",
      "onUpdate:modelValue": _cache[5] || (_cache[5] = ($event) => $setup.color = $event)
    }, null, 512), [
      [vModelText, $setup.color]
    ])
  ]);
}
const ColorPicker = /* @__PURE__ */ _export_sfc(_sfc_main$4, [["render", _sfc_render$4], ["__file", "ColorPicker.vue"]]);
const _sfc_main$3 = {
  __name: "IconPicker",
  props: {
    modelValue: {
      type: String,
      default: ""
    },
    id: {
      type: String,
      required: true
    },
    name: {
      type: String,
      required: true
    },
    required: {
      type: Boolean,
      default: false
    },
    icons: {
      type: Array,
      default: () => []
    },
    value: {
      type: String,
      default: ""
    }
  },
  emits: ["update:modelValue"],
  setup(__props, { expose: __expose, emit: __emit }) {
    __expose();
    const props = __props;
    const search = ref("");
    const isOpen = ref(false);
    const emit2 = __emit;
    const internalValue = ref(props.value || props.modelValue);
    const selectedIcon = computed({
      get: () => internalValue.value,
      set: (newValue) => {
        if (props.icons.includes(newValue) || newValue === "") {
          internalValue.value = newValue;
          emit2("update:modelValue", newValue);
        }
      }
    });
    const matchSearch = (name2) => {
      if (search.value.length == 0) {
        return true;
      }
      const escapedSearch = search.value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
      const regex = new RegExp(escapedSearch, "i");
      return regex.test(name2);
    };
    const openDropdown = () => {
      isOpen.value = true;
    };
    const closeDropdown = () => {
      isOpen.value = false;
    };
    const handleClickOutside = (event) => {
      if (!event.target.closest(".picker-icon")) {
        closeDropdown();
      }
    };
    const selectIcon = (icon) => {
      selectedIcon.value = icon;
      search.value = icon;
      closeDropdown();
    };
    onMounted(() => {
      search.value = props.value || props.modelValue;
      document.addEventListener("click", handleClickOutside);
    });
    onBeforeUnmount(() => {
      document.removeEventListener("click", handleClickOutside);
    });
    const __returned__ = { props, search, isOpen, emit: emit2, internalValue, selectedIcon, matchSearch, openDropdown, closeDropdown, handleClickOutside, selectIcon, ref, computed, onMounted, onBeforeUnmount };
    Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
    return __returned__;
  }
};
const _hoisted_1$3 = { class: "picker-icon" };
const _hoisted_2$3 = { class: "picker-preview-icon" };
const _hoisted_3$3 = {
  key: 0,
  class: "picker-icon-dropdown"
};
const _hoisted_4$3 = { class: "picker-icon-grid" };
const _hoisted_5$3 = ["onClick"];
const _hoisted_6$3 = { class: "picker-icon-input" };
const _hoisted_7$2 = ["name", "value", "required"];
function _sfc_render$3(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", _hoisted_1$3, [
    createBaseVNode("div", null, [
      createBaseVNode("div", _hoisted_2$3, [
        createBaseVNode("div", {
          class: "picker-icon-item",
          onClick: $setup.openDropdown
        }, [
          createBaseVNode("i", {
            class: normalizeClass($setup.selectedIcon)
          }, null, 2)
        ]),
        $setup.isOpen ? (openBlock(), createElementBlock("div", _hoisted_3$3, [
          createBaseVNode("div", _hoisted_4$3, [
            (openBlock(true), createElementBlock(Fragment, null, renderList($props.icons, (icon, key) => {
              return openBlock(), createElementBlock(Fragment, null, [
                $setup.matchSearch(icon) ? (openBlock(), createElementBlock("div", {
                  key,
                  class: "picker-icon-item",
                  onClick: ($event) => $setup.selectIcon(icon)
                }, [
                  createBaseVNode("i", {
                    class: normalizeClass(icon)
                  }, null, 2)
                ], 8, _hoisted_5$3)) : createCommentVNode("", true)
              ], 64);
            }), 256))
          ])
        ])) : createCommentVNode("", true)
      ])
    ]),
    createBaseVNode("div", _hoisted_6$3, [
      withDirectives(createBaseVNode("input", {
        type: "text",
        class: "form-input",
        "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.search = $event),
        onFocus: $setup.openDropdown,
        placeholder: "Choisir une icône..."
      }, null, 544), [
        [vModelText, $setup.search]
      ])
    ]),
    createBaseVNode("input", {
      type: "hidden",
      name: $props.name,
      value: $setup.selectedIcon,
      required: $props.required
    }, null, 8, _hoisted_7$2)
  ]);
}
const IconPicker = /* @__PURE__ */ _export_sfc(_sfc_main$3, [["render", _sfc_render$3], ["__file", "IconPicker.vue"]]);
var as = Object.defineProperty;
var Yb = Object.getOwnPropertyDescriptor;
var Jb = Object.getOwnPropertyNames;
var Xb = Object.prototype.hasOwnProperty;
var ze = (t, e) => () => (t && (e = t(t = 0)), e);
var D = (t, e) => () => (e || t((e = { exports: {} }).exports, e), e.exports), Kr = (t, e) => {
  for (var r in e) as(t, r, { get: e[r], enumerable: true });
}, Zb = (t, e, r, n) => {
  if (e && typeof e == "object" || typeof e == "function") for (let i of Jb(e)) !Xb.call(t, i) && i !== r && as(t, i, { get: () => e[i], enumerable: !(n = Yb(e, i)) || n.enumerable });
  return t;
};
var me = (t) => Zb(as({}, "__esModule", { value: true }), t);
var A = ze(() => {
});
var M = {};
Kr(M, { _debugEnd: () => Gc, _debugProcess: () => Vc, _events: () => lf, _eventsCount: () => cf, _exiting: () => xc, _fatalExceptions: () => Fc, _getActiveHandles: () => Rc, _getActiveRequests: () => Oc, _kill: () => Cc, _linkedBinding: () => Ac, _maxListeners: () => uf, _preload_modules: () => of, _rawDebug: () => Sc, _startProfilerIdleNotifier: () => zc, _stopProfilerIdleNotifier: () => Kc, _tickCallback: () => Hc, abort: () => Xc, addListener: () => ff, allowedNodeEnvironmentFlags: () => Uc, arch: () => uc, argv: () => fc, argv0: () => nf, assert: () => Dc, binding: () => gc, browser: () => vc, chdir: () => mc, config: () => Tc, cpuUsage: () => Ri, cwd: () => wc, debugPort: () => rf, default: () => _f, dlopen: () => Pc, domain: () => Ic, emit: () => gf, emitWarning: () => yc, env: () => cc, execArgv: () => hc, execPath: () => tf, exit: () => qc, features: () => jc, hasUncaughtExceptionCaptureCallback: () => $c, hrtime: () => Oi, kill: () => Nc, listeners: () => mf, memoryUsage: () => Mc, moduleLoadList: () => Ec, nextTick: () => oc, off: () => df, on: () => Nt, once: () => hf, openStdin: () => Lc, pid: () => Zc, platform: () => lc, ppid: () => ef, prependListener: () => bf, prependOnceListener: () => wf, reallyExit: () => kc, release: () => _c, removeAllListeners: () => yf, removeListener: () => pf, resourceUsage: () => Bc, setSourceMapsEnabled: () => sf, setUncaughtExceptionCaptureCallback: () => Wc, stderr: () => Yc, stdin: () => Jc, stdout: () => Qc, title: () => ac, umask: () => bc, uptime: () => af, version: () => dc, versions: () => pc });
function cs(t) {
  throw new Error("Node.js process " + t + " is not supported by JSPM core outside of Node.js");
}
function ew() {
  !Qr || !wr || (Qr = false, wr.length ? Mt = wr.concat(Mt) : Pi = -1, Mt.length && ic());
}
function ic() {
  if (!Qr) {
    var t = setTimeout(ew, 0);
    Qr = true;
    for (var e = Mt.length; e; ) {
      for (wr = Mt, Mt = []; ++Pi < e; ) wr && wr[Pi].run();
      Pi = -1, e = Mt.length;
    }
    wr = null, Qr = false, clearTimeout(t);
  }
}
function oc(t) {
  var e = new Array(arguments.length - 1);
  if (arguments.length > 1) for (var r = 1; r < arguments.length; r++) e[r - 1] = arguments[r];
  Mt.push(new sc(t, e)), Mt.length === 1 && !Qr && setTimeout(ic, 0);
}
function sc(t, e) {
  this.fun = t, this.array = e;
}
function Ue() {
}
function Ac(t) {
  cs("_linkedBinding");
}
function Pc(t) {
  cs("dlopen");
}
function Oc() {
  return [];
}
function Rc() {
  return [];
}
function Dc(t, e) {
  if (!t) throw new Error(e || "assertion error");
}
function $c() {
  return false;
}
function af() {
  return Jt.now() / 1e3;
}
function Oi(t) {
  var e = Math.floor((Date.now() - Jt.now()) * 1e-3), r = Jt.now() * 1e-3, n = Math.floor(r) + e, i = Math.floor(r % 1 * 1e9);
  return t && (n = n - t[0], i = i - t[1], i < 0 && (n--, i += ls)), [n, i];
}
function Nt() {
  return _f;
}
function mf(t) {
  return [];
}
var Mt, Qr, wr, Pi, ac, uc, lc, cc, fc, hc, dc, pc, yc, gc, bc, wc, mc, _c, vc, Sc, Ec, Ic, xc, Tc, kc, Cc, Ri, Bc, Mc, Nc, qc, Lc, Uc, jc, Fc, Wc, Hc, Vc, Gc, zc, Kc, Qc, Yc, Jc, Xc, Zc, ef, tf, rf, nf, of, sf, Jt, us, ls, uf, lf, cf, ff, hf, df, pf, yf, gf, bf, wf, _f, vf = ze(() => {
  A();
  x();
  I();
  Mt = [], Qr = false, Pi = -1;
  sc.prototype.run = function() {
    this.fun.apply(null, this.array);
  };
  ac = "browser", uc = "x64", lc = "browser", cc = { PATH: "/usr/bin", LANG: typeof navigator < "u" ? navigator.language + ".UTF-8" : void 0, PWD: "/", HOME: "/home", TMP: "/tmp" }, fc = ["/usr/bin/node"], hc = [], dc = "v16.8.0", pc = {}, yc = function(t, e) {
    console.warn((e ? e + ": " : "") + t);
  }, gc = function(t) {
    cs("binding");
  }, bc = function(t) {
    return 0;
  }, wc = function() {
    return "/";
  }, mc = function(t) {
  }, _c = { name: "node", sourceUrl: "", headersUrl: "", libUrl: "" };
  vc = true, Sc = Ue, Ec = [];
  Ic = {}, xc = false, Tc = {};
  kc = Ue, Cc = Ue, Ri = function() {
    return {};
  }, Bc = Ri, Mc = Ri, Nc = Ue, qc = Ue, Lc = Ue, Uc = {};
  jc = { inspector: false, debug: false, uv: false, ipv6: false, tls_alpn: false, tls_sni: false, tls_ocsp: false, tls: false, cached_builtins: true }, Fc = Ue, Wc = Ue;
  Hc = Ue, Vc = Ue, Gc = Ue, zc = Ue, Kc = Ue, Qc = void 0, Yc = void 0, Jc = void 0, Xc = Ue, Zc = 2, ef = 1, tf = "/bin/usr/node", rf = 9229, nf = "node", of = [], sf = Ue, Jt = { now: typeof performance < "u" ? performance.now.bind(performance) : void 0, timing: typeof performance < "u" ? performance.timing : void 0 };
  Jt.now === void 0 && (us = Date.now(), Jt.timing && Jt.timing.navigationStart && (us = Jt.timing.navigationStart), Jt.now = () => Date.now() - us);
  ls = 1e9;
  Oi.bigint = function(t) {
    var e = Oi(t);
    return typeof BigInt > "u" ? e[0] * ls + e[1] : BigInt(e[0] * ls) + BigInt(e[1]);
  };
  uf = 10, lf = {}, cf = 0;
  ff = Nt, hf = Nt, df = Nt, pf = Nt, yf = Nt, gf = Ue, bf = Nt, wf = Nt;
  _f = { version: dc, versions: pc, arch: uc, platform: lc, browser: vc, release: _c, _rawDebug: Sc, moduleLoadList: Ec, binding: gc, _linkedBinding: Ac, _events: lf, _eventsCount: cf, _maxListeners: uf, on: Nt, addListener: ff, once: hf, off: df, removeListener: pf, removeAllListeners: yf, emit: gf, prependListener: bf, prependOnceListener: wf, listeners: mf, domain: Ic, _exiting: xc, config: Tc, dlopen: Pc, uptime: af, _getActiveRequests: Oc, _getActiveHandles: Rc, reallyExit: kc, _kill: Cc, cpuUsage: Ri, resourceUsage: Bc, memoryUsage: Mc, kill: Nc, exit: qc, openStdin: Lc, allowedNodeEnvironmentFlags: Uc, assert: Dc, features: jc, _fatalExceptions: Fc, setUncaughtExceptionCaptureCallback: Wc, hasUncaughtExceptionCaptureCallback: $c, emitWarning: yc, nextTick: oc, _tickCallback: Hc, _debugProcess: Vc, _debugEnd: Gc, _startProfilerIdleNotifier: zc, _stopProfilerIdleNotifier: Kc, stdout: Qc, stdin: Jc, stderr: Yc, abort: Xc, umask: bc, chdir: mc, cwd: wc, env: cc, title: ac, argv: fc, execArgv: hc, pid: Zc, ppid: ef, execPath: tf, debugPort: rf, hrtime: Oi, argv0: nf, _preload_modules: of, setSourceMapsEnabled: sf };
});
var I = ze(() => {
  vf();
});
function tw() {
  if (Sf) return On;
  Sf = true, On.byteLength = a, On.toByteArray = f, On.fromByteArray = w;
  for (var t = [], e = [], r = typeof Uint8Array < "u" ? Uint8Array : Array, n = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/", i = 0, s = n.length; i < s; ++i) t[i] = n[i], e[n.charCodeAt(i)] = i;
  e[45] = 62, e[95] = 63;
  function o(y) {
    var P = y.length;
    if (P % 4 > 0) throw new Error("Invalid string. Length must be a multiple of 4");
    var E = y.indexOf("=");
    E === -1 && (E = P);
    var b = E === P ? 0 : 4 - E % 4;
    return [E, b];
  }
  function a(y) {
    var P = o(y), E = P[0], b = P[1];
    return (E + b) * 3 / 4 - b;
  }
  function c(y, P, E) {
    return (P + E) * 3 / 4 - E;
  }
  function f(y) {
    var P, E = o(y), b = E[0], m = E[1], v = new r(c(y, b, m)), R = 0, T = m > 0 ? b - 4 : b, g;
    for (g = 0; g < T; g += 4) P = e[y.charCodeAt(g)] << 18 | e[y.charCodeAt(g + 1)] << 12 | e[y.charCodeAt(g + 2)] << 6 | e[y.charCodeAt(g + 3)], v[R++] = P >> 16 & 255, v[R++] = P >> 8 & 255, v[R++] = P & 255;
    return m === 2 && (P = e[y.charCodeAt(g)] << 2 | e[y.charCodeAt(g + 1)] >> 4, v[R++] = P & 255), m === 1 && (P = e[y.charCodeAt(g)] << 10 | e[y.charCodeAt(g + 1)] << 4 | e[y.charCodeAt(g + 2)] >> 2, v[R++] = P >> 8 & 255, v[R++] = P & 255), v;
  }
  function h2(y) {
    return t[y >> 18 & 63] + t[y >> 12 & 63] + t[y >> 6 & 63] + t[y & 63];
  }
  function d(y, P, E) {
    for (var b, m = [], v = P; v < E; v += 3) b = (y[v] << 16 & 16711680) + (y[v + 1] << 8 & 65280) + (y[v + 2] & 255), m.push(h2(b));
    return m.join("");
  }
  function w(y) {
    for (var P, E = y.length, b = E % 3, m = [], v = 16383, R = 0, T = E - b; R < T; R += v) m.push(d(y, R, R + v > T ? T : R + v));
    return b === 1 ? (P = y[E - 1], m.push(t[P >> 2] + t[P << 4 & 63] + "==")) : b === 2 && (P = (y[E - 2] << 8) + y[E - 1], m.push(t[P >> 10] + t[P >> 4 & 63] + t[P << 2 & 63] + "=")), m.join("");
  }
  return On;
}
function rw() {
  if (Ef) return ki;
  Ef = true;
  return ki.read = function(t, e, r, n, i) {
    var s, o, a = i * 8 - n - 1, c = (1 << a) - 1, f = c >> 1, h2 = -7, d = r ? i - 1 : 0, w = r ? -1 : 1, y = t[e + d];
    for (d += w, s = y & (1 << -h2) - 1, y >>= -h2, h2 += a; h2 > 0; s = s * 256 + t[e + d], d += w, h2 -= 8) ;
    for (o = s & (1 << -h2) - 1, s >>= -h2, h2 += n; h2 > 0; o = o * 256 + t[e + d], d += w, h2 -= 8) ;
    if (s === 0) s = 1 - f;
    else {
      if (s === c) return o ? NaN : (y ? -1 : 1) * (1 / 0);
      o = o + Math.pow(2, n), s = s - f;
    }
    return (y ? -1 : 1) * o * Math.pow(2, s - n);
  }, ki.write = function(t, e, r, n, i, s) {
    var o, a, c, f = s * 8 - i - 1, h2 = (1 << f) - 1, d = h2 >> 1, w = i === 23 ? Math.pow(2, -24) - Math.pow(2, -77) : 0, y = n ? 0 : s - 1, P = n ? 1 : -1, E = e < 0 || e === 0 && 1 / e < 0 ? 1 : 0;
    for (e = Math.abs(e), isNaN(e) || e === 1 / 0 ? (a = isNaN(e) ? 1 : 0, o = h2) : (o = Math.floor(Math.log(e) / Math.LN2), e * (c = Math.pow(2, -o)) < 1 && (o--, c *= 2), o + d >= 1 ? e += w / c : e += w * Math.pow(2, 1 - d), e * c >= 2 && (o++, c /= 2), o + d >= h2 ? (a = 0, o = h2) : o + d >= 1 ? (a = (e * c - 1) * Math.pow(2, i), o = o + d) : (a = e * Math.pow(2, d - 1) * Math.pow(2, i), o = 0)); i >= 8; t[r + y] = a & 255, y += P, a /= 256, i -= 8) ;
    for (o = o << i | a, f += i; f > 0; t[r + y] = o & 255, y += P, o /= 256, f -= 8) ;
    t[r + y - P] |= E * 128;
  }, ki;
}
function If() {
  if (Af) return mr;
  Af = true;
  let t = tw(), e = rw(), r = typeof Symbol == "function" && typeof Symbol.for == "function" ? Symbol.for("nodejs.util.inspect.custom") : null;
  mr.Buffer = o, mr.SlowBuffer = m, mr.INSPECT_MAX_BYTES = 50;
  let n = 2147483647;
  mr.kMaxLength = n, o.TYPED_ARRAY_SUPPORT = i(), !o.TYPED_ARRAY_SUPPORT && typeof console < "u" && typeof console.error == "function" && console.error("This browser lacks typed array (Uint8Array) support which is required by `buffer` v5.x. Use `buffer` v4.x if you require old browser support.");
  function i() {
    try {
      let p2 = new Uint8Array(1), u = { foo: function() {
        return 42;
      } };
      return Object.setPrototypeOf(u, Uint8Array.prototype), Object.setPrototypeOf(p2, u), p2.foo() === 42;
    } catch {
      return false;
    }
  }
  Object.defineProperty(o.prototype, "parent", { enumerable: true, get: function() {
    if (o.isBuffer(this)) return this.buffer;
  } }), Object.defineProperty(o.prototype, "offset", { enumerable: true, get: function() {
    if (o.isBuffer(this)) return this.byteOffset;
  } });
  function s(p2) {
    if (p2 > n) throw new RangeError('The value "' + p2 + '" is invalid for option "size"');
    let u = new Uint8Array(p2);
    return Object.setPrototypeOf(u, o.prototype), u;
  }
  function o(p2, u, l) {
    if (typeof p2 == "number") {
      if (typeof u == "string") throw new TypeError('The "string" argument must be of type string. Received type number');
      return h2(p2);
    }
    return a(p2, u, l);
  }
  o.poolSize = 8192;
  function a(p2, u, l) {
    if (typeof p2 == "string") return d(p2, u);
    if (ArrayBuffer.isView(p2)) return y(p2);
    if (p2 == null) throw new TypeError("The first argument must be one of type string, Buffer, ArrayBuffer, Array, or Array-like Object. Received type " + typeof p2);
    if (Ze(p2, ArrayBuffer) || p2 && Ze(p2.buffer, ArrayBuffer) || typeof SharedArrayBuffer < "u" && (Ze(p2, SharedArrayBuffer) || p2 && Ze(p2.buffer, SharedArrayBuffer))) return P(p2, u, l);
    if (typeof p2 == "number") throw new TypeError('The "value" argument must not be of type number. Received type number');
    let S = p2.valueOf && p2.valueOf();
    if (S != null && S !== p2) return o.from(S, u, l);
    let C = E(p2);
    if (C) return C;
    if (typeof Symbol < "u" && Symbol.toPrimitive != null && typeof p2[Symbol.toPrimitive] == "function") return o.from(p2[Symbol.toPrimitive]("string"), u, l);
    throw new TypeError("The first argument must be one of type string, Buffer, ArrayBuffer, Array, or Array-like Object. Received type " + typeof p2);
  }
  o.from = function(p2, u, l) {
    return a(p2, u, l);
  }, Object.setPrototypeOf(o.prototype, Uint8Array.prototype), Object.setPrototypeOf(o, Uint8Array);
  function c(p2) {
    if (typeof p2 != "number") throw new TypeError('"size" argument must be of type number');
    if (p2 < 0) throw new RangeError('The value "' + p2 + '" is invalid for option "size"');
  }
  function f(p2, u, l) {
    return c(p2), p2 <= 0 ? s(p2) : u !== void 0 ? typeof l == "string" ? s(p2).fill(u, l) : s(p2).fill(u) : s(p2);
  }
  o.alloc = function(p2, u, l) {
    return f(p2, u, l);
  };
  function h2(p2) {
    return c(p2), s(p2 < 0 ? 0 : b(p2) | 0);
  }
  o.allocUnsafe = function(p2) {
    return h2(p2);
  }, o.allocUnsafeSlow = function(p2) {
    return h2(p2);
  };
  function d(p2, u) {
    if ((typeof u != "string" || u === "") && (u = "utf8"), !o.isEncoding(u)) throw new TypeError("Unknown encoding: " + u);
    let l = v(p2, u) | 0, S = s(l), C = S.write(p2, u);
    return C !== l && (S = S.slice(0, C)), S;
  }
  function w(p2) {
    let u = p2.length < 0 ? 0 : b(p2.length) | 0, l = s(u);
    for (let S = 0; S < u; S += 1) l[S] = p2[S] & 255;
    return l;
  }
  function y(p2) {
    if (Ze(p2, Uint8Array)) {
      let u = new Uint8Array(p2);
      return P(u.buffer, u.byteOffset, u.byteLength);
    }
    return w(p2);
  }
  function P(p2, u, l) {
    if (u < 0 || p2.byteLength < u) throw new RangeError('"offset" is outside of buffer bounds');
    if (p2.byteLength < u + (l || 0)) throw new RangeError('"length" is outside of buffer bounds');
    let S;
    return u === void 0 && l === void 0 ? S = new Uint8Array(p2) : l === void 0 ? S = new Uint8Array(p2, u) : S = new Uint8Array(p2, u, l), Object.setPrototypeOf(S, o.prototype), S;
  }
  function E(p2) {
    if (o.isBuffer(p2)) {
      let u = b(p2.length) | 0, l = s(u);
      return l.length === 0 || p2.copy(l, 0, 0, u), l;
    }
    if (p2.length !== void 0) return typeof p2.length != "number" || Tn(p2.length) ? s(0) : w(p2);
    if (p2.type === "Buffer" && Array.isArray(p2.data)) return w(p2.data);
  }
  function b(p2) {
    if (p2 >= n) throw new RangeError("Attempt to allocate Buffer larger than maximum size: 0x" + n.toString(16) + " bytes");
    return p2 | 0;
  }
  function m(p2) {
    return +p2 != p2 && (p2 = 0), o.alloc(+p2);
  }
  o.isBuffer = function(u) {
    return u != null && u._isBuffer === true && u !== o.prototype;
  }, o.compare = function(u, l) {
    if (Ze(u, Uint8Array) && (u = o.from(u, u.offset, u.byteLength)), Ze(l, Uint8Array) && (l = o.from(l, l.offset, l.byteLength)), !o.isBuffer(u) || !o.isBuffer(l)) throw new TypeError('The "buf1", "buf2" arguments must be one of type Buffer or Uint8Array');
    if (u === l) return 0;
    let S = u.length, C = l.length;
    for (let N = 0, F = Math.min(S, C); N < F; ++N) if (u[N] !== l[N]) {
      S = u[N], C = l[N];
      break;
    }
    return S < C ? -1 : C < S ? 1 : 0;
  }, o.isEncoding = function(u) {
    switch (String(u).toLowerCase()) {
      case "hex":
      case "utf8":
      case "utf-8":
      case "ascii":
      case "latin1":
      case "binary":
      case "base64":
      case "ucs2":
      case "ucs-2":
      case "utf16le":
      case "utf-16le":
        return true;
      default:
        return false;
    }
  }, o.concat = function(u, l) {
    if (!Array.isArray(u)) throw new TypeError('"list" argument must be an Array of Buffers');
    if (u.length === 0) return o.alloc(0);
    let S;
    if (l === void 0) for (l = 0, S = 0; S < u.length; ++S) l += u[S].length;
    let C = o.allocUnsafe(l), N = 0;
    for (S = 0; S < u.length; ++S) {
      let F = u[S];
      if (Ze(F, Uint8Array)) N + F.length > C.length ? (o.isBuffer(F) || (F = o.from(F)), F.copy(C, N)) : Uint8Array.prototype.set.call(C, F, N);
      else if (o.isBuffer(F)) F.copy(C, N);
      else throw new TypeError('"list" argument must be an Array of Buffers');
      N += F.length;
    }
    return C;
  };
  function v(p2, u) {
    if (o.isBuffer(p2)) return p2.length;
    if (ArrayBuffer.isView(p2) || Ze(p2, ArrayBuffer)) return p2.byteLength;
    if (typeof p2 != "string") throw new TypeError('The "string" argument must be one of type string, Buffer, or ArrayBuffer. Received type ' + typeof p2);
    let l = p2.length, S = arguments.length > 2 && arguments[2] === true;
    if (!S && l === 0) return 0;
    let C = false;
    for (; ; ) switch (u) {
      case "ascii":
      case "latin1":
      case "binary":
        return l;
      case "utf8":
      case "utf-8":
        return Ct(p2).length;
      case "ucs2":
      case "ucs-2":
      case "utf16le":
      case "utf-16le":
        return l * 2;
      case "hex":
        return l >>> 1;
      case "base64":
        return Ii(p2).length;
      default:
        if (C) return S ? -1 : Ct(p2).length;
        u = ("" + u).toLowerCase(), C = true;
    }
  }
  o.byteLength = v;
  function R(p2, u, l) {
    let S = false;
    if ((u === void 0 || u < 0) && (u = 0), u > this.length || ((l === void 0 || l > this.length) && (l = this.length), l <= 0) || (l >>>= 0, u >>>= 0, l <= u)) return "";
    for (p2 || (p2 = "utf8"); ; ) switch (p2) {
      case "hex":
        return V(this, u, l);
      case "utf8":
      case "utf-8":
        return H(this, u, l);
      case "ascii":
        return X(this, u, l);
      case "latin1":
      case "binary":
        return Q(this, u, l);
      case "base64":
        return W(this, u, l);
      case "ucs2":
      case "ucs-2":
      case "utf16le":
      case "utf-16le":
        return te(this, u, l);
      default:
        if (S) throw new TypeError("Unknown encoding: " + p2);
        p2 = (p2 + "").toLowerCase(), S = true;
    }
  }
  o.prototype._isBuffer = true;
  function T(p2, u, l) {
    let S = p2[u];
    p2[u] = p2[l], p2[l] = S;
  }
  o.prototype.swap16 = function() {
    let u = this.length;
    if (u % 2 !== 0) throw new RangeError("Buffer size must be a multiple of 16-bits");
    for (let l = 0; l < u; l += 2) T(this, l, l + 1);
    return this;
  }, o.prototype.swap32 = function() {
    let u = this.length;
    if (u % 4 !== 0) throw new RangeError("Buffer size must be a multiple of 32-bits");
    for (let l = 0; l < u; l += 4) T(this, l, l + 3), T(this, l + 1, l + 2);
    return this;
  }, o.prototype.swap64 = function() {
    let u = this.length;
    if (u % 8 !== 0) throw new RangeError("Buffer size must be a multiple of 64-bits");
    for (let l = 0; l < u; l += 8) T(this, l, l + 7), T(this, l + 1, l + 6), T(this, l + 2, l + 5), T(this, l + 3, l + 4);
    return this;
  }, o.prototype.toString = function() {
    let u = this.length;
    return u === 0 ? "" : arguments.length === 0 ? H(this, 0, u) : R.apply(this, arguments);
  }, o.prototype.toLocaleString = o.prototype.toString, o.prototype.equals = function(u) {
    if (!o.isBuffer(u)) throw new TypeError("Argument must be a Buffer");
    return this === u ? true : o.compare(this, u) === 0;
  }, o.prototype.inspect = function() {
    let u = "", l = mr.INSPECT_MAX_BYTES;
    return u = this.toString("hex", 0, l).replace(/(.{2})/g, "$1 ").trim(), this.length > l && (u += " ... "), "<Buffer " + u + ">";
  }, r && (o.prototype[r] = o.prototype.inspect), o.prototype.compare = function(u, l, S, C, N) {
    if (Ze(u, Uint8Array) && (u = o.from(u, u.offset, u.byteLength)), !o.isBuffer(u)) throw new TypeError('The "target" argument must be one of type Buffer or Uint8Array. Received type ' + typeof u);
    if (l === void 0 && (l = 0), S === void 0 && (S = u ? u.length : 0), C === void 0 && (C = 0), N === void 0 && (N = this.length), l < 0 || S > u.length || C < 0 || N > this.length) throw new RangeError("out of range index");
    if (C >= N && l >= S) return 0;
    if (C >= N) return -1;
    if (l >= S) return 1;
    if (l >>>= 0, S >>>= 0, C >>>= 0, N >>>= 0, this === u) return 0;
    let F = N - C, k = S - l, B = Math.min(F, k), re = this.slice(C, N), ie = u.slice(l, S);
    for (let oe = 0; oe < B; ++oe) if (re[oe] !== ie[oe]) {
      F = re[oe], k = ie[oe];
      break;
    }
    return F < k ? -1 : k < F ? 1 : 0;
  };
  function g(p2, u, l, S, C) {
    if (p2.length === 0) return -1;
    if (typeof l == "string" ? (S = l, l = 0) : l > 2147483647 ? l = 2147483647 : l < -2147483648 && (l = -2147483648), l = +l, Tn(l) && (l = C ? 0 : p2.length - 1), l < 0 && (l = p2.length + l), l >= p2.length) {
      if (C) return -1;
      l = p2.length - 1;
    } else if (l < 0) if (C) l = 0;
    else return -1;
    if (typeof u == "string" && (u = o.from(u, S)), o.isBuffer(u)) return u.length === 0 ? -1 : O(p2, u, l, S, C);
    if (typeof u == "number") return u = u & 255, typeof Uint8Array.prototype.indexOf == "function" ? C ? Uint8Array.prototype.indexOf.call(p2, u, l) : Uint8Array.prototype.lastIndexOf.call(p2, u, l) : O(p2, [u], l, S, C);
    throw new TypeError("val must be string, number or Buffer");
  }
  function O(p2, u, l, S, C) {
    let N = 1, F = p2.length, k = u.length;
    if (S !== void 0 && (S = String(S).toLowerCase(), S === "ucs2" || S === "ucs-2" || S === "utf16le" || S === "utf-16le")) {
      if (p2.length < 2 || u.length < 2) return -1;
      N = 2, F /= 2, k /= 2, l /= 2;
    }
    function B(ie, oe) {
      return N === 1 ? ie[oe] : ie.readUInt16BE(oe * N);
    }
    let re;
    if (C) {
      let ie = -1;
      for (re = l; re < F; re++) if (B(p2, re) === B(u, ie === -1 ? 0 : re - ie)) {
        if (ie === -1 && (ie = re), re - ie + 1 === k) return ie * N;
      } else ie !== -1 && (re -= re - ie), ie = -1;
    } else for (l + k > F && (l = F - k), re = l; re >= 0; re--) {
      let ie = true;
      for (let oe = 0; oe < k; oe++) if (B(p2, re + oe) !== B(u, oe)) {
        ie = false;
        break;
      }
      if (ie) return re;
    }
    return -1;
  }
  o.prototype.includes = function(u, l, S) {
    return this.indexOf(u, l, S) !== -1;
  }, o.prototype.indexOf = function(u, l, S) {
    return g(this, u, l, S, true);
  }, o.prototype.lastIndexOf = function(u, l, S) {
    return g(this, u, l, S, false);
  };
  function _(p2, u, l, S) {
    l = Number(l) || 0;
    let C = p2.length - l;
    S ? (S = Number(S), S > C && (S = C)) : S = C;
    let N = u.length;
    S > N / 2 && (S = N / 2);
    let F;
    for (F = 0; F < S; ++F) {
      let k = parseInt(u.substr(F * 2, 2), 16);
      if (Tn(k)) return F;
      p2[l + F] = k;
    }
    return F;
  }
  function q(p2, u, l, S) {
    return Vr(Ct(u, p2.length - l), p2, l, S);
  }
  function $(p2, u, l, S) {
    return Vr(Bt(u), p2, l, S);
  }
  function j(p2, u, l, S) {
    return Vr(Ii(u), p2, l, S);
  }
  function J(p2, u, l, S) {
    return Vr(xn(u, p2.length - l), p2, l, S);
  }
  o.prototype.write = function(u, l, S, C) {
    if (l === void 0) C = "utf8", S = this.length, l = 0;
    else if (S === void 0 && typeof l == "string") C = l, S = this.length, l = 0;
    else if (isFinite(l)) l = l >>> 0, isFinite(S) ? (S = S >>> 0, C === void 0 && (C = "utf8")) : (C = S, S = void 0);
    else throw new Error("Buffer.write(string, encoding, offset[, length]) is no longer supported");
    let N = this.length - l;
    if ((S === void 0 || S > N) && (S = N), u.length > 0 && (S < 0 || l < 0) || l > this.length) throw new RangeError("Attempt to write outside buffer bounds");
    C || (C = "utf8");
    let F = false;
    for (; ; ) switch (C) {
      case "hex":
        return _(this, u, l, S);
      case "utf8":
      case "utf-8":
        return q(this, u, l, S);
      case "ascii":
      case "latin1":
      case "binary":
        return $(this, u, l, S);
      case "base64":
        return j(this, u, l, S);
      case "ucs2":
      case "ucs-2":
      case "utf16le":
      case "utf-16le":
        return J(this, u, l, S);
      default:
        if (F) throw new TypeError("Unknown encoding: " + C);
        C = ("" + C).toLowerCase(), F = true;
    }
  }, o.prototype.toJSON = function() {
    return { type: "Buffer", data: Array.prototype.slice.call(this._arr || this, 0) };
  };
  function W(p2, u, l) {
    return u === 0 && l === p2.length ? t.fromByteArray(p2) : t.fromByteArray(p2.slice(u, l));
  }
  function H(p2, u, l) {
    l = Math.min(p2.length, l);
    let S = [], C = u;
    for (; C < l; ) {
      let N = p2[C], F = null, k = N > 239 ? 4 : N > 223 ? 3 : N > 191 ? 2 : 1;
      if (C + k <= l) {
        let B, re, ie, oe;
        switch (k) {
          case 1:
            N < 128 && (F = N);
            break;
          case 2:
            B = p2[C + 1], (B & 192) === 128 && (oe = (N & 31) << 6 | B & 63, oe > 127 && (F = oe));
            break;
          case 3:
            B = p2[C + 1], re = p2[C + 2], (B & 192) === 128 && (re & 192) === 128 && (oe = (N & 15) << 12 | (B & 63) << 6 | re & 63, oe > 2047 && (oe < 55296 || oe > 57343) && (F = oe));
            break;
          case 4:
            B = p2[C + 1], re = p2[C + 2], ie = p2[C + 3], (B & 192) === 128 && (re & 192) === 128 && (ie & 192) === 128 && (oe = (N & 15) << 18 | (B & 63) << 12 | (re & 63) << 6 | ie & 63, oe > 65535 && oe < 1114112 && (F = oe));
        }
      }
      F === null ? (F = 65533, k = 1) : F > 65535 && (F -= 65536, S.push(F >>> 10 & 1023 | 55296), F = 56320 | F & 1023), S.push(F), C += k;
    }
    return K(S);
  }
  let G = 4096;
  function K(p2) {
    let u = p2.length;
    if (u <= G) return String.fromCharCode.apply(String, p2);
    let l = "", S = 0;
    for (; S < u; ) l += String.fromCharCode.apply(String, p2.slice(S, S += G));
    return l;
  }
  function X(p2, u, l) {
    let S = "";
    l = Math.min(p2.length, l);
    for (let C = u; C < l; ++C) S += String.fromCharCode(p2[C] & 127);
    return S;
  }
  function Q(p2, u, l) {
    let S = "";
    l = Math.min(p2.length, l);
    for (let C = u; C < l; ++C) S += String.fromCharCode(p2[C]);
    return S;
  }
  function V(p2, u, l) {
    let S = p2.length;
    (!u || u < 0) && (u = 0), (!l || l < 0 || l > S) && (l = S);
    let C = "";
    for (let N = u; N < l; ++N) C += Xo[p2[N]];
    return C;
  }
  function te(p2, u, l) {
    let S = p2.slice(u, l), C = "";
    for (let N = 0; N < S.length - 1; N += 2) C += String.fromCharCode(S[N] + S[N + 1] * 256);
    return C;
  }
  o.prototype.slice = function(u, l) {
    let S = this.length;
    u = ~~u, l = l === void 0 ? S : ~~l, u < 0 ? (u += S, u < 0 && (u = 0)) : u > S && (u = S), l < 0 ? (l += S, l < 0 && (l = 0)) : l > S && (l = S), l < u && (l = u);
    let C = this.subarray(u, l);
    return Object.setPrototypeOf(C, o.prototype), C;
  };
  function Y(p2, u, l) {
    if (p2 % 1 !== 0 || p2 < 0) throw new RangeError("offset is not uint");
    if (p2 + u > l) throw new RangeError("Trying to access beyond buffer length");
  }
  o.prototype.readUintLE = o.prototype.readUIntLE = function(u, l, S) {
    u = u >>> 0, l = l >>> 0, S || Y(u, l, this.length);
    let C = this[u], N = 1, F = 0;
    for (; ++F < l && (N *= 256); ) C += this[u + F] * N;
    return C;
  }, o.prototype.readUintBE = o.prototype.readUIntBE = function(u, l, S) {
    u = u >>> 0, l = l >>> 0, S || Y(u, l, this.length);
    let C = this[u + --l], N = 1;
    for (; l > 0 && (N *= 256); ) C += this[u + --l] * N;
    return C;
  }, o.prototype.readUint8 = o.prototype.readUInt8 = function(u, l) {
    return u = u >>> 0, l || Y(u, 1, this.length), this[u];
  }, o.prototype.readUint16LE = o.prototype.readUInt16LE = function(u, l) {
    return u = u >>> 0, l || Y(u, 2, this.length), this[u] | this[u + 1] << 8;
  }, o.prototype.readUint16BE = o.prototype.readUInt16BE = function(u, l) {
    return u = u >>> 0, l || Y(u, 2, this.length), this[u] << 8 | this[u + 1];
  }, o.prototype.readUint32LE = o.prototype.readUInt32LE = function(u, l) {
    return u = u >>> 0, l || Y(u, 4, this.length), (this[u] | this[u + 1] << 8 | this[u + 2] << 16) + this[u + 3] * 16777216;
  }, o.prototype.readUint32BE = o.prototype.readUInt32BE = function(u, l) {
    return u = u >>> 0, l || Y(u, 4, this.length), this[u] * 16777216 + (this[u + 1] << 16 | this[u + 2] << 8 | this[u + 3]);
  }, o.prototype.readBigUInt64LE = at(function(u) {
    u = u >>> 0, xe(u, "offset");
    let l = this[u], S = this[u + 7];
    (l === void 0 || S === void 0) && st(u, this.length - 8);
    let C = l + this[++u] * 2 ** 8 + this[++u] * 2 ** 16 + this[++u] * 2 ** 24, N = this[++u] + this[++u] * 2 ** 8 + this[++u] * 2 ** 16 + S * 2 ** 24;
    return BigInt(C) + (BigInt(N) << BigInt(32));
  }), o.prototype.readBigUInt64BE = at(function(u) {
    u = u >>> 0, xe(u, "offset");
    let l = this[u], S = this[u + 7];
    (l === void 0 || S === void 0) && st(u, this.length - 8);
    let C = l * 2 ** 24 + this[++u] * 2 ** 16 + this[++u] * 2 ** 8 + this[++u], N = this[++u] * 2 ** 24 + this[++u] * 2 ** 16 + this[++u] * 2 ** 8 + S;
    return (BigInt(C) << BigInt(32)) + BigInt(N);
  }), o.prototype.readIntLE = function(u, l, S) {
    u = u >>> 0, l = l >>> 0, S || Y(u, l, this.length);
    let C = this[u], N = 1, F = 0;
    for (; ++F < l && (N *= 256); ) C += this[u + F] * N;
    return N *= 128, C >= N && (C -= Math.pow(2, 8 * l)), C;
  }, o.prototype.readIntBE = function(u, l, S) {
    u = u >>> 0, l = l >>> 0, S || Y(u, l, this.length);
    let C = l, N = 1, F = this[u + --C];
    for (; C > 0 && (N *= 256); ) F += this[u + --C] * N;
    return N *= 128, F >= N && (F -= Math.pow(2, 8 * l)), F;
  }, o.prototype.readInt8 = function(u, l) {
    return u = u >>> 0, l || Y(u, 1, this.length), this[u] & 128 ? (255 - this[u] + 1) * -1 : this[u];
  }, o.prototype.readInt16LE = function(u, l) {
    u = u >>> 0, l || Y(u, 2, this.length);
    let S = this[u] | this[u + 1] << 8;
    return S & 32768 ? S | 4294901760 : S;
  }, o.prototype.readInt16BE = function(u, l) {
    u = u >>> 0, l || Y(u, 2, this.length);
    let S = this[u + 1] | this[u] << 8;
    return S & 32768 ? S | 4294901760 : S;
  }, o.prototype.readInt32LE = function(u, l) {
    return u = u >>> 0, l || Y(u, 4, this.length), this[u] | this[u + 1] << 8 | this[u + 2] << 16 | this[u + 3] << 24;
  }, o.prototype.readInt32BE = function(u, l) {
    return u = u >>> 0, l || Y(u, 4, this.length), this[u] << 24 | this[u + 1] << 16 | this[u + 2] << 8 | this[u + 3];
  }, o.prototype.readBigInt64LE = at(function(u) {
    u = u >>> 0, xe(u, "offset");
    let l = this[u], S = this[u + 7];
    (l === void 0 || S === void 0) && st(u, this.length - 8);
    let C = this[u + 4] + this[u + 5] * 2 ** 8 + this[u + 6] * 2 ** 16 + (S << 24);
    return (BigInt(C) << BigInt(32)) + BigInt(l + this[++u] * 2 ** 8 + this[++u] * 2 ** 16 + this[++u] * 2 ** 24);
  }), o.prototype.readBigInt64BE = at(function(u) {
    u = u >>> 0, xe(u, "offset");
    let l = this[u], S = this[u + 7];
    (l === void 0 || S === void 0) && st(u, this.length - 8);
    let C = (l << 24) + this[++u] * 2 ** 16 + this[++u] * 2 ** 8 + this[++u];
    return (BigInt(C) << BigInt(32)) + BigInt(this[++u] * 2 ** 24 + this[++u] * 2 ** 16 + this[++u] * 2 ** 8 + S);
  }), o.prototype.readFloatLE = function(u, l) {
    return u = u >>> 0, l || Y(u, 4, this.length), e.read(this, u, true, 23, 4);
  }, o.prototype.readFloatBE = function(u, l) {
    return u = u >>> 0, l || Y(u, 4, this.length), e.read(this, u, false, 23, 4);
  }, o.prototype.readDoubleLE = function(u, l) {
    return u = u >>> 0, l || Y(u, 8, this.length), e.read(this, u, true, 52, 8);
  }, o.prototype.readDoubleBE = function(u, l) {
    return u = u >>> 0, l || Y(u, 8, this.length), e.read(this, u, false, 52, 8);
  };
  function ee(p2, u, l, S, C, N) {
    if (!o.isBuffer(p2)) throw new TypeError('"buffer" argument must be a Buffer instance');
    if (u > C || u < N) throw new RangeError('"value" argument is out of bounds');
    if (l + S > p2.length) throw new RangeError("Index out of range");
  }
  o.prototype.writeUintLE = o.prototype.writeUIntLE = function(u, l, S, C) {
    if (u = +u, l = l >>> 0, S = S >>> 0, !C) {
      let k = Math.pow(2, 8 * S) - 1;
      ee(this, u, l, S, k, 0);
    }
    let N = 1, F = 0;
    for (this[l] = u & 255; ++F < S && (N *= 256); ) this[l + F] = u / N & 255;
    return l + S;
  }, o.prototype.writeUintBE = o.prototype.writeUIntBE = function(u, l, S, C) {
    if (u = +u, l = l >>> 0, S = S >>> 0, !C) {
      let k = Math.pow(2, 8 * S) - 1;
      ee(this, u, l, S, k, 0);
    }
    let N = S - 1, F = 1;
    for (this[l + N] = u & 255; --N >= 0 && (F *= 256); ) this[l + N] = u / F & 255;
    return l + S;
  }, o.prototype.writeUint8 = o.prototype.writeUInt8 = function(u, l, S) {
    return u = +u, l = l >>> 0, S || ee(this, u, l, 1, 255, 0), this[l] = u & 255, l + 1;
  }, o.prototype.writeUint16LE = o.prototype.writeUInt16LE = function(u, l, S) {
    return u = +u, l = l >>> 0, S || ee(this, u, l, 2, 65535, 0), this[l] = u & 255, this[l + 1] = u >>> 8, l + 2;
  }, o.prototype.writeUint16BE = o.prototype.writeUInt16BE = function(u, l, S) {
    return u = +u, l = l >>> 0, S || ee(this, u, l, 2, 65535, 0), this[l] = u >>> 8, this[l + 1] = u & 255, l + 2;
  }, o.prototype.writeUint32LE = o.prototype.writeUInt32LE = function(u, l, S) {
    return u = +u, l = l >>> 0, S || ee(this, u, l, 4, 4294967295, 0), this[l + 3] = u >>> 24, this[l + 2] = u >>> 16, this[l + 1] = u >>> 8, this[l] = u & 255, l + 4;
  }, o.prototype.writeUint32BE = o.prototype.writeUInt32BE = function(u, l, S) {
    return u = +u, l = l >>> 0, S || ee(this, u, l, 4, 4294967295, 0), this[l] = u >>> 24, this[l + 1] = u >>> 16, this[l + 2] = u >>> 8, this[l + 3] = u & 255, l + 4;
  };
  function pe(p2, u, l, S, C) {
    Oe(u, S, C, p2, l, 7);
    let N = Number(u & BigInt(4294967295));
    p2[l++] = N, N = N >> 8, p2[l++] = N, N = N >> 8, p2[l++] = N, N = N >> 8, p2[l++] = N;
    let F = Number(u >> BigInt(32) & BigInt(4294967295));
    return p2[l++] = F, F = F >> 8, p2[l++] = F, F = F >> 8, p2[l++] = F, F = F >> 8, p2[l++] = F, l;
  }
  function Z(p2, u, l, S, C) {
    Oe(u, S, C, p2, l, 7);
    let N = Number(u & BigInt(4294967295));
    p2[l + 7] = N, N = N >> 8, p2[l + 6] = N, N = N >> 8, p2[l + 5] = N, N = N >> 8, p2[l + 4] = N;
    let F = Number(u >> BigInt(32) & BigInt(4294967295));
    return p2[l + 3] = F, F = F >> 8, p2[l + 2] = F, F = F >> 8, p2[l + 1] = F, F = F >> 8, p2[l] = F, l + 8;
  }
  o.prototype.writeBigUInt64LE = at(function(u, l = 0) {
    return pe(this, u, l, BigInt(0), BigInt("0xffffffffffffffff"));
  }), o.prototype.writeBigUInt64BE = at(function(u, l = 0) {
    return Z(this, u, l, BigInt(0), BigInt("0xffffffffffffffff"));
  }), o.prototype.writeIntLE = function(u, l, S, C) {
    if (u = +u, l = l >>> 0, !C) {
      let B = Math.pow(2, 8 * S - 1);
      ee(this, u, l, S, B - 1, -B);
    }
    let N = 0, F = 1, k = 0;
    for (this[l] = u & 255; ++N < S && (F *= 256); ) u < 0 && k === 0 && this[l + N - 1] !== 0 && (k = 1), this[l + N] = (u / F >> 0) - k & 255;
    return l + S;
  }, o.prototype.writeIntBE = function(u, l, S, C) {
    if (u = +u, l = l >>> 0, !C) {
      let B = Math.pow(2, 8 * S - 1);
      ee(this, u, l, S, B - 1, -B);
    }
    let N = S - 1, F = 1, k = 0;
    for (this[l + N] = u & 255; --N >= 0 && (F *= 256); ) u < 0 && k === 0 && this[l + N + 1] !== 0 && (k = 1), this[l + N] = (u / F >> 0) - k & 255;
    return l + S;
  }, o.prototype.writeInt8 = function(u, l, S) {
    return u = +u, l = l >>> 0, S || ee(this, u, l, 1, 127, -128), u < 0 && (u = 255 + u + 1), this[l] = u & 255, l + 1;
  }, o.prototype.writeInt16LE = function(u, l, S) {
    return u = +u, l = l >>> 0, S || ee(this, u, l, 2, 32767, -32768), this[l] = u & 255, this[l + 1] = u >>> 8, l + 2;
  }, o.prototype.writeInt16BE = function(u, l, S) {
    return u = +u, l = l >>> 0, S || ee(this, u, l, 2, 32767, -32768), this[l] = u >>> 8, this[l + 1] = u & 255, l + 2;
  }, o.prototype.writeInt32LE = function(u, l, S) {
    return u = +u, l = l >>> 0, S || ee(this, u, l, 4, 2147483647, -2147483648), this[l] = u & 255, this[l + 1] = u >>> 8, this[l + 2] = u >>> 16, this[l + 3] = u >>> 24, l + 4;
  }, o.prototype.writeInt32BE = function(u, l, S) {
    return u = +u, l = l >>> 0, S || ee(this, u, l, 4, 2147483647, -2147483648), u < 0 && (u = 4294967295 + u + 1), this[l] = u >>> 24, this[l + 1] = u >>> 16, this[l + 2] = u >>> 8, this[l + 3] = u & 255, l + 4;
  }, o.prototype.writeBigInt64LE = at(function(u, l = 0) {
    return pe(this, u, l, -BigInt("0x8000000000000000"), BigInt("0x7fffffffffffffff"));
  }), o.prototype.writeBigInt64BE = at(function(u, l = 0) {
    return Z(this, u, l, -BigInt("0x8000000000000000"), BigInt("0x7fffffffffffffff"));
  });
  function ne(p2, u, l, S, C, N) {
    if (l + S > p2.length) throw new RangeError("Index out of range");
    if (l < 0) throw new RangeError("Index out of range");
  }
  function fe(p2, u, l, S, C) {
    return u = +u, l = l >>> 0, C || ne(p2, u, l, 4), e.write(p2, u, l, S, 23, 4), l + 4;
  }
  o.prototype.writeFloatLE = function(u, l, S) {
    return fe(this, u, l, true, S);
  }, o.prototype.writeFloatBE = function(u, l, S) {
    return fe(this, u, l, false, S);
  };
  function ge(p2, u, l, S, C) {
    return u = +u, l = l >>> 0, C || ne(p2, u, l, 8), e.write(p2, u, l, S, 52, 8), l + 8;
  }
  o.prototype.writeDoubleLE = function(u, l, S) {
    return ge(this, u, l, true, S);
  }, o.prototype.writeDoubleBE = function(u, l, S) {
    return ge(this, u, l, false, S);
  }, o.prototype.copy = function(u, l, S, C) {
    if (!o.isBuffer(u)) throw new TypeError("argument should be a Buffer");
    if (S || (S = 0), !C && C !== 0 && (C = this.length), l >= u.length && (l = u.length), l || (l = 0), C > 0 && C < S && (C = S), C === S || u.length === 0 || this.length === 0) return 0;
    if (l < 0) throw new RangeError("targetStart out of bounds");
    if (S < 0 || S >= this.length) throw new RangeError("Index out of range");
    if (C < 0) throw new RangeError("sourceEnd out of bounds");
    C > this.length && (C = this.length), u.length - l < C - S && (C = u.length - l + S);
    let N = C - S;
    return this === u && typeof Uint8Array.prototype.copyWithin == "function" ? this.copyWithin(l, S, C) : Uint8Array.prototype.set.call(u, this.subarray(S, C), l), N;
  }, o.prototype.fill = function(u, l, S, C) {
    if (typeof u == "string") {
      if (typeof l == "string" ? (C = l, l = 0, S = this.length) : typeof S == "string" && (C = S, S = this.length), C !== void 0 && typeof C != "string") throw new TypeError("encoding must be a string");
      if (typeof C == "string" && !o.isEncoding(C)) throw new TypeError("Unknown encoding: " + C);
      if (u.length === 1) {
        let F = u.charCodeAt(0);
        (C === "utf8" && F < 128 || C === "latin1") && (u = F);
      }
    } else typeof u == "number" ? u = u & 255 : typeof u == "boolean" && (u = Number(u));
    if (l < 0 || this.length < l || this.length < S) throw new RangeError("Out of range index");
    if (S <= l) return this;
    l = l >>> 0, S = S === void 0 ? this.length : S >>> 0, u || (u = 0);
    let N;
    if (typeof u == "number") for (N = l; N < S; ++N) this[N] = u;
    else {
      let F = o.isBuffer(u) ? u : o.from(u, C), k = F.length;
      if (k === 0) throw new TypeError('The value "' + u + '" is invalid for argument "value"');
      for (N = 0; N < S - l; ++N) this[N + l] = F[N % k];
    }
    return this;
  };
  let ye = {};
  function Ae(p2, u, l) {
    ye[p2] = class extends l {
      constructor() {
        super(), Object.defineProperty(this, "message", { value: u.apply(this, arguments), writable: true, configurable: true }), this.name = `${this.name} [${p2}]`, this.stack, delete this.name;
      }
      get code() {
        return p2;
      }
      set code(C) {
        Object.defineProperty(this, "code", { configurable: true, enumerable: true, value: C, writable: true });
      }
      toString() {
        return `${this.name} [${p2}]: ${this.message}`;
      }
    };
  }
  Ae("ERR_BUFFER_OUT_OF_BOUNDS", function(p2) {
    return p2 ? `${p2} is outside of buffer bounds` : "Attempt to access memory outside buffer bounds";
  }, RangeError), Ae("ERR_INVALID_ARG_TYPE", function(p2, u) {
    return `The "${p2}" argument must be of type number. Received type ${typeof u}`;
  }, TypeError), Ae("ERR_OUT_OF_RANGE", function(p2, u, l) {
    let S = `The value of "${p2}" is out of range.`, C = l;
    return Number.isInteger(l) && Math.abs(l) > 2 ** 32 ? C = _e(String(l)) : typeof l == "bigint" && (C = String(l), (l > BigInt(2) ** BigInt(32) || l < -(BigInt(2) ** BigInt(32))) && (C = _e(C)), C += "n"), S += ` It must be ${u}. Received ${C}`, S;
  }, RangeError);
  function _e(p2) {
    let u = "", l = p2.length, S = p2[0] === "-" ? 1 : 0;
    for (; l >= S + 4; l -= 3) u = `_${p2.slice(l - 3, l)}${u}`;
    return `${p2.slice(0, l)}${u}`;
  }
  function ve(p2, u, l) {
    xe(u, "offset"), (p2[u] === void 0 || p2[u + l] === void 0) && st(u, p2.length - (l + 1));
  }
  function Oe(p2, u, l, S, C, N) {
    if (p2 > l || p2 < u) {
      let F = typeof u == "bigint" ? "n" : "", k;
      throw u === 0 || u === BigInt(0) ? k = `>= 0${F} and < 2${F} ** ${(N + 1) * 8}${F}` : k = `>= -(2${F} ** ${(N + 1) * 8 - 1}${F}) and < 2 ** ${(N + 1) * 8 - 1}${F}`, new ye.ERR_OUT_OF_RANGE("value", k, p2);
    }
    ve(S, C, N);
  }
  function xe(p2, u) {
    if (typeof p2 != "number") throw new ye.ERR_INVALID_ARG_TYPE(u, "number", p2);
  }
  function st(p2, u, l) {
    throw Math.floor(p2) !== p2 ? (xe(p2, l), new ye.ERR_OUT_OF_RANGE("offset", "an integer", p2)) : u < 0 ? new ye.ERR_BUFFER_OUT_OF_BOUNDS() : new ye.ERR_OUT_OF_RANGE("offset", `>= 0 and <= ${u}`, p2);
  }
  let Re = /[^+/0-9A-Za-z-_]/g;
  function Ne(p2) {
    if (p2 = p2.split("=")[0], p2 = p2.trim().replace(Re, ""), p2.length < 2) return "";
    for (; p2.length % 4 !== 0; ) p2 = p2 + "=";
    return p2;
  }
  function Ct(p2, u) {
    u = u || 1 / 0;
    let l, S = p2.length, C = null, N = [];
    for (let F = 0; F < S; ++F) {
      if (l = p2.charCodeAt(F), l > 55295 && l < 57344) {
        if (!C) {
          if (l > 56319) {
            (u -= 3) > -1 && N.push(239, 191, 189);
            continue;
          } else if (F + 1 === S) {
            (u -= 3) > -1 && N.push(239, 191, 189);
            continue;
          }
          C = l;
          continue;
        }
        if (l < 56320) {
          (u -= 3) > -1 && N.push(239, 191, 189), C = l;
          continue;
        }
        l = (C - 55296 << 10 | l - 56320) + 65536;
      } else C && (u -= 3) > -1 && N.push(239, 191, 189);
      if (C = null, l < 128) {
        if ((u -= 1) < 0) break;
        N.push(l);
      } else if (l < 2048) {
        if ((u -= 2) < 0) break;
        N.push(l >> 6 | 192, l & 63 | 128);
      } else if (l < 65536) {
        if ((u -= 3) < 0) break;
        N.push(l >> 12 | 224, l >> 6 & 63 | 128, l & 63 | 128);
      } else if (l < 1114112) {
        if ((u -= 4) < 0) break;
        N.push(l >> 18 | 240, l >> 12 & 63 | 128, l >> 6 & 63 | 128, l & 63 | 128);
      } else throw new Error("Invalid code point");
    }
    return N;
  }
  function Bt(p2) {
    let u = [];
    for (let l = 0; l < p2.length; ++l) u.push(p2.charCodeAt(l) & 255);
    return u;
  }
  function xn(p2, u) {
    let l, S, C, N = [];
    for (let F = 0; F < p2.length && !((u -= 2) < 0); ++F) l = p2.charCodeAt(F), S = l >> 8, C = l % 256, N.push(C), N.push(S);
    return N;
  }
  function Ii(p2) {
    return t.toByteArray(Ne(p2));
  }
  function Vr(p2, u, l, S) {
    let C;
    for (C = 0; C < S && !(C + l >= u.length || C >= p2.length); ++C) u[C + l] = p2[C];
    return C;
  }
  function Ze(p2, u) {
    return p2 instanceof u || p2 != null && p2.constructor != null && p2.constructor.name != null && p2.constructor.name === u.name;
  }
  function Tn(p2) {
    return p2 !== p2;
  }
  let Xo = (function() {
    let p2 = "0123456789abcdef", u = new Array(256);
    for (let l = 0; l < 16; ++l) {
      let S = l * 16;
      for (let C = 0; C < 16; ++C) u[S + C] = p2[l] + p2[C];
    }
    return u;
  })();
  function at(p2) {
    return typeof BigInt > "u" ? Zo : p2;
  }
  function Zo() {
    throw new Error("BigInt not supported");
  }
  return mr;
}
var On, Sf, ki, Ef, mr, Af, xf = ze(() => {
  A();
  x();
  I();
  On = {}, Sf = false;
  ki = {}, Ef = false;
  mr = {}, Af = false;
});
var qe = {};
Kr(qe, { Buffer: () => U, INSPECT_MAX_BYTES: () => nw, default: () => Xt, kMaxLength: () => iw });
var Xt, U, nw, iw, ke = ze(() => {
  A();
  x();
  I();
  xf();
  Xt = If();
  Xt.Buffer;
  Xt.SlowBuffer;
  Xt.INSPECT_MAX_BYTES;
  Xt.kMaxLength;
  U = Xt.Buffer, nw = Xt.INSPECT_MAX_BYTES, iw = Xt.kMaxLength;
});
var x = ze(() => {
  ke();
});
var Ee = D((MP, Tf) => {
  A();
  x();
  I();
  var fs = class extends Error {
    constructor(e) {
      if (!Array.isArray(e)) throw new TypeError(`Expected input to be an Array, got ${typeof e}`);
      let r = "";
      for (let n = 0; n < e.length; n++) r += `    ${e[n].stack}
`;
      super(r), this.name = "AggregateError", this.errors = e;
    }
  };
  Tf.exports = { AggregateError: fs, ArrayIsArray(t) {
    return Array.isArray(t);
  }, ArrayPrototypeIncludes(t, e) {
    return t.includes(e);
  }, ArrayPrototypeIndexOf(t, e) {
    return t.indexOf(e);
  }, ArrayPrototypeJoin(t, e) {
    return t.join(e);
  }, ArrayPrototypeMap(t, e) {
    return t.map(e);
  }, ArrayPrototypePop(t, e) {
    return t.pop(e);
  }, ArrayPrototypePush(t, e) {
    return t.push(e);
  }, ArrayPrototypeSlice(t, e, r) {
    return t.slice(e, r);
  }, Error, FunctionPrototypeCall(t, e, ...r) {
    return t.call(e, ...r);
  }, FunctionPrototypeSymbolHasInstance(t, e) {
    return Function.prototype[Symbol.hasInstance].call(t, e);
  }, MathFloor: Math.floor, Number, NumberIsInteger: Number.isInteger, NumberIsNaN: Number.isNaN, NumberMAX_SAFE_INTEGER: Number.MAX_SAFE_INTEGER, NumberMIN_SAFE_INTEGER: Number.MIN_SAFE_INTEGER, NumberParseInt: Number.parseInt, ObjectDefineProperties(t, e) {
    return Object.defineProperties(t, e);
  }, ObjectDefineProperty(t, e, r) {
    return Object.defineProperty(t, e, r);
  }, ObjectGetOwnPropertyDescriptor(t, e) {
    return Object.getOwnPropertyDescriptor(t, e);
  }, ObjectKeys(t) {
    return Object.keys(t);
  }, ObjectSetPrototypeOf(t, e) {
    return Object.setPrototypeOf(t, e);
  }, Promise, PromisePrototypeCatch(t, e) {
    return t.catch(e);
  }, PromisePrototypeThen(t, e, r) {
    return t.then(e, r);
  }, PromiseReject(t) {
    return Promise.reject(t);
  }, PromiseResolve(t) {
    return Promise.resolve(t);
  }, ReflectApply: Reflect.apply, RegExpPrototypeTest(t, e) {
    return t.test(e);
  }, SafeSet: Set, String, StringPrototypeSlice(t, e, r) {
    return t.slice(e, r);
  }, StringPrototypeToLowerCase(t) {
    return t.toLowerCase();
  }, StringPrototypeToUpperCase(t) {
    return t.toUpperCase();
  }, StringPrototypeTrim(t) {
    return t.trim();
  }, Symbol, SymbolFor: Symbol.for, SymbolAsyncIterator: Symbol.asyncIterator, SymbolHasInstance: Symbol.hasInstance, SymbolIterator: Symbol.iterator, SymbolDispose: Symbol.dispose || Symbol("Symbol.dispose"), SymbolAsyncDispose: Symbol.asyncDispose || Symbol("Symbol.asyncDispose"), TypedArrayPrototypeSet(t, e, r) {
    return t.set(e, r);
  }, Boolean, Uint8Array };
});
var hs = D((HP, Pf) => {
  A();
  x();
  I();
  Pf.exports = { format(t, ...e) {
    return t.replace(/%([sdifj])/g, function(...[r, n]) {
      let i = e.shift();
      return n === "f" ? i.toFixed(6) : n === "j" ? JSON.stringify(i) : n === "s" && typeof i == "object" ? `${i.constructor !== Object ? i.constructor.name : ""} {}`.trim() : i.toString();
    });
  }, inspect(t) {
    switch (typeof t) {
      case "string":
        if (t.includes("'")) if (t.includes('"')) {
          if (!t.includes("`") && !t.includes("${")) return `\`${t}\``;
        } else return `"${t}"`;
        return `'${t}'`;
      case "number":
        return isNaN(t) ? "NaN" : Object.is(t, -0) ? String(t) : t;
      case "bigint":
        return `${String(t)}n`;
      case "boolean":
      case "undefined":
        return String(t);
      case "object":
        return "{}";
    }
  } };
});
var De = D((eO, kf) => {
  A();
  x();
  I();
  var { format: ow, inspect: Ci } = hs(), { AggregateError: sw } = Ee(), aw = globalThis.AggregateError || sw, uw = Symbol("kIsNodeError"), lw = ["string", "function", "number", "object", "Function", "Object", "boolean", "bigint", "symbol"], cw = /^([A-Z][a-z0-9]*)+$/, fw = "__node_internal_", Bi = {};
  function _r(t, e) {
    if (!t) throw new Bi.ERR_INTERNAL_ASSERTION(e);
  }
  function Of(t) {
    let e = "", r = t.length, n = t[0] === "-" ? 1 : 0;
    for (; r >= n + 4; r -= 3) e = `_${t.slice(r - 3, r)}${e}`;
    return `${t.slice(0, r)}${e}`;
  }
  function hw(t, e, r) {
    if (typeof e == "function") return _r(e.length <= r.length, `Code: ${t}; The provided arguments length (${r.length}) does not match the required ones (${e.length}).`), e(...r);
    let n = (e.match(/%[dfijoOs]/g) || []).length;
    return _r(n === r.length, `Code: ${t}; The provided arguments length (${r.length}) does not match the required ones (${n}).`), r.length === 0 ? e : ow(e, ...r);
  }
  function Le(t, e, r) {
    r || (r = Error);
    class n extends r {
      constructor(...s) {
        super(hw(t, e, s));
      }
      toString() {
        return `${this.name} [${t}]: ${this.message}`;
      }
    }
    Object.defineProperties(n.prototype, { name: { value: r.name, writable: true, enumerable: false, configurable: true }, toString: { value() {
      return `${this.name} [${t}]: ${this.message}`;
    }, writable: true, enumerable: false, configurable: true } }), n.prototype.code = t, n.prototype[uw] = true, Bi[t] = n;
  }
  function Rf(t) {
    let e = fw + t.name;
    return Object.defineProperty(t, "name", { value: e }), t;
  }
  function dw(t, e) {
    if (t && e && t !== e) {
      if (Array.isArray(e.errors)) return e.errors.push(t), e;
      let r = new aw([e, t], e.message);
      return r.code = e.code, r;
    }
    return t || e;
  }
  var ds = class extends Error {
    constructor(e = "The operation was aborted", r = void 0) {
      if (r !== void 0 && typeof r != "object") throw new Bi.ERR_INVALID_ARG_TYPE("options", "Object", r);
      super(e, r), this.code = "ABORT_ERR", this.name = "AbortError";
    }
  };
  Le("ERR_ASSERTION", "%s", Error);
  Le("ERR_INVALID_ARG_TYPE", (t, e, r) => {
    _r(typeof t == "string", "'name' must be a string"), Array.isArray(e) || (e = [e]);
    let n = "The ";
    t.endsWith(" argument") ? n += `${t} ` : n += `"${t}" ${t.includes(".") ? "property" : "argument"} `, n += "must be ";
    let i = [], s = [], o = [];
    for (let c of e) _r(typeof c == "string", "All expected entries have to be of type string"), lw.includes(c) ? i.push(c.toLowerCase()) : cw.test(c) ? s.push(c) : (_r(c !== "object", 'The value "object" should be written as "Object"'), o.push(c));
    if (s.length > 0) {
      let c = i.indexOf("object");
      c !== -1 && (i.splice(i, c, 1), s.push("Object"));
    }
    if (i.length > 0) {
      switch (i.length) {
        case 1:
          n += `of type ${i[0]}`;
          break;
        case 2:
          n += `one of type ${i[0]} or ${i[1]}`;
          break;
        default: {
          let c = i.pop();
          n += `one of type ${i.join(", ")}, or ${c}`;
        }
      }
      (s.length > 0 || o.length > 0) && (n += " or ");
    }
    if (s.length > 0) {
      switch (s.length) {
        case 1:
          n += `an instance of ${s[0]}`;
          break;
        case 2:
          n += `an instance of ${s[0]} or ${s[1]}`;
          break;
        default: {
          let c = s.pop();
          n += `an instance of ${s.join(", ")}, or ${c}`;
        }
      }
      o.length > 0 && (n += " or ");
    }
    switch (o.length) {
      case 0:
        break;
      case 1:
        o[0].toLowerCase() !== o[0] && (n += "an "), n += `${o[0]}`;
        break;
      case 2:
        n += `one of ${o[0]} or ${o[1]}`;
        break;
      default: {
        let c = o.pop();
        n += `one of ${o.join(", ")}, or ${c}`;
      }
    }
    if (r == null) n += `. Received ${r}`;
    else if (typeof r == "function" && r.name) n += `. Received function ${r.name}`;
    else if (typeof r == "object") {
      var a;
      if ((a = r.constructor) !== null && a !== void 0 && a.name) n += `. Received an instance of ${r.constructor.name}`;
      else {
        let c = Ci(r, { depth: -1 });
        n += `. Received ${c}`;
      }
    } else {
      let c = Ci(r, { colors: false });
      c.length > 25 && (c = `${c.slice(0, 25)}...`), n += `. Received type ${typeof r} (${c})`;
    }
    return n;
  }, TypeError);
  Le("ERR_INVALID_ARG_VALUE", (t, e, r = "is invalid") => {
    let n = Ci(e);
    return n.length > 128 && (n = n.slice(0, 128) + "..."), `The ${t.includes(".") ? "property" : "argument"} '${t}' ${r}. Received ${n}`;
  }, TypeError);
  Le("ERR_INVALID_RETURN_VALUE", (t, e, r) => {
    var n;
    let i = r != null && (n = r.constructor) !== null && n !== void 0 && n.name ? `instance of ${r.constructor.name}` : `type ${typeof r}`;
    return `Expected ${t} to be returned from the "${e}" function but got ${i}.`;
  }, TypeError);
  Le("ERR_MISSING_ARGS", (...t) => {
    _r(t.length > 0, "At least one arg needs to be specified");
    let e, r = t.length;
    switch (t = (Array.isArray(t) ? t : [t]).map((n) => `"${n}"`).join(" or "), r) {
      case 1:
        e += `The ${t[0]} argument`;
        break;
      case 2:
        e += `The ${t[0]} and ${t[1]} arguments`;
        break;
      default:
        {
          let n = t.pop();
          e += `The ${t.join(", ")}, and ${n} arguments`;
        }
        break;
    }
    return `${e} must be specified`;
  }, TypeError);
  Le("ERR_OUT_OF_RANGE", (t, e, r) => {
    _r(e, 'Missing "range" argument');
    let n;
    if (Number.isInteger(r) && Math.abs(r) > 2 ** 32) n = Of(String(r));
    else if (typeof r == "bigint") {
      n = String(r);
      let i = BigInt(2) ** BigInt(32);
      (r > i || r < -i) && (n = Of(n)), n += "n";
    } else n = Ci(r);
    return `The value of "${t}" is out of range. It must be ${e}. Received ${n}`;
  }, RangeError);
  Le("ERR_MULTIPLE_CALLBACK", "Callback called multiple times", Error);
  Le("ERR_METHOD_NOT_IMPLEMENTED", "The %s method is not implemented", Error);
  Le("ERR_STREAM_ALREADY_FINISHED", "Cannot call %s after a stream was finished", Error);
  Le("ERR_STREAM_CANNOT_PIPE", "Cannot pipe, not readable", Error);
  Le("ERR_STREAM_DESTROYED", "Cannot call %s after a stream was destroyed", Error);
  Le("ERR_STREAM_NULL_VALUES", "May not write null values to stream", TypeError);
  Le("ERR_STREAM_PREMATURE_CLOSE", "Premature close", Error);
  Le("ERR_STREAM_PUSH_AFTER_EOF", "stream.push() after EOF", Error);
  Le("ERR_STREAM_UNSHIFT_AFTER_END_EVENT", "stream.unshift() after end event", Error);
  Le("ERR_STREAM_WRITE_AFTER_END", "write after end", Error);
  Le("ERR_UNKNOWN_ENCODING", "Unknown encoding: %s", TypeError);
  kf.exports = { AbortError: ds, aggregateTwoErrors: Rf(dw), hideStackFrames: Rf, codes: Bi };
});
var Yr = D((cO, Mi) => {
  A();
  x();
  I();
  var { AbortController: Cf, AbortSignal: pw } = typeof self < "u" ? self : typeof window < "u" ? window : void 0;
  Mi.exports = Cf;
  Mi.exports.AbortSignal = pw;
  Mi.exports.default = Cf;
});
var Zt = {};
Kr(Zt, { EventEmitter: () => gw, default: () => Rn, defaultMaxListeners: () => bw, init: () => ww, listenerCount: () => mw, on: () => _w, once: () => vw });
function yw() {
  if (Bf) return Ni;
  Bf = true;
  var t = typeof Reflect == "object" ? Reflect : null, e = t && typeof t.apply == "function" ? t.apply : function(g, O, _) {
    return Function.prototype.apply.call(g, O, _);
  }, r;
  t && typeof t.ownKeys == "function" ? r = t.ownKeys : Object.getOwnPropertySymbols ? r = function(g) {
    return Object.getOwnPropertyNames(g).concat(Object.getOwnPropertySymbols(g));
  } : r = function(g) {
    return Object.getOwnPropertyNames(g);
  };
  function n(T) {
    console && console.warn && console.warn(T);
  }
  var i = Number.isNaN || function(g) {
    return g !== g;
  };
  function s() {
    s.init.call(this);
  }
  Ni = s, Ni.once = m, s.EventEmitter = s, s.prototype._events = void 0, s.prototype._eventsCount = 0, s.prototype._maxListeners = void 0;
  var o = 10;
  function a(T) {
    if (typeof T != "function") throw new TypeError('The "listener" argument must be of type Function. Received type ' + typeof T);
  }
  Object.defineProperty(s, "defaultMaxListeners", { enumerable: true, get: function() {
    return o;
  }, set: function(T) {
    if (typeof T != "number" || T < 0 || i(T)) throw new RangeError('The value of "defaultMaxListeners" is out of range. It must be a non-negative number. Received ' + T + ".");
    o = T;
  } }), s.init = function() {
    (this._events === void 0 || this._events === Object.getPrototypeOf(this)._events) && (this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0), this._maxListeners = this._maxListeners || void 0;
  }, s.prototype.setMaxListeners = function(g) {
    if (typeof g != "number" || g < 0 || i(g)) throw new RangeError('The value of "n" is out of range. It must be a non-negative number. Received ' + g + ".");
    return this._maxListeners = g, this;
  };
  function c(T) {
    return T._maxListeners === void 0 ? s.defaultMaxListeners : T._maxListeners;
  }
  s.prototype.getMaxListeners = function() {
    return c(this);
  }, s.prototype.emit = function(g) {
    for (var O = [], _ = 1; _ < arguments.length; _++) O.push(arguments[_]);
    var q = g === "error", $ = this._events;
    if ($ !== void 0) q = q && $.error === void 0;
    else if (!q) return false;
    if (q) {
      var j;
      if (O.length > 0 && (j = O[0]), j instanceof Error) throw j;
      var J = new Error("Unhandled error." + (j ? " (" + j.message + ")" : ""));
      throw J.context = j, J;
    }
    var W = $[g];
    if (W === void 0) return false;
    if (typeof W == "function") e(W, this, O);
    else for (var H = W.length, G = P(W, H), _ = 0; _ < H; ++_) e(G[_], this, O);
    return true;
  };
  function f(T, g, O, _) {
    var q, $, j;
    if (a(O), $ = T._events, $ === void 0 ? ($ = T._events = /* @__PURE__ */ Object.create(null), T._eventsCount = 0) : ($.newListener !== void 0 && (T.emit("newListener", g, O.listener ? O.listener : O), $ = T._events), j = $[g]), j === void 0) j = $[g] = O, ++T._eventsCount;
    else if (typeof j == "function" ? j = $[g] = _ ? [O, j] : [j, O] : _ ? j.unshift(O) : j.push(O), q = c(T), q > 0 && j.length > q && !j.warned) {
      j.warned = true;
      var J = new Error("Possible EventEmitter memory leak detected. " + j.length + " " + String(g) + " listeners added. Use emitter.setMaxListeners() to increase limit");
      J.name = "MaxListenersExceededWarning", J.emitter = T, J.type = g, J.count = j.length, n(J);
    }
    return T;
  }
  s.prototype.addListener = function(g, O) {
    return f(this, g, O, false);
  }, s.prototype.on = s.prototype.addListener, s.prototype.prependListener = function(g, O) {
    return f(this, g, O, true);
  };
  function h2() {
    if (!this.fired) return this.target.removeListener(this.type, this.wrapFn), this.fired = true, arguments.length === 0 ? this.listener.call(this.target) : this.listener.apply(this.target, arguments);
  }
  function d(T, g, O) {
    var _ = { fired: false, wrapFn: void 0, target: T, type: g, listener: O }, q = h2.bind(_);
    return q.listener = O, _.wrapFn = q, q;
  }
  s.prototype.once = function(g, O) {
    return a(O), this.on(g, d(this, g, O)), this;
  }, s.prototype.prependOnceListener = function(g, O) {
    return a(O), this.prependListener(g, d(this, g, O)), this;
  }, s.prototype.removeListener = function(g, O) {
    var _, q, $, j, J;
    if (a(O), q = this._events, q === void 0) return this;
    if (_ = q[g], _ === void 0) return this;
    if (_ === O || _.listener === O) --this._eventsCount === 0 ? this._events = /* @__PURE__ */ Object.create(null) : (delete q[g], q.removeListener && this.emit("removeListener", g, _.listener || O));
    else if (typeof _ != "function") {
      for ($ = -1, j = _.length - 1; j >= 0; j--) if (_[j] === O || _[j].listener === O) {
        J = _[j].listener, $ = j;
        break;
      }
      if ($ < 0) return this;
      $ === 0 ? _.shift() : E(_, $), _.length === 1 && (q[g] = _[0]), q.removeListener !== void 0 && this.emit("removeListener", g, J || O);
    }
    return this;
  }, s.prototype.off = s.prototype.removeListener, s.prototype.removeAllListeners = function(g) {
    var O, _, q;
    if (_ = this._events, _ === void 0) return this;
    if (_.removeListener === void 0) return arguments.length === 0 ? (this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0) : _[g] !== void 0 && (--this._eventsCount === 0 ? this._events = /* @__PURE__ */ Object.create(null) : delete _[g]), this;
    if (arguments.length === 0) {
      var $ = Object.keys(_), j;
      for (q = 0; q < $.length; ++q) j = $[q], j !== "removeListener" && this.removeAllListeners(j);
      return this.removeAllListeners("removeListener"), this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0, this;
    }
    if (O = _[g], typeof O == "function") this.removeListener(g, O);
    else if (O !== void 0) for (q = O.length - 1; q >= 0; q--) this.removeListener(g, O[q]);
    return this;
  };
  function w(T, g, O) {
    var _ = T._events;
    if (_ === void 0) return [];
    var q = _[g];
    return q === void 0 ? [] : typeof q == "function" ? O ? [q.listener || q] : [q] : O ? b(q) : P(q, q.length);
  }
  s.prototype.listeners = function(g) {
    return w(this, g, true);
  }, s.prototype.rawListeners = function(g) {
    return w(this, g, false);
  }, s.listenerCount = function(T, g) {
    return typeof T.listenerCount == "function" ? T.listenerCount(g) : y.call(T, g);
  }, s.prototype.listenerCount = y;
  function y(T) {
    var g = this._events;
    if (g !== void 0) {
      var O = g[T];
      if (typeof O == "function") return 1;
      if (O !== void 0) return O.length;
    }
    return 0;
  }
  s.prototype.eventNames = function() {
    return this._eventsCount > 0 ? r(this._events) : [];
  };
  function P(T, g) {
    for (var O = new Array(g), _ = 0; _ < g; ++_) O[_] = T[_];
    return O;
  }
  function E(T, g) {
    for (; g + 1 < T.length; g++) T[g] = T[g + 1];
    T.pop();
  }
  function b(T) {
    for (var g = new Array(T.length), O = 0; O < g.length; ++O) g[O] = T[O].listener || T[O];
    return g;
  }
  function m(T, g) {
    return new Promise(function(O, _) {
      function q(j) {
        T.removeListener(g, $), _(j);
      }
      function $() {
        typeof T.removeListener == "function" && T.removeListener("error", q), O([].slice.call(arguments));
      }
      R(T, g, $, { once: true }), g !== "error" && v(T, q, { once: true });
    });
  }
  function v(T, g, O) {
    typeof T.on == "function" && R(T, "error", g, O);
  }
  function R(T, g, O, _) {
    if (typeof T.on == "function") _.once ? T.once(g, O) : T.on(g, O);
    else if (typeof T.addEventListener == "function") T.addEventListener(g, function q($) {
      _.once && T.removeEventListener(g, q), O($);
    });
    else throw new TypeError('The "emitter" argument must be of type EventEmitter. Received type ' + typeof T);
  }
  return Ni;
}
var Ni, Bf, Rn, gw, bw, ww, mw, _w, vw, er = ze(() => {
  A();
  x();
  I();
  Ni = {}, Bf = false;
  Rn = yw();
  Rn.once;
  Rn.once = function(t, e) {
    return new Promise((r, n) => {
      function i(...o) {
        s !== void 0 && t.removeListener("error", s), r(o);
      }
      let s;
      e !== "error" && (s = (o) => {
        t.removeListener(name, i), n(o);
      }, t.once("error", s)), t.once(e, i);
    });
  };
  Rn.on = function(t, e) {
    let r = [], n = [], i = null, s = false, o = { async next() {
      let f = r.shift();
      if (f) return createIterResult(f, false);
      if (i) {
        let h2 = Promise.reject(i);
        return i = null, h2;
      }
      return s ? createIterResult(void 0, true) : new Promise((h2, d) => n.push({ resolve: h2, reject: d }));
    }, async return() {
      t.removeListener(e, a), t.removeListener("error", c), s = true;
      for (let f of n) f.resolve(createIterResult(void 0, true));
      return createIterResult(void 0, true);
    }, throw(f) {
      i = f, t.removeListener(e, a), t.removeListener("error", c);
    }, [Symbol.asyncIterator]() {
      return this;
    } };
    return t.on(e, a), t.on("error", c), o;
    function a(...f) {
      let h2 = n.shift();
      h2 ? h2.resolve(createIterResult(f, false)) : r.push(f);
    }
    function c(f) {
      s = true;
      let h2 = n.shift();
      h2 ? h2.reject(f) : i = f, o.return();
    }
  };
  ({ EventEmitter: gw, defaultMaxListeners: bw, init: ww, listenerCount: mw, on: _w, once: vw } = Rn);
});
var $e = D((OO, ys) => {
  A();
  x();
  I();
  var Sw = (ke(), me(qe)), { format: Ew, inspect: Aw } = hs(), { codes: { ERR_INVALID_ARG_TYPE: ps } } = De(), { kResistStopPropagation: Iw, AggregateError: xw, SymbolDispose: Tw } = Ee(), Pw = globalThis.AbortSignal || Yr().AbortSignal, Ow = globalThis.AbortController || Yr().AbortController, Rw = Object.getPrototypeOf(async function() {
  }).constructor, Mf = globalThis.Blob || Sw.Blob, kw = typeof Mf < "u" ? function(e) {
    return e instanceof Mf;
  } : function(e) {
    return false;
  }, Nf = (t, e) => {
    if (t !== void 0 && (t === null || typeof t != "object" || !("aborted" in t))) throw new ps(e, "AbortSignal", t);
  }, Cw = (t, e) => {
    if (typeof t != "function") throw new ps(e, "Function", t);
  };
  ys.exports = { AggregateError: xw, kEmptyObject: Object.freeze({}), once(t) {
    let e = false;
    return function(...r) {
      e || (e = true, t.apply(this, r));
    };
  }, createDeferredPromise: function() {
    let t, e;
    return { promise: new Promise((n, i) => {
      t = n, e = i;
    }), resolve: t, reject: e };
  }, promisify(t) {
    return new Promise((e, r) => {
      t((n, ...i) => n ? r(n) : e(...i));
    });
  }, debuglog() {
    return function() {
    };
  }, format: Ew, inspect: Aw, types: { isAsyncFunction(t) {
    return t instanceof Rw;
  }, isArrayBufferView(t) {
    return ArrayBuffer.isView(t);
  } }, isBlob: kw, deprecate(t, e) {
    return t;
  }, addAbortListener: (er(), me(Zt)).addAbortListener || function(e, r) {
    if (e === void 0) throw new ps("signal", "AbortSignal", e);
    Nf(e, "signal"), Cw(r, "listener");
    let n;
    return e.aborted ? queueMicrotask(() => r()) : (e.addEventListener("abort", r, { __proto__: null, once: true, [Iw]: true }), n = () => {
      e.removeEventListener("abort", r);
    }), { __proto__: null, [Tw]() {
      var i;
      (i = n) === null || i === void 0 || i();
    } };
  }, AbortSignalAny: Pw.any || function(e) {
    if (e.length === 1) return e[0];
    let r = new Ow(), n = () => r.abort();
    return e.forEach((i) => {
      Nf(i, "signals"), i.addEventListener("abort", n, { once: true });
    }), r.signal.addEventListener("abort", () => {
      e.forEach((i) => i.removeEventListener("abort", n));
    }, { once: true }), r.signal;
  } };
  ys.exports.promisify.custom = Symbol.for("nodejs.util.promisify.custom");
});
var Xr = D((DO, Vf) => {
  A();
  x();
  I();
  var { ArrayIsArray: bs, ArrayPrototypeIncludes: Df, ArrayPrototypeJoin: jf, ArrayPrototypeMap: Bw, NumberIsInteger: ws, NumberIsNaN: Mw, NumberMAX_SAFE_INTEGER: Nw, NumberMIN_SAFE_INTEGER: qw, NumberParseInt: Lw, ObjectPrototypeHasOwnProperty: Uw, RegExpPrototypeExec: Ff, String: Dw, StringPrototypeToUpperCase: jw, StringPrototypeTrim: Fw } = Ee(), { hideStackFrames: et, codes: { ERR_SOCKET_BAD_PORT: Ww, ERR_INVALID_ARG_TYPE: je, ERR_INVALID_ARG_VALUE: Jr, ERR_OUT_OF_RANGE: vr, ERR_UNKNOWN_SIGNAL: qf } } = De(), { normalizeEncoding: $w } = $e(), { isAsyncFunction: Hw, isArrayBufferView: Vw } = $e().types, Lf = {};
  function Gw(t) {
    return t === (t | 0);
  }
  function zw(t) {
    return t === t >>> 0;
  }
  var Kw = /^[0-7]+$/, Qw = "must be a 32-bit unsigned integer or an octal string";
  function Yw(t, e, r) {
    if (typeof t > "u" && (t = r), typeof t == "string") {
      if (Ff(Kw, t) === null) throw new Jr(e, t, Qw);
      t = Lw(t, 8);
    }
    return Wf(t, e), t;
  }
  var Jw = et((t, e, r = qw, n = Nw) => {
    if (typeof t != "number") throw new je(e, "number", t);
    if (!ws(t)) throw new vr(e, "an integer", t);
    if (t < r || t > n) throw new vr(e, `>= ${r} && <= ${n}`, t);
  }), Xw = et((t, e, r = -2147483648, n = 2147483647) => {
    if (typeof t != "number") throw new je(e, "number", t);
    if (!ws(t)) throw new vr(e, "an integer", t);
    if (t < r || t > n) throw new vr(e, `>= ${r} && <= ${n}`, t);
  }), Wf = et((t, e, r = false) => {
    if (typeof t != "number") throw new je(e, "number", t);
    if (!ws(t)) throw new vr(e, "an integer", t);
    let n = r ? 1 : 0, i = 4294967295;
    if (t < n || t > i) throw new vr(e, `>= ${n} && <= ${i}`, t);
  });
  function ms(t, e) {
    if (typeof t != "string") throw new je(e, "string", t);
  }
  function Zw(t, e, r = void 0, n) {
    if (typeof t != "number") throw new je(e, "number", t);
    if (r != null && t < r || n != null && t > n || (r != null || n != null) && Mw(t)) throw new vr(e, `${r != null ? `>= ${r}` : ""}${r != null && n != null ? " && " : ""}${n != null ? `<= ${n}` : ""}`, t);
  }
  var em = et((t, e, r) => {
    if (!Df(r, t)) {
      let i = "must be one of: " + jf(Bw(r, (s) => typeof s == "string" ? `'${s}'` : Dw(s)), ", ");
      throw new Jr(e, t, i);
    }
  });
  function $f(t, e) {
    if (typeof t != "boolean") throw new je(e, "boolean", t);
  }
  function gs(t, e, r) {
    return t == null || !Uw(t, e) ? r : t[e];
  }
  var tm = et((t, e, r = null) => {
    let n = gs(r, "allowArray", false), i = gs(r, "allowFunction", false);
    if (!gs(r, "nullable", false) && t === null || !n && bs(t) || typeof t != "object" && (!i || typeof t != "function")) throw new je(e, "Object", t);
  }), rm = et((t, e) => {
    if (t != null && typeof t != "object" && typeof t != "function") throw new je(e, "a dictionary", t);
  }), qi = et((t, e, r = 0) => {
    if (!bs(t)) throw new je(e, "Array", t);
    if (t.length < r) {
      let n = `must be longer than ${r}`;
      throw new Jr(e, t, n);
    }
  });
  function nm(t, e) {
    qi(t, e);
    for (let r = 0; r < t.length; r++) ms(t[r], `${e}[${r}]`);
  }
  function im(t, e) {
    qi(t, e);
    for (let r = 0; r < t.length; r++) $f(t[r], `${e}[${r}]`);
  }
  function om(t, e) {
    qi(t, e);
    for (let r = 0; r < t.length; r++) {
      let n = t[r], i = `${e}[${r}]`;
      if (n == null) throw new je(i, "AbortSignal", n);
      Hf(n, i);
    }
  }
  function sm(t, e = "signal") {
    if (ms(t, e), Lf[t] === void 0) throw Lf[jw(t)] !== void 0 ? new qf(t + " (signals must use all capital letters)") : new qf(t);
  }
  var am = et((t, e = "buffer") => {
    if (!Vw(t)) throw new je(e, ["Buffer", "TypedArray", "DataView"], t);
  });
  function um(t, e) {
    let r = $w(e), n = t.length;
    if (r === "hex" && n % 2 !== 0) throw new Jr("encoding", e, `is invalid for data of length ${n}`);
  }
  function lm(t, e = "Port", r = true) {
    if (typeof t != "number" && typeof t != "string" || typeof t == "string" && Fw(t).length === 0 || +t !== +t >>> 0 || t > 65535 || t === 0 && !r) throw new Ww(e, t, r);
    return t | 0;
  }
  var Hf = et((t, e) => {
    if (t !== void 0 && (t === null || typeof t != "object" || !("aborted" in t))) throw new je(e, "AbortSignal", t);
  }), cm = et((t, e) => {
    if (typeof t != "function") throw new je(e, "Function", t);
  }), fm = et((t, e) => {
    if (typeof t != "function" || Hw(t)) throw new je(e, "Function", t);
  }), hm = et((t, e) => {
    if (t !== void 0) throw new je(e, "undefined", t);
  });
  function dm(t, e, r) {
    if (!Df(r, t)) throw new je(e, `('${jf(r, "|")}')`, t);
  }
  var pm = /^(?:<[^>]*>)(?:\s*;\s*[^;"\s]+(?:=(")?[^;"\s]*\1)?)*$/;
  function Uf(t, e) {
    if (typeof t > "u" || !Ff(pm, t)) throw new Jr(e, t, 'must be an array or string of format "</styles.css>; rel=preload; as=style"');
  }
  function ym(t) {
    if (typeof t == "string") return Uf(t, "hints"), t;
    if (bs(t)) {
      let e = t.length, r = "";
      if (e === 0) return r;
      for (let n = 0; n < e; n++) {
        let i = t[n];
        Uf(i, "hints"), r += i, n !== e - 1 && (r += ", ");
      }
      return r;
    }
    throw new Jr("hints", t, 'must be an array or string of format "</styles.css>; rel=preload; as=style"');
  }
  Vf.exports = { isInt32: Gw, isUint32: zw, parseFileMode: Yw, validateArray: qi, validateStringArray: nm, validateBooleanArray: im, validateAbortSignalArray: om, validateBoolean: $f, validateBuffer: am, validateDictionary: rm, validateEncoding: um, validateFunction: cm, validateInt32: Xw, validateInteger: Jw, validateNumber: Zw, validateObject: tm, validateOneOf: em, validatePlainFunction: fm, validatePort: lm, validateSignalName: sm, validateString: ms, validateUint32: Wf, validateUndefined: hm, validateUnion: dm, validateAbortSignal: Hf, validateLinkHeaderValue: ym };
});
var tr = D((QO, Qf) => {
  A();
  x();
  I();
  var Te = Qf.exports = {}, gt, bt;
  function _s() {
    throw new Error("setTimeout has not been defined");
  }
  function vs() {
    throw new Error("clearTimeout has not been defined");
  }
  (function() {
    try {
      typeof setTimeout == "function" ? gt = setTimeout : gt = _s;
    } catch {
      gt = _s;
    }
    try {
      typeof clearTimeout == "function" ? bt = clearTimeout : bt = vs;
    } catch {
      bt = vs;
    }
  })();
  function Gf(t) {
    if (gt === setTimeout) return setTimeout(t, 0);
    if ((gt === _s || !gt) && setTimeout) return gt = setTimeout, setTimeout(t, 0);
    try {
      return gt(t, 0);
    } catch {
      try {
        return gt.call(null, t, 0);
      } catch {
        return gt.call(this, t, 0);
      }
    }
  }
  function gm(t) {
    if (bt === clearTimeout) return clearTimeout(t);
    if ((bt === vs || !bt) && clearTimeout) return bt = clearTimeout, clearTimeout(t);
    try {
      return bt(t);
    } catch {
      try {
        return bt.call(null, t);
      } catch {
        return bt.call(this, t);
      }
    }
  }
  var qt = [], Zr = false, Sr, Li = -1;
  function bm() {
    !Zr || !Sr || (Zr = false, Sr.length ? qt = Sr.concat(qt) : Li = -1, qt.length && zf());
  }
  function zf() {
    if (!Zr) {
      var t = Gf(bm);
      Zr = true;
      for (var e = qt.length; e; ) {
        for (Sr = qt, qt = []; ++Li < e; ) Sr && Sr[Li].run();
        Li = -1, e = qt.length;
      }
      Sr = null, Zr = false, gm(t);
    }
  }
  Te.nextTick = function(t) {
    var e = new Array(arguments.length - 1);
    if (arguments.length > 1) for (var r = 1; r < arguments.length; r++) e[r - 1] = arguments[r];
    qt.push(new Kf(t, e)), qt.length === 1 && !Zr && Gf(zf);
  };
  function Kf(t, e) {
    this.fun = t, this.array = e;
  }
  Kf.prototype.run = function() {
    this.fun.apply(null, this.array);
  };
  Te.title = "browser";
  Te.browser = true;
  Te.env = {};
  Te.argv = [];
  Te.version = "";
  Te.versions = {};
  function Lt() {
  }
  Te.on = Lt;
  Te.addListener = Lt;
  Te.once = Lt;
  Te.off = Lt;
  Te.removeListener = Lt;
  Te.removeAllListeners = Lt;
  Te.emit = Lt;
  Te.prependListener = Lt;
  Te.prependOnceListener = Lt;
  Te.listeners = function(t) {
    return [];
  };
  Te.binding = function(t) {
    throw new Error("process.binding is not supported");
  };
  Te.cwd = function() {
    return "/";
  };
  Te.chdir = function(t) {
    throw new Error("process.chdir is not supported");
  };
  Te.umask = function() {
    return 0;
  };
});
var mt = D((oR, ch) => {
  A();
  x();
  I();
  var { SymbolAsyncIterator: Yf, SymbolIterator: Jf, SymbolFor: Er } = Ee(), Xf = Er("nodejs.stream.destroyed"), Zf = Er("nodejs.stream.errored"), Ss = Er("nodejs.stream.readable"), Es = Er("nodejs.stream.writable"), eh = Er("nodejs.stream.disturbed"), wm = Er("nodejs.webstream.isClosedPromise"), mm = Er("nodejs.webstream.controllerErrorFunction");
  function Ui(t, e = false) {
    var r;
    return !!(t && typeof t.pipe == "function" && typeof t.on == "function" && (!e || typeof t.pause == "function" && typeof t.resume == "function") && (!t._writableState || ((r = t._readableState) === null || r === void 0 ? void 0 : r.readable) !== false) && (!t._writableState || t._readableState));
  }
  function Di(t) {
    var e;
    return !!(t && typeof t.write == "function" && typeof t.on == "function" && (!t._readableState || ((e = t._writableState) === null || e === void 0 ? void 0 : e.writable) !== false));
  }
  function _m(t) {
    return !!(t && typeof t.pipe == "function" && t._readableState && typeof t.on == "function" && typeof t.write == "function");
  }
  function wt(t) {
    return t && (t._readableState || t._writableState || typeof t.write == "function" && typeof t.on == "function" || typeof t.pipe == "function" && typeof t.on == "function");
  }
  function th(t) {
    return !!(t && !wt(t) && typeof t.pipeThrough == "function" && typeof t.getReader == "function" && typeof t.cancel == "function");
  }
  function rh(t) {
    return !!(t && !wt(t) && typeof t.getWriter == "function" && typeof t.abort == "function");
  }
  function nh(t) {
    return !!(t && !wt(t) && typeof t.readable == "object" && typeof t.writable == "object");
  }
  function vm(t) {
    return th(t) || rh(t) || nh(t);
  }
  function Sm(t, e) {
    return t == null ? false : e === true ? typeof t[Yf] == "function" : e === false ? typeof t[Jf] == "function" : typeof t[Yf] == "function" || typeof t[Jf] == "function";
  }
  function ji(t) {
    if (!wt(t)) return null;
    let e = t._writableState, r = t._readableState, n = e || r;
    return !!(t.destroyed || t[Xf] || n != null && n.destroyed);
  }
  function ih(t) {
    if (!Di(t)) return null;
    if (t.writableEnded === true) return true;
    let e = t._writableState;
    return e != null && e.errored ? false : typeof e?.ended != "boolean" ? null : e.ended;
  }
  function Em(t, e) {
    if (!Di(t)) return null;
    if (t.writableFinished === true) return true;
    let r = t._writableState;
    return r != null && r.errored ? false : typeof r?.finished != "boolean" ? null : !!(r.finished || e === false && r.ended === true && r.length === 0);
  }
  function Am(t) {
    if (!Ui(t)) return null;
    if (t.readableEnded === true) return true;
    let e = t._readableState;
    return !e || e.errored ? false : typeof e?.ended != "boolean" ? null : e.ended;
  }
  function oh(t, e) {
    if (!Ui(t)) return null;
    let r = t._readableState;
    return r != null && r.errored ? false : typeof r?.endEmitted != "boolean" ? null : !!(r.endEmitted || e === false && r.ended === true && r.length === 0);
  }
  function sh(t) {
    return t && t[Ss] != null ? t[Ss] : typeof t?.readable != "boolean" ? null : ji(t) ? false : Ui(t) && t.readable && !oh(t);
  }
  function ah(t) {
    return t && t[Es] != null ? t[Es] : typeof t?.writable != "boolean" ? null : ji(t) ? false : Di(t) && t.writable && !ih(t);
  }
  function Im(t, e) {
    return wt(t) ? ji(t) ? true : !(e?.readable !== false && sh(t) || e?.writable !== false && ah(t)) : null;
  }
  function xm(t) {
    var e, r;
    return wt(t) ? t.writableErrored ? t.writableErrored : (e = (r = t._writableState) === null || r === void 0 ? void 0 : r.errored) !== null && e !== void 0 ? e : null : null;
  }
  function Tm(t) {
    var e, r;
    return wt(t) ? t.readableErrored ? t.readableErrored : (e = (r = t._readableState) === null || r === void 0 ? void 0 : r.errored) !== null && e !== void 0 ? e : null : null;
  }
  function Pm(t) {
    if (!wt(t)) return null;
    if (typeof t.closed == "boolean") return t.closed;
    let e = t._writableState, r = t._readableState;
    return typeof e?.closed == "boolean" || typeof r?.closed == "boolean" ? e?.closed || r?.closed : typeof t._closed == "boolean" && uh(t) ? t._closed : null;
  }
  function uh(t) {
    return typeof t._closed == "boolean" && typeof t._defaultKeepAlive == "boolean" && typeof t._removedConnection == "boolean" && typeof t._removedContLen == "boolean";
  }
  function lh(t) {
    return typeof t._sent100 == "boolean" && uh(t);
  }
  function Om(t) {
    var e;
    return typeof t._consuming == "boolean" && typeof t._dumped == "boolean" && ((e = t.req) === null || e === void 0 ? void 0 : e.upgradeOrConnect) === void 0;
  }
  function Rm(t) {
    if (!wt(t)) return null;
    let e = t._writableState, r = t._readableState, n = e || r;
    return !n && lh(t) || !!(n && n.autoDestroy && n.emitClose && n.closed === false);
  }
  function km(t) {
    var e;
    return !!(t && ((e = t[eh]) !== null && e !== void 0 ? e : t.readableDidRead || t.readableAborted));
  }
  function Cm(t) {
    var e, r, n, i, s, o, a, c, f, h2;
    return !!(t && ((e = (r = (n = (i = (s = (o = t[Zf]) !== null && o !== void 0 ? o : t.readableErrored) !== null && s !== void 0 ? s : t.writableErrored) !== null && i !== void 0 ? i : (a = t._readableState) === null || a === void 0 ? void 0 : a.errorEmitted) !== null && n !== void 0 ? n : (c = t._writableState) === null || c === void 0 ? void 0 : c.errorEmitted) !== null && r !== void 0 ? r : (f = t._readableState) === null || f === void 0 ? void 0 : f.errored) !== null && e !== void 0 ? e : !((h2 = t._writableState) === null || h2 === void 0) && h2.errored));
  }
  ch.exports = { isDestroyed: ji, kIsDestroyed: Xf, isDisturbed: km, kIsDisturbed: eh, isErrored: Cm, kIsErrored: Zf, isReadable: sh, kIsReadable: Ss, kIsClosedPromise: wm, kControllerErrorFunction: mm, kIsWritable: Es, isClosed: Pm, isDuplexNodeStream: _m, isFinished: Im, isIterable: Sm, isReadableNodeStream: Ui, isReadableStream: th, isReadableEnded: Am, isReadableFinished: oh, isReadableErrored: Tm, isNodeStream: wt, isWebStream: vm, isWritable: ah, isWritableNodeStream: Di, isWritableStream: rh, isWritableEnded: ih, isWritableFinished: Em, isWritableErrored: xm, isServerRequest: Om, isServerResponse: lh, willEmitClose: Rm, isTransformStream: nh };
});
var Ut = D((yR, Ps) => {
  A();
  x();
  I();
  var rr = tr(), { AbortError: mh, codes: Bm } = De(), { ERR_INVALID_ARG_TYPE: Mm, ERR_STREAM_PREMATURE_CLOSE: fh } = Bm, { kEmptyObject: Is, once: xs } = $e(), { validateAbortSignal: Nm, validateFunction: qm, validateObject: Lm, validateBoolean: Um } = Xr(), { Promise: Dm, PromisePrototypeThen: jm, SymbolDispose: _h } = Ee(), { isClosed: Fm, isReadable: hh, isReadableNodeStream: As, isReadableStream: Wm, isReadableFinished: dh, isReadableErrored: ph, isWritable: yh, isWritableNodeStream: gh, isWritableStream: $m, isWritableFinished: bh, isWritableErrored: wh, isNodeStream: Hm, willEmitClose: Vm, kIsClosedPromise: Gm } = mt(), en;
  function zm(t) {
    return t.setHeader && typeof t.abort == "function";
  }
  var Ts = () => {
  };
  function vh(t, e, r) {
    var n, i;
    if (arguments.length === 2 ? (r = e, e = Is) : e == null ? e = Is : Lm(e, "options"), qm(r, "callback"), Nm(e.signal, "options.signal"), r = xs(r), Wm(t) || $m(t)) return Km(t, e, r);
    if (!Hm(t)) throw new Mm("stream", ["ReadableStream", "WritableStream", "Stream"], t);
    let s = (n = e.readable) !== null && n !== void 0 ? n : As(t), o = (i = e.writable) !== null && i !== void 0 ? i : gh(t), a = t._writableState, c = t._readableState, f = () => {
      t.writable || w();
    }, h2 = Vm(t) && As(t) === s && gh(t) === o, d = bh(t, false), w = () => {
      d = true, t.destroyed && (h2 = false), !(h2 && (!t.readable || s)) && (!s || y) && r.call(t);
    }, y = dh(t, false), P = () => {
      y = true, t.destroyed && (h2 = false), !(h2 && (!t.writable || o)) && (!o || d) && r.call(t);
    }, E = (g) => {
      r.call(t, g);
    }, b = Fm(t), m = () => {
      b = true;
      let g = wh(t) || ph(t);
      if (g && typeof g != "boolean") return r.call(t, g);
      if (s && !y && As(t, true) && !dh(t, false)) return r.call(t, new fh());
      if (o && !d && !bh(t, false)) return r.call(t, new fh());
      r.call(t);
    }, v = () => {
      b = true;
      let g = wh(t) || ph(t);
      if (g && typeof g != "boolean") return r.call(t, g);
      r.call(t);
    }, R = () => {
      t.req.on("finish", w);
    };
    zm(t) ? (t.on("complete", w), h2 || t.on("abort", m), t.req ? R() : t.on("request", R)) : o && !a && (t.on("end", f), t.on("close", f)), !h2 && typeof t.aborted == "boolean" && t.on("aborted", m), t.on("end", P), t.on("finish", w), e.error !== false && t.on("error", E), t.on("close", m), b ? rr.nextTick(m) : a != null && a.errorEmitted || c != null && c.errorEmitted ? h2 || rr.nextTick(v) : (!s && (!h2 || hh(t)) && (d || yh(t) === false) || !o && (!h2 || yh(t)) && (y || hh(t) === false) || c && t.req && t.aborted) && rr.nextTick(v);
    let T = () => {
      r = Ts, t.removeListener("aborted", m), t.removeListener("complete", w), t.removeListener("abort", m), t.removeListener("request", R), t.req && t.req.removeListener("finish", w), t.removeListener("end", f), t.removeListener("close", f), t.removeListener("finish", w), t.removeListener("end", P), t.removeListener("error", E), t.removeListener("close", m);
    };
    if (e.signal && !b) {
      let g = () => {
        let O = r;
        T(), O.call(t, new mh(void 0, { cause: e.signal.reason }));
      };
      if (e.signal.aborted) rr.nextTick(g);
      else {
        en = en || $e().addAbortListener;
        let O = en(e.signal, g), _ = r;
        r = xs((...q) => {
          O[_h](), _.apply(t, q);
        });
      }
    }
    return T;
  }
  function Km(t, e, r) {
    let n = false, i = Ts;
    if (e.signal) if (i = () => {
      n = true, r.call(t, new mh(void 0, { cause: e.signal.reason }));
    }, e.signal.aborted) rr.nextTick(i);
    else {
      en = en || $e().addAbortListener;
      let o = en(e.signal, i), a = r;
      r = xs((...c) => {
        o[_h](), a.apply(t, c);
      });
    }
    let s = (...o) => {
      n || rr.nextTick(() => r.apply(t, o));
    };
    return jm(t[Gm].promise, s, s), Ts;
  }
  function Qm(t, e) {
    var r;
    let n = false;
    return e === null && (e = Is), (r = e) !== null && r !== void 0 && r.cleanup && (Um(e.cleanup, "cleanup"), n = e.cleanup), new Dm((i, s) => {
      let o = vh(t, e, (a) => {
        n && o(), a ? s(a) : i();
      });
    });
  }
  Ps.exports = vh;
  Ps.exports.finished = Qm;
});
var Ar = D((IR, Oh) => {
  A();
  x();
  I();
  var _t = tr(), { aggregateTwoErrors: Ym, codes: { ERR_MULTIPLE_CALLBACK: Jm }, AbortError: Xm } = De(), { Symbol: Ah } = Ee(), { kIsDestroyed: Zm, isDestroyed: e_, isFinished: t_, isServerRequest: r_ } = mt(), Ih = Ah("kDestroy"), Os = Ah("kConstruct");
  function xh(t, e, r) {
    t && (t.stack, e && !e.errored && (e.errored = t), r && !r.errored && (r.errored = t));
  }
  function n_(t, e) {
    let r = this._readableState, n = this._writableState, i = n || r;
    return n != null && n.destroyed || r != null && r.destroyed ? (typeof e == "function" && e(), this) : (xh(t, n, r), n && (n.destroyed = true), r && (r.destroyed = true), i.constructed ? Sh(this, t, e) : this.once(Ih, function(s) {
      Sh(this, Ym(s, t), e);
    }), this);
  }
  function Sh(t, e, r) {
    let n = false;
    function i(s) {
      if (n) return;
      n = true;
      let o = t._readableState, a = t._writableState;
      xh(s, a, o), a && (a.closed = true), o && (o.closed = true), typeof r == "function" && r(s), s ? _t.nextTick(i_, t, s) : _t.nextTick(Th, t);
    }
    try {
      t._destroy(e || null, i);
    } catch (s) {
      i(s);
    }
  }
  function i_(t, e) {
    Rs(t, e), Th(t);
  }
  function Th(t) {
    let e = t._readableState, r = t._writableState;
    r && (r.closeEmitted = true), e && (e.closeEmitted = true), (r != null && r.emitClose || e != null && e.emitClose) && t.emit("close");
  }
  function Rs(t, e) {
    let r = t._readableState, n = t._writableState;
    n != null && n.errorEmitted || r != null && r.errorEmitted || (n && (n.errorEmitted = true), r && (r.errorEmitted = true), t.emit("error", e));
  }
  function o_() {
    let t = this._readableState, e = this._writableState;
    t && (t.constructed = true, t.closed = false, t.closeEmitted = false, t.destroyed = false, t.errored = null, t.errorEmitted = false, t.reading = false, t.ended = t.readable === false, t.endEmitted = t.readable === false), e && (e.constructed = true, e.destroyed = false, e.closed = false, e.closeEmitted = false, e.errored = null, e.errorEmitted = false, e.finalCalled = false, e.prefinished = false, e.ended = e.writable === false, e.ending = e.writable === false, e.finished = e.writable === false);
  }
  function ks(t, e, r) {
    let n = t._readableState, i = t._writableState;
    if (i != null && i.destroyed || n != null && n.destroyed) return this;
    n != null && n.autoDestroy || i != null && i.autoDestroy ? t.destroy(e) : e && (e.stack, i && !i.errored && (i.errored = e), n && !n.errored && (n.errored = e), r ? _t.nextTick(Rs, t, e) : Rs(t, e));
  }
  function s_(t, e) {
    if (typeof t._construct != "function") return;
    let r = t._readableState, n = t._writableState;
    r && (r.constructed = false), n && (n.constructed = false), t.once(Os, e), !(t.listenerCount(Os) > 1) && _t.nextTick(a_, t);
  }
  function a_(t) {
    let e = false;
    function r(n) {
      if (e) {
        ks(t, n ?? new Jm());
        return;
      }
      e = true;
      let i = t._readableState, s = t._writableState, o = s || i;
      i && (i.constructed = true), s && (s.constructed = true), o.destroyed ? t.emit(Ih, n) : n ? ks(t, n, true) : _t.nextTick(u_, t);
    }
    try {
      t._construct((n) => {
        _t.nextTick(r, n);
      });
    } catch (n) {
      _t.nextTick(r, n);
    }
  }
  function u_(t) {
    t.emit(Os);
  }
  function Eh(t) {
    return t?.setHeader && typeof t.abort == "function";
  }
  function Ph(t) {
    t.emit("close");
  }
  function l_(t, e) {
    t.emit("error", e), _t.nextTick(Ph, t);
  }
  function c_(t, e) {
    !t || e_(t) || (!e && !t_(t) && (e = new Xm()), r_(t) ? (t.socket = null, t.destroy(e)) : Eh(t) ? t.abort() : Eh(t.req) ? t.req.abort() : typeof t.destroy == "function" ? t.destroy(e) : typeof t.close == "function" ? t.close() : e ? _t.nextTick(l_, t, e) : _t.nextTick(Ph, t), t.destroyed || (t[Zm] = true));
  }
  Oh.exports = { construct: s_, destroyer: c_, destroy: n_, undestroy: o_, errorOrDestroy: ks };
});
var $i = D((NR, kh) => {
  A();
  x();
  I();
  var { ArrayIsArray: f_, ObjectSetPrototypeOf: Rh } = Ee(), { EventEmitter: Fi } = (er(), me(Zt));
  function Wi(t) {
    Fi.call(this, t);
  }
  Rh(Wi.prototype, Fi.prototype);
  Rh(Wi, Fi);
  Wi.prototype.pipe = function(t, e) {
    let r = this;
    function n(h2) {
      t.writable && t.write(h2) === false && r.pause && r.pause();
    }
    r.on("data", n);
    function i() {
      r.readable && r.resume && r.resume();
    }
    t.on("drain", i), !t._isStdio && (!e || e.end !== false) && (r.on("end", o), r.on("close", a));
    let s = false;
    function o() {
      s || (s = true, t.end());
    }
    function a() {
      s || (s = true, typeof t.destroy == "function" && t.destroy());
    }
    function c(h2) {
      f(), Fi.listenerCount(this, "error") === 0 && this.emit("error", h2);
    }
    Cs(r, "error", c), Cs(t, "error", c);
    function f() {
      r.removeListener("data", n), t.removeListener("drain", i), r.removeListener("end", o), r.removeListener("close", a), r.removeListener("error", c), t.removeListener("error", c), r.removeListener("end", f), r.removeListener("close", f), t.removeListener("close", f);
    }
    return r.on("end", f), r.on("close", f), t.on("close", f), t.emit("pipe", r), t;
  };
  function Cs(t, e, r) {
    if (typeof t.prependListener == "function") return t.prependListener(e, r);
    !t._events || !t._events[e] ? t.on(e, r) : f_(t._events[e]) ? t._events[e].unshift(r) : t._events[e] = [r, t._events[e]];
  }
  kh.exports = { Stream: Wi, prependListener: Cs };
});
var kn = D((VR, Hi) => {
  A();
  x();
  I();
  var { SymbolDispose: h_ } = Ee(), { AbortError: Ch, codes: d_ } = De(), { isNodeStream: Bh, isWebStream: p_, kControllerErrorFunction: y_ } = mt(), g_ = Ut(), { ERR_INVALID_ARG_TYPE: Mh } = d_, Bs, b_ = (t, e) => {
    if (typeof t != "object" || !("aborted" in t)) throw new Mh(e, "AbortSignal", t);
  };
  Hi.exports.addAbortSignal = function(e, r) {
    if (b_(e, "signal"), !Bh(r) && !p_(r)) throw new Mh("stream", ["ReadableStream", "WritableStream", "Stream"], r);
    return Hi.exports.addAbortSignalNoValidate(e, r);
  };
  Hi.exports.addAbortSignalNoValidate = function(t, e) {
    if (typeof t != "object" || !("aborted" in t)) return e;
    let r = Bh(e) ? () => {
      e.destroy(new Ch(void 0, { cause: t.reason }));
    } : () => {
      e[y_](new Ch(void 0, { cause: t.reason }));
    };
    if (t.aborted) r();
    else {
      Bs = Bs || $e().addAbortListener;
      let n = Bs(t, r);
      g_(e, n[h_]);
    }
    return e;
  };
});
var Lh = D((rk, qh) => {
  A();
  x();
  I();
  var { StringPrototypeSlice: Nh, SymbolIterator: w_, TypedArrayPrototypeSet: Vi, Uint8Array: m_ } = Ee(), { Buffer: Ms } = (ke(), me(qe)), { inspect: __ } = $e();
  qh.exports = class {
    constructor() {
      this.head = null, this.tail = null, this.length = 0;
    }
    push(e) {
      let r = { data: e, next: null };
      this.length > 0 ? this.tail.next = r : this.head = r, this.tail = r, ++this.length;
    }
    unshift(e) {
      let r = { data: e, next: this.head };
      this.length === 0 && (this.tail = r), this.head = r, ++this.length;
    }
    shift() {
      if (this.length === 0) return;
      let e = this.head.data;
      return this.length === 1 ? this.head = this.tail = null : this.head = this.head.next, --this.length, e;
    }
    clear() {
      this.head = this.tail = null, this.length = 0;
    }
    join(e) {
      if (this.length === 0) return "";
      let r = this.head, n = "" + r.data;
      for (; (r = r.next) !== null; ) n += e + r.data;
      return n;
    }
    concat(e) {
      if (this.length === 0) return Ms.alloc(0);
      let r = Ms.allocUnsafe(e >>> 0), n = this.head, i = 0;
      for (; n; ) Vi(r, n.data, i), i += n.data.length, n = n.next;
      return r;
    }
    consume(e, r) {
      let n = this.head.data;
      if (e < n.length) {
        let i = n.slice(0, e);
        return this.head.data = n.slice(e), i;
      }
      return e === n.length ? this.shift() : r ? this._getString(e) : this._getBuffer(e);
    }
    first() {
      return this.head.data;
    }
    *[w_]() {
      for (let e = this.head; e; e = e.next) yield e.data;
    }
    _getString(e) {
      let r = "", n = this.head, i = 0;
      do {
        let s = n.data;
        if (e > s.length) r += s, e -= s.length;
        else {
          e === s.length ? (r += s, ++i, n.next ? this.head = n.next : this.head = this.tail = null) : (r += Nh(s, 0, e), this.head = n, n.data = Nh(s, e));
          break;
        }
        ++i;
      } while ((n = n.next) !== null);
      return this.length -= i, r;
    }
    _getBuffer(e) {
      let r = Ms.allocUnsafe(e), n = e, i = this.head, s = 0;
      do {
        let o = i.data;
        if (e > o.length) Vi(r, o, n - e), e -= o.length;
        else {
          e === o.length ? (Vi(r, o, n - e), ++s, i.next ? this.head = i.next : this.head = this.tail = null) : (Vi(r, new m_(o.buffer, o.byteOffset, e), n - e), this.head = i, i.data = o.slice(e));
          break;
        }
        ++s;
      } while ((i = i.next) !== null);
      return this.length -= s, r;
    }
    [Symbol.for("nodejs.util.inspect.custom")](e, r) {
      return __(this, { ...r, depth: 0, customInspect: false });
    }
  };
});
var Cn = D((hk, Fh) => {
  A();
  x();
  I();
  var { MathFloor: v_, NumberIsInteger: S_ } = Ee(), { validateInteger: E_ } = Xr(), { ERR_INVALID_ARG_VALUE: A_ } = De().codes, Uh = 16 * 1024, Dh = 16;
  function I_(t, e, r) {
    return t.highWaterMark != null ? t.highWaterMark : e ? t[r] : null;
  }
  function jh(t) {
    return t ? Dh : Uh;
  }
  function x_(t, e) {
    E_(e, "value", 0), t ? Dh = e : Uh = e;
  }
  function T_(t, e, r, n) {
    let i = I_(e, n, r);
    if (i != null) {
      if (!S_(i) || i < 0) {
        let s = n ? `options.${r}` : "options.highWaterMark";
        throw new A_(s, i);
      }
      return v_(i);
    }
    return jh(t.objectMode);
  }
  Fh.exports = { getHighWaterMark: T_, getDefaultHighWaterMark: jh, setDefaultHighWaterMark: x_ };
});
var Hh = D((Ns, $h) => {
  A();
  x();
  I();
  var Gi = (ke(), me(qe)), vt = Gi.Buffer;
  function Wh(t, e) {
    for (var r in t) e[r] = t[r];
  }
  vt.from && vt.alloc && vt.allocUnsafe && vt.allocUnsafeSlow ? $h.exports = Gi : (Wh(Gi, Ns), Ns.Buffer = Ir);
  function Ir(t, e, r) {
    return vt(t, e, r);
  }
  Ir.prototype = Object.create(vt.prototype);
  Wh(vt, Ir);
  Ir.from = function(t, e, r) {
    if (typeof t == "number") throw new TypeError("Argument must not be a number");
    return vt(t, e, r);
  };
  Ir.alloc = function(t, e, r) {
    if (typeof t != "number") throw new TypeError("Argument must be a number");
    var n = vt(t);
    return e !== void 0 ? typeof r == "string" ? n.fill(e, r) : n.fill(e) : n.fill(0), n;
  };
  Ir.allocUnsafe = function(t) {
    if (typeof t != "number") throw new TypeError("Argument must be a number");
    return vt(t);
  };
  Ir.allocUnsafeSlow = function(t) {
    if (typeof t != "number") throw new TypeError("Argument must be a number");
    return Gi.SlowBuffer(t);
  };
});
var zh = D((Gh) => {
  A();
  x();
  I();
  var Ls = Hh().Buffer, Vh = Ls.isEncoding || function(t) {
    switch (t = "" + t, t && t.toLowerCase()) {
      case "hex":
      case "utf8":
      case "utf-8":
      case "ascii":
      case "binary":
      case "base64":
      case "ucs2":
      case "ucs-2":
      case "utf16le":
      case "utf-16le":
      case "raw":
        return true;
      default:
        return false;
    }
  };
  function P_(t) {
    if (!t) return "utf8";
    for (var e; ; ) switch (t) {
      case "utf8":
      case "utf-8":
        return "utf8";
      case "ucs2":
      case "ucs-2":
      case "utf16le":
      case "utf-16le":
        return "utf16le";
      case "latin1":
      case "binary":
        return "latin1";
      case "base64":
      case "ascii":
      case "hex":
        return t;
      default:
        if (e) return;
        t = ("" + t).toLowerCase(), e = true;
    }
  }
  function O_(t) {
    var e = P_(t);
    if (typeof e != "string" && (Ls.isEncoding === Vh || !Vh(t))) throw new Error("Unknown encoding: " + t);
    return e || t;
  }
  Gh.StringDecoder = Bn;
  function Bn(t) {
    this.encoding = O_(t);
    var e;
    switch (this.encoding) {
      case "utf16le":
        this.text = N_, this.end = q_, e = 4;
        break;
      case "utf8":
        this.fillLast = C_, e = 4;
        break;
      case "base64":
        this.text = L_, this.end = U_, e = 3;
        break;
      default:
        this.write = D_, this.end = j_;
        return;
    }
    this.lastNeed = 0, this.lastTotal = 0, this.lastChar = Ls.allocUnsafe(e);
  }
  Bn.prototype.write = function(t) {
    if (t.length === 0) return "";
    var e, r;
    if (this.lastNeed) {
      if (e = this.fillLast(t), e === void 0) return "";
      r = this.lastNeed, this.lastNeed = 0;
    } else r = 0;
    return r < t.length ? e ? e + this.text(t, r) : this.text(t, r) : e || "";
  };
  Bn.prototype.end = M_;
  Bn.prototype.text = B_;
  Bn.prototype.fillLast = function(t) {
    if (this.lastNeed <= t.length) return t.copy(this.lastChar, this.lastTotal - this.lastNeed, 0, this.lastNeed), this.lastChar.toString(this.encoding, 0, this.lastTotal);
    t.copy(this.lastChar, this.lastTotal - this.lastNeed, 0, t.length), this.lastNeed -= t.length;
  };
  function qs(t) {
    return t <= 127 ? 0 : t >> 5 === 6 ? 2 : t >> 4 === 14 ? 3 : t >> 3 === 30 ? 4 : t >> 6 === 2 ? -1 : -2;
  }
  function R_(t, e, r) {
    var n = e.length - 1;
    if (n < r) return 0;
    var i = qs(e[n]);
    return i >= 0 ? (i > 0 && (t.lastNeed = i - 1), i) : --n < r || i === -2 ? 0 : (i = qs(e[n]), i >= 0 ? (i > 0 && (t.lastNeed = i - 2), i) : --n < r || i === -2 ? 0 : (i = qs(e[n]), i >= 0 ? (i > 0 && (i === 2 ? i = 0 : t.lastNeed = i - 3), i) : 0));
  }
  function k_(t, e, r) {
    if ((e[0] & 192) !== 128) return t.lastNeed = 0, "�";
    if (t.lastNeed > 1 && e.length > 1) {
      if ((e[1] & 192) !== 128) return t.lastNeed = 1, "�";
      if (t.lastNeed > 2 && e.length > 2 && (e[2] & 192) !== 128) return t.lastNeed = 2, "�";
    }
  }
  function C_(t) {
    var e = this.lastTotal - this.lastNeed, r = k_(this, t);
    if (r !== void 0) return r;
    if (this.lastNeed <= t.length) return t.copy(this.lastChar, e, 0, this.lastNeed), this.lastChar.toString(this.encoding, 0, this.lastTotal);
    t.copy(this.lastChar, e, 0, t.length), this.lastNeed -= t.length;
  }
  function B_(t, e) {
    var r = R_(this, t, e);
    if (!this.lastNeed) return t.toString("utf8", e);
    this.lastTotal = r;
    var n = t.length - (r - this.lastNeed);
    return t.copy(this.lastChar, 0, n), t.toString("utf8", e, n);
  }
  function M_(t) {
    var e = t && t.length ? this.write(t) : "";
    return this.lastNeed ? e + "�" : e;
  }
  function N_(t, e) {
    if ((t.length - e) % 2 === 0) {
      var r = t.toString("utf16le", e);
      if (r) {
        var n = r.charCodeAt(r.length - 1);
        if (n >= 55296 && n <= 56319) return this.lastNeed = 2, this.lastTotal = 4, this.lastChar[0] = t[t.length - 2], this.lastChar[1] = t[t.length - 1], r.slice(0, -1);
      }
      return r;
    }
    return this.lastNeed = 1, this.lastTotal = 2, this.lastChar[0] = t[t.length - 1], t.toString("utf16le", e, t.length - 1);
  }
  function q_(t) {
    var e = t && t.length ? this.write(t) : "";
    if (this.lastNeed) {
      var r = this.lastTotal - this.lastNeed;
      return e + this.lastChar.toString("utf16le", 0, r);
    }
    return e;
  }
  function L_(t, e) {
    var r = (t.length - e) % 3;
    return r === 0 ? t.toString("base64", e) : (this.lastNeed = 3 - r, this.lastTotal = 3, r === 1 ? this.lastChar[0] = t[t.length - 1] : (this.lastChar[0] = t[t.length - 2], this.lastChar[1] = t[t.length - 1]), t.toString("base64", e, t.length - r));
  }
  function U_(t) {
    var e = t && t.length ? this.write(t) : "";
    return this.lastNeed ? e + this.lastChar.toString("base64", 0, 3 - this.lastNeed) : e;
  }
  function D_(t) {
    return t.toString(this.encoding);
  }
  function j_(t) {
    return t && t.length ? this.write(t) : "";
  }
});
var Us = D((Fk, Jh) => {
  A();
  x();
  I();
  var Kh = tr(), { PromisePrototypeThen: F_, SymbolAsyncIterator: Qh, SymbolIterator: Yh } = Ee(), { Buffer: W_ } = (ke(), me(qe)), { ERR_INVALID_ARG_TYPE: $_, ERR_STREAM_NULL_VALUES: H_ } = De().codes;
  function V_(t, e, r) {
    let n;
    if (typeof e == "string" || e instanceof W_) return new t({ objectMode: true, ...r, read() {
      this.push(e), this.push(null);
    } });
    let i;
    if (e && e[Qh]) i = true, n = e[Qh]();
    else if (e && e[Yh]) i = false, n = e[Yh]();
    else throw new $_("iterable", ["Iterable"], e);
    let s = new t({ objectMode: true, highWaterMark: 1, ...r }), o = false;
    s._read = function() {
      o || (o = true, c());
    }, s._destroy = function(f, h2) {
      F_(a(f), () => Kh.nextTick(h2, f), (d) => Kh.nextTick(h2, d || f));
    };
    async function a(f) {
      let h2 = f != null, d = typeof n.throw == "function";
      if (h2 && d) {
        let { value: w, done: y } = await n.throw(f);
        if (await w, y) return;
      }
      if (typeof n.return == "function") {
        let { value: w } = await n.return();
        await w;
      }
    }
    async function c() {
      for (; ; ) {
        try {
          let { value: f, done: h2 } = i ? await n.next() : n.next();
          if (h2) s.push(null);
          else {
            let d = f && typeof f.then == "function" ? await f : f;
            if (d === null) throw o = false, new H_();
            if (s.push(d)) continue;
            o = false;
          }
        } catch (f) {
          s.destroy(f);
        }
        break;
      }
    }
    return s;
  }
  Jh.exports = V_;
});
var Nn = D((Jk, yd) => {
  A();
  x();
  I();
  var lt = tr(), { ArrayPrototypeIndexOf: G_, NumberIsInteger: z_, NumberIsNaN: K_, NumberParseInt: Q_, ObjectDefineProperties: Gs, ObjectKeys: Y_, ObjectSetPrototypeOf: ed, Promise: td, SafeSet: J_, SymbolAsyncDispose: X_, SymbolAsyncIterator: Z_, Symbol: ev } = Ee();
  yd.exports = le;
  le.ReadableState = Yi;
  var { EventEmitter: tv } = (er(), me(Zt)), { Stream: nr, prependListener: rv } = $i(), { Buffer: Ds } = (ke(), me(qe)), { addAbortSignal: nv } = kn(), rd = Ut(), he = $e().debuglog("stream", (t) => {
    he = t;
  }), iv = Lh(), nn = Ar(), { getHighWaterMark: ov, getDefaultHighWaterMark: sv } = Cn(), { aggregateTwoErrors: Xh, codes: { ERR_INVALID_ARG_TYPE: av, ERR_METHOD_NOT_IMPLEMENTED: uv, ERR_OUT_OF_RANGE: lv, ERR_STREAM_PUSH_AFTER_EOF: cv, ERR_STREAM_UNSHIFT_AFTER_END_EVENT: fv }, AbortError: hv } = De(), { validateObject: dv } = Xr(), xr = ev("kPaused"), { StringDecoder: nd } = zh(), pv = Us();
  ed(le.prototype, nr.prototype);
  ed(le, nr);
  var js = () => {
  }, { errorOrDestroy: tn } = nn, rn = 1, yv = 2, id = 4, Mn = 8, od = 16, zi = 32, Ki = 64, sd = 128, gv = 256, bv = 512, wv = 1024, Hs = 2048, Vs = 4096, mv = 8192, _v = 16384, vv = 32768, ad = 65536, Sv = 1 << 17, Ev = 1 << 18;
  function Ce(t) {
    return { enumerable: false, get() {
      return (this.state & t) !== 0;
    }, set(e) {
      e ? this.state |= t : this.state &= ~t;
    } };
  }
  Gs(Yi.prototype, { objectMode: Ce(rn), ended: Ce(yv), endEmitted: Ce(id), reading: Ce(Mn), constructed: Ce(od), sync: Ce(zi), needReadable: Ce(Ki), emittedReadable: Ce(sd), readableListening: Ce(gv), resumeScheduled: Ce(bv), errorEmitted: Ce(wv), emitClose: Ce(Hs), autoDestroy: Ce(Vs), destroyed: Ce(mv), closed: Ce(_v), closeEmitted: Ce(vv), multiAwaitDrain: Ce(ad), readingMore: Ce(Sv), dataEmitted: Ce(Ev) });
  function Yi(t, e, r) {
    typeof r != "boolean" && (r = e instanceof St()), this.state = Hs | Vs | od | zi, t && t.objectMode && (this.state |= rn), r && t && t.readableObjectMode && (this.state |= rn), this.highWaterMark = t ? ov(this, t, "readableHighWaterMark", r) : sv(false), this.buffer = new iv(), this.length = 0, this.pipes = [], this.flowing = null, this[xr] = null, t && t.emitClose === false && (this.state &= ~Hs), t && t.autoDestroy === false && (this.state &= ~Vs), this.errored = null, this.defaultEncoding = t && t.defaultEncoding || "utf8", this.awaitDrainWriters = null, this.decoder = null, this.encoding = null, t && t.encoding && (this.decoder = new nd(t.encoding), this.encoding = t.encoding);
  }
  function le(t) {
    if (!(this instanceof le)) return new le(t);
    let e = this instanceof St();
    this._readableState = new Yi(t, this, e), t && (typeof t.read == "function" && (this._read = t.read), typeof t.destroy == "function" && (this._destroy = t.destroy), typeof t.construct == "function" && (this._construct = t.construct), t.signal && !e && nv(t.signal, this)), nr.call(this, t), nn.construct(this, () => {
      this._readableState.needReadable && Qi(this, this._readableState);
    });
  }
  le.prototype.destroy = nn.destroy;
  le.prototype._undestroy = nn.undestroy;
  le.prototype._destroy = function(t, e) {
    e(t);
  };
  le.prototype[tv.captureRejectionSymbol] = function(t) {
    this.destroy(t);
  };
  le.prototype[X_] = function() {
    let t;
    return this.destroyed || (t = this.readableEnded ? null : new hv(), this.destroy(t)), new td((e, r) => rd(this, (n) => n && n !== t ? r(n) : e(null)));
  };
  le.prototype.push = function(t, e) {
    return ud(this, t, e, false);
  };
  le.prototype.unshift = function(t, e) {
    return ud(this, t, e, true);
  };
  function ud(t, e, r, n) {
    he("readableAddChunk", e);
    let i = t._readableState, s;
    if ((i.state & rn) === 0 && (typeof e == "string" ? (r = r || i.defaultEncoding, i.encoding !== r && (n && i.encoding ? e = Ds.from(e, r).toString(i.encoding) : (e = Ds.from(e, r), r = ""))) : e instanceof Ds ? r = "" : nr._isUint8Array(e) ? (e = nr._uint8ArrayToBuffer(e), r = "") : e != null && (s = new av("chunk", ["string", "Buffer", "Uint8Array"], e))), s) tn(t, s);
    else if (e === null) i.state &= ~Mn, xv(t, i);
    else if ((i.state & rn) !== 0 || e && e.length > 0) if (n) if ((i.state & id) !== 0) tn(t, new fv());
    else {
      if (i.destroyed || i.errored) return false;
      Fs(t, i, e, true);
    }
    else if (i.ended) tn(t, new cv());
    else {
      if (i.destroyed || i.errored) return false;
      i.state &= ~Mn, i.decoder && !r ? (e = i.decoder.write(e), i.objectMode || e.length !== 0 ? Fs(t, i, e, false) : Qi(t, i)) : Fs(t, i, e, false);
    }
    else n || (i.state &= ~Mn, Qi(t, i));
    return !i.ended && (i.length < i.highWaterMark || i.length === 0);
  }
  function Fs(t, e, r, n) {
    e.flowing && e.length === 0 && !e.sync && t.listenerCount("data") > 0 ? ((e.state & ad) !== 0 ? e.awaitDrainWriters.clear() : e.awaitDrainWriters = null, e.dataEmitted = true, t.emit("data", r)) : (e.length += e.objectMode ? 1 : r.length, n ? e.buffer.unshift(r) : e.buffer.push(r), (e.state & Ki) !== 0 && Ji(t)), Qi(t, e);
  }
  le.prototype.isPaused = function() {
    let t = this._readableState;
    return t[xr] === true || t.flowing === false;
  };
  le.prototype.setEncoding = function(t) {
    let e = new nd(t);
    this._readableState.decoder = e, this._readableState.encoding = this._readableState.decoder.encoding;
    let r = this._readableState.buffer, n = "";
    for (let i of r) n += e.write(i);
    return r.clear(), n !== "" && r.push(n), this._readableState.length = n.length, this;
  };
  var Av = 1073741824;
  function Iv(t) {
    if (t > Av) throw new lv("size", "<= 1GiB", t);
    return t--, t |= t >>> 1, t |= t >>> 2, t |= t >>> 4, t |= t >>> 8, t |= t >>> 16, t++, t;
  }
  function Zh(t, e) {
    return t <= 0 || e.length === 0 && e.ended ? 0 : (e.state & rn) !== 0 ? 1 : K_(t) ? e.flowing && e.length ? e.buffer.first().length : e.length : t <= e.length ? t : e.ended ? e.length : 0;
  }
  le.prototype.read = function(t) {
    he("read", t), t === void 0 ? t = NaN : z_(t) || (t = Q_(t, 10));
    let e = this._readableState, r = t;
    if (t > e.highWaterMark && (e.highWaterMark = Iv(t)), t !== 0 && (e.state &= ~sd), t === 0 && e.needReadable && ((e.highWaterMark !== 0 ? e.length >= e.highWaterMark : e.length > 0) || e.ended)) return he("read: emitReadable", e.length, e.ended), e.length === 0 && e.ended ? Ws(this) : Ji(this), null;
    if (t = Zh(t, e), t === 0 && e.ended) return e.length === 0 && Ws(this), null;
    let n = (e.state & Ki) !== 0;
    if (he("need readable", n), (e.length === 0 || e.length - t < e.highWaterMark) && (n = true, he("length less than watermark", n)), e.ended || e.reading || e.destroyed || e.errored || !e.constructed) n = false, he("reading, ended or constructing", n);
    else if (n) {
      he("do read"), e.state |= Mn | zi, e.length === 0 && (e.state |= Ki);
      try {
        this._read(e.highWaterMark);
      } catch (s) {
        tn(this, s);
      }
      e.state &= ~zi, e.reading || (t = Zh(r, e));
    }
    let i;
    return t > 0 ? i = dd(t, e) : i = null, i === null ? (e.needReadable = e.length <= e.highWaterMark, t = 0) : (e.length -= t, e.multiAwaitDrain ? e.awaitDrainWriters.clear() : e.awaitDrainWriters = null), e.length === 0 && (e.ended || (e.needReadable = true), r !== t && e.ended && Ws(this)), i !== null && !e.errorEmitted && !e.closeEmitted && (e.dataEmitted = true, this.emit("data", i)), i;
  };
  function xv(t, e) {
    if (he("onEofChunk"), !e.ended) {
      if (e.decoder) {
        let r = e.decoder.end();
        r && r.length && (e.buffer.push(r), e.length += e.objectMode ? 1 : r.length);
      }
      e.ended = true, e.sync ? Ji(t) : (e.needReadable = false, e.emittedReadable = true, ld(t));
    }
  }
  function Ji(t) {
    let e = t._readableState;
    he("emitReadable", e.needReadable, e.emittedReadable), e.needReadable = false, e.emittedReadable || (he("emitReadable", e.flowing), e.emittedReadable = true, lt.nextTick(ld, t));
  }
  function ld(t) {
    let e = t._readableState;
    he("emitReadable_", e.destroyed, e.length, e.ended), !e.destroyed && !e.errored && (e.length || e.ended) && (t.emit("readable"), e.emittedReadable = false), e.needReadable = !e.flowing && !e.ended && e.length <= e.highWaterMark, fd(t);
  }
  function Qi(t, e) {
    !e.readingMore && e.constructed && (e.readingMore = true, lt.nextTick(Tv, t, e));
  }
  function Tv(t, e) {
    for (; !e.reading && !e.ended && (e.length < e.highWaterMark || e.flowing && e.length === 0); ) {
      let r = e.length;
      if (he("maybeReadMore read 0"), t.read(0), r === e.length) break;
    }
    e.readingMore = false;
  }
  le.prototype._read = function(t) {
    throw new uv("_read()");
  };
  le.prototype.pipe = function(t, e) {
    let r = this, n = this._readableState;
    n.pipes.length === 1 && (n.multiAwaitDrain || (n.multiAwaitDrain = true, n.awaitDrainWriters = new J_(n.awaitDrainWriters ? [n.awaitDrainWriters] : []))), n.pipes.push(t), he("pipe count=%d opts=%j", n.pipes.length, e);
    let s = (!e || e.end !== false) && t !== lt.stdout && t !== lt.stderr ? a : b;
    n.endEmitted ? lt.nextTick(s) : r.once("end", s), t.on("unpipe", o);
    function o(m, v) {
      he("onunpipe"), m === r && v && v.hasUnpiped === false && (v.hasUnpiped = true, h2());
    }
    function a() {
      he("onend"), t.end();
    }
    let c, f = false;
    function h2() {
      he("cleanup"), t.removeListener("close", P), t.removeListener("finish", E), c && t.removeListener("drain", c), t.removeListener("error", y), t.removeListener("unpipe", o), r.removeListener("end", a), r.removeListener("end", b), r.removeListener("data", w), f = true, c && n.awaitDrainWriters && (!t._writableState || t._writableState.needDrain) && c();
    }
    function d() {
      f || (n.pipes.length === 1 && n.pipes[0] === t ? (he("false write response, pause", 0), n.awaitDrainWriters = t, n.multiAwaitDrain = false) : n.pipes.length > 1 && n.pipes.includes(t) && (he("false write response, pause", n.awaitDrainWriters.size), n.awaitDrainWriters.add(t)), r.pause()), c || (c = Pv(r, t), t.on("drain", c));
    }
    r.on("data", w);
    function w(m) {
      he("ondata");
      let v = t.write(m);
      he("dest.write", v), v === false && d();
    }
    function y(m) {
      if (he("onerror", m), b(), t.removeListener("error", y), t.listenerCount("error") === 0) {
        let v = t._writableState || t._readableState;
        v && !v.errorEmitted ? tn(t, m) : t.emit("error", m);
      }
    }
    rv(t, "error", y);
    function P() {
      t.removeListener("finish", E), b();
    }
    t.once("close", P);
    function E() {
      he("onfinish"), t.removeListener("close", P), b();
    }
    t.once("finish", E);
    function b() {
      he("unpipe"), r.unpipe(t);
    }
    return t.emit("pipe", r), t.writableNeedDrain === true ? d() : n.flowing || (he("pipe resume"), r.resume()), t;
  };
  function Pv(t, e) {
    return function() {
      let n = t._readableState;
      n.awaitDrainWriters === e ? (he("pipeOnDrain", 1), n.awaitDrainWriters = null) : n.multiAwaitDrain && (he("pipeOnDrain", n.awaitDrainWriters.size), n.awaitDrainWriters.delete(e)), (!n.awaitDrainWriters || n.awaitDrainWriters.size === 0) && t.listenerCount("data") && t.resume();
    };
  }
  le.prototype.unpipe = function(t) {
    let e = this._readableState, r = { hasUnpiped: false };
    if (e.pipes.length === 0) return this;
    if (!t) {
      let i = e.pipes;
      e.pipes = [], this.pause();
      for (let s = 0; s < i.length; s++) i[s].emit("unpipe", this, { hasUnpiped: false });
      return this;
    }
    let n = G_(e.pipes, t);
    return n === -1 ? this : (e.pipes.splice(n, 1), e.pipes.length === 0 && this.pause(), t.emit("unpipe", this, r), this);
  };
  le.prototype.on = function(t, e) {
    let r = nr.prototype.on.call(this, t, e), n = this._readableState;
    return t === "data" ? (n.readableListening = this.listenerCount("readable") > 0, n.flowing !== false && this.resume()) : t === "readable" && !n.endEmitted && !n.readableListening && (n.readableListening = n.needReadable = true, n.flowing = false, n.emittedReadable = false, he("on readable", n.length, n.reading), n.length ? Ji(this) : n.reading || lt.nextTick(Ov, this)), r;
  };
  le.prototype.addListener = le.prototype.on;
  le.prototype.removeListener = function(t, e) {
    let r = nr.prototype.removeListener.call(this, t, e);
    return t === "readable" && lt.nextTick(cd, this), r;
  };
  le.prototype.off = le.prototype.removeListener;
  le.prototype.removeAllListeners = function(t) {
    let e = nr.prototype.removeAllListeners.apply(this, arguments);
    return (t === "readable" || t === void 0) && lt.nextTick(cd, this), e;
  };
  function cd(t) {
    let e = t._readableState;
    e.readableListening = t.listenerCount("readable") > 0, e.resumeScheduled && e[xr] === false ? e.flowing = true : t.listenerCount("data") > 0 ? t.resume() : e.readableListening || (e.flowing = null);
  }
  function Ov(t) {
    he("readable nexttick read 0"), t.read(0);
  }
  le.prototype.resume = function() {
    let t = this._readableState;
    return t.flowing || (he("resume"), t.flowing = !t.readableListening, Rv(this, t)), t[xr] = false, this;
  };
  function Rv(t, e) {
    e.resumeScheduled || (e.resumeScheduled = true, lt.nextTick(kv, t, e));
  }
  function kv(t, e) {
    he("resume", e.reading), e.reading || t.read(0), e.resumeScheduled = false, t.emit("resume"), fd(t), e.flowing && !e.reading && t.read(0);
  }
  le.prototype.pause = function() {
    return he("call pause flowing=%j", this._readableState.flowing), this._readableState.flowing !== false && (he("pause"), this._readableState.flowing = false, this.emit("pause")), this._readableState[xr] = true, this;
  };
  function fd(t) {
    let e = t._readableState;
    for (he("flow", e.flowing); e.flowing && t.read() !== null; ) ;
  }
  le.prototype.wrap = function(t) {
    let e = false;
    t.on("data", (n) => {
      !this.push(n) && t.pause && (e = true, t.pause());
    }), t.on("end", () => {
      this.push(null);
    }), t.on("error", (n) => {
      tn(this, n);
    }), t.on("close", () => {
      this.destroy();
    }), t.on("destroy", () => {
      this.destroy();
    }), this._read = () => {
      e && t.resume && (e = false, t.resume());
    };
    let r = Y_(t);
    for (let n = 1; n < r.length; n++) {
      let i = r[n];
      this[i] === void 0 && typeof t[i] == "function" && (this[i] = t[i].bind(t));
    }
    return this;
  };
  le.prototype[Z_] = function() {
    return hd(this);
  };
  le.prototype.iterator = function(t) {
    return t !== void 0 && dv(t, "options"), hd(this, t);
  };
  function hd(t, e) {
    typeof t.read != "function" && (t = le.wrap(t, { objectMode: true }));
    let r = Cv(t, e);
    return r.stream = t, r;
  }
  async function* Cv(t, e) {
    let r = js;
    function n(o) {
      this === t ? (r(), r = js) : r = o;
    }
    t.on("readable", n);
    let i, s = rd(t, { writable: false }, (o) => {
      i = o ? Xh(i, o) : null, r(), r = js;
    });
    try {
      for (; ; ) {
        let o = t.destroyed ? null : t.read();
        if (o !== null) yield o;
        else {
          if (i) throw i;
          if (i === null) return;
          await new td(n);
        }
      }
    } catch (o) {
      throw i = Xh(i, o), i;
    } finally {
      (i || e?.destroyOnReturn !== false) && (i === void 0 || t._readableState.autoDestroy) ? nn.destroyer(t, null) : (t.off("readable", n), s());
    }
  }
  Gs(le.prototype, { readable: { __proto__: null, get() {
    let t = this._readableState;
    return !!t && t.readable !== false && !t.destroyed && !t.errorEmitted && !t.endEmitted;
  }, set(t) {
    this._readableState && (this._readableState.readable = !!t);
  } }, readableDidRead: { __proto__: null, enumerable: false, get: function() {
    return this._readableState.dataEmitted;
  } }, readableAborted: { __proto__: null, enumerable: false, get: function() {
    return !!(this._readableState.readable !== false && (this._readableState.destroyed || this._readableState.errored) && !this._readableState.endEmitted);
  } }, readableHighWaterMark: { __proto__: null, enumerable: false, get: function() {
    return this._readableState.highWaterMark;
  } }, readableBuffer: { __proto__: null, enumerable: false, get: function() {
    return this._readableState && this._readableState.buffer;
  } }, readableFlowing: { __proto__: null, enumerable: false, get: function() {
    return this._readableState.flowing;
  }, set: function(t) {
    this._readableState && (this._readableState.flowing = t);
  } }, readableLength: { __proto__: null, enumerable: false, get() {
    return this._readableState.length;
  } }, readableObjectMode: { __proto__: null, enumerable: false, get() {
    return this._readableState ? this._readableState.objectMode : false;
  } }, readableEncoding: { __proto__: null, enumerable: false, get() {
    return this._readableState ? this._readableState.encoding : null;
  } }, errored: { __proto__: null, enumerable: false, get() {
    return this._readableState ? this._readableState.errored : null;
  } }, closed: { __proto__: null, get() {
    return this._readableState ? this._readableState.closed : false;
  } }, destroyed: { __proto__: null, enumerable: false, get() {
    return this._readableState ? this._readableState.destroyed : false;
  }, set(t) {
    this._readableState && (this._readableState.destroyed = t);
  } }, readableEnded: { __proto__: null, enumerable: false, get() {
    return this._readableState ? this._readableState.endEmitted : false;
  } } });
  Gs(Yi.prototype, { pipesCount: { __proto__: null, get() {
    return this.pipes.length;
  } }, paused: { __proto__: null, get() {
    return this[xr] !== false;
  }, set(t) {
    this[xr] = !!t;
  } } });
  le._fromList = dd;
  function dd(t, e) {
    if (e.length === 0) return null;
    let r;
    return e.objectMode ? r = e.buffer.shift() : !t || t >= e.length ? (e.decoder ? r = e.buffer.join("") : e.buffer.length === 1 ? r = e.buffer.first() : r = e.buffer.concat(e.length), e.buffer.clear()) : r = e.buffer.consume(t, e.decoder), r;
  }
  function Ws(t) {
    let e = t._readableState;
    he("endReadable", e.endEmitted), e.endEmitted || (e.ended = true, lt.nextTick(Bv, e, t));
  }
  function Bv(t, e) {
    if (he("endReadableNT", t.endEmitted, t.length), !t.errored && !t.closeEmitted && !t.endEmitted && t.length === 0) {
      if (t.endEmitted = true, e.emit("end"), e.writable && e.allowHalfOpen === false) lt.nextTick(Mv, e);
      else if (t.autoDestroy) {
        let r = e._writableState;
        (!r || r.autoDestroy && (r.finished || r.writable === false)) && e.destroy();
      }
    }
  }
  function Mv(t) {
    t.writable && !t.writableEnded && !t.destroyed && t.end();
  }
  le.from = function(t, e) {
    return pv(le, t, e);
  };
  var $s;
  function pd() {
    return $s === void 0 && ($s = {}), $s;
  }
  le.fromWeb = function(t, e) {
    return pd().newStreamReadableFromReadableStream(t, e);
  };
  le.toWeb = function(t, e) {
    return pd().newReadableStreamFromStreamReadable(t, e);
  };
  le.wrap = function(t, e) {
    var r, n;
    return new le({ objectMode: (r = (n = t.readableObjectMode) !== null && n !== void 0 ? n : t.objectMode) !== null && r !== void 0 ? r : true, ...e, destroy(i, s) {
      nn.destroyer(t, i), s(i);
    } }).wrap(t);
  };
});
var ro = D((aC, Td) => {
  A();
  x();
  I();
  var Tr = tr(), { ArrayPrototypeSlice: wd, Error: Nv, FunctionPrototypeSymbolHasInstance: md, ObjectDefineProperty: _d, ObjectDefineProperties: qv, ObjectSetPrototypeOf: vd, StringPrototypeToLowerCase: Lv, Symbol: Uv, SymbolHasInstance: Dv } = Ee();
  Td.exports = Ie;
  Ie.WritableState = Un;
  var { EventEmitter: jv } = (er(), me(Zt)), qn = $i().Stream, { Buffer: Xi } = (ke(), me(qe)), to = Ar(), { addAbortSignal: Fv } = kn(), { getHighWaterMark: Wv, getDefaultHighWaterMark: $v } = Cn(), { ERR_INVALID_ARG_TYPE: Hv, ERR_METHOD_NOT_IMPLEMENTED: Vv, ERR_MULTIPLE_CALLBACK: Sd, ERR_STREAM_CANNOT_PIPE: Gv, ERR_STREAM_DESTROYED: Ln, ERR_STREAM_ALREADY_FINISHED: zv, ERR_STREAM_NULL_VALUES: Kv, ERR_STREAM_WRITE_AFTER_END: Qv, ERR_UNKNOWN_ENCODING: Ed } = De().codes, { errorOrDestroy: on } = to;
  vd(Ie.prototype, qn.prototype);
  vd(Ie, qn);
  function Qs() {
  }
  var sn = Uv("kOnFinished");
  function Un(t, e, r) {
    typeof r != "boolean" && (r = e instanceof St()), this.objectMode = !!(t && t.objectMode), r && (this.objectMode = this.objectMode || !!(t && t.writableObjectMode)), this.highWaterMark = t ? Wv(this, t, "writableHighWaterMark", r) : $v(false), this.finalCalled = false, this.needDrain = false, this.ending = false, this.ended = false, this.finished = false, this.destroyed = false;
    let n = !!(t && t.decodeStrings === false);
    this.decodeStrings = !n, this.defaultEncoding = t && t.defaultEncoding || "utf8", this.length = 0, this.writing = false, this.corked = 0, this.sync = true, this.bufferProcessing = false, this.onwrite = Jv.bind(void 0, e), this.writecb = null, this.writelen = 0, this.afterWriteTickInfo = null, eo(this), this.pendingcb = 0, this.constructed = true, this.prefinished = false, this.errorEmitted = false, this.emitClose = !t || t.emitClose !== false, this.autoDestroy = !t || t.autoDestroy !== false, this.errored = null, this.closed = false, this.closeEmitted = false, this[sn] = [];
  }
  function eo(t) {
    t.buffered = [], t.bufferedIndex = 0, t.allBuffers = true, t.allNoop = true;
  }
  Un.prototype.getBuffer = function() {
    return wd(this.buffered, this.bufferedIndex);
  };
  _d(Un.prototype, "bufferedRequestCount", { __proto__: null, get() {
    return this.buffered.length - this.bufferedIndex;
  } });
  function Ie(t) {
    let e = this instanceof St();
    if (!e && !md(Ie, this)) return new Ie(t);
    this._writableState = new Un(t, this, e), t && (typeof t.write == "function" && (this._write = t.write), typeof t.writev == "function" && (this._writev = t.writev), typeof t.destroy == "function" && (this._destroy = t.destroy), typeof t.final == "function" && (this._final = t.final), typeof t.construct == "function" && (this._construct = t.construct), t.signal && Fv(t.signal, this)), qn.call(this, t), to.construct(this, () => {
      let r = this._writableState;
      r.writing || Js(this, r), Xs(this, r);
    });
  }
  _d(Ie, Dv, { __proto__: null, value: function(t) {
    return md(this, t) ? true : this !== Ie ? false : t && t._writableState instanceof Un;
  } });
  Ie.prototype.pipe = function() {
    on(this, new Gv());
  };
  function Ad(t, e, r, n) {
    let i = t._writableState;
    if (typeof r == "function") n = r, r = i.defaultEncoding;
    else {
      if (!r) r = i.defaultEncoding;
      else if (r !== "buffer" && !Xi.isEncoding(r)) throw new Ed(r);
      typeof n != "function" && (n = Qs);
    }
    if (e === null) throw new Kv();
    if (!i.objectMode) if (typeof e == "string") i.decodeStrings !== false && (e = Xi.from(e, r), r = "buffer");
    else if (e instanceof Xi) r = "buffer";
    else if (qn._isUint8Array(e)) e = qn._uint8ArrayToBuffer(e), r = "buffer";
    else throw new Hv("chunk", ["string", "Buffer", "Uint8Array"], e);
    let s;
    return i.ending ? s = new Qv() : i.destroyed && (s = new Ln("write")), s ? (Tr.nextTick(n, s), on(t, s, true), s) : (i.pendingcb++, Yv(t, i, e, r, n));
  }
  Ie.prototype.write = function(t, e, r) {
    return Ad(this, t, e, r) === true;
  };
  Ie.prototype.cork = function() {
    this._writableState.corked++;
  };
  Ie.prototype.uncork = function() {
    let t = this._writableState;
    t.corked && (t.corked--, t.writing || Js(this, t));
  };
  Ie.prototype.setDefaultEncoding = function(e) {
    if (typeof e == "string" && (e = Lv(e)), !Xi.isEncoding(e)) throw new Ed(e);
    return this._writableState.defaultEncoding = e, this;
  };
  function Yv(t, e, r, n, i) {
    let s = e.objectMode ? 1 : r.length;
    e.length += s;
    let o = e.length < e.highWaterMark;
    return o || (e.needDrain = true), e.writing || e.corked || e.errored || !e.constructed ? (e.buffered.push({ chunk: r, encoding: n, callback: i }), e.allBuffers && n !== "buffer" && (e.allBuffers = false), e.allNoop && i !== Qs && (e.allNoop = false)) : (e.writelen = s, e.writecb = i, e.writing = true, e.sync = true, t._write(r, n, e.onwrite), e.sync = false), o && !e.errored && !e.destroyed;
  }
  function gd(t, e, r, n, i, s, o) {
    e.writelen = n, e.writecb = o, e.writing = true, e.sync = true, e.destroyed ? e.onwrite(new Ln("write")) : r ? t._writev(i, e.onwrite) : t._write(i, s, e.onwrite), e.sync = false;
  }
  function bd(t, e, r, n) {
    --e.pendingcb, n(r), Ys(e), on(t, r);
  }
  function Jv(t, e) {
    let r = t._writableState, n = r.sync, i = r.writecb;
    if (typeof i != "function") {
      on(t, new Sd());
      return;
    }
    r.writing = false, r.writecb = null, r.length -= r.writelen, r.writelen = 0, e ? (e.stack, r.errored || (r.errored = e), t._readableState && !t._readableState.errored && (t._readableState.errored = e), n ? Tr.nextTick(bd, t, r, e, i) : bd(t, r, e, i)) : (r.buffered.length > r.bufferedIndex && Js(t, r), n ? r.afterWriteTickInfo !== null && r.afterWriteTickInfo.cb === i ? r.afterWriteTickInfo.count++ : (r.afterWriteTickInfo = { count: 1, cb: i, stream: t, state: r }, Tr.nextTick(Xv, r.afterWriteTickInfo)) : Id(t, r, 1, i));
  }
  function Xv({ stream: t, state: e, count: r, cb: n }) {
    return e.afterWriteTickInfo = null, Id(t, e, r, n);
  }
  function Id(t, e, r, n) {
    for (!e.ending && !t.destroyed && e.length === 0 && e.needDrain && (e.needDrain = false, t.emit("drain")); r-- > 0; ) e.pendingcb--, n();
    e.destroyed && Ys(e), Xs(t, e);
  }
  function Ys(t) {
    if (t.writing) return;
    for (let i = t.bufferedIndex; i < t.buffered.length; ++i) {
      var e;
      let { chunk: s, callback: o } = t.buffered[i], a = t.objectMode ? 1 : s.length;
      t.length -= a, o((e = t.errored) !== null && e !== void 0 ? e : new Ln("write"));
    }
    let r = t[sn].splice(0);
    for (let i = 0; i < r.length; i++) {
      var n;
      r[i]((n = t.errored) !== null && n !== void 0 ? n : new Ln("end"));
    }
    eo(t);
  }
  function Js(t, e) {
    if (e.corked || e.bufferProcessing || e.destroyed || !e.constructed) return;
    let { buffered: r, bufferedIndex: n, objectMode: i } = e, s = r.length - n;
    if (!s) return;
    let o = n;
    if (e.bufferProcessing = true, s > 1 && t._writev) {
      e.pendingcb -= s - 1;
      let a = e.allNoop ? Qs : (f) => {
        for (let h2 = o; h2 < r.length; ++h2) r[h2].callback(f);
      }, c = e.allNoop && o === 0 ? r : wd(r, o);
      c.allBuffers = e.allBuffers, gd(t, e, true, e.length, c, "", a), eo(e);
    } else {
      do {
        let { chunk: a, encoding: c, callback: f } = r[o];
        r[o++] = null;
        let h2 = i ? 1 : a.length;
        gd(t, e, false, h2, a, c, f);
      } while (o < r.length && !e.writing);
      o === r.length ? eo(e) : o > 256 ? (r.splice(0, o), e.bufferedIndex = 0) : e.bufferedIndex = o;
    }
    e.bufferProcessing = false;
  }
  Ie.prototype._write = function(t, e, r) {
    if (this._writev) this._writev([{ chunk: t, encoding: e }], r);
    else throw new Vv("_write()");
  };
  Ie.prototype._writev = null;
  Ie.prototype.end = function(t, e, r) {
    let n = this._writableState;
    typeof t == "function" ? (r = t, t = null, e = null) : typeof e == "function" && (r = e, e = null);
    let i;
    if (t != null) {
      let s = Ad(this, t, e);
      s instanceof Nv && (i = s);
    }
    return n.corked && (n.corked = 1, this.uncork()), i || (!n.errored && !n.ending ? (n.ending = true, Xs(this, n, true), n.ended = true) : n.finished ? i = new zv("end") : n.destroyed && (i = new Ln("end"))), typeof r == "function" && (i || n.finished ? Tr.nextTick(r, i) : n[sn].push(r)), this;
  };
  function Zi(t) {
    return t.ending && !t.destroyed && t.constructed && t.length === 0 && !t.errored && t.buffered.length === 0 && !t.finished && !t.writing && !t.errorEmitted && !t.closeEmitted;
  }
  function Zv(t, e) {
    let r = false;
    function n(i) {
      if (r) {
        on(t, i ?? Sd());
        return;
      }
      if (r = true, e.pendingcb--, i) {
        let s = e[sn].splice(0);
        for (let o = 0; o < s.length; o++) s[o](i);
        on(t, i, e.sync);
      } else Zi(e) && (e.prefinished = true, t.emit("prefinish"), e.pendingcb++, Tr.nextTick(Ks, t, e));
    }
    e.sync = true, e.pendingcb++;
    try {
      t._final(n);
    } catch (i) {
      n(i);
    }
    e.sync = false;
  }
  function e0(t, e) {
    !e.prefinished && !e.finalCalled && (typeof t._final == "function" && !e.destroyed ? (e.finalCalled = true, Zv(t, e)) : (e.prefinished = true, t.emit("prefinish")));
  }
  function Xs(t, e, r) {
    Zi(e) && (e0(t, e), e.pendingcb === 0 && (r ? (e.pendingcb++, Tr.nextTick((n, i) => {
      Zi(i) ? Ks(n, i) : i.pendingcb--;
    }, t, e)) : Zi(e) && (e.pendingcb++, Ks(t, e))));
  }
  function Ks(t, e) {
    e.pendingcb--, e.finished = true;
    let r = e[sn].splice(0);
    for (let n = 0; n < r.length; n++) r[n]();
    if (t.emit("finish"), e.autoDestroy) {
      let n = t._readableState;
      (!n || n.autoDestroy && (n.endEmitted || n.readable === false)) && t.destroy();
    }
  }
  qv(Ie.prototype, { closed: { __proto__: null, get() {
    return this._writableState ? this._writableState.closed : false;
  } }, destroyed: { __proto__: null, get() {
    return this._writableState ? this._writableState.destroyed : false;
  }, set(t) {
    this._writableState && (this._writableState.destroyed = t);
  } }, writable: { __proto__: null, get() {
    let t = this._writableState;
    return !!t && t.writable !== false && !t.destroyed && !t.errored && !t.ending && !t.ended;
  }, set(t) {
    this._writableState && (this._writableState.writable = !!t);
  } }, writableFinished: { __proto__: null, get() {
    return this._writableState ? this._writableState.finished : false;
  } }, writableObjectMode: { __proto__: null, get() {
    return this._writableState ? this._writableState.objectMode : false;
  } }, writableBuffer: { __proto__: null, get() {
    return this._writableState && this._writableState.getBuffer();
  } }, writableEnded: { __proto__: null, get() {
    return this._writableState ? this._writableState.ending : false;
  } }, writableNeedDrain: { __proto__: null, get() {
    let t = this._writableState;
    return t ? !t.destroyed && !t.ending && t.needDrain : false;
  } }, writableHighWaterMark: { __proto__: null, get() {
    return this._writableState && this._writableState.highWaterMark;
  } }, writableCorked: { __proto__: null, get() {
    return this._writableState ? this._writableState.corked : 0;
  } }, writableLength: { __proto__: null, get() {
    return this._writableState && this._writableState.length;
  } }, errored: { __proto__: null, enumerable: false, get() {
    return this._writableState ? this._writableState.errored : null;
  } }, writableAborted: { __proto__: null, enumerable: false, get: function() {
    return !!(this._writableState.writable !== false && (this._writableState.destroyed || this._writableState.errored) && !this._writableState.finished);
  } } });
  var t0 = to.destroy;
  Ie.prototype.destroy = function(t, e) {
    let r = this._writableState;
    return !r.destroyed && (r.bufferedIndex < r.buffered.length || r[sn].length) && Tr.nextTick(Ys, r), t0.call(this, t, e), this;
  };
  Ie.prototype._undestroy = to.undestroy;
  Ie.prototype._destroy = function(t, e) {
    e(t);
  };
  Ie.prototype[jv.captureRejectionSymbol] = function(t) {
    this.destroy(t);
  };
  var zs;
  function xd() {
    return zs === void 0 && (zs = {}), zs;
  }
  Ie.fromWeb = function(t, e) {
    return xd().newStreamWritableFromWritableStream(t, e);
  };
  Ie.toWeb = function(t) {
    return xd().newWritableStreamFromStreamWritable(t);
  };
});
var Wd = D((bC, Fd) => {
  A();
  x();
  I();
  var Zs = tr(), r0 = (ke(), me(qe)), { isReadable: n0, isWritable: i0, isIterable: Pd, isNodeStream: o0, isReadableNodeStream: Od, isWritableNodeStream: Rd, isDuplexNodeStream: s0, isReadableStream: kd, isWritableStream: Cd } = mt(), Bd = Ut(), { AbortError: Dd, codes: { ERR_INVALID_ARG_TYPE: a0, ERR_INVALID_RETURN_VALUE: Md } } = De(), { destroyer: un } = Ar(), u0 = St(), jd = Nn(), l0 = ro(), { createDeferredPromise: Nd } = $e(), qd = Us(), Ld = globalThis.Blob || r0.Blob, c0 = typeof Ld < "u" ? function(e) {
    return e instanceof Ld;
  } : function(e) {
    return false;
  }, f0 = globalThis.AbortController || Yr().AbortController, { FunctionPrototypeCall: Ud } = Ee(), ir = class extends u0 {
    constructor(e) {
      super(e), e?.readable === false && (this._readableState.readable = false, this._readableState.ended = true, this._readableState.endEmitted = true), e?.writable === false && (this._writableState.writable = false, this._writableState.ending = true, this._writableState.ended = true, this._writableState.finished = true);
    }
  };
  Fd.exports = function t(e, r) {
    if (s0(e)) return e;
    if (Od(e)) return an({ readable: e });
    if (Rd(e)) return an({ writable: e });
    if (o0(e)) return an({ writable: false, readable: false });
    if (kd(e)) return an({ readable: jd.fromWeb(e) });
    if (Cd(e)) return an({ writable: l0.fromWeb(e) });
    if (typeof e == "function") {
      let { value: i, write: s, final: o, destroy: a } = h0(e);
      if (Pd(i)) return qd(ir, i, { objectMode: true, write: s, final: o, destroy: a });
      let c = i?.then;
      if (typeof c == "function") {
        let f, h2 = Ud(c, i, (d) => {
          if (d != null) throw new Md("nully", "body", d);
        }, (d) => {
          un(f, d);
        });
        return f = new ir({ objectMode: true, readable: false, write: s, final(d) {
          o(async () => {
            try {
              await h2, Zs.nextTick(d, null);
            } catch (w) {
              Zs.nextTick(d, w);
            }
          });
        }, destroy: a });
      }
      throw new Md("Iterable, AsyncIterable or AsyncFunction", r, i);
    }
    if (c0(e)) return t(e.arrayBuffer());
    if (Pd(e)) return qd(ir, e, { objectMode: true, writable: false });
    if (kd(e?.readable) && Cd(e?.writable)) return ir.fromWeb(e);
    if (typeof e?.writable == "object" || typeof e?.readable == "object") {
      let i = e != null && e.readable ? Od(e?.readable) ? e?.readable : t(e.readable) : void 0, s = e != null && e.writable ? Rd(e?.writable) ? e?.writable : t(e.writable) : void 0;
      return an({ readable: i, writable: s });
    }
    let n = e?.then;
    if (typeof n == "function") {
      let i;
      return Ud(n, e, (s) => {
        s != null && i.push(s), i.push(null);
      }, (s) => {
        un(i, s);
      }), i = new ir({ objectMode: true, writable: false, read() {
      } });
    }
    throw new a0(r, ["Blob", "ReadableStream", "WritableStream", "Stream", "Iterable", "AsyncIterable", "Function", "{ readable, writable } pair", "Promise"], e);
  };
  function h0(t) {
    let { promise: e, resolve: r } = Nd(), n = new f0(), i = n.signal;
    return { value: t((async function* () {
      for (; ; ) {
        let o = e;
        e = null;
        let { chunk: a, done: c, cb: f } = await o;
        if (Zs.nextTick(f), c) return;
        if (i.aborted) throw new Dd(void 0, { cause: i.reason });
        ({ promise: e, resolve: r } = Nd()), yield a;
      }
    })(), { signal: i }), write(o, a, c) {
      let f = r;
      r = null, f({ chunk: o, done: false, cb: c });
    }, final(o) {
      let a = r;
      r = null, a({ done: true, cb: o });
    }, destroy(o, a) {
      n.abort(), a(o);
    } };
  }
  function an(t) {
    let e = t.readable && typeof t.readable.read != "function" ? jd.wrap(t.readable) : t.readable, r = t.writable, n = !!n0(e), i = !!i0(r), s, o, a, c, f;
    function h2(d) {
      let w = c;
      c = null, w ? w(d) : d && f.destroy(d);
    }
    return f = new ir({ readableObjectMode: !!(e != null && e.readableObjectMode), writableObjectMode: !!(r != null && r.writableObjectMode), readable: n, writable: i }), i && (Bd(r, (d) => {
      i = false, d && un(e, d), h2(d);
    }), f._write = function(d, w, y) {
      r.write(d, w) ? y() : s = y;
    }, f._final = function(d) {
      r.end(), o = d;
    }, r.on("drain", function() {
      if (s) {
        let d = s;
        s = null, d();
      }
    }), r.on("finish", function() {
      if (o) {
        let d = o;
        o = null, d();
      }
    })), n && (Bd(e, (d) => {
      n = false, d && un(e, d), h2(d);
    }), e.on("readable", function() {
      if (a) {
        let d = a;
        a = null, d();
      }
    }), e.on("end", function() {
      f.push(null);
    }), f._read = function() {
      for (; ; ) {
        let d = e.read();
        if (d === null) {
          a = f._read;
          return;
        }
        if (!f.push(d)) return;
      }
    }), f._destroy = function(d, w) {
      !d && c !== null && (d = new Dd()), a = null, s = null, o = null, c === null ? w(d) : (c = w, un(r, d), un(e, d));
    }, f;
  }
});
var St = D((TC, Vd) => {
  A();
  x();
  I();
  var { ObjectDefineProperties: d0, ObjectGetOwnPropertyDescriptor: Dt, ObjectKeys: p0, ObjectSetPrototypeOf: $d } = Ee();
  Vd.exports = ct;
  var ra = Nn(), tt2 = ro();
  $d(ct.prototype, ra.prototype);
  $d(ct, ra);
  {
    let t = p0(tt2.prototype);
    for (let e = 0; e < t.length; e++) {
      let r = t[e];
      ct.prototype[r] || (ct.prototype[r] = tt2.prototype[r]);
    }
  }
  function ct(t) {
    if (!(this instanceof ct)) return new ct(t);
    ra.call(this, t), tt2.call(this, t), t ? (this.allowHalfOpen = t.allowHalfOpen !== false, t.readable === false && (this._readableState.readable = false, this._readableState.ended = true, this._readableState.endEmitted = true), t.writable === false && (this._writableState.writable = false, this._writableState.ending = true, this._writableState.ended = true, this._writableState.finished = true)) : this.allowHalfOpen = true;
  }
  d0(ct.prototype, { writable: { __proto__: null, ...Dt(tt2.prototype, "writable") }, writableHighWaterMark: { __proto__: null, ...Dt(tt2.prototype, "writableHighWaterMark") }, writableObjectMode: { __proto__: null, ...Dt(tt2.prototype, "writableObjectMode") }, writableBuffer: { __proto__: null, ...Dt(tt2.prototype, "writableBuffer") }, writableLength: { __proto__: null, ...Dt(tt2.prototype, "writableLength") }, writableFinished: { __proto__: null, ...Dt(tt2.prototype, "writableFinished") }, writableCorked: { __proto__: null, ...Dt(tt2.prototype, "writableCorked") }, writableEnded: { __proto__: null, ...Dt(tt2.prototype, "writableEnded") }, writableNeedDrain: { __proto__: null, ...Dt(tt2.prototype, "writableNeedDrain") }, destroyed: { __proto__: null, get() {
    return this._readableState === void 0 || this._writableState === void 0 ? false : this._readableState.destroyed && this._writableState.destroyed;
  }, set(t) {
    this._readableState && this._writableState && (this._readableState.destroyed = t, this._writableState.destroyed = t);
  } } });
  var ea;
  function Hd() {
    return ea === void 0 && (ea = {}), ea;
  }
  ct.fromWeb = function(t, e) {
    return Hd().newStreamDuplexFromReadableWritablePair(t, e);
  };
  ct.toWeb = function(t) {
    return Hd().newReadableWritablePairFromDuplex(t);
  };
  var ta;
  ct.from = function(t) {
    return ta || (ta = Wd()), ta(t, "body");
  };
});
var oa = D((LC, zd) => {
  A();
  x();
  I();
  var { ObjectSetPrototypeOf: Gd, Symbol: y0 } = Ee();
  zd.exports = jt;
  var { ERR_METHOD_NOT_IMPLEMENTED: g0 } = De().codes, ia = St(), { getHighWaterMark: b0 } = Cn();
  Gd(jt.prototype, ia.prototype);
  Gd(jt, ia);
  var Dn = y0("kCallback");
  function jt(t) {
    if (!(this instanceof jt)) return new jt(t);
    let e = t ? b0(this, t, "readableHighWaterMark", true) : null;
    e === 0 && (t = { ...t, highWaterMark: null, readableHighWaterMark: e, writableHighWaterMark: t.writableHighWaterMark || 0 }), ia.call(this, t), this._readableState.sync = false, this[Dn] = null, t && (typeof t.transform == "function" && (this._transform = t.transform), typeof t.flush == "function" && (this._flush = t.flush)), this.on("prefinish", w0);
  }
  function na(t) {
    typeof this._flush == "function" && !this.destroyed ? this._flush((e, r) => {
      if (e) {
        t ? t(e) : this.destroy(e);
        return;
      }
      r != null && this.push(r), this.push(null), t && t();
    }) : (this.push(null), t && t());
  }
  function w0() {
    this._final !== na && na.call(this);
  }
  jt.prototype._final = na;
  jt.prototype._transform = function(t, e, r) {
    throw new g0("_transform()");
  };
  jt.prototype._write = function(t, e, r) {
    let n = this._readableState, i = this._writableState, s = n.length;
    this._transform(t, e, (o, a) => {
      if (o) {
        r(o);
        return;
      }
      a != null && this.push(a), i.ended || s === n.length || n.length < n.highWaterMark ? r() : this[Dn] = r;
    });
  };
  jt.prototype._read = function() {
    if (this[Dn]) {
      let t = this[Dn];
      this[Dn] = null, t();
    }
  };
});
var aa = D((zC, Qd) => {
  A();
  x();
  I();
  var { ObjectSetPrototypeOf: Kd } = Ee();
  Qd.exports = ln;
  var sa = oa();
  Kd(ln.prototype, sa.prototype);
  Kd(ln, sa);
  function ln(t) {
    if (!(this instanceof ln)) return new ln(t);
    sa.call(this, t);
  }
  ln.prototype._transform = function(t, e, r) {
    r(null, t);
  };
});
var so = D((nB, ep) => {
  A();
  x();
  I();
  var jn = tr(), { ArrayIsArray: m0, Promise: _0, SymbolAsyncIterator: v0, SymbolDispose: S0 } = Ee(), oo = Ut(), { once: E0 } = $e(), A0 = Ar(), Yd = St(), { aggregateTwoErrors: I0, codes: { ERR_INVALID_ARG_TYPE: ga, ERR_INVALID_RETURN_VALUE: ua, ERR_MISSING_ARGS: x0, ERR_STREAM_DESTROYED: T0, ERR_STREAM_PREMATURE_CLOSE: P0 }, AbortError: O0 } = De(), { validateFunction: R0, validateAbortSignal: k0 } = Xr(), { isIterable: Pr, isReadable: la, isReadableNodeStream: io, isNodeStream: Jd, isTransformStream: cn, isWebStream: C0, isReadableStream: ca, isReadableFinished: B0 } = mt(), M0 = globalThis.AbortController || Yr().AbortController, fa, ha, da;
  function Xd(t, e, r) {
    let n = false;
    t.on("close", () => {
      n = true;
    });
    let i = oo(t, { readable: e, writable: r }, (s) => {
      n = !s;
    });
    return { destroy: (s) => {
      n || (n = true, A0.destroyer(t, s || new T0("pipe")));
    }, cleanup: i };
  }
  function N0(t) {
    return R0(t[t.length - 1], "streams[stream.length - 1]"), t.pop();
  }
  function pa(t) {
    if (Pr(t)) return t;
    if (io(t)) return q0(t);
    throw new ga("val", ["Readable", "Iterable", "AsyncIterable"], t);
  }
  async function* q0(t) {
    ha || (ha = Nn()), yield* ha.prototype[v0].call(t);
  }
  async function no(t, e, r, { end: n }) {
    let i, s = null, o = (f) => {
      if (f && (i = f), s) {
        let h2 = s;
        s = null, h2();
      }
    }, a = () => new _0((f, h2) => {
      i ? h2(i) : s = () => {
        i ? h2(i) : f();
      };
    });
    e.on("drain", o);
    let c = oo(e, { readable: false }, o);
    try {
      e.writableNeedDrain && await a();
      for await (let f of t) e.write(f) || await a();
      n && (e.end(), await a()), r();
    } catch (f) {
      r(i !== f ? I0(i, f) : f);
    } finally {
      c(), e.off("drain", o);
    }
  }
  async function ya(t, e, r, { end: n }) {
    cn(e) && (e = e.writable);
    let i = e.getWriter();
    try {
      for await (let s of t) await i.ready, i.write(s).catch(() => {
      });
      await i.ready, n && await i.close(), r();
    } catch (s) {
      try {
        await i.abort(s), r(s);
      } catch (o) {
        r(o);
      }
    }
  }
  function L0(...t) {
    return Zd(t, E0(N0(t)));
  }
  function Zd(t, e, r) {
    if (t.length === 1 && m0(t[0]) && (t = t[0]), t.length < 2) throw new x0("streams");
    let n = new M0(), i = n.signal, s = r?.signal, o = [];
    k0(s, "options.signal");
    function a() {
      P(new O0());
    }
    da = da || $e().addAbortListener;
    let c;
    s && (c = da(s, a));
    let f, h2, d = [], w = 0;
    function y(R) {
      P(R, --w === 0);
    }
    function P(R, T) {
      var g;
      if (R && (!f || f.code === "ERR_STREAM_PREMATURE_CLOSE") && (f = R), !(!f && !T)) {
        for (; d.length; ) d.shift()(f);
        (g = c) === null || g === void 0 || g[S0](), n.abort(), T && (f || o.forEach((O) => O()), jn.nextTick(e, f, h2));
      }
    }
    let E;
    for (let R = 0; R < t.length; R++) {
      let T = t[R], g = R < t.length - 1, O = R > 0, _ = g || r?.end !== false, q = R === t.length - 1;
      if (Jd(T)) {
        let $ = function(j) {
          j && j.name !== "AbortError" && j.code !== "ERR_STREAM_PREMATURE_CLOSE" && y(j);
        };
        if (_) {
          let { destroy: j, cleanup: J } = Xd(T, g, O);
          d.push(j), la(T) && q && o.push(J);
        }
        T.on("error", $), la(T) && q && o.push(() => {
          T.removeListener("error", $);
        });
      }
      if (R === 0) if (typeof T == "function") {
        if (E = T({ signal: i }), !Pr(E)) throw new ua("Iterable, AsyncIterable or Stream", "source", E);
      } else Pr(T) || io(T) || cn(T) ? E = T : E = Yd.from(T);
      else if (typeof T == "function") {
        if (cn(E)) {
          var b;
          E = pa((b = E) === null || b === void 0 ? void 0 : b.readable);
        } else E = pa(E);
        if (E = T(E, { signal: i }), g) {
          if (!Pr(E, true)) throw new ua("AsyncIterable", `transform[${R - 1}]`, E);
        } else {
          var m;
          fa || (fa = aa());
          let $ = new fa({ objectMode: true }), j = (m = E) === null || m === void 0 ? void 0 : m.then;
          if (typeof j == "function") w++, j.call(E, (H) => {
            h2 = H, H != null && $.write(H), _ && $.end(), jn.nextTick(y);
          }, (H) => {
            $.destroy(H), jn.nextTick(y, H);
          });
          else if (Pr(E, true)) w++, no(E, $, y, { end: _ });
          else if (ca(E) || cn(E)) {
            let H = E.readable || E;
            w++, no(H, $, y, { end: _ });
          } else throw new ua("AsyncIterable or Promise", "destination", E);
          E = $;
          let { destroy: J, cleanup: W } = Xd(E, false, true);
          d.push(J), q && o.push(W);
        }
      } else if (Jd(T)) {
        if (io(E)) {
          w += 2;
          let $ = U0(E, T, y, { end: _ });
          la(T) && q && o.push($);
        } else if (cn(E) || ca(E)) {
          let $ = E.readable || E;
          w++, no($, T, y, { end: _ });
        } else if (Pr(E)) w++, no(E, T, y, { end: _ });
        else throw new ga("val", ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"], E);
        E = T;
      } else if (C0(T)) {
        if (io(E)) w++, ya(pa(E), T, y, { end: _ });
        else if (ca(E) || Pr(E)) w++, ya(E, T, y, { end: _ });
        else if (cn(E)) w++, ya(E.readable, T, y, { end: _ });
        else throw new ga("val", ["Readable", "Iterable", "AsyncIterable", "ReadableStream", "TransformStream"], E);
        E = T;
      } else E = Yd.from(T);
    }
    return (i != null && i.aborted || s != null && s.aborted) && jn.nextTick(a), E;
  }
  function U0(t, e, r, { end: n }) {
    let i = false;
    if (e.on("close", () => {
      i || r(new P0());
    }), t.pipe(e, { end: false }), n) {
      let o = function() {
        i = true, e.end();
      };
      B0(t) ? jn.nextTick(o) : t.once("end", o);
    } else r();
    return oo(t, { readable: true, writable: false }, (o) => {
      let a = t._readableState;
      o && o.code === "ERR_STREAM_PREMATURE_CLOSE" && a && a.ended && !a.errored && !a.errorEmitted ? t.once("end", r).once("error", r) : r(o);
    }), oo(e, { readable: false, writable: true }, r);
  }
  ep.exports = { pipelineImpl: Zd, pipeline: L0 };
});
var wa = D((dB, sp) => {
  A();
  x();
  I();
  var { pipeline: D0 } = so(), ao = St(), { destroyer: j0 } = Ar(), { isNodeStream: uo, isReadable: tp, isWritable: rp, isWebStream: ba, isTransformStream: Or, isWritableStream: np, isReadableStream: ip } = mt(), { AbortError: F0, codes: { ERR_INVALID_ARG_VALUE: op, ERR_MISSING_ARGS: W0 } } = De(), $0 = Ut();
  sp.exports = function(...e) {
    if (e.length === 0) throw new W0("streams");
    if (e.length === 1) return ao.from(e[0]);
    let r = [...e];
    if (typeof e[0] == "function" && (e[0] = ao.from(e[0])), typeof e[e.length - 1] == "function") {
      let y = e.length - 1;
      e[y] = ao.from(e[y]);
    }
    for (let y = 0; y < e.length; ++y) if (!(!uo(e[y]) && !ba(e[y]))) {
      if (y < e.length - 1 && !(tp(e[y]) || ip(e[y]) || Or(e[y]))) throw new op(`streams[${y}]`, r[y], "must be readable");
      if (y > 0 && !(rp(e[y]) || np(e[y]) || Or(e[y]))) throw new op(`streams[${y}]`, r[y], "must be writable");
    }
    let n, i, s, o, a;
    function c(y) {
      let P = o;
      o = null, P ? P(y) : y ? a.destroy(y) : !w && !d && a.destroy();
    }
    let f = e[0], h2 = D0(e, c), d = !!(rp(f) || np(f) || Or(f)), w = !!(tp(h2) || ip(h2) || Or(h2));
    if (a = new ao({ writableObjectMode: !!(f != null && f.writableObjectMode), readableObjectMode: !!(h2 != null && h2.readableObjectMode), writable: d, readable: w }), d) {
      if (uo(f)) a._write = function(P, E, b) {
        f.write(P, E) ? b() : n = b;
      }, a._final = function(P) {
        f.end(), i = P;
      }, f.on("drain", function() {
        if (n) {
          let P = n;
          n = null, P();
        }
      });
      else if (ba(f)) {
        let E = (Or(f) ? f.writable : f).getWriter();
        a._write = async function(b, m, v) {
          try {
            await E.ready, E.write(b).catch(() => {
            }), v();
          } catch (R) {
            v(R);
          }
        }, a._final = async function(b) {
          try {
            await E.ready, E.close().catch(() => {
            }), i = b;
          } catch (m) {
            b(m);
          }
        };
      }
      let y = Or(h2) ? h2.readable : h2;
      $0(y, () => {
        if (i) {
          let P = i;
          i = null, P();
        }
      });
    }
    if (w) {
      if (uo(h2)) h2.on("readable", function() {
        if (s) {
          let y = s;
          s = null, y();
        }
      }), h2.on("end", function() {
        a.push(null);
      }), a._read = function() {
        for (; ; ) {
          let y = h2.read();
          if (y === null) {
            s = a._read;
            return;
          }
          if (!a.push(y)) return;
        }
      };
      else if (ba(h2)) {
        let P = (Or(h2) ? h2.readable : h2).getReader();
        a._read = async function() {
          for (; ; ) try {
            let { value: E, done: b } = await P.read();
            if (!a.push(E)) return;
            if (b) {
              a.push(null);
              return;
            }
          } catch {
            return;
          }
        };
      }
    }
    return a._destroy = function(y, P) {
      !y && o !== null && (y = new F0()), s = null, n = null, i = null, o === null ? P(y) : (o = P, uo(h2) && j0(h2, y));
    }, a;
  };
});
var gp = D((EB, va) => {
  A();
  x();
  I();
  var H0 = globalThis.AbortController || Yr().AbortController, { codes: { ERR_INVALID_ARG_VALUE: V0, ERR_INVALID_ARG_TYPE: Fn, ERR_MISSING_ARGS: G0, ERR_OUT_OF_RANGE: z0 }, AbortError: Et } = De(), { validateAbortSignal: Rr, validateInteger: ap, validateObject: kr } = Xr(), K0 = Ee().Symbol("kWeak"), Q0 = Ee().Symbol("kResistStopPropagation"), { finished: Y0 } = Ut(), J0 = wa(), { addAbortSignalNoValidate: X0 } = kn(), { isWritable: Z0, isNodeStream: eS } = mt(), { deprecate: tS } = $e(), { ArrayPrototypePush: rS, Boolean: nS, MathFloor: up, Number: iS, NumberIsNaN: oS, Promise: lp, PromiseReject: cp, PromiseResolve: sS, PromisePrototypeThen: fp, Symbol: dp } = Ee(), lo = dp("kEmpty"), hp = dp("kEof");
  function aS(t, e) {
    if (e != null && kr(e, "options"), e?.signal != null && Rr(e.signal, "options.signal"), eS(t) && !Z0(t)) throw new V0("stream", t, "must be writable");
    let r = J0(this, t);
    return e != null && e.signal && X0(e.signal, r), r;
  }
  function co(t, e) {
    if (typeof t != "function") throw new Fn("fn", ["Function", "AsyncFunction"], t);
    e != null && kr(e, "options"), e?.signal != null && Rr(e.signal, "options.signal");
    let r = 1;
    e?.concurrency != null && (r = up(e.concurrency));
    let n = r - 1;
    return e?.highWaterMark != null && (n = up(e.highWaterMark)), ap(r, "options.concurrency", 1), ap(n, "options.highWaterMark", 0), n += r, (async function* () {
      let s = $e().AbortSignalAny([e?.signal].filter(nS)), o = this, a = [], c = { signal: s }, f, h2, d = false, w = 0;
      function y() {
        d = true, P();
      }
      function P() {
        w -= 1, E();
      }
      function E() {
        h2 && !d && w < r && a.length < n && (h2(), h2 = null);
      }
      async function b() {
        try {
          for await (let m of o) {
            if (d) return;
            if (s.aborted) throw new Et();
            try {
              if (m = t(m, c), m === lo) continue;
              m = sS(m);
            } catch (v) {
              m = cp(v);
            }
            w += 1, fp(m, P, y), a.push(m), f && (f(), f = null), !d && (a.length >= n || w >= r) && await new lp((v) => {
              h2 = v;
            });
          }
          a.push(hp);
        } catch (m) {
          let v = cp(m);
          fp(v, P, y), a.push(v);
        } finally {
          d = true, f && (f(), f = null);
        }
      }
      b();
      try {
        for (; ; ) {
          for (; a.length > 0; ) {
            let m = await a[0];
            if (m === hp) return;
            if (s.aborted) throw new Et();
            m !== lo && (yield m), a.shift(), E();
          }
          await new lp((m) => {
            f = m;
          });
        }
      } finally {
        d = true, h2 && (h2(), h2 = null);
      }
    }).call(this);
  }
  function uS(t = void 0) {
    return t != null && kr(t, "options"), t?.signal != null && Rr(t.signal, "options.signal"), (async function* () {
      let r = 0;
      for await (let i of this) {
        var n;
        if (t != null && (n = t.signal) !== null && n !== void 0 && n.aborted) throw new Et({ cause: t.signal.reason });
        yield [r++, i];
      }
    }).call(this);
  }
  async function pp(t, e = void 0) {
    for await (let r of _a.call(this, t, e)) return true;
    return false;
  }
  async function lS(t, e = void 0) {
    if (typeof t != "function") throw new Fn("fn", ["Function", "AsyncFunction"], t);
    return !await pp.call(this, async (...r) => !await t(...r), e);
  }
  async function cS(t, e) {
    for await (let r of _a.call(this, t, e)) return r;
  }
  async function fS(t, e) {
    if (typeof t != "function") throw new Fn("fn", ["Function", "AsyncFunction"], t);
    async function r(n, i) {
      return await t(n, i), lo;
    }
    for await (let n of co.call(this, r, e)) ;
  }
  function _a(t, e) {
    if (typeof t != "function") throw new Fn("fn", ["Function", "AsyncFunction"], t);
    async function r(n, i) {
      return await t(n, i) ? n : lo;
    }
    return co.call(this, r, e);
  }
  var ma = class extends G0 {
    constructor() {
      super("reduce"), this.message = "Reduce of an empty stream requires an initial value";
    }
  };
  async function hS(t, e, r) {
    var n;
    if (typeof t != "function") throw new Fn("reducer", ["Function", "AsyncFunction"], t);
    r != null && kr(r, "options"), r?.signal != null && Rr(r.signal, "options.signal");
    let i = arguments.length > 1;
    if (r != null && (n = r.signal) !== null && n !== void 0 && n.aborted) {
      let f = new Et(void 0, { cause: r.signal.reason });
      throw this.once("error", () => {
      }), await Y0(this.destroy(f)), f;
    }
    let s = new H0(), o = s.signal;
    if (r != null && r.signal) {
      let f = { once: true, [K0]: this, [Q0]: true };
      r.signal.addEventListener("abort", () => s.abort(), f);
    }
    let a = false;
    try {
      for await (let f of this) {
        var c;
        if (a = true, r != null && (c = r.signal) !== null && c !== void 0 && c.aborted) throw new Et();
        i ? e = await t(e, f, { signal: o }) : (e = f, i = true);
      }
      if (!a && !i) throw new ma();
    } finally {
      s.abort();
    }
    return e;
  }
  async function dS(t) {
    t != null && kr(t, "options"), t?.signal != null && Rr(t.signal, "options.signal");
    let e = [];
    for await (let n of this) {
      var r;
      if (t != null && (r = t.signal) !== null && r !== void 0 && r.aborted) throw new Et(void 0, { cause: t.signal.reason });
      rS(e, n);
    }
    return e;
  }
  function pS(t, e) {
    let r = co.call(this, t, e);
    return (async function* () {
      for await (let i of r) yield* i;
    }).call(this);
  }
  function yp(t) {
    if (t = iS(t), oS(t)) return 0;
    if (t < 0) throw new z0("number", ">= 0", t);
    return t;
  }
  function yS(t, e = void 0) {
    return e != null && kr(e, "options"), e?.signal != null && Rr(e.signal, "options.signal"), t = yp(t), (async function* () {
      var n;
      if (e != null && (n = e.signal) !== null && n !== void 0 && n.aborted) throw new Et();
      for await (let s of this) {
        var i;
        if (e != null && (i = e.signal) !== null && i !== void 0 && i.aborted) throw new Et();
        t-- <= 0 && (yield s);
      }
    }).call(this);
  }
  function gS(t, e = void 0) {
    return e != null && kr(e, "options"), e?.signal != null && Rr(e.signal, "options.signal"), t = yp(t), (async function* () {
      var n;
      if (e != null && (n = e.signal) !== null && n !== void 0 && n.aborted) throw new Et();
      for await (let s of this) {
        var i;
        if (e != null && (i = e.signal) !== null && i !== void 0 && i.aborted) throw new Et();
        if (t-- > 0 && (yield s), t <= 0) return;
      }
    }).call(this);
  }
  va.exports.streamReturningOperators = { asIndexedPairs: tS(uS, "readable.asIndexedPairs will be removed in a future version."), drop: yS, filter: _a, flatMap: pS, map: co, take: gS, compose: aS };
  va.exports.promiseReturningOperators = { every: lS, forEach: fS, reduce: hS, toArray: dS, some: pp, find: cS };
});
var Sa = D((BB, bp) => {
  A();
  x();
  I();
  var { ArrayPrototypePop: bS, Promise: wS } = Ee(), { isIterable: mS, isNodeStream: _S, isWebStream: vS } = mt(), { pipelineImpl: SS } = so(), { finished: ES } = Ut();
  Ea();
  function AS(...t) {
    return new wS((e, r) => {
      let n, i, s = t[t.length - 1];
      if (s && typeof s == "object" && !_S(s) && !mS(s) && !vS(s)) {
        let o = bS(t);
        n = o.signal, i = o.end;
      }
      SS(t, (o, a) => {
        o ? r(o) : e(a);
      }, { signal: n, end: i });
    });
  }
  bp.exports = { finished: ES, pipeline: AS };
});
var Ea = D(($B, xp) => {
  A();
  x();
  I();
  var { Buffer: IS } = (ke(), me(qe)), { ObjectDefineProperty: Ft, ObjectKeys: _p, ReflectApply: vp } = Ee(), { promisify: { custom: Sp } } = $e(), { streamReturningOperators: wp, promiseReturningOperators: mp } = gp(), { codes: { ERR_ILLEGAL_CONSTRUCTOR: Ep } } = De(), xS = wa(), { setDefaultHighWaterMark: TS, getDefaultHighWaterMark: PS } = Cn(), { pipeline: Ap } = so(), { destroyer: OS } = Ar(), Ip = Ut(), Aa = Sa(), Wn = mt(), Se = xp.exports = $i().Stream;
  Se.isDestroyed = Wn.isDestroyed;
  Se.isDisturbed = Wn.isDisturbed;
  Se.isErrored = Wn.isErrored;
  Se.isReadable = Wn.isReadable;
  Se.isWritable = Wn.isWritable;
  Se.Readable = Nn();
  for (let t of _p(wp)) {
    let r = function(...n) {
      if (new.target) throw Ep();
      return Se.Readable.from(vp(e, this, n));
    }, e = wp[t];
    Ft(r, "name", { __proto__: null, value: e.name }), Ft(r, "length", { __proto__: null, value: e.length }), Ft(Se.Readable.prototype, t, { __proto__: null, value: r, enumerable: false, configurable: true, writable: true });
  }
  for (let t of _p(mp)) {
    let r = function(...n) {
      if (new.target) throw Ep();
      return vp(e, this, n);
    }, e = mp[t];
    Ft(r, "name", { __proto__: null, value: e.name }), Ft(r, "length", { __proto__: null, value: e.length }), Ft(Se.Readable.prototype, t, { __proto__: null, value: r, enumerable: false, configurable: true, writable: true });
  }
  Se.Writable = ro();
  Se.Duplex = St();
  Se.Transform = oa();
  Se.PassThrough = aa();
  Se.pipeline = Ap;
  var { addAbortSignal: RS } = kn();
  Se.addAbortSignal = RS;
  Se.finished = Ip;
  Se.destroy = OS;
  Se.compose = xS;
  Se.setDefaultHighWaterMark = TS;
  Se.getDefaultHighWaterMark = PS;
  Ft(Se, "promises", { __proto__: null, configurable: true, enumerable: true, get() {
    return Aa;
  } });
  Ft(Ap, Sp, { __proto__: null, enumerable: true, get() {
    return Aa.pipeline;
  } });
  Ft(Ip, Sp, { __proto__: null, enumerable: true, get() {
    return Aa.finished;
  } });
  Se.Stream = Se;
  Se._isUint8Array = function(e) {
    return e instanceof Uint8Array;
  };
  Se._uint8ArrayToBuffer = function(e) {
    return IS.from(e.buffer, e.byteOffset, e.byteLength);
  };
});
var or = D((ZB, Pe) => {
  A();
  x();
  I();
  var Be = Ea(), kS = Sa(), CS = Be.Readable.destroy;
  Pe.exports = Be.Readable;
  Pe.exports._uint8ArrayToBuffer = Be._uint8ArrayToBuffer;
  Pe.exports._isUint8Array = Be._isUint8Array;
  Pe.exports.isDisturbed = Be.isDisturbed;
  Pe.exports.isErrored = Be.isErrored;
  Pe.exports.isReadable = Be.isReadable;
  Pe.exports.Readable = Be.Readable;
  Pe.exports.Writable = Be.Writable;
  Pe.exports.Duplex = Be.Duplex;
  Pe.exports.Transform = Be.Transform;
  Pe.exports.PassThrough = Be.PassThrough;
  Pe.exports.addAbortSignal = Be.addAbortSignal;
  Pe.exports.finished = Be.finished;
  Pe.exports.destroy = Be.destroy;
  Pe.exports.destroy = CS;
  Pe.exports.pipeline = Be.pipeline;
  Pe.exports.compose = Be.compose;
  Object.defineProperty(Be, "promises", { configurable: true, enumerable: true, get() {
    return kS;
  } });
  Pe.exports.Stream = Be.Stream;
  Pe.exports.default = Pe.exports;
});
var Tp = D((lM, Ia) => {
  A();
  x();
  I();
  typeof Object.create == "function" ? Ia.exports = function(e, r) {
    r && (e.super_ = r, e.prototype = Object.create(r.prototype, { constructor: { value: e, enumerable: false, writable: true, configurable: true } }));
  } : Ia.exports = function(e, r) {
    if (r) {
      e.super_ = r;
      var n = function() {
      };
      n.prototype = r.prototype, e.prototype = new n(), e.prototype.constructor = e;
    }
  };
});
var Rp = D((mM, Op) => {
  A();
  x();
  I();
  var { Buffer: ft } = (ke(), me(qe)), Pp = Symbol.for("BufferList");
  function be(t) {
    if (!(this instanceof be)) return new be(t);
    be._init.call(this, t);
  }
  be._init = function(e) {
    Object.defineProperty(this, Pp, { value: true }), this._bufs = [], this.length = 0, e && this.append(e);
  };
  be.prototype._new = function(e) {
    return new be(e);
  };
  be.prototype._offset = function(e) {
    if (e === 0) return [0, 0];
    let r = 0;
    for (let n = 0; n < this._bufs.length; n++) {
      let i = r + this._bufs[n].length;
      if (e < i || n === this._bufs.length - 1) return [n, e - r];
      r = i;
    }
  };
  be.prototype._reverseOffset = function(t) {
    let e = t[0], r = t[1];
    for (let n = 0; n < e; n++) r += this._bufs[n].length;
    return r;
  };
  be.prototype.getBuffers = function() {
    return this._bufs;
  };
  be.prototype.get = function(e) {
    if (e > this.length || e < 0) return;
    let r = this._offset(e);
    return this._bufs[r[0]][r[1]];
  };
  be.prototype.slice = function(e, r) {
    return typeof e == "number" && e < 0 && (e += this.length), typeof r == "number" && r < 0 && (r += this.length), this.copy(null, 0, e, r);
  };
  be.prototype.copy = function(e, r, n, i) {
    if ((typeof n != "number" || n < 0) && (n = 0), (typeof i != "number" || i > this.length) && (i = this.length), n >= this.length || i <= 0) return e || ft.alloc(0);
    let s = !!e, o = this._offset(n), a = i - n, c = a, f = s && r || 0, h2 = o[1];
    if (n === 0 && i === this.length) {
      if (!s) return this._bufs.length === 1 ? this._bufs[0] : ft.concat(this._bufs, this.length);
      for (let d = 0; d < this._bufs.length; d++) this._bufs[d].copy(e, f), f += this._bufs[d].length;
      return e;
    }
    if (c <= this._bufs[o[0]].length - h2) return s ? this._bufs[o[0]].copy(e, r, h2, h2 + c) : this._bufs[o[0]].slice(h2, h2 + c);
    s || (e = ft.allocUnsafe(a));
    for (let d = o[0]; d < this._bufs.length; d++) {
      let w = this._bufs[d].length - h2;
      if (c > w) this._bufs[d].copy(e, f, h2), f += w;
      else {
        this._bufs[d].copy(e, f, h2, h2 + c), f += w;
        break;
      }
      c -= w, h2 && (h2 = 0);
    }
    return e.length > f ? e.slice(0, f) : e;
  };
  be.prototype.shallowSlice = function(e, r) {
    if (e = e || 0, r = typeof r != "number" ? this.length : r, e < 0 && (e += this.length), r < 0 && (r += this.length), e === r) return this._new();
    let n = this._offset(e), i = this._offset(r), s = this._bufs.slice(n[0], i[0] + 1);
    return i[1] === 0 ? s.pop() : s[s.length - 1] = s[s.length - 1].slice(0, i[1]), n[1] !== 0 && (s[0] = s[0].slice(n[1])), this._new(s);
  };
  be.prototype.toString = function(e, r, n) {
    return this.slice(r, n).toString(e);
  };
  be.prototype.consume = function(e) {
    if (e = Math.trunc(e), Number.isNaN(e) || e <= 0) return this;
    for (; this._bufs.length; ) if (e >= this._bufs[0].length) e -= this._bufs[0].length, this.length -= this._bufs[0].length, this._bufs.shift();
    else {
      this._bufs[0] = this._bufs[0].slice(e), this.length -= e;
      break;
    }
    return this;
  };
  be.prototype.duplicate = function() {
    let e = this._new();
    for (let r = 0; r < this._bufs.length; r++) e.append(this._bufs[r]);
    return e;
  };
  be.prototype.append = function(e) {
    return this._attach(e, be.prototype._appendBuffer);
  };
  be.prototype.prepend = function(e) {
    return this._attach(e, be.prototype._prependBuffer, true);
  };
  be.prototype._attach = function(e, r, n) {
    if (e == null) return this;
    if (e.buffer) r.call(this, ft.from(e.buffer, e.byteOffset, e.byteLength));
    else if (Array.isArray(e)) {
      let [i, s] = n ? [e.length - 1, -1] : [0, 1];
      for (let o = i; o >= 0 && o < e.length; o += s) this._attach(e[o], r, n);
    } else if (this._isBufferList(e)) {
      let [i, s] = n ? [e._bufs.length - 1, -1] : [0, 1];
      for (let o = i; o >= 0 && o < e._bufs.length; o += s) this._attach(e._bufs[o], r, n);
    } else typeof e == "number" && (e = e.toString()), r.call(this, ft.from(e));
    return this;
  };
  be.prototype._appendBuffer = function(e) {
    this._bufs.push(e), this.length += e.length;
  };
  be.prototype._prependBuffer = function(e) {
    this._bufs.unshift(e), this.length += e.length;
  };
  be.prototype.indexOf = function(t, e, r) {
    if (r === void 0 && typeof e == "string" && (r = e, e = void 0), typeof t == "function" || Array.isArray(t)) throw new TypeError('The "value" argument must be one of type string, Buffer, BufferList, or Uint8Array.');
    if (typeof t == "number" ? t = ft.from([t]) : typeof t == "string" ? t = ft.from(t, r) : this._isBufferList(t) ? t = t.slice() : Array.isArray(t.buffer) ? t = ft.from(t.buffer, t.byteOffset, t.byteLength) : ft.isBuffer(t) || (t = ft.from(t)), e = Number(e || 0), isNaN(e) && (e = 0), e < 0 && (e = this.length + e), e < 0 && (e = 0), t.length === 0) return e > this.length ? this.length : e;
    let n = this._offset(e), i = n[0], s = n[1];
    for (; i < this._bufs.length; i++) {
      let o = this._bufs[i];
      for (; s < o.length; ) if (o.length - s >= t.length) {
        let c = o.indexOf(t, s);
        if (c !== -1) return this._reverseOffset([i, c]);
        s = o.length - t.length + 1;
      } else {
        let c = this._reverseOffset([i, s]);
        if (this._match(c, t)) return c;
        s++;
      }
      s = 0;
    }
    return -1;
  };
  be.prototype._match = function(t, e) {
    if (this.length - t < e.length) return false;
    for (let r = 0; r < e.length; r++) if (this.get(t + r) !== e[r]) return false;
    return true;
  };
  (function() {
    let t = { readDoubleBE: 8, readDoubleLE: 8, readFloatBE: 4, readFloatLE: 4, readBigInt64BE: 8, readBigInt64LE: 8, readBigUInt64BE: 8, readBigUInt64LE: 8, readInt32BE: 4, readInt32LE: 4, readUInt32BE: 4, readUInt32LE: 4, readInt16BE: 2, readInt16LE: 2, readUInt16BE: 2, readUInt16LE: 2, readInt8: 1, readUInt8: 1, readIntBE: null, readIntLE: null, readUIntBE: null, readUIntLE: null };
    for (let e in t) (function(r) {
      t[r] === null ? be.prototype[r] = function(n, i) {
        return this.slice(n, n + i)[r](0, i);
      } : be.prototype[r] = function(n = 0) {
        return this.slice(n, n + t[r])[r](0);
      };
    })(e);
  })();
  be.prototype._isBufferList = function(e) {
    return e instanceof be || be.isBufferList(e);
  };
  be.isBufferList = function(e) {
    return e != null && e[Pp];
  };
  Op.exports = be;
});
var kp = D((OM, fo) => {
  A();
  x();
  I();
  var xa = or().Duplex, BS = Tp(), $n = Rp();
  function Fe(t) {
    if (!(this instanceof Fe)) return new Fe(t);
    if (typeof t == "function") {
      this._callback = t;
      let e = (function(n) {
        this._callback && (this._callback(n), this._callback = null);
      }).bind(this);
      this.on("pipe", function(n) {
        n.on("error", e);
      }), this.on("unpipe", function(n) {
        n.removeListener("error", e);
      }), t = null;
    }
    $n._init.call(this, t), xa.call(this);
  }
  BS(Fe, xa);
  Object.assign(Fe.prototype, $n.prototype);
  Fe.prototype._new = function(e) {
    return new Fe(e);
  };
  Fe.prototype._write = function(e, r, n) {
    this._appendBuffer(e), typeof n == "function" && n();
  };
  Fe.prototype._read = function(e) {
    if (!this.length) return this.push(null);
    e = Math.min(e, this.length), this.push(this.slice(0, e)), this.consume(e);
  };
  Fe.prototype.end = function(e) {
    xa.prototype.end.call(this, e), this._callback && (this._callback(null, this.slice()), this._callback = null);
  };
  Fe.prototype._destroy = function(e, r) {
    this._bufs.length = 0, this.length = 0, r(e);
  };
  Fe.prototype._isBufferList = function(e) {
    return e instanceof Fe || e instanceof $n || Fe.isBufferList(e);
  };
  Fe.isBufferList = $n.isBufferList;
  fo.exports = Fe;
  fo.exports.BufferListStream = Fe;
  fo.exports.BufferList = $n;
});
var Bp = D((DM, Cp) => {
  A();
  x();
  I();
  var Ta = class {
    constructor() {
      this.cmd = null, this.retain = false, this.qos = 0, this.dup = false, this.length = -1, this.topic = null, this.payload = null;
    }
  };
  Cp.exports = Ta;
});
var Pa = D((QM, Mp) => {
  A();
  x();
  I();
  var z = Mp.exports, { Buffer: Ke } = (ke(), me(qe));
  z.types = { 0: "reserved", 1: "connect", 2: "connack", 3: "publish", 4: "puback", 5: "pubrec", 6: "pubrel", 7: "pubcomp", 8: "subscribe", 9: "suback", 10: "unsubscribe", 11: "unsuback", 12: "pingreq", 13: "pingresp", 14: "disconnect", 15: "auth" };
  z.requiredHeaderFlags = { 1: 0, 2: 0, 4: 0, 5: 0, 6: 2, 7: 0, 8: 2, 9: 0, 10: 2, 11: 0, 12: 0, 13: 0, 14: 0, 15: 0 };
  z.requiredHeaderFlagsErrors = {};
  for (let t in z.requiredHeaderFlags) {
    let e = z.requiredHeaderFlags[t];
    z.requiredHeaderFlagsErrors[t] = "Invalid header flag bits, must be 0x" + e.toString(16) + " for " + z.types[t] + " packet";
  }
  z.codes = {};
  for (let t in z.types) {
    let e = z.types[t];
    z.codes[e] = t;
  }
  z.CMD_SHIFT = 4;
  z.CMD_MASK = 240;
  z.DUP_MASK = 8;
  z.QOS_MASK = 3;
  z.QOS_SHIFT = 1;
  z.RETAIN_MASK = 1;
  z.VARBYTEINT_MASK = 127;
  z.VARBYTEINT_FIN_MASK = 128;
  z.VARBYTEINT_MAX = 268435455;
  z.SESSIONPRESENT_MASK = 1;
  z.SESSIONPRESENT_HEADER = Ke.from([z.SESSIONPRESENT_MASK]);
  z.CONNACK_HEADER = Ke.from([z.codes.connack << z.CMD_SHIFT]);
  z.USERNAME_MASK = 128;
  z.PASSWORD_MASK = 64;
  z.WILL_RETAIN_MASK = 32;
  z.WILL_QOS_MASK = 24;
  z.WILL_QOS_SHIFT = 3;
  z.WILL_FLAG_MASK = 4;
  z.CLEAN_SESSION_MASK = 2;
  z.CONNECT_HEADER = Ke.from([z.codes.connect << z.CMD_SHIFT]);
  z.properties = { sessionExpiryInterval: 17, willDelayInterval: 24, receiveMaximum: 33, maximumPacketSize: 39, topicAliasMaximum: 34, requestResponseInformation: 25, requestProblemInformation: 23, userProperties: 38, authenticationMethod: 21, authenticationData: 22, payloadFormatIndicator: 1, messageExpiryInterval: 2, contentType: 3, responseTopic: 8, correlationData: 9, maximumQoS: 36, retainAvailable: 37, assignedClientIdentifier: 18, reasonString: 31, wildcardSubscriptionAvailable: 40, subscriptionIdentifiersAvailable: 41, sharedSubscriptionAvailable: 42, serverKeepAlive: 19, responseInformation: 26, serverReference: 28, topicAlias: 35, subscriptionIdentifier: 11 };
  z.propertiesCodes = {};
  for (let t in z.properties) {
    let e = z.properties[t];
    z.propertiesCodes[e] = t;
  }
  z.propertiesTypes = { sessionExpiryInterval: "int32", willDelayInterval: "int32", receiveMaximum: "int16", maximumPacketSize: "int32", topicAliasMaximum: "int16", requestResponseInformation: "byte", requestProblemInformation: "byte", userProperties: "pair", authenticationMethod: "string", authenticationData: "binary", payloadFormatIndicator: "byte", messageExpiryInterval: "int32", contentType: "string", responseTopic: "string", correlationData: "binary", maximumQoS: "int8", retainAvailable: "byte", assignedClientIdentifier: "string", reasonString: "string", wildcardSubscriptionAvailable: "byte", subscriptionIdentifiersAvailable: "byte", sharedSubscriptionAvailable: "byte", serverKeepAlive: "int16", responseInformation: "string", serverReference: "string", topicAlias: "int16", subscriptionIdentifier: "var" };
  function sr(t) {
    return [0, 1, 2].map((e) => [0, 1].map((r) => [0, 1].map((n) => {
      let i = Ke.alloc(1);
      return i.writeUInt8(z.codes[t] << z.CMD_SHIFT | (r ? z.DUP_MASK : 0) | e << z.QOS_SHIFT | n, 0, true), i;
    })));
  }
  z.PUBLISH_HEADER = sr("publish");
  z.SUBSCRIBE_HEADER = sr("subscribe");
  z.SUBSCRIBE_OPTIONS_QOS_MASK = 3;
  z.SUBSCRIBE_OPTIONS_NL_MASK = 1;
  z.SUBSCRIBE_OPTIONS_NL_SHIFT = 2;
  z.SUBSCRIBE_OPTIONS_RAP_MASK = 1;
  z.SUBSCRIBE_OPTIONS_RAP_SHIFT = 3;
  z.SUBSCRIBE_OPTIONS_RH_MASK = 3;
  z.SUBSCRIBE_OPTIONS_RH_SHIFT = 4;
  z.SUBSCRIBE_OPTIONS_RH = [0, 16, 32];
  z.SUBSCRIBE_OPTIONS_NL = 4;
  z.SUBSCRIBE_OPTIONS_RAP = 8;
  z.SUBSCRIBE_OPTIONS_QOS = [0, 1, 2];
  z.UNSUBSCRIBE_HEADER = sr("unsubscribe");
  z.ACKS = { unsuback: sr("unsuback"), puback: sr("puback"), pubcomp: sr("pubcomp"), pubrel: sr("pubrel"), pubrec: sr("pubrec") };
  z.SUBACK_HEADER = Ke.from([z.codes.suback << z.CMD_SHIFT]);
  z.VERSION3 = Ke.from([3]);
  z.VERSION4 = Ke.from([4]);
  z.VERSION5 = Ke.from([5]);
  z.VERSION131 = Ke.from([131]);
  z.VERSION132 = Ke.from([132]);
  z.QOS = [0, 1, 2].map((t) => Ke.from([t]));
  z.EMPTY = { pingreq: Ke.from([z.codes.pingreq << 4, 0]), pingresp: Ke.from([z.codes.pingresp << 4, 0]), disconnect: Ke.from([z.codes.disconnect << 4, 0]) };
  z.MQTT5_PUBACK_PUBREC_CODES = { 0: "Success", 16: "No matching subscribers", 128: "Unspecified error", 131: "Implementation specific error", 135: "Not authorized", 144: "Topic Name invalid", 145: "Packet identifier in use", 151: "Quota exceeded", 153: "Payload format invalid" };
  z.MQTT5_PUBREL_PUBCOMP_CODES = { 0: "Success", 146: "Packet Identifier not found" };
  z.MQTT5_SUBACK_CODES = { 0: "Granted QoS 0", 1: "Granted QoS 1", 2: "Granted QoS 2", 128: "Unspecified error", 131: "Implementation specific error", 135: "Not authorized", 143: "Topic Filter invalid", 145: "Packet Identifier in use", 151: "Quota exceeded", 158: "Shared Subscriptions not supported", 161: "Subscription Identifiers not supported", 162: "Wildcard Subscriptions not supported" };
  z.MQTT5_UNSUBACK_CODES = { 0: "Success", 17: "No subscription existed", 128: "Unspecified error", 131: "Implementation specific error", 135: "Not authorized", 143: "Topic Filter invalid", 145: "Packet Identifier in use" };
  z.MQTT5_DISCONNECT_CODES = { 0: "Normal disconnection", 4: "Disconnect with Will Message", 128: "Unspecified error", 129: "Malformed Packet", 130: "Protocol Error", 131: "Implementation specific error", 135: "Not authorized", 137: "Server busy", 139: "Server shutting down", 141: "Keep Alive timeout", 142: "Session taken over", 143: "Topic Filter invalid", 144: "Topic Name invalid", 147: "Receive Maximum exceeded", 148: "Topic Alias invalid", 149: "Packet too large", 150: "Message rate too high", 151: "Quota exceeded", 152: "Administrative action", 153: "Payload format invalid", 154: "Retain not supported", 155: "QoS not supported", 156: "Use another server", 157: "Server moved", 158: "Shared Subscriptions not supported", 159: "Connection rate exceeded", 160: "Maximum connect time", 161: "Subscription Identifiers not supported", 162: "Wildcard Subscriptions not supported" };
  z.MQTT5_AUTH_CODES = { 0: "Success", 24: "Continue authentication", 25: "Re-authenticate" };
});
var qp = D((o2, Np) => {
  A();
  x();
  I();
  var fn = 1e3, hn = fn * 60, dn = hn * 60, Cr = dn * 24, MS = Cr * 7, NS = Cr * 365.25;
  Np.exports = function(t, e) {
    e = e || {};
    var r = typeof t;
    if (r === "string" && t.length > 0) return qS(t);
    if (r === "number" && isFinite(t)) return e.long ? US(t) : LS(t);
    throw new Error("val is not a non-empty string or a valid number. val=" + JSON.stringify(t));
  };
  function qS(t) {
    if (t = String(t), !(t.length > 100)) {
      var e = /^(-?(?:\d+)?\.?\d+) *(milliseconds?|msecs?|ms|seconds?|secs?|s|minutes?|mins?|m|hours?|hrs?|h|days?|d|weeks?|w|years?|yrs?|y)?$/i.exec(t);
      if (e) {
        var r = parseFloat(e[1]), n = (e[2] || "ms").toLowerCase();
        switch (n) {
          case "years":
          case "year":
          case "yrs":
          case "yr":
          case "y":
            return r * NS;
          case "weeks":
          case "week":
          case "w":
            return r * MS;
          case "days":
          case "day":
          case "d":
            return r * Cr;
          case "hours":
          case "hour":
          case "hrs":
          case "hr":
          case "h":
            return r * dn;
          case "minutes":
          case "minute":
          case "mins":
          case "min":
          case "m":
            return r * hn;
          case "seconds":
          case "second":
          case "secs":
          case "sec":
          case "s":
            return r * fn;
          case "milliseconds":
          case "millisecond":
          case "msecs":
          case "msec":
          case "ms":
            return r;
          default:
            return;
        }
      }
    }
  }
  function LS(t) {
    var e = Math.abs(t);
    return e >= Cr ? Math.round(t / Cr) + "d" : e >= dn ? Math.round(t / dn) + "h" : e >= hn ? Math.round(t / hn) + "m" : e >= fn ? Math.round(t / fn) + "s" : t + "ms";
  }
  function US(t) {
    var e = Math.abs(t);
    return e >= Cr ? ho(t, e, Cr, "day") : e >= dn ? ho(t, e, dn, "hour") : e >= hn ? ho(t, e, hn, "minute") : e >= fn ? ho(t, e, fn, "second") : t + " ms";
  }
  function ho(t, e, r, n) {
    var i = e >= r * 1.5;
    return Math.round(t / r) + " " + n + (i ? "s" : "");
  }
});
var Up = D((y2, Lp) => {
  A();
  x();
  I();
  function DS(t) {
    r.debug = r, r.default = r, r.coerce = c, r.disable = o, r.enable = i, r.enabled = a, r.humanize = qp(), r.destroy = f, Object.keys(t).forEach((h2) => {
      r[h2] = t[h2];
    }), r.names = [], r.skips = [], r.formatters = {};
    function e(h2) {
      let d = 0;
      for (let w = 0; w < h2.length; w++) d = (d << 5) - d + h2.charCodeAt(w), d |= 0;
      return r.colors[Math.abs(d) % r.colors.length];
    }
    r.selectColor = e;
    function r(h2) {
      let d, w = null, y, P;
      function E(...b) {
        if (!E.enabled) return;
        let m = E, v = Number(/* @__PURE__ */ new Date()), R = v - (d || v);
        m.diff = R, m.prev = d, m.curr = v, d = v, b[0] = r.coerce(b[0]), typeof b[0] != "string" && b.unshift("%O");
        let T = 0;
        b[0] = b[0].replace(/%([a-zA-Z%])/g, (O, _) => {
          if (O === "%%") return "%";
          T++;
          let q = r.formatters[_];
          if (typeof q == "function") {
            let $ = b[T];
            O = q.call(m, $), b.splice(T, 1), T--;
          }
          return O;
        }), r.formatArgs.call(m, b), (m.log || r.log).apply(m, b);
      }
      return E.namespace = h2, E.useColors = r.useColors(), E.color = r.selectColor(h2), E.extend = n, E.destroy = r.destroy, Object.defineProperty(E, "enabled", { enumerable: true, configurable: false, get: () => w !== null ? w : (y !== r.namespaces && (y = r.namespaces, P = r.enabled(h2)), P), set: (b) => {
        w = b;
      } }), typeof r.init == "function" && r.init(E), E;
    }
    function n(h2, d) {
      let w = r(this.namespace + (typeof d > "u" ? ":" : d) + h2);
      return w.log = this.log, w;
    }
    function i(h2) {
      r.save(h2), r.namespaces = h2, r.names = [], r.skips = [];
      let d = (typeof h2 == "string" ? h2 : "").trim().replace(/\s+/g, ",").split(",").filter(Boolean);
      for (let w of d) w[0] === "-" ? r.skips.push(w.slice(1)) : r.names.push(w);
    }
    function s(h2, d) {
      let w = 0, y = 0, P = -1, E = 0;
      for (; w < h2.length; ) if (y < d.length && (d[y] === h2[w] || d[y] === "*")) d[y] === "*" ? (P = y, E = w, y++) : (w++, y++);
      else if (P !== -1) y = P + 1, E++, w = E;
      else return false;
      for (; y < d.length && d[y] === "*"; ) y++;
      return y === d.length;
    }
    function o() {
      let h2 = [...r.names, ...r.skips.map((d) => "-" + d)].join(",");
      return r.enable(""), h2;
    }
    function a(h2) {
      for (let d of r.skips) if (s(h2, d)) return false;
      for (let d of r.names) if (s(h2, d)) return true;
      return false;
    }
    function c(h2) {
      return h2 instanceof Error ? h2.stack || h2.message : h2;
    }
    function f() {
      console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`.");
    }
    return r.enable(r.load()), r;
  }
  Lp.exports = DS;
});
var At = D((Qe, po) => {
  A();
  x();
  I();
  Qe.formatArgs = FS;
  Qe.save = WS;
  Qe.load = $S;
  Qe.useColors = jS;
  Qe.storage = HS();
  Qe.destroy = /* @__PURE__ */ (() => {
    let t = false;
    return () => {
      t || (t = true, console.warn("Instance method `debug.destroy()` is deprecated and no longer does anything. It will be removed in the next major version of `debug`."));
    };
  })();
  Qe.colors = ["#0000CC", "#0000FF", "#0033CC", "#0033FF", "#0066CC", "#0066FF", "#0099CC", "#0099FF", "#00CC00", "#00CC33", "#00CC66", "#00CC99", "#00CCCC", "#00CCFF", "#3300CC", "#3300FF", "#3333CC", "#3333FF", "#3366CC", "#3366FF", "#3399CC", "#3399FF", "#33CC00", "#33CC33", "#33CC66", "#33CC99", "#33CCCC", "#33CCFF", "#6600CC", "#6600FF", "#6633CC", "#6633FF", "#66CC00", "#66CC33", "#9900CC", "#9900FF", "#9933CC", "#9933FF", "#99CC00", "#99CC33", "#CC0000", "#CC0033", "#CC0066", "#CC0099", "#CC00CC", "#CC00FF", "#CC3300", "#CC3333", "#CC3366", "#CC3399", "#CC33CC", "#CC33FF", "#CC6600", "#CC6633", "#CC9900", "#CC9933", "#CCCC00", "#CCCC33", "#FF0000", "#FF0033", "#FF0066", "#FF0099", "#FF00CC", "#FF00FF", "#FF3300", "#FF3333", "#FF3366", "#FF3399", "#FF33CC", "#FF33FF", "#FF6600", "#FF6633", "#FF9900", "#FF9933", "#FFCC00", "#FFCC33"];
  function jS() {
    if (typeof window < "u" && window.process && (window.process.type === "renderer" || window.process.__nwjs)) return true;
    if (typeof navigator < "u" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/(edge|trident)\/(\d+)/)) return false;
    let t;
    return typeof document < "u" && document.documentElement && document.documentElement.style && document.documentElement.style.WebkitAppearance || typeof window < "u" && window.console && (window.console.firebug || window.console.exception && window.console.table) || typeof navigator < "u" && navigator.userAgent && (t = navigator.userAgent.toLowerCase().match(/firefox\/(\d+)/)) && parseInt(t[1], 10) >= 31 || typeof navigator < "u" && navigator.userAgent && navigator.userAgent.toLowerCase().match(/applewebkit\/(\d+)/);
  }
  function FS(t) {
    if (t[0] = (this.useColors ? "%c" : "") + this.namespace + (this.useColors ? " %c" : " ") + t[0] + (this.useColors ? "%c " : " ") + "+" + po.exports.humanize(this.diff), !this.useColors) return;
    let e = "color: " + this.color;
    t.splice(1, 0, e, "color: inherit");
    let r = 0, n = 0;
    t[0].replace(/%[a-zA-Z%]/g, (i) => {
      i !== "%%" && (r++, i === "%c" && (n = r));
    }), t.splice(n, 0, e);
  }
  Qe.log = console.debug || console.log || (() => {
  });
  function WS(t) {
    try {
      t ? Qe.storage.setItem("debug", t) : Qe.storage.removeItem("debug");
    } catch {
    }
  }
  function $S() {
    let t;
    try {
      t = Qe.storage.getItem("debug") || Qe.storage.getItem("DEBUG");
    } catch {
    }
    return !t && typeof M < "u" && "env" in M && (t = M.env.DEBUG), t;
  }
  function HS() {
    try {
      return localStorage;
    } catch {
    }
  }
  po.exports = Up()(Qe);
  var { formatters: VS } = po.exports;
  VS.j = function(t) {
    try {
      return JSON.stringify(t);
    } catch (e) {
      return "[UnexpectedJSONParseError]: " + e.message;
    }
  };
});
var Fp = D((M2, jp) => {
  A();
  x();
  I();
  var GS = kp(), { EventEmitter: zS } = (er(), me(Zt)), Dp = Bp(), de = Pa(), ae = At()("mqtt-packet:parser"), Oa = class t extends zS {
    constructor() {
      super(), this.parser = this.constructor.parser;
    }
    static parser(e) {
      return this instanceof t ? (this.settings = e || {}, this._states = ["_parseHeader", "_parseLength", "_parsePayload", "_newPacket"], this._resetState(), this) : new t().parser(e);
    }
    _resetState() {
      ae("_resetState: resetting packet, error, _list, and _stateCounter"), this.packet = new Dp(), this.error = null, this._list = GS(), this._stateCounter = 0;
    }
    parse(e) {
      for (this.error && this._resetState(), this._list.append(e), ae("parse: current state: %s", this._states[this._stateCounter]); (this.packet.length !== -1 || this._list.length > 0) && this[this._states[this._stateCounter]]() && !this.error; ) this._stateCounter++, ae("parse: state complete. _stateCounter is now: %d", this._stateCounter), ae("parse: packet.length: %d, buffer list length: %d", this.packet.length, this._list.length), this._stateCounter >= this._states.length && (this._stateCounter = 0);
      return ae("parse: exited while loop. packet: %d, buffer list length: %d", this.packet.length, this._list.length), this._list.length;
    }
    _parseHeader() {
      let e = this._list.readUInt8(0), r = e >> de.CMD_SHIFT;
      this.packet.cmd = de.types[r];
      let n = e & 15, i = de.requiredHeaderFlags[r];
      return i != null && n !== i ? this._emitError(new Error(de.requiredHeaderFlagsErrors[r])) : (this.packet.retain = (e & de.RETAIN_MASK) !== 0, this.packet.qos = e >> de.QOS_SHIFT & de.QOS_MASK, this.packet.qos > 2 ? this._emitError(new Error("Packet must not have both QoS bits set to 1")) : (this.packet.dup = (e & de.DUP_MASK) !== 0, ae("_parseHeader: packet: %o", this.packet), this._list.consume(1), true));
    }
    _parseLength() {
      let e = this._parseVarByteNum(true);
      return e && (this.packet.length = e.value, this._list.consume(e.bytes)), ae("_parseLength %d", e.value), !!e;
    }
    _parsePayload() {
      ae("_parsePayload: payload %O", this._list);
      let e = false;
      if (this.packet.length === 0 || this._list.length >= this.packet.length) {
        switch (this._pos = 0, this.packet.cmd) {
          case "connect":
            this._parseConnect();
            break;
          case "connack":
            this._parseConnack();
            break;
          case "publish":
            this._parsePublish();
            break;
          case "puback":
          case "pubrec":
          case "pubrel":
          case "pubcomp":
            this._parseConfirmation();
            break;
          case "subscribe":
            this._parseSubscribe();
            break;
          case "suback":
            this._parseSuback();
            break;
          case "unsubscribe":
            this._parseUnsubscribe();
            break;
          case "unsuback":
            this._parseUnsuback();
            break;
          case "pingreq":
          case "pingresp":
            break;
          case "disconnect":
            this._parseDisconnect();
            break;
          case "auth":
            this._parseAuth();
            break;
          default:
            this._emitError(new Error("Not supported"));
        }
        e = true;
      }
      return ae("_parsePayload complete result: %s", e), e;
    }
    _parseConnect() {
      ae("_parseConnect");
      let e, r, n, i, s = {}, o = this.packet, a = this._parseString();
      if (a === null) return this._emitError(new Error("Cannot parse protocolId"));
      if (a !== "MQTT" && a !== "MQIsdp") return this._emitError(new Error("Invalid protocolId"));
      if (o.protocolId = a, this._pos >= this._list.length) return this._emitError(new Error("Packet too short"));
      if (o.protocolVersion = this._list.readUInt8(this._pos), o.protocolVersion >= 128 && (o.bridgeMode = true, o.protocolVersion = o.protocolVersion - 128), o.protocolVersion !== 3 && o.protocolVersion !== 4 && o.protocolVersion !== 5) return this._emitError(new Error("Invalid protocol version"));
      if (this._pos++, this._pos >= this._list.length) return this._emitError(new Error("Packet too short"));
      if (this._list.readUInt8(this._pos) & 1) return this._emitError(new Error("Connect flag bit 0 must be 0, but got 1"));
      s.username = this._list.readUInt8(this._pos) & de.USERNAME_MASK, s.password = this._list.readUInt8(this._pos) & de.PASSWORD_MASK, s.will = this._list.readUInt8(this._pos) & de.WILL_FLAG_MASK;
      let c = !!(this._list.readUInt8(this._pos) & de.WILL_RETAIN_MASK), f = (this._list.readUInt8(this._pos) & de.WILL_QOS_MASK) >> de.WILL_QOS_SHIFT;
      if (s.will) o.will = {}, o.will.retain = c, o.will.qos = f;
      else {
        if (c) return this._emitError(new Error("Will Retain Flag must be set to zero when Will Flag is set to 0"));
        if (f) return this._emitError(new Error("Will QoS must be set to zero when Will Flag is set to 0"));
      }
      if (o.clean = (this._list.readUInt8(this._pos) & de.CLEAN_SESSION_MASK) !== 0, this._pos++, o.keepalive = this._parseNum(), o.keepalive === -1) return this._emitError(new Error("Packet too short"));
      if (o.protocolVersion === 5) {
        let d = this._parseProperties();
        Object.getOwnPropertyNames(d).length && (o.properties = d);
      }
      let h2 = this._parseString();
      if (h2 === null) return this._emitError(new Error("Packet too short"));
      if (o.clientId = h2, ae("_parseConnect: packet.clientId: %s", o.clientId), s.will) {
        if (o.protocolVersion === 5) {
          let d = this._parseProperties();
          Object.getOwnPropertyNames(d).length && (o.will.properties = d);
        }
        if (e = this._parseString(), e === null) return this._emitError(new Error("Cannot parse will topic"));
        if (o.will.topic = e, ae("_parseConnect: packet.will.topic: %s", o.will.topic), r = this._parseBuffer(), r === null) return this._emitError(new Error("Cannot parse will payload"));
        o.will.payload = r, ae("_parseConnect: packet.will.paylaod: %s", o.will.payload);
      }
      if (s.username) {
        if (i = this._parseString(), i === null) return this._emitError(new Error("Cannot parse username"));
        o.username = i, ae("_parseConnect: packet.username: %s", o.username);
      }
      if (s.password) {
        if (n = this._parseBuffer(), n === null) return this._emitError(new Error("Cannot parse password"));
        o.password = n;
      }
      return this.settings = o, ae("_parseConnect: complete"), o;
    }
    _parseConnack() {
      ae("_parseConnack");
      let e = this.packet;
      if (this._list.length < 1) return null;
      let r = this._list.readUInt8(this._pos++);
      if (r > 1) return this._emitError(new Error("Invalid connack flags, bits 7-1 must be set to 0"));
      if (e.sessionPresent = !!(r & de.SESSIONPRESENT_MASK), this.settings.protocolVersion === 5) this._list.length >= 2 ? e.reasonCode = this._list.readUInt8(this._pos++) : e.reasonCode = 0;
      else {
        if (this._list.length < 2) return null;
        e.returnCode = this._list.readUInt8(this._pos++);
      }
      if (e.returnCode === -1 || e.reasonCode === -1) return this._emitError(new Error("Cannot parse return code"));
      if (this.settings.protocolVersion === 5) {
        let n = this._parseProperties();
        Object.getOwnPropertyNames(n).length && (e.properties = n);
      }
      ae("_parseConnack: complete");
    }
    _parsePublish() {
      ae("_parsePublish");
      let e = this.packet;
      if (e.topic = this._parseString(), e.topic === null) return this._emitError(new Error("Cannot parse topic"));
      if (!(e.qos > 0 && !this._parseMessageId())) {
        if (this.settings.protocolVersion === 5) {
          let r = this._parseProperties();
          Object.getOwnPropertyNames(r).length && (e.properties = r);
        }
        e.payload = this._list.slice(this._pos, e.length), ae("_parsePublish: payload from buffer list: %o", e.payload);
      }
    }
    _parseSubscribe() {
      ae("_parseSubscribe");
      let e = this.packet, r, n, i, s, o, a, c;
      if (e.subscriptions = [], !!this._parseMessageId()) {
        if (this.settings.protocolVersion === 5) {
          let f = this._parseProperties();
          Object.getOwnPropertyNames(f).length && (e.properties = f);
        }
        if (e.length <= 0) return this._emitError(new Error("Malformed subscribe, no payload specified"));
        for (; this._pos < e.length; ) {
          if (r = this._parseString(), r === null) return this._emitError(new Error("Cannot parse topic"));
          if (this._pos >= e.length) return this._emitError(new Error("Malformed Subscribe Payload"));
          if (n = this._parseByte(), this.settings.protocolVersion === 5) {
            if (n & 192) return this._emitError(new Error("Invalid subscribe topic flag bits, bits 7-6 must be 0"));
          } else if (n & 252) return this._emitError(new Error("Invalid subscribe topic flag bits, bits 7-2 must be 0"));
          if (i = n & de.SUBSCRIBE_OPTIONS_QOS_MASK, i > 2) return this._emitError(new Error("Invalid subscribe QoS, must be <= 2"));
          if (a = (n >> de.SUBSCRIBE_OPTIONS_NL_SHIFT & de.SUBSCRIBE_OPTIONS_NL_MASK) !== 0, o = (n >> de.SUBSCRIBE_OPTIONS_RAP_SHIFT & de.SUBSCRIBE_OPTIONS_RAP_MASK) !== 0, s = n >> de.SUBSCRIBE_OPTIONS_RH_SHIFT & de.SUBSCRIBE_OPTIONS_RH_MASK, s > 2) return this._emitError(new Error("Invalid retain handling, must be <= 2"));
          c = { topic: r, qos: i }, this.settings.protocolVersion === 5 ? (c.nl = a, c.rap = o, c.rh = s) : this.settings.bridgeMode && (c.rh = 0, c.rap = true, c.nl = true), ae("_parseSubscribe: push subscription `%s` to subscription", c), e.subscriptions.push(c);
        }
      }
    }
    _parseSuback() {
      ae("_parseSuback");
      let e = this.packet;
      if (this.packet.granted = [], !!this._parseMessageId()) {
        if (this.settings.protocolVersion === 5) {
          let r = this._parseProperties();
          Object.getOwnPropertyNames(r).length && (e.properties = r);
        }
        if (e.length <= 0) return this._emitError(new Error("Malformed suback, no payload specified"));
        for (; this._pos < this.packet.length; ) {
          let r = this._list.readUInt8(this._pos++);
          if (this.settings.protocolVersion === 5) {
            if (!de.MQTT5_SUBACK_CODES[r]) return this._emitError(new Error("Invalid suback code"));
          } else if (r > 2 && r !== 128) return this._emitError(new Error("Invalid suback QoS, must be 0, 1, 2 or 128"));
          this.packet.granted.push(r);
        }
      }
    }
    _parseUnsubscribe() {
      ae("_parseUnsubscribe");
      let e = this.packet;
      if (e.unsubscriptions = [], !!this._parseMessageId()) {
        if (this.settings.protocolVersion === 5) {
          let r = this._parseProperties();
          Object.getOwnPropertyNames(r).length && (e.properties = r);
        }
        if (e.length <= 0) return this._emitError(new Error("Malformed unsubscribe, no payload specified"));
        for (; this._pos < e.length; ) {
          let r = this._parseString();
          if (r === null) return this._emitError(new Error("Cannot parse topic"));
          ae("_parseUnsubscribe: push topic `%s` to unsubscriptions", r), e.unsubscriptions.push(r);
        }
      }
    }
    _parseUnsuback() {
      ae("_parseUnsuback");
      let e = this.packet;
      if (!this._parseMessageId()) return this._emitError(new Error("Cannot parse messageId"));
      if ((this.settings.protocolVersion === 3 || this.settings.protocolVersion === 4) && e.length !== 2) return this._emitError(new Error("Malformed unsuback, payload length must be 2"));
      if (e.length <= 0) return this._emitError(new Error("Malformed unsuback, no payload specified"));
      if (this.settings.protocolVersion === 5) {
        let r = this._parseProperties();
        for (Object.getOwnPropertyNames(r).length && (e.properties = r), e.granted = []; this._pos < this.packet.length; ) {
          let n = this._list.readUInt8(this._pos++);
          if (!de.MQTT5_UNSUBACK_CODES[n]) return this._emitError(new Error("Invalid unsuback code"));
          this.packet.granted.push(n);
        }
      }
    }
    _parseConfirmation() {
      ae("_parseConfirmation: packet.cmd: `%s`", this.packet.cmd);
      let e = this.packet;
      if (this._parseMessageId(), this.settings.protocolVersion === 5) {
        if (e.length > 2) {
          switch (e.reasonCode = this._parseByte(), this.packet.cmd) {
            case "puback":
            case "pubrec":
              if (!de.MQTT5_PUBACK_PUBREC_CODES[e.reasonCode]) return this._emitError(new Error("Invalid " + this.packet.cmd + " reason code"));
              break;
            case "pubrel":
            case "pubcomp":
              if (!de.MQTT5_PUBREL_PUBCOMP_CODES[e.reasonCode]) return this._emitError(new Error("Invalid " + this.packet.cmd + " reason code"));
              break;
          }
          ae("_parseConfirmation: packet.reasonCode `%d`", e.reasonCode);
        } else e.reasonCode = 0;
        if (e.length > 3) {
          let r = this._parseProperties();
          Object.getOwnPropertyNames(r).length && (e.properties = r);
        }
      }
      return true;
    }
    _parseDisconnect() {
      let e = this.packet;
      if (ae("_parseDisconnect"), this.settings.protocolVersion === 5) {
        this._list.length > 0 ? (e.reasonCode = this._parseByte(), de.MQTT5_DISCONNECT_CODES[e.reasonCode] || this._emitError(new Error("Invalid disconnect reason code"))) : e.reasonCode = 0;
        let r = this._parseProperties();
        Object.getOwnPropertyNames(r).length && (e.properties = r);
      }
      return ae("_parseDisconnect result: true"), true;
    }
    _parseAuth() {
      ae("_parseAuth");
      let e = this.packet;
      if (this.settings.protocolVersion !== 5) return this._emitError(new Error("Not supported auth packet for this version MQTT"));
      if (e.reasonCode = this._parseByte(), !de.MQTT5_AUTH_CODES[e.reasonCode]) return this._emitError(new Error("Invalid auth reason code"));
      let r = this._parseProperties();
      return Object.getOwnPropertyNames(r).length && (e.properties = r), ae("_parseAuth: result: true"), true;
    }
    _parseMessageId() {
      let e = this.packet;
      return e.messageId = this._parseNum(), e.messageId === null ? (this._emitError(new Error("Cannot parse messageId")), false) : (ae("_parseMessageId: packet.messageId %d", e.messageId), true);
    }
    _parseString(e) {
      let r = this._parseNum(), n = r + this._pos;
      if (r === -1 || n > this._list.length || n > this.packet.length) return null;
      let i = this._list.toString("utf8", this._pos, n);
      return this._pos += r, ae("_parseString: result: %s", i), i;
    }
    _parseStringPair() {
      return ae("_parseStringPair"), { name: this._parseString(), value: this._parseString() };
    }
    _parseBuffer() {
      let e = this._parseNum(), r = e + this._pos;
      if (e === -1 || r > this._list.length || r > this.packet.length) return null;
      let n = this._list.slice(this._pos, r);
      return this._pos += e, ae("_parseBuffer: result: %o", n), n;
    }
    _parseNum() {
      if (this._list.length - this._pos < 2) return -1;
      let e = this._list.readUInt16BE(this._pos);
      return this._pos += 2, ae("_parseNum: result: %s", e), e;
    }
    _parse4ByteNum() {
      if (this._list.length - this._pos < 4) return -1;
      let e = this._list.readUInt32BE(this._pos);
      return this._pos += 4, ae("_parse4ByteNum: result: %s", e), e;
    }
    _parseVarByteNum(e) {
      ae("_parseVarByteNum");
      let r = 4, n = 0, i = 1, s = 0, o = false, a, c = this._pos ? this._pos : 0;
      for (; n < r && c + n < this._list.length; ) {
        if (a = this._list.readUInt8(c + n++), s += i * (a & de.VARBYTEINT_MASK), i *= 128, (a & de.VARBYTEINT_FIN_MASK) === 0) {
          o = true;
          break;
        }
        if (this._list.length <= n) break;
      }
      return !o && n === r && this._list.length >= n && this._emitError(new Error("Invalid variable byte integer")), c && (this._pos += n), o ? e ? o = { bytes: n, value: s } : o = s : o = false, ae("_parseVarByteNum: result: %o", o), o;
    }
    _parseByte() {
      let e;
      return this._pos < this._list.length && (e = this._list.readUInt8(this._pos), this._pos++), ae("_parseByte: result: %o", e), e;
    }
    _parseByType(e) {
      switch (ae("_parseByType: type: %s", e), e) {
        case "byte":
          return this._parseByte() !== 0;
        case "int8":
          return this._parseByte();
        case "int16":
          return this._parseNum();
        case "int32":
          return this._parse4ByteNum();
        case "var":
          return this._parseVarByteNum();
        case "string":
          return this._parseString();
        case "pair":
          return this._parseStringPair();
        case "binary":
          return this._parseBuffer();
      }
    }
    _parseProperties() {
      ae("_parseProperties");
      let e = this._parseVarByteNum(), n = this._pos + e, i = {};
      for (; this._pos < n; ) {
        let s = this._parseByte();
        if (!s) return this._emitError(new Error("Cannot parse property code type")), false;
        let o = de.propertiesCodes[s];
        if (!o) return this._emitError(new Error("Unknown property")), false;
        if (o === "userProperties") {
          i[o] || (i[o] = /* @__PURE__ */ Object.create(null));
          let a = this._parseByType(de.propertiesTypes[o]);
          if (i[o][a.name]) if (Array.isArray(i[o][a.name])) i[o][a.name].push(a.value);
          else {
            let c = i[o][a.name];
            i[o][a.name] = [c], i[o][a.name].push(a.value);
          }
          else i[o][a.name] = a.value;
          continue;
        }
        i[o] ? Array.isArray(i[o]) ? i[o].push(this._parseByType(de.propertiesTypes[o])) : (i[o] = [i[o]], i[o].push(this._parseByType(de.propertiesTypes[o]))) : i[o] = this._parseByType(de.propertiesTypes[o]);
      }
      return i;
    }
    _newPacket() {
      return ae("_newPacket"), this.packet && (this._list.consume(this.packet.length), ae("_newPacket: parser emit packet: packet.cmd: %s, packet.payload: %s, packet.length: %d", this.packet.cmd, this.packet.payload, this.packet.length), this.emit("packet", this.packet)), ae("_newPacket: new packet"), this.packet = new Dp(), this._pos = 0, true;
    }
    _emitError(e) {
      ae("_emitError", e), this.error = e, this.emit("error", e);
    }
  };
  jp.exports = Oa;
});
var Vp = D((H2, Hp) => {
  A();
  x();
  I();
  var { Buffer: Hn } = (ke(), me(qe)), KS = 65536, Wp = {}, QS = Hn.isBuffer(Hn.from([1, 2]).subarray(0, 1));
  function $p(t) {
    let e = Hn.allocUnsafe(2);
    return e.writeUInt8(t >> 8, 0), e.writeUInt8(t & 255, 1), e;
  }
  function YS() {
    for (let t = 0; t < KS; t++) Wp[t] = $p(t);
  }
  function JS(t) {
    let r = 0, n = 0, i = Hn.allocUnsafe(4);
    do
      r = t % 128 | 0, t = t / 128 | 0, t > 0 && (r = r | 128), i.writeUInt8(r, n++);
    while (t > 0 && n < 4);
    return t > 0 && (n = 0), QS ? i.subarray(0, n) : i.slice(0, n);
  }
  function XS(t) {
    let e = Hn.allocUnsafe(4);
    return e.writeUInt32BE(t, 0), e;
  }
  Hp.exports = { cache: Wp, generateCache: YS, generateNumber: $p, genBufVariableByteInt: JS, generate4ByteBuffer: XS };
});
var Gp = D((eN, Ra) => {
  A();
  x();
  I();
  typeof M > "u" || !M.version || M.version.indexOf("v0.") === 0 || M.version.indexOf("v1.") === 0 && M.version.indexOf("v1.8.") !== 0 ? Ra.exports = { nextTick: ZS } : Ra.exports = M;
  function ZS(t, e, r, n) {
    if (typeof t != "function") throw new TypeError('"callback" argument must be a function');
    var i = arguments.length, s, o;
    switch (i) {
      case 0:
      case 1:
        return M.nextTick(t);
      case 2:
        return M.nextTick(function() {
          t.call(null, e);
        });
      case 3:
        return M.nextTick(function() {
          t.call(null, e, r);
        });
      case 4:
        return M.nextTick(function() {
          t.call(null, e, r, n);
        });
      default:
        for (s = new Array(i - 1), o = 0; o < s.length; ) s[o++] = arguments[o];
        return M.nextTick(function() {
          t.apply(null, s);
        });
    }
  }
});
var Ba = D((cN, ey) => {
  A();
  x();
  I();
  var ue = Pa(), { Buffer: se } = (ke(), me(qe)), eE = se.allocUnsafe(0), tE = se.from([0]), Vn = Vp(), rE = Gp().nextTick, rt = At()("mqtt-packet:writeToStream"), yo = Vn.cache, nE = Vn.generateNumber, iE = Vn.generateCache, ka = Vn.genBufVariableByteInt, oE = Vn.generate4ByteBuffer, He = Ca, go = true;
  function Xp(t, e, r) {
    switch (rt("generate called"), e.cork && (e.cork(), rE(sE, e)), go && (go = false, iE()), rt("generate: packet.cmd: %s", t.cmd), t.cmd) {
      case "connect":
        return aE(t, e);
      case "connack":
        return uE(t, e, r);
      case "publish":
        return lE(t, e, r);
      case "puback":
      case "pubrec":
      case "pubrel":
      case "pubcomp":
        return cE(t, e, r);
      case "subscribe":
        return fE(t, e, r);
      case "suback":
        return hE(t, e, r);
      case "unsubscribe":
        return dE(t, e, r);
      case "unsuback":
        return pE(t, e, r);
      case "pingreq":
      case "pingresp":
        return yE(t, e);
      case "disconnect":
        return gE(t, e, r);
      case "auth":
        return bE(t, e, r);
      default:
        return e.destroy(new Error("Unknown command")), false;
    }
  }
  Object.defineProperty(Xp, "cacheNumbers", { get() {
    return He === Ca;
  }, set(t) {
    t ? ((!yo || Object.keys(yo).length === 0) && (go = true), He = Ca) : (go = false, He = wE);
  } });
  function sE(t) {
    t.uncork();
  }
  function aE(t, e, r) {
    let n = t || {}, i = n.protocolId || "MQTT", s = n.protocolVersion || 4, o = n.will, a = n.clean, c = n.keepalive || 0, f = n.clientId || "", h2 = n.username, d = n.password, w = n.properties;
    a === void 0 && (a = true);
    let y = 0;
    if (typeof i != "string" && !se.isBuffer(i)) return e.destroy(new Error("Invalid protocolId")), false;
    if (y += i.length + 2, s !== 3 && s !== 4 && s !== 5) return e.destroy(new Error("Invalid protocol version")), false;
    if (y += 1, (typeof f == "string" || se.isBuffer(f)) && (f || s >= 4) && (f || a)) y += se.byteLength(f) + 2;
    else {
      if (s < 4) return e.destroy(new Error("clientId must be supplied before 3.1.1")), false;
      if (a * 1 === 0) return e.destroy(new Error("clientId must be given if cleanSession set to 0")), false;
    }
    if (typeof c != "number" || c < 0 || c > 65535 || c % 1 !== 0) return e.destroy(new Error("Invalid keepalive")), false;
    y += 2, y += 1;
    let P, E;
    if (s === 5) {
      if (P = ar(e, w), !P) return false;
      y += P.length;
    }
    if (o) {
      if (typeof o != "object") return e.destroy(new Error("Invalid will")), false;
      if (!o.topic || typeof o.topic != "string") return e.destroy(new Error("Invalid will topic")), false;
      if (y += se.byteLength(o.topic) + 2, y += 2, o.payload) if (o.payload.length >= 0) typeof o.payload == "string" ? y += se.byteLength(o.payload) : y += o.payload.length;
      else return e.destroy(new Error("Invalid will payload")), false;
      if (E = {}, s === 5) {
        if (E = ar(e, o.properties), !E) return false;
        y += E.length;
      }
    }
    let b = false;
    if (h2 != null) if (Jp(h2)) b = true, y += se.byteLength(h2) + 2;
    else return e.destroy(new Error("Invalid username")), false;
    if (d != null) {
      if (!b) return e.destroy(new Error("Username is required to use password")), false;
      if (Jp(d)) y += Zp(d) + 2;
      else return e.destroy(new Error("Invalid password")), false;
    }
    e.write(ue.CONNECT_HEADER), nt(e, y), pn(e, i), n.bridgeMode && (s += 128), e.write(s === 131 ? ue.VERSION131 : s === 132 ? ue.VERSION132 : s === 4 ? ue.VERSION4 : s === 5 ? ue.VERSION5 : ue.VERSION3);
    let m = 0;
    return m |= h2 != null ? ue.USERNAME_MASK : 0, m |= d != null ? ue.PASSWORD_MASK : 0, m |= o && o.retain ? ue.WILL_RETAIN_MASK : 0, m |= o && o.qos ? o.qos << ue.WILL_QOS_SHIFT : 0, m |= o ? ue.WILL_FLAG_MASK : 0, m |= a ? ue.CLEAN_SESSION_MASK : 0, e.write(se.from([m])), He(e, c), s === 5 && P.write(), pn(e, f), o && (s === 5 && E.write(), Br(e, o.topic), pn(e, o.payload)), h2 != null && pn(e, h2), d != null && pn(e, d), true;
  }
  function uE(t, e, r) {
    let n = r ? r.protocolVersion : 4, i = t || {}, s = n === 5 ? i.reasonCode : i.returnCode, o = i.properties, a = 2;
    if (typeof s != "number") return e.destroy(new Error("Invalid return code")), false;
    let c = null;
    if (n === 5) {
      if (c = ar(e, o), !c) return false;
      a += c.length;
    }
    return e.write(ue.CONNACK_HEADER), nt(e, a), e.write(i.sessionPresent ? ue.SESSIONPRESENT_HEADER : tE), e.write(se.from([s])), c?.write(), true;
  }
  function lE(t, e, r) {
    rt("publish: packet: %o", t);
    let n = r ? r.protocolVersion : 4, i = t || {}, s = i.qos || 0, o = i.retain ? ue.RETAIN_MASK : 0, a = i.topic, c = i.payload || eE, f = i.messageId, h2 = i.properties, d = 0;
    if (typeof a == "string") d += se.byteLength(a) + 2;
    else if (se.isBuffer(a)) d += a.length + 2;
    else return e.destroy(new Error("Invalid topic")), false;
    if (se.isBuffer(c) ? d += c.length : d += se.byteLength(c), s && typeof f != "number") return e.destroy(new Error("Invalid messageId")), false;
    s && (d += 2);
    let w = null;
    if (n === 5) {
      if (w = ar(e, h2), !w) return false;
      d += w.length;
    }
    return e.write(ue.PUBLISH_HEADER[s][i.dup ? 1 : 0][o ? 1 : 0]), nt(e, d), He(e, Zp(a)), e.write(a), s > 0 && He(e, f), w?.write(), rt("publish: payload: %o", c), e.write(c);
  }
  function cE(t, e, r) {
    let n = r ? r.protocolVersion : 4, i = t || {}, s = i.cmd || "puback", o = i.messageId, a = i.dup && s === "pubrel" ? ue.DUP_MASK : 0, c = 0, f = i.reasonCode, h2 = i.properties, d = n === 5 ? 3 : 2;
    if (s === "pubrel" && (c = 1), typeof o != "number") return e.destroy(new Error("Invalid messageId")), false;
    let w = null;
    if (n === 5 && typeof h2 == "object") {
      if (w = Gn(e, h2, r, d), !w) return false;
      d += w.length;
    }
    return e.write(ue.ACKS[s][c][a][0]), d === 3 && (d += f !== 0 ? 1 : -1), nt(e, d), He(e, o), n === 5 && d !== 2 && e.write(se.from([f])), w !== null ? w.write() : d === 4 && e.write(se.from([0])), true;
  }
  function fE(t, e, r) {
    rt("subscribe: packet: ");
    let n = r ? r.protocolVersion : 4, i = t || {}, s = i.dup ? ue.DUP_MASK : 0, o = i.messageId, a = i.subscriptions, c = i.properties, f = 0;
    if (typeof o != "number") return e.destroy(new Error("Invalid messageId")), false;
    f += 2;
    let h2 = null;
    if (n === 5) {
      if (h2 = ar(e, c), !h2) return false;
      f += h2.length;
    }
    if (typeof a == "object" && a.length) for (let w = 0; w < a.length; w += 1) {
      let y = a[w].topic, P = a[w].qos;
      if (typeof y != "string") return e.destroy(new Error("Invalid subscriptions - invalid topic")), false;
      if (typeof P != "number") return e.destroy(new Error("Invalid subscriptions - invalid qos")), false;
      if (n === 5) {
        if (typeof (a[w].nl || false) != "boolean") return e.destroy(new Error("Invalid subscriptions - invalid No Local")), false;
        if (typeof (a[w].rap || false) != "boolean") return e.destroy(new Error("Invalid subscriptions - invalid Retain as Published")), false;
        let m = a[w].rh || 0;
        if (typeof m != "number" || m > 2) return e.destroy(new Error("Invalid subscriptions - invalid Retain Handling")), false;
      }
      f += se.byteLength(y) + 2 + 1;
    }
    else return e.destroy(new Error("Invalid subscriptions")), false;
    rt("subscribe: writing to stream: %o", ue.SUBSCRIBE_HEADER), e.write(ue.SUBSCRIBE_HEADER[1][s ? 1 : 0][0]), nt(e, f), He(e, o), h2 !== null && h2.write();
    let d = true;
    for (let w of a) {
      let y = w.topic, P = w.qos, E = +w.nl, b = +w.rap, m = w.rh, v;
      Br(e, y), v = ue.SUBSCRIBE_OPTIONS_QOS[P], n === 5 && (v |= E ? ue.SUBSCRIBE_OPTIONS_NL : 0, v |= b ? ue.SUBSCRIBE_OPTIONS_RAP : 0, v |= m ? ue.SUBSCRIBE_OPTIONS_RH[m] : 0), d = e.write(se.from([v]));
    }
    return d;
  }
  function hE(t, e, r) {
    let n = r ? r.protocolVersion : 4, i = t || {}, s = i.messageId, o = i.granted, a = i.properties, c = 0;
    if (typeof s != "number") return e.destroy(new Error("Invalid messageId")), false;
    if (c += 2, typeof o == "object" && o.length) for (let h2 = 0; h2 < o.length; h2 += 1) {
      if (typeof o[h2] != "number") return e.destroy(new Error("Invalid qos vector")), false;
      c += 1;
    }
    else return e.destroy(new Error("Invalid qos vector")), false;
    let f = null;
    if (n === 5) {
      if (f = Gn(e, a, r, c), !f) return false;
      c += f.length;
    }
    return e.write(ue.SUBACK_HEADER), nt(e, c), He(e, s), f !== null && f.write(), e.write(se.from(o));
  }
  function dE(t, e, r) {
    let n = r ? r.protocolVersion : 4, i = t || {}, s = i.messageId, o = i.dup ? ue.DUP_MASK : 0, a = i.unsubscriptions, c = i.properties, f = 0;
    if (typeof s != "number") return e.destroy(new Error("Invalid messageId")), false;
    if (f += 2, typeof a == "object" && a.length) for (let w = 0; w < a.length; w += 1) {
      if (typeof a[w] != "string") return e.destroy(new Error("Invalid unsubscriptions")), false;
      f += se.byteLength(a[w]) + 2;
    }
    else return e.destroy(new Error("Invalid unsubscriptions")), false;
    let h2 = null;
    if (n === 5) {
      if (h2 = ar(e, c), !h2) return false;
      f += h2.length;
    }
    e.write(ue.UNSUBSCRIBE_HEADER[1][o ? 1 : 0][0]), nt(e, f), He(e, s), h2 !== null && h2.write();
    let d = true;
    for (let w = 0; w < a.length; w++) d = Br(e, a[w]);
    return d;
  }
  function pE(t, e, r) {
    let n = r ? r.protocolVersion : 4, i = t || {}, s = i.messageId, o = i.dup ? ue.DUP_MASK : 0, a = i.granted, c = i.properties, f = i.cmd, h2 = 0, d = 2;
    if (typeof s != "number") return e.destroy(new Error("Invalid messageId")), false;
    if (n === 5) if (typeof a == "object" && a.length) for (let y = 0; y < a.length; y += 1) {
      if (typeof a[y] != "number") return e.destroy(new Error("Invalid qos vector")), false;
      d += 1;
    }
    else return e.destroy(new Error("Invalid qos vector")), false;
    let w = null;
    if (n === 5) {
      if (w = Gn(e, c, r, d), !w) return false;
      d += w.length;
    }
    return e.write(ue.ACKS[f][h2][o][0]), nt(e, d), He(e, s), w !== null && w.write(), n === 5 && e.write(se.from(a)), true;
  }
  function yE(t, e, r) {
    return e.write(ue.EMPTY[t.cmd]);
  }
  function gE(t, e, r) {
    let n = r ? r.protocolVersion : 4, i = t || {}, s = i.reasonCode, o = i.properties, a = n === 5 ? 1 : 0, c = null;
    if (n === 5) {
      if (c = Gn(e, o, r, a), !c) return false;
      a += c.length;
    }
    return e.write(se.from([ue.codes.disconnect << 4])), nt(e, a), n === 5 && e.write(se.from([s])), c !== null && c.write(), true;
  }
  function bE(t, e, r) {
    let n = r ? r.protocolVersion : 4, i = t || {}, s = i.reasonCode, o = i.properties, a = n === 5 ? 1 : 0;
    n !== 5 && e.destroy(new Error("Invalid mqtt version for auth packet"));
    let c = Gn(e, o, r, a);
    return c ? (a += c.length, e.write(se.from([ue.codes.auth << 4])), nt(e, a), e.write(se.from([s])), c !== null && c.write(), true) : false;
  }
  var zp = {};
  function nt(t, e) {
    if (e > ue.VARBYTEINT_MAX) return t.destroy(new Error(`Invalid variable byte integer: ${e}`)), false;
    let r = zp[e];
    return r || (r = ka(e), e < 16384 && (zp[e] = r)), rt("writeVarByteInt: writing to stream: %o", r), t.write(r);
  }
  function Br(t, e) {
    let r = se.byteLength(e);
    return He(t, r), rt("writeString: %s", e), t.write(e, "utf8");
  }
  function Kp(t, e, r) {
    Br(t, e), Br(t, r);
  }
  function Ca(t, e) {
    return rt("writeNumberCached: number: %d", e), rt("writeNumberCached: %o", yo[e]), t.write(yo[e]);
  }
  function wE(t, e) {
    let r = nE(e);
    return rt("writeNumberGenerated: %o", r), t.write(r);
  }
  function mE(t, e) {
    let r = oE(e);
    return rt("write4ByteNumber: %o", r), t.write(r);
  }
  function pn(t, e) {
    typeof e == "string" ? Br(t, e) : e ? (He(t, e.length), t.write(e)) : He(t, 0);
  }
  function ar(t, e) {
    if (typeof e != "object" || e.length != null) return { length: 1, write() {
      Yp(t, {}, 0);
    } };
    let r = 0;
    function n(s, o) {
      let a = ue.propertiesTypes[s], c = 0;
      switch (a) {
        case "byte": {
          if (typeof o != "boolean") return t.destroy(new Error(`Invalid ${s}: ${o}`)), false;
          c += 2;
          break;
        }
        case "int8": {
          if (typeof o != "number" || o < 0 || o > 255) return t.destroy(new Error(`Invalid ${s}: ${o}`)), false;
          c += 2;
          break;
        }
        case "binary": {
          if (o && o === null) return t.destroy(new Error(`Invalid ${s}: ${o}`)), false;
          c += 1 + se.byteLength(o) + 2;
          break;
        }
        case "int16": {
          if (typeof o != "number" || o < 0 || o > 65535) return t.destroy(new Error(`Invalid ${s}: ${o}`)), false;
          c += 3;
          break;
        }
        case "int32": {
          if (typeof o != "number" || o < 0 || o > 4294967295) return t.destroy(new Error(`Invalid ${s}: ${o}`)), false;
          c += 5;
          break;
        }
        case "var": {
          if (typeof o != "number" || o < 0 || o > 268435455) return t.destroy(new Error(`Invalid ${s}: ${o}`)), false;
          c += 1 + se.byteLength(ka(o));
          break;
        }
        case "string": {
          if (typeof o != "string") return t.destroy(new Error(`Invalid ${s}: ${o}`)), false;
          c += 3 + se.byteLength(o.toString());
          break;
        }
        case "pair": {
          if (typeof o != "object") return t.destroy(new Error(`Invalid ${s}: ${o}`)), false;
          c += Object.getOwnPropertyNames(o).reduce((f, h2) => {
            let d = o[h2];
            return Array.isArray(d) ? f += d.reduce((w, y) => (w += 3 + se.byteLength(h2.toString()) + 2 + se.byteLength(y.toString()), w), 0) : f += 3 + se.byteLength(h2.toString()) + 2 + se.byteLength(o[h2].toString()), f;
          }, 0);
          break;
        }
        default:
          return t.destroy(new Error(`Invalid property ${s}: ${o}`)), false;
      }
      return c;
    }
    if (e) for (let s in e) {
      let o = 0, a = 0, c = e[s];
      if (c !== void 0) {
        if (Array.isArray(c)) for (let f = 0; f < c.length; f++) {
          if (a = n(s, c[f]), !a) return false;
          o += a;
        }
        else {
          if (a = n(s, c), !a) return false;
          o = a;
        }
        if (!o) return false;
        r += o;
      }
    }
    return { length: se.byteLength(ka(r)) + r, write() {
      Yp(t, e, r);
    } };
  }
  function Gn(t, e, r, n) {
    let i = ["reasonString", "userProperties"], s = r && r.properties && r.properties.maximumPacketSize ? r.properties.maximumPacketSize : 0, o = ar(t, e);
    if (s) for (; n + o.length > s; ) {
      let a = i.shift();
      if (a && e[a]) delete e[a], o = ar(t, e);
      else return false;
    }
    return o;
  }
  function Qp(t, e, r) {
    switch (ue.propertiesTypes[e]) {
      case "byte": {
        t.write(se.from([ue.properties[e]])), t.write(se.from([+r]));
        break;
      }
      case "int8": {
        t.write(se.from([ue.properties[e]])), t.write(se.from([r]));
        break;
      }
      case "binary": {
        t.write(se.from([ue.properties[e]])), pn(t, r);
        break;
      }
      case "int16": {
        t.write(se.from([ue.properties[e]])), He(t, r);
        break;
      }
      case "int32": {
        t.write(se.from([ue.properties[e]])), mE(t, r);
        break;
      }
      case "var": {
        t.write(se.from([ue.properties[e]])), nt(t, r);
        break;
      }
      case "string": {
        t.write(se.from([ue.properties[e]])), Br(t, r);
        break;
      }
      case "pair": {
        Object.getOwnPropertyNames(r).forEach((i) => {
          let s = r[i];
          Array.isArray(s) ? s.forEach((o) => {
            t.write(se.from([ue.properties[e]])), Kp(t, i.toString(), o.toString());
          }) : (t.write(se.from([ue.properties[e]])), Kp(t, i.toString(), s.toString()));
        });
        break;
      }
      default:
        return t.destroy(new Error(`Invalid property ${e} value: ${r}`)), false;
    }
  }
  function Yp(t, e, r) {
    nt(t, r);
    for (let n in e) if (Object.prototype.hasOwnProperty.call(e, n) && e[n] != null) {
      let i = e[n];
      if (Array.isArray(i)) for (let s = 0; s < i.length; s++) Qp(t, n, i[s]);
      else Qp(t, n, i);
    }
  }
  function Zp(t) {
    return t ? t instanceof se ? t.length : se.byteLength(t) : 0;
  }
  function Jp(t) {
    return typeof t == "string" || t instanceof se;
  }
  ey.exports = Xp;
});
var ny = D((_N, ry) => {
  A();
  x();
  I();
  var _E = Ba(), { EventEmitter: vE } = (er(), me(Zt)), { Buffer: ty } = (ke(), me(qe));
  function SE(t, e) {
    let r = new Ma();
    return _E(t, r, e), r.concat();
  }
  var Ma = class extends vE {
    constructor() {
      super(), this._array = new Array(20), this._i = 0;
    }
    write(e) {
      return this._array[this._i++] = e, true;
    }
    concat() {
      let e = 0, r = new Array(this._array.length), n = this._array, i = 0, s;
      for (s = 0; s < n.length && n[s] !== void 0; s++) typeof n[s] != "string" ? r[s] = n[s].length : r[s] = ty.byteLength(n[s]), e += r[s];
      let o = ty.allocUnsafe(e);
      for (s = 0; s < n.length && n[s] !== void 0; s++) typeof n[s] != "string" ? (n[s].copy(o, i), i += r[s]) : (o.write(n[s], i), i += r[s]);
      return o;
    }
    destroy(e) {
      e && this.emit("error", e);
    }
  };
  ry.exports = SE;
});
var iy = D((bo) => {
  A();
  x();
  I();
  bo.parser = Fp().parser;
  bo.generate = ny();
  bo.writeToStream = Ba();
});
var sy = D((jN, oy) => {
  A();
  x();
  I();
  oy.exports = EE;
  function yn(t) {
    return t instanceof U ? U.from(t) : new t.constructor(t.buffer.slice(), t.byteOffset, t.length);
  }
  function EE(t) {
    if (t = t || {}, t.circles) return AE(t);
    let e = /* @__PURE__ */ new Map();
    if (e.set(Date, (o) => new Date(o)), e.set(Map, (o, a) => new Map(n(Array.from(o), a))), e.set(Set, (o, a) => new Set(n(Array.from(o), a))), t.constructorHandlers) for (let o of t.constructorHandlers) e.set(o[0], o[1]);
    let r = null;
    return t.proto ? s : i;
    function n(o, a) {
      let c = Object.keys(o), f = new Array(c.length);
      for (let h2 = 0; h2 < c.length; h2++) {
        let d = c[h2], w = o[d];
        typeof w != "object" || w === null ? f[d] = w : w.constructor !== Object && (r = e.get(w.constructor)) ? f[d] = r(w, a) : ArrayBuffer.isView(w) ? f[d] = yn(w) : f[d] = a(w);
      }
      return f;
    }
    function i(o) {
      if (typeof o != "object" || o === null) return o;
      if (Array.isArray(o)) return n(o, i);
      if (o.constructor !== Object && (r = e.get(o.constructor))) return r(o, i);
      let a = {};
      for (let c in o) {
        if (Object.hasOwnProperty.call(o, c) === false) continue;
        let f = o[c];
        typeof f != "object" || f === null ? a[c] = f : f.constructor !== Object && (r = e.get(f.constructor)) ? a[c] = r(f, i) : ArrayBuffer.isView(f) ? a[c] = yn(f) : a[c] = i(f);
      }
      return a;
    }
    function s(o) {
      if (typeof o != "object" || o === null) return o;
      if (Array.isArray(o)) return n(o, s);
      if (o.constructor !== Object && (r = e.get(o.constructor))) return r(o, s);
      let a = {};
      for (let c in o) {
        let f = o[c];
        typeof f != "object" || f === null ? a[c] = f : f.constructor !== Object && (r = e.get(f.constructor)) ? a[c] = r(f, s) : ArrayBuffer.isView(f) ? a[c] = yn(f) : a[c] = s(f);
      }
      return a;
    }
  }
  function AE(t) {
    let e = [], r = [], n = /* @__PURE__ */ new Map();
    if (n.set(Date, (c) => new Date(c)), n.set(Map, (c, f) => new Map(s(Array.from(c), f))), n.set(Set, (c, f) => new Set(s(Array.from(c), f))), t.constructorHandlers) for (let c of t.constructorHandlers) n.set(c[0], c[1]);
    let i = null;
    return t.proto ? a : o;
    function s(c, f) {
      let h2 = Object.keys(c), d = new Array(h2.length);
      for (let w = 0; w < h2.length; w++) {
        let y = h2[w], P = c[y];
        if (typeof P != "object" || P === null) d[y] = P;
        else if (P.constructor !== Object && (i = n.get(P.constructor))) d[y] = i(P, f);
        else if (ArrayBuffer.isView(P)) d[y] = yn(P);
        else {
          let E = e.indexOf(P);
          E !== -1 ? d[y] = r[E] : d[y] = f(P);
        }
      }
      return d;
    }
    function o(c) {
      if (typeof c != "object" || c === null) return c;
      if (Array.isArray(c)) return s(c, o);
      if (c.constructor !== Object && (i = n.get(c.constructor))) return i(c, o);
      let f = {};
      e.push(c), r.push(f);
      for (let h2 in c) {
        if (Object.hasOwnProperty.call(c, h2) === false) continue;
        let d = c[h2];
        if (typeof d != "object" || d === null) f[h2] = d;
        else if (d.constructor !== Object && (i = n.get(d.constructor))) f[h2] = i(d, o);
        else if (ArrayBuffer.isView(d)) f[h2] = yn(d);
        else {
          let w = e.indexOf(d);
          w !== -1 ? f[h2] = r[w] : f[h2] = o(d);
        }
      }
      return e.pop(), r.pop(), f;
    }
    function a(c) {
      if (typeof c != "object" || c === null) return c;
      if (Array.isArray(c)) return s(c, a);
      if (c.constructor !== Object && (i = n.get(c.constructor))) return i(c, a);
      let f = {};
      e.push(c), r.push(f);
      for (let h2 in c) {
        let d = c[h2];
        if (typeof d != "object" || d === null) f[h2] = d;
        else if (d.constructor !== Object && (i = n.get(d.constructor))) f[h2] = i(d, a);
        else if (ArrayBuffer.isView(d)) f[h2] = yn(d);
        else {
          let w = e.indexOf(d);
          w !== -1 ? f[h2] = r[w] : f[h2] = a(d);
        }
      }
      return e.pop(), r.pop(), f;
    }
  }
});
var uy = D((YN, ay) => {
  A();
  x();
  I();
  ay.exports = sy()();
});
var cy = D((wo) => {
  A();
  x();
  I();
  Object.defineProperty(wo, "__esModule", { value: true });
  wo.validateTopic = ly;
  wo.validateTopics = IE;
  function ly(t) {
    let e = t.split("/");
    for (let r = 0; r < e.length; r++) if (e[r] !== "+") {
      if (e[r] === "#") return r === e.length - 1;
      if (e[r].indexOf("+") !== -1 || e[r].indexOf("#") !== -1) return false;
    }
    return true;
  }
  function IE(t) {
    if (t.length === 0) return "empty_topic_list";
    for (let e = 0; e < t.length; e++) if (!ly(t[e])) return t[e];
    return null;
  }
});
var La = D((qa) => {
  A();
  x();
  I();
  Object.defineProperty(qa, "__esModule", { value: true });
  var xE = or(), TE = { objectMode: true }, PE = { clean: true }, Na = class {
    options;
    _inflights;
    constructor(e) {
      this.options = e || {}, this.options = { ...PE, ...e }, this._inflights = /* @__PURE__ */ new Map();
    }
    put(e, r) {
      return this._inflights.set(e.messageId, e), r && r(), this;
    }
    createStream() {
      let e = new xE.Readable(TE), r = [], n = false, i = 0;
      return this._inflights.forEach((s, o) => {
        r.push(s);
      }), e._read = () => {
        !n && i < r.length ? e.push(r[i++]) : e.push(null);
      }, e.destroy = (s) => {
        if (!n) return n = true, setTimeout(() => {
          e.emit("close");
        }, 0), e;
      }, e;
    }
    del(e, r) {
      let n = this._inflights.get(e.messageId);
      return n ? (this._inflights.delete(e.messageId), r(null, n)) : r && r(new Error("missing packet")), this;
    }
    get(e, r) {
      let n = this._inflights.get(e.messageId);
      return n ? r(null, n) : r && r(new Error("missing packet")), this;
    }
    close(e) {
      this.options.clean && (this._inflights = null), e && e();
    }
  };
  qa.default = Na;
});
var hy = D((Ua) => {
  A();
  x();
  I();
  Object.defineProperty(Ua, "__esModule", { value: true });
  var fy = [0, 16, 128, 131, 135, 144, 145, 151, 153], OE = (t, e, r) => {
    t.log("handlePublish: packet %o", e), r = typeof r < "u" ? r : t.noop;
    let n = e.topic.toString(), i = e.payload, { qos: s } = e, { messageId: o } = e, { options: a } = t;
    if (t.options.protocolVersion === 5) {
      let c;
      if (e.properties && (c = e.properties.topicAlias), typeof c < "u") if (n.length === 0) if (c > 0 && c <= 65535) {
        let f = t.topicAliasRecv.getTopicByAlias(c);
        if (f) n = f, t.log("handlePublish :: topic complemented by alias. topic: %s - alias: %d", n, c);
        else {
          t.log("handlePublish :: unregistered topic alias. alias: %d", c), t.emit("error", new Error("Received unregistered Topic Alias"));
          return;
        }
      } else {
        t.log("handlePublish :: topic alias out of range. alias: %d", c), t.emit("error", new Error("Received Topic Alias is out of range"));
        return;
      }
      else if (t.topicAliasRecv.put(n, c)) t.log("handlePublish :: registered topic: %s - alias: %d", n, c);
      else {
        t.log("handlePublish :: topic alias out of range. alias: %d", c), t.emit("error", new Error("Received Topic Alias is out of range"));
        return;
      }
    }
    switch (t.log("handlePublish: qos %d", s), s) {
      case 2: {
        a.customHandleAcks(n, i, e, (c, f) => {
          if (typeof c == "number" && (f = c, c = null), c) return t.emit("error", c);
          if (fy.indexOf(f) === -1) return t.emit("error", new Error("Wrong reason code for pubrec"));
          f ? t._sendPacket({ cmd: "pubrec", messageId: o, reasonCode: f }, r) : t.incomingStore.put(e, () => {
            t._sendPacket({ cmd: "pubrec", messageId: o }, r);
          });
        });
        break;
      }
      case 1: {
        a.customHandleAcks(n, i, e, (c, f) => {
          if (typeof c == "number" && (f = c, c = null), c) return t.emit("error", c);
          if (fy.indexOf(f) === -1) return t.emit("error", new Error("Wrong reason code for puback"));
          f || t.emit("message", n, i, e), t.handleMessage(e, (h2) => {
            if (h2) return r && r(h2);
            t._sendPacket({ cmd: "puback", messageId: o, reasonCode: f }, r);
          });
        });
        break;
      }
      case 0:
        t.emit("message", n, i, e), t.handleMessage(e, r);
        break;
      default:
        t.log("handlePublish: unknown QoS. Doing nothing.");
        break;
    }
  };
  Ua.default = OE;
});
var dy = D((qq, RE) => {
  RE.exports = { version: "5.14.1" };
});
var Mr = D((ht) => {
  A();
  x();
  I();
  Object.defineProperty(ht, "__esModule", { value: true });
  ht.MQTTJS_VERSION = ht.nextTick = ht.ErrorWithSubackPacket = ht.ErrorWithReasonCode = void 0;
  ht.applyMixin = kE;
  var Da = class t extends Error {
    code;
    constructor(e, r) {
      super(e), this.code = r, Object.setPrototypeOf(this, t.prototype), Object.getPrototypeOf(this).name = "ErrorWithReasonCode";
    }
  };
  ht.ErrorWithReasonCode = Da;
  var ja = class t extends Error {
    packet;
    constructor(e, r) {
      super(e), this.packet = r, Object.setPrototypeOf(this, t.prototype), Object.getPrototypeOf(this).name = "ErrorWithSubackPacket";
    }
  };
  ht.ErrorWithSubackPacket = ja;
  function kE(t, e, r = false) {
    let n = [e];
    for (; ; ) {
      let i = n[0], s = Object.getPrototypeOf(i);
      if (s?.prototype) n.unshift(s);
      else break;
    }
    for (let i of n) for (let s of Object.getOwnPropertyNames(i.prototype)) (r || s !== "constructor") && Object.defineProperty(t.prototype, s, Object.getOwnPropertyDescriptor(i.prototype, s) ?? /* @__PURE__ */ Object.create(null));
  }
  ht.nextTick = typeof M?.nextTick == "function" ? M.nextTick : (t) => {
    setTimeout(t, 0);
  };
  ht.MQTTJS_VERSION = dy().version;
});
var zn = D((ur) => {
  A();
  x();
  I();
  Object.defineProperty(ur, "__esModule", { value: true });
  ur.ReasonCodes = void 0;
  var py = Mr();
  ur.ReasonCodes = { 0: "", 1: "Unacceptable protocol version", 2: "Identifier rejected", 3: "Server unavailable", 4: "Bad username or password", 5: "Not authorized", 16: "No matching subscribers", 17: "No subscription existed", 128: "Unspecified error", 129: "Malformed Packet", 130: "Protocol Error", 131: "Implementation specific error", 132: "Unsupported Protocol Version", 133: "Client Identifier not valid", 134: "Bad User Name or Password", 135: "Not authorized", 136: "Server unavailable", 137: "Server busy", 138: "Banned", 139: "Server shutting down", 140: "Bad authentication method", 141: "Keep Alive timeout", 142: "Session taken over", 143: "Topic Filter invalid", 144: "Topic Name invalid", 145: "Packet identifier in use", 146: "Packet Identifier not found", 147: "Receive Maximum exceeded", 148: "Topic Alias invalid", 149: "Packet too large", 150: "Message rate too high", 151: "Quota exceeded", 152: "Administrative action", 153: "Payload format invalid", 154: "Retain not supported", 155: "QoS not supported", 156: "Use another server", 157: "Server moved", 158: "Shared Subscriptions not supported", 159: "Connection rate exceeded", 160: "Maximum connect time", 161: "Subscription Identifiers not supported", 162: "Wildcard Subscriptions not supported" };
  var CE = (t, e) => {
    let { messageId: r } = e, n = e.cmd, i = null, s = t.outgoing[r] ? t.outgoing[r].cb : null, o = null;
    if (!s) {
      t.log("_handleAck :: Server sent an ack in error. Ignoring.");
      return;
    }
    switch (t.log("_handleAck :: packet type", n), n) {
      case "pubcomp":
      case "puback": {
        let a = e.reasonCode;
        a && a > 0 && a !== 16 ? (o = new py.ErrorWithReasonCode(`Publish error: ${ur.ReasonCodes[a]}`, a), t._removeOutgoingAndStoreMessage(r, () => {
          s(o, e);
        })) : t._removeOutgoingAndStoreMessage(r, s);
        break;
      }
      case "pubrec": {
        i = { cmd: "pubrel", qos: 2, messageId: r };
        let a = e.reasonCode;
        a && a > 0 && a !== 16 ? (o = new py.ErrorWithReasonCode(`Publish error: ${ur.ReasonCodes[a]}`, a), t._removeOutgoingAndStoreMessage(r, () => {
          s(o, e);
        })) : t._sendPacket(i);
        break;
      }
      case "suback": {
        delete t.outgoing[r], t.messageIdProvider.deallocate(r);
        let a = e.granted;
        for (let c = 0; c < a.length; c++) {
          let f = a[c];
          if ((f & 128) !== 0) {
            o = new Error(`Subscribe error: ${ur.ReasonCodes[f]}`), o.code = f;
            let h2 = t.messageIdToTopic[r];
            h2 && h2.forEach((d) => {
              delete t._resubscribeTopics[d];
            });
          }
        }
        delete t.messageIdToTopic[r], t._invokeStoreProcessingQueue(), s(o, e);
        break;
      }
      case "unsuback": {
        delete t.outgoing[r], t.messageIdProvider.deallocate(r), t._invokeStoreProcessingQueue(), s(null, e);
        break;
      }
      default:
        t.emit("error", new Error("unrecognized packet type"));
    }
    t.disconnecting && Object.keys(t.outgoing).length === 0 && t.emit("outgoingEmpty");
  };
  ur.default = CE;
});
var gy = D((Fa) => {
  A();
  x();
  I();
  Object.defineProperty(Fa, "__esModule", { value: true });
  var yy = Mr(), BE = zn(), ME = (t, e) => {
    let { options: r } = t, n = r.protocolVersion, i = n === 5 ? e.reasonCode : e.returnCode;
    if (n !== 5) {
      let s = new yy.ErrorWithReasonCode(`Protocol error: Auth packets are only supported in MQTT 5. Your version:${n}`, i);
      t.emit("error", s);
      return;
    }
    t.handleAuth(e, (s, o) => {
      if (s) {
        t.emit("error", s);
        return;
      }
      if (i === 24) t.reconnecting = false, t._sendPacket(o);
      else {
        let a = new yy.ErrorWithReasonCode(`Connection refused: ${BE.ReasonCodes[i]}`, i);
        t.emit("error", a);
      }
    });
  };
  Fa.default = ME;
});
var vy = D((_o) => {
  A();
  x();
  I();
  Object.defineProperty(_o, "__esModule", { value: true });
  _o.LRUCache = void 0;
  var gn = typeof performance == "object" && performance && typeof performance.now == "function" ? performance : Date, wy = /* @__PURE__ */ new Set(), Wa = typeof M == "object" && M ? M : {}, my = (t, e, r, n) => {
    typeof Wa.emitWarning == "function" ? Wa.emitWarning(t, e, r, n) : console.error(`[${r}] ${e}: ${t}`);
  }, mo = globalThis.AbortController, by = globalThis.AbortSignal;
  if (typeof mo > "u") {
    by = class {
      onabort;
      _onabort = [];
      reason;
      aborted = false;
      addEventListener(n, i) {
        this._onabort.push(i);
      }
    }, mo = class {
      constructor() {
        e();
      }
      signal = new by();
      abort(n) {
        if (!this.signal.aborted) {
          this.signal.reason = n, this.signal.aborted = true;
          for (let i of this.signal._onabort) i(n);
          this.signal.onabort?.(n);
        }
      }
    };
    let t = Wa.env?.LRU_CACHE_IGNORE_AC_WARNING !== "1", e = () => {
      t && (t = false, my("AbortController is not defined. If using lru-cache in node 14, load an AbortController polyfill from the `node-abort-controller` package. A minimal polyfill is provided for use by LRUCache.fetch(), but it should not be relied upon in other contexts (eg, passing it to other APIs that use AbortController/AbortSignal might have undesirable effects). You may disable this with LRU_CACHE_IGNORE_AC_WARNING=1 in the env.", "NO_ABORT_CONTROLLER", "ENOTSUP", e));
    };
  }
  var NE = (t) => !wy.has(t), lr = (t) => t && t === Math.floor(t) && t > 0 && isFinite(t), _y = (t) => lr(t) ? t <= Math.pow(2, 8) ? Uint8Array : t <= Math.pow(2, 16) ? Uint16Array : t <= Math.pow(2, 32) ? Uint32Array : t <= Number.MAX_SAFE_INTEGER ? bn : null : null, bn = class extends Array {
    constructor(e) {
      super(e), this.fill(0);
    }
  }, $a = class t {
    heap;
    length;
    static #u = false;
    static create(e) {
      let r = _y(e);
      if (!r) return [];
      t.#u = true;
      let n = new t(e, r);
      return t.#u = false, n;
    }
    constructor(e, r) {
      if (!t.#u) throw new TypeError("instantiate Stack using Stack.create(n)");
      this.heap = new r(e), this.length = 0;
    }
    push(e) {
      this.heap[this.length++] = e;
    }
    pop() {
      return this.heap[--this.length];
    }
  }, Ha = class t {
    #u;
    #f;
    #y;
    #g;
    #R;
    #k;
    ttl;
    ttlResolution;
    ttlAutopurge;
    updateAgeOnGet;
    updateAgeOnHas;
    allowStale;
    noDisposeOnSet;
    noUpdateTTL;
    maxEntrySize;
    sizeCalculation;
    noDeleteOnFetchRejection;
    noDeleteOnStaleGet;
    allowStaleOnFetchAbort;
    allowStaleOnFetchRejection;
    ignoreFetchAbort;
    #i;
    #b;
    #n;
    #r;
    #e;
    #l;
    #h;
    #a;
    #o;
    #w;
    #s;
    #m;
    #_;
    #d;
    #v;
    #x;
    #c;
    static unsafeExposeInternals(e) {
      return { starts: e.#_, ttls: e.#d, sizes: e.#m, keyMap: e.#n, keyList: e.#r, valList: e.#e, next: e.#l, prev: e.#h, get head() {
        return e.#a;
      }, get tail() {
        return e.#o;
      }, free: e.#w, isBackgroundFetch: (r) => e.#t(r), backgroundFetch: (r, n, i, s) => e.#M(r, n, i, s), moveToTail: (r) => e.#O(r), indexes: (r) => e.#S(r), rindexes: (r) => e.#E(r), isStale: (r) => e.#p(r) };
    }
    get max() {
      return this.#u;
    }
    get maxSize() {
      return this.#f;
    }
    get calculatedSize() {
      return this.#b;
    }
    get size() {
      return this.#i;
    }
    get fetchMethod() {
      return this.#R;
    }
    get memoMethod() {
      return this.#k;
    }
    get dispose() {
      return this.#y;
    }
    get disposeAfter() {
      return this.#g;
    }
    constructor(e) {
      let { max: r = 0, ttl: n, ttlResolution: i = 1, ttlAutopurge: s, updateAgeOnGet: o, updateAgeOnHas: a, allowStale: c, dispose: f, disposeAfter: h2, noDisposeOnSet: d, noUpdateTTL: w, maxSize: y = 0, maxEntrySize: P = 0, sizeCalculation: E, fetchMethod: b, memoMethod: m, noDeleteOnFetchRejection: v, noDeleteOnStaleGet: R, allowStaleOnFetchRejection: T, allowStaleOnFetchAbort: g, ignoreFetchAbort: O } = e;
      if (r !== 0 && !lr(r)) throw new TypeError("max option must be a nonnegative integer");
      let _ = r ? _y(r) : Array;
      if (!_) throw new Error("invalid max value: " + r);
      if (this.#u = r, this.#f = y, this.maxEntrySize = P || this.#f, this.sizeCalculation = E, this.sizeCalculation) {
        if (!this.#f && !this.maxEntrySize) throw new TypeError("cannot set sizeCalculation without setting maxSize or maxEntrySize");
        if (typeof this.sizeCalculation != "function") throw new TypeError("sizeCalculation set to non-function");
      }
      if (m !== void 0 && typeof m != "function") throw new TypeError("memoMethod must be a function if defined");
      if (this.#k = m, b !== void 0 && typeof b != "function") throw new TypeError("fetchMethod must be a function if specified");
      if (this.#R = b, this.#x = !!b, this.#n = /* @__PURE__ */ new Map(), this.#r = new Array(r).fill(void 0), this.#e = new Array(r).fill(void 0), this.#l = new _(r), this.#h = new _(r), this.#a = 0, this.#o = 0, this.#w = $a.create(r), this.#i = 0, this.#b = 0, typeof f == "function" && (this.#y = f), typeof h2 == "function" ? (this.#g = h2, this.#s = []) : (this.#g = void 0, this.#s = void 0), this.#v = !!this.#y, this.#c = !!this.#g, this.noDisposeOnSet = !!d, this.noUpdateTTL = !!w, this.noDeleteOnFetchRejection = !!v, this.allowStaleOnFetchRejection = !!T, this.allowStaleOnFetchAbort = !!g, this.ignoreFetchAbort = !!O, this.maxEntrySize !== 0) {
        if (this.#f !== 0 && !lr(this.#f)) throw new TypeError("maxSize must be a positive integer if specified");
        if (!lr(this.maxEntrySize)) throw new TypeError("maxEntrySize must be a positive integer if specified");
        this.#F();
      }
      if (this.allowStale = !!c, this.noDeleteOnStaleGet = !!R, this.updateAgeOnGet = !!o, this.updateAgeOnHas = !!a, this.ttlResolution = lr(i) || i === 0 ? i : 1, this.ttlAutopurge = !!s, this.ttl = n || 0, this.ttl) {
        if (!lr(this.ttl)) throw new TypeError("ttl must be a positive integer if specified");
        this.#N();
      }
      if (this.#u === 0 && this.ttl === 0 && this.#f === 0) throw new TypeError("At least one of max, maxSize, or ttl is required");
      if (!this.ttlAutopurge && !this.#u && !this.#f) {
        let q = "LRU_CACHE_UNBOUNDED";
        NE(q) && (wy.add(q), my("TTL caching without ttlAutopurge, max, or maxSize can result in unbounded memory consumption.", "UnboundedCacheWarning", q, t));
      }
    }
    getRemainingTTL(e) {
      return this.#n.has(e) ? 1 / 0 : 0;
    }
    #N() {
      let e = new bn(this.#u), r = new bn(this.#u);
      this.#d = e, this.#_ = r, this.#q = (s, o, a = gn.now()) => {
        if (r[s] = o !== 0 ? a : 0, e[s] = o, o !== 0 && this.ttlAutopurge) {
          let c = setTimeout(() => {
            this.#p(s) && this.#A(this.#r[s], "expire");
          }, o + 1);
          c.unref && c.unref();
        }
      }, this.#T = (s) => {
        r[s] = e[s] !== 0 ? gn.now() : 0;
      }, this.#I = (s, o) => {
        if (e[o]) {
          let a = e[o], c = r[o];
          if (!a || !c) return;
          s.ttl = a, s.start = c, s.now = n || i();
          let f = s.now - c;
          s.remainingTTL = a - f;
        }
      };
      let n = 0, i = () => {
        let s = gn.now();
        if (this.ttlResolution > 0) {
          n = s;
          let o = setTimeout(() => n = 0, this.ttlResolution);
          o.unref && o.unref();
        }
        return s;
      };
      this.getRemainingTTL = (s) => {
        let o = this.#n.get(s);
        if (o === void 0) return 0;
        let a = e[o], c = r[o];
        if (!a || !c) return 1 / 0;
        let f = (n || i()) - c;
        return a - f;
      }, this.#p = (s) => {
        let o = r[s], a = e[s];
        return !!a && !!o && (n || i()) - o > a;
      };
    }
    #T = () => {
    };
    #I = () => {
    };
    #q = () => {
    };
    #p = () => false;
    #F() {
      let e = new bn(this.#u);
      this.#b = 0, this.#m = e, this.#P = (r) => {
        this.#b -= e[r], e[r] = 0;
      }, this.#L = (r, n, i, s) => {
        if (this.#t(n)) return 0;
        if (!lr(i)) if (s) {
          if (typeof s != "function") throw new TypeError("sizeCalculation must be a function");
          if (i = s(n, r), !lr(i)) throw new TypeError("sizeCalculation return invalid (expect positive integer)");
        } else throw new TypeError("invalid size value (must be positive integer). When maxSize or maxEntrySize is used, sizeCalculation or size must be set.");
        return i;
      }, this.#C = (r, n, i) => {
        if (e[r] = n, this.#f) {
          let s = this.#f - e[r];
          for (; this.#b > s; ) this.#B(true);
        }
        this.#b += e[r], i && (i.entrySize = n, i.totalCalculatedSize = this.#b);
      };
    }
    #P = (e) => {
    };
    #C = (e, r, n) => {
    };
    #L = (e, r, n, i) => {
      if (n || i) throw new TypeError("cannot set size without setting maxSize or maxEntrySize on cache");
      return 0;
    };
    *#S({ allowStale: e = this.allowStale } = {}) {
      if (this.#i) for (let r = this.#o; !(!this.#U(r) || ((e || !this.#p(r)) && (yield r), r === this.#a)); ) r = this.#h[r];
    }
    *#E({ allowStale: e = this.allowStale } = {}) {
      if (this.#i) for (let r = this.#a; !(!this.#U(r) || ((e || !this.#p(r)) && (yield r), r === this.#o)); ) r = this.#l[r];
    }
    #U(e) {
      return e !== void 0 && this.#n.get(this.#r[e]) === e;
    }
    *entries() {
      for (let e of this.#S()) this.#e[e] !== void 0 && this.#r[e] !== void 0 && !this.#t(this.#e[e]) && (yield [this.#r[e], this.#e[e]]);
    }
    *rentries() {
      for (let e of this.#E()) this.#e[e] !== void 0 && this.#r[e] !== void 0 && !this.#t(this.#e[e]) && (yield [this.#r[e], this.#e[e]]);
    }
    *keys() {
      for (let e of this.#S()) {
        let r = this.#r[e];
        r !== void 0 && !this.#t(this.#e[e]) && (yield r);
      }
    }
    *rkeys() {
      for (let e of this.#E()) {
        let r = this.#r[e];
        r !== void 0 && !this.#t(this.#e[e]) && (yield r);
      }
    }
    *values() {
      for (let e of this.#S()) this.#e[e] !== void 0 && !this.#t(this.#e[e]) && (yield this.#e[e]);
    }
    *rvalues() {
      for (let e of this.#E()) this.#e[e] !== void 0 && !this.#t(this.#e[e]) && (yield this.#e[e]);
    }
    [Symbol.iterator]() {
      return this.entries();
    }
    [Symbol.toStringTag] = "LRUCache";
    find(e, r = {}) {
      for (let n of this.#S()) {
        let i = this.#e[n], s = this.#t(i) ? i.__staleWhileFetching : i;
        if (s !== void 0 && e(s, this.#r[n], this)) return this.get(this.#r[n], r);
      }
    }
    forEach(e, r = this) {
      for (let n of this.#S()) {
        let i = this.#e[n], s = this.#t(i) ? i.__staleWhileFetching : i;
        s !== void 0 && e.call(r, s, this.#r[n], this);
      }
    }
    rforEach(e, r = this) {
      for (let n of this.#E()) {
        let i = this.#e[n], s = this.#t(i) ? i.__staleWhileFetching : i;
        s !== void 0 && e.call(r, s, this.#r[n], this);
      }
    }
    purgeStale() {
      let e = false;
      for (let r of this.#E({ allowStale: true })) this.#p(r) && (this.#A(this.#r[r], "expire"), e = true);
      return e;
    }
    info(e) {
      let r = this.#n.get(e);
      if (r === void 0) return;
      let n = this.#e[r], i = this.#t(n) ? n.__staleWhileFetching : n;
      if (i === void 0) return;
      let s = { value: i };
      if (this.#d && this.#_) {
        let o = this.#d[r], a = this.#_[r];
        if (o && a) {
          let c = o - (gn.now() - a);
          s.ttl = c, s.start = Date.now();
        }
      }
      return this.#m && (s.size = this.#m[r]), s;
    }
    dump() {
      let e = [];
      for (let r of this.#S({ allowStale: true })) {
        let n = this.#r[r], i = this.#e[r], s = this.#t(i) ? i.__staleWhileFetching : i;
        if (s === void 0 || n === void 0) continue;
        let o = { value: s };
        if (this.#d && this.#_) {
          o.ttl = this.#d[r];
          let a = gn.now() - this.#_[r];
          o.start = Math.floor(Date.now() - a);
        }
        this.#m && (o.size = this.#m[r]), e.unshift([n, o]);
      }
      return e;
    }
    load(e) {
      this.clear();
      for (let [r, n] of e) {
        if (n.start) {
          let i = Date.now() - n.start;
          n.start = gn.now() - i;
        }
        this.set(r, n.value, n);
      }
    }
    set(e, r, n = {}) {
      if (r === void 0) return this.delete(e), this;
      let { ttl: i = this.ttl, start: s, noDisposeOnSet: o = this.noDisposeOnSet, sizeCalculation: a = this.sizeCalculation, status: c } = n, { noUpdateTTL: f = this.noUpdateTTL } = n, h2 = this.#L(e, r, n.size || 0, a);
      if (this.maxEntrySize && h2 > this.maxEntrySize) return c && (c.set = "miss", c.maxEntrySizeExceeded = true), this.#A(e, "set"), this;
      let d = this.#i === 0 ? void 0 : this.#n.get(e);
      if (d === void 0) d = this.#i === 0 ? this.#o : this.#w.length !== 0 ? this.#w.pop() : this.#i === this.#u ? this.#B(false) : this.#i, this.#r[d] = e, this.#e[d] = r, this.#n.set(e, d), this.#l[this.#o] = d, this.#h[d] = this.#o, this.#o = d, this.#i++, this.#C(d, h2, c), c && (c.set = "add"), f = false;
      else {
        this.#O(d);
        let w = this.#e[d];
        if (r !== w) {
          if (this.#x && this.#t(w)) {
            w.__abortController.abort(new Error("replaced"));
            let { __staleWhileFetching: y } = w;
            y !== void 0 && !o && (this.#v && this.#y?.(y, e, "set"), this.#c && this.#s?.push([y, e, "set"]));
          } else o || (this.#v && this.#y?.(w, e, "set"), this.#c && this.#s?.push([w, e, "set"]));
          if (this.#P(d), this.#C(d, h2, c), this.#e[d] = r, c) {
            c.set = "replace";
            let y = w && this.#t(w) ? w.__staleWhileFetching : w;
            y !== void 0 && (c.oldValue = y);
          }
        } else c && (c.set = "update");
      }
      if (i !== 0 && !this.#d && this.#N(), this.#d && (f || this.#q(d, i, s), c && this.#I(c, d)), !o && this.#c && this.#s) {
        let w = this.#s, y;
        for (; y = w?.shift(); ) this.#g?.(...y);
      }
      return this;
    }
    pop() {
      try {
        for (; this.#i; ) {
          let e = this.#e[this.#a];
          if (this.#B(true), this.#t(e)) {
            if (e.__staleWhileFetching) return e.__staleWhileFetching;
          } else if (e !== void 0) return e;
        }
      } finally {
        if (this.#c && this.#s) {
          let e = this.#s, r;
          for (; r = e?.shift(); ) this.#g?.(...r);
        }
      }
    }
    #B(e) {
      let r = this.#a, n = this.#r[r], i = this.#e[r];
      return this.#x && this.#t(i) ? i.__abortController.abort(new Error("evicted")) : (this.#v || this.#c) && (this.#v && this.#y?.(i, n, "evict"), this.#c && this.#s?.push([i, n, "evict"])), this.#P(r), e && (this.#r[r] = void 0, this.#e[r] = void 0, this.#w.push(r)), this.#i === 1 ? (this.#a = this.#o = 0, this.#w.length = 0) : this.#a = this.#l[r], this.#n.delete(n), this.#i--, r;
    }
    has(e, r = {}) {
      let { updateAgeOnHas: n = this.updateAgeOnHas, status: i } = r, s = this.#n.get(e);
      if (s !== void 0) {
        let o = this.#e[s];
        if (this.#t(o) && o.__staleWhileFetching === void 0) return false;
        if (this.#p(s)) i && (i.has = "stale", this.#I(i, s));
        else return n && this.#T(s), i && (i.has = "hit", this.#I(i, s)), true;
      } else i && (i.has = "miss");
      return false;
    }
    peek(e, r = {}) {
      let { allowStale: n = this.allowStale } = r, i = this.#n.get(e);
      if (i === void 0 || !n && this.#p(i)) return;
      let s = this.#e[i];
      return this.#t(s) ? s.__staleWhileFetching : s;
    }
    #M(e, r, n, i) {
      let s = r === void 0 ? void 0 : this.#e[r];
      if (this.#t(s)) return s;
      let o = new mo(), { signal: a } = n;
      a?.addEventListener("abort", () => o.abort(a.reason), { signal: o.signal });
      let c = { signal: o.signal, options: n, context: i }, f = (E, b = false) => {
        let { aborted: m } = o.signal, v = n.ignoreFetchAbort && E !== void 0;
        if (n.status && (m && !b ? (n.status.fetchAborted = true, n.status.fetchError = o.signal.reason, v && (n.status.fetchAbortIgnored = true)) : n.status.fetchResolved = true), m && !v && !b) return d(o.signal.reason);
        let R = y;
        return this.#e[r] === y && (E === void 0 ? R.__staleWhileFetching ? this.#e[r] = R.__staleWhileFetching : this.#A(e, "fetch") : (n.status && (n.status.fetchUpdated = true), this.set(e, E, c.options))), E;
      }, h2 = (E) => (n.status && (n.status.fetchRejected = true, n.status.fetchError = E), d(E)), d = (E) => {
        let { aborted: b } = o.signal, m = b && n.allowStaleOnFetchAbort, v = m || n.allowStaleOnFetchRejection, R = v || n.noDeleteOnFetchRejection, T = y;
        if (this.#e[r] === y && (!R || T.__staleWhileFetching === void 0 ? this.#A(e, "fetch") : m || (this.#e[r] = T.__staleWhileFetching)), v) return n.status && T.__staleWhileFetching !== void 0 && (n.status.returnedStale = true), T.__staleWhileFetching;
        if (T.__returned === T) throw E;
      }, w = (E, b) => {
        let m = this.#R?.(e, s, c);
        m && m instanceof Promise && m.then((v) => E(v === void 0 ? void 0 : v), b), o.signal.addEventListener("abort", () => {
          (!n.ignoreFetchAbort || n.allowStaleOnFetchAbort) && (E(void 0), n.allowStaleOnFetchAbort && (E = (v) => f(v, true)));
        });
      };
      n.status && (n.status.fetchDispatched = true);
      let y = new Promise(w).then(f, h2), P = Object.assign(y, { __abortController: o, __staleWhileFetching: s, __returned: void 0 });
      return r === void 0 ? (this.set(e, P, { ...c.options, status: void 0 }), r = this.#n.get(e)) : this.#e[r] = P, P;
    }
    #t(e) {
      if (!this.#x) return false;
      let r = e;
      return !!r && r instanceof Promise && r.hasOwnProperty("__staleWhileFetching") && r.__abortController instanceof mo;
    }
    async fetch(e, r = {}) {
      let { allowStale: n = this.allowStale, updateAgeOnGet: i = this.updateAgeOnGet, noDeleteOnStaleGet: s = this.noDeleteOnStaleGet, ttl: o = this.ttl, noDisposeOnSet: a = this.noDisposeOnSet, size: c = 0, sizeCalculation: f = this.sizeCalculation, noUpdateTTL: h2 = this.noUpdateTTL, noDeleteOnFetchRejection: d = this.noDeleteOnFetchRejection, allowStaleOnFetchRejection: w = this.allowStaleOnFetchRejection, ignoreFetchAbort: y = this.ignoreFetchAbort, allowStaleOnFetchAbort: P = this.allowStaleOnFetchAbort, context: E, forceRefresh: b = false, status: m, signal: v } = r;
      if (!this.#x) return m && (m.fetch = "get"), this.get(e, { allowStale: n, updateAgeOnGet: i, noDeleteOnStaleGet: s, status: m });
      let R = { allowStale: n, updateAgeOnGet: i, noDeleteOnStaleGet: s, ttl: o, noDisposeOnSet: a, size: c, sizeCalculation: f, noUpdateTTL: h2, noDeleteOnFetchRejection: d, allowStaleOnFetchRejection: w, allowStaleOnFetchAbort: P, ignoreFetchAbort: y, status: m, signal: v }, T = this.#n.get(e);
      if (T === void 0) {
        m && (m.fetch = "miss");
        let g = this.#M(e, T, R, E);
        return g.__returned = g;
      } else {
        let g = this.#e[T];
        if (this.#t(g)) {
          let j = n && g.__staleWhileFetching !== void 0;
          return m && (m.fetch = "inflight", j && (m.returnedStale = true)), j ? g.__staleWhileFetching : g.__returned = g;
        }
        let O = this.#p(T);
        if (!b && !O) return m && (m.fetch = "hit"), this.#O(T), i && this.#T(T), m && this.#I(m, T), g;
        let _ = this.#M(e, T, R, E), $ = _.__staleWhileFetching !== void 0 && n;
        return m && (m.fetch = O ? "stale" : "refresh", $ && O && (m.returnedStale = true)), $ ? _.__staleWhileFetching : _.__returned = _;
      }
    }
    async forceFetch(e, r = {}) {
      let n = await this.fetch(e, r);
      if (n === void 0) throw new Error("fetch() returned undefined");
      return n;
    }
    memo(e, r = {}) {
      let n = this.#k;
      if (!n) throw new Error("no memoMethod provided to constructor");
      let { context: i, forceRefresh: s, ...o } = r, a = this.get(e, o);
      if (!s && a !== void 0) return a;
      let c = n(e, a, { options: o, context: i });
      return this.set(e, c, o), c;
    }
    get(e, r = {}) {
      let { allowStale: n = this.allowStale, updateAgeOnGet: i = this.updateAgeOnGet, noDeleteOnStaleGet: s = this.noDeleteOnStaleGet, status: o } = r, a = this.#n.get(e);
      if (a !== void 0) {
        let c = this.#e[a], f = this.#t(c);
        return o && this.#I(o, a), this.#p(a) ? (o && (o.get = "stale"), f ? (o && n && c.__staleWhileFetching !== void 0 && (o.returnedStale = true), n ? c.__staleWhileFetching : void 0) : (s || this.#A(e, "expire"), o && n && (o.returnedStale = true), n ? c : void 0)) : (o && (o.get = "hit"), f ? c.__staleWhileFetching : (this.#O(a), i && this.#T(a), c));
      } else o && (o.get = "miss");
    }
    #D(e, r) {
      this.#h[r] = e, this.#l[e] = r;
    }
    #O(e) {
      e !== this.#o && (e === this.#a ? this.#a = this.#l[e] : this.#D(this.#h[e], this.#l[e]), this.#D(this.#o, e), this.#o = e);
    }
    delete(e) {
      return this.#A(e, "delete");
    }
    #A(e, r) {
      let n = false;
      if (this.#i !== 0) {
        let i = this.#n.get(e);
        if (i !== void 0) if (n = true, this.#i === 1) this.#j(r);
        else {
          this.#P(i);
          let s = this.#e[i];
          if (this.#t(s) ? s.__abortController.abort(new Error("deleted")) : (this.#v || this.#c) && (this.#v && this.#y?.(s, e, r), this.#c && this.#s?.push([s, e, r])), this.#n.delete(e), this.#r[i] = void 0, this.#e[i] = void 0, i === this.#o) this.#o = this.#h[i];
          else if (i === this.#a) this.#a = this.#l[i];
          else {
            let o = this.#h[i];
            this.#l[o] = this.#l[i];
            let a = this.#l[i];
            this.#h[a] = this.#h[i];
          }
          this.#i--, this.#w.push(i);
        }
      }
      if (this.#c && this.#s?.length) {
        let i = this.#s, s;
        for (; s = i?.shift(); ) this.#g?.(...s);
      }
      return n;
    }
    clear() {
      return this.#j("delete");
    }
    #j(e) {
      for (let r of this.#E({ allowStale: true })) {
        let n = this.#e[r];
        if (this.#t(n)) n.__abortController.abort(new Error("deleted"));
        else {
          let i = this.#r[r];
          this.#v && this.#y?.(n, i, e), this.#c && this.#s?.push([n, i, e]);
        }
      }
      if (this.#n.clear(), this.#e.fill(void 0), this.#r.fill(void 0), this.#d && this.#_ && (this.#d.fill(0), this.#_.fill(0)), this.#m && this.#m.fill(0), this.#a = 0, this.#o = 0, this.#w.length = 0, this.#b = 0, this.#i = 0, this.#c && this.#s) {
        let r = this.#s, n;
        for (; n = r?.shift(); ) this.#g?.(...n);
      }
    }
  };
  _o.LRUCache = Ha;
});
var It = D((cr) => {
  A();
  x();
  I();
  Object.defineProperty(cr, "t", { value: true });
  cr.ContainerIterator = cr.Container = cr.Base = void 0;
  var Va = class {
    constructor(e = 0) {
      this.iteratorType = e;
    }
    equals(e) {
      return this.o === e.o;
    }
  };
  cr.ContainerIterator = Va;
  var vo = class {
    constructor() {
      this.i = 0;
    }
    get length() {
      return this.i;
    }
    size() {
      return this.i;
    }
    empty() {
      return this.i === 0;
    }
  };
  cr.Base = vo;
  var Ga = class extends vo {
  };
  cr.Container = Ga;
});
var Sy = D((So) => {
  A();
  x();
  I();
  Object.defineProperty(So, "t", { value: true });
  So.default = void 0;
  var qE = It(), za = class extends qE.Base {
    constructor(e = []) {
      super(), this.S = [];
      let r = this;
      e.forEach(function(n) {
        r.push(n);
      });
    }
    clear() {
      this.i = 0, this.S = [];
    }
    push(e) {
      return this.S.push(e), this.i += 1, this.i;
    }
    pop() {
      if (this.i !== 0) return this.i -= 1, this.S.pop();
    }
    top() {
      return this.S[this.i - 1];
    }
  }, LE = za;
  So.default = LE;
});
var Ey = D((Eo) => {
  A();
  x();
  I();
  Object.defineProperty(Eo, "t", { value: true });
  Eo.default = void 0;
  var UE = It(), Ka = class extends UE.Base {
    constructor(e = []) {
      super(), this.j = 0, this.q = [];
      let r = this;
      e.forEach(function(n) {
        r.push(n);
      });
    }
    clear() {
      this.q = [], this.i = this.j = 0;
    }
    push(e) {
      let r = this.q.length;
      if (this.j / r > 0.5 && this.j + this.i >= r && r > 4096) {
        let n = this.i;
        for (let i = 0; i < n; ++i) this.q[i] = this.q[this.j + i];
        this.j = 0, this.q[this.i] = e;
      } else this.q[this.j + this.i] = e;
      return ++this.i;
    }
    pop() {
      if (this.i === 0) return;
      let e = this.q[this.j++];
      return this.i -= 1, e;
    }
    front() {
      if (this.i !== 0) return this.q[this.j];
    }
  }, DE = Ka;
  Eo.default = DE;
});
var Ay = D((Ao) => {
  A();
  x();
  I();
  Object.defineProperty(Ao, "t", { value: true });
  Ao.default = void 0;
  var jE = It(), Qa = class extends jE.Base {
    constructor(e = [], r = function(i, s) {
      return i > s ? -1 : i < s ? 1 : 0;
    }, n = true) {
      if (super(), this.v = r, Array.isArray(e)) this.C = n ? [...e] : e;
      else {
        this.C = [];
        let s = this;
        e.forEach(function(o) {
          s.C.push(o);
        });
      }
      this.i = this.C.length;
      let i = this.i >> 1;
      for (let s = this.i - 1 >> 1; s >= 0; --s) this.k(s, i);
    }
    m(e) {
      let r = this.C[e];
      for (; e > 0; ) {
        let n = e - 1 >> 1, i = this.C[n];
        if (this.v(i, r) <= 0) break;
        this.C[e] = i, e = n;
      }
      this.C[e] = r;
    }
    k(e, r) {
      let n = this.C[e];
      for (; e < r; ) {
        let i = e << 1 | 1, s = i + 1, o = this.C[i];
        if (s < this.i && this.v(o, this.C[s]) > 0 && (i = s, o = this.C[s]), this.v(o, n) >= 0) break;
        this.C[e] = o, e = i;
      }
      this.C[e] = n;
    }
    clear() {
      this.i = 0, this.C.length = 0;
    }
    push(e) {
      this.C.push(e), this.m(this.i), this.i += 1;
    }
    pop() {
      if (this.i === 0) return;
      let e = this.C[0], r = this.C.pop();
      return this.i -= 1, this.i && (this.C[0] = r, this.k(0, this.i >> 1)), e;
    }
    top() {
      return this.C[0];
    }
    find(e) {
      return this.C.indexOf(e) >= 0;
    }
    remove(e) {
      let r = this.C.indexOf(e);
      return r < 0 ? false : (r === 0 ? this.pop() : r === this.i - 1 ? (this.C.pop(), this.i -= 1) : (this.C.splice(r, 1, this.C.pop()), this.i -= 1, this.m(r), this.k(r, this.i >> 1)), true);
    }
    updateItem(e) {
      let r = this.C.indexOf(e);
      return r < 0 ? false : (this.m(r), this.k(r, this.i >> 1), true);
    }
    toArray() {
      return [...this.C];
    }
  }, FE = Qa;
  Ao.default = FE;
});
var xo = D((Io) => {
  A();
  x();
  I();
  Object.defineProperty(Io, "t", { value: true });
  Io.default = void 0;
  var WE = It(), Ya = class extends WE.Container {
  }, $E = Ya;
  Io.default = $E;
});
var xt = D((Ja) => {
  A();
  x();
  I();
  Object.defineProperty(Ja, "t", { value: true });
  Ja.throwIteratorAccessError = HE;
  function HE() {
    throw new RangeError("Iterator access denied!");
  }
});
var Za = D((Po) => {
  A();
  x();
  I();
  Object.defineProperty(Po, "t", { value: true });
  Po.RandomIterator = void 0;
  var VE = It(), To = xt(), Xa = class extends VE.ContainerIterator {
    constructor(e, r) {
      super(r), this.o = e, this.iteratorType === 0 ? (this.pre = function() {
        return this.o === 0 && (0, To.throwIteratorAccessError)(), this.o -= 1, this;
      }, this.next = function() {
        return this.o === this.container.size() && (0, To.throwIteratorAccessError)(), this.o += 1, this;
      }) : (this.pre = function() {
        return this.o === this.container.size() - 1 && (0, To.throwIteratorAccessError)(), this.o += 1, this;
      }, this.next = function() {
        return this.o === -1 && (0, To.throwIteratorAccessError)(), this.o -= 1, this;
      });
    }
    get pointer() {
      return this.container.getElementByPos(this.o);
    }
    set pointer(e) {
      this.container.setElementByPos(this.o, e);
    }
  };
  Po.RandomIterator = Xa;
});
var Iy = D((Oo) => {
  A();
  x();
  I();
  Object.defineProperty(Oo, "t", { value: true });
  Oo.default = void 0;
  var GE = KE(xo()), zE = Za();
  function KE(t) {
    return t && t.t ? t : { default: t };
  }
  var Nr = class t extends zE.RandomIterator {
    constructor(e, r, n) {
      super(e, n), this.container = r;
    }
    copy() {
      return new t(this.o, this.container, this.iteratorType);
    }
  }, eu = class extends GE.default {
    constructor(e = [], r = true) {
      if (super(), Array.isArray(e)) this.J = r ? [...e] : e, this.i = e.length;
      else {
        this.J = [];
        let n = this;
        e.forEach(function(i) {
          n.pushBack(i);
        });
      }
    }
    clear() {
      this.i = 0, this.J.length = 0;
    }
    begin() {
      return new Nr(0, this);
    }
    end() {
      return new Nr(this.i, this);
    }
    rBegin() {
      return new Nr(this.i - 1, this, 1);
    }
    rEnd() {
      return new Nr(-1, this, 1);
    }
    front() {
      return this.J[0];
    }
    back() {
      return this.J[this.i - 1];
    }
    getElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      return this.J[e];
    }
    eraseElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      return this.J.splice(e, 1), this.i -= 1, this.i;
    }
    eraseElementByValue(e) {
      let r = 0;
      for (let n = 0; n < this.i; ++n) this.J[n] !== e && (this.J[r++] = this.J[n]);
      return this.i = this.J.length = r, this.i;
    }
    eraseElementByIterator(e) {
      let r = e.o;
      return e = e.next(), this.eraseElementByPos(r), e;
    }
    pushBack(e) {
      return this.J.push(e), this.i += 1, this.i;
    }
    popBack() {
      if (this.i !== 0) return this.i -= 1, this.J.pop();
    }
    setElementByPos(e, r) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      this.J[e] = r;
    }
    insert(e, r, n = 1) {
      if (e < 0 || e > this.i) throw new RangeError();
      return this.J.splice(e, 0, ...new Array(n).fill(r)), this.i += n, this.i;
    }
    find(e) {
      for (let r = 0; r < this.i; ++r) if (this.J[r] === e) return new Nr(r, this);
      return this.end();
    }
    reverse() {
      this.J.reverse();
    }
    unique() {
      let e = 1;
      for (let r = 1; r < this.i; ++r) this.J[r] !== this.J[r - 1] && (this.J[e++] = this.J[r]);
      return this.i = this.J.length = e, this.i;
    }
    sort(e) {
      this.J.sort(e);
    }
    forEach(e) {
      for (let r = 0; r < this.i; ++r) e(this.J[r], r, this);
    }
    [Symbol.iterator]() {
      return (function* () {
        yield* this.J;
      }).bind(this)();
    }
  }, QE = eu;
  Oo.default = QE;
});
var xy = D((Ro) => {
  A();
  x();
  I();
  Object.defineProperty(Ro, "t", { value: true });
  Ro.default = void 0;
  var YE = XE(xo()), JE = It(), qr = xt();
  function XE(t) {
    return t && t.t ? t : { default: t };
  }
  var Lr = class t extends JE.ContainerIterator {
    constructor(e, r, n, i) {
      super(i), this.o = e, this.h = r, this.container = n, this.iteratorType === 0 ? (this.pre = function() {
        return this.o.L === this.h && (0, qr.throwIteratorAccessError)(), this.o = this.o.L, this;
      }, this.next = function() {
        return this.o === this.h && (0, qr.throwIteratorAccessError)(), this.o = this.o.B, this;
      }) : (this.pre = function() {
        return this.o.B === this.h && (0, qr.throwIteratorAccessError)(), this.o = this.o.B, this;
      }, this.next = function() {
        return this.o === this.h && (0, qr.throwIteratorAccessError)(), this.o = this.o.L, this;
      });
    }
    get pointer() {
      return this.o === this.h && (0, qr.throwIteratorAccessError)(), this.o.l;
    }
    set pointer(e) {
      this.o === this.h && (0, qr.throwIteratorAccessError)(), this.o.l = e;
    }
    copy() {
      return new t(this.o, this.h, this.container, this.iteratorType);
    }
  }, tu = class extends YE.default {
    constructor(e = []) {
      super(), this.h = {}, this.p = this._ = this.h.L = this.h.B = this.h;
      let r = this;
      e.forEach(function(n) {
        r.pushBack(n);
      });
    }
    V(e) {
      let { L: r, B: n } = e;
      r.B = n, n.L = r, e === this.p && (this.p = n), e === this._ && (this._ = r), this.i -= 1;
    }
    G(e, r) {
      let n = r.B, i = { l: e, L: r, B: n };
      r.B = i, n.L = i, r === this.h && (this.p = i), n === this.h && (this._ = i), this.i += 1;
    }
    clear() {
      this.i = 0, this.p = this._ = this.h.L = this.h.B = this.h;
    }
    begin() {
      return new Lr(this.p, this.h, this);
    }
    end() {
      return new Lr(this.h, this.h, this);
    }
    rBegin() {
      return new Lr(this._, this.h, this, 1);
    }
    rEnd() {
      return new Lr(this.h, this.h, this, 1);
    }
    front() {
      return this.p.l;
    }
    back() {
      return this._.l;
    }
    getElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      let r = this.p;
      for (; e--; ) r = r.B;
      return r.l;
    }
    eraseElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      let r = this.p;
      for (; e--; ) r = r.B;
      return this.V(r), this.i;
    }
    eraseElementByValue(e) {
      let r = this.p;
      for (; r !== this.h; ) r.l === e && this.V(r), r = r.B;
      return this.i;
    }
    eraseElementByIterator(e) {
      let r = e.o;
      return r === this.h && (0, qr.throwIteratorAccessError)(), e = e.next(), this.V(r), e;
    }
    pushBack(e) {
      return this.G(e, this._), this.i;
    }
    popBack() {
      if (this.i === 0) return;
      let e = this._.l;
      return this.V(this._), e;
    }
    pushFront(e) {
      return this.G(e, this.h), this.i;
    }
    popFront() {
      if (this.i === 0) return;
      let e = this.p.l;
      return this.V(this.p), e;
    }
    setElementByPos(e, r) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      let n = this.p;
      for (; e--; ) n = n.B;
      n.l = r;
    }
    insert(e, r, n = 1) {
      if (e < 0 || e > this.i) throw new RangeError();
      if (n <= 0) return this.i;
      if (e === 0) for (; n--; ) this.pushFront(r);
      else if (e === this.i) for (; n--; ) this.pushBack(r);
      else {
        let i = this.p;
        for (let o = 1; o < e; ++o) i = i.B;
        let s = i.B;
        for (this.i += n; n--; ) i.B = { l: r, L: i }, i.B.L = i, i = i.B;
        i.B = s, s.L = i;
      }
      return this.i;
    }
    find(e) {
      let r = this.p;
      for (; r !== this.h; ) {
        if (r.l === e) return new Lr(r, this.h, this);
        r = r.B;
      }
      return this.end();
    }
    reverse() {
      if (this.i <= 1) return;
      let e = this.p, r = this._, n = 0;
      for (; n << 1 < this.i; ) {
        let i = e.l;
        e.l = r.l, r.l = i, e = e.B, r = r.L, n += 1;
      }
    }
    unique() {
      if (this.i <= 1) return this.i;
      let e = this.p;
      for (; e !== this.h; ) {
        let r = e;
        for (; r.B !== this.h && r.l === r.B.l; ) r = r.B, this.i -= 1;
        e.B = r.B, e.B.L = e, e = e.B;
      }
      return this.i;
    }
    sort(e) {
      if (this.i <= 1) return;
      let r = [];
      this.forEach(function(i) {
        r.push(i);
      }), r.sort(e);
      let n = this.p;
      r.forEach(function(i) {
        n.l = i, n = n.B;
      });
    }
    merge(e) {
      let r = this;
      if (this.i === 0) e.forEach(function(n) {
        r.pushBack(n);
      });
      else {
        let n = this.p;
        e.forEach(function(i) {
          for (; n !== r.h && n.l <= i; ) n = n.B;
          r.G(i, n.L);
        });
      }
      return this.i;
    }
    forEach(e) {
      let r = this.p, n = 0;
      for (; r !== this.h; ) e(r.l, n++, this), r = r.B;
    }
    [Symbol.iterator]() {
      return (function* () {
        if (this.i === 0) return;
        let e = this.p;
        for (; e !== this.h; ) yield e.l, e = e.B;
      }).bind(this)();
    }
  }, ZE = tu;
  Ro.default = ZE;
});
var Ty = D((ko) => {
  A();
  x();
  I();
  Object.defineProperty(ko, "t", { value: true });
  ko.default = void 0;
  var e1 = r1(xo()), t1 = Za();
  function r1(t) {
    return t && t.t ? t : { default: t };
  }
  var Ur = class t extends t1.RandomIterator {
    constructor(e, r, n) {
      super(e, n), this.container = r;
    }
    copy() {
      return new t(this.o, this.container, this.iteratorType);
    }
  }, ru = class extends e1.default {
    constructor(e = [], r = 4096) {
      super(), this.j = 0, this.D = 0, this.R = 0, this.N = 0, this.P = 0, this.A = [];
      let n = (() => {
        if (typeof e.length == "number") return e.length;
        if (typeof e.size == "number") return e.size;
        if (typeof e.size == "function") return e.size();
        throw new TypeError("Cannot get the length or size of the container");
      })();
      this.F = r, this.P = Math.max(Math.ceil(n / this.F), 1);
      for (let o = 0; o < this.P; ++o) this.A.push(new Array(this.F));
      let i = Math.ceil(n / this.F);
      this.j = this.R = (this.P >> 1) - (i >> 1), this.D = this.N = this.F - n % this.F >> 1;
      let s = this;
      e.forEach(function(o) {
        s.pushBack(o);
      });
    }
    T() {
      let e = [], r = Math.max(this.P >> 1, 1);
      for (let n = 0; n < r; ++n) e[n] = new Array(this.F);
      for (let n = this.j; n < this.P; ++n) e[e.length] = this.A[n];
      for (let n = 0; n < this.R; ++n) e[e.length] = this.A[n];
      e[e.length] = [...this.A[this.R]], this.j = r, this.R = e.length - 1;
      for (let n = 0; n < r; ++n) e[e.length] = new Array(this.F);
      this.A = e, this.P = e.length;
    }
    O(e) {
      let r = this.D + e + 1, n = r % this.F, i = n - 1, s = this.j + (r - n) / this.F;
      return n === 0 && (s -= 1), s %= this.P, i < 0 && (i += this.F), { curNodeBucketIndex: s, curNodePointerIndex: i };
    }
    clear() {
      this.A = [new Array(this.F)], this.P = 1, this.j = this.R = this.i = 0, this.D = this.N = this.F >> 1;
    }
    begin() {
      return new Ur(0, this);
    }
    end() {
      return new Ur(this.i, this);
    }
    rBegin() {
      return new Ur(this.i - 1, this, 1);
    }
    rEnd() {
      return new Ur(-1, this, 1);
    }
    front() {
      if (this.i !== 0) return this.A[this.j][this.D];
    }
    back() {
      if (this.i !== 0) return this.A[this.R][this.N];
    }
    pushBack(e) {
      return this.i && (this.N < this.F - 1 ? this.N += 1 : this.R < this.P - 1 ? (this.R += 1, this.N = 0) : (this.R = 0, this.N = 0), this.R === this.j && this.N === this.D && this.T()), this.i += 1, this.A[this.R][this.N] = e, this.i;
    }
    popBack() {
      if (this.i === 0) return;
      let e = this.A[this.R][this.N];
      return this.i !== 1 && (this.N > 0 ? this.N -= 1 : this.R > 0 ? (this.R -= 1, this.N = this.F - 1) : (this.R = this.P - 1, this.N = this.F - 1)), this.i -= 1, e;
    }
    pushFront(e) {
      return this.i && (this.D > 0 ? this.D -= 1 : this.j > 0 ? (this.j -= 1, this.D = this.F - 1) : (this.j = this.P - 1, this.D = this.F - 1), this.j === this.R && this.D === this.N && this.T()), this.i += 1, this.A[this.j][this.D] = e, this.i;
    }
    popFront() {
      if (this.i === 0) return;
      let e = this.A[this.j][this.D];
      return this.i !== 1 && (this.D < this.F - 1 ? this.D += 1 : this.j < this.P - 1 ? (this.j += 1, this.D = 0) : (this.j = 0, this.D = 0)), this.i -= 1, e;
    }
    getElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      let { curNodeBucketIndex: r, curNodePointerIndex: n } = this.O(e);
      return this.A[r][n];
    }
    setElementByPos(e, r) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      let { curNodeBucketIndex: n, curNodePointerIndex: i } = this.O(e);
      this.A[n][i] = r;
    }
    insert(e, r, n = 1) {
      if (e < 0 || e > this.i) throw new RangeError();
      if (e === 0) for (; n--; ) this.pushFront(r);
      else if (e === this.i) for (; n--; ) this.pushBack(r);
      else {
        let i = [];
        for (let s = e; s < this.i; ++s) i.push(this.getElementByPos(s));
        this.cut(e - 1);
        for (let s = 0; s < n; ++s) this.pushBack(r);
        for (let s = 0; s < i.length; ++s) this.pushBack(i[s]);
      }
      return this.i;
    }
    cut(e) {
      if (e < 0) return this.clear(), 0;
      let { curNodeBucketIndex: r, curNodePointerIndex: n } = this.O(e);
      return this.R = r, this.N = n, this.i = e + 1, this.i;
    }
    eraseElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      if (e === 0) this.popFront();
      else if (e === this.i - 1) this.popBack();
      else {
        let r = [];
        for (let i = e + 1; i < this.i; ++i) r.push(this.getElementByPos(i));
        this.cut(e), this.popBack();
        let n = this;
        r.forEach(function(i) {
          n.pushBack(i);
        });
      }
      return this.i;
    }
    eraseElementByValue(e) {
      if (this.i === 0) return 0;
      let r = [];
      for (let i = 0; i < this.i; ++i) {
        let s = this.getElementByPos(i);
        s !== e && r.push(s);
      }
      let n = r.length;
      for (let i = 0; i < n; ++i) this.setElementByPos(i, r[i]);
      return this.cut(n - 1);
    }
    eraseElementByIterator(e) {
      let r = e.o;
      return this.eraseElementByPos(r), e = e.next(), e;
    }
    find(e) {
      for (let r = 0; r < this.i; ++r) if (this.getElementByPos(r) === e) return new Ur(r, this);
      return this.end();
    }
    reverse() {
      let e = 0, r = this.i - 1;
      for (; e < r; ) {
        let n = this.getElementByPos(e);
        this.setElementByPos(e, this.getElementByPos(r)), this.setElementByPos(r, n), e += 1, r -= 1;
      }
    }
    unique() {
      if (this.i <= 1) return this.i;
      let e = 1, r = this.getElementByPos(0);
      for (let n = 1; n < this.i; ++n) {
        let i = this.getElementByPos(n);
        i !== r && (r = i, this.setElementByPos(e++, i));
      }
      for (; this.i > e; ) this.popBack();
      return this.i;
    }
    sort(e) {
      let r = [];
      for (let n = 0; n < this.i; ++n) r.push(this.getElementByPos(n));
      r.sort(e);
      for (let n = 0; n < this.i; ++n) this.setElementByPos(n, r[n]);
    }
    shrinkToFit() {
      if (this.i === 0) return;
      let e = [];
      this.forEach(function(r) {
        e.push(r);
      }), this.P = Math.max(Math.ceil(this.i / this.F), 1), this.i = this.j = this.R = this.D = this.N = 0, this.A = [];
      for (let r = 0; r < this.P; ++r) this.A.push(new Array(this.F));
      for (let r = 0; r < e.length; ++r) this.pushBack(e[r]);
    }
    forEach(e) {
      for (let r = 0; r < this.i; ++r) e(this.getElementByPos(r), r, this);
    }
    [Symbol.iterator]() {
      return (function* () {
        for (let e = 0; e < this.i; ++e) yield this.getElementByPos(e);
      }).bind(this)();
    }
  }, n1 = ru;
  ko.default = n1;
});
var Py = D((wn) => {
  A();
  x();
  I();
  Object.defineProperty(wn, "t", { value: true });
  wn.TreeNodeEnableIndex = wn.TreeNode = void 0;
  var Co = class {
    constructor(e, r) {
      this.ee = 1, this.u = void 0, this.l = void 0, this.U = void 0, this.W = void 0, this.tt = void 0, this.u = e, this.l = r;
    }
    L() {
      let e = this;
      if (e.ee === 1 && e.tt.tt === e) e = e.W;
      else if (e.U) for (e = e.U; e.W; ) e = e.W;
      else {
        let r = e.tt;
        for (; r.U === e; ) e = r, r = e.tt;
        e = r;
      }
      return e;
    }
    B() {
      let e = this;
      if (e.W) {
        for (e = e.W; e.U; ) e = e.U;
        return e;
      } else {
        let r = e.tt;
        for (; r.W === e; ) e = r, r = e.tt;
        return e.W !== r ? r : e;
      }
    }
    te() {
      let e = this.tt, r = this.W, n = r.U;
      return e.tt === this ? e.tt = r : e.U === this ? e.U = r : e.W = r, r.tt = e, r.U = this, this.tt = r, this.W = n, n && (n.tt = this), r;
    }
    se() {
      let e = this.tt, r = this.U, n = r.W;
      return e.tt === this ? e.tt = r : e.U === this ? e.U = r : e.W = r, r.tt = e, r.W = this, this.tt = r, this.U = n, n && (n.tt = this), r;
    }
  };
  wn.TreeNode = Co;
  var nu = class extends Co {
    constructor() {
      super(...arguments), this.rt = 1;
    }
    te() {
      let e = super.te();
      return this.ie(), e.ie(), e;
    }
    se() {
      let e = super.se();
      return this.ie(), e.ie(), e;
    }
    ie() {
      this.rt = 1, this.U && (this.rt += this.U.rt), this.W && (this.rt += this.W.rt);
    }
  };
  wn.TreeNodeEnableIndex = nu;
});
var ou = D((Bo) => {
  A();
  x();
  I();
  Object.defineProperty(Bo, "t", { value: true });
  Bo.default = void 0;
  var Oy = Py(), i1 = It(), Ry = xt(), iu = class extends i1.Container {
    constructor(e = function(n, i) {
      return n < i ? -1 : n > i ? 1 : 0;
    }, r = false) {
      super(), this.Y = void 0, this.v = e, r ? (this.re = Oy.TreeNodeEnableIndex, this.M = function(n, i, s) {
        let o = this.ne(n, i, s);
        if (o) {
          let a = o.tt;
          for (; a !== this.h; ) a.rt += 1, a = a.tt;
          let c = this.he(o);
          if (c) {
            let { parentNode: f, grandParent: h2, curNode: d } = c;
            f.ie(), h2.ie(), d.ie();
          }
        }
        return this.i;
      }, this.V = function(n) {
        let i = this.fe(n);
        for (; i !== this.h; ) i.rt -= 1, i = i.tt;
      }) : (this.re = Oy.TreeNode, this.M = function(n, i, s) {
        let o = this.ne(n, i, s);
        return o && this.he(o), this.i;
      }, this.V = this.fe), this.h = new this.re();
    }
    X(e, r) {
      let n = this.h;
      for (; e; ) {
        let i = this.v(e.u, r);
        if (i < 0) e = e.W;
        else if (i > 0) n = e, e = e.U;
        else return e;
      }
      return n;
    }
    Z(e, r) {
      let n = this.h;
      for (; e; ) this.v(e.u, r) <= 0 ? e = e.W : (n = e, e = e.U);
      return n;
    }
    $(e, r) {
      let n = this.h;
      for (; e; ) {
        let i = this.v(e.u, r);
        if (i < 0) n = e, e = e.W;
        else if (i > 0) e = e.U;
        else return e;
      }
      return n;
    }
    rr(e, r) {
      let n = this.h;
      for (; e; ) this.v(e.u, r) < 0 ? (n = e, e = e.W) : e = e.U;
      return n;
    }
    ue(e) {
      for (; ; ) {
        let r = e.tt;
        if (r === this.h) return;
        if (e.ee === 1) {
          e.ee = 0;
          return;
        }
        if (e === r.U) {
          let n = r.W;
          if (n.ee === 1) n.ee = 0, r.ee = 1, r === this.Y ? this.Y = r.te() : r.te();
          else if (n.W && n.W.ee === 1) {
            n.ee = r.ee, r.ee = 0, n.W.ee = 0, r === this.Y ? this.Y = r.te() : r.te();
            return;
          } else n.U && n.U.ee === 1 ? (n.ee = 1, n.U.ee = 0, n.se()) : (n.ee = 1, e = r);
        } else {
          let n = r.U;
          if (n.ee === 1) n.ee = 0, r.ee = 1, r === this.Y ? this.Y = r.se() : r.se();
          else if (n.U && n.U.ee === 1) {
            n.ee = r.ee, r.ee = 0, n.U.ee = 0, r === this.Y ? this.Y = r.se() : r.se();
            return;
          } else n.W && n.W.ee === 1 ? (n.ee = 1, n.W.ee = 0, n.te()) : (n.ee = 1, e = r);
        }
      }
    }
    fe(e) {
      if (this.i === 1) return this.clear(), this.h;
      let r = e;
      for (; r.U || r.W; ) {
        if (r.W) for (r = r.W; r.U; ) r = r.U;
        else r = r.U;
        [e.u, r.u] = [r.u, e.u], [e.l, r.l] = [r.l, e.l], e = r;
      }
      this.h.U === r ? this.h.U = r.tt : this.h.W === r && (this.h.W = r.tt), this.ue(r);
      let n = r.tt;
      return r === n.U ? n.U = void 0 : n.W = void 0, this.i -= 1, this.Y.ee = 0, n;
    }
    oe(e, r) {
      return e === void 0 ? false : this.oe(e.U, r) || r(e) ? true : this.oe(e.W, r);
    }
    he(e) {
      for (; ; ) {
        let r = e.tt;
        if (r.ee === 0) return;
        let n = r.tt;
        if (r === n.U) {
          let i = n.W;
          if (i && i.ee === 1) {
            if (i.ee = r.ee = 0, n === this.Y) return;
            n.ee = 1, e = n;
            continue;
          } else if (e === r.W) {
            if (e.ee = 0, e.U && (e.U.tt = r), e.W && (e.W.tt = n), r.W = e.U, n.U = e.W, e.U = r, e.W = n, n === this.Y) this.Y = e, this.h.tt = e;
            else {
              let s = n.tt;
              s.U === n ? s.U = e : s.W = e;
            }
            return e.tt = n.tt, r.tt = e, n.tt = e, n.ee = 1, { parentNode: r, grandParent: n, curNode: e };
          } else r.ee = 0, n === this.Y ? this.Y = n.se() : n.se(), n.ee = 1;
        } else {
          let i = n.U;
          if (i && i.ee === 1) {
            if (i.ee = r.ee = 0, n === this.Y) return;
            n.ee = 1, e = n;
            continue;
          } else if (e === r.U) {
            if (e.ee = 0, e.U && (e.U.tt = n), e.W && (e.W.tt = r), n.W = e.U, r.U = e.W, e.U = n, e.W = r, n === this.Y) this.Y = e, this.h.tt = e;
            else {
              let s = n.tt;
              s.U === n ? s.U = e : s.W = e;
            }
            return e.tt = n.tt, r.tt = e, n.tt = e, n.ee = 1, { parentNode: r, grandParent: n, curNode: e };
          } else r.ee = 0, n === this.Y ? this.Y = n.te() : n.te(), n.ee = 1;
        }
        return;
      }
    }
    ne(e, r, n) {
      if (this.Y === void 0) {
        this.i += 1, this.Y = new this.re(e, r), this.Y.ee = 0, this.Y.tt = this.h, this.h.tt = this.Y, this.h.U = this.Y, this.h.W = this.Y;
        return;
      }
      let i, s = this.h.U, o = this.v(s.u, e);
      if (o === 0) {
        s.l = r;
        return;
      } else if (o > 0) s.U = new this.re(e, r), s.U.tt = s, i = s.U, this.h.U = i;
      else {
        let a = this.h.W, c = this.v(a.u, e);
        if (c === 0) {
          a.l = r;
          return;
        } else if (c < 0) a.W = new this.re(e, r), a.W.tt = a, i = a.W, this.h.W = i;
        else {
          if (n !== void 0) {
            let f = n.o;
            if (f !== this.h) {
              let h2 = this.v(f.u, e);
              if (h2 === 0) {
                f.l = r;
                return;
              } else if (h2 > 0) {
                let d = f.L(), w = this.v(d.u, e);
                if (w === 0) {
                  d.l = r;
                  return;
                } else w < 0 && (i = new this.re(e, r), d.W === void 0 ? (d.W = i, i.tt = d) : (f.U = i, i.tt = f));
              }
            }
          }
          if (i === void 0) for (i = this.Y; ; ) {
            let f = this.v(i.u, e);
            if (f > 0) {
              if (i.U === void 0) {
                i.U = new this.re(e, r), i.U.tt = i, i = i.U;
                break;
              }
              i = i.U;
            } else if (f < 0) {
              if (i.W === void 0) {
                i.W = new this.re(e, r), i.W.tt = i, i = i.W;
                break;
              }
              i = i.W;
            } else {
              i.l = r;
              return;
            }
          }
        }
      }
      return this.i += 1, i;
    }
    I(e, r) {
      for (; e; ) {
        let n = this.v(e.u, r);
        if (n < 0) e = e.W;
        else if (n > 0) e = e.U;
        else return e;
      }
      return e || this.h;
    }
    clear() {
      this.i = 0, this.Y = void 0, this.h.tt = void 0, this.h.U = this.h.W = void 0;
    }
    updateKeyByIterator(e, r) {
      let n = e.o;
      if (n === this.h && (0, Ry.throwIteratorAccessError)(), this.i === 1) return n.u = r, true;
      if (n === this.h.U) return this.v(n.B().u, r) > 0 ? (n.u = r, true) : false;
      if (n === this.h.W) return this.v(n.L().u, r) < 0 ? (n.u = r, true) : false;
      let i = n.L().u;
      if (this.v(i, r) >= 0) return false;
      let s = n.B().u;
      return this.v(s, r) <= 0 ? false : (n.u = r, true);
    }
    eraseElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      let r = 0, n = this;
      return this.oe(this.Y, function(i) {
        return e === r ? (n.V(i), true) : (r += 1, false);
      }), this.i;
    }
    eraseElementByKey(e) {
      if (this.i === 0) return false;
      let r = this.I(this.Y, e);
      return r === this.h ? false : (this.V(r), true);
    }
    eraseElementByIterator(e) {
      let r = e.o;
      r === this.h && (0, Ry.throwIteratorAccessError)();
      let n = r.W === void 0;
      return e.iteratorType === 0 ? n && e.next() : (!n || r.U === void 0) && e.next(), this.V(r), e;
    }
    forEach(e) {
      let r = 0;
      for (let n of this) e(n, r++, this);
    }
    getElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      let r, n = 0;
      for (let i of this) {
        if (n === e) {
          r = i;
          break;
        }
        n += 1;
      }
      return r;
    }
    getHeight() {
      if (this.i === 0) return 0;
      let e = function(r) {
        return r ? Math.max(e(r.U), e(r.W)) + 1 : 0;
      };
      return e(this.Y);
    }
  }, o1 = iu;
  Bo.default = o1;
});
var au = D((No) => {
  A();
  x();
  I();
  Object.defineProperty(No, "t", { value: true });
  No.default = void 0;
  var s1 = It(), Mo = xt(), su = class extends s1.ContainerIterator {
    constructor(e, r, n) {
      super(n), this.o = e, this.h = r, this.iteratorType === 0 ? (this.pre = function() {
        return this.o === this.h.U && (0, Mo.throwIteratorAccessError)(), this.o = this.o.L(), this;
      }, this.next = function() {
        return this.o === this.h && (0, Mo.throwIteratorAccessError)(), this.o = this.o.B(), this;
      }) : (this.pre = function() {
        return this.o === this.h.W && (0, Mo.throwIteratorAccessError)(), this.o = this.o.B(), this;
      }, this.next = function() {
        return this.o === this.h && (0, Mo.throwIteratorAccessError)(), this.o = this.o.L(), this;
      });
    }
    get index() {
      let e = this.o, r = this.h.tt;
      if (e === this.h) return r ? r.rt - 1 : 0;
      let n = 0;
      for (e.U && (n += e.U.rt); e !== r; ) {
        let i = e.tt;
        e === i.W && (n += 1, i.U && (n += i.U.rt)), e = i;
      }
      return n;
    }
  }, a1 = su;
  No.default = a1;
});
var Cy = D((qo) => {
  A();
  x();
  I();
  Object.defineProperty(qo, "t", { value: true });
  qo.default = void 0;
  var u1 = ky(ou()), l1 = ky(au()), c1 = xt();
  function ky(t) {
    return t && t.t ? t : { default: t };
  }
  var dt = class t extends l1.default {
    constructor(e, r, n, i) {
      super(e, r, i), this.container = n;
    }
    get pointer() {
      return this.o === this.h && (0, c1.throwIteratorAccessError)(), this.o.u;
    }
    copy() {
      return new t(this.o, this.h, this.container, this.iteratorType);
    }
  }, uu = class extends u1.default {
    constructor(e = [], r, n) {
      super(r, n);
      let i = this;
      e.forEach(function(s) {
        i.insert(s);
      });
    }
    *K(e) {
      e !== void 0 && (yield* this.K(e.U), yield e.u, yield* this.K(e.W));
    }
    begin() {
      return new dt(this.h.U || this.h, this.h, this);
    }
    end() {
      return new dt(this.h, this.h, this);
    }
    rBegin() {
      return new dt(this.h.W || this.h, this.h, this, 1);
    }
    rEnd() {
      return new dt(this.h, this.h, this, 1);
    }
    front() {
      return this.h.U ? this.h.U.u : void 0;
    }
    back() {
      return this.h.W ? this.h.W.u : void 0;
    }
    insert(e, r) {
      return this.M(e, void 0, r);
    }
    find(e) {
      let r = this.I(this.Y, e);
      return new dt(r, this.h, this);
    }
    lowerBound(e) {
      let r = this.X(this.Y, e);
      return new dt(r, this.h, this);
    }
    upperBound(e) {
      let r = this.Z(this.Y, e);
      return new dt(r, this.h, this);
    }
    reverseLowerBound(e) {
      let r = this.$(this.Y, e);
      return new dt(r, this.h, this);
    }
    reverseUpperBound(e) {
      let r = this.rr(this.Y, e);
      return new dt(r, this.h, this);
    }
    union(e) {
      let r = this;
      return e.forEach(function(n) {
        r.insert(n);
      }), this.i;
    }
    [Symbol.iterator]() {
      return this.K(this.Y);
    }
  }, f1 = uu;
  qo.default = f1;
});
var My = D((Lo) => {
  A();
  x();
  I();
  Object.defineProperty(Lo, "t", { value: true });
  Lo.default = void 0;
  var h1 = By(ou()), d1 = By(au()), p1 = xt();
  function By(t) {
    return t && t.t ? t : { default: t };
  }
  var pt = class t extends d1.default {
    constructor(e, r, n, i) {
      super(e, r, i), this.container = n;
    }
    get pointer() {
      this.o === this.h && (0, p1.throwIteratorAccessError)();
      let e = this;
      return new Proxy([], { get(r, n) {
        if (n === "0") return e.o.u;
        if (n === "1") return e.o.l;
      }, set(r, n, i) {
        if (n !== "1") throw new TypeError("props must be 1");
        return e.o.l = i, true;
      } });
    }
    copy() {
      return new t(this.o, this.h, this.container, this.iteratorType);
    }
  }, lu = class extends h1.default {
    constructor(e = [], r, n) {
      super(r, n);
      let i = this;
      e.forEach(function(s) {
        i.setElement(s[0], s[1]);
      });
    }
    *K(e) {
      e !== void 0 && (yield* this.K(e.U), yield [e.u, e.l], yield* this.K(e.W));
    }
    begin() {
      return new pt(this.h.U || this.h, this.h, this);
    }
    end() {
      return new pt(this.h, this.h, this);
    }
    rBegin() {
      return new pt(this.h.W || this.h, this.h, this, 1);
    }
    rEnd() {
      return new pt(this.h, this.h, this, 1);
    }
    front() {
      if (this.i === 0) return;
      let e = this.h.U;
      return [e.u, e.l];
    }
    back() {
      if (this.i === 0) return;
      let e = this.h.W;
      return [e.u, e.l];
    }
    lowerBound(e) {
      let r = this.X(this.Y, e);
      return new pt(r, this.h, this);
    }
    upperBound(e) {
      let r = this.Z(this.Y, e);
      return new pt(r, this.h, this);
    }
    reverseLowerBound(e) {
      let r = this.$(this.Y, e);
      return new pt(r, this.h, this);
    }
    reverseUpperBound(e) {
      let r = this.rr(this.Y, e);
      return new pt(r, this.h, this);
    }
    setElement(e, r, n) {
      return this.M(e, r, n);
    }
    find(e) {
      let r = this.I(this.Y, e);
      return new pt(r, this.h, this);
    }
    getElementByKey(e) {
      return this.I(this.Y, e).l;
    }
    union(e) {
      let r = this;
      return e.forEach(function(n) {
        r.setElement(n[0], n[1]);
      }), this.i;
    }
    [Symbol.iterator]() {
      return this.K(this.Y);
    }
  }, y1 = lu;
  Lo.default = y1;
});
var fu = D((cu) => {
  A();
  x();
  I();
  Object.defineProperty(cu, "t", { value: true });
  cu.default = g1;
  function g1(t) {
    let e = typeof t;
    return e === "object" && t !== null || e === "function";
  }
});
var yu = D((mn) => {
  A();
  x();
  I();
  Object.defineProperty(mn, "t", { value: true });
  mn.HashContainerIterator = mn.HashContainer = void 0;
  var Ny = It(), hu = b1(fu()), Kn = xt();
  function b1(t) {
    return t && t.t ? t : { default: t };
  }
  var du = class extends Ny.ContainerIterator {
    constructor(e, r, n) {
      super(n), this.o = e, this.h = r, this.iteratorType === 0 ? (this.pre = function() {
        return this.o.L === this.h && (0, Kn.throwIteratorAccessError)(), this.o = this.o.L, this;
      }, this.next = function() {
        return this.o === this.h && (0, Kn.throwIteratorAccessError)(), this.o = this.o.B, this;
      }) : (this.pre = function() {
        return this.o.B === this.h && (0, Kn.throwIteratorAccessError)(), this.o = this.o.B, this;
      }, this.next = function() {
        return this.o === this.h && (0, Kn.throwIteratorAccessError)(), this.o = this.o.L, this;
      });
    }
  };
  mn.HashContainerIterator = du;
  var pu = class extends Ny.Container {
    constructor() {
      super(), this.H = [], this.g = {}, this.HASH_TAG = Symbol("@@HASH_TAG"), Object.setPrototypeOf(this.g, null), this.h = {}, this.h.L = this.h.B = this.p = this._ = this.h;
    }
    V(e) {
      let { L: r, B: n } = e;
      r.B = n, n.L = r, e === this.p && (this.p = n), e === this._ && (this._ = r), this.i -= 1;
    }
    M(e, r, n) {
      n === void 0 && (n = (0, hu.default)(e));
      let i;
      if (n) {
        let s = e[this.HASH_TAG];
        if (s !== void 0) return this.H[s].l = r, this.i;
        Object.defineProperty(e, this.HASH_TAG, { value: this.H.length, configurable: true }), i = { u: e, l: r, L: this._, B: this.h }, this.H.push(i);
      } else {
        let s = this.g[e];
        if (s) return s.l = r, this.i;
        i = { u: e, l: r, L: this._, B: this.h }, this.g[e] = i;
      }
      return this.i === 0 ? (this.p = i, this.h.B = i) : this._.B = i, this._ = i, this.h.L = i, ++this.i;
    }
    I(e, r) {
      if (r === void 0 && (r = (0, hu.default)(e)), r) {
        let n = e[this.HASH_TAG];
        return n === void 0 ? this.h : this.H[n];
      } else return this.g[e] || this.h;
    }
    clear() {
      let e = this.HASH_TAG;
      this.H.forEach(function(r) {
        delete r.u[e];
      }), this.H = [], this.g = {}, Object.setPrototypeOf(this.g, null), this.i = 0, this.p = this._ = this.h.L = this.h.B = this.h;
    }
    eraseElementByKey(e, r) {
      let n;
      if (r === void 0 && (r = (0, hu.default)(e)), r) {
        let i = e[this.HASH_TAG];
        if (i === void 0) return false;
        delete e[this.HASH_TAG], n = this.H[i], delete this.H[i];
      } else {
        if (n = this.g[e], n === void 0) return false;
        delete this.g[e];
      }
      return this.V(n), true;
    }
    eraseElementByIterator(e) {
      let r = e.o;
      return r === this.h && (0, Kn.throwIteratorAccessError)(), this.V(r), e.next();
    }
    eraseElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      let r = this.p;
      for (; e--; ) r = r.B;
      return this.V(r), this.i;
    }
  };
  mn.HashContainer = pu;
});
var Ly = D((Uo) => {
  A();
  x();
  I();
  Object.defineProperty(Uo, "t", { value: true });
  Uo.default = void 0;
  var qy = yu(), w1 = xt(), Dr = class t extends qy.HashContainerIterator {
    constructor(e, r, n, i) {
      super(e, r, i), this.container = n;
    }
    get pointer() {
      return this.o === this.h && (0, w1.throwIteratorAccessError)(), this.o.u;
    }
    copy() {
      return new t(this.o, this.h, this.container, this.iteratorType);
    }
  }, gu = class extends qy.HashContainer {
    constructor(e = []) {
      super();
      let r = this;
      e.forEach(function(n) {
        r.insert(n);
      });
    }
    begin() {
      return new Dr(this.p, this.h, this);
    }
    end() {
      return new Dr(this.h, this.h, this);
    }
    rBegin() {
      return new Dr(this._, this.h, this, 1);
    }
    rEnd() {
      return new Dr(this.h, this.h, this, 1);
    }
    front() {
      return this.p.u;
    }
    back() {
      return this._.u;
    }
    insert(e, r) {
      return this.M(e, void 0, r);
    }
    getElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      let r = this.p;
      for (; e--; ) r = r.B;
      return r.u;
    }
    find(e, r) {
      let n = this.I(e, r);
      return new Dr(n, this.h, this);
    }
    forEach(e) {
      let r = 0, n = this.p;
      for (; n !== this.h; ) e(n.u, r++, this), n = n.B;
    }
    [Symbol.iterator]() {
      return (function* () {
        let e = this.p;
        for (; e !== this.h; ) yield e.u, e = e.B;
      }).bind(this)();
    }
  }, m1 = gu;
  Uo.default = m1;
});
var Dy = D((Do) => {
  A();
  x();
  I();
  Object.defineProperty(Do, "t", { value: true });
  Do.default = void 0;
  var Uy = yu(), _1 = S1(fu()), v1 = xt();
  function S1(t) {
    return t && t.t ? t : { default: t };
  }
  var jr = class t extends Uy.HashContainerIterator {
    constructor(e, r, n, i) {
      super(e, r, i), this.container = n;
    }
    get pointer() {
      this.o === this.h && (0, v1.throwIteratorAccessError)();
      let e = this;
      return new Proxy([], { get(r, n) {
        if (n === "0") return e.o.u;
        if (n === "1") return e.o.l;
      }, set(r, n, i) {
        if (n !== "1") throw new TypeError("props must be 1");
        return e.o.l = i, true;
      } });
    }
    copy() {
      return new t(this.o, this.h, this.container, this.iteratorType);
    }
  }, bu = class extends Uy.HashContainer {
    constructor(e = []) {
      super();
      let r = this;
      e.forEach(function(n) {
        r.setElement(n[0], n[1]);
      });
    }
    begin() {
      return new jr(this.p, this.h, this);
    }
    end() {
      return new jr(this.h, this.h, this);
    }
    rBegin() {
      return new jr(this._, this.h, this, 1);
    }
    rEnd() {
      return new jr(this.h, this.h, this, 1);
    }
    front() {
      if (this.i !== 0) return [this.p.u, this.p.l];
    }
    back() {
      if (this.i !== 0) return [this._.u, this._.l];
    }
    setElement(e, r, n) {
      return this.M(e, r, n);
    }
    getElementByKey(e, r) {
      if (r === void 0 && (r = (0, _1.default)(e)), r) {
        let i = e[this.HASH_TAG];
        return i !== void 0 ? this.H[i].l : void 0;
      }
      let n = this.g[e];
      return n ? n.l : void 0;
    }
    getElementByPos(e) {
      if (e < 0 || e > this.i - 1) throw new RangeError();
      let r = this.p;
      for (; e--; ) r = r.B;
      return [r.u, r.l];
    }
    find(e, r) {
      let n = this.I(e, r);
      return new jr(n, this.h, this);
    }
    forEach(e) {
      let r = 0, n = this.p;
      for (; n !== this.h; ) e([n.u, n.l], r++, this), n = n.B;
    }
    [Symbol.iterator]() {
      return (function* () {
        let e = this.p;
        for (; e !== this.h; ) yield [e.u, e.l], e = e.B;
      }).bind(this)();
    }
  }, E1 = bu;
  Do.default = E1;
});
var jy = D((it) => {
  A();
  x();
  I();
  Object.defineProperty(it, "t", { value: true });
  Object.defineProperty(it, "Deque", { enumerable: true, get: function() {
    return O1.default;
  } });
  Object.defineProperty(it, "HashMap", { enumerable: true, get: function() {
    return B1.default;
  } });
  Object.defineProperty(it, "HashSet", { enumerable: true, get: function() {
    return C1.default;
  } });
  Object.defineProperty(it, "LinkList", { enumerable: true, get: function() {
    return P1.default;
  } });
  Object.defineProperty(it, "OrderedMap", { enumerable: true, get: function() {
    return k1.default;
  } });
  Object.defineProperty(it, "OrderedSet", { enumerable: true, get: function() {
    return R1.default;
  } });
  Object.defineProperty(it, "PriorityQueue", { enumerable: true, get: function() {
    return x1.default;
  } });
  Object.defineProperty(it, "Queue", { enumerable: true, get: function() {
    return I1.default;
  } });
  Object.defineProperty(it, "Stack", { enumerable: true, get: function() {
    return A1.default;
  } });
  Object.defineProperty(it, "Vector", { enumerable: true, get: function() {
    return T1.default;
  } });
  var A1 = Tt(Sy()), I1 = Tt(Ey()), x1 = Tt(Ay()), T1 = Tt(Iy()), P1 = Tt(xy()), O1 = Tt(Ty()), R1 = Tt(Cy()), k1 = Tt(My()), C1 = Tt(Ly()), B1 = Tt(Dy());
  function Tt(t) {
    return t && t.t ? t : { default: t };
  }
});
var Wy = D((cj, Fy) => {
  A();
  x();
  I();
  var M1 = jy().OrderedSet, Pt = At()("number-allocator:trace"), N1 = At()("number-allocator:error");
  function Ve(t, e) {
    this.low = t, this.high = e;
  }
  Ve.prototype.equals = function(t) {
    return this.low === t.low && this.high === t.high;
  };
  Ve.prototype.compare = function(t) {
    return this.low < t.low && this.high < t.low ? -1 : t.low < this.low && t.high < this.low ? 1 : 0;
  };
  function Ot(t, e) {
    if (!(this instanceof Ot)) return new Ot(t, e);
    this.min = t, this.max = e, this.ss = new M1([], (r, n) => r.compare(n)), Pt("Create"), this.clear();
  }
  Ot.prototype.firstVacant = function() {
    return this.ss.size() === 0 ? null : this.ss.front().low;
  };
  Ot.prototype.alloc = function() {
    if (this.ss.size() === 0) return Pt("alloc():empty"), null;
    let t = this.ss.begin(), e = t.pointer.low, r = t.pointer.high, n = e;
    return n + 1 <= r ? this.ss.updateKeyByIterator(t, new Ve(e + 1, r)) : this.ss.eraseElementByPos(0), Pt("alloc():" + n), n;
  };
  Ot.prototype.use = function(t) {
    let e = new Ve(t, t), r = this.ss.lowerBound(e);
    if (!r.equals(this.ss.end())) {
      let n = r.pointer.low, i = r.pointer.high;
      return r.pointer.equals(e) ? (this.ss.eraseElementByIterator(r), Pt("use():" + t), true) : n > t ? false : n === t ? (this.ss.updateKeyByIterator(r, new Ve(n + 1, i)), Pt("use():" + t), true) : i === t ? (this.ss.updateKeyByIterator(r, new Ve(n, i - 1)), Pt("use():" + t), true) : (this.ss.updateKeyByIterator(r, new Ve(t + 1, i)), this.ss.insert(new Ve(n, t - 1)), Pt("use():" + t), true);
    }
    return Pt("use():failed"), false;
  };
  Ot.prototype.free = function(t) {
    if (t < this.min || t > this.max) {
      N1("free():" + t + " is out of range");
      return;
    }
    let e = new Ve(t, t), r = this.ss.upperBound(e);
    if (r.equals(this.ss.end())) {
      if (r.equals(this.ss.begin())) {
        this.ss.insert(e);
        return;
      }
      r.pre();
      let n = r.pointer.high;
      r.pointer.high + 1 === t ? this.ss.updateKeyByIterator(r, new Ve(n, t)) : this.ss.insert(e);
    } else if (r.equals(this.ss.begin())) if (t + 1 === r.pointer.low) {
      let n = r.pointer.high;
      this.ss.updateKeyByIterator(r, new Ve(t, n));
    } else this.ss.insert(e);
    else {
      let n = r.pointer.low, i = r.pointer.high;
      r.pre();
      let s = r.pointer.low;
      r.pointer.high + 1 === t ? t + 1 === n ? (this.ss.eraseElementByIterator(r), this.ss.updateKeyByIterator(r, new Ve(s, i))) : this.ss.updateKeyByIterator(r, new Ve(s, t)) : t + 1 === n ? (this.ss.eraseElementByIterator(r.next()), this.ss.insert(new Ve(t, i))) : this.ss.insert(e);
    }
    Pt("free():" + t);
  };
  Ot.prototype.clear = function() {
    Pt("clear()"), this.ss.clear(), this.ss.insert(new Ve(this.min, this.max));
  };
  Ot.prototype.intervalCount = function() {
    return this.ss.size();
  };
  Ot.prototype.dump = function() {
    console.log("length:" + this.ss.size());
    for (let t of this.ss) console.log(t);
  };
  Fy.exports = Ot;
});
var wu = D((_j, $y) => {
  A();
  x();
  I();
  var q1 = Wy();
  $y.exports.NumberAllocator = q1;
});
var Hy = D((_u) => {
  A();
  x();
  I();
  Object.defineProperty(_u, "__esModule", { value: true });
  var L1 = vy(), U1 = wu(), mu = class {
    aliasToTopic;
    topicToAlias;
    max;
    numberAllocator;
    length;
    constructor(e) {
      e > 0 && (this.aliasToTopic = new L1.LRUCache({ max: e }), this.topicToAlias = {}, this.numberAllocator = new U1.NumberAllocator(1, e), this.max = e, this.length = 0);
    }
    put(e, r) {
      if (r === 0 || r > this.max) return false;
      let n = this.aliasToTopic.get(r);
      return n && delete this.topicToAlias[n], this.aliasToTopic.set(r, e), this.topicToAlias[e] = r, this.numberAllocator.use(r), this.length = this.aliasToTopic.size, true;
    }
    getTopicByAlias(e) {
      return this.aliasToTopic.get(e);
    }
    getAliasByTopic(e) {
      let r = this.topicToAlias[e];
      return typeof r < "u" && this.aliasToTopic.get(r), r;
    }
    clear() {
      this.aliasToTopic.clear(), this.topicToAlias = {}, this.numberAllocator.clear(), this.length = 0;
    }
    getLruAlias() {
      let e = this.numberAllocator.firstVacant();
      return e || [...this.aliasToTopic.keys()][this.aliasToTopic.size - 1];
    }
  };
  _u.default = mu;
});
var Vy = D((Qn) => {
  A();
  x();
  I();
  var D1 = Qn && Qn.__importDefault || function(t) {
    return t && t.__esModule ? t : { default: t };
  };
  Object.defineProperty(Qn, "__esModule", { value: true });
  var j1 = zn(), F1 = D1(Hy()), W1 = Mr(), $1 = (t, e) => {
    t.log("_handleConnack");
    let { options: r } = t, i = r.protocolVersion === 5 ? e.reasonCode : e.returnCode;
    if (clearTimeout(t.connackTimer), delete t.topicAliasSend, e.properties) {
      if (e.properties.topicAliasMaximum) {
        if (e.properties.topicAliasMaximum > 65535) {
          t.emit("error", new Error("topicAliasMaximum from broker is out of range"));
          return;
        }
        e.properties.topicAliasMaximum > 0 && (t.topicAliasSend = new F1.default(e.properties.topicAliasMaximum));
      }
      e.properties.serverKeepAlive && r.keepalive && (r.keepalive = e.properties.serverKeepAlive), e.properties.maximumPacketSize && (r.properties || (r.properties = {}), r.properties.maximumPacketSize = e.properties.maximumPacketSize);
    }
    if (i === 0) t.reconnecting = false, t._onConnect(e);
    else if (i > 0) {
      let s = new W1.ErrorWithReasonCode(`Connection refused: ${j1.ReasonCodes[i]}`, i);
      t.emit("error", s), t.options.reconnectOnConnackError && t._cleanUp(true);
    }
  };
  Qn.default = $1;
});
var Gy = D((vu) => {
  A();
  x();
  I();
  Object.defineProperty(vu, "__esModule", { value: true });
  var H1 = (t, e, r) => {
    t.log("handling pubrel packet");
    let n = typeof r < "u" ? r : t.noop, { messageId: i } = e, s = { cmd: "pubcomp", messageId: i };
    t.incomingStore.get(e, (o, a) => {
      o ? t._sendPacket(s, n) : (t.emit("message", a.topic, a.payload, a), t.handleMessage(a, (c) => {
        if (c) return n(c);
        t.incomingStore.del(a, t.noop), t._sendPacket(s, n);
      }));
    });
  };
  vu.default = H1;
});
var zy = D((Yn) => {
  A();
  x();
  I();
  var Jn = Yn && Yn.__importDefault || function(t) {
    return t && t.__esModule ? t : { default: t };
  };
  Object.defineProperty(Yn, "__esModule", { value: true });
  var V1 = Jn(hy()), G1 = Jn(gy()), z1 = Jn(Vy()), K1 = Jn(zn()), Q1 = Jn(Gy()), Y1 = (t, e, r) => {
    let { options: n } = t;
    if (n.protocolVersion === 5 && n.properties && n.properties.maximumPacketSize && n.properties.maximumPacketSize < e.length) return t.emit("error", new Error(`exceeding packets size ${e.cmd}`)), t.end({ reasonCode: 149, properties: { reasonString: "Maximum packet size was exceeded" } }), t;
    switch (t.log("_handlePacket :: emitting packetreceive"), t.emit("packetreceive", e), e.cmd) {
      case "publish":
        (0, V1.default)(t, e, r);
        break;
      case "puback":
      case "pubrec":
      case "pubcomp":
      case "suback":
      case "unsuback":
        t.reschedulePing(), (0, K1.default)(t, e), r();
        break;
      case "pubrel":
        t.reschedulePing(), (0, Q1.default)(t, e, r);
        break;
      case "connack":
        (0, z1.default)(t, e), r();
        break;
      case "auth":
        t.reschedulePing(), (0, G1.default)(t, e), r();
        break;
      case "pingresp":
        t.log("_handlePacket :: received pingresp"), t.reschedulePing(true), r();
        break;
      case "disconnect":
        t.emit("disconnect", e), r();
        break;
      default:
        t.log("_handlePacket :: unknown command"), r();
        break;
    }
  };
  Yn.default = Y1;
});
var Au = D((Eu) => {
  A();
  x();
  I();
  Object.defineProperty(Eu, "__esModule", { value: true });
  var Su = class {
    nextId;
    constructor() {
      this.nextId = Math.max(1, Math.floor(Math.random() * 65535));
    }
    allocate() {
      let e = this.nextId++;
      return this.nextId === 65536 && (this.nextId = 1), e;
    }
    getLastAllocated() {
      return this.nextId === 1 ? 65535 : this.nextId - 1;
    }
    register(e) {
      return true;
    }
    deallocate(e) {
    }
    clear() {
    }
  };
  Eu.default = Su;
});
var Ky = D((xu) => {
  A();
  x();
  I();
  Object.defineProperty(xu, "__esModule", { value: true });
  var Iu = class {
    aliasToTopic;
    max;
    length;
    constructor(e) {
      this.aliasToTopic = {}, this.max = e;
    }
    put(e, r) {
      return r === 0 || r > this.max ? false : (this.aliasToTopic[r] = e, this.length = Object.keys(this.aliasToTopic).length, true);
    }
    getTopicByAlias(e) {
      return this.aliasToTopic[e];
    }
    clear() {
      this.aliasToTopic = {};
    }
  };
  xu.default = Iu;
});
var Qy = D((_n) => {
  A();
  x();
  I();
  var J1 = _n && _n.__importDefault || function(t) {
    return t && t.__esModule ? t : { default: t };
  };
  Object.defineProperty(_n, "__esModule", { value: true });
  _n.TypedEventEmitter = void 0;
  var X1 = J1((er(), me(Zt))), Z1 = Mr(), jo = class {
  };
  _n.TypedEventEmitter = jo;
  (0, Z1.applyMixin)(jo, X1.default);
});
var Xn = D((GF, Wt) => {
  A();
  x();
  I();
  function Tu(t) {
    "@babel/helpers - typeof";
    return Wt.exports = Tu = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(e) {
      return typeof e;
    } : function(e) {
      return e && typeof Symbol == "function" && e.constructor === Symbol && e !== Symbol.prototype ? "symbol" : typeof e;
    }, Wt.exports.__esModule = true, Wt.exports.default = Wt.exports, Tu(t);
  }
  Wt.exports = Tu, Wt.exports.__esModule = true, Wt.exports.default = Wt.exports;
});
var Jy = D((r8, Zn) => {
  A();
  x();
  I();
  var Yy = Xn().default;
  function eA(t, e) {
    if (Yy(t) != "object" || !t) return t;
    var r = t[Symbol.toPrimitive];
    if (r !== void 0) {
      var n = r.call(t, e || "default");
      if (Yy(n) != "object") return n;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return (e === "string" ? String : Number)(t);
  }
  Zn.exports = eA, Zn.exports.__esModule = true, Zn.exports.default = Zn.exports;
});
var Xy = D((h8, ei) => {
  A();
  x();
  I();
  var tA = Xn().default, rA = Jy();
  function nA(t) {
    var e = rA(t, "string");
    return tA(e) == "symbol" ? e : e + "";
  }
  ei.exports = nA, ei.exports.__esModule = true, ei.exports.default = ei.exports;
});
var Zy = D((S8, ti) => {
  A();
  x();
  I();
  var iA = Xy();
  function oA(t, e, r) {
    return (e = iA(e)) in t ? Object.defineProperty(t, e, { value: r, enumerable: true, configurable: true, writable: true }) : t[e] = r, t;
  }
  ti.exports = oA, ti.exports.__esModule = true, ti.exports.default = ti.exports;
});
var eg = D((C8, ri) => {
  A();
  x();
  I();
  function sA(t) {
    if (Array.isArray(t)) return t;
  }
  ri.exports = sA, ri.exports.__esModule = true, ri.exports.default = ri.exports;
});
var tg = D((W8, ni) => {
  A();
  x();
  I();
  function aA(t, e) {
    var r = t == null ? null : typeof Symbol < "u" && t[Symbol.iterator] || t["@@iterator"];
    if (r != null) {
      var n, i, s, o, a = [], c = true, f = false;
      try {
        if (s = (r = r.call(t)).next, e === 0) {
          if (Object(r) !== r) return;
          c = false;
        } else for (; !(c = (n = s.call(r)).done) && (a.push(n.value), a.length !== e); c = true) ;
      } catch (h2) {
        f = true, i = h2;
      } finally {
        try {
          if (!c && r.return != null && (o = r.return(), Object(o) !== o)) return;
        } finally {
          if (f) throw i;
        }
      }
      return a;
    }
  }
  ni.exports = aA, ni.exports.__esModule = true, ni.exports.default = ni.exports;
});
var rg = D((X8, ii) => {
  A();
  x();
  I();
  function uA(t, e) {
    (e == null || e > t.length) && (e = t.length);
    for (var r = 0, n = Array(e); r < e; r++) n[r] = t[r];
    return n;
  }
  ii.exports = uA, ii.exports.__esModule = true, ii.exports.default = ii.exports;
});
var ig = D((u6, oi) => {
  A();
  x();
  I();
  var ng = rg();
  function lA(t, e) {
    if (t) {
      if (typeof t == "string") return ng(t, e);
      var r = {}.toString.call(t).slice(8, -1);
      return r === "Object" && t.constructor && (r = t.constructor.name), r === "Map" || r === "Set" ? Array.from(t) : r === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r) ? ng(t, e) : void 0;
    }
  }
  oi.exports = lA, oi.exports.__esModule = true, oi.exports.default = oi.exports;
});
var og = D((w6, si) => {
  A();
  x();
  I();
  function cA() {
    throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
  }
  si.exports = cA, si.exports.__esModule = true, si.exports.default = si.exports;
});
var sg = D((P6, ai) => {
  A();
  x();
  I();
  var fA = eg(), hA = tg(), dA = ig(), pA = og();
  function yA(t, e) {
    return fA(t) || hA(t, e) || dA(t, e) || pA();
  }
  ai.exports = yA, ai.exports.__esModule = true, ai.exports.default = ai.exports;
});
var Pu = D((Fo, ag) => {
  A();
  x();
  I();
  (function(t, e) {
    typeof Fo == "object" && typeof ag < "u" ? e(Fo) : typeof define == "function" && define.amd ? define(["exports"], e) : (t = typeof globalThis < "u" ? globalThis : t || self, e(t.fastUniqueNumbers = {}));
  })(Fo, function(t) {
    var e = function(w) {
      return function(y) {
        var P = w(y);
        return y.add(P), P;
      };
    }, r = function(w) {
      return function(y, P) {
        return w.set(y, P), P;
      };
    }, n = Number.MAX_SAFE_INTEGER === void 0 ? 9007199254740991 : Number.MAX_SAFE_INTEGER, i = 536870912, s = i * 2, o = function(w, y) {
      return function(P) {
        var E = y.get(P), b = E === void 0 ? P.size : E < s ? E + 1 : 0;
        if (!P.has(b)) return w(P, b);
        if (P.size < i) {
          for (; P.has(b); ) b = Math.floor(Math.random() * s);
          return w(P, b);
        }
        if (P.size > n) throw new Error("Congratulations, you created a collection of unique numbers which uses all available integers!");
        for (; P.has(b); ) b = Math.floor(Math.random() * n);
        return w(P, b);
      };
    }, a = /* @__PURE__ */ new WeakMap(), c = r(a), f = o(c, a), h2 = e(f);
    t.addUniqueNumber = h2, t.generateUniqueNumber = f;
  });
});
var lg = D((z6, ui) => {
  A();
  x();
  I();
  function ug(t, e, r, n, i, s, o) {
    try {
      var a = t[s](o), c = a.value;
    } catch (f) {
      return void r(f);
    }
    a.done ? e(c) : Promise.resolve(c).then(n, i);
  }
  function gA(t) {
    return function() {
      var e = this, r = arguments;
      return new Promise(function(n, i) {
        var s = t.apply(e, r);
        function o(c) {
          ug(s, n, i, o, a, "next", c);
        }
        function a(c) {
          ug(s, n, i, o, a, "throw", c);
        }
        o(void 0);
      });
    };
  }
  ui.exports = gA, ui.exports.__esModule = true, ui.exports.default = ui.exports;
});
var Ou = D((nW, li) => {
  A();
  x();
  I();
  function bA(t, e) {
    this.v = t, this.k = e;
  }
  li.exports = bA, li.exports.__esModule = true, li.exports.default = li.exports;
});
var Ru = D((dW, $t) => {
  A();
  x();
  I();
  function Wo(t, e, r, n) {
    var i = Object.defineProperty;
    try {
      i({}, "", {});
    } catch {
      i = 0;
    }
    $t.exports = Wo = function(o, a, c, f) {
      function h2(d, w) {
        Wo(o, d, function(y) {
          return this._invoke(d, w, y);
        });
      }
      a ? i ? i(o, a, { value: c, enumerable: !f, configurable: !f, writable: !f }) : o[a] = c : (h2("next", 0), h2("throw", 1), h2("return", 2));
    }, $t.exports.__esModule = true, $t.exports.default = $t.exports, Wo(t, e, r, n);
  }
  $t.exports = Wo, $t.exports.__esModule = true, $t.exports.default = $t.exports;
});
var ku = D((EW, Ht) => {
  A();
  x();
  I();
  var Rt = Ru();
  function cg() {
    var t, e, r = typeof Symbol == "function" ? Symbol : {}, n = r.iterator || "@@iterator", i = r.toStringTag || "@@toStringTag";
    function s(y, P, E, b) {
      var m = P && P.prototype instanceof a ? P : a, v = Object.create(m.prototype);
      return Rt(v, "_invoke", (function(R, T, g) {
        var O, _, q, $ = 0, j = g || [], J = false, W = { p: 0, n: 0, v: t, a: H, f: H.bind(t, 4), d: function(K, X) {
          return O = K, _ = 0, q = t, W.n = X, o;
        } };
        function H(G, K) {
          for (_ = G, q = K, e = 0; !J && $ && !X && e < j.length; e++) {
            var X, Q = j[e], V = W.p, te = Q[2];
            G > 3 ? (X = te === K) && (q = Q[(_ = Q[4]) ? 5 : (_ = 3, 3)], Q[4] = Q[5] = t) : Q[0] <= V && ((X = G < 2 && V < Q[1]) ? (_ = 0, W.v = K, W.n = Q[1]) : V < te && (X = G < 3 || Q[0] > K || K > te) && (Q[4] = G, Q[5] = K, W.n = te, _ = 0));
          }
          if (X || G > 1) return o;
          throw J = true, K;
        }
        return function(G, K, X) {
          if ($ > 1) throw TypeError("Generator is already running");
          for (J && K === 1 && H(K, X), _ = K, q = X; (e = _ < 2 ? t : q) || !J; ) {
            O || (_ ? _ < 3 ? (_ > 1 && (W.n = -1), H(_, q)) : W.n = q : W.v = q);
            try {
              if ($ = 2, O) {
                if (_ || (G = "next"), e = O[G]) {
                  if (!(e = e.call(O, q))) throw TypeError("iterator result is not an object");
                  if (!e.done) return e;
                  q = e.value, _ < 2 && (_ = 0);
                } else _ === 1 && (e = O.return) && e.call(O), _ < 2 && (q = TypeError("The iterator does not provide a '" + G + "' method"), _ = 1);
                O = t;
              } else if ((e = (J = W.n < 0) ? q : R.call(T, W)) !== o) break;
            } catch (Q) {
              O = t, _ = 1, q = Q;
            } finally {
              $ = 1;
            }
          }
          return { value: e, done: J };
        };
      })(y, E, b), true), v;
    }
    var o = {};
    function a() {
    }
    function c() {
    }
    function f() {
    }
    e = Object.getPrototypeOf;
    var h2 = [][n] ? e(e([][n]())) : (Rt(e = {}, n, function() {
      return this;
    }), e), d = f.prototype = a.prototype = Object.create(h2);
    function w(y) {
      return Object.setPrototypeOf ? Object.setPrototypeOf(y, f) : (y.__proto__ = f, Rt(y, i, "GeneratorFunction")), y.prototype = Object.create(d), y;
    }
    return c.prototype = f, Rt(d, "constructor", f), Rt(f, "constructor", c), c.displayName = "GeneratorFunction", Rt(f, i, "GeneratorFunction"), Rt(d), Rt(d, i, "Generator"), Rt(d, n, function() {
      return this;
    }), Rt(d, "toString", function() {
      return "[object Generator]";
    }), (Ht.exports = cg = function() {
      return { w: s, m: w };
    }, Ht.exports.__esModule = true, Ht.exports.default = Ht.exports)();
  }
  Ht.exports = cg, Ht.exports.__esModule = true, Ht.exports.default = Ht.exports;
});
var Mu = D((BW, ci) => {
  A();
  x();
  I();
  var wA = Ou(), Cu = Ru();
  function Bu(t, e) {
    function r(i, s, o, a) {
      try {
        var c = t[i](s), f = c.value;
        return f instanceof wA ? e.resolve(f.v).then(function(h2) {
          r("next", h2, o, a);
        }, function(h2) {
          r("throw", h2, o, a);
        }) : e.resolve(f).then(function(h2) {
          c.value = h2, o(c);
        }, function(h2) {
          return r("throw", h2, o, a);
        });
      } catch (h2) {
        a(h2);
      }
    }
    var n;
    this.next || (Cu(Bu.prototype), Cu(Bu.prototype, typeof Symbol == "function" && Symbol.asyncIterator || "@asyncIterator", function() {
      return this;
    })), Cu(this, "_invoke", function(i, s, o) {
      function a() {
        return new e(function(c, f) {
          r(i, o, c, f);
        });
      }
      return n = n ? n.then(a, a) : a();
    }, true);
  }
  ci.exports = Bu, ci.exports.__esModule = true, ci.exports.default = ci.exports;
});
var Nu = D(($W, fi) => {
  A();
  x();
  I();
  var mA = ku(), _A = Mu();
  function vA(t, e, r, n, i) {
    return new _A(mA().w(t, e, r, n), i || Promise);
  }
  fi.exports = vA, fi.exports.__esModule = true, fi.exports.default = fi.exports;
});
var fg = D((ZW, hi) => {
  A();
  x();
  I();
  var SA = Nu();
  function EA(t, e, r, n, i) {
    var s = SA(t, e, r, n, i);
    return s.next().then(function(o) {
      return o.done ? o.value : s.next();
    });
  }
  hi.exports = EA, hi.exports.__esModule = true, hi.exports.default = hi.exports;
});
var hg = D((l4, di) => {
  A();
  x();
  I();
  function AA(t) {
    var e = Object(t), r = [];
    for (var n in e) r.unshift(n);
    return function i() {
      for (; r.length; ) if ((n = r.pop()) in e) return i.value = n, i.done = false, i;
      return i.done = true, i;
    };
  }
  di.exports = AA, di.exports.__esModule = true, di.exports.default = di.exports;
});
var dg = D((m4, pi) => {
  A();
  x();
  I();
  var IA = Xn().default;
  function xA(t) {
    if (t != null) {
      var e = t[typeof Symbol == "function" && Symbol.iterator || "@@iterator"], r = 0;
      if (e) return e.call(t);
      if (typeof t.next == "function") return t;
      if (!isNaN(t.length)) return { next: function() {
        return t && r >= t.length && (t = void 0), { value: t && t[r++], done: !t };
      } };
    }
    throw new TypeError(IA(t) + " is not iterable");
  }
  pi.exports = xA, pi.exports.__esModule = true, pi.exports.default = pi.exports;
});
var yg = D((O4, Vt) => {
  A();
  x();
  I();
  var TA = Ou(), PA = ku(), OA = fg(), RA = Nu(), kA = Mu(), CA = hg(), pg = dg();
  function qu() {
    var t = PA(), e = t.m(qu), r = (Object.getPrototypeOf ? Object.getPrototypeOf(e) : e.__proto__).constructor;
    function n(o) {
      var a = typeof o == "function" && o.constructor;
      return !!a && (a === r || (a.displayName || a.name) === "GeneratorFunction");
    }
    var i = { throw: 1, return: 2, break: 3, continue: 3 };
    function s(o) {
      var a, c;
      return function(f) {
        a || (a = { stop: function() {
          return c(f.a, 2);
        }, catch: function() {
          return f.v;
        }, abrupt: function(d, w) {
          return c(f.a, i[d], w);
        }, delegateYield: function(d, w, y) {
          return a.resultName = w, c(f.d, pg(d), y);
        }, finish: function(d) {
          return c(f.f, d);
        } }, c = function(d, w, y) {
          f.p = a.prev, f.n = a.next;
          try {
            return d(w, y);
          } finally {
            a.next = f.n;
          }
        }), a.resultName && (a[a.resultName] = f.v, a.resultName = void 0), a.sent = f.v, a.next = f.n;
        try {
          return o.call(this, a);
        } finally {
          f.p = a.prev, f.n = a.next;
        }
      };
    }
    return (Vt.exports = qu = function() {
      return { wrap: function(c, f, h2, d) {
        return t.w(s(c), f, h2, d && d.reverse());
      }, isGeneratorFunction: n, mark: t.m, awrap: function(c, f) {
        return new TA(c, f);
      }, AsyncIterator: kA, async: function(c, f, h2, d, w) {
        return (n(f) ? RA : OA)(s(c), f, h2, d, w);
      }, keys: CA, values: pg };
    }, Vt.exports.__esModule = true, Vt.exports.default = Vt.exports)();
  }
  Vt.exports = qu, Vt.exports.__esModule = true, Vt.exports.default = Vt.exports;
});
var bg = D((D4, gg) => {
  A();
  x();
  I();
  var $o = yg()();
  gg.exports = $o;
  try {
    regeneratorRuntime = $o;
  } catch {
    typeof globalThis == "object" ? globalThis.regeneratorRuntime = $o : Function("r", "regeneratorRuntime = r")($o);
  }
});
var mg = D((Ho, wg) => {
  A();
  x();
  I();
  (function(t, e) {
    typeof Ho == "object" && typeof wg < "u" ? e(Ho, Zy(), sg(), Pu(), lg(), bg()) : typeof define == "function" && define.amd ? define(["exports", "@babel/runtime/helpers/defineProperty", "@babel/runtime/helpers/slicedToArray", "fast-unique-numbers", "@babel/runtime/helpers/asyncToGenerator", "@babel/runtime/regenerator"], e) : (t = typeof globalThis < "u" ? globalThis : t || self, e(t.brokerFactory = {}, t._defineProperty, t._slicedToArray, t.fastUniqueNumbers, t._asyncToGenerator, t._regeneratorRuntime));
  })(Ho, function(t, e, r, n, i, s) {
    var o = function(m) {
      return typeof m.start == "function";
    }, a = /* @__PURE__ */ new WeakMap();
    function c(b, m) {
      var v = Object.keys(b);
      if (Object.getOwnPropertySymbols) {
        var R = Object.getOwnPropertySymbols(b);
        m && (R = R.filter(function(T) {
          return Object.getOwnPropertyDescriptor(b, T).enumerable;
        })), v.push.apply(v, R);
      }
      return v;
    }
    function f(b) {
      for (var m = 1; m < arguments.length; m++) {
        var v = arguments[m] != null ? arguments[m] : {};
        m % 2 ? c(Object(v), true).forEach(function(R) {
          e(b, R, v[R]);
        }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(b, Object.getOwnPropertyDescriptors(v)) : c(Object(v)).forEach(function(R) {
          Object.defineProperty(b, R, Object.getOwnPropertyDescriptor(v, R));
        });
      }
      return b;
    }
    var h2 = function(m) {
      return f(f({}, m), {}, { connect: function(R) {
        var T = R.call;
        return i(s.mark(function g() {
          var O, _, q, $;
          return s.wrap(function(j) {
            for (; ; ) switch (j.prev = j.next) {
              case 0:
                return O = new MessageChannel(), _ = O.port1, q = O.port2, j.next = 1, T("connect", { port: _ }, [_]);
              case 1:
                return $ = j.sent, a.set(q, $), j.abrupt("return", q);
              case 2:
              case "end":
                return j.stop();
            }
          }, g);
        }));
      }, disconnect: function(R) {
        var T = R.call;
        return (function() {
          var g = i(s.mark(function O(_) {
            var q;
            return s.wrap(function($) {
              for (; ; ) switch ($.prev = $.next) {
                case 0:
                  if (q = a.get(_), q !== void 0) {
                    $.next = 1;
                    break;
                  }
                  throw new Error("The given port is not connected.");
                case 1:
                  return $.next = 2, T("disconnect", { portId: q });
                case 2:
                case "end":
                  return $.stop();
              }
            }, O);
          }));
          return function(O) {
            return g.apply(this, arguments);
          };
        })();
      }, isSupported: function(R) {
        var T = R.call;
        return function() {
          return T("isSupported");
        };
      } });
    };
    function d(b, m) {
      var v = Object.keys(b);
      if (Object.getOwnPropertySymbols) {
        var R = Object.getOwnPropertySymbols(b);
        m && (R = R.filter(function(T) {
          return Object.getOwnPropertyDescriptor(b, T).enumerable;
        })), v.push.apply(v, R);
      }
      return v;
    }
    function w(b) {
      for (var m = 1; m < arguments.length; m++) {
        var v = arguments[m] != null ? arguments[m] : {};
        m % 2 ? d(Object(v), true).forEach(function(R) {
          e(b, R, v[R]);
        }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(b, Object.getOwnPropertyDescriptors(v)) : d(Object(v)).forEach(function(R) {
          Object.defineProperty(b, R, Object.getOwnPropertyDescriptor(v, R));
        });
      }
      return b;
    }
    var y = /* @__PURE__ */ new WeakMap(), P = function(m) {
      if (y.has(m)) return y.get(m);
      var v = /* @__PURE__ */ new Map();
      return y.set(m, v), v;
    }, E = function(m) {
      var v = h2(m);
      return function(R) {
        var T = P(R);
        R.addEventListener("message", function(H) {
          var G = H.data, K = G.id;
          if (K !== null && T.has(K)) {
            var X = T.get(K), Q = X.reject, V = X.resolve;
            T.delete(K), G.error === void 0 ? V(G.result) : Q(new Error(G.error.message));
          }
        }), o(R) && R.start();
        for (var g = function(G) {
          var K = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null, X = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : [];
          return new Promise(function(Q, V) {
            var te = n.generateUniqueNumber(T);
            T.set(te, { reject: V, resolve: Q }), K === null ? R.postMessage({ id: te, method: G }, X) : R.postMessage({ id: te, method: G, params: K }, X);
          });
        }, O = function(G, K) {
          var X = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : [];
          R.postMessage({ id: null, method: G, params: K }, X);
        }, _ = {}, q = 0, $ = Object.entries(v); q < $.length; q++) {
          var j = r($[q], 2), J = j[0], W = j[1];
          _ = w(w({}, _), {}, e({}, J, W({ call: g, notify: O })));
        }
        return w({}, _);
      };
    };
    t.createBroker = E;
  });
});
var vg = D((Vo, _g) => {
  A();
  x();
  I();
  (function(t, e) {
    typeof Vo == "object" && typeof _g < "u" ? e(Vo, Xn(), mg(), Pu()) : typeof define == "function" && define.amd ? define(["exports", "@babel/runtime/helpers/typeof", "broker-factory", "fast-unique-numbers"], e) : (t = typeof globalThis < "u" ? globalThis : t || self, e(t.workerTimersBroker = {}, t._typeof, t.brokerFactory, t.fastUniqueNumbers));
  })(Vo, function(t, e, r, n) {
    var i = /* @__PURE__ */ new Map([[0, null]]), s = /* @__PURE__ */ new Map([[0, null]]), o = r.createBroker({ clearInterval: function(f) {
      var h2 = f.call;
      return function(d) {
        e(i.get(d)) === "symbol" && (i.set(d, null), h2("clear", { timerId: d, timerType: "interval" }).then(function() {
          i.delete(d);
        }));
      };
    }, clearTimeout: function(f) {
      var h2 = f.call;
      return function(d) {
        e(s.get(d)) === "symbol" && (s.set(d, null), h2("clear", { timerId: d, timerType: "timeout" }).then(function() {
          s.delete(d);
        }));
      };
    }, setInterval: function(f) {
      var h2 = f.call;
      return function(d) {
        for (var w = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, y = arguments.length, P = new Array(y > 2 ? y - 2 : 0), E = 2; E < y; E++) P[E - 2] = arguments[E];
        var b = Symbol(), m = n.generateUniqueNumber(i);
        i.set(m, b);
        var v = function() {
          return h2("set", { delay: w, now: performance.timeOrigin + performance.now(), timerId: m, timerType: "interval" }).then(function() {
            var T = i.get(m);
            if (T === void 0) throw new Error("The timer is in an undefined state.");
            T === b && (d.apply(void 0, P), i.get(m) === b && v());
          });
        };
        return v(), m;
      };
    }, setTimeout: function(f) {
      var h2 = f.call;
      return function(d) {
        for (var w = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0, y = arguments.length, P = new Array(y > 2 ? y - 2 : 0), E = 2; E < y; E++) P[E - 2] = arguments[E];
        var b = Symbol(), m = n.generateUniqueNumber(s);
        return s.set(m, b), h2("set", { delay: w, now: performance.timeOrigin + performance.now(), timerId: m, timerType: "timeout" }).then(function() {
          var v = s.get(m);
          if (v === void 0) throw new Error("The timer is in an undefined state.");
          v === b && (s.delete(m), d.apply(void 0, P));
        }), m;
      };
    } }), a = function(f) {
      var h2 = new Worker(f);
      return o(h2);
    };
    t.load = a, t.wrap = o;
  });
});
var Eg = D((Go, Sg) => {
  A();
  x();
  I();
  (function(t, e) {
    typeof Go == "object" && typeof Sg < "u" ? e(Go, vg()) : typeof define == "function" && define.amd ? define(["exports", "worker-timers-broker"], e) : (t = typeof globalThis < "u" ? globalThis : t || self, e(t.workerTimers = {}, t.workerTimersBroker));
  })(Go, function(t, e) {
    var r = function(h2, d) {
      var w = null;
      return function() {
        if (w !== null) return w;
        var y = new Blob([d], { type: "application/javascript; charset=utf-8" }), P = URL.createObjectURL(y);
        return w = h2(P), setTimeout(function() {
          return URL.revokeObjectURL(P);
        }), w;
      };
    }, n = `(()=>{var e={45:(e,t,r)=>{var n=r(738).default;e.exports=function(e,t){if("object"!=n(e)||!e)return e;var r=e[Symbol.toPrimitive];if(void 0!==r){var o=r.call(e,t||"default");if("object"!=n(o))return o;throw new TypeError("@@toPrimitive must return a primitive value.")}return("string"===t?String:Number)(e)},e.exports.__esModule=!0,e.exports.default=e.exports},79:e=>{e.exports=function(e,t){(null==t||t>e.length)&&(t=e.length);for(var r=0,n=Array(t);r<t;r++)n[r]=e[r];return n},e.exports.__esModule=!0,e.exports.default=e.exports},122:(e,t,r)=>{var n=r(79);e.exports=function(e,t){if(e){if("string"==typeof e)return n(e,t);var r={}.toString.call(e).slice(8,-1);return"Object"===r&&e.constructor&&(r=e.constructor.name),"Map"===r||"Set"===r?Array.from(e):"Arguments"===r||/^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(r)?n(e,t):void 0}},e.exports.__esModule=!0,e.exports.default=e.exports},156:e=>{e.exports=function(e,t){var r=null==e?null:"undefined"!=typeof Symbol&&e[Symbol.iterator]||e["@@iterator"];if(null!=r){var n,o,u,a,i=[],s=!0,c=!1;try{if(u=(r=r.call(e)).next,0===t){if(Object(r)!==r)return;s=!1}else for(;!(s=(n=u.call(r)).done)&&(i.push(n.value),i.length!==t);s=!0);}catch(e){c=!0,o=e}finally{try{if(!s&&null!=r.return&&(a=r.return(),Object(a)!==a))return}finally{if(c)throw o}}return i}},e.exports.__esModule=!0,e.exports.default=e.exports},172:e=>{e.exports=function(e,t){this.v=e,this.k=t},e.exports.__esModule=!0,e.exports.default=e.exports},293:e=>{function t(e,t,r,n,o,u,a){try{var i=e[u](a),s=i.value}catch(e){return void r(e)}i.done?t(s):Promise.resolve(s).then(n,o)}e.exports=function(e){return function(){var r=this,n=arguments;return new Promise((function(o,u){var a=e.apply(r,n);function i(e){t(a,o,u,i,s,"next",e)}function s(e){t(a,o,u,i,s,"throw",e)}i(void 0)}))}},e.exports.__esModule=!0,e.exports.default=e.exports},373:e=>{e.exports=function(e){var t=Object(e),r=[];for(var n in t)r.unshift(n);return function e(){for(;r.length;)if((n=r.pop())in t)return e.value=n,e.done=!1,e;return e.done=!0,e}},e.exports.__esModule=!0,e.exports.default=e.exports},389:function(e,t){!function(e){"use strict";var t=function(e){return function(t){var r=e(t);return t.add(r),r}},r=function(e){return function(t,r){return e.set(t,r),r}},n=void 0===Number.MAX_SAFE_INTEGER?9007199254740991:Number.MAX_SAFE_INTEGER,o=536870912,u=2*o,a=function(e,t){return function(r){var a=t.get(r),i=void 0===a?r.size:a<u?a+1:0;if(!r.has(i))return e(r,i);if(r.size<o){for(;r.has(i);)i=Math.floor(Math.random()*u);return e(r,i)}if(r.size>n)throw new Error("Congratulations, you created a collection of unique numbers which uses all available integers!");for(;r.has(i);)i=Math.floor(Math.random()*n);return e(r,i)}},i=new WeakMap,s=r(i),c=a(s,i),f=t(c);e.addUniqueNumber=f,e.generateUniqueNumber=c}(t)},472:function(e,t,r){!function(e,t,r,n){"use strict";var o=function(e,t){return function(r){var o=t.get(r);if(void 0===o)return Promise.resolve(!1);var u=n(o,2),a=u[0],i=u[1];return e(a),t.delete(r),i(!1),Promise.resolve(!0)}},u=function(e,t){var r=function(n,o,u,a){var i=n-e.now();i>0?o.set(a,[t(r,i,n,o,u,a),u]):(o.delete(a),u(!0))};return r},a=function(e,t,r,n){return function(o,u,a){var i=o+u-t.timeOrigin,s=i-t.now();return new Promise((function(t){e.set(a,[r(n,s,i,e,t,a),t])}))}},i=new Map,s=o(globalThis.clearTimeout,i),c=new Map,f=o(globalThis.clearTimeout,c),l=u(performance,globalThis.setTimeout),p=a(i,performance,globalThis.setTimeout,l),d=a(c,performance,globalThis.setTimeout,l);r.createWorker(self,{clear:function(){var r=e(t.mark((function e(r){var n,o,u;return t.wrap((function(e){for(;;)switch(e.prev=e.next){case 0:return n=r.timerId,o=r.timerType,e.next=1,"interval"===o?s(n):f(n);case 1:return u=e.sent,e.abrupt("return",{result:u});case 2:case"end":return e.stop()}}),e)})));function n(e){return r.apply(this,arguments)}return n}(),set:function(){var r=e(t.mark((function e(r){var n,o,u,a,i;return t.wrap((function(e){for(;;)switch(e.prev=e.next){case 0:return n=r.delay,o=r.now,u=r.timerId,a=r.timerType,e.next=1,("interval"===a?p:d)(n,o,u);case 1:return i=e.sent,e.abrupt("return",{result:i});case 2:case"end":return e.stop()}}),e)})));function n(e){return r.apply(this,arguments)}return n}()})}(r(293),r(756),r(623),r(715))},546:e=>{function t(r,n,o,u){var a=Object.defineProperty;try{a({},"",{})}catch(r){a=0}e.exports=t=function(e,r,n,o){if(r)a?a(e,r,{value:n,enumerable:!o,configurable:!o,writable:!o}):e[r]=n;else{var u=function(r,n){t(e,r,(function(e){return this._invoke(r,n,e)}))};u("next",0),u("throw",1),u("return",2)}},e.exports.__esModule=!0,e.exports.default=e.exports,t(r,n,o,u)}e.exports=t,e.exports.__esModule=!0,e.exports.default=e.exports},579:(e,t,r)=>{var n=r(738).default;e.exports=function(e){if(null!=e){var t=e["function"==typeof Symbol&&Symbol.iterator||"@@iterator"],r=0;if(t)return t.call(e);if("function"==typeof e.next)return e;if(!isNaN(e.length))return{next:function(){return e&&r>=e.length&&(e=void 0),{value:e&&e[r++],done:!e}}}}throw new TypeError(n(e)+" is not iterable")},e.exports.__esModule=!0,e.exports.default=e.exports},623:function(e,t,r){!function(e,t,r,n,o){"use strict";var u={INTERNAL_ERROR:-32603,INVALID_PARAMS:-32602,METHOD_NOT_FOUND:-32601},a=function(e,t){return Object.assign(new Error(e),{status:t})},i=function(e){return a('The requested method called "'.concat(e,'" is not supported.'),u.METHOD_NOT_FOUND)},s=function(e){return a('The handler of the method called "'.concat(e,'" returned no required result.'),u.INTERNAL_ERROR)},c=function(e){return a('The handler of the method called "'.concat(e,'" returned an unexpected result.'),u.INTERNAL_ERROR)},f=function(e){return a('The specified parameter called "portId" with the given value "'.concat(e,'" does not identify a port connected to this worker.'),u.INVALID_PARAMS)},l=function(e,n){return function(){var o=t(r.mark((function t(o){var u,a,f,l,p,d,v,x,y,b,h,m,_,g,w;return r.wrap((function(t){for(;;)switch(t.prev=t.next){case 0:if(u=o.data,a=u.id,f=u.method,l=u.params,p=n[f],t.prev=1,void 0!==p){t.next=2;break}throw i(f);case 2:if(void 0!==(d=void 0===l?p():p(l))){t.next=3;break}throw s(f);case 3:if(!(d instanceof Promise)){t.next=5;break}return t.next=4,d;case 4:g=t.sent,t.next=6;break;case 5:g=d;case 6:if(v=g,null!==a){t.next=8;break}if(void 0===v.result){t.next=7;break}throw c(f);case 7:t.next=10;break;case 8:if(void 0!==v.result){t.next=9;break}throw c(f);case 9:x=v.result,y=v.transferables,b=void 0===y?[]:y,e.postMessage({id:a,result:x},b);case 10:t.next=12;break;case 11:t.prev=11,w=t.catch(1),h=w.message,m=w.status,_=void 0===m?-32603:m,e.postMessage({error:{code:_,message:h},id:a});case 12:case"end":return t.stop()}}),t,null,[[1,11]])})));return function(e){return o.apply(this,arguments)}}()},p=function(){return new Promise((function(e){var t=new ArrayBuffer(0),r=new MessageChannel,n=r.port1,o=r.port2;n.onmessage=function(t){var r=t.data;return e(null!==r)},o.postMessage(t,[t])}))};function d(e,t){var r=Object.keys(e);if(Object.getOwnPropertySymbols){var n=Object.getOwnPropertySymbols(e);t&&(n=n.filter((function(t){return Object.getOwnPropertyDescriptor(e,t).enumerable}))),r.push.apply(r,n)}return r}function v(e){for(var t=1;t<arguments.length;t++){var r=null!=arguments[t]?arguments[t]:{};t%2?d(Object(r),!0).forEach((function(t){n(e,t,r[t])})):Object.getOwnPropertyDescriptors?Object.defineProperties(e,Object.getOwnPropertyDescriptors(r)):d(Object(r)).forEach((function(t){Object.defineProperty(e,t,Object.getOwnPropertyDescriptor(r,t))}))}return e}var x=new Map,y=function(e,n,u){return v(v({},n),{},{connect:function(t){var r=t.port;r.start();var u=e(r,n),a=o.generateUniqueNumber(x);return x.set(a,(function(){u(),r.close(),x.delete(a)})),{result:a}},disconnect:function(e){var t=e.portId,r=x.get(t);if(void 0===r)throw f(t);return r(),{result:null}},isSupported:function(){var e=t(r.mark((function e(){var t,n,o;return r.wrap((function(e){for(;;)switch(e.prev=e.next){case 0:return e.next=1,p();case 1:if(!e.sent){e.next=5;break}if(!((t=u())instanceof Promise)){e.next=3;break}return e.next=2,t;case 2:o=e.sent,e.next=4;break;case 3:o=t;case 4:return n=o,e.abrupt("return",{result:n});case 5:return e.abrupt("return",{result:!1});case 6:case"end":return e.stop()}}),e)})));function n(){return e.apply(this,arguments)}return n}()})},b=function(e,t){var r=y(b,t,arguments.length>2&&void 0!==arguments[2]?arguments[2]:function(){return!0}),n=l(e,r);return e.addEventListener("message",n),function(){return e.removeEventListener("message",n)}};e.createWorker=b,e.isSupported=p}(t,r(293),r(756),r(693),r(389))},633:(e,t,r)=>{var n=r(172),o=r(993),u=r(869),a=r(887),i=r(791),s=r(373),c=r(579);function f(){"use strict";var t=o(),r=t.m(f),l=(Object.getPrototypeOf?Object.getPrototypeOf(r):r.__proto__).constructor;function p(e){var t="function"==typeof e&&e.constructor;return!!t&&(t===l||"GeneratorFunction"===(t.displayName||t.name))}var d={throw:1,return:2,break:3,continue:3};function v(e){var t,r;return function(n){t||(t={stop:function(){return r(n.a,2)},catch:function(){return n.v},abrupt:function(e,t){return r(n.a,d[e],t)},delegateYield:function(e,o,u){return t.resultName=o,r(n.d,c(e),u)},finish:function(e){return r(n.f,e)}},r=function(e,r,o){n.p=t.prev,n.n=t.next;try{return e(r,o)}finally{t.next=n.n}}),t.resultName&&(t[t.resultName]=n.v,t.resultName=void 0),t.sent=n.v,t.next=n.n;try{return e.call(this,t)}finally{n.p=t.prev,n.n=t.next}}}return(e.exports=f=function(){return{wrap:function(e,r,n,o){return t.w(v(e),r,n,o&&o.reverse())},isGeneratorFunction:p,mark:t.m,awrap:function(e,t){return new n(e,t)},AsyncIterator:i,async:function(e,t,r,n,o){return(p(t)?a:u)(v(e),t,r,n,o)},keys:s,values:c}},e.exports.__esModule=!0,e.exports.default=e.exports)()}e.exports=f,e.exports.__esModule=!0,e.exports.default=e.exports},693:(e,t,r)=>{var n=r(736);e.exports=function(e,t,r){return(t=n(t))in e?Object.defineProperty(e,t,{value:r,enumerable:!0,configurable:!0,writable:!0}):e[t]=r,e},e.exports.__esModule=!0,e.exports.default=e.exports},715:(e,t,r)=>{var n=r(987),o=r(156),u=r(122),a=r(752);e.exports=function(e,t){return n(e)||o(e,t)||u(e,t)||a()},e.exports.__esModule=!0,e.exports.default=e.exports},736:(e,t,r)=>{var n=r(738).default,o=r(45);e.exports=function(e){var t=o(e,"string");return"symbol"==n(t)?t:t+""},e.exports.__esModule=!0,e.exports.default=e.exports},738:e=>{function t(r){return e.exports=t="function"==typeof Symbol&&"symbol"==typeof Symbol.iterator?function(e){return typeof e}:function(e){return e&&"function"==typeof Symbol&&e.constructor===Symbol&&e!==Symbol.prototype?"symbol":typeof e},e.exports.__esModule=!0,e.exports.default=e.exports,t(r)}e.exports=t,e.exports.__esModule=!0,e.exports.default=e.exports},752:e=>{e.exports=function(){throw new TypeError("Invalid attempt to destructure non-iterable instance.\\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.")},e.exports.__esModule=!0,e.exports.default=e.exports},756:(e,t,r)=>{var n=r(633)();e.exports=n;try{regeneratorRuntime=n}catch(e){"object"==typeof globalThis?globalThis.regeneratorRuntime=n:Function("r","regeneratorRuntime = r")(n)}},791:(e,t,r)=>{var n=r(172),o=r(546);e.exports=function e(t,r){function u(e,o,a,i){try{var s=t[e](o),c=s.value;return c instanceof n?r.resolve(c.v).then((function(e){u("next",e,a,i)}),(function(e){u("throw",e,a,i)})):r.resolve(c).then((function(e){s.value=e,a(s)}),(function(e){return u("throw",e,a,i)}))}catch(e){i(e)}}var a;this.next||(o(e.prototype),o(e.prototype,"function"==typeof Symbol&&Symbol.asyncIterator||"@asyncIterator",(function(){return this}))),o(this,"_invoke",(function(e,t,n){function o(){return new r((function(t,r){u(e,n,t,r)}))}return a=a?a.then(o,o):o()}),!0)},e.exports.__esModule=!0,e.exports.default=e.exports},869:(e,t,r)=>{var n=r(887);e.exports=function(e,t,r,o,u){var a=n(e,t,r,o,u);return a.next().then((function(e){return e.done?e.value:a.next()}))},e.exports.__esModule=!0,e.exports.default=e.exports},887:(e,t,r)=>{var n=r(993),o=r(791);e.exports=function(e,t,r,u,a){return new o(n().w(e,t,r,u),a||Promise)},e.exports.__esModule=!0,e.exports.default=e.exports},987:e=>{e.exports=function(e){if(Array.isArray(e))return e},e.exports.__esModule=!0,e.exports.default=e.exports},993:(e,t,r)=>{var n=r(546);function o(){var t,r,u="function"==typeof Symbol?Symbol:{},a=u.iterator||"@@iterator",i=u.toStringTag||"@@toStringTag";function s(e,o,u,a){var i=o&&o.prototype instanceof f?o:f,s=Object.create(i.prototype);return n(s,"_invoke",function(e,n,o){var u,a,i,s=0,f=o||[],l=!1,p={p:0,n:0,v:t,a:d,f:d.bind(t,4),d:function(e,r){return u=e,a=0,i=t,p.n=r,c}};function d(e,n){for(a=e,i=n,r=0;!l&&s&&!o&&r<f.length;r++){var o,u=f[r],d=p.p,v=u[2];e>3?(o=v===n)&&(i=u[(a=u[4])?5:(a=3,3)],u[4]=u[5]=t):u[0]<=d&&((o=e<2&&d<u[1])?(a=0,p.v=n,p.n=u[1]):d<v&&(o=e<3||u[0]>n||n>v)&&(u[4]=e,u[5]=n,p.n=v,a=0))}if(o||e>1)return c;throw l=!0,n}return function(o,f,v){if(s>1)throw TypeError("Generator is already running");for(l&&1===f&&d(f,v),a=f,i=v;(r=a<2?t:i)||!l;){u||(a?a<3?(a>1&&(p.n=-1),d(a,i)):p.n=i:p.v=i);try{if(s=2,u){if(a||(o="next"),r=u[o]){if(!(r=r.call(u,i)))throw TypeError("iterator result is not an object");if(!r.done)return r;i=r.value,a<2&&(a=0)}else 1===a&&(r=u.return)&&r.call(u),a<2&&(i=TypeError("The iterator does not provide a '"+o+"' method"),a=1);u=t}else if((r=(l=p.n<0)?i:e.call(n,p))!==c)break}catch(e){u=t,a=1,i=e}finally{s=1}}return{value:r,done:l}}}(e,u,a),!0),s}var c={};function f(){}function l(){}function p(){}r=Object.getPrototypeOf;var d=[][a]?r(r([][a]())):(n(r={},a,(function(){return this})),r),v=p.prototype=f.prototype=Object.create(d);function x(e){return Object.setPrototypeOf?Object.setPrototypeOf(e,p):(e.__proto__=p,n(e,i,"GeneratorFunction")),e.prototype=Object.create(v),e}return l.prototype=p,n(v,"constructor",p),n(p,"constructor",l),l.displayName="GeneratorFunction",n(p,i,"GeneratorFunction"),n(v),n(v,i,"Generator"),n(v,a,(function(){return this})),n(v,"toString",(function(){return"[object Generator]"})),(e.exports=o=function(){return{w:s,m:x}},e.exports.__esModule=!0,e.exports.default=e.exports)()}e.exports=o,e.exports.__esModule=!0,e.exports.default=e.exports}},t={};function r(n){var o=t[n];if(void 0!==o)return o.exports;var u=t[n]={exports:{}};return e[n].call(u.exports,u,u.exports,r),u.exports}r.n=e=>{var t=e&&e.__esModule?()=>e.default:()=>e;return r.d(t,{a:t}),t},r.d=(e,t)=>{for(var n in t)r.o(t,n)&&!r.o(e,n)&&Object.defineProperty(e,n,{enumerable:!0,get:t[n]})},r.o=(e,t)=>Object.prototype.hasOwnProperty.call(e,t),(()=>{"use strict";r(472)})()})();`, i = r(e.load, n), s = function(h2) {
      return i().clearInterval(h2);
    }, o = function(h2) {
      return i().clearTimeout(h2);
    }, a = function() {
      var h2;
      return (h2 = i()).setInterval.apply(h2, arguments);
    }, c = function() {
      var h2;
      return (h2 = i()).setTimeout.apply(h2, arguments);
    };
    t.clearInterval = s, t.clearTimeout = o, t.setInterval = a, t.setTimeout = c;
  });
});
var yi = D((Fr) => {
  A();
  x();
  I();
  Object.defineProperty(Fr, "__esModule", { value: true });
  Fr.isReactNativeBrowser = Fr.isWebWorker = void 0;
  var BA = () => typeof window < "u" ? typeof navigator < "u" && navigator.userAgent?.toLowerCase().indexOf(" electron/") > -1 && M?.versions ? !Object.prototype.hasOwnProperty.call(M.versions, "electron") : typeof window.document < "u" : false, Ag = () => !!(typeof self == "object" && self?.constructor?.name?.includes("WorkerGlobalScope") && typeof Deno > "u"), Ig = () => typeof navigator < "u" && navigator.product === "ReactNative", MA = BA() || Ag() || Ig();
  Fr.isWebWorker = Ag();
  Fr.isReactNativeBrowser = Ig();
  Fr.default = MA;
});
var Og = D((Gt) => {
  A();
  x();
  I();
  var NA = Gt && Gt.__createBinding || (Object.create ? function(t, e, r, n) {
    n === void 0 && (n = r);
    var i = Object.getOwnPropertyDescriptor(e, r);
    (!i || ("get" in i ? !e.__esModule : i.writable || i.configurable)) && (i = { enumerable: true, get: function() {
      return e[r];
    } }), Object.defineProperty(t, n, i);
  } : function(t, e, r, n) {
    n === void 0 && (n = r), t[n] = e[r];
  }), qA = Gt && Gt.__setModuleDefault || (Object.create ? function(t, e) {
    Object.defineProperty(t, "default", { enumerable: true, value: e });
  } : function(t, e) {
    t.default = e;
  }), LA = Gt && Gt.__importStar || /* @__PURE__ */ (function() {
    var t = function(e) {
      return t = Object.getOwnPropertyNames || function(r) {
        var n = [];
        for (var i in r) Object.prototype.hasOwnProperty.call(r, i) && (n[n.length] = i);
        return n;
      }, t(e);
    };
    return function(e) {
      if (e && e.__esModule) return e;
      var r = {};
      if (e != null) for (var n = t(e), i = 0; i < n.length; i++) n[i] !== "default" && NA(r, e, n[i]);
      return qA(r, e), r;
    };
  })();
  Object.defineProperty(Gt, "__esModule", { value: true });
  var xg = Eg(), Lu = LA(yi()), Tg = { set: xg.setInterval, clear: xg.clearInterval }, Pg = { set: (t, e) => setInterval(t, e), clear: (t) => clearInterval(t) }, UA = (t) => {
    switch (t) {
      case "native":
        return Pg;
      case "worker":
        return Tg;
      case "auto":
      default:
        return Lu.default && !Lu.isWebWorker && !Lu.isReactNativeBrowser ? Tg : Pg;
    }
  };
  Gt.default = UA;
});
var Du = D((gi) => {
  A();
  x();
  I();
  var DA = gi && gi.__importDefault || function(t) {
    return t && t.__esModule ? t : { default: t };
  };
  Object.defineProperty(gi, "__esModule", { value: true });
  var jA = DA(Og()), Uu = class {
    _keepalive;
    timerId;
    timer;
    destroyed = false;
    counter;
    client;
    _keepaliveTimeoutTimestamp;
    _intervalEvery;
    get keepaliveTimeoutTimestamp() {
      return this._keepaliveTimeoutTimestamp;
    }
    get intervalEvery() {
      return this._intervalEvery;
    }
    get keepalive() {
      return this._keepalive;
    }
    constructor(e, r) {
      this.client = e, this.timer = typeof r == "object" && "set" in r && "clear" in r ? r : (0, jA.default)(r), this.setKeepalive(e.options.keepalive);
    }
    clear() {
      this.timerId && (this.timer.clear(this.timerId), this.timerId = null);
    }
    setKeepalive(e) {
      if (e *= 1e3, isNaN(e) || e <= 0 || e > 2147483647) throw new Error(`Keepalive value must be an integer between 0 and 2147483647. Provided value is ${e}`);
      this._keepalive = e, this.reschedule(), this.client.log(`KeepaliveManager: set keepalive to ${e}ms`);
    }
    destroy() {
      this.clear(), this.destroyed = true;
    }
    reschedule() {
      if (this.destroyed) return;
      this.clear(), this.counter = 0;
      let e = Math.ceil(this._keepalive * 1.5);
      this._keepaliveTimeoutTimestamp = Date.now() + e, this._intervalEvery = Math.ceil(this._keepalive / 2), this.timerId = this.timer.set(() => {
        this.destroyed || (this.counter += 1, this.counter === 2 ? this.client.sendPing() : this.counter > 2 && this.client.onKeepaliveTimeout());
      }, this._intervalEvery);
    }
  };
  gi.default = Uu;
});
var zo = D((yt) => {
  A();
  x();
  I();
  var FA = yt && yt.__createBinding || (Object.create ? function(t, e, r, n) {
    n === void 0 && (n = r);
    var i = Object.getOwnPropertyDescriptor(e, r);
    (!i || ("get" in i ? !e.__esModule : i.writable || i.configurable)) && (i = { enumerable: true, get: function() {
      return e[r];
    } }), Object.defineProperty(t, n, i);
  } : function(t, e, r, n) {
    n === void 0 && (n = r), t[n] = e[r];
  }), WA = yt && yt.__setModuleDefault || (Object.create ? function(t, e) {
    Object.defineProperty(t, "default", { enumerable: true, value: e });
  } : function(t, e) {
    t.default = e;
  }), Mg = yt && yt.__importStar || /* @__PURE__ */ (function() {
    var t = function(e) {
      return t = Object.getOwnPropertyNames || function(r) {
        var n = [];
        for (var i in r) Object.prototype.hasOwnProperty.call(r, i) && (n[n.length] = i);
        return n;
      }, t(e);
    };
    return function(e) {
      if (e && e.__esModule) return e;
      var r = {};
      if (e != null) for (var n = t(e), i = 0; i < n.length; i++) n[i] !== "default" && FA(r, e, n[i]);
      return WA(r, e), r;
    };
  })(), fr = yt && yt.__importDefault || function(t) {
    return t && t.__esModule ? t : { default: t };
  };
  Object.defineProperty(yt, "__esModule", { value: true });
  var ju = fr(iy()), $A = or(), Rg = fr(uy()), HA = fr(At()), kg = Mg(cy()), vn = fr(La()), VA = fr(zy()), GA = fr(Au()), zA = fr(Ky()), bi = Mr(), KA = Qy(), QA = fr(Du()), Cg = Mg(yi()), Fu = globalThis.setImmediate || ((...t) => {
    let e = t.shift();
    (0, bi.nextTick)(() => {
      e(...t);
    });
  }), Bg = { keepalive: 60, reschedulePings: true, protocolId: "MQTT", protocolVersion: 4, reconnectPeriod: 1e3, connectTimeout: 30 * 1e3, clean: true, resubscribe: true, subscribeBatchSize: null, writeCache: true, timerVariant: "auto" }, Wu = class t extends KA.TypedEventEmitter {
    static VERSION = bi.MQTTJS_VERSION;
    connected;
    disconnecting;
    disconnected;
    reconnecting;
    incomingStore;
    outgoingStore;
    options;
    queueQoSZero;
    _reconnectCount;
    log;
    messageIdProvider;
    outgoing;
    messageIdToTopic;
    noop;
    keepaliveManager;
    stream;
    queue;
    streamBuilder;
    _resubscribeTopics;
    connackTimer;
    reconnectTimer;
    _storeProcessing;
    _packetIdsDuringStoreProcessing;
    _storeProcessingQueue;
    _firstConnection;
    topicAliasRecv;
    topicAliasSend;
    _deferredReconnect;
    connackPacket;
    static defaultId() {
      return `mqttjs_${Math.random().toString(16).substr(2, 8)}`;
    }
    constructor(e, r) {
      super(), this.options = r || {};
      for (let n in Bg) typeof this.options[n] > "u" ? this.options[n] = Bg[n] : this.options[n] = r[n];
      this.log = this.options.log || (0, HA.default)("mqttjs:client"), this.noop = this._noop.bind(this), this.log("MqttClient :: version:", t.VERSION), Cg.isWebWorker ? this.log("MqttClient :: environment", "webworker") : this.log("MqttClient :: environment", Cg.default ? "browser" : "node"), this.log("MqttClient :: options.protocol", r.protocol), this.log("MqttClient :: options.protocolVersion", r.protocolVersion), this.log("MqttClient :: options.username", r.username), this.log("MqttClient :: options.keepalive", r.keepalive), this.log("MqttClient :: options.reconnectPeriod", r.reconnectPeriod), this.log("MqttClient :: options.rejectUnauthorized", r.rejectUnauthorized), this.log("MqttClient :: options.properties.topicAliasMaximum", r.properties ? r.properties.topicAliasMaximum : void 0), this.options.clientId = typeof r.clientId == "string" ? r.clientId : t.defaultId(), this.log("MqttClient :: clientId", this.options.clientId), this.options.customHandleAcks = r.protocolVersion === 5 && r.customHandleAcks ? r.customHandleAcks : (...n) => {
        n[3](null, 0);
      }, this.options.writeCache || (ju.default.writeToStream.cacheNumbers = false), this.streamBuilder = e, this.messageIdProvider = typeof this.options.messageIdProvider > "u" ? new GA.default() : this.options.messageIdProvider, this.outgoingStore = r.outgoingStore || new vn.default(), this.incomingStore = r.incomingStore || new vn.default(), this.queueQoSZero = r.queueQoSZero === void 0 ? true : r.queueQoSZero, this._resubscribeTopics = {}, this.messageIdToTopic = {}, this.keepaliveManager = null, this.connected = false, this.disconnecting = false, this.reconnecting = false, this.queue = [], this.connackTimer = null, this.reconnectTimer = null, this._storeProcessing = false, this._packetIdsDuringStoreProcessing = {}, this._storeProcessingQueue = [], this.outgoing = {}, this._firstConnection = true, r.properties && r.properties.topicAliasMaximum > 0 && (r.properties.topicAliasMaximum > 65535 ? this.log("MqttClient :: options.properties.topicAliasMaximum is out of range") : this.topicAliasRecv = new zA.default(r.properties.topicAliasMaximum)), this.on("connect", () => {
        let { queue: n } = this, i = () => {
          let s = n.shift();
          this.log("deliver :: entry %o", s);
          let o = null;
          if (!s) {
            this._resubscribe();
            return;
          }
          o = s.packet, this.log("deliver :: call _sendPacket for %o", o);
          let a = true;
          o.messageId && o.messageId !== 0 && (this.messageIdProvider.register(o.messageId) || (a = false)), a ? this._sendPacket(o, (c) => {
            s.cb && s.cb(c), i();
          }) : (this.log("messageId: %d has already used. The message is skipped and removed.", o.messageId), i());
        };
        this.log("connect :: sending queued packets"), i();
      }), this.on("close", () => {
        this.log("close :: connected set to `false`"), this.connected = false, this.log("close :: clearing connackTimer"), clearTimeout(this.connackTimer), this._destroyKeepaliveManager(), this.topicAliasRecv && this.topicAliasRecv.clear(), this.log("close :: calling _setupReconnect"), this._setupReconnect();
      }), this.options.manualConnect || (this.log("MqttClient :: setting up stream"), this.connect());
    }
    handleAuth(e, r) {
      r();
    }
    handleMessage(e, r) {
      r();
    }
    _nextId() {
      return this.messageIdProvider.allocate();
    }
    getLastMessageId() {
      return this.messageIdProvider.getLastAllocated();
    }
    connect() {
      let e = new $A.Writable(), r = ju.default.parser(this.options), n = null, i = [];
      this.log("connect :: calling method to clear reconnect"), this._clearReconnect(), this.disconnected && !this.reconnecting && (this.incomingStore = this.options.incomingStore || new vn.default(), this.outgoingStore = this.options.outgoingStore || new vn.default(), this.disconnecting = false, this.disconnected = false), this.log("connect :: using streamBuilder provided to client to create stream"), this.stream = this.streamBuilder(this), r.on("packet", (f) => {
        this.log("parser :: on packet push to packets array."), i.push(f);
      });
      let s = () => {
        this.log("work :: getting next packet in queue");
        let f = i.shift();
        if (f) this.log("work :: packet pulled from queue"), (0, VA.default)(this, f, o);
        else {
          this.log("work :: no packets in queue");
          let h2 = n;
          n = null, this.log("work :: done flag is %s", !!h2), h2 && h2();
        }
      }, o = () => {
        if (i.length) (0, bi.nextTick)(s);
        else {
          let f = n;
          n = null, f();
        }
      };
      e._write = (f, h2, d) => {
        n = d, this.log("writable stream :: parsing buffer"), r.parse(f), s();
      };
      let a = (f) => {
        this.log("streamErrorHandler :: error", f.message), f.code ? (this.log("streamErrorHandler :: emitting error"), this.emit("error", f)) : this.noop(f);
      };
      this.log("connect :: pipe stream to writable stream"), this.stream.pipe(e), this.stream.on("error", a), this.stream.on("close", () => {
        this.log("(%s)stream :: on close", this.options.clientId), this._flushVolatile(), this.log("stream: emit close to MqttClient"), this.emit("close");
      }), this.log("connect: sending packet `connect`");
      let c = { cmd: "connect", protocolId: this.options.protocolId, protocolVersion: this.options.protocolVersion, clean: this.options.clean, clientId: this.options.clientId, keepalive: this.options.keepalive, username: this.options.username, password: this.options.password, properties: this.options.properties };
      if (this.options.will && (c.will = { ...this.options.will, payload: this.options.will?.payload }), this.topicAliasRecv && (c.properties || (c.properties = {}), this.topicAliasRecv && (c.properties.topicAliasMaximum = this.topicAliasRecv.max)), this._writePacket(c), r.on("error", this.emit.bind(this, "error")), this.options.properties) {
        if (!this.options.properties.authenticationMethod && this.options.properties.authenticationData) return this.end(() => this.emit("error", new Error("Packet has no Authentication Method"))), this;
        if (this.options.properties.authenticationMethod && this.options.authPacket && typeof this.options.authPacket == "object") {
          let f = { cmd: "auth", reasonCode: 0, ...this.options.authPacket };
          this._writePacket(f);
        }
      }
      return this.stream.setMaxListeners(1e3), clearTimeout(this.connackTimer), this.connackTimer = setTimeout(() => {
        this.log("!!connectTimeout hit!! Calling _cleanUp with force `true`"), this.emit("error", new Error("connack timeout")), this._cleanUp(true);
      }, this.options.connectTimeout), this;
    }
    publish(e, r, n, i) {
      this.log("publish :: message `%s` to topic `%s`", r, e);
      let { options: s } = this;
      typeof n == "function" && (i = n, n = null), n = n || {}, n = { ...{ qos: 0, retain: false, dup: false }, ...n };
      let { qos: a, retain: c, dup: f, properties: h2, cbStorePut: d } = n;
      if (this._checkDisconnecting(i)) return this;
      let w = () => {
        let y = 0;
        if ((a === 1 || a === 2) && (y = this._nextId(), y === null)) return this.log("No messageId left"), false;
        let P = { cmd: "publish", topic: e, payload: r, qos: a, retain: c, messageId: y, dup: f };
        switch (s.protocolVersion === 5 && (P.properties = h2), this.log("publish :: qos", a), a) {
          case 1:
          case 2:
            this.outgoing[P.messageId] = { volatile: false, cb: i || this.noop }, this.log("MqttClient:publish: packet cmd: %s", P.cmd), this._sendPacket(P, void 0, d);
            break;
          default:
            this.log("MqttClient:publish: packet cmd: %s", P.cmd), this._sendPacket(P, i, d);
            break;
        }
        return true;
      };
      return (this._storeProcessing || this._storeProcessingQueue.length > 0 || !w()) && this._storeProcessingQueue.push({ invoke: w, cbStorePut: n.cbStorePut, callback: i }), this;
    }
    publishAsync(e, r, n) {
      return new Promise((i, s) => {
        this.publish(e, r, n, (o, a) => {
          o ? s(o) : i(a);
        });
      });
    }
    subscribe(e, r, n) {
      let i = this.options.protocolVersion;
      typeof r == "function" && (n = r), n = n || this.noop;
      let s = false, o = [];
      typeof e == "string" ? (e = [e], o = e) : Array.isArray(e) ? o = e : typeof e == "object" && (s = e.resubscribe, delete e.resubscribe, o = Object.keys(e));
      let a = kg.validateTopics(o);
      if (a !== null) return Fu(n, new Error(`Invalid topic ${a}`)), this;
      if (this._checkDisconnecting(n)) return this.log("subscribe: discconecting true"), this;
      let c = { qos: 0 };
      i === 5 && (c.nl = false, c.rap = false, c.rh = 0), r = { ...c, ...r };
      let { properties: f } = r, h2 = [], d = (P, E) => {
        if (E = E || r, !Object.prototype.hasOwnProperty.call(this._resubscribeTopics, P) || this._resubscribeTopics[P].qos < E.qos || s) {
          let b = { topic: P, qos: E.qos };
          i === 5 && (b.nl = E.nl, b.rap = E.rap, b.rh = E.rh, b.properties = f), this.log("subscribe: pushing topic `%s` and qos `%s` to subs list", b.topic, b.qos), h2.push(b);
        }
      };
      if (Array.isArray(e) ? e.forEach((P) => {
        this.log("subscribe: array topic %s", P), d(P);
      }) : Object.keys(e).forEach((P) => {
        this.log("subscribe: object topic %s, %o", P, e[P]), d(P, e[P]);
      }), !h2.length) return n(null, []), this;
      let w = (P, E) => {
        let b = { cmd: "subscribe", subscriptions: P, messageId: E };
        if (f && (b.properties = f), this.options.resubscribe) {
          this.log("subscribe :: resubscribe true");
          let v = [];
          P.forEach((R) => {
            if (this.options.reconnectPeriod > 0) {
              let T = { qos: R.qos };
              i === 5 && (T.nl = R.nl || false, T.rap = R.rap || false, T.rh = R.rh || 0, T.properties = R.properties), this._resubscribeTopics[R.topic] = T, v.push(R.topic);
            }
          }), this.messageIdToTopic[b.messageId] = v;
        }
        let m = new Promise((v, R) => {
          this.outgoing[b.messageId] = { volatile: true, cb(T, g) {
            if (!T) {
              let { granted: O } = g;
              for (let _ = 0; _ < O.length; _ += 1) P[_].qos = O[_];
            }
            T ? R(new bi.ErrorWithSubackPacket(T.message, g)) : v(g);
          } };
        });
        return this.log("subscribe :: call _sendPacket"), this._sendPacket(b), m;
      }, y = () => {
        let P = this.options.subscribeBatchSize ?? h2.length, E = [];
        for (let b = 0; b < h2.length; b += P) {
          let m = h2.slice(b, b + P), v = this._nextId();
          if (v === null) return this.log("No messageId left"), false;
          E.push(w(m, v));
        }
        return Promise.all(E).then((b) => {
          n(null, h2, b.at(-1));
        }).catch((b) => {
          n(b, h2, b.packet);
        }), true;
      };
      return (this._storeProcessing || this._storeProcessingQueue.length > 0 || !y()) && this._storeProcessingQueue.push({ invoke: y, callback: n }), this;
    }
    subscribeAsync(e, r) {
      return new Promise((n, i) => {
        this.subscribe(e, r, (s, o) => {
          s ? i(s) : n(o);
        });
      });
    }
    unsubscribe(e, r, n) {
      typeof e == "string" && (e = [e]), typeof r == "function" && (n = r), n = n || this.noop;
      let i = kg.validateTopics(e);
      if (i !== null) return Fu(n, new Error(`Invalid topic ${i}`)), this;
      if (this._checkDisconnecting(n)) return this;
      let s = () => {
        let o = this._nextId();
        if (o === null) return this.log("No messageId left"), false;
        let a = { cmd: "unsubscribe", messageId: o, unsubscriptions: [] };
        return typeof e == "string" ? a.unsubscriptions = [e] : Array.isArray(e) && (a.unsubscriptions = e), this.options.resubscribe && a.unsubscriptions.forEach((c) => {
          delete this._resubscribeTopics[c];
        }), typeof r == "object" && r.properties && (a.properties = r.properties), this.outgoing[a.messageId] = { volatile: true, cb: n }, this.log("unsubscribe: call _sendPacket"), this._sendPacket(a), true;
      };
      return (this._storeProcessing || this._storeProcessingQueue.length > 0 || !s()) && this._storeProcessingQueue.push({ invoke: s, callback: n }), this;
    }
    unsubscribeAsync(e, r) {
      return new Promise((n, i) => {
        this.unsubscribe(e, r, (s, o) => {
          s ? i(s) : n(o);
        });
      });
    }
    end(e, r, n) {
      this.log("end :: (%s)", this.options.clientId), (e == null || typeof e != "boolean") && (n = n || r, r = e, e = false), typeof r != "object" && (n = n || r, r = null), this.log("end :: cb? %s", !!n), (!n || typeof n != "function") && (n = this.noop);
      let i = () => {
        this.log("end :: closeStores: closing incoming and outgoing stores"), this.disconnected = true, this.incomingStore.close((o) => {
          this.outgoingStore.close((a) => {
            if (this.log("end :: closeStores: emitting end"), this.emit("end"), n) {
              let c = o || a;
              this.log("end :: closeStores: invoking callback with args"), n(c);
            }
          });
        }), this._deferredReconnect ? this._deferredReconnect() : (this.options.reconnectPeriod === 0 || this.options.manualConnect) && (this.disconnecting = false);
      }, s = () => {
        this.log("end :: (%s) :: finish :: calling _cleanUp with force %s", this.options.clientId, e), this._cleanUp(e, () => {
          this.log("end :: finish :: calling process.nextTick on closeStores"), (0, bi.nextTick)(i);
        }, r);
      };
      return this.disconnecting ? (n(), this) : (this._clearReconnect(), this.disconnecting = true, !e && Object.keys(this.outgoing).length > 0 ? (this.log("end :: (%s) :: calling finish in 10ms once outgoing is empty", this.options.clientId), this.once("outgoingEmpty", setTimeout.bind(null, s, 10))) : (this.log("end :: (%s) :: immediately calling finish", this.options.clientId), s()), this);
    }
    endAsync(e, r) {
      return new Promise((n, i) => {
        this.end(e, r, (s) => {
          s ? i(s) : n();
        });
      });
    }
    removeOutgoingMessage(e) {
      if (this.outgoing[e]) {
        let { cb: r } = this.outgoing[e];
        this._removeOutgoingAndStoreMessage(e, () => {
          r(new Error("Message removed"));
        });
      }
      return this;
    }
    reconnect(e) {
      this.log("client reconnect");
      let r = () => {
        e ? (this.options.incomingStore = e.incomingStore, this.options.outgoingStore = e.outgoingStore) : (this.options.incomingStore = null, this.options.outgoingStore = null), this.incomingStore = this.options.incomingStore || new vn.default(), this.outgoingStore = this.options.outgoingStore || new vn.default(), this.disconnecting = false, this.disconnected = false, this._deferredReconnect = null, this._reconnect();
      };
      return this.disconnecting && !this.disconnected ? this._deferredReconnect = r : r(), this;
    }
    _flushVolatile() {
      this.outgoing && (this.log("_flushVolatile :: deleting volatile messages from the queue and setting their callbacks as error function"), Object.keys(this.outgoing).forEach((e) => {
        this.outgoing[e].volatile && typeof this.outgoing[e].cb == "function" && (this.outgoing[e].cb(new Error("Connection closed")), delete this.outgoing[e]);
      }));
    }
    _flush() {
      this.outgoing && (this.log("_flush: queue exists? %b", !!this.outgoing), Object.keys(this.outgoing).forEach((e) => {
        typeof this.outgoing[e].cb == "function" && (this.outgoing[e].cb(new Error("Connection closed")), delete this.outgoing[e]);
      }));
    }
    _removeTopicAliasAndRecoverTopicName(e) {
      let r;
      e.properties && (r = e.properties.topicAlias);
      let n = e.topic.toString();
      if (this.log("_removeTopicAliasAndRecoverTopicName :: alias %d, topic %o", r, n), n.length === 0) {
        if (typeof r > "u") return new Error("Unregistered Topic Alias");
        if (n = this.topicAliasSend.getTopicByAlias(r), typeof n > "u") return new Error("Unregistered Topic Alias");
        e.topic = n;
      }
      r && delete e.properties.topicAlias;
    }
    _checkDisconnecting(e) {
      return this.disconnecting && (e && e !== this.noop ? e(new Error("client disconnecting")) : this.emit("error", new Error("client disconnecting"))), this.disconnecting;
    }
    _reconnect() {
      this.log("_reconnect: emitting reconnect to client"), this.emit("reconnect"), this.connected ? (this.end(() => {
        this.connect();
      }), this.log("client already connected. disconnecting first.")) : (this.log("_reconnect: calling connect"), this.connect());
    }
    _setupReconnect() {
      !this.disconnecting && !this.reconnectTimer && this.options.reconnectPeriod > 0 ? (this.reconnecting || (this.log("_setupReconnect :: emit `offline` state"), this.emit("offline"), this.log("_setupReconnect :: set `reconnecting` to `true`"), this.reconnecting = true), this.log("_setupReconnect :: setting reconnectTimer for %d ms", this.options.reconnectPeriod), this.reconnectTimer = setInterval(() => {
        this.log("reconnectTimer :: reconnect triggered!"), this._reconnect();
      }, this.options.reconnectPeriod)) : this.log("_setupReconnect :: doing nothing...");
    }
    _clearReconnect() {
      this.log("_clearReconnect : clearing reconnect timer"), this.reconnectTimer && (clearInterval(this.reconnectTimer), this.reconnectTimer = null);
    }
    _cleanUp(e, r, n = {}) {
      if (r && (this.log("_cleanUp :: done callback provided for on stream close"), this.stream.on("close", r)), this.log("_cleanUp :: forced? %s", e), e) this.options.reconnectPeriod === 0 && this.options.clean && this._flush(), this.log("_cleanUp :: (%s) :: destroying stream", this.options.clientId), this.stream.destroy();
      else {
        let i = { cmd: "disconnect", ...n };
        this.log("_cleanUp :: (%s) :: call _sendPacket with disconnect packet", this.options.clientId), this._sendPacket(i, () => {
          this.log("_cleanUp :: (%s) :: destroying stream", this.options.clientId), Fu(() => {
            this.stream.end(() => {
              this.log("_cleanUp :: (%s) :: stream destroyed", this.options.clientId);
            });
          });
        });
      }
      !this.disconnecting && !this.reconnecting && (this.log("_cleanUp :: client not disconnecting/reconnecting. Clearing and resetting reconnect."), this._clearReconnect(), this._setupReconnect()), this._destroyKeepaliveManager(), r && !this.connected && (this.log("_cleanUp :: (%s) :: removing stream `done` callback `close` listener", this.options.clientId), this.stream.removeListener("close", r), r());
    }
    _storeAndSend(e, r, n) {
      this.log("storeAndSend :: store packet with cmd %s to outgoingStore", e.cmd);
      let i = e, s;
      if (i.cmd === "publish" && (i = (0, Rg.default)(e), s = this._removeTopicAliasAndRecoverTopicName(i), s)) return r && r(s);
      this.outgoingStore.put(i, (o) => {
        if (o) return r && r(o);
        n(), this._writePacket(e, r);
      });
    }
    _applyTopicAlias(e) {
      if (this.options.protocolVersion === 5 && e.cmd === "publish") {
        let r;
        e.properties && (r = e.properties.topicAlias);
        let n = e.topic.toString();
        if (this.topicAliasSend) if (r) {
          if (n.length !== 0 && (this.log("applyTopicAlias :: register topic: %s - alias: %d", n, r), !this.topicAliasSend.put(n, r))) return this.log("applyTopicAlias :: error out of range. topic: %s - alias: %d", n, r), new Error("Sending Topic Alias out of range");
        } else n.length !== 0 && (this.options.autoAssignTopicAlias ? (r = this.topicAliasSend.getAliasByTopic(n), r ? (e.topic = "", e.properties = { ...e.properties, topicAlias: r }, this.log("applyTopicAlias :: auto assign(use) topic: %s - alias: %d", n, r)) : (r = this.topicAliasSend.getLruAlias(), this.topicAliasSend.put(n, r), e.properties = { ...e.properties, topicAlias: r }, this.log("applyTopicAlias :: auto assign topic: %s - alias: %d", n, r))) : this.options.autoUseTopicAlias && (r = this.topicAliasSend.getAliasByTopic(n), r && (e.topic = "", e.properties = { ...e.properties, topicAlias: r }, this.log("applyTopicAlias :: auto use topic: %s - alias: %d", n, r))));
        else if (r) return this.log("applyTopicAlias :: error out of range. topic: %s - alias: %d", n, r), new Error("Sending Topic Alias out of range");
      }
    }
    _noop(e) {
      this.log("noop ::", e);
    }
    _writePacket(e, r) {
      this.log("_writePacket :: packet: %O", e), this.log("_writePacket :: emitting `packetsend`"), this.emit("packetsend", e), this.log("_writePacket :: writing to stream");
      let n = ju.default.writeToStream(e, this.stream, this.options);
      this.log("_writePacket :: writeToStream result %s", n), !n && r && r !== this.noop ? (this.log("_writePacket :: handle events on `drain` once through callback."), this.stream.once("drain", r)) : r && (this.log("_writePacket :: invoking cb"), r());
    }
    _sendPacket(e, r, n, i) {
      this.log("_sendPacket :: (%s) ::  start", this.options.clientId), n = n || this.noop, r = r || this.noop;
      let s = this._applyTopicAlias(e);
      if (s) {
        r(s);
        return;
      }
      if (!this.connected) {
        if (e.cmd === "auth") {
          this._writePacket(e, r);
          return;
        }
        this.log("_sendPacket :: client not connected. Storing packet offline."), this._storePacket(e, r, n);
        return;
      }
      if (i) {
        this._writePacket(e, r);
        return;
      }
      switch (e.cmd) {
        case "publish":
          break;
        case "pubrel":
          this._storeAndSend(e, r, n);
          return;
        default:
          this._writePacket(e, r);
          return;
      }
      switch (e.qos) {
        case 2:
        case 1:
          this._storeAndSend(e, r, n);
          break;
        case 0:
        default:
          this._writePacket(e, r);
          break;
      }
      this.log("_sendPacket :: (%s) ::  end", this.options.clientId);
    }
    _storePacket(e, r, n) {
      this.log("_storePacket :: packet: %o", e), this.log("_storePacket :: cb? %s", !!r), n = n || this.noop;
      let i = e;
      if (i.cmd === "publish") {
        i = (0, Rg.default)(e);
        let o = this._removeTopicAliasAndRecoverTopicName(i);
        if (o) return r && r(o);
      }
      let s = i.qos || 0;
      s === 0 && this.queueQoSZero || i.cmd !== "publish" ? this.queue.push({ packet: i, cb: r }) : s > 0 ? (r = this.outgoing[i.messageId] ? this.outgoing[i.messageId].cb : null, this.outgoingStore.put(i, (o) => {
        if (o) return r && r(o);
        n();
      })) : r && r(new Error("No connection to broker"));
    }
    _setupKeepaliveManager() {
      this.log("_setupKeepaliveManager :: keepalive %d (seconds)", this.options.keepalive), !this.keepaliveManager && this.options.keepalive && (this.keepaliveManager = new QA.default(this, this.options.timerVariant));
    }
    _destroyKeepaliveManager() {
      this.keepaliveManager && (this.log("_destroyKeepaliveManager :: destroying keepalive manager"), this.keepaliveManager.destroy(), this.keepaliveManager = null);
    }
    reschedulePing(e = false) {
      this.keepaliveManager && this.options.keepalive && (e || this.options.reschedulePings) && this._reschedulePing();
    }
    _reschedulePing() {
      this.log("_reschedulePing :: rescheduling ping"), this.keepaliveManager.reschedule();
    }
    sendPing() {
      this.log("_sendPing :: sending pingreq"), this._sendPacket({ cmd: "pingreq" });
    }
    onKeepaliveTimeout() {
      this.emit("error", new Error("Keepalive timeout")), this.log("onKeepaliveTimeout :: calling _cleanUp with force true"), this._cleanUp(true);
    }
    _resubscribe() {
      this.log("_resubscribe");
      let e = Object.keys(this._resubscribeTopics);
      if (!this._firstConnection && (this.options.clean || this.options.protocolVersion >= 4 && !this.connackPacket.sessionPresent) && e.length > 0) if (this.options.resubscribe) if (this.options.protocolVersion === 5) {
        this.log("_resubscribe: protocolVersion 5");
        for (let r = 0; r < e.length; r++) {
          let n = {};
          n[e[r]] = this._resubscribeTopics[e[r]], n.resubscribe = true, this.subscribe(n, { properties: n[e[r]].properties });
        }
      } else this._resubscribeTopics.resubscribe = true, this.subscribe(this._resubscribeTopics);
      else this._resubscribeTopics = {};
      this._firstConnection = false;
    }
    _onConnect(e) {
      if (this.disconnected) {
        this.emit("connect", e);
        return;
      }
      this.connackPacket = e, this.messageIdProvider.clear(), this._setupKeepaliveManager(), this.connected = true;
      let r = () => {
        let n = this.outgoingStore.createStream(), i = () => {
          n.destroy(), n = null, this._flushStoreProcessingQueue(), s();
        }, s = () => {
          this._storeProcessing = false, this._packetIdsDuringStoreProcessing = {};
        };
        this.once("close", i), n.on("error", (a) => {
          s(), this._flushStoreProcessingQueue(), this.removeListener("close", i), this.emit("error", a);
        });
        let o = () => {
          if (!n) return;
          let a = n.read(1), c;
          if (!a) {
            n.once("readable", o);
            return;
          }
          if (this._storeProcessing = true, this._packetIdsDuringStoreProcessing[a.messageId]) {
            o();
            return;
          }
          !this.disconnecting && !this.reconnectTimer ? (c = this.outgoing[a.messageId] ? this.outgoing[a.messageId].cb : null, this.outgoing[a.messageId] = { volatile: false, cb(f, h2) {
            c && c(f, h2), o();
          } }, this._packetIdsDuringStoreProcessing[a.messageId] = true, this.messageIdProvider.register(a.messageId) ? this._sendPacket(a, void 0, void 0, true) : this.log("messageId: %d has already used.", a.messageId)) : n.destroy && n.destroy();
        };
        n.on("end", () => {
          let a = true;
          for (let c in this._packetIdsDuringStoreProcessing) if (!this._packetIdsDuringStoreProcessing[c]) {
            a = false;
            break;
          }
          this.removeListener("close", i), a ? (s(), this._invokeAllStoreProcessingQueue(), this.emit("connect", e)) : r();
        }), o();
      };
      r();
    }
    _invokeStoreProcessingQueue() {
      if (!this._storeProcessing && this._storeProcessingQueue.length > 0) {
        let e = this._storeProcessingQueue[0];
        if (e && e.invoke()) return this._storeProcessingQueue.shift(), true;
      }
      return false;
    }
    _invokeAllStoreProcessingQueue() {
      for (; this._invokeStoreProcessingQueue(); ) ;
    }
    _flushStoreProcessingQueue() {
      for (let e of this._storeProcessingQueue) e.cbStorePut && e.cbStorePut(new Error("Connection closed")), e.callback && e.callback(new Error("Connection closed"));
      this._storeProcessingQueue.splice(0);
    }
    _removeOutgoingAndStoreMessage(e, r) {
      delete this.outgoing[e], this.outgoingStore.del({ messageId: e }, (n, i) => {
        r(n, i), this.messageIdProvider.deallocate(e), this._invokeStoreProcessingQueue();
      });
    }
  };
  yt.default = Wu;
});
var Ng = D((Hu) => {
  A();
  x();
  I();
  Object.defineProperty(Hu, "__esModule", { value: true });
  var YA = wu(), $u = class {
    numberAllocator;
    lastId;
    constructor() {
      this.numberAllocator = new YA.NumberAllocator(1, 65535);
    }
    allocate() {
      return this.lastId = this.numberAllocator.alloc(), this.lastId;
    }
    getLastAllocated() {
      return this.lastId;
    }
    register(e) {
      return this.numberAllocator.use(e);
    }
    deallocate(e) {
      this.numberAllocator.free(e);
    }
    clear() {
      this.numberAllocator.clear();
    }
  };
  Hu.default = $u;
});
function JA() {
  if (qg) return Vu;
  qg = true;
  let t = 2147483647, e = 36, r = 1, n = 26, i = 38, s = 700, o = 72, a = 128, c = "-", f = /^xn--/, h2 = /[^\0-\x7F]/, d = /[\x2E\u3002\uFF0E\uFF61]/g, w = { overflow: "Overflow: input needs wider integers to process", "not-basic": "Illegal input >= 0x80 (not a basic code point)", "invalid-input": "Invalid input" }, y = e - r, P = Math.floor, E = String.fromCharCode;
  function b(H) {
    throw new RangeError(w[H]);
  }
  function m(H, G) {
    let K = [], X = H.length;
    for (; X--; ) K[X] = G(H[X]);
    return K;
  }
  function v(H, G) {
    let K = H.split("@"), X = "";
    K.length > 1 && (X = K[0] + "@", H = K[1]), H = H.replace(d, ".");
    let Q = H.split("."), V = m(Q, G).join(".");
    return X + V;
  }
  function R(H) {
    let G = [], K = 0, X = H.length;
    for (; K < X; ) {
      let Q = H.charCodeAt(K++);
      if (Q >= 55296 && Q <= 56319 && K < X) {
        let V = H.charCodeAt(K++);
        (V & 64512) == 56320 ? G.push(((Q & 1023) << 10) + (V & 1023) + 65536) : (G.push(Q), K--);
      } else G.push(Q);
    }
    return G;
  }
  let T = (H) => String.fromCodePoint(...H), g = function(H) {
    return H >= 48 && H < 58 ? 26 + (H - 48) : H >= 65 && H < 91 ? H - 65 : H >= 97 && H < 123 ? H - 97 : e;
  }, O = function(H, G) {
    return H + 22 + 75 * (H < 26) - ((G != 0) << 5);
  }, _ = function(H, G, K) {
    let X = 0;
    for (H = K ? P(H / s) : H >> 1, H += P(H / G); H > y * n >> 1; X += e) H = P(H / y);
    return P(X + (y + 1) * H / (H + i));
  }, q = function(H) {
    let G = [], K = H.length, X = 0, Q = a, V = o, te = H.lastIndexOf(c);
    te < 0 && (te = 0);
    for (let Y = 0; Y < te; ++Y) H.charCodeAt(Y) >= 128 && b("not-basic"), G.push(H.charCodeAt(Y));
    for (let Y = te > 0 ? te + 1 : 0; Y < K; ) {
      let ee = X;
      for (let Z = 1, ne = e; ; ne += e) {
        Y >= K && b("invalid-input");
        let fe = g(H.charCodeAt(Y++));
        fe >= e && b("invalid-input"), fe > P((t - X) / Z) && b("overflow"), X += fe * Z;
        let ge = ne <= V ? r : ne >= V + n ? n : ne - V;
        if (fe < ge) break;
        let ye = e - ge;
        Z > P(t / ye) && b("overflow"), Z *= ye;
      }
      let pe = G.length + 1;
      V = _(X - ee, pe, ee == 0), P(X / pe) > t - Q && b("overflow"), Q += P(X / pe), X %= pe, G.splice(X++, 0, Q);
    }
    return String.fromCodePoint(...G);
  }, $ = function(H) {
    let G = [];
    H = R(H);
    let K = H.length, X = a, Q = 0, V = o;
    for (let ee of H) ee < 128 && G.push(E(ee));
    let te = G.length, Y = te;
    for (te && G.push(c); Y < K; ) {
      let ee = t;
      for (let Z of H) Z >= X && Z < ee && (ee = Z);
      let pe = Y + 1;
      ee - X > P((t - Q) / pe) && b("overflow"), Q += (ee - X) * pe, X = ee;
      for (let Z of H) if (Z < X && ++Q > t && b("overflow"), Z === X) {
        let ne = Q;
        for (let fe = e; ; fe += e) {
          let ge = fe <= V ? r : fe >= V + n ? n : fe - V;
          if (ne < ge) break;
          let ye = ne - ge, Ae = e - ge;
          G.push(E(O(ge + ye % Ae, 0))), ne = P(ye / Ae);
        }
        G.push(E(O(ne, 0))), V = _(Q, pe, Y === te), Q = 0, ++Y;
      }
      ++Q, ++X;
    }
    return G.join("");
  };
  return Vu = { version: "2.3.1", ucs2: { decode: R, encode: T }, decode: q, encode: $, toASCII: function(H) {
    return v(H, function(G) {
      return h2.test(G) ? "xn--" + $(G) : G;
    });
  }, toUnicode: function(H) {
    return v(H, function(G) {
      return f.test(G) ? q(G.slice(4).toLowerCase()) : G;
    });
  } }, Vu;
}
var Vu, qg, hr, Lg = ze(() => {
  A();
  x();
  I();
  Vu = {}, qg = false;
  hr = JA(), hr.decode, hr.encode, hr.toASCII, hr.toUnicode, hr.ucs2, hr.version;
});
function XA() {
  return Ug || (Ug = true, Gu = function() {
    if (typeof Symbol != "function" || typeof Object.getOwnPropertySymbols != "function") return false;
    if (typeof Symbol.iterator == "symbol") return true;
    var e = {}, r = Symbol("test"), n = Object(r);
    if (typeof r == "string" || Object.prototype.toString.call(r) !== "[object Symbol]" || Object.prototype.toString.call(n) !== "[object Symbol]") return false;
    var i = 42;
    e[r] = i;
    for (r in e) return false;
    if (typeof Object.keys == "function" && Object.keys(e).length !== 0 || typeof Object.getOwnPropertyNames == "function" && Object.getOwnPropertyNames(e).length !== 0) return false;
    var s = Object.getOwnPropertySymbols(e);
    if (s.length !== 1 || s[0] !== r || !Object.prototype.propertyIsEnumerable.call(e, r)) return false;
    if (typeof Object.getOwnPropertyDescriptor == "function") {
      var o = Object.getOwnPropertyDescriptor(e, r);
      if (o.value !== i || o.enumerable !== true) return false;
    }
    return true;
  }), Gu;
}
function ZA() {
  return Dg || (Dg = true, zu = Error), zu;
}
function eI() {
  return jg || (jg = true, Ku = EvalError), Ku;
}
function tI() {
  return Fg || (Fg = true, Qu = RangeError), Qu;
}
function rI() {
  return Wg || (Wg = true, Yu = ReferenceError), Yu;
}
function ob() {
  return $g || ($g = true, Ju = SyntaxError), Ju;
}
function Sn() {
  return Hg || (Hg = true, Xu = TypeError), Xu;
}
function nI() {
  return Vg || (Vg = true, Zu = URIError), Zu;
}
function iI() {
  if (Gg) return el;
  Gg = true;
  var t = typeof Symbol < "u" && Symbol, e = XA();
  return el = function() {
    return typeof t != "function" || typeof Symbol != "function" || typeof t("foo") != "symbol" || typeof Symbol("bar") != "symbol" ? false : e();
  }, el;
}
function oI() {
  if (zg) return tl;
  zg = true;
  var t = { __proto__: null, foo: {} }, e = Object;
  return tl = function() {
    return { __proto__: t }.foo === t.foo && !(t instanceof e);
  }, tl;
}
function sI() {
  if (Kg) return rl;
  Kg = true;
  var t = "Function.prototype.bind called on incompatible ", e = Object.prototype.toString, r = Math.max, n = "[object Function]", i = function(c, f) {
    for (var h2 = [], d = 0; d < c.length; d += 1) h2[d] = c[d];
    for (var w = 0; w < f.length; w += 1) h2[w + c.length] = f[w];
    return h2;
  }, s = function(c, f) {
    for (var h2 = [], d = f, w = 0; d < c.length; d += 1, w += 1) h2[w] = c[d];
    return h2;
  }, o = function(a, c) {
    for (var f = "", h2 = 0; h2 < a.length; h2 += 1) f += a[h2], h2 + 1 < a.length && (f += c);
    return f;
  };
  return rl = function(c) {
    var f = this;
    if (typeof f != "function" || e.apply(f) !== n) throw new TypeError(t + f);
    for (var h2 = s(arguments, 1), d, w = function() {
      if (this instanceof d) {
        var m = f.apply(this, i(h2, arguments));
        return Object(m) === m ? m : this;
      }
      return f.apply(c, i(h2, arguments));
    }, y = r(0, f.length - h2.length), P = [], E = 0; E < y; E++) P[E] = "$" + E;
    if (d = Function("binder", "return function (" + o(P, ",") + "){ return binder.apply(this,arguments); }")(w), f.prototype) {
      var b = function() {
      };
      b.prototype = f.prototype, d.prototype = new b(), b.prototype = null;
    }
    return d;
  }, rl;
}
function hl() {
  if (Qg) return nl;
  Qg = true;
  var t = sI();
  return nl = Function.prototype.bind || t, nl;
}
function aI() {
  if (Yg) return il;
  Yg = true;
  var t = Function.prototype.call, e = Object.prototype.hasOwnProperty, r = hl();
  return il = r.call(t, e), il;
}
function Wr() {
  if (Jg) return ol;
  Jg = true;
  var t, e = ZA(), r = eI(), n = tI(), i = rI(), s = ob(), o = Sn(), a = nI(), c = Function, f = function(Q) {
    try {
      return c('"use strict"; return (' + Q + ").constructor;")();
    } catch {
    }
  }, h2 = Object.getOwnPropertyDescriptor;
  if (h2) try {
    h2({}, "");
  } catch {
    h2 = null;
  }
  var d = function() {
    throw new o();
  }, w = h2 ? (function() {
    try {
      return arguments.callee, d;
    } catch {
      try {
        return h2(arguments, "callee").get;
      } catch {
        return d;
      }
    }
  })() : d, y = iI()(), P = oI()(), E = Object.getPrototypeOf || (P ? function(Q) {
    return Q.__proto__;
  } : null), b = {}, m = typeof Uint8Array > "u" || !E ? t : E(Uint8Array), v = { __proto__: null, "%AggregateError%": typeof AggregateError > "u" ? t : AggregateError, "%Array%": Array, "%ArrayBuffer%": typeof ArrayBuffer > "u" ? t : ArrayBuffer, "%ArrayIteratorPrototype%": y && E ? E([][Symbol.iterator]()) : t, "%AsyncFromSyncIteratorPrototype%": t, "%AsyncFunction%": b, "%AsyncGenerator%": b, "%AsyncGeneratorFunction%": b, "%AsyncIteratorPrototype%": b, "%Atomics%": typeof Atomics > "u" ? t : Atomics, "%BigInt%": typeof BigInt > "u" ? t : BigInt, "%BigInt64Array%": typeof BigInt64Array > "u" ? t : BigInt64Array, "%BigUint64Array%": typeof BigUint64Array > "u" ? t : BigUint64Array, "%Boolean%": Boolean, "%DataView%": typeof DataView > "u" ? t : DataView, "%Date%": Date, "%decodeURI%": decodeURI, "%decodeURIComponent%": decodeURIComponent, "%encodeURI%": encodeURI, "%encodeURIComponent%": encodeURIComponent, "%Error%": e, "%eval%": eval, "%EvalError%": r, "%Float32Array%": typeof Float32Array > "u" ? t : Float32Array, "%Float64Array%": typeof Float64Array > "u" ? t : Float64Array, "%FinalizationRegistry%": typeof FinalizationRegistry > "u" ? t : FinalizationRegistry, "%Function%": c, "%GeneratorFunction%": b, "%Int8Array%": typeof Int8Array > "u" ? t : Int8Array, "%Int16Array%": typeof Int16Array > "u" ? t : Int16Array, "%Int32Array%": typeof Int32Array > "u" ? t : Int32Array, "%isFinite%": isFinite, "%isNaN%": isNaN, "%IteratorPrototype%": y && E ? E(E([][Symbol.iterator]())) : t, "%JSON%": typeof JSON == "object" ? JSON : t, "%Map%": typeof Map > "u" ? t : Map, "%MapIteratorPrototype%": typeof Map > "u" || !y || !E ? t : E((/* @__PURE__ */ new Map())[Symbol.iterator]()), "%Math%": Math, "%Number%": Number, "%Object%": Object, "%parseFloat%": parseFloat, "%parseInt%": parseInt, "%Promise%": typeof Promise > "u" ? t : Promise, "%Proxy%": typeof Proxy > "u" ? t : Proxy, "%RangeError%": n, "%ReferenceError%": i, "%Reflect%": typeof Reflect > "u" ? t : Reflect, "%RegExp%": RegExp, "%Set%": typeof Set > "u" ? t : Set, "%SetIteratorPrototype%": typeof Set > "u" || !y || !E ? t : E((/* @__PURE__ */ new Set())[Symbol.iterator]()), "%SharedArrayBuffer%": typeof SharedArrayBuffer > "u" ? t : SharedArrayBuffer, "%String%": String, "%StringIteratorPrototype%": y && E ? E(""[Symbol.iterator]()) : t, "%Symbol%": y ? Symbol : t, "%SyntaxError%": s, "%ThrowTypeError%": w, "%TypedArray%": m, "%TypeError%": o, "%Uint8Array%": typeof Uint8Array > "u" ? t : Uint8Array, "%Uint8ClampedArray%": typeof Uint8ClampedArray > "u" ? t : Uint8ClampedArray, "%Uint16Array%": typeof Uint16Array > "u" ? t : Uint16Array, "%Uint32Array%": typeof Uint32Array > "u" ? t : Uint32Array, "%URIError%": a, "%WeakMap%": typeof WeakMap > "u" ? t : WeakMap, "%WeakRef%": typeof WeakRef > "u" ? t : WeakRef, "%WeakSet%": typeof WeakSet > "u" ? t : WeakSet };
  if (E) try {
    null.error;
  } catch (Q) {
    var R = E(E(Q));
    v["%Error.prototype%"] = R;
  }
  var T = function Q(V) {
    var te;
    if (V === "%AsyncFunction%") te = f("async function () {}");
    else if (V === "%GeneratorFunction%") te = f("function* () {}");
    else if (V === "%AsyncGeneratorFunction%") te = f("async function* () {}");
    else if (V === "%AsyncGenerator%") {
      var Y = Q("%AsyncGeneratorFunction%");
      Y && (te = Y.prototype);
    } else if (V === "%AsyncIteratorPrototype%") {
      var ee = Q("%AsyncGenerator%");
      ee && E && (te = E(ee.prototype));
    }
    return v[V] = te, te;
  }, g = { __proto__: null, "%ArrayBufferPrototype%": ["ArrayBuffer", "prototype"], "%ArrayPrototype%": ["Array", "prototype"], "%ArrayProto_entries%": ["Array", "prototype", "entries"], "%ArrayProto_forEach%": ["Array", "prototype", "forEach"], "%ArrayProto_keys%": ["Array", "prototype", "keys"], "%ArrayProto_values%": ["Array", "prototype", "values"], "%AsyncFunctionPrototype%": ["AsyncFunction", "prototype"], "%AsyncGenerator%": ["AsyncGeneratorFunction", "prototype"], "%AsyncGeneratorPrototype%": ["AsyncGeneratorFunction", "prototype", "prototype"], "%BooleanPrototype%": ["Boolean", "prototype"], "%DataViewPrototype%": ["DataView", "prototype"], "%DatePrototype%": ["Date", "prototype"], "%ErrorPrototype%": ["Error", "prototype"], "%EvalErrorPrototype%": ["EvalError", "prototype"], "%Float32ArrayPrototype%": ["Float32Array", "prototype"], "%Float64ArrayPrototype%": ["Float64Array", "prototype"], "%FunctionPrototype%": ["Function", "prototype"], "%Generator%": ["GeneratorFunction", "prototype"], "%GeneratorPrototype%": ["GeneratorFunction", "prototype", "prototype"], "%Int8ArrayPrototype%": ["Int8Array", "prototype"], "%Int16ArrayPrototype%": ["Int16Array", "prototype"], "%Int32ArrayPrototype%": ["Int32Array", "prototype"], "%JSONParse%": ["JSON", "parse"], "%JSONStringify%": ["JSON", "stringify"], "%MapPrototype%": ["Map", "prototype"], "%NumberPrototype%": ["Number", "prototype"], "%ObjectPrototype%": ["Object", "prototype"], "%ObjProto_toString%": ["Object", "prototype", "toString"], "%ObjProto_valueOf%": ["Object", "prototype", "valueOf"], "%PromisePrototype%": ["Promise", "prototype"], "%PromiseProto_then%": ["Promise", "prototype", "then"], "%Promise_all%": ["Promise", "all"], "%Promise_reject%": ["Promise", "reject"], "%Promise_resolve%": ["Promise", "resolve"], "%RangeErrorPrototype%": ["RangeError", "prototype"], "%ReferenceErrorPrototype%": ["ReferenceError", "prototype"], "%RegExpPrototype%": ["RegExp", "prototype"], "%SetPrototype%": ["Set", "prototype"], "%SharedArrayBufferPrototype%": ["SharedArrayBuffer", "prototype"], "%StringPrototype%": ["String", "prototype"], "%SymbolPrototype%": ["Symbol", "prototype"], "%SyntaxErrorPrototype%": ["SyntaxError", "prototype"], "%TypedArrayPrototype%": ["TypedArray", "prototype"], "%TypeErrorPrototype%": ["TypeError", "prototype"], "%Uint8ArrayPrototype%": ["Uint8Array", "prototype"], "%Uint8ClampedArrayPrototype%": ["Uint8ClampedArray", "prototype"], "%Uint16ArrayPrototype%": ["Uint16Array", "prototype"], "%Uint32ArrayPrototype%": ["Uint32Array", "prototype"], "%URIErrorPrototype%": ["URIError", "prototype"], "%WeakMapPrototype%": ["WeakMap", "prototype"], "%WeakSetPrototype%": ["WeakSet", "prototype"] }, O = hl(), _ = aI(), q = O.call(Function.call, Array.prototype.concat), $ = O.call(Function.apply, Array.prototype.splice), j = O.call(Function.call, String.prototype.replace), J = O.call(Function.call, String.prototype.slice), W = O.call(Function.call, RegExp.prototype.exec), H = /[^%.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|%$))/g, G = /\\(\\)?/g, K = function(V) {
    var te = J(V, 0, 1), Y = J(V, -1);
    if (te === "%" && Y !== "%") throw new s("invalid intrinsic syntax, expected closing `%`");
    if (Y === "%" && te !== "%") throw new s("invalid intrinsic syntax, expected opening `%`");
    var ee = [];
    return j(V, H, function(pe, Z, ne, fe) {
      ee[ee.length] = ne ? j(fe, G, "$1") : Z || pe;
    }), ee;
  }, X = function(V, te) {
    var Y = V, ee;
    if (_(g, Y) && (ee = g[Y], Y = "%" + ee[0] + "%"), _(v, Y)) {
      var pe = v[Y];
      if (pe === b && (pe = T(Y)), typeof pe > "u" && !te) throw new o("intrinsic " + V + " exists, but is not available. Please file an issue!");
      return { alias: ee, name: Y, value: pe };
    }
    throw new s("intrinsic " + V + " does not exist!");
  };
  return ol = function(V, te) {
    if (typeof V != "string" || V.length === 0) throw new o("intrinsic name must be a non-empty string");
    if (arguments.length > 1 && typeof te != "boolean") throw new o('"allowMissing" argument must be a boolean');
    if (W(/^%?[^%]*%?$/, V) === null) throw new s("`%` may not be present anywhere but at the beginning and end of the intrinsic name");
    var Y = K(V), ee = Y.length > 0 ? Y[0] : "", pe = X("%" + ee + "%", te), Z = pe.name, ne = pe.value, fe = false, ge = pe.alias;
    ge && (ee = ge[0], $(Y, q([0, 1], ge)));
    for (var ye = 1, Ae = true; ye < Y.length; ye += 1) {
      var _e = Y[ye], ve = J(_e, 0, 1), Oe = J(_e, -1);
      if ((ve === '"' || ve === "'" || ve === "`" || Oe === '"' || Oe === "'" || Oe === "`") && ve !== Oe) throw new s("property names with quotes must have matching quotes");
      if ((_e === "constructor" || !Ae) && (fe = true), ee += "." + _e, Z = "%" + ee + "%", _(v, Z)) ne = v[Z];
      else if (ne != null) {
        if (!(_e in ne)) {
          if (!te) throw new o("base intrinsic for " + V + " exists, but the property is not available.");
          return;
        }
        if (h2 && ye + 1 >= Y.length) {
          var xe = h2(ne, _e);
          Ae = !!xe, Ae && "get" in xe && !("originalValue" in xe.get) ? ne = xe.get : ne = ne[_e];
        } else Ae = _(ne, _e), ne = ne[_e];
        Ae && !fe && (v[Z] = ne);
      }
    }
    return ne;
  }, ol;
}
function dl() {
  if (Xg) return sl;
  Xg = true;
  var t = Wr(), e = t("%Object.defineProperty%", true) || false;
  if (e) try {
    e({}, "a", { value: 1 });
  } catch {
    e = false;
  }
  return sl = e, sl;
}
function sb() {
  if (Zg) return al;
  Zg = true;
  var t = Wr(), e = t("%Object.getOwnPropertyDescriptor%", true);
  if (e) try {
    e([], "length");
  } catch {
    e = null;
  }
  return al = e, al;
}
function uI() {
  if (eb) return ul;
  eb = true;
  var t = dl(), e = ob(), r = Sn(), n = sb();
  return ul = function(s, o, a) {
    if (!s || typeof s != "object" && typeof s != "function") throw new r("`obj` must be an object or a function`");
    if (typeof o != "string" && typeof o != "symbol") throw new r("`property` must be a string or a symbol`");
    if (arguments.length > 3 && typeof arguments[3] != "boolean" && arguments[3] !== null) throw new r("`nonEnumerable`, if provided, must be a boolean or null");
    if (arguments.length > 4 && typeof arguments[4] != "boolean" && arguments[4] !== null) throw new r("`nonWritable`, if provided, must be a boolean or null");
    if (arguments.length > 5 && typeof arguments[5] != "boolean" && arguments[5] !== null) throw new r("`nonConfigurable`, if provided, must be a boolean or null");
    if (arguments.length > 6 && typeof arguments[6] != "boolean") throw new r("`loose`, if provided, must be a boolean");
    var c = arguments.length > 3 ? arguments[3] : null, f = arguments.length > 4 ? arguments[4] : null, h2 = arguments.length > 5 ? arguments[5] : null, d = arguments.length > 6 ? arguments[6] : false, w = !!n && n(s, o);
    if (t) t(s, o, { configurable: h2 === null && w ? w.configurable : !h2, enumerable: c === null && w ? w.enumerable : !c, value: a, writable: f === null && w ? w.writable : !f });
    else if (d || !c && !f && !h2) s[o] = a;
    else throw new e("This environment does not support defining a property as non-configurable, non-writable, or non-enumerable.");
  }, ul;
}
function lI() {
  if (tb) return ll;
  tb = true;
  var t = dl(), e = function() {
    return !!t;
  };
  return e.hasArrayLengthDefineBug = function() {
    if (!t) return null;
    try {
      return t([], "length", { value: 1 }).length !== 1;
    } catch {
      return true;
    }
  }, ll = e, ll;
}
function cI() {
  if (rb) return cl;
  rb = true;
  var t = Wr(), e = uI(), r = lI()(), n = sb(), i = Sn(), s = t("%Math.floor%");
  return cl = function(a, c) {
    if (typeof a != "function") throw new i("`fn` is not a function");
    if (typeof c != "number" || c < 0 || c > 4294967295 || s(c) !== c) throw new i("`length` must be a positive 32-bit integer");
    var f = arguments.length > 2 && !!arguments[2], h2 = true, d = true;
    if ("length" in a && n) {
      var w = n(a, "length");
      w && !w.configurable && (h2 = false), w && !w.writable && (d = false);
    }
    return (h2 || d || !f) && (r ? e(a, "length", c, true, true) : e(a, "length", c)), a;
  }, cl;
}
function fI() {
  if (nb) return wi;
  nb = true;
  var t = hl(), e = Wr(), r = cI(), n = Sn(), i = e("%Function.prototype.apply%"), s = e("%Function.prototype.call%"), o = e("%Reflect.apply%", true) || t.call(s, i), a = dl(), c = e("%Math.max%");
  wi = function(d) {
    if (typeof d != "function") throw new n("a function is required");
    var w = o(t, s, arguments);
    return r(w, 1 + c(0, d.length - (arguments.length - 1)), true);
  };
  var f = function() {
    return o(t, i, arguments);
  };
  return a ? a(wi, "apply", { value: f }) : wi.apply = f, wi;
}
function ab() {
  if (ib) return fl;
  ib = true;
  var t = Wr(), e = fI(), r = e(t("String.prototype.indexOf"));
  return fl = function(i, s) {
    var o = t(i, !!s);
    return typeof o == "function" && r(i, ".prototype.") > -1 ? e(o) : o;
  }, fl;
}
var Gu, Ug, zu, Dg, Ku, jg, Qu, Fg, Yu, Wg, Ju, $g, Xu, Hg, Zu, Vg, el, Gg, tl, zg, rl, Kg, nl, Qg, il, Yg, ol, Jg, sl, Xg, al, Zg, ul, eb, ll, tb, cl, rb, wi, nb, fl, ib, ub = ze(() => {
  A();
  x();
  I();
  Gu = {}, Ug = false;
  zu = {}, Dg = false;
  Ku = {}, jg = false;
  Qu = {}, Fg = false;
  Yu = {}, Wg = false;
  Ju = {}, $g = false;
  Xu = {}, Hg = false;
  Zu = {}, Vg = false;
  el = {}, Gg = false;
  tl = {}, zg = false;
  rl = {}, Kg = false;
  nl = {}, Qg = false;
  il = {}, Yg = false;
  ol = {}, Jg = false;
  sl = {}, Xg = false;
  al = {}, Zg = false;
  ul = {}, eb = false;
  ll = {}, tb = false;
  cl = {}, rb = false;
  wi = {}, nb = false;
  fl = {}, ib = false;
});
function bl(t) {
  throw new Error("Node.js process " + t + " is not supported by JSPM core outside of Node.js");
}
function hI() {
  !En || !$r || (En = false, $r.length ? zt = $r.concat(zt) : Ko = -1, zt.length && lb());
}
function lb() {
  if (!En) {
    var t = setTimeout(hI, 0);
    En = true;
    for (var e = zt.length; e; ) {
      for ($r = zt, zt = []; ++Ko < e; ) $r && $r[Ko].run();
      Ko = -1, e = zt.length;
    }
    $r = null, En = false, clearTimeout(t);
  }
}
function dI(t) {
  var e = new Array(arguments.length - 1);
  if (arguments.length > 1) for (var r = 1; r < arguments.length; r++) e[r - 1] = arguments[r];
  zt.push(new cb(t, e)), zt.length === 1 && !En && setTimeout(lb, 0);
}
function cb(t, e) {
  this.fun = t, this.array = e;
}
function We() {
}
function RI(t) {
  bl("_linkedBinding");
}
function MI(t) {
  bl("dlopen");
}
function NI() {
  return [];
}
function qI() {
  return [];
}
function VI(t, e) {
  if (!t) throw new Error(e || "assertion error");
}
function QI() {
  return false;
}
function hx() {
  return dr.now() / 1e3;
}
function gl(t) {
  var e = Math.floor((Date.now() - dr.now()) * 1e-3), r = dr.now() * 1e-3, n = Math.floor(r) + e, i = Math.floor(r % 1 * 1e9);
  return t && (n = n - t[0], i = i - t[1], i < 0 && (n--, i += yl)), [n, i];
}
function pr() {
  return ml;
}
function Ix(t) {
  return [];
}
var zt, En, $r, Ko, pI, yI, gI, bI, wI, mI, _I, vI, SI, EI, AI, II, xI, TI, PI, OI, kI, CI, BI, LI, UI, wl, DI, jI, FI, WI, $I, HI, GI, zI, KI, YI, JI, XI, ZI, ex, tx, rx, nx, ix, ox, sx, ax, ux, lx, cx, fx, dr, pl, yl, dx, px, yx, gx, bx, mx, _x, vx, Sx, Ex, Ax, ml, _l = ze(() => {
  A();
  x();
  I();
  zt = [], En = false, Ko = -1;
  cb.prototype.run = function() {
    this.fun.apply(null, this.array);
  };
  pI = "browser", yI = "x64", gI = "browser", bI = { PATH: "/usr/bin", LANG: navigator.language + ".UTF-8", PWD: "/", HOME: "/home", TMP: "/tmp" }, wI = ["/usr/bin/node"], mI = [], _I = "v16.8.0", vI = {}, SI = function(t, e) {
    console.warn((e ? e + ": " : "") + t);
  }, EI = function(t) {
    bl("binding");
  }, AI = function(t) {
    return 0;
  }, II = function() {
    return "/";
  }, xI = function(t) {
  }, TI = { name: "node", sourceUrl: "", headersUrl: "", libUrl: "" };
  PI = We, OI = [];
  kI = {}, CI = false, BI = {};
  LI = We, UI = We, wl = function() {
    return {};
  }, DI = wl, jI = wl, FI = We, WI = We, $I = We, HI = {};
  GI = { inspector: false, debug: false, uv: false, ipv6: false, tls_alpn: false, tls_sni: false, tls_ocsp: false, tls: false, cached_builtins: true }, zI = We, KI = We;
  YI = We, JI = We, XI = We, ZI = We, ex = We, tx = void 0, rx = void 0, nx = void 0, ix = We, ox = 2, sx = 1, ax = "/bin/usr/node", ux = 9229, lx = "node", cx = [], fx = We, dr = { now: typeof performance < "u" ? performance.now.bind(performance) : void 0, timing: typeof performance < "u" ? performance.timing : void 0 };
  dr.now === void 0 && (pl = Date.now(), dr.timing && dr.timing.navigationStart && (pl = dr.timing.navigationStart), dr.now = () => Date.now() - pl);
  yl = 1e9;
  gl.bigint = function(t) {
    var e = gl(t);
    return typeof BigInt > "u" ? e[0] * yl + e[1] : BigInt(e[0] * yl) + BigInt(e[1]);
  };
  dx = 10, px = {}, yx = 0;
  gx = pr, bx = pr, mx = pr, _x = pr, vx = pr, Sx = We, Ex = pr, Ax = pr;
  ml = { version: _I, versions: vI, arch: yI, platform: gI, release: TI, _rawDebug: PI, moduleLoadList: OI, binding: EI, _linkedBinding: RI, _events: px, _eventsCount: yx, _maxListeners: dx, on: pr, addListener: gx, once: bx, off: mx, removeListener: _x, removeAllListeners: vx, emit: Sx, prependListener: Ex, prependOnceListener: Ax, listeners: Ix, domain: kI, _exiting: CI, config: BI, dlopen: MI, uptime: hx, _getActiveRequests: NI, _getActiveHandles: qI, reallyExit: LI, _kill: UI, cpuUsage: wl, resourceUsage: DI, memoryUsage: jI, kill: FI, exit: WI, openStdin: $I, allowedNodeEnvironmentFlags: HI, assert: VI, features: GI, _fatalExceptions: zI, setUncaughtExceptionCaptureCallback: KI, hasUncaughtExceptionCaptureCallback: QI, emitWarning: SI, nextTick: dI, _tickCallback: YI, _debugProcess: JI, _debugEnd: XI, _startProfilerIdleNotifier: ZI, _stopProfilerIdleNotifier: ex, stdout: tx, stdin: nx, stderr: rx, abort: ix, umask: AI, chdir: xI, cwd: II, env: bI, title: pI, argv: wI, execArgv: mI, pid: ox, ppid: sx, execPath: ax, debugPort: ux, hrtime: gl, argv0: lx, _preload_modules: cx, setSourceMapsEnabled: fx };
});
function xx() {
  if (fb) return vl;
  fb = true;
  var t = ml;
  function e(s) {
    if (typeof s != "string") throw new TypeError("Path must be a string. Received " + JSON.stringify(s));
  }
  function r(s, o) {
    for (var a = "", c = 0, f = -1, h2 = 0, d, w = 0; w <= s.length; ++w) {
      if (w < s.length) d = s.charCodeAt(w);
      else {
        if (d === 47) break;
        d = 47;
      }
      if (d === 47) {
        if (!(f === w - 1 || h2 === 1)) if (f !== w - 1 && h2 === 2) {
          if (a.length < 2 || c !== 2 || a.charCodeAt(a.length - 1) !== 46 || a.charCodeAt(a.length - 2) !== 46) {
            if (a.length > 2) {
              var y = a.lastIndexOf("/");
              if (y !== a.length - 1) {
                y === -1 ? (a = "", c = 0) : (a = a.slice(0, y), c = a.length - 1 - a.lastIndexOf("/")), f = w, h2 = 0;
                continue;
              }
            } else if (a.length === 2 || a.length === 1) {
              a = "", c = 0, f = w, h2 = 0;
              continue;
            }
          }
          o && (a.length > 0 ? a += "/.." : a = "..", c = 2);
        } else a.length > 0 ? a += "/" + s.slice(f + 1, w) : a = s.slice(f + 1, w), c = w - f - 1;
        f = w, h2 = 0;
      } else d === 46 && h2 !== -1 ? ++h2 : h2 = -1;
    }
    return a;
  }
  function n(s, o) {
    var a = o.dir || o.root, c = o.base || (o.name || "") + (o.ext || "");
    return a ? a === o.root ? a + c : a + s + c : c;
  }
  var i = { resolve: function() {
    for (var o = "", a = false, c, f = arguments.length - 1; f >= -1 && !a; f--) {
      var h2;
      f >= 0 ? h2 = arguments[f] : (c === void 0 && (c = t.cwd()), h2 = c), e(h2), h2.length !== 0 && (o = h2 + "/" + o, a = h2.charCodeAt(0) === 47);
    }
    return o = r(o, !a), a ? o.length > 0 ? "/" + o : "/" : o.length > 0 ? o : ".";
  }, normalize: function(o) {
    if (e(o), o.length === 0) return ".";
    var a = o.charCodeAt(0) === 47, c = o.charCodeAt(o.length - 1) === 47;
    return o = r(o, !a), o.length === 0 && !a && (o = "."), o.length > 0 && c && (o += "/"), a ? "/" + o : o;
  }, isAbsolute: function(o) {
    return e(o), o.length > 0 && o.charCodeAt(0) === 47;
  }, join: function() {
    if (arguments.length === 0) return ".";
    for (var o, a = 0; a < arguments.length; ++a) {
      var c = arguments[a];
      e(c), c.length > 0 && (o === void 0 ? o = c : o += "/" + c);
    }
    return o === void 0 ? "." : i.normalize(o);
  }, relative: function(o, a) {
    if (e(o), e(a), o === a || (o = i.resolve(o), a = i.resolve(a), o === a)) return "";
    for (var c = 1; c < o.length && o.charCodeAt(c) === 47; ++c) ;
    for (var f = o.length, h2 = f - c, d = 1; d < a.length && a.charCodeAt(d) === 47; ++d) ;
    for (var w = a.length, y = w - d, P = h2 < y ? h2 : y, E = -1, b = 0; b <= P; ++b) {
      if (b === P) {
        if (y > P) {
          if (a.charCodeAt(d + b) === 47) return a.slice(d + b + 1);
          if (b === 0) return a.slice(d + b);
        } else h2 > P && (o.charCodeAt(c + b) === 47 ? E = b : b === 0 && (E = 0));
        break;
      }
      var m = o.charCodeAt(c + b), v = a.charCodeAt(d + b);
      if (m !== v) break;
      m === 47 && (E = b);
    }
    var R = "";
    for (b = c + E + 1; b <= f; ++b) (b === f || o.charCodeAt(b) === 47) && (R.length === 0 ? R += ".." : R += "/..");
    return R.length > 0 ? R + a.slice(d + E) : (d += E, a.charCodeAt(d) === 47 && ++d, a.slice(d));
  }, _makeLong: function(o) {
    return o;
  }, dirname: function(o) {
    if (e(o), o.length === 0) return ".";
    for (var a = o.charCodeAt(0), c = a === 47, f = -1, h2 = true, d = o.length - 1; d >= 1; --d) if (a = o.charCodeAt(d), a === 47) {
      if (!h2) {
        f = d;
        break;
      }
    } else h2 = false;
    return f === -1 ? c ? "/" : "." : c && f === 1 ? "//" : o.slice(0, f);
  }, basename: function(o, a) {
    if (a !== void 0 && typeof a != "string") throw new TypeError('"ext" argument must be a string');
    e(o);
    var c = 0, f = -1, h2 = true, d;
    if (a !== void 0 && a.length > 0 && a.length <= o.length) {
      if (a.length === o.length && a === o) return "";
      var w = a.length - 1, y = -1;
      for (d = o.length - 1; d >= 0; --d) {
        var P = o.charCodeAt(d);
        if (P === 47) {
          if (!h2) {
            c = d + 1;
            break;
          }
        } else y === -1 && (h2 = false, y = d + 1), w >= 0 && (P === a.charCodeAt(w) ? --w === -1 && (f = d) : (w = -1, f = y));
      }
      return c === f ? f = y : f === -1 && (f = o.length), o.slice(c, f);
    } else {
      for (d = o.length - 1; d >= 0; --d) if (o.charCodeAt(d) === 47) {
        if (!h2) {
          c = d + 1;
          break;
        }
      } else f === -1 && (h2 = false, f = d + 1);
      return f === -1 ? "" : o.slice(c, f);
    }
  }, extname: function(o) {
    e(o);
    for (var a = -1, c = 0, f = -1, h2 = true, d = 0, w = o.length - 1; w >= 0; --w) {
      var y = o.charCodeAt(w);
      if (y === 47) {
        if (!h2) {
          c = w + 1;
          break;
        }
        continue;
      }
      f === -1 && (h2 = false, f = w + 1), y === 46 ? a === -1 ? a = w : d !== 1 && (d = 1) : a !== -1 && (d = -1);
    }
    return a === -1 || f === -1 || d === 0 || d === 1 && a === f - 1 && a === c + 1 ? "" : o.slice(a, f);
  }, format: function(o) {
    if (o === null || typeof o != "object") throw new TypeError('The "pathObject" argument must be of type Object. Received type ' + typeof o);
    return n("/", o);
  }, parse: function(o) {
    e(o);
    var a = { root: "", dir: "", base: "", ext: "", name: "" };
    if (o.length === 0) return a;
    var c = o.charCodeAt(0), f = c === 47, h2;
    f ? (a.root = "/", h2 = 1) : h2 = 0;
    for (var d = -1, w = 0, y = -1, P = true, E = o.length - 1, b = 0; E >= h2; --E) {
      if (c = o.charCodeAt(E), c === 47) {
        if (!P) {
          w = E + 1;
          break;
        }
        continue;
      }
      y === -1 && (P = false, y = E + 1), c === 46 ? d === -1 ? d = E : b !== 1 && (b = 1) : d !== -1 && (b = -1);
    }
    return d === -1 || y === -1 || b === 0 || b === 1 && d === y - 1 && d === w + 1 ? y !== -1 && (w === 0 && f ? a.base = a.name = o.slice(1, y) : a.base = a.name = o.slice(w, y)) : (w === 0 && f ? (a.name = o.slice(1, d), a.base = o.slice(1, y)) : (a.name = o.slice(w, d), a.base = o.slice(w, y)), a.ext = o.slice(d, y)), w > 0 ? a.dir = o.slice(0, w - 1) : f && (a.dir = "/"), a;
  }, sep: "/", delimiter: ":", win32: null, posix: null };
  return i.posix = i, vl = i, vl;
}
var vl, fb, Sl, hb = ze(() => {
  A();
  x();
  I();
  _l();
  vl = {}, fb = false;
  Sl = xx();
});
var Ab = {};
Kr(Ab, { URL: () => jx, Url: () => Nx, default: () => Ge, fileURLToPath: () => Sb, format: () => qx, parse: () => Dx, pathToFileURL: () => Eb, resolve: () => Lx, resolveObject: () => Ux });
function Px() {
  if (db) return El;
  db = true;
  var t = typeof Map == "function" && Map.prototype, e = Object.getOwnPropertyDescriptor && t ? Object.getOwnPropertyDescriptor(Map.prototype, "size") : null, r = t && e && typeof e.get == "function" ? e.get : null, n = t && Map.prototype.forEach, i = typeof Set == "function" && Set.prototype, s = Object.getOwnPropertyDescriptor && i ? Object.getOwnPropertyDescriptor(Set.prototype, "size") : null, o = i && s && typeof s.get == "function" ? s.get : null, a = i && Set.prototype.forEach, c = typeof WeakMap == "function" && WeakMap.prototype, f = c ? WeakMap.prototype.has : null, h2 = typeof WeakSet == "function" && WeakSet.prototype, d = h2 ? WeakSet.prototype.has : null, w = typeof WeakRef == "function" && WeakRef.prototype, y = w ? WeakRef.prototype.deref : null, P = Boolean.prototype.valueOf, E = Object.prototype.toString, b = Function.prototype.toString, m = String.prototype.match, v = String.prototype.slice, R = String.prototype.replace, T = String.prototype.toUpperCase, g = String.prototype.toLowerCase, O = RegExp.prototype.test, _ = Array.prototype.concat, q = Array.prototype.join, $ = Array.prototype.slice, j = Math.floor, J = typeof BigInt == "function" ? BigInt.prototype.valueOf : null, W = Object.getOwnPropertySymbols, H = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? Symbol.prototype.toString : null, G = typeof Symbol == "function" && typeof Symbol.iterator == "object", K = typeof Symbol == "function" && Symbol.toStringTag && (typeof Symbol.toStringTag === G || true) ? Symbol.toStringTag : null, X = Object.prototype.propertyIsEnumerable, Q = (typeof Reflect == "function" ? Reflect.getPrototypeOf : Object.getPrototypeOf) || ([].__proto__ === Array.prototype ? function(k) {
    return k.__proto__;
  } : null);
  function V(k, B) {
    if (k === 1 / 0 || k === -1 / 0 || k !== k || k && k > -1e3 && k < 1e3 || O.call(/e/, B)) return B;
    var re = /[0-9](?=(?:[0-9]{3})+(?![0-9]))/g;
    if (typeof k == "number") {
      var ie = k < 0 ? -j(-k) : j(k);
      if (ie !== k) {
        var oe = String(ie), ce = v.call(B, oe.length + 1);
        return R.call(oe, re, "$&_") + "." + R.call(R.call(ce, /([0-9]{3})/g, "$&_"), /_$/, "");
      }
    }
    return R.call(B, re, "$&_");
  }
  var te = Tx, Y = te.custom, ee = Oe(Y) ? Y : null;
  El = function k(B, re, ie, oe) {
    var ce = re || {};
    if (Re(ce, "quoteStyle") && ce.quoteStyle !== "single" && ce.quoteStyle !== "double") throw new TypeError('option "quoteStyle" must be "single" or "double"');
    if (Re(ce, "maxStringLength") && (typeof ce.maxStringLength == "number" ? ce.maxStringLength < 0 && ce.maxStringLength !== 1 / 0 : ce.maxStringLength !== null)) throw new TypeError('option "maxStringLength", if provided, must be a positive integer, Infinity, or `null`');
    var Qt = Re(ce, "customInspect") ? ce.customInspect : true;
    if (typeof Qt != "boolean" && Qt !== "symbol") throw new TypeError("option \"customInspect\", if provided, must be `true`, `false`, or `'symbol'`");
    if (Re(ce, "indent") && ce.indent !== null && ce.indent !== "	" && !(parseInt(ce.indent, 10) === ce.indent && ce.indent > 0)) throw new TypeError('option "indent" must be "\\t", an integer > 0, or `null`');
    if (Re(ce, "numericSeparator") && typeof ce.numericSeparator != "boolean") throw new TypeError('option "numericSeparator", if provided, must be `true` or `false`');
    var br = ce.numericSeparator;
    if (typeof B > "u") return "undefined";
    if (B === null) return "null";
    if (typeof B == "boolean") return B ? "true" : "false";
    if (typeof B == "string") return at(B, ce);
    if (typeof B == "number") {
      if (B === 0) return 1 / 0 / B > 0 ? "0" : "-0";
      var Ye = String(B);
      return br ? V(B, Ye) : Ye;
    }
    if (typeof B == "bigint") {
      var Yt = String(B) + "n";
      return br ? V(B, Yt) : Yt;
    }
    var es = typeof ce.depth > "u" ? 5 : ce.depth;
    if (typeof ie > "u" && (ie = 0), ie >= es && es > 0 && typeof B == "object") return ne(B) ? "[Array]" : "[Object]";
    var Gr = C(ce, ie);
    if (typeof oe > "u") oe = [];
    else if (Bt(oe, B) >= 0) return "[Circular]";
    function ut(zr, Ti, Qb) {
      if (Ti && (oe = $.call(oe), oe.push(Ti)), Qb) {
        var nc = { depth: ce.depth };
        return Re(ce, "quoteStyle") && (nc.quoteStyle = ce.quoteStyle), k(zr, nc, ie + 1, oe);
      }
      return k(zr, ce, ie + 1, oe);
    }
    if (typeof B == "function" && !ge(B)) {
      var Yl = Ct(B), Jl = F(B, ut);
      return "[Function" + (Yl ? ": " + Yl : " (anonymous)") + "]" + (Jl.length > 0 ? " { " + q.call(Jl, ", ") + " }" : "");
    }
    if (Oe(B)) {
      var Xl = G ? R.call(String(B), /^(Symbol\(.*\))_[^)]*$/, "$1") : H.call(B);
      return typeof B == "object" && !G ? p2(Xl) : Xl;
    }
    if (Xo(B)) {
      for (var Pn = "<" + g.call(String(B.nodeName)), ts = B.attributes || [], xi = 0; xi < ts.length; xi++) Pn += " " + ts[xi].name + "=" + pe(Z(ts[xi].value), "double", ce);
      return Pn += ">", B.childNodes && B.childNodes.length && (Pn += "..."), Pn += "</" + g.call(String(B.nodeName)) + ">", Pn;
    }
    if (ne(B)) {
      if (B.length === 0) return "[]";
      var rs = F(B, ut);
      return Gr && !S(rs) ? "[" + N(rs, Gr) + "]" : "[ " + q.call(rs, ", ") + " ]";
    }
    if (ye(B)) {
      var ns = F(B, ut);
      return !("cause" in Error.prototype) && "cause" in B && !X.call(B, "cause") ? "{ [" + String(B) + "] " + q.call(_.call("[cause]: " + ut(B.cause), ns), ", ") + " }" : ns.length === 0 ? "[" + String(B) + "]" : "{ [" + String(B) + "] " + q.call(ns, ", ") + " }";
    }
    if (typeof B == "object" && Qt) {
      if (ee && typeof B[ee] == "function" && te) return te(B, { depth: es - ie });
      if (Qt !== "symbol" && typeof B.inspect == "function") return B.inspect();
    }
    if (xn(B)) {
      var Zl = [];
      return n && n.call(B, function(zr, Ti) {
        Zl.push(ut(Ti, B, true) + " => " + ut(zr, B));
      }), l("Map", r.call(B), Zl, Gr);
    }
    if (Ze(B)) {
      var ec = [];
      return a && a.call(B, function(zr) {
        ec.push(ut(zr, B));
      }), l("Set", o.call(B), ec, Gr);
    }
    if (Ii(B)) return u("WeakMap");
    if (Tn(B)) return u("WeakSet");
    if (Vr(B)) return u("WeakRef");
    if (_e(B)) return p2(ut(Number(B)));
    if (xe(B)) return p2(ut(J.call(B)));
    if (ve(B)) return p2(P.call(B));
    if (Ae(B)) return p2(ut(String(B)));
    if (typeof window < "u" && B === window) return "{ [object Window] }";
    if (typeof globalThis < "u" && B === globalThis || typeof Al < "u" && B === Al) return "{ [object globalThis] }";
    if (!fe(B) && !ge(B)) {
      var is = F(B, ut), tc = Q ? Q(B) === Object.prototype : B instanceof Object || B.constructor === Object, os = B instanceof Object ? "" : "null prototype", rc = !tc && K && Object(B) === B && K in B ? v.call(Ne(B), 8, -1) : os ? "Object" : "", Kb = tc || typeof B.constructor != "function" ? "" : B.constructor.name ? B.constructor.name + " " : "", ss = Kb + (rc || os ? "[" + q.call(_.call([], rc || [], os || []), ": ") + "] " : "");
      return is.length === 0 ? ss + "{}" : Gr ? ss + "{" + N(is, Gr) + "}" : ss + "{ " + q.call(is, ", ") + " }";
    }
    return String(B);
  };
  function pe(k, B, re) {
    var ie = (re.quoteStyle || B) === "double" ? '"' : "'";
    return ie + k + ie;
  }
  function Z(k) {
    return R.call(String(k), /"/g, "&quot;");
  }
  function ne(k) {
    return Ne(k) === "[object Array]" && (!K || !(typeof k == "object" && K in k));
  }
  function fe(k) {
    return Ne(k) === "[object Date]" && (!K || !(typeof k == "object" && K in k));
  }
  function ge(k) {
    return Ne(k) === "[object RegExp]" && (!K || !(typeof k == "object" && K in k));
  }
  function ye(k) {
    return Ne(k) === "[object Error]" && (!K || !(typeof k == "object" && K in k));
  }
  function Ae(k) {
    return Ne(k) === "[object String]" && (!K || !(typeof k == "object" && K in k));
  }
  function _e(k) {
    return Ne(k) === "[object Number]" && (!K || !(typeof k == "object" && K in k));
  }
  function ve(k) {
    return Ne(k) === "[object Boolean]" && (!K || !(typeof k == "object" && K in k));
  }
  function Oe(k) {
    if (G) return k && typeof k == "object" && k instanceof Symbol;
    if (typeof k == "symbol") return true;
    if (!k || typeof k != "object" || !H) return false;
    try {
      return H.call(k), true;
    } catch {
    }
    return false;
  }
  function xe(k) {
    if (!k || typeof k != "object" || !J) return false;
    try {
      return J.call(k), true;
    } catch {
    }
    return false;
  }
  var st = Object.prototype.hasOwnProperty || function(k) {
    return k in (this || Al);
  };
  function Re(k, B) {
    return st.call(k, B);
  }
  function Ne(k) {
    return E.call(k);
  }
  function Ct(k) {
    if (k.name) return k.name;
    var B = m.call(b.call(k), /^function\s*([\w$]+)/);
    return B ? B[1] : null;
  }
  function Bt(k, B) {
    if (k.indexOf) return k.indexOf(B);
    for (var re = 0, ie = k.length; re < ie; re++) if (k[re] === B) return re;
    return -1;
  }
  function xn(k) {
    if (!r || !k || typeof k != "object") return false;
    try {
      r.call(k);
      try {
        o.call(k);
      } catch {
        return true;
      }
      return k instanceof Map;
    } catch {
    }
    return false;
  }
  function Ii(k) {
    if (!f || !k || typeof k != "object") return false;
    try {
      f.call(k, f);
      try {
        d.call(k, d);
      } catch {
        return true;
      }
      return k instanceof WeakMap;
    } catch {
    }
    return false;
  }
  function Vr(k) {
    if (!y || !k || typeof k != "object") return false;
    try {
      return y.call(k), true;
    } catch {
    }
    return false;
  }
  function Ze(k) {
    if (!o || !k || typeof k != "object") return false;
    try {
      o.call(k);
      try {
        r.call(k);
      } catch {
        return true;
      }
      return k instanceof Set;
    } catch {
    }
    return false;
  }
  function Tn(k) {
    if (!d || !k || typeof k != "object") return false;
    try {
      d.call(k, d);
      try {
        f.call(k, f);
      } catch {
        return true;
      }
      return k instanceof WeakSet;
    } catch {
    }
    return false;
  }
  function Xo(k) {
    return !k || typeof k != "object" ? false : typeof HTMLElement < "u" && k instanceof HTMLElement ? true : typeof k.nodeName == "string" && typeof k.getAttribute == "function";
  }
  function at(k, B) {
    if (k.length > B.maxStringLength) {
      var re = k.length - B.maxStringLength, ie = "... " + re + " more character" + (re > 1 ? "s" : "");
      return at(v.call(k, 0, B.maxStringLength), B) + ie;
    }
    var oe = R.call(R.call(k, /(['\\])/g, "\\$1"), /[\x00-\x1f]/g, Zo);
    return pe(oe, "single", B);
  }
  function Zo(k) {
    var B = k.charCodeAt(0), re = { 8: "b", 9: "t", 10: "n", 12: "f", 13: "r" }[B];
    return re ? "\\" + re : "\\x" + (B < 16 ? "0" : "") + T.call(B.toString(16));
  }
  function p2(k) {
    return "Object(" + k + ")";
  }
  function u(k) {
    return k + " { ? }";
  }
  function l(k, B, re, ie) {
    var oe = ie ? N(re, ie) : q.call(re, ", ");
    return k + " (" + B + ") {" + oe + "}";
  }
  function S(k) {
    for (var B = 0; B < k.length; B++) if (Bt(k[B], `
`) >= 0) return false;
    return true;
  }
  function C(k, B) {
    var re;
    if (k.indent === "	") re = "	";
    else if (typeof k.indent == "number" && k.indent > 0) re = q.call(Array(k.indent + 1), " ");
    else return null;
    return { base: re, prev: q.call(Array(B + 1), re) };
  }
  function N(k, B) {
    if (k.length === 0) return "";
    var re = `
` + B.prev + B.base;
    return re + q.call(k, "," + re) + `
` + B.prev;
  }
  function F(k, B) {
    var re = ne(k), ie = [];
    if (re) {
      ie.length = k.length;
      for (var oe = 0; oe < k.length; oe++) ie[oe] = Re(k, oe) ? B(k[oe], k) : "";
    }
    var ce = typeof W == "function" ? W(k) : [], Qt;
    if (G) {
      Qt = {};
      for (var br = 0; br < ce.length; br++) Qt["$" + ce[br]] = ce[br];
    }
    for (var Ye in k) Re(k, Ye) && (re && String(Number(Ye)) === Ye && Ye < k.length || G && Qt["$" + Ye] instanceof Symbol || (O.call(/[^\w$]/, Ye) ? ie.push(B(Ye, k) + ": " + B(k[Ye], k)) : ie.push(Ye + ": " + B(k[Ye], k))));
    if (typeof W == "function") for (var Yt = 0; Yt < ce.length; Yt++) X.call(k, ce[Yt]) && ie.push("[" + B(ce[Yt]) + "]: " + B(k[ce[Yt]], k));
    return ie;
  }
  return El;
}
function Ox() {
  if (pb) return Il;
  pb = true;
  var t = Wr(), e = ab(), r = Px(), n = Sn(), i = t("%WeakMap%", true), s = t("%Map%", true), o = e("WeakMap.prototype.get", true), a = e("WeakMap.prototype.set", true), c = e("WeakMap.prototype.has", true), f = e("Map.prototype.get", true), h2 = e("Map.prototype.set", true), d = e("Map.prototype.has", true), w = function(b, m) {
    for (var v = b, R; (R = v.next) !== null; v = R) if (R.key === m) return v.next = R.next, R.next = b.next, b.next = R, R;
  }, y = function(b, m) {
    var v = w(b, m);
    return v && v.value;
  }, P = function(b, m, v) {
    var R = w(b, m);
    R ? R.value = v : b.next = { key: m, next: b.next, value: v };
  }, E = function(b, m) {
    return !!w(b, m);
  };
  return Il = function() {
    var m, v, R, T = { assert: function(g) {
      if (!T.has(g)) throw new n("Side channel does not contain " + r(g));
    }, get: function(g) {
      if (i && g && (typeof g == "object" || typeof g == "function")) {
        if (m) return o(m, g);
      } else if (s) {
        if (v) return f(v, g);
      } else if (R) return y(R, g);
    }, has: function(g) {
      if (i && g && (typeof g == "object" || typeof g == "function")) {
        if (m) return c(m, g);
      } else if (s) {
        if (v) return d(v, g);
      } else if (R) return E(R, g);
      return false;
    }, set: function(g, O) {
      i && g && (typeof g == "object" || typeof g == "function") ? (m || (m = new i()), a(m, g, O)) : s ? (v || (v = new s()), h2(v, g, O)) : (R || (R = { key: {}, next: null }), P(R, g, O));
    } };
    return T;
  }, Il;
}
function Cl() {
  if (yb) return xl;
  yb = true;
  var t = String.prototype.replace, e = /%20/g, r = { RFC1738: "RFC1738", RFC3986: "RFC3986" };
  return xl = { default: r.RFC3986, formatters: { RFC1738: function(n) {
    return t.call(n, e, "+");
  }, RFC3986: function(n) {
    return String(n);
  } }, RFC1738: r.RFC1738, RFC3986: r.RFC3986 }, xl;
}
function vb() {
  if (gb) return Tl;
  gb = true;
  var t = Cl(), e = Object.prototype.hasOwnProperty, r = Array.isArray, n = (function() {
    for (var b = [], m = 0; m < 256; ++m) b.push("%" + ((m < 16 ? "0" : "") + m.toString(16)).toUpperCase());
    return b;
  })(), i = function(m) {
    for (; m.length > 1; ) {
      var v = m.pop(), R = v.obj[v.prop];
      if (r(R)) {
        for (var T = [], g = 0; g < R.length; ++g) typeof R[g] < "u" && T.push(R[g]);
        v.obj[v.prop] = T;
      }
    }
  }, s = function(m, v) {
    for (var R = v && v.plainObjects ? /* @__PURE__ */ Object.create(null) : {}, T = 0; T < m.length; ++T) typeof m[T] < "u" && (R[T] = m[T]);
    return R;
  }, o = function b(m, v, R) {
    if (!v) return m;
    if (typeof v != "object") {
      if (r(m)) m.push(v);
      else if (m && typeof m == "object") (R && (R.plainObjects || R.allowPrototypes) || !e.call(Object.prototype, v)) && (m[v] = true);
      else return [m, v];
      return m;
    }
    if (!m || typeof m != "object") return [m].concat(v);
    var T = m;
    return r(m) && !r(v) && (T = s(m, R)), r(m) && r(v) ? (v.forEach(function(g, O) {
      if (e.call(m, O)) {
        var _ = m[O];
        _ && typeof _ == "object" && g && typeof g == "object" ? m[O] = b(_, g, R) : m.push(g);
      } else m[O] = g;
    }), m) : Object.keys(v).reduce(function(g, O) {
      var _ = v[O];
      return e.call(g, O) ? g[O] = b(g[O], _, R) : g[O] = _, g;
    }, T);
  }, a = function(m, v) {
    return Object.keys(v).reduce(function(R, T) {
      return R[T] = v[T], R;
    }, m);
  }, c = function(b, m, v) {
    var R = b.replace(/\+/g, " ");
    if (v === "iso-8859-1") return R.replace(/%[0-9a-f]{2}/gi, unescape);
    try {
      return decodeURIComponent(R);
    } catch {
      return R;
    }
  }, f = 1024, h2 = function(m, v, R, T, g) {
    if (m.length === 0) return m;
    var O = m;
    if (typeof m == "symbol" ? O = Symbol.prototype.toString.call(m) : typeof m != "string" && (O = String(m)), R === "iso-8859-1") return escape(O).replace(/%u[0-9a-f]{4}/gi, function(H) {
      return "%26%23" + parseInt(H.slice(2), 16) + "%3B";
    });
    for (var _ = "", q = 0; q < O.length; q += f) {
      for (var $ = O.length >= f ? O.slice(q, q + f) : O, j = [], J = 0; J < $.length; ++J) {
        var W = $.charCodeAt(J);
        if (W === 45 || W === 46 || W === 95 || W === 126 || W >= 48 && W <= 57 || W >= 65 && W <= 90 || W >= 97 && W <= 122 || g === t.RFC1738 && (W === 40 || W === 41)) {
          j[j.length] = $.charAt(J);
          continue;
        }
        if (W < 128) {
          j[j.length] = n[W];
          continue;
        }
        if (W < 2048) {
          j[j.length] = n[192 | W >> 6] + n[128 | W & 63];
          continue;
        }
        if (W < 55296 || W >= 57344) {
          j[j.length] = n[224 | W >> 12] + n[128 | W >> 6 & 63] + n[128 | W & 63];
          continue;
        }
        J += 1, W = 65536 + ((W & 1023) << 10 | $.charCodeAt(J) & 1023), j[j.length] = n[240 | W >> 18] + n[128 | W >> 12 & 63] + n[128 | W >> 6 & 63] + n[128 | W & 63];
      }
      _ += j.join("");
    }
    return _;
  }, d = function(m) {
    for (var v = [{ obj: { o: m }, prop: "o" }], R = [], T = 0; T < v.length; ++T) for (var g = v[T], O = g.obj[g.prop], _ = Object.keys(O), q = 0; q < _.length; ++q) {
      var $ = _[q], j = O[$];
      typeof j == "object" && j !== null && R.indexOf(j) === -1 && (v.push({ obj: O, prop: $ }), R.push(j));
    }
    return i(v), m;
  }, w = function(m) {
    return Object.prototype.toString.call(m) === "[object RegExp]";
  }, y = function(m) {
    return !m || typeof m != "object" ? false : !!(m.constructor && m.constructor.isBuffer && m.constructor.isBuffer(m));
  }, P = function(m, v) {
    return [].concat(m, v);
  }, E = function(m, v) {
    if (r(m)) {
      for (var R = [], T = 0; T < m.length; T += 1) R.push(v(m[T]));
      return R;
    }
    return v(m);
  };
  return Tl = { arrayToObject: s, assign: a, combine: P, compact: d, decode: c, encode: h2, isBuffer: y, isRegExp: w, maybeMap: E, merge: o }, Tl;
}
function Rx() {
  if (bb) return Pl;
  bb = true;
  var t = Ox(), e = vb(), r = Cl(), n = Object.prototype.hasOwnProperty, i = { brackets: function(b) {
    return b + "[]";
  }, comma: "comma", indices: function(b, m) {
    return b + "[" + m + "]";
  }, repeat: function(b) {
    return b;
  } }, s = Array.isArray, o = Array.prototype.push, a = function(E, b) {
    o.apply(E, s(b) ? b : [b]);
  }, c = Date.prototype.toISOString, f = r.default, h2 = { addQueryPrefix: false, allowDots: false, allowEmptyArrays: false, arrayFormat: "indices", charset: "utf-8", charsetSentinel: false, delimiter: "&", encode: true, encodeDotInKeys: false, encoder: e.encode, encodeValuesOnly: false, format: f, formatter: r.formatters[f], indices: false, serializeDate: function(b) {
    return c.call(b);
  }, skipNulls: false, strictNullHandling: false }, d = function(b) {
    return typeof b == "string" || typeof b == "number" || typeof b == "boolean" || typeof b == "symbol" || typeof b == "bigint";
  }, w = {}, y = function E(b, m, v, R, T, g, O, _, q, $, j, J, W, H, G, K, X, Q) {
    for (var V = b, te = Q, Y = 0, ee = false; (te = te.get(w)) !== void 0 && !ee; ) {
      var pe = te.get(b);
      if (Y += 1, typeof pe < "u") {
        if (pe === Y) throw new RangeError("Cyclic object value");
        ee = true;
      }
      typeof te.get(w) > "u" && (Y = 0);
    }
    if (typeof $ == "function" ? V = $(m, V) : V instanceof Date ? V = W(V) : v === "comma" && s(V) && (V = e.maybeMap(V, function(Ne) {
      return Ne instanceof Date ? W(Ne) : Ne;
    })), V === null) {
      if (g) return q && !K ? q(m, h2.encoder, X, "key", H) : m;
      V = "";
    }
    if (d(V) || e.isBuffer(V)) {
      if (q) {
        var Z = K ? m : q(m, h2.encoder, X, "key", H);
        return [G(Z) + "=" + G(q(V, h2.encoder, X, "value", H))];
      }
      return [G(m) + "=" + G(String(V))];
    }
    var ne = [];
    if (typeof V > "u") return ne;
    var fe;
    if (v === "comma" && s(V)) K && q && (V = e.maybeMap(V, q)), fe = [{ value: V.length > 0 ? V.join(",") || null : void 0 }];
    else if (s($)) fe = $;
    else {
      var ge = Object.keys(V);
      fe = j ? ge.sort(j) : ge;
    }
    var ye = _ ? m.replace(/\./g, "%2E") : m, Ae = R && s(V) && V.length === 1 ? ye + "[]" : ye;
    if (T && s(V) && V.length === 0) return Ae + "[]";
    for (var _e = 0; _e < fe.length; ++_e) {
      var ve = fe[_e], Oe = typeof ve == "object" && typeof ve.value < "u" ? ve.value : V[ve];
      if (!(O && Oe === null)) {
        var xe = J && _ ? ve.replace(/\./g, "%2E") : ve, st = s(V) ? typeof v == "function" ? v(Ae, xe) : Ae : Ae + (J ? "." + xe : "[" + xe + "]");
        Q.set(b, Y);
        var Re = t();
        Re.set(w, Q), a(ne, E(Oe, st, v, R, T, g, O, _, v === "comma" && K && s(V) ? null : q, $, j, J, W, H, G, K, X, Re));
      }
    }
    return ne;
  }, P = function(b) {
    if (!b) return h2;
    if (typeof b.allowEmptyArrays < "u" && typeof b.allowEmptyArrays != "boolean") throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
    if (typeof b.encodeDotInKeys < "u" && typeof b.encodeDotInKeys != "boolean") throw new TypeError("`encodeDotInKeys` option can only be `true` or `false`, when provided");
    if (b.encoder !== null && typeof b.encoder < "u" && typeof b.encoder != "function") throw new TypeError("Encoder has to be a function.");
    var m = b.charset || h2.charset;
    if (typeof b.charset < "u" && b.charset !== "utf-8" && b.charset !== "iso-8859-1") throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
    var v = r.default;
    if (typeof b.format < "u") {
      if (!n.call(r.formatters, b.format)) throw new TypeError("Unknown format option provided.");
      v = b.format;
    }
    var R = r.formatters[v], T = h2.filter;
    (typeof b.filter == "function" || s(b.filter)) && (T = b.filter);
    var g;
    if (b.arrayFormat in i ? g = b.arrayFormat : "indices" in b ? g = b.indices ? "indices" : "repeat" : g = h2.arrayFormat, "commaRoundTrip" in b && typeof b.commaRoundTrip != "boolean") throw new TypeError("`commaRoundTrip` must be a boolean, or absent");
    var O = typeof b.allowDots > "u" ? b.encodeDotInKeys === true ? true : h2.allowDots : !!b.allowDots;
    return { addQueryPrefix: typeof b.addQueryPrefix == "boolean" ? b.addQueryPrefix : h2.addQueryPrefix, allowDots: O, allowEmptyArrays: typeof b.allowEmptyArrays == "boolean" ? !!b.allowEmptyArrays : h2.allowEmptyArrays, arrayFormat: g, charset: m, charsetSentinel: typeof b.charsetSentinel == "boolean" ? b.charsetSentinel : h2.charsetSentinel, commaRoundTrip: b.commaRoundTrip, delimiter: typeof b.delimiter > "u" ? h2.delimiter : b.delimiter, encode: typeof b.encode == "boolean" ? b.encode : h2.encode, encodeDotInKeys: typeof b.encodeDotInKeys == "boolean" ? b.encodeDotInKeys : h2.encodeDotInKeys, encoder: typeof b.encoder == "function" ? b.encoder : h2.encoder, encodeValuesOnly: typeof b.encodeValuesOnly == "boolean" ? b.encodeValuesOnly : h2.encodeValuesOnly, filter: T, format: v, formatter: R, serializeDate: typeof b.serializeDate == "function" ? b.serializeDate : h2.serializeDate, skipNulls: typeof b.skipNulls == "boolean" ? b.skipNulls : h2.skipNulls, sort: typeof b.sort == "function" ? b.sort : null, strictNullHandling: typeof b.strictNullHandling == "boolean" ? b.strictNullHandling : h2.strictNullHandling };
  };
  return Pl = function(E, b) {
    var m = E, v = P(b), R, T;
    typeof v.filter == "function" ? (T = v.filter, m = T("", m)) : s(v.filter) && (T = v.filter, R = T);
    var g = [];
    if (typeof m != "object" || m === null) return "";
    var O = i[v.arrayFormat], _ = O === "comma" && v.commaRoundTrip;
    R || (R = Object.keys(m)), v.sort && R.sort(v.sort);
    for (var q = t(), $ = 0; $ < R.length; ++$) {
      var j = R[$];
      v.skipNulls && m[j] === null || a(g, y(m[j], j, O, _, v.allowEmptyArrays, v.strictNullHandling, v.skipNulls, v.encodeDotInKeys, v.encode ? v.encoder : null, v.filter, v.sort, v.allowDots, v.serializeDate, v.format, v.formatter, v.encodeValuesOnly, v.charset, q));
    }
    var J = g.join(v.delimiter), W = v.addQueryPrefix === true ? "?" : "";
    return v.charsetSentinel && (v.charset === "iso-8859-1" ? W += "utf8=%26%2310003%3B&" : W += "utf8=%E2%9C%93&"), J.length > 0 ? W + J : "";
  }, Pl;
}
function kx() {
  if (wb) return Ol;
  wb = true;
  var t = vb(), e = Object.prototype.hasOwnProperty, r = Array.isArray, n = { allowDots: false, allowEmptyArrays: false, allowPrototypes: false, allowSparse: false, arrayLimit: 20, charset: "utf-8", charsetSentinel: false, comma: false, decodeDotInKeys: false, decoder: t.decode, delimiter: "&", depth: 5, duplicates: "combine", ignoreQueryPrefix: false, interpretNumericEntities: false, parameterLimit: 1e3, parseArrays: true, plainObjects: false, strictDepth: false, strictNullHandling: false }, i = function(w) {
    return w.replace(/&#(\d+);/g, function(y, P) {
      return String.fromCharCode(parseInt(P, 10));
    });
  }, s = function(w, y) {
    return w && typeof w == "string" && y.comma && w.indexOf(",") > -1 ? w.split(",") : w;
  }, o = "utf8=%26%2310003%3B", a = "utf8=%E2%9C%93", c = function(y, P) {
    var E = { __proto__: null }, b = P.ignoreQueryPrefix ? y.replace(/^\?/, "") : y;
    b = b.replace(/%5B/gi, "[").replace(/%5D/gi, "]");
    var m = P.parameterLimit === 1 / 0 ? void 0 : P.parameterLimit, v = b.split(P.delimiter, m), R = -1, T, g = P.charset;
    if (P.charsetSentinel) for (T = 0; T < v.length; ++T) v[T].indexOf("utf8=") === 0 && (v[T] === a ? g = "utf-8" : v[T] === o && (g = "iso-8859-1"), R = T, T = v.length);
    for (T = 0; T < v.length; ++T) if (T !== R) {
      var O = v[T], _ = O.indexOf("]="), q = _ === -1 ? O.indexOf("=") : _ + 1, $, j;
      q === -1 ? ($ = P.decoder(O, n.decoder, g, "key"), j = P.strictNullHandling ? null : "") : ($ = P.decoder(O.slice(0, q), n.decoder, g, "key"), j = t.maybeMap(s(O.slice(q + 1), P), function(W) {
        return P.decoder(W, n.decoder, g, "value");
      })), j && P.interpretNumericEntities && g === "iso-8859-1" && (j = i(j)), O.indexOf("[]=") > -1 && (j = r(j) ? [j] : j);
      var J = e.call(E, $);
      J && P.duplicates === "combine" ? E[$] = t.combine(E[$], j) : (!J || P.duplicates === "last") && (E[$] = j);
    }
    return E;
  }, f = function(w, y, P, E) {
    for (var b = E ? y : s(y, P), m = w.length - 1; m >= 0; --m) {
      var v, R = w[m];
      if (R === "[]" && P.parseArrays) v = P.allowEmptyArrays && (b === "" || P.strictNullHandling && b === null) ? [] : [].concat(b);
      else {
        v = P.plainObjects ? /* @__PURE__ */ Object.create(null) : {};
        var T = R.charAt(0) === "[" && R.charAt(R.length - 1) === "]" ? R.slice(1, -1) : R, g = P.decodeDotInKeys ? T.replace(/%2E/g, ".") : T, O = parseInt(g, 10);
        !P.parseArrays && g === "" ? v = { 0: b } : !isNaN(O) && R !== g && String(O) === g && O >= 0 && P.parseArrays && O <= P.arrayLimit ? (v = [], v[O] = b) : g !== "__proto__" && (v[g] = b);
      }
      b = v;
    }
    return b;
  }, h2 = function(y, P, E, b) {
    if (y) {
      var m = E.allowDots ? y.replace(/\.([^.[]+)/g, "[$1]") : y, v = /(\[[^[\]]*])/, R = /(\[[^[\]]*])/g, T = E.depth > 0 && v.exec(m), g = T ? m.slice(0, T.index) : m, O = [];
      if (g) {
        if (!E.plainObjects && e.call(Object.prototype, g) && !E.allowPrototypes) return;
        O.push(g);
      }
      for (var _ = 0; E.depth > 0 && (T = R.exec(m)) !== null && _ < E.depth; ) {
        if (_ += 1, !E.plainObjects && e.call(Object.prototype, T[1].slice(1, -1)) && !E.allowPrototypes) return;
        O.push(T[1]);
      }
      if (T) {
        if (E.strictDepth === true) throw new RangeError("Input depth exceeded depth option of " + E.depth + " and strictDepth is true");
        O.push("[" + m.slice(T.index) + "]");
      }
      return f(O, P, E, b);
    }
  }, d = function(y) {
    if (!y) return n;
    if (typeof y.allowEmptyArrays < "u" && typeof y.allowEmptyArrays != "boolean") throw new TypeError("`allowEmptyArrays` option can only be `true` or `false`, when provided");
    if (typeof y.decodeDotInKeys < "u" && typeof y.decodeDotInKeys != "boolean") throw new TypeError("`decodeDotInKeys` option can only be `true` or `false`, when provided");
    if (y.decoder !== null && typeof y.decoder < "u" && typeof y.decoder != "function") throw new TypeError("Decoder has to be a function.");
    if (typeof y.charset < "u" && y.charset !== "utf-8" && y.charset !== "iso-8859-1") throw new TypeError("The charset option must be either utf-8, iso-8859-1, or undefined");
    var P = typeof y.charset > "u" ? n.charset : y.charset, E = typeof y.duplicates > "u" ? n.duplicates : y.duplicates;
    if (E !== "combine" && E !== "first" && E !== "last") throw new TypeError("The duplicates option must be either combine, first, or last");
    var b = typeof y.allowDots > "u" ? y.decodeDotInKeys === true ? true : n.allowDots : !!y.allowDots;
    return { allowDots: b, allowEmptyArrays: typeof y.allowEmptyArrays == "boolean" ? !!y.allowEmptyArrays : n.allowEmptyArrays, allowPrototypes: typeof y.allowPrototypes == "boolean" ? y.allowPrototypes : n.allowPrototypes, allowSparse: typeof y.allowSparse == "boolean" ? y.allowSparse : n.allowSparse, arrayLimit: typeof y.arrayLimit == "number" ? y.arrayLimit : n.arrayLimit, charset: P, charsetSentinel: typeof y.charsetSentinel == "boolean" ? y.charsetSentinel : n.charsetSentinel, comma: typeof y.comma == "boolean" ? y.comma : n.comma, decodeDotInKeys: typeof y.decodeDotInKeys == "boolean" ? y.decodeDotInKeys : n.decodeDotInKeys, decoder: typeof y.decoder == "function" ? y.decoder : n.decoder, delimiter: typeof y.delimiter == "string" || t.isRegExp(y.delimiter) ? y.delimiter : n.delimiter, depth: typeof y.depth == "number" || y.depth === false ? +y.depth : n.depth, duplicates: E, ignoreQueryPrefix: y.ignoreQueryPrefix === true, interpretNumericEntities: typeof y.interpretNumericEntities == "boolean" ? y.interpretNumericEntities : n.interpretNumericEntities, parameterLimit: typeof y.parameterLimit == "number" ? y.parameterLimit : n.parameterLimit, parseArrays: y.parseArrays !== false, plainObjects: typeof y.plainObjects == "boolean" ? y.plainObjects : n.plainObjects, strictDepth: typeof y.strictDepth == "boolean" ? !!y.strictDepth : n.strictDepth, strictNullHandling: typeof y.strictNullHandling == "boolean" ? y.strictNullHandling : n.strictNullHandling };
  };
  return Ol = function(w, y) {
    var P = d(y);
    if (w === "" || w === null || typeof w > "u") return P.plainObjects ? /* @__PURE__ */ Object.create(null) : {};
    for (var E = typeof w == "string" ? c(w, P) : w, b = P.plainObjects ? /* @__PURE__ */ Object.create(null) : {}, m = Object.keys(E), v = 0; v < m.length; ++v) {
      var R = m[v], T = h2(R, E[R], P, typeof w == "string");
      b = t.merge(b, T, P);
    }
    return P.allowSparse === true ? b : t.compact(b);
  }, Ol;
}
function Cx() {
  if (mb) return Rl;
  mb = true;
  var t = Rx(), e = kx(), r = Cl();
  return Rl = { formats: r, parse: e, stringify: t }, Rl;
}
function Bx() {
  if (_b) return Hr;
  _b = true;
  var t = hr;
  function e() {
    this.protocol = null, this.slashes = null, this.auth = null, this.host = null, this.port = null, this.hostname = null, this.hash = null, this.search = null, this.query = null, this.pathname = null, this.path = null, this.href = null;
  }
  var r = /^([a-z0-9.+-]+:)/i, n = /:[0-9]*$/, i = /^(\/\/?(?!\/)[^?\s]*)(\?[^\s]*)?$/, s = ["<", ">", '"', "`", " ", "\r", `
`, "	"], o = ["{", "}", "|", "\\", "^", "`"].concat(s), a = ["'"].concat(o), c = ["%", "/", "?", ";", "#"].concat(a), f = ["/", "?", "#"], h2 = 255, d = /^[+a-z0-9A-Z_-]{0,63}$/, w = /^([+a-z0-9A-Z_-]{0,63})(.*)$/, y = { javascript: true, "javascript:": true }, P = { javascript: true, "javascript:": true }, E = { http: true, https: true, ftp: true, gopher: true, file: true, "http:": true, "https:": true, "ftp:": true, "gopher:": true, "file:": true }, b = Cx();
  function m(g, O, _) {
    if (g && typeof g == "object" && g instanceof e) return g;
    var q = new e();
    return q.parse(g, O, _), q;
  }
  e.prototype.parse = function(g, O, _) {
    if (typeof g != "string") throw new TypeError("Parameter 'url' must be a string, not " + typeof g);
    var q = g.indexOf("?"), $ = q !== -1 && q < g.indexOf("#") ? "?" : "#", j = g.split($), J = /\\/g;
    j[0] = j[0].replace(J, "/"), g = j.join($);
    var W = g;
    if (W = W.trim(), !_ && g.split("#").length === 1) {
      var H = i.exec(W);
      if (H) return this.path = W, this.href = W, this.pathname = H[1], H[2] ? (this.search = H[2], O ? this.query = b.parse(this.search.substr(1)) : this.query = this.search.substr(1)) : O && (this.search = "", this.query = {}), this;
    }
    var G = r.exec(W);
    if (G) {
      G = G[0];
      var K = G.toLowerCase();
      this.protocol = K, W = W.substr(G.length);
    }
    if (_ || G || W.match(/^\/\/[^@/]+@[^@/]+/)) {
      var X = W.substr(0, 2) === "//";
      X && !(G && P[G]) && (W = W.substr(2), this.slashes = true);
    }
    if (!P[G] && (X || G && !E[G])) {
      for (var Q = -1, V = 0; V < f.length; V++) {
        var te = W.indexOf(f[V]);
        te !== -1 && (Q === -1 || te < Q) && (Q = te);
      }
      var Y, ee;
      Q === -1 ? ee = W.lastIndexOf("@") : ee = W.lastIndexOf("@", Q), ee !== -1 && (Y = W.slice(0, ee), W = W.slice(ee + 1), this.auth = decodeURIComponent(Y)), Q = -1;
      for (var V = 0; V < c.length; V++) {
        var te = W.indexOf(c[V]);
        te !== -1 && (Q === -1 || te < Q) && (Q = te);
      }
      Q === -1 && (Q = W.length), this.host = W.slice(0, Q), W = W.slice(Q), this.parseHost(), this.hostname = this.hostname || "";
      var pe = this.hostname[0] === "[" && this.hostname[this.hostname.length - 1] === "]";
      if (!pe) for (var Z = this.hostname.split(/\./), V = 0, ne = Z.length; V < ne; V++) {
        var fe = Z[V];
        if (fe && !fe.match(d)) {
          for (var ge = "", ye = 0, Ae = fe.length; ye < Ae; ye++) fe.charCodeAt(ye) > 127 ? ge += "x" : ge += fe[ye];
          if (!ge.match(d)) {
            var _e = Z.slice(0, V), ve = Z.slice(V + 1), Oe = fe.match(w);
            Oe && (_e.push(Oe[1]), ve.unshift(Oe[2])), ve.length && (W = "/" + ve.join(".") + W), this.hostname = _e.join(".");
            break;
          }
        }
      }
      this.hostname.length > h2 ? this.hostname = "" : this.hostname = this.hostname.toLowerCase(), pe || (this.hostname = t.toASCII(this.hostname));
      var xe = this.port ? ":" + this.port : "", st = this.hostname || "";
      this.host = st + xe, this.href += this.host, pe && (this.hostname = this.hostname.substr(1, this.hostname.length - 2), W[0] !== "/" && (W = "/" + W));
    }
    if (!y[K]) for (var V = 0, ne = a.length; V < ne; V++) {
      var Re = a[V];
      if (W.indexOf(Re) !== -1) {
        var Ne = encodeURIComponent(Re);
        Ne === Re && (Ne = escape(Re)), W = W.split(Re).join(Ne);
      }
    }
    var Ct = W.indexOf("#");
    Ct !== -1 && (this.hash = W.substr(Ct), W = W.slice(0, Ct));
    var Bt = W.indexOf("?");
    if (Bt !== -1 ? (this.search = W.substr(Bt), this.query = W.substr(Bt + 1), O && (this.query = b.parse(this.query)), W = W.slice(0, Bt)) : O && (this.search = "", this.query = {}), W && (this.pathname = W), E[K] && this.hostname && !this.pathname && (this.pathname = "/"), this.pathname || this.search) {
      var xe = this.pathname || "", xn = this.search || "";
      this.path = xe + xn;
    }
    return this.href = this.format(), this;
  };
  function v(g) {
    return typeof g == "string" && (g = m(g)), g instanceof e ? g.format() : e.prototype.format.call(g);
  }
  e.prototype.format = function() {
    var g = this.auth || "";
    g && (g = encodeURIComponent(g), g = g.replace(/%3A/i, ":"), g += "@");
    var O = this.protocol || "", _ = this.pathname || "", q = this.hash || "", $ = false, j = "";
    this.host ? $ = g + this.host : this.hostname && ($ = g + (this.hostname.indexOf(":") === -1 ? this.hostname : "[" + this.hostname + "]"), this.port && ($ += ":" + this.port)), this.query && typeof this.query == "object" && Object.keys(this.query).length && (j = b.stringify(this.query, { arrayFormat: "repeat", addQueryPrefix: false }));
    var J = this.search || j && "?" + j || "";
    return O && O.substr(-1) !== ":" && (O += ":"), this.slashes || (!O || E[O]) && $ !== false ? ($ = "//" + ($ || ""), _ && _.charAt(0) !== "/" && (_ = "/" + _)) : $ || ($ = ""), q && q.charAt(0) !== "#" && (q = "#" + q), J && J.charAt(0) !== "?" && (J = "?" + J), _ = _.replace(/[?#]/g, function(W) {
      return encodeURIComponent(W);
    }), J = J.replace("#", "%23"), O + $ + _ + J + q;
  };
  function R(g, O) {
    return m(g, false, true).resolve(O);
  }
  e.prototype.resolve = function(g) {
    return this.resolveObject(m(g, false, true)).format();
  };
  function T(g, O) {
    return g ? m(g, false, true).resolveObject(O) : O;
  }
  return e.prototype.resolveObject = function(g) {
    if (typeof g == "string") {
      var O = new e();
      O.parse(g, false, true), g = O;
    }
    for (var _ = new e(), q = Object.keys(this), $ = 0; $ < q.length; $++) {
      var j = q[$];
      _[j] = this[j];
    }
    if (_.hash = g.hash, g.href === "") return _.href = _.format(), _;
    if (g.slashes && !g.protocol) {
      for (var J = Object.keys(g), W = 0; W < J.length; W++) {
        var H = J[W];
        H !== "protocol" && (_[H] = g[H]);
      }
      return E[_.protocol] && _.hostname && !_.pathname && (_.pathname = "/", _.path = _.pathname), _.href = _.format(), _;
    }
    if (g.protocol && g.protocol !== _.protocol) {
      if (!E[g.protocol]) {
        for (var G = Object.keys(g), K = 0; K < G.length; K++) {
          var X = G[K];
          _[X] = g[X];
        }
        return _.href = _.format(), _;
      }
      if (_.protocol = g.protocol, !g.host && !P[g.protocol]) {
        for (var ne = (g.pathname || "").split("/"); ne.length && !(g.host = ne.shift()); ) ;
        g.host || (g.host = ""), g.hostname || (g.hostname = ""), ne[0] !== "" && ne.unshift(""), ne.length < 2 && ne.unshift(""), _.pathname = ne.join("/");
      } else _.pathname = g.pathname;
      if (_.search = g.search, _.query = g.query, _.host = g.host || "", _.auth = g.auth, _.hostname = g.hostname || g.host, _.port = g.port, _.pathname || _.search) {
        var Q = _.pathname || "", V = _.search || "";
        _.path = Q + V;
      }
      return _.slashes = _.slashes || g.slashes, _.href = _.format(), _;
    }
    var te = _.pathname && _.pathname.charAt(0) === "/", Y = g.host || g.pathname && g.pathname.charAt(0) === "/", ee = Y || te || _.host && g.pathname, pe = ee, Z = _.pathname && _.pathname.split("/") || [], ne = g.pathname && g.pathname.split("/") || [], fe = _.protocol && !E[_.protocol];
    if (fe && (_.hostname = "", _.port = null, _.host && (Z[0] === "" ? Z[0] = _.host : Z.unshift(_.host)), _.host = "", g.protocol && (g.hostname = null, g.port = null, g.host && (ne[0] === "" ? ne[0] = g.host : ne.unshift(g.host)), g.host = null), ee = ee && (ne[0] === "" || Z[0] === "")), Y) _.host = g.host || g.host === "" ? g.host : _.host, _.hostname = g.hostname || g.hostname === "" ? g.hostname : _.hostname, _.search = g.search, _.query = g.query, Z = ne;
    else if (ne.length) Z || (Z = []), Z.pop(), Z = Z.concat(ne), _.search = g.search, _.query = g.query;
    else if (g.search != null) {
      if (fe) {
        _.host = Z.shift(), _.hostname = _.host;
        var ge = _.host && _.host.indexOf("@") > 0 ? _.host.split("@") : false;
        ge && (_.auth = ge.shift(), _.hostname = ge.shift(), _.host = _.hostname);
      }
      return _.search = g.search, _.query = g.query, (_.pathname !== null || _.search !== null) && (_.path = (_.pathname ? _.pathname : "") + (_.search ? _.search : "")), _.href = _.format(), _;
    }
    if (!Z.length) return _.pathname = null, _.search ? _.path = "/" + _.search : _.path = null, _.href = _.format(), _;
    for (var ye = Z.slice(-1)[0], Ae = (_.host || g.host || Z.length > 1) && (ye === "." || ye === "..") || ye === "", _e = 0, ve = Z.length; ve >= 0; ve--) ye = Z[ve], ye === "." ? Z.splice(ve, 1) : ye === ".." ? (Z.splice(ve, 1), _e++) : _e && (Z.splice(ve, 1), _e--);
    if (!ee && !pe) for (; _e--; _e) Z.unshift("..");
    ee && Z[0] !== "" && (!Z[0] || Z[0].charAt(0) !== "/") && Z.unshift(""), Ae && Z.join("/").substr(-1) !== "/" && Z.push("");
    var Oe = Z[0] === "" || Z[0] && Z[0].charAt(0) === "/";
    if (fe) {
      _.hostname = Oe ? "" : Z.length ? Z.shift() : "", _.host = _.hostname;
      var ge = _.host && _.host.indexOf("@") > 0 ? _.host.split("@") : false;
      ge && (_.auth = ge.shift(), _.hostname = ge.shift(), _.host = _.hostname);
    }
    return ee = ee || _.host && Z.length, ee && !Oe && Z.unshift(""), Z.length > 0 ? _.pathname = Z.join("/") : (_.pathname = null, _.path = null), (_.pathname !== null || _.search !== null) && (_.path = (_.pathname ? _.pathname : "") + (_.search ? _.search : "")), _.auth = g.auth || _.auth, _.slashes = _.slashes || g.slashes, _.href = _.format(), _;
  }, e.prototype.parseHost = function() {
    var g = this.host, O = n.exec(g);
    O && (O = O[0], O !== ":" && (this.port = O.substr(1)), g = g.substr(0, g.length - O.length)), g && (this.hostname = g);
  }, Hr.parse = m, Hr.resolve = R, Hr.resolveObject = T, Hr.format = v, Hr.Url = e, Hr;
}
function Sb(t) {
  if (typeof t == "string") t = new URL(t);
  else if (!(t instanceof URL)) throw new Deno.errors.InvalidData("invalid argument path , must be a string or URL");
  if (t.protocol !== "file:") throw new Deno.errors.InvalidData("invalid url scheme");
  return kl ? Jx(t) : Xx(t);
}
function Jx(t) {
  let e = t.hostname, r = t.pathname;
  for (let n = 0; n < r.length; n++) if (r[n] === "%") {
    let i = r.codePointAt(n + 2) || 32;
    if (r[n + 1] === "2" && i === 102 || r[n + 1] === "5" && i === 99) throw new Deno.errors.InvalidData("must not include encoded \\ or / characters");
  }
  if (r = r.replace(Vx, "\\"), r = decodeURIComponent(r), e !== "") return `\\\\${e}${r}`;
  {
    let n = r.codePointAt(1) | 32, i = r[2];
    if (n < $x || n > Hx || i !== ":") throw new Deno.errors.InvalidData("file url path must be absolute");
    return r.slice(1);
  }
}
function Xx(t) {
  if (t.hostname !== "") throw new Deno.errors.InvalidData("invalid file url hostname");
  let e = t.pathname;
  for (let r = 0; r < e.length; r++) if (e[r] === "%") {
    let n = e.codePointAt(r + 2) || 32;
    if (e[r + 1] === "2" && n === 102) throw new Deno.errors.InvalidData("must not include encoded / characters");
  }
  return decodeURIComponent(e);
}
function Eb(t) {
  let e = Sl.resolve(t), r = t.charCodeAt(t.length - 1);
  (r === Wx || kl && r === Fx) && e[e.length - 1] !== Sl.sep && (e += "/");
  let n = new URL("file://");
  return e.includes("%") && (e = e.replace(Gx, "%25")), !kl && e.includes("\\") && (e = e.replace(zx, "%5C")), e.includes(`
`) && (e = e.replace(Kx, "%0A")), e.includes("\r") && (e = e.replace(Qx, "%0D")), e.includes("	") && (e = e.replace(Yx, "%09")), n.pathname = e, n;
}
var Tx, El, db, Al, Il, pb, xl, yb, Tl, gb, Pl, bb, Ol, wb, Rl, mb, Hr, _b, Ge, Mx, Nx, qx, Lx, Ux, Dx, jx, Fx, Wx, $x, Hx, kl, Vx, Gx, zx, Kx, Qx, Yx, Ib = ze(() => {
  A();
  x();
  I();
  Lg();
  ub();
  hb();
  _l();
  Tx = Object.freeze(/* @__PURE__ */ Object.create(null)), El = {}, db = false, Al = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : global;
  Il = {}, pb = false;
  xl = {}, yb = false;
  Tl = {}, gb = false;
  Pl = {}, bb = false;
  Ol = {}, wb = false;
  Rl = {}, mb = false;
  Hr = {}, _b = false;
  Ge = Bx();
  Ge.parse;
  Ge.resolve;
  Ge.resolveObject;
  Ge.format;
  Ge.Url;
  Mx = typeof Deno < "u" ? Deno.build.os === "windows" ? "win32" : Deno.build.os : void 0;
  Ge.URL = typeof URL < "u" ? URL : null;
  Ge.pathToFileURL = Eb;
  Ge.fileURLToPath = Sb;
  Nx = Ge.Url, qx = Ge.format, Lx = Ge.resolve, Ux = Ge.resolveObject, Dx = Ge.parse, jx = Ge.URL, Fx = 92, Wx = 47, $x = 97, Hx = 122, kl = Mx === "win32", Vx = /\//g, Gx = /%/g, zx = /\\/g, Kx = /\n/g, Qx = /\r/g, Yx = /\t/g;
});
var Tb = D((A9, xb) => {
  A();
  x();
  I();
  xb.exports = function() {
    throw new Error("ws does not work in the browser. Browser clients must use the native WebSocket object");
  };
});
var Qo = D((mi) => {
  A();
  x();
  I();
  Object.defineProperty(mi, "__esModule", { value: true });
  mi.BufferedDuplex = void 0;
  mi.writev = Ob;
  var Zx = or(), Pb = (ke(), me(qe));
  function Ob(t, e) {
    let r = new Array(t.length);
    for (let n = 0; n < t.length; n++) typeof t[n].chunk == "string" ? r[n] = Pb.Buffer.from(t[n].chunk, "utf8") : r[n] = t[n].chunk;
    this._write(Pb.Buffer.concat(r), "binary", e);
  }
  var Bl = class extends Zx.Duplex {
    socket;
    proxy;
    isSocketOpen;
    writeQueue;
    constructor(e, r, n) {
      super({ objectMode: true }), this.proxy = r, this.socket = n, this.writeQueue = [], e.objectMode || (this._writev = Ob.bind(this)), this.isSocketOpen = false, this.proxy.on("data", (i) => {
        !this.destroyed && this.readable && this.push(i);
      });
    }
    _read(e) {
      this.proxy.read(e);
    }
    _write(e, r, n) {
      this.isSocketOpen ? this.writeToProxy(e, r, n) : this.writeQueue.push({ chunk: e, encoding: r, cb: n });
    }
    _final(e) {
      this.writeQueue = [], this.proxy.end(e);
    }
    _destroy(e, r) {
      this.writeQueue = [], this.proxy.destroy(), r(e);
    }
    socketReady() {
      this.emit("connect"), this.isSocketOpen = true, this.processWriteQueue();
    }
    writeToProxy(e, r, n) {
      this.proxy.write(e, r) === false ? this.proxy.once("drain", n) : n();
    }
    processWriteQueue() {
      for (; this.writeQueue.length > 0; ) {
        let { chunk: e, encoding: r, cb: n } = this.writeQueue.shift();
        this.writeToProxy(e, r, n);
      }
    }
  };
  mi.BufferedDuplex = Bl;
});
var _i = D((gr) => {
  A();
  x();
  I();
  var Nl = gr && gr.__importDefault || function(t) {
    return t && t.__esModule ? t : { default: t };
  };
  Object.defineProperty(gr, "__esModule", { value: true });
  gr.streamBuilder = gr.browserStreamBuilder = void 0;
  var Yo = (ke(), me(qe)), Rb = Nl(Tb()), eT = Nl(At()), tT = or(), rT = Nl(yi()), Ml = Qo(), yr = (0, eT.default)("mqttjs:ws"), nT = ["rejectUnauthorized", "ca", "cert", "key", "pfx", "passphrase"];
  function kb(t, e) {
    let r = `${t.protocol}://${t.hostname}:${t.port}${t.path}`;
    return typeof t.transformWsUrl == "function" && (r = t.transformWsUrl(r, t, e)), r;
  }
  function Cb(t) {
    let e = t;
    return t.port || (t.protocol === "wss" ? e.port = 443 : e.port = 80), t.path || (e.path = "/"), t.wsOptions || (e.wsOptions = {}), !rT.default && !t.forceNativeWebSocket && t.protocol === "wss" && nT.forEach((r) => {
      Object.prototype.hasOwnProperty.call(t, r) && !Object.prototype.hasOwnProperty.call(t.wsOptions, r) && (e.wsOptions[r] = t[r]);
    }), e;
  }
  function iT(t) {
    let e = Cb(t);
    if (e.hostname || (e.hostname = e.host), !e.hostname) {
      if (typeof document > "u") throw new Error("Could not determine host. Specify host manually.");
      let r = new URL(document.URL);
      e.hostname = r.hostname, e.port || (e.port = Number(r.port));
    }
    return e.objectMode === void 0 && (e.objectMode = !(e.binary === true || e.binary === void 0)), e;
  }
  function oT(t, e, r) {
    yr("createWebSocket"), yr(`protocol: ${r.protocolId} ${r.protocolVersion}`);
    let n = r.protocolId === "MQIsdp" && r.protocolVersion === 3 ? "mqttv3.1" : "mqtt";
    yr(`creating new Websocket for url: ${e} and protocol: ${n}`);
    let i;
    return r.createWebsocket ? i = r.createWebsocket(e, [n], r) : i = new Rb.default(e, [n], r.wsOptions), i;
  }
  function sT(t, e) {
    let r = e.protocolId === "MQIsdp" && e.protocolVersion === 3 ? "mqttv3.1" : "mqtt", n = kb(e, t), i;
    return e.createWebsocket ? i = e.createWebsocket(n, [r], e) : i = new WebSocket(n, [r]), i.binaryType = "arraybuffer", i;
  }
  var aT = (t, e) => {
    yr("streamBuilder");
    let r = Cb(e);
    r.hostname = r.hostname || r.host || "localhost";
    let n = kb(r, t), i = oT(t, n, r), s = Rb.default.createWebSocketStream(i, r.wsOptions);
    return s.url = n, i.on("close", () => {
      s.destroy();
    }), s;
  };
  gr.streamBuilder = aT;
  var uT = (t, e) => {
    yr("browserStreamBuilder");
    let r, i = iT(e).browserBufferSize || 1024 * 512, s = e.browserBufferTimeout || 1e3, o = !e.objectMode, a = sT(t, e), c = h2(e, E, b);
    e.objectMode || (c._writev = Ml.writev.bind(c)), c.on("close", () => {
      a.close();
    });
    let f = typeof a.addEventListener < "u";
    a.readyState === a.OPEN ? (r = c, r.socket = a) : (r = new Ml.BufferedDuplex(e, c, a), f ? a.addEventListener("open", d) : a.onopen = d), f ? (a.addEventListener("close", w), a.addEventListener("error", y), a.addEventListener("message", P)) : (a.onclose = w, a.onerror = y, a.onmessage = P);
    function h2(m, v, R) {
      let T = new tT.Transform({ objectMode: m.objectMode });
      return T._write = v, T._flush = R, T;
    }
    function d() {
      yr("WebSocket onOpen"), r instanceof Ml.BufferedDuplex && r.socketReady();
    }
    function w(m) {
      yr("WebSocket onClose", m), r.end(), r.destroy();
    }
    function y(m) {
      yr("WebSocket onError", m);
      let v = new Error("WebSocket error");
      v.event = m, r.destroy(v);
    }
    async function P(m) {
      if (!c || !c.readable || !c.writable) return;
      let { data: v } = m;
      v instanceof ArrayBuffer ? v = Yo.Buffer.from(v) : v instanceof Blob ? v = Yo.Buffer.from(await new Response(v).arrayBuffer()) : v = Yo.Buffer.from(v, "utf8"), c.push(v);
    }
    function E(m, v, R) {
      if (a.bufferedAmount > i) {
        setTimeout(E, s, m, v, R);
        return;
      }
      o && typeof m == "string" && (m = Yo.Buffer.from(m, "utf8"));
      try {
        a.send(m);
      } catch (T) {
        return R(T);
      }
      R();
    }
    function b(m) {
      a.close(), m();
    }
    return r;
  };
  gr.browserStreamBuilder = uT;
});
var ql = {};
Kr(ql, { Server: () => Je, Socket: () => Je, Stream: () => Je, _createServerHandle: () => Je, _normalizeArgs: () => Je, _setSimultaneousAccepts: () => Je, connect: () => Je, createConnection: () => Je, createServer: () => Je, default: () => lT, isIP: () => Je, isIPv4: () => Je, isIPv6: () => Je });
function Je() {
  throw new Error("Node.js net module is not supported by JSPM core outside of Node.js");
}
var lT, Ll = ze(() => {
  A();
  x();
  I();
  lT = { _createServerHandle: Je, _normalizeArgs: Je, _setSimultaneousAccepts: Je, connect: Je, createConnection: Je, createServer: Je, isIP: Je, isIPv4: Je, isIPv6: Je, Server: Je, Socket: Je, Stream: Je };
});
var Ul = D((lH, Bb) => {
  A();
  x();
  I();
  Bb.exports = {};
});
var jl = D((vi) => {
  A();
  x();
  I();
  var Dl = vi && vi.__importDefault || function(t) {
    return t && t.__esModule ? t : { default: t };
  };
  Object.defineProperty(vi, "__esModule", { value: true });
  var cT = Dl((Ll(), me(ql))), fT = Dl(At()), hT = Dl(Ul()), dT = (0, fT.default)("mqttjs:tcp"), pT = (t, e) => {
    if (e.port = e.port || 1883, e.hostname = e.hostname || e.host || "localhost", e.socksProxy) return (0, hT.default)(e.hostname, e.port, e.socksProxy, { timeout: e.socksTimeout });
    let { port: r, path: n } = e, i = e.hostname;
    return dT("port %d and host %s", r, i), cT.default.createConnection({ port: r, host: i, path: n });
  };
  vi.default = pT;
});
var Mb = {};
Kr(Mb, { default: () => yT });
var yT, Nb = ze(() => {
  A();
  x();
  I();
  yT = {};
});
var Wl = D((Si) => {
  A();
  x();
  I();
  var Fl = Si && Si.__importDefault || function(t) {
    return t && t.__esModule ? t : { default: t };
  };
  Object.defineProperty(Si, "__esModule", { value: true });
  var qb = (Nb(), me(Mb)), gT = Fl((Ll(), me(ql))), bT = Fl(At()), wT = Fl(Ul()), mT = (0, bT.default)("mqttjs:tls");
  function _T(t) {
    let { host: e, port: r, socksProxy: n, ...i } = t;
    if (n !== void 0) {
      let s = (0, wT.default)(e, r, n, { timeout: t.socksTimeout });
      return (0, qb.connect)({ ...i, socket: s });
    }
    return (0, qb.connect)(t);
  }
  var vT = (t, e) => {
    e.port = e.port || 8883, e.host = e.hostname || e.host || "localhost", gT.default.isIP(e.host) === 0 && (e.servername = e.host), e.rejectUnauthorized = e.rejectUnauthorized !== false, delete e.path, mT("port %d host %s rejectUnauthorized %b", e.port, e.host, e.rejectUnauthorized);
    let r = _T(e);
    r.on("secureConnect", () => {
      e.rejectUnauthorized && !r.authorized ? r.emit("error", new Error("TLS not authorized")) : r.removeListener("error", n);
    });
    function n(i) {
      e.rejectUnauthorized && t.emit("error", i), r.end();
    }
    return r.on("error", n), r;
  };
  Si.default = vT;
});
var Vl = D((Hl) => {
  A();
  x();
  I();
  Object.defineProperty(Hl, "__esModule", { value: true });
  var Lb = (ke(), me(qe)), ST = or(), ET = Qo(), kt, $l, Xe;
  function AT() {
    let t = new ST.Transform();
    return t._write = (e, r, n) => {
      kt.send({ data: e.buffer, success() {
        n();
      }, fail(i) {
        n(new Error(i));
      } });
    }, t._flush = (e) => {
      kt.close({ success() {
        e();
      } });
    }, t;
  }
  function IT(t) {
    t.hostname || (t.hostname = "localhost"), t.path || (t.path = "/"), t.wsOptions || (t.wsOptions = {});
  }
  function xT(t, e) {
    let r = t.protocol === "wxs" ? "wss" : "ws", n = `${r}://${t.hostname}${t.path}`;
    return t.port && t.port !== 80 && t.port !== 443 && (n = `${r}://${t.hostname}:${t.port}${t.path}`), typeof t.transformWsUrl == "function" && (n = t.transformWsUrl(n, t, e)), n;
  }
  function TT() {
    kt.onOpen(() => {
      Xe.socketReady();
    }), kt.onMessage((t) => {
      let { data: e } = t;
      e instanceof ArrayBuffer ? e = Lb.Buffer.from(e) : e = Lb.Buffer.from(e, "utf8"), $l.push(e);
    }), kt.onClose(() => {
      Xe.emit("close"), Xe.end(), Xe.destroy();
    }), kt.onError((t) => {
      let e = new Error(t.errMsg);
      Xe.destroy(e);
    });
  }
  var PT = (t, e) => {
    if (e.hostname = e.hostname || e.host, !e.hostname) throw new Error("Could not determine host. Specify host manually.");
    let r = e.protocolId === "MQIsdp" && e.protocolVersion === 3 ? "mqttv3.1" : "mqtt";
    IT(e);
    let n = xT(e, t);
    kt = wx.connectSocket({ url: n, protocols: [r] }), $l = AT(), Xe = new ET.BufferedDuplex(e, $l, kt), Xe._destroy = (s, o) => {
      kt.close({ success() {
        o && o(s);
      } });
    };
    let i = Xe.destroy;
    return Xe.destroy = (s, o) => (Xe.destroy = i, setTimeout(() => {
      kt.close({ fail() {
        Xe._destroy(s, o);
      } });
    }, 0), Xe), TT(), Xe;
  };
  Hl.default = PT;
});
var Kl = D((zl) => {
  A();
  x();
  I();
  Object.defineProperty(zl, "__esModule", { value: true });
  var Gl = (ke(), me(qe)), OT = or(), RT = Qo(), Kt, Ei, An, Ub = false;
  function kT() {
    let t = new OT.Transform();
    return t._write = (e, r, n) => {
      Kt.sendSocketMessage({ data: e.buffer, success() {
        n();
      }, fail() {
        n(new Error());
      } });
    }, t._flush = (e) => {
      Kt.closeSocket({ success() {
        e();
      } });
    }, t;
  }
  function CT(t) {
    t.hostname || (t.hostname = "localhost"), t.path || (t.path = "/"), t.wsOptions || (t.wsOptions = {});
  }
  function BT(t, e) {
    let r = t.protocol === "alis" ? "wss" : "ws", n = `${r}://${t.hostname}${t.path}`;
    return t.port && t.port !== 80 && t.port !== 443 && (n = `${r}://${t.hostname}:${t.port}${t.path}`), typeof t.transformWsUrl == "function" && (n = t.transformWsUrl(n, t, e)), n;
  }
  function MT() {
    Ub || (Ub = true, Kt.onSocketOpen(() => {
      An.socketReady();
    }), Kt.onSocketMessage((t) => {
      if (typeof t.data == "string") {
        let e = Gl.Buffer.from(t.data, "base64");
        Ei.push(e);
      } else {
        let e = new FileReader();
        e.addEventListener("load", () => {
          if (e.result instanceof ArrayBuffer) {
            Ei.push(Gl.Buffer.from(e.result));
            return;
          }
          Ei.push(Gl.Buffer.from(e.result, "utf-8"));
        }), e.readAsArrayBuffer(t.data);
      }
    }), Kt.onSocketClose(() => {
      An.end(), An.destroy();
    }), Kt.onSocketError((t) => {
      An.destroy(t);
    }));
  }
  var NT = (t, e) => {
    if (e.hostname = e.hostname || e.host, !e.hostname) throw new Error("Could not determine host. Specify host manually.");
    let r = e.protocolId === "MQIsdp" && e.protocolVersion === 3 ? "mqttv3.1" : "mqtt";
    CT(e);
    let n = BT(e, t);
    return Kt = e.my, Kt.connectSocket({ url: n, protocols: r }), Ei = kT(), An = new RT.BufferedDuplex(e, Ei, Kt), MT(), An;
  };
  zl.default = NT;
});
var Wb = D((In) => {
  A();
  x();
  I();
  var Jo = In && In.__importDefault || function(t) {
    return t && t.__esModule ? t : { default: t };
  };
  Object.defineProperty(In, "__esModule", { value: true });
  In.connectAsync = jT;
  var qT = Jo(At()), LT = Jo((Ib(), me(Ab))), UT = Jo(zo()), Db = Jo(yi());
  typeof M?.nextTick != "function" && (M.nextTick = setImmediate);
  var jb = (0, qT.default)("mqttjs"), Me = null;
  function DT(t) {
    let e;
    if (t.auth) if (e = t.auth.match(/^(.+):(.+)$/), e) {
      let [, r, n] = e;
      t.username = r, t.password = n;
    } else t.username = t.auth;
  }
  function Fb(t, e) {
    if (jb("connecting to an MQTT broker..."), typeof t == "object" && !e && (e = t, t = ""), e = e || {}, t && typeof t == "string") {
      let i = LT.default.parse(t, true), s = {};
      if (i.port != null && (s.port = Number(i.port)), s.host = i.hostname, s.query = i.query, s.auth = i.auth, s.protocol = i.protocol, s.path = i.path, e = { ...s, ...e }, !e.protocol) throw new Error("Missing protocol");
      e.protocol = e.protocol.replace(/:$/, "");
    }
    if (e.unixSocket = e.unixSocket || e.protocol?.includes("+unix"), e.unixSocket ? e.protocol = e.protocol.replace("+unix", "") : !e.protocol?.startsWith("ws") && !e.protocol?.startsWith("wx") && delete e.path, DT(e), e.query && typeof e.query.clientId == "string" && (e.clientId = e.query.clientId), Db.default || e.unixSocket ? e.socksProxy = void 0 : e.socksProxy === void 0 && typeof M < "u" && (e.socksProxy = M.env.MQTTJS_SOCKS_PROXY), e.cert && e.key) if (e.protocol) {
      if (["mqtts", "wss", "wxs", "alis"].indexOf(e.protocol) === -1) switch (e.protocol) {
        case "mqtt":
          e.protocol = "mqtts";
          break;
        case "ws":
          e.protocol = "wss";
          break;
        case "wx":
          e.protocol = "wxs";
          break;
        case "ali":
          e.protocol = "alis";
          break;
        default:
          throw new Error(`Unknown protocol for secure connection: "${e.protocol}"!`);
      }
    } else throw new Error("Missing secure protocol key");
    if (Me || (Me = {}, !Db.default && !e.forceNativeWebSocket ? (Me.ws = _i().streamBuilder, Me.wss = _i().streamBuilder, Me.mqtt = jl().default, Me.tcp = jl().default, Me.ssl = Wl().default, Me.tls = Me.ssl, Me.mqtts = Wl().default) : (Me.ws = _i().browserStreamBuilder, Me.wss = _i().browserStreamBuilder, Me.wx = Vl().default, Me.wxs = Vl().default, Me.ali = Kl().default, Me.alis = Kl().default)), !Me[e.protocol]) {
      let i = ["mqtts", "wss"].indexOf(e.protocol) !== -1;
      e.protocol = ["mqtt", "mqtts", "ws", "wss", "wx", "wxs", "ali", "alis"].filter((s, o) => i && o % 2 === 0 ? false : typeof Me[s] == "function")[0];
    }
    if (e.clean === false && !e.clientId) throw new Error("Missing clientId for unclean clients");
    e.protocol && (e.defaultProtocol = e.protocol);
    function r(i) {
      return e.servers && ((!i._reconnectCount || i._reconnectCount === e.servers.length) && (i._reconnectCount = 0), e.host = e.servers[i._reconnectCount].host, e.port = e.servers[i._reconnectCount].port, e.protocol = e.servers[i._reconnectCount].protocol ? e.servers[i._reconnectCount].protocol : e.defaultProtocol, e.hostname = e.host, i._reconnectCount++), jb("calling streambuilder for", e.protocol), Me[e.protocol](i, e);
    }
    let n = new UT.default(r, e);
    return n.on("error", () => {
    }), n;
  }
  function jT(t, e, r = true) {
    return new Promise((n, i) => {
      let s = Fb(t, e), o = { connect: (c) => {
        a(), n(s);
      }, end: () => {
        a(), n(s);
      }, error: (c) => {
        a(), s.end(), i(c);
      } };
      r === false && (o.close = () => {
        o.error(new Error("Couldn't connect to server"));
      });
      function a() {
        Object.keys(o).forEach((c) => {
          s.off(c, o[c]);
        });
      }
      Object.keys(o).forEach((c) => {
        s.on(c, o[c]);
      });
    });
  }
  In.default = Fb;
});
var Ql = D((we) => {
  A();
  x();
  I();
  var $b = we && we.__createBinding || (Object.create ? function(t, e, r, n) {
    n === void 0 && (n = r);
    var i = Object.getOwnPropertyDescriptor(e, r);
    (!i || ("get" in i ? !e.__esModule : i.writable || i.configurable)) && (i = { enumerable: true, get: function() {
      return e[r];
    } }), Object.defineProperty(t, n, i);
  } : function(t, e, r, n) {
    n === void 0 && (n = r), t[n] = e[r];
  }), FT = we && we.__setModuleDefault || (Object.create ? function(t, e) {
    Object.defineProperty(t, "default", { enumerable: true, value: e });
  } : function(t, e) {
    t.default = e;
  }), WT = we && we.__importStar || /* @__PURE__ */ (function() {
    var t = function(e) {
      return t = Object.getOwnPropertyNames || function(r) {
        var n = [];
        for (var i in r) Object.prototype.hasOwnProperty.call(r, i) && (n[n.length] = i);
        return n;
      }, t(e);
    };
    return function(e) {
      if (e && e.__esModule) return e;
      var r = {};
      if (e != null) for (var n = t(e), i = 0; i < n.length; i++) n[i] !== "default" && $b(r, e, n[i]);
      return FT(r, e), r;
    };
  })(), Hb = we && we.__exportStar || function(t, e) {
    for (var r in t) r !== "default" && !Object.prototype.hasOwnProperty.call(e, r) && $b(e, t, r);
  }, Ai = we && we.__importDefault || function(t) {
    return t && t.__esModule ? t : { default: t };
  };
  Object.defineProperty(we, "__esModule", { value: true });
  we.ReasonCodes = we.KeepaliveManager = we.UniqueMessageIdProvider = we.DefaultMessageIdProvider = we.Store = we.MqttClient = we.connectAsync = we.connect = we.Client = void 0;
  var Vb = Ai(zo());
  we.MqttClient = Vb.default;
  var $T = Ai(Au());
  we.DefaultMessageIdProvider = $T.default;
  var HT = Ai(Ng());
  we.UniqueMessageIdProvider = HT.default;
  var VT = Ai(La());
  we.Store = VT.default;
  var Gb = WT(Wb());
  we.connect = Gb.default;
  Object.defineProperty(we, "connectAsync", { enumerable: true, get: function() {
    return Gb.connectAsync;
  } });
  var GT = Ai(Du());
  we.KeepaliveManager = GT.default;
  we.Client = Vb.default;
  Hb(zo(), we);
  Hb(Mr(), we);
  var zT = zn();
  Object.defineProperty(we, "ReasonCodes", { enumerable: true, get: function() {
    return zT.ReasonCodes;
  } });
});
var XT = D((ot) => {
  A();
  x();
  I();
  var zb = ot && ot.__createBinding || (Object.create ? function(t, e, r, n) {
    n === void 0 && (n = r);
    var i = Object.getOwnPropertyDescriptor(e, r);
    (!i || ("get" in i ? !e.__esModule : i.writable || i.configurable)) && (i = { enumerable: true, get: function() {
      return e[r];
    } }), Object.defineProperty(t, n, i);
  } : function(t, e, r, n) {
    n === void 0 && (n = r), t[n] = e[r];
  }), KT = ot && ot.__setModuleDefault || (Object.create ? function(t, e) {
    Object.defineProperty(t, "default", { enumerable: true, value: e });
  } : function(t, e) {
    t.default = e;
  }), QT = ot && ot.__importStar || /* @__PURE__ */ (function() {
    var t = function(e) {
      return t = Object.getOwnPropertyNames || function(r) {
        var n = [];
        for (var i in r) Object.prototype.hasOwnProperty.call(r, i) && (n[n.length] = i);
        return n;
      }, t(e);
    };
    return function(e) {
      if (e && e.__esModule) return e;
      var r = {};
      if (e != null) for (var n = t(e), i = 0; i < n.length; i++) n[i] !== "default" && zb(r, e, n[i]);
      return KT(r, e), r;
    };
  })(), YT = ot && ot.__exportStar || function(t, e) {
    for (var r in t) r !== "default" && !Object.prototype.hasOwnProperty.call(e, r) && zb(e, t, r);
  };
  Object.defineProperty(ot, "__esModule", { value: true });
  var JT = QT(Ql());
  ot.default = JT;
  YT(Ql(), ot);
});
const mqtt = XT();
const _sfc_main$2 = {
  __name: "LiveMode",
  props: {
    effects: Array
  },
  setup(__props, { expose: __expose }) {
    __expose();
    const props = __props;
    const client = ref(null);
    const connected = ref(false);
    const status = ref("Déconnecté");
    const search = ref("");
    const currentColor = ref("#FFFFFF");
    const currentEffect = ref("none");
    const connectMQTT = () => {
      client.value = mqtt.connect("ws://skankyhome.local:8083/mqtt", {
        clientId: "web_client_" + Math.random().toString(16).substr(2, 8),
        clean: true,
        reconnectPeriod: 1e3
      });
      client.value.on("connect", () => {
        console.log("✅ Connected to MQTT");
        connected.value = true;
        status.value = "Connecté";
        client.value.subscribe("skankyhome/cmd/led_master");
      });
      client.value.on("error", (err) => {
        console.error("❌ MQTT Error:", err);
        status.value = "Erreur: " + err.message;
      });
      client.value.on("message", (topic, message) => {
        console.log("📨 Received:", topic, message.toString());
        if (topic === "skankyhome/message/led_master") {
          const data = JSON.parse(message.toString());
          console.log("Status:", data);
        }
      });
      client.value.on("close", () => {
        connected.value = false;
        status.value = "Déconnecté";
      });
    };
    const sendCommand = (command, value) => {
      if (!connected.value) {
        alert("MQTT non connecté !");
        return;
      }
      const payload = { [command]: value };
      console.log("📤 Sending:", payload);
      client.value.publish(
        "skankyhome/cmd/led_master",
        JSON.stringify(payload),
        { qos: 1 }
      );
    };
    const changeColor = (color) => {
      sendCommand("color", color);
    };
    const changeEffect = (effect2) => {
      sendCommand("effect", effect2);
    };
    const matchSearch = (name2) => {
      if (search.value.length == 0) {
        return true;
      }
      const escapedSearch = search.value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
      const regex = new RegExp(escapedSearch, "i");
      return regex.test(name2);
    };
    onMounted(() => {
      connectMQTT();
    });
    onBeforeUnmount(() => {
      if (client.value) {
        client.value.end();
      }
    });
    const __returned__ = { props, client, connected, status, search, currentColor, currentEffect, connectMQTT, sendCommand, changeColor, changeEffect, matchSearch, ref, onMounted, onBeforeUnmount, get mqtt() {
      return mqtt;
    }, ColorPicker };
    Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
    return __returned__;
  }
};
const _hoisted_1$2 = { class: "grid-layout" };
const _hoisted_2$2 = { class: "grid-half card" };
const _hoisted_3$2 = { class: "card-body" };
const _hoisted_4$2 = { class: "form-group" };
const _hoisted_5$2 = { class: "grid-half card" };
const _hoisted_6$2 = { class: "form-group ph-m" };
const _hoisted_7$1 = { class: "effects-list pb-l" };
const _hoisted_8$1 = { class: "scrollable" };
const _hoisted_9$1 = ["onClick"];
const _hoisted_10$1 = { class: "effect-key" };
const _hoisted_11$1 = { class: "effect-name" };
function _sfc_render$2(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", _hoisted_1$2, [
    createBaseVNode("div", _hoisted_2$2, [
      _cache[3] || (_cache[3] = createBaseVNode("div", { class: "card-header" }, [
        createBaseVNode("h2", { class: "corner-accent-primary" }, "Color")
      ], -1)),
      createBaseVNode("div", _hoisted_3$2, [
        createBaseVNode("div", _hoisted_4$2, [
          _cache[2] || (_cache[2] = createBaseVNode("label", {
            for: "Color",
            class: "form-label"
          }, "Couleur", -1)),
          createVNode($setup["ColorPicker"], {
            modelValue: $setup.currentColor,
            "onUpdate:modelValue": [
              _cache[0] || (_cache[0] = ($event) => $setup.currentColor = $event),
              $setup.changeColor
            ]
          }, null, 8, ["modelValue"])
        ])
      ])
    ]),
    createBaseVNode("div", _hoisted_5$2, [
      _cache[6] || (_cache[6] = createBaseVNode("div", { class: "card-header" }, [
        createBaseVNode("h2", { class: "corner-accent-primary" }, "Effect")
      ], -1)),
      createBaseVNode("div", null, [
        createBaseVNode("div", _hoisted_6$2, [
          _cache[4] || (_cache[4] = createBaseVNode("label", {
            for: "",
            class: "form-label"
          }, null, -1)),
          withDirectives(createBaseVNode("input", {
            type: "text",
            class: "form-input",
            "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.search = $event)
          }, null, 512), [
            [vModelText, $setup.search]
          ])
        ]),
        createBaseVNode("div", _hoisted_7$1, [
          createBaseVNode("div", _hoisted_8$1, [
            (openBlock(true), createElementBlock(Fragment, null, renderList($setup.props.effects, (name2, key) => {
              return openBlock(), createElementBlock(Fragment, null, [
                $setup.matchSearch(name2) ? (openBlock(), createElementBlock("div", {
                  key: 0,
                  class: "effect",
                  onClick: ($event) => $setup.changeEffect(key)
                }, [
                  createBaseVNode("div", _hoisted_10$1, toDisplayString(key), 1),
                  _cache[5] || (_cache[5] = createBaseVNode("div", { class: "color-cyan" }, " => ", -1)),
                  createBaseVNode("div", _hoisted_11$1, toDisplayString(name2), 1)
                ], 8, _hoisted_9$1)) : createCommentVNode("", true)
              ], 64);
            }), 256))
          ])
        ])
      ])
    ])
  ]);
}
const LiveMode = /* @__PURE__ */ _export_sfc(_sfc_main$2, [["render", _sfc_render$2], ["__file", "LiveMode.vue"]]);
const _sfc_main$1 = {
  __name: "QuickSelectColor",
  props: {
    modelValue: {
      type: String,
      default: ""
    },
    list: {
      type: Array,
      default: []
    }
  },
  emits: ["update:modelValue"],
  setup(__props, { expose: __expose, emit: __emit }) {
    __expose();
    const props = __props;
    const isOpen = ref(false);
    const emit2 = __emit;
    const internalValue = ref(props.modelValue);
    const selectedItem = computed({
      get: () => internalValue.value,
      set: (newValue) => {
        internalValue.value = newValue;
        emit2("update:modelValue", newValue);
      }
    });
    const selectItem = (color) => {
      selectedItem.value = color;
      closeDropdown();
    };
    const openDropdown = () => {
      isOpen.value = true;
    };
    const closeDropdown = () => {
      isOpen.value = false;
    };
    const handleClickOutside = (event) => {
      if (!event.target.closest(".quick-select-color")) {
        closeDropdown();
      }
    };
    onMounted(() => {
      document.addEventListener("click", handleClickOutside);
    });
    onBeforeUnmount(() => {
      document.removeEventListener("click", handleClickOutside);
    });
    const __returned__ = { props, isOpen, emit: emit2, internalValue, selectedItem, selectItem, openDropdown, closeDropdown, handleClickOutside, ref, computed, onMounted, onBeforeUnmount };
    Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
    return __returned__;
  }
};
const _hoisted_1$1 = { class: "quick-select-color" };
const _hoisted_2$1 = { class: "picker-preview-icon" };
const _hoisted_3$1 = {
  key: 0,
  class: "picker-icon-dropdown"
};
const _hoisted_4$1 = { class: "picker-icon-grid" };
const _hoisted_5$1 = ["onClick"];
function _sfc_render$1(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", _hoisted_1$1, [
    createBaseVNode("div", _hoisted_2$1, [
      createBaseVNode("div", {
        class: "picker-icon-item",
        onClick: $setup.openDropdown
      }, [
        createBaseVNode("div", {
          class: "pref-color",
          style: normalizeStyle({ "background-color": $setup.internalValue })
        }, null, 4)
      ]),
      $setup.isOpen ? (openBlock(), createElementBlock("div", _hoisted_3$1, [
        createBaseVNode("div", _hoisted_4$1, [
          (openBlock(true), createElementBlock(Fragment, null, renderList($props.list, (prefColor, key) => {
            return openBlock(), createElementBlock("div", {
              class: "pref-color",
              style: normalizeStyle({ "background-color": prefColor }),
              onClick: ($event) => $setup.selectItem(prefColor)
            }, null, 12, _hoisted_5$1);
          }), 256))
        ])
      ])) : createCommentVNode("", true)
    ])
  ]);
}
const QuickSelectColor = /* @__PURE__ */ _export_sfc(_sfc_main$1, [["render", _sfc_render$1], ["__file", "QuickSelectColor.vue"]]);
class ModuleBridge extends EventTarget {
  constructor(module) {
    super();
    this.module = module;
    this.client = null;
    this.connected = false;
    this.brokerUrl = "ws://skankyhome.local:8083/mqtt";
    this.clientId = `web_${module.name}_${this._randomId()}`;
    this.connect();
  }
  /**
   * Connexion au broker MQTT
   */
  connect() {
    this.client = mqtt.connect(this.brokerUrl, {
      clientId: this.clientId,
      clean: true,
      reconnectPeriod: 1e3
    });
    this.client.on("connect", () => {
      console.log(`✅ ModuleBridge connected: ${this.module.name}`);
      this.connected = true;
      this.client.subscribe(this.module.topic_message);
      this.dispatchEvent(new CustomEvent("connected", {
        detail: { module: this.module }
      }));
    });
    this.client.on("error", (err) => {
      console.error(`❌ ModuleBridge error: ${err.message}`);
      this.connected = false;
      this.dispatchEvent(new CustomEvent("error", {
        detail: { error: err.message }
      }));
    });
    this.client.on("message", (topic, message) => {
      if (topic === this.module.topic_message) {
        try {
          const data = JSON.parse(message.toString());
          this.dispatchEvent(new CustomEvent("module-message", {
            detail: data
          }));
        } catch (err) {
          console.error("Failed to parse MQTT message:", err);
        }
      }
    });
    this.client.on("close", () => {
      console.log(`🔌 ModuleBridge disconnected: ${this.module.name}`);
      this.connected = false;
      this.dispatchEvent(new CustomEvent("disconnected"));
    });
  }
  /**
   * Envoyer une commande à l'ESP32
   * @param {Object} payload - Commande à envoyer
   */
  send(payload) {
    if (!this.isConnected()) {
      console.warn("ModuleBridge: Not connected, cannot send");
      return false;
    }
    this.client.publish(
      this.module.topic_cmd,
      JSON.stringify(payload),
      { qos: 1 }
    );
    console.log(`📤 Sent to ${this.module.name}:`, payload);
    return true;
  }
  /**
   * Vérifier si connecté
   * @returns {boolean}
   */
  isConnected() {
    return this.connected;
  }
  /**
   * Déconnexion propre
   */
  disconnect() {
    if (this.client) {
      this.client.end();
      this.connected = false;
    }
  }
  /**
   * Générer un ID aléatoire
   * @private
   */
  _randomId() {
    return Math.random().toString(16).substr(2, 8);
  }
  /**
   * Raccourci pour addEventListener
   */
  on(event, callback) {
    this.addEventListener(event, (e) => callback(e.detail));
  }
  /**
   * Raccourci pour removeEventListener
   */
  off(event, callback) {
    this.removeEventListener(event, callback);
  }
}
const _sfc_main = {
  __name: "ScenarioMaker",
  props: {
    module: Object,
    scenarioOrigin: Object,
    icons: Array,
    effects: Array,
    links: Object
  },
  setup(__props, { expose: __expose }) {
    __expose();
    const props = __props;
    let bridge = null;
    const currentColor = ref("#0000FF");
    const searchEffect = ref("");
    const scenario = ref(props.scenarioOrigin);
    const isConnected = ref("error");
    const result = ref({
      status: "",
      message: "",
      url: "",
      isNew: false,
      display: false
    });
    const dragState = ref({
      isDragging: false,
      cursorIndex: null,
      lineIndex: null,
      stepIndex: null
    });
    const colorAddPref = () => {
      if (!scenario.value.preference.colors.includes(currentColor.value)) {
        scenario.value.preference.colors.push(currentColor.value);
      }
    };
    const effectAddPref = (key) => {
      if (!scenario.value.preference.effects.includes(key)) {
        scenario.value.preference.effects.push(key);
      }
    };
    const removePrefColor = (key, event) => {
      event.stopPropagation();
      scenario.value.preference.colors.splice(key, 1);
    };
    const removePrefEffect = (key, event) => {
      event.stopPropagation();
      scenario.value.preference.effects.splice(key, 1);
    };
    const matchSearch = (name2) => {
      if (searchEffect.value.length == 0) {
        return true;
      }
      const escapedSearch = searchEffect.value.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
      const regex = new RegExp(escapedSearch, "i");
      return regex.test(name2);
    };
    const setColor = (color) => {
      console.log("setColor", color);
    };
    const setEffect = (effect2) => {
      console.log("setEffect", effect2);
    };
    const save = async () => {
      let body = await fetch(props.links.save, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "Accept": "application/json"
        },
        body: JSON.stringify(scenario.value)
      }).then((response) => {
        return response.json();
      });
      scenario.value = structuredClone(body.scenario);
      result.value = structuredClone(body.result);
      if (result.value.isNew) {
        window.history.pushState({}, null, result.value.url);
      }
      setTimeout(() => {
        result.value.display = false;
      }, 2e3);
    };
    const addStep = (lineIndex) => {
      scenario.value.lines[lineIndex].steps.push({
        duration: 5,
        cursors: [0, props.module.nb_led - 1],
        segments: [{
          first: 0,
          last: props.module.nb_led - 1,
          effect: 0,
          colors: ["", "", ""],
          speed: 1e3,
          reverse: false
        }]
      });
    };
    const removeStep = (lineIndex, stepIndex) => {
      scenario.value.lines[lineIndex].steps.splice(stepIndex, 1);
    };
    const addCursor = (lineIndex, stepIndex, position) => {
      let cursors = scenario.value.lines[lineIndex].steps[stepIndex].cursors;
      if (cursors.length >= 5) {
        return;
      }
      if (!cursors.includes(position)) {
        cursors.push(position);
        cursors.sort((a, b) => a - b);
      }
      scenario.value.lines[lineIndex].steps[stepIndex].cursors = cursors;
      splitSegment(lineIndex, stepIndex);
    };
    const removeCursor = (lineIndex, stepIndex, cursorIndex) => {
      const cursors = scenario.value.lines[lineIndex].steps[stepIndex].cursors;
      if (cursorIndex === 0 || cursorIndex === cursors.length - 1) {
        return;
      }
      cursors.splice(cursorIndex, 1);
      scenario.value.lines[lineIndex].steps[stepIndex].cursors = cursors;
      splitSegment(lineIndex, stepIndex);
    };
    const splitSegment = (lineIndex, stepIndex) => {
      let cursors = scenario.value.lines[lineIndex].steps[stepIndex].cursors;
      let segments = scenario.value.lines[lineIndex].steps[stepIndex].segments;
      for (let i = 0; i < cursors.length - 1; i++) {
        if (segments[i] !== void 0) {
          segments[i].first = cursors[i];
          segments[i].last = cursors[i + 1] - 1;
        } else {
          let prev = segments[i - 1] || {};
          segments.push({
            first: cursors[i],
            last: cursors[i + 1] - 1,
            effect: prev?.effect ?? 0,
            colors: prev?.colors ?? ["", "", ""],
            speed: prev?.speed ?? 1e3,
            reverse: prev?.reverse ?? false
          });
        }
      }
      segments[segments.length - 1].last = props.module.nb_led - 1;
      scenario.value.lines[lineIndex].steps[stepIndex].segments = segments;
    };
    const startDrag = (lineIndex, stepIndex, cursorIndex, event) => {
      const cursors = scenario.value.lines[lineIndex].steps[stepIndex].cursors;
      if (cursorIndex === 0 || cursorIndex === cursors.length - 1) {
        return;
      }
      dragState.value = {
        isDragging: true,
        cursorIndex,
        lineIndex,
        stepIndex
      };
      event.preventDefault();
    };
    const onDrag = (lineIndex, stepIndex, ledPosition, event) => {
      if (!dragState.value.isDragging) return;
      if (dragState.value.lineIndex !== lineIndex) return;
      if (dragState.value.stepIndex !== stepIndex) return;
      const cursors = scenario.value.lines[lineIndex].steps[stepIndex].cursors;
      const cursorIndex = dragState.value.cursorIndex;
      const prevCursor = cursors[cursorIndex - 1];
      const nextCursor = cursors[cursorIndex + 1];
      if (ledPosition <= prevCursor || ledPosition >= nextCursor) {
        return;
      }
      cursors[cursorIndex] = ledPosition;
      splitSegment(lineIndex, stepIndex);
    };
    const stopDrag = () => {
      dragState.value.isDragging = false;
    };
    const previewSegments = (segments) => {
      let message = { segments };
      bridge.send(message);
    };
    onMounted(() => {
      bridge = new ModuleBridge(props.module);
      bridge.on("module-message", (data) => {
        console.log("📨 Message reçu:", data);
      });
      bridge.on("connected", () => {
        isConnected.value = "success";
      });
      bridge.on("disconnected", () => {
        isConnected.value = "warning";
      });
      bridge.on("error", () => {
        isConnected.value = "error";
      });
      if (!scenario.value.lines || scenario.value.lines.length === 0) {
        scenario.value.lines = [];
        for (let i = 0; i < props.module.nb_line; i++) {
          scenario.value.lines.push({
            steps: []
          });
          addStep(i);
        }
      }
      window.addEventListener("mouseup", stopDrag);
      window.addEventListener("mouseleave", stopDrag);
    });
    onBeforeUnmount(() => {
      if (bridge) {
        bridge.disconnect();
      }
      window.removeEventListener("mouseup", stopDrag);
      window.removeEventListener("mouseleave", stopDrag);
    });
    const __returned__ = { props, get bridge() {
      return bridge;
    }, set bridge(v) {
      bridge = v;
    }, currentColor, searchEffect, scenario, isConnected, result, dragState, colorAddPref, effectAddPref, removePrefColor, removePrefEffect, matchSearch, setColor, setEffect, save, addStep, removeStep, addCursor, removeCursor, splitSegment, startDrag, onDrag, stopDrag, previewSegments, ref, onMounted, onBeforeUnmount, ColorPicker, IconPicker, QuickSelectColor, get ModuleBridge() {
      return ModuleBridge;
    } };
    Object.defineProperty(__returned__, "__isScriptSetup", { enumerable: false, value: true });
    return __returned__;
  }
};
const _hoisted_1 = { class: "scenario-layout" };
const _hoisted_2 = { class: "scenario-side-bar accordion" };
const _hoisted_3 = { class: "accordion-item card card-primary mb-s" };
const _hoisted_4 = { class: "accordion-content card-body" };
const _hoisted_5 = { class: "text-right" };
const _hoisted_6 = { class: "color-result" };
const _hoisted_7 = { class: "color-text" };
const _hoisted_8 = { class: "card mb-s" };
const _hoisted_9 = { class: "pref-color-wrapper card-body" };
const _hoisted_10 = ["onClick"];
const _hoisted_11 = ["onClick"];
const _hoisted_12 = { class: "accordion-item card card-primary mb-s" };
const _hoisted_13 = { class: "accordion-content" };
const _hoisted_14 = { class: "form-group ph-m" };
const _hoisted_15 = { class: "effects-list pb-l" };
const _hoisted_16 = { class: "scrollable" };
const _hoisted_17 = {
  key: 0,
  class: "effect"
};
const _hoisted_18 = { class: "effect-key" };
const _hoisted_19 = { class: "effect-name" };
const _hoisted_20 = { class: "effect-favorie" };
const _hoisted_21 = ["onClick"];
const _hoisted_22 = { class: "card mb-l" };
const _hoisted_23 = { class: "pref-effect-wrapper card-body" };
const _hoisted_24 = ["onClick"];
const _hoisted_25 = { class: "effect-key" };
const _hoisted_26 = { class: "effect-name" };
const _hoisted_27 = ["onClick"];
const _hoisted_28 = { class: "scenario-content" };
const _hoisted_29 = { class: "card mb-s" };
const _hoisted_30 = { class: "scenario-info card-body form-inline" };
const _hoisted_31 = { class: "form-group" };
const _hoisted_32 = { class: "form-group" };
const _hoisted_33 = {
  class: "accordion-item card card-primary mb-s",
  open: ""
};
const _hoisted_34 = { class: "accordion-header" };
const _hoisted_35 = { class: "rainbow-underline" };
const _hoisted_36 = { class: "accordion-content card-body" };
const _hoisted_37 = { class: "accordion-header" };
const _hoisted_38 = { class: "card-header-action" };
const _hoisted_39 = { class: "form-group" };
const _hoisted_40 = ["onUpdate:modelValue"];
const _hoisted_41 = ["onClick"];
const _hoisted_42 = ["onClick"];
const _hoisted_43 = ["onClick"];
const _hoisted_44 = { class: "led-visualizer" };
const _hoisted_45 = { class: "led-bar-container" };
const _hoisted_46 = ["onClick", "onMouseenter", "title"];
const _hoisted_47 = ["onMousedown", "onClick"];
const _hoisted_48 = { class: "cursor-label" };
const _hoisted_49 = { class: "segment-form" };
const _hoisted_50 = { class: "form-group" };
const _hoisted_51 = { class: "segment-form-leds" };
const _hoisted_52 = ["onUpdate:modelValue"];
const _hoisted_53 = ["onUpdate:modelValue"];
const _hoisted_54 = { class: "form-group" };
const _hoisted_55 = ["onUpdate:modelValue"];
const _hoisted_56 = ["value"];
const _hoisted_57 = { class: "form-group" };
const _hoisted_58 = { class: "segment-form-color" };
const _hoisted_59 = { class: "form-group" };
const _hoisted_60 = ["onUpdate:modelValue"];
const _hoisted_61 = { class: "form-group" };
const _hoisted_62 = ["id", "onUpdate:modelValue"];
const _hoisted_63 = ["for"];
const _hoisted_64 = {
  key: 0,
  class: "scenario-flash"
};
const _hoisted_65 = {
  class: /* @__PURE__ */ normalizeClass("scenario-is-conneced ")
};
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("section", _hoisted_1, [
    createBaseVNode("div", _hoisted_2, [
      createBaseVNode("details", _hoisted_3, [
        _cache[6] || (_cache[6] = createBaseVNode("summary", { class: "accordion-header" }, [
          createBaseVNode("h3", {
            class: "glitch",
            "data-text": "Palette"
          }, "Palette")
        ], -1)),
        createBaseVNode("div", _hoisted_4, [
          createVNode($setup["ColorPicker"], {
            modelValue: $setup.currentColor,
            "onUpdate:modelValue": _cache[0] || (_cache[0] = ($event) => $setup.currentColor = $event)
          }, null, 8, ["modelValue"]),
          createBaseVNode("div", _hoisted_5, [
            createBaseVNode("div", _hoisted_6, [
              createBaseVNode("span", {
                class: "color-preview",
                style: normalizeStyle({ "background-color": $setup.currentColor })
              }, null, 4),
              createBaseVNode("span", _hoisted_7, toDisplayString($setup.currentColor), 1),
              createBaseVNode("span", {
                class: "btn-mini btn-favorie",
                onClick: $setup.colorAddPref
              }, [..._cache[5] || (_cache[5] = [
                createBaseVNode("i", { class: "icon-heart" }, null, -1)
              ])])
            ])
          ])
        ])
      ]),
      createBaseVNode("div", _hoisted_8, [
        _cache[8] || (_cache[8] = createBaseVNode("div", { class: "card-header" }, [
          createBaseVNode("h6", null, "Couleur")
        ], -1)),
        createBaseVNode("div", _hoisted_9, [
          (openBlock(true), createElementBlock(Fragment, null, renderList($setup.scenario.preference.colors, (prefColor, key) => {
            return openBlock(), createElementBlock("div", {
              class: "pref-color",
              style: normalizeStyle({ "background-color": prefColor }),
              onClick: ($event) => $setup.setColor(prefColor)
            }, [
              createBaseVNode("div", {
                class: "pref-color-trash",
                onClick: ($event) => $setup.removePrefColor(key, $event)
              }, [..._cache[7] || (_cache[7] = [
                createBaseVNode("i", { class: "icon-trash" }, null, -1)
              ])], 8, _hoisted_11)
            ], 12, _hoisted_10);
          }), 256))
        ])
      ]),
      createBaseVNode("details", _hoisted_12, [
        _cache[12] || (_cache[12] = createBaseVNode("summary", { class: "accordion-header" }, [
          createBaseVNode("h3", {
            class: "glitch",
            "data-text": "Effect"
          }, "Effect")
        ], -1)),
        createBaseVNode("div", _hoisted_13, [
          createBaseVNode("div", _hoisted_14, [
            _cache[9] || (_cache[9] = createBaseVNode("label", {
              for: "",
              class: "form-label"
            }, null, -1)),
            withDirectives(createBaseVNode("input", {
              type: "text",
              class: "form-input",
              "onUpdate:modelValue": _cache[1] || (_cache[1] = ($event) => $setup.searchEffect = $event)
            }, null, 512), [
              [vModelText, $setup.searchEffect]
            ])
          ]),
          createBaseVNode("div", _hoisted_15, [
            createBaseVNode("div", _hoisted_16, [
              (openBlock(true), createElementBlock(Fragment, null, renderList($setup.props.effects, (name2, key) => {
                return openBlock(), createElementBlock(Fragment, null, [
                  $setup.matchSearch(name2) ? (openBlock(), createElementBlock("div", _hoisted_17, [
                    createBaseVNode("div", _hoisted_18, toDisplayString(key), 1),
                    _cache[11] || (_cache[11] = createBaseVNode("div", { class: "color-cyan" }, " => ", -1)),
                    createBaseVNode("div", _hoisted_19, toDisplayString(name2), 1),
                    createBaseVNode("div", _hoisted_20, [
                      createBaseVNode("span", {
                        class: "btn-mini btn-favorie",
                        onClick: ($event) => $setup.effectAddPref(key)
                      }, [..._cache[10] || (_cache[10] = [
                        createBaseVNode("i", { class: "icon-heart" }, null, -1)
                      ])], 8, _hoisted_21)
                    ])
                  ])) : createCommentVNode("", true)
                ], 64);
              }), 256))
            ])
          ])
        ])
      ]),
      createBaseVNode("div", _hoisted_22, [
        _cache[15] || (_cache[15] = createBaseVNode("div", { class: "card-header" }, [
          createBaseVNode("h6", null, "Effet")
        ], -1)),
        createBaseVNode("div", _hoisted_23, [
          (openBlock(true), createElementBlock(Fragment, null, renderList($setup.scenario.preference.effects, (prefEffect, key) => {
            return openBlock(), createElementBlock("div", {
              class: "pref-effect",
              onClick: ($event) => $setup.setEffect(prefEffect)
            }, [
              createBaseVNode("div", _hoisted_25, toDisplayString(prefEffect), 1),
              _cache[14] || (_cache[14] = createBaseVNode("div", { class: "color-cyan" }, " => ", -1)),
              createBaseVNode("div", _hoisted_26, toDisplayString($setup.props.effects[prefEffect]), 1),
              createBaseVNode("div", {
                class: "effect-trash",
                onClick: ($event) => $setup.removePrefEffect(key, $event)
              }, [..._cache[13] || (_cache[13] = [
                createBaseVNode("i", { class: "icon-trash" }, null, -1)
              ])], 8, _hoisted_27)
            ], 8, _hoisted_24);
          }), 256))
        ])
      ])
    ]),
    createBaseVNode("div", _hoisted_28, [
      createBaseVNode("div", _hoisted_29, [
        _cache[19] || (_cache[19] = createBaseVNode("header", { class: "card-header" }, [
          createBaseVNode("h3", {
            class: "glitch",
            "data-text": "Info"
          }, "Info")
        ], -1)),
        createBaseVNode("div", _hoisted_30, [
          createBaseVNode("div", _hoisted_31, [
            _cache[16] || (_cache[16] = createBaseVNode("label", {
              for: "Name",
              class: "form-label required"
            }, "Icon", -1)),
            createVNode($setup["IconPicker"], {
              modelValue: $setup.scenario.icon,
              "onUpdate:modelValue": _cache[2] || (_cache[2] = ($event) => $setup.scenario.icon = $event),
              icons: $setup.props.icons
            }, null, 8, ["modelValue", "icons"])
          ]),
          createBaseVNode("div", _hoisted_32, [
            _cache[17] || (_cache[17] = createBaseVNode("label", {
              for: "Name",
              class: "form-label required"
            }, "Name", -1)),
            withDirectives(createBaseVNode("input", {
              type: "text",
              id: "Name",
              name: "name",
              required: "",
              class: "form-input",
              "onUpdate:modelValue": _cache[3] || (_cache[3] = ($event) => $setup.scenario.name = $event)
            }, null, 512), [
              [vModelText, $setup.scenario.name]
            ])
          ]),
          createBaseVNode("div", { class: "form-group" }, [
            createBaseVNode("button", {
              type: "button",
              class: "btn-success",
              onClick: $setup.save
            }, [..._cache[18] || (_cache[18] = [
              createBaseVNode("i", { class: "icon-save" }, null, -1),
              createTextVNode(" SAVE", -1)
            ])])
          ])
        ])
      ]),
      (openBlock(true), createElementBlock(Fragment, null, renderList($setup.scenario.lines, (line, keyLine) => {
        return openBlock(), createElementBlock("details", _hoisted_33, [
          createBaseVNode("summary", _hoisted_34, [
            createBaseVNode("div", null, [
              createBaseVNode("h2", _hoisted_35, "Ligne " + toDisplayString(keyLine + 1), 1)
            ])
          ]),
          createBaseVNode("div", _hoisted_36, [
            (openBlock(true), createElementBlock(Fragment, null, renderList(line.steps, (step, keySteps) => {
              return openBlock(), createElementBlock("details", null, [
                createBaseVNode("summary", _hoisted_37, [
                  createBaseVNode("div", null, [
                    createBaseVNode("h3", null, "Step " + toDisplayString(keySteps + 1), 1)
                  ])
                ]),
                createBaseVNode("div", _hoisted_38, [
                  createBaseVNode("div", _hoisted_39, [
                    _cache[20] || (_cache[20] = createBaseVNode("label", {
                      for: "Speed",
                      class: "form-label"
                    }, "Durée (sec)", -1)),
                    withDirectives(createBaseVNode("input", {
                      type: "number",
                      name: "first",
                      class: "form-input",
                      "onUpdate:modelValue": ($event) => step.duration = $event
                    }, null, 8, _hoisted_40), [
                      [vModelText, step.duration]
                    ])
                  ]),
                  createBaseVNode("div", null, [
                    createBaseVNode("div", {
                      class: "btn-mini btn-info mh-s",
                      onClick: ($event) => $setup.addStep(keyLine)
                    }, [..._cache[21] || (_cache[21] = [
                      createBaseVNode("i", { class: "icon-add" }, null, -1)
                    ])], 8, _hoisted_41),
                    createBaseVNode("div", {
                      class: "btn-mini btn-success mh-s",
                      onClick: ($event) => $setup.previewSegments(step.segments)
                    }, [..._cache[22] || (_cache[22] = [
                      createBaseVNode("i", { class: "icon-upload" }, null, -1)
                    ])], 8, _hoisted_42),
                    createBaseVNode("div", {
                      class: "btn-mini btn-error mh-s",
                      onClick: ($event) => $setup.removeStep(keyLine, keySteps)
                    }, [..._cache[23] || (_cache[23] = [
                      createBaseVNode("i", { class: "icon-trash" }, null, -1)
                    ])], 8, _hoisted_43)
                  ])
                ]),
                createBaseVNode("div", _hoisted_44, [
                  _cache[24] || (_cache[24] = createBaseVNode("label", null, "line", -1)),
                  createBaseVNode("div", _hoisted_45, [
                    createBaseVNode("div", {
                      class: "led-bar",
                      style: normalizeStyle({ gridTemplateColumns: `repeat(${$setup.props.module.nb_led}, 1fr)` })
                    }, [
                      (openBlock(true), createElementBlock(Fragment, null, renderList($setup.props.module.nb_led, (i) => {
                        return openBlock(), createElementBlock("div", {
                          key: i,
                          class: normalizeClass(["led-point", { "led-cursor-here": step.cursors.includes(i - 1) }]),
                          onClick: ($event) => $setup.addCursor(keyLine, keySteps, i - 1),
                          onMouseenter: ($event) => $setup.onDrag(keyLine, keySteps, i - 1, $event),
                          title: `LED ${i - 1}`
                        }, null, 42, _hoisted_46);
                      }), 128))
                    ], 4),
                    createBaseVNode("div", {
                      class: "cursors-layer",
                      style: normalizeStyle({ gridTemplateColumns: `repeat(${$setup.props.module.nb_led}, 1fr)` })
                    }, [
                      (openBlock(true), createElementBlock(Fragment, null, renderList(step.cursors, (pos, index) => {
                        return openBlock(), createElementBlock("div", {
                          key: "cursor-" + index,
                          class: normalizeClass(["led-cursor", {
                            "cursor-locked": index === 0 || index === step.cursors.length - 1,
                            "cursor-dragging": $setup.dragState.isDragging && $setup.dragState.cursorIndex === index && $setup.dragState.lineIndex === keyLine && $setup.dragState.stepIndex === keySteps
                          }]),
                          style: normalizeStyle({ gridColumn: pos + 1 }),
                          onMousedown: ($event) => $setup.startDrag(keyLine, keySteps, index, $event),
                          onClick: withModifiers(($event) => $setup.removeCursor(keyLine, keySteps, index), ["stop"])
                        }, [
                          createBaseVNode("span", _hoisted_48, toDisplayString(pos), 1)
                        ], 46, _hoisted_47);
                      }), 128))
                    ], 4)
                  ])
                ]),
                (openBlock(true), createElementBlock(Fragment, null, renderList(step.segments, (segment, keySegment) => {
                  return openBlock(), createElementBlock("div", _hoisted_49, [
                    createBaseVNode("div", _hoisted_50, [
                      _cache[25] || (_cache[25] = createBaseVNode("label", {
                        for: "Speed",
                        class: "form-label"
                      }, "Led", -1)),
                      createBaseVNode("div", _hoisted_51, [
                        withDirectives(createBaseVNode("input", {
                          type: "number",
                          name: "first",
                          class: "form-input",
                          disabled: "",
                          "onUpdate:modelValue": ($event) => segment.first = $event
                        }, null, 8, _hoisted_52), [
                          [vModelText, segment.first]
                        ]),
                        withDirectives(createBaseVNode("input", {
                          type: "number",
                          name: "last",
                          class: "form-input",
                          disabled: "",
                          "onUpdate:modelValue": ($event) => segment.last = $event
                        }, null, 8, _hoisted_53), [
                          [vModelText, segment.last]
                        ])
                      ])
                    ]),
                    createBaseVNode("div", _hoisted_54, [
                      _cache[27] || (_cache[27] = createBaseVNode("label", {
                        for: "Effect",
                        class: "form-label"
                      }, "Effect", -1)),
                      withDirectives(createBaseVNode("select", {
                        name: "effect",
                        id: "",
                        "onUpdate:modelValue": ($event) => segment.effect = $event
                      }, [
                        _cache[26] || (_cache[26] = createBaseVNode("option", { value: "" }, null, -1)),
                        (openBlock(true), createElementBlock(Fragment, null, renderList($setup.scenario.preference.effects, (prefEffect, keyEffect) => {
                          return openBlock(), createElementBlock("option", { value: prefEffect }, toDisplayString(prefEffect) + " => " + toDisplayString($setup.props.effects[prefEffect]), 9, _hoisted_56);
                        }), 256))
                      ], 8, _hoisted_55), [
                        [vModelSelect, segment.effect]
                      ])
                    ]),
                    createBaseVNode("div", _hoisted_57, [
                      _cache[28] || (_cache[28] = createBaseVNode("label", {
                        for: "Effect",
                        class: "form-label"
                      }, "Color", -1)),
                      createBaseVNode("div", _hoisted_58, [
                        (openBlock(true), createElementBlock(Fragment, null, renderList(segment.colors, (color, keyColor) => {
                          return openBlock(), createBlock($setup["QuickSelectColor"], {
                            modelValue: segment.colors[keyColor],
                            "onUpdate:modelValue": ($event) => segment.colors[keyColor] = $event,
                            list: $setup.scenario.preference.colors
                          }, null, 8, ["modelValue", "onUpdate:modelValue", "list"]);
                        }), 256))
                      ])
                    ]),
                    createBaseVNode("div", _hoisted_59, [
                      _cache[29] || (_cache[29] = createBaseVNode("label", {
                        for: "Speed",
                        class: "form-label"
                      }, "Speed", -1)),
                      withDirectives(createBaseVNode("input", {
                        type: "number",
                        id: "Speed",
                        name: "speed",
                        class: "form-input",
                        "onUpdate:modelValue": ($event) => segment.speed = $event
                      }, null, 8, _hoisted_60), [
                        [vModelText, segment.speed]
                      ])
                    ]),
                    createBaseVNode("div", _hoisted_61, [
                      withDirectives(createBaseVNode("input", {
                        type: "checkbox",
                        id: "reverse" + keyLine + "_" + keySteps + "_" + keySegment,
                        name: "reverse",
                        class: "form-input",
                        "onUpdate:modelValue": ($event) => segment.reverse = $event
                      }, null, 8, _hoisted_62), [
                        [vModelCheckbox, segment.reverse]
                      ]),
                      createBaseVNode("label", {
                        for: "reverse" + keyLine + "_" + keySteps + "_" + keySegment,
                        class: "form-label"
                      }, "reverse", 8, _hoisted_63)
                    ])
                  ]);
                }), 256))
              ]);
            }), 256))
          ])
        ]);
      }), 256))
    ]),
    $setup.result.display ? (openBlock(), createElementBlock("div", _hoisted_64, [
      createBaseVNode("div", {
        class: normalizeClass("flash-message flash-" + $setup.result.status),
        onClick: _cache[4] || (_cache[4] = ($event) => $setup.result.display = false)
      }, [
        createBaseVNode("i", {
          class: normalizeClass("icon-" + $setup.result.status)
        }, null, 2),
        createTextVNode(toDisplayString($setup.result.message), 1)
      ], 2)
    ])) : createCommentVNode("", true),
    createBaseVNode("div", _hoisted_65, [
      createBaseVNode("i", {
        class: normalizeClass("icon-" + $setup.isConnected + " neon-" + $setup.isConnected + "-light")
      }, null, 2)
    ])
  ]);
}
const ScenarioMaker = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render], ["__file", "ScenarioMaker.vue"]]);
window.remove = function(element) {
  if (element && element.parentNode) {
    element.parentNode.removeChild(element);
  }
};
document.addEventListener("DOMContentLoaded", () => {
  document.querySelectorAll(".clickable-row").forEach((row) => {
    row.addEventListener("click", function() {
      console.log(this.dataset.url);
      window.location.href = this.dataset.url;
    });
  });
  let target = document.getElementById("MyLedEditor");
  if (target) {
    var editor = createApp({});
    editor.component("led-editor", LedEditor);
    editor.component("color-picker", ColorPicker);
    editor.mount("#MyLedEditor");
  }
  target = document.getElementById("AppForm");
  if (target) {
    var editor = createApp({});
    editor.component("icon-picker", IconPicker);
    editor.mount("#AppForm");
  }
  target = document.getElementById("LiveMode");
  if (target) {
    var editor = createApp({});
    editor.component("live-mode", LiveMode);
    editor.mount("#LiveMode");
  }
  target = document.getElementById("ScenarioMaker");
  if (target) {
    var scenario = createApp({});
    scenario.component("scenario-maker", ScenarioMaker);
    scenario.mount("#ScenarioMaker");
  }
});
//# sourceMappingURL=app.js.map
